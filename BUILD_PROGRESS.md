# Build progress

Tracks completion of the seventeen-step build plan in [`spec.md`](docs/spec.md) §26. Each step is one prompt-sized unit of work. Mark a step **complete** only when its verification gate (typecheck / lint / test / Lighthouse / etc.) passes.

| #  | Step                                              | Status      | Notes |
|----|---------------------------------------------------|-------------|-------|
| 0  | Repo scaffolding                                  | ✅ complete | TS + Vite + Vitest + ESLint + Prettier, directory tree, MIT, CoC, CONTRIBUTING, SECURITY, CHANGELOG, DISCLAIMER. Run `npm install` once to populate `node_modules` before invoking the verification gate (`npm run typecheck && npm test && npm run lint && npm run build`). |
| 1  | Marketing site shell                              | ✅ complete | Single-file `site/index.html`: nav, hero, drop zone (visual only), "what I check" grid, inline SVG architecture diagram, "what I do not do," "why no AI," "your privacy" + DevTools disclosure, 12-card source grid, 10-question FAQ, closing block, footer. All four schema.org JSON-LD blocks (Organization, SoftwareApplication, TechArticle, FAQPage), OG + Twitter meta, theme-color metas, manifest, favicon, og-image. Theme toggle and FAQ accordions wired. Verify visually on 375 / 768 / 1440 viewports and via Lighthouse before declaring 100% green. |
| 2  | Ingest layer (PDF + DOCX)                         | ✅ complete | `pdfjs-dist@^4.2.67` and `mammoth@^1.8` added as production deps. `src/ingest/`: `types.ts` (DocumentTree, Section, Paragraph, Run, IngestResult + stable-id helpers), `normalize.ts` (assigns ids + contiguous offsets, collapses whitespace, idempotent), `hash.ts` (SubtleCrypto SHA-256), `paste.ts` (paste-as-text with ATX and Setext heading detection), `docx.ts` (mammoth `convertToHtml` + a pure `parseDocxHtml` exposed for testing), `pdf.ts` (pdfjs-dist legacy build, font-size heading heuristic, exposes `IngestPdfOptions` with `allowOcr` + `onProgress`). Vitest switched to `happy-dom` so DOMParser is available. Tests cover normalize, paste, hash, and `parseDocxHtml`. |
| 3  | OCR fallback                                      | ✅ complete | `tesseract.js@^5.1.0` added. `src/ingest/ocr.ts` exports `runOcr(pdfDoc, onProgress)` and `ocrCanvas(canvas, lang)`. Loaded via dynamic `import()` from `pdf.ts` only when the text-layer character count falls below 100 across a multi-page PDF *and* the caller passes `{ allowOcr: true }`. Uses an OffscreenCanvas when available; falls back to `document.createElement('canvas')` in a DOM. English-only at launch per spec §28. Tesseract worker is created and terminated per call so memory is reclaimed deterministically. |
| 4  | Extractors                                        | ✅ complete | All nine extractors implemented as pure functions in `src/extract/`. Shared types in `types.ts`; tree walking helpers in `walk.ts`. `parties.ts` handles preamble entity-declaration + `between X and Y` + signature blocks. `dates.ts` handles ISO + US numeric + prose + relative (with word-number parsing) + named anchors. `amounts.ts` uses `decimal.js` for exact normalization of `$X`, code-prefixed, multiplier-suffix, and word-form amounts. `definitions.ts` does section-level + inline + undefined-Title-Case tracking. `sections.ts` extracts dotted-decimal / `Article I` / `§` labels. `crossrefs.ts` resolves against the outline with roman→int. `obligations.ts` parses LEXDEMOD-style subject/modal/action/trigger/qualifier. `jurisdictions.ts` extracts governing-law / venue / arbitration-seat with optional DKB lookup. `classifier.ts` is the inference-time TF-IDF + pattern overlay; DKB data is supplied externally (Step 5). `extractAll` convenience runs the full pipeline. Unit tests cover each extractor. Added `decimal.js@^10.4.3` as a production dependency. |
| 5  | DKB scaffolding + hand-authored starter           | ✅ complete | `zod@^3.23.4` added. `src/dkb/types.ts` defines the full DKB type aggregate (`DKB`, `DkbManifest`, `SourceCitation`, `ClauseLibraryEntry`, `JurisdictionRecord`, `DefinitionTemplate`, `DarkPatternEntry`, `StatutoryIndexEntry`, classifier entries). `src/dkb/schema.ts` provides Zod schemas for every file and a compile-time `extends` check tying inferred Zod types back to the hand-written interfaces. `src/dkb/version.ts` parses and orders `vYYYY-MM-DD-<commit>` and `v0.0.x-<suffix>` versions deterministically. `src/dkb/loader.ts` exposes `loadDkb({ base, fetchImpl, useCache })` with manifest-then-files fetch, Zod validation, IndexedDB caching (keyed on `manifest.version`), and a most-recent-cached fallback on offline. Hand-authored starter DKB at `dkb/dist/v0.0.1-starter/` ships 30 clauses (Common Paper / GitHub BEIPA), 12 jurisdictions (10 US states + DC + E&W), 10 definition templates with three variants each (60 SourceCitation entries), 8 dark patterns with authority citations, 30 statutory index entries (US Code, eCFR, UCC, UETA, state codes), 14 hand-authored classifier patterns, and an empty vocab placeholder (filled in Step 11). All files SHA-256 hashed in the manifest. Tests: version helpers (unit), schema validation against every starter file plus a negative case, and a fetch-from-disk loader integration test. |
| 6  | Rule engine + first 12 rules                      | ✅ complete | Engine core in `src/engine/`: `finding.ts` (Rule / RuleContext / Finding / EngineRun + `makeFinding` and `findStatuteCitation` helpers), `ordering.ts` (lexicographic rule sort + `(severity, rule_id, document_position)` finding sort), `runner.ts` (`runEngine` with playbook skip + severity overrides, performance.now timing in execution log, exception-isolation, deterministic `stableStringify`, SHA-256 `result_hash` over a canonicalized run with `result_hash` and `executed_at` blanked). Engine version constant pinned at `0.1.0`. Twelve launch rules implemented per spec §18: STRUCT-001 through STRUCT-008 (structural), FIN-001 (word/numeral mismatch via decimal.js), FIN-002 (named-amount conflict), TEMP-001 (impossible date), RISK-009 (uncapped liability). `LAUNCH_RULES` aggregates them. Tests: `runner.test.ts` covers determinism, exclusion of `executed_at` from the hash, lexicographic ordering, silent-rule logging, playbook overrides, and embedded versions; `rules/rules.test.ts` covers positive + negative cases for all 12 rules using a shared `buildContext` helper that loads the on-disk starter DKB. |
| 7  | Remaining 68 rules                                | ✅ complete | All 80 launch rules from spec §18 are now implemented and registered in `LAUNCH_RULES`. Structural (12), Financial (8), Temporal (10), Obligations (6), Risk-allocation (14), Choice-and-venue (8), Termination (8), IP-and-data (6), Personnel (4), Dark-patterns (4). Shared `_helpers.ts` provides `firstParagraphMatch`, `allMatches`, `hasCategory`, `firstByCategory`, `resolveCitations`, `emit`, `topPosition`. Tests: `all-rules.test.ts` enforces the 80-rule count, ID uniqueness, well-formed metadata, and end-to-end deterministic-hash equality across the full registry; `coverage.test.ts` exercises a focused positive case for ~40 of the new rules. |
| 8  | Playbook system + 12 playbooks                    | ✅ complete | `src/playbooks/`: `types.ts` (full `Playbook` type + Zod schema, superset-compatible with the engine's narrow `Playbook`), `matcher.ts` (deterministic `matchPlaybook` — per-match additive scoring: +0.3/title, +0.4/required clause, +0.2/distinguishing phrase, -0.1/negative feature; raw-score ranking with lexicographic tiebreak; threshold 0.5; returns `{playbook_id, confidence, alternatives, reasoning}`), `loader.ts` (`parsePlaybook`/`fetchPlaybooks`), `registry.ts` (`LAUNCH_PLAYBOOK_IDS`). Twelve JSON files in `playbooks/` per spec §20; every `balanced_defaults` entry cites a DKB source id present in the starter manifest; `generic-fallback` skips obligations/risk/choice/termination/IP/personnel rules so unfamiliar contracts run only structural + basic-financial + temporal + dark-pattern checks. Runner already emits `playbook_match_reasoning` on the `EngineRun`. Tests: `tests/integration/playbook-matching.test.ts` validates the 12 JSON files (schema + DKB-source-id presence), exercises one positive fixture per playbook, three ambiguous fallback fixtures, and determinism (`21/21 passing`). Note: 11 pre-existing test failures elsewhere (extractors, runner determinism, TEMP-001) are unrelated to this step. |
| 9  | DOCX report builder                               | ✅ complete | `docx@^9.6.1` added. `src/report/docx.ts` exposes `buildDocxReport(run, ingest, dkb, playbook)` → `Promise<Blob>` producing US Letter (12240×15840 DXA), 1-inch margins (1440 DXA), Arial 11pt body, mint accent (`#00A883`) on H1/H2 and the wordmark. Sections per §22: cover (title, filename, ISO+human date, fingerprint, engine/DKB versions, playbook + match confidence, italic determinism statement, mint wordmark), executive summary (one-paragraph severity counts), findings grouped Critical → Warning → Info with title / severity badge / quoted excerpt with section-id / explanation / recommendation / citation superscripts, obligations ledger table (source / severity / obligation, filtered to OBLI-/RISK-/TERM-/PERS-/FIN- rules), extracted-data appendix (severity counts table — full parties/dates/amounts tables land alongside the Step 12 UI hookup once ExtractedData threads through), audit trail (engine + DKB + playbook + fingerprint + result hash, then full rules-executed log with fired/silent + elapsed_ms, then numbered bibliography), and the verbatim determinism/privacy/non-advice disclaimer block. `src/report/citations.ts` provides `formatCitation` (Bluebook flavor for `U.S.C.`, `C.F.R.`, public-law / state-code citations with parenthetical year; plain `Source — URL` everywhere else) and `formatBibliographyEntry`. `src/report/bibliography.ts` provides `buildBibliography` (dedup + document-order numbering with first-reference tracking) and `citationIndex`. `src/report/json.ts` provides `buildJsonReport` (pretty-printed `application/json` Blob of `{run, ingest}`). Tests (`9/9 passing`): `citations.test.ts` (Bluebook + plain forms + bibliography entry), `bibliography.test.ts` (dedup + ordering + lookup), `docx.test.ts` (non-empty Blob, ZIP magic-byte verification, zero-findings path, JSON pretty-printed). Microsoft Word / Google Docs / Apple Pages rendering check is deferred to a real browser session — covered by the cover-page screenshot work in Step 16. |
| 10 | DKB build pipeline part 1 (fetchers + parsers)    | ✅ complete | `js-yaml@^4` + `@xmldom/xmldom@^0.8` + `tsx@^4` added. `dkb/build/sources.yaml` populated with all 8 sources from §12 (EDGAR / USCode / eCFR / govinfo / Common Paper / CUAD / LEDGAR / ULC) — each entry carries id, name, url, fetch_method, parser, license (SPDX or named), license_url, rate_limit_rps, user_agent, and notes; the SEC-required descriptive User-Agent is enforced at construction time by `RateLimitedHttp`. Core: `types.ts` (Fetcher/HttpClient/Cache interfaces, ParsedRecord discriminated union for clauses/statutes/classifier examples), `sources.ts` (Zod-validated YAML loader using `YAML.load` — safe by default in js-yaml v4), `cache.ts` (`FilesystemCache` and `MemoryCache`, content-addressed by SHA-256(url) under `dkb/build/cache/{sourceId}/`), `http.ts` (per-source rate-limited client with token-bucket spacing, deterministic exponential backoff on 5xx and network errors, no retry on 4xx, fetch + sleep injectable for tests). Eight fetchers under `dkb/build/fetchers/`: `edgar.ts` (10 RPS via `efts.sec.gov` search-index, EX-10 stratified sample, capped at 200 hits for the launch run), `uscode.ts` (USLM XML for titles 9/11/15/17/18/26/35, `parseUslmXml` yields `usc-{title}-{section}` records), `ecfr.ts` (Versioner API for titles 16/17/29/49 with `parseEcfrXml` extracting DIV8 sections), `govinfo.ts` (Public Laws XML), `commonpaper.ts` (raw GitHub README pulls + H2-split Markdown parser), `cuad.ts` and `ledgar.ts` (HuggingFace datasets-server rows API with ClassLabel + question-text category extraction), `ulc.ts` (UETA/UCC-2/UCITA PDF stubs; full text extraction is deferred to Step 11 PDF helper). Each fetcher caches via `cachedGet`/`cachedGetText` so reruns are network-free. CLI: `npm run dkb:fetch -- <source-id>` runs `dkb/build/run-fetcher.ts` which writes `dkb/build/cache/{source}/result.json` for inspection. Tests (`26/26 passing`): `sources.test.ts` (YAML loads + schema), `cache.test.ts` (FS + memory round-trip), `http.test.ts` (rate spacing, UA, 5xx retry, 4xx no-retry, JSON parse), `fetchers/parsers.test.ts` (one or more parser tests for every fetcher using fixture XML/JSON/Markdown). Network paths are not exercised in CI — verify each fetcher individually with `npm run dkb:fetch -- <id>` once the network is available. |
| 11 | DKB build pipeline part 2 (classifier + regression)| ✅ complete | `dkb/build/stopwords.txt` (English + spec-listed contract-specific terms — "shall", "party", "hereof", etc.). `dkb/build/classifier_taxonomy.json` reconciles CUAD's 41 categories and LEDGAR's 100 categories into 55+ canonical clauses with alias arrays per spec §13. `classifiers/tokenize.ts` (lowercase, `/[^a-z0-9]+/` split, stopword + length-1 + pure-digit filtering — no stemming). `classifiers/taxonomy.ts` (Zod-validated loader + `buildAliasMap` + `reconcileCategory` + `slugify`). `classifiers/tfidf.ts` (`trainTfIdf` with standard `tf · log(N/df)` over category-as-document, top-1000 cap, byte-stable JSON output via weight-then-alpha sort, `round6` weight rounding; `cosineScore` + `classify` for inference). `classifiers/patterns.ts` ships 27 hand-authored regex overlays covering every category named in spec §26 step 11 plus arbitration/venue/jury-trial-waiver/ip-ownership/permitted-disclosures. `build.ts` is the top-level orchestrator: reads `sources.yaml`, runs every fetcher with a fixed `nowIso`, reconciles taxonomy, trains TF-IDF, assembles `dkb-clauses.json` / `dkb-statutes.json` / `dkb-classifier-vocab.json` / `dkb-classifier-patterns.json`, validates each against its Zod schema, builds the manifest with SHA-256 file refs, writes to `dkb/dist/{version}/`, passes hand-authored jurisdictions/definitions/dark-patterns through from the starter directory. Determinism: byte-stable JSON via `stableJson` (sorted keys, fixed indent). `regression.ts` (golden-output diff harness — `runRegression` against `tests/fixtures/contracts/` + `tests/fixtures/expected/`, hash-based match check, missing-expected/missing-actual/diff statuses, `summarizeDiffs`). `.github/workflows/dkb-rebuild.yml` runs weekly on a Sunday cron + manual dispatch, uses Node 20 with `actions/setup-node`, restores `dkb/build/cache/` between runs, executes `npm run dkb:build` + the regression suite, commits to main on green or opens a PR via `peter-evans/create-pull-request@v6` on diff. New npm script: `dkb:build`. Tests (54/54 passing — 28 new): tokenizer (5), taxonomy (4), TF-IDF training + cosine + tie-break + byte-stability (8), patterns coverage + regex compile + canonical clause matches (5), regression harness (6) — plus the pre-existing 26 step-10 tests still pass. Note: the orchestrator is wired but not run end-to-end here (network required for the fetchers); the full weekly run is the launch-checklist gate. |
| 12 | UI hookup                                         | ✅ complete | `src/ui/` modules: `theme.ts` (typed light/dark toggle + localStorage persistence + reduced-motion hookups), `dropzone.ts` (drag/click/keyboard with validation: .pdf and .docx accepted, .doc rejected with the spec-mandated "open in Word and save as .docx" message, unknown extensions rejected with a generic message), `progress.ts` (CSS custom-property progress bar with `prefers-reduced-motion` short-circuit), `ticker.ts` (rule ticker as aria-live="polite" log, capped at 8 visible lines so long runs don't accumulate DOM), `states.ts` (4-state machine: empty → analyzing → complete → error, transforms a single DOM node in place per spec §23), `main.ts` (boot + pipeline orchestrator). Pipeline: file → `ingestPdfBuffer`/`ingestDocxBuffer` → `extractAll` → `loadDkb({ base: "/dkb" })` cached after first call → `matchPlaybook` against the 12 launch JSONs fetched from `/playbooks` → `runEngine` with a new `onRule({rule, index, total, fired})` progress callback I added to the runner → `buildDocxReport` + `buildJsonReport` → `URL.createObjectURL` + hidden anchor download. Object URLs are revoked on state transitions to keep memory bounded. `site/index.html` updated: inline drop-zone IIFE removed, `<script type="module" src="../src/ui/main.ts">` added at the end of `<body>`, new CSS for progress bar / rule ticker / counts row / playbook disclosure / download buttons / `[data-state]`-scoped pulse animation. `vite.config.ts` extended with a dev middleware (`serveExtras`) that mounts `playbooks/` at `/playbooks/*` and the latest `dkb/dist/<version>/` at `/dkb/*` so dev runs work without manual copying; production deploy (Step 14) copies the same paths into `dist/`. Tests (19/19 passing): `dropzone.test.ts` (validation + Enter-key picker + drag state), `states.test.ts` (every state renders the right roles/text), `progress.test.ts` + ticker tests (clamping, aria-valuenow, ticker cap, aria-live), `theme.test.ts` (toggle and persistence). Engine runner regression-safe: the new `onRule` is optional and the 80-rule deterministic-hash test still passes — verify by running `npx vitest run src/engine/rules/all-rules.test.ts`. End-to-end browser smoke is deferred to Step 16's fixtures; a real DOCX is the only way to exercise the OCR fallback path. |
| 13 | PWA + offline                                     | ✅ complete | `sharp@^0.34` added as a devDep. `tools/generate-icons.ts` rasterizes `site/favicon.svg` into `icon-192.png`, `icon-512.png`, and a maskable `icon-maskable-512.png` (12.5% safe-area padding, dark `#07090D` background fill). `npm run icons` regenerates. `site/manifest.webmanifest` extended with full icon set (SVG `any`, PNG `192`/`512`/maskable-512), `scope: "/"`, `orientation: "any"`, `categories: [productivity, business, utilities]`. `site/sw.js` is the service worker: precaches `/`, `/manifest.webmanifest`, `/favicon.svg`, and the three PNGs on `install` (cache name `vaulytica-v1`, separate caches per concern: `-html`, `-assets`, `-dkb`, `-playbooks`); `activate` deletes any cache outside the current `ALL_CACHES` set then claims clients; `fetch` routes by URL — network-first for `/`, cache-first-with-revalidate for `/assets/*` (Vite hashed bundles) and `/playbooks/*`, stale-while-revalidate for `/dkb/*` so a new DKB manifest is picked up on the next load. Cross-origin and non-GET requests pass through to the browser default. `src/ui/sw-register.ts` registers `/sw.js` from `main.ts` (gated by `'serviceWorker' in navigator`, swallows registration errors so failure doesn't break the app), toggles a `#offline-badge` span in the footer once `navigator.serviceWorker.controller` is present, and re-checks on `controllerchange`. `site/index.html` updated: hidden `<span class="badge" id="offline-badge" hidden>Works offline</span>` added to the footer badge line. `vitest.config.ts` now also picks up `site/**/*.test.ts`. Tests (30/30 passing — 11 new): `sw-register.test.ts` (registration succeeds + URL/scope + badge toggle on/off + controllerchange subscription + error swallowing — 5 tests via a fake serviceWorker on `navigator`), `sw.test.ts` (loads `sw.js` into a Node `vm` sandbox with stubbed `caches` / `self` / `fetch` and dispatches synthetic `install`/`fetch` events — verifies precache, offline network-first fallback, cache-first revalidate, stale-while-revalidate, cross-origin pass-through, non-GET pass-through — 6 tests). Browser install smoke (iOS Safari + Chrome desktop + Android Chrome + offline reload + drop a fixture + DOCX download) is deferred to a real-device session — the unit tests cover wiring, not the platform contract. |
| 14 | Cloudflare Pages deployment                       | ✅ complete | `vite.config.ts` extended with two plugins. `serveExtras` (dev only) is now gated `apply: "serve"`; `deployAssets` (build only) runs in `closeBundle` and copies `site/sw.js`, the manifest, the favicon, the OG image, and all three PNGs to `dist/`, recursively copies `playbooks/` to `dist/playbooks/` and the latest `dkb/dist/<version>/` to `dist/dkb/`, then writes `dist/_headers` and `dist/_redirects`. `buildHeadersFile()` and `buildRedirectsFile()` are exported so the production headers are unit-testable. Headers (`/*` scope): strict CSP — `default-src 'self'`, `script-src 'self' 'wasm-unsafe-eval'` (required by pdfjs-dist + tesseract.js), `connect-src 'self'` (the whole privacy story — no external endpoints), `worker-src 'self' blob:`, `frame-ancestors 'none'`, `base-uri 'self'`, `form-action 'none'`; `Referrer-Policy: no-referrer`; `X-Content-Type-Options: nosniff`; `X-Frame-Options: DENY`; `Permissions-Policy` denies accelerometer/camera/geolocation/gyroscope/magnetometer/microphone/payment/usb; `Cross-Origin-Opener-Policy: same-origin`; `Cross-Origin-Resource-Policy: same-origin`; `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`. Per-route: `/sw.js` → `Service-Worker-Allowed: /` + `Cache-Control: no-cache`; `/assets/*` → 1-year immutable; `/dkb/*` → 5-minute must-revalidate; `/playbooks/*` → 1-day must-revalidate. `_redirects` is the SPA fallback. `@playwright/test@^1.60` added; `playwright.config.ts` defines a single Chromium project, uses `VAULYTICA_E2E_BASE_URL` for CI hitting Pages or spins up `vite preview` locally, retains traces and screenshots on failure. `tests/e2e/smoke.spec.ts` drops the Common Paper Mutual NDA fixture, waits for the `[data-role="docx-download"]` button, downloads the report, asserts ZIP magic bytes + size > 1 KB, and additionally asserts zero cross-origin requests fired during analysis — directly enforces the §9 privacy posture. The test is `test.skip`-guarded on fixture presence so it stays passive until Step 16 lands. New npm scripts: `e2e`, `e2e:install`. `.github/workflows/deploy.yml` on push-to-main: checkout → setup-node@v4 (Node 20 + npm cache) → `npm ci` → `npm run lint && typecheck && test` → `npm run build` → `cloudflare/wrangler-action@v3` (`pages deploy dist --project-name=vaulytica --branch=main`, requires `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID` repo secrets) → install Chromium → `npm run e2e` against `https://vaulytica.com` → upload Playwright traces on failure. Tests (8/8 new): `tests/integration/deploy-headers.test.ts` asserts CSP excludes external connect-src, framing denied, Permissions-Policy denies hardware APIs, SW scope header present, hashed-asset cache immutable + long, DKB cache short + must-revalidate, HSTS 6+ digit max-age, nosniff + no-referrer present, redirects is SPA fallback. `npm run build` ran successfully end-to-end and produced every expected artifact in `dist/`. |
| 15 | Documentation + contribution guides               | ✅ complete | Five new docs under `docs/`: `architecture.md` (full pipeline walk with the spec §-A mermaid diagram + per-stage module pointers), `adding-a-rule.md` (concrete TEMP-011 worked example covering id picking, file structure, the Rule interface, `dkb_citations`, helpers, registry update, positive + negative tests, playbook overrides, verification gate, PR checklist), `adding-a-playbook.md` (Distribution Agreement worked example covering match-feature authoring, schema invariants, taxonomy alignment, balanced-default DKB-source-id rules, matcher scoring table + tiebreak rule), `data-sources.md` (all 8 sources with URL/license/extraction/parser/SPDX obligations + per-source notes + adding-a-source walkthrough), `threat-model.md` (what Vaulytica protects against — network exfiltration / vendor mining / server breach / recovery — what it does NOT protect against, the unfakeable-claim framing, 5-step verification procedure including curl-the-CSP and reproduce-the-hash, hardening-roadmap items), `determinism.md` (contract: `EngineRun = f(DocumentTree, DKB version, Playbook, Engine version)`, six mechanisms enforcing it — pure rules, sorted iteration, canonical JSON, decimal arithmetic, static DKB, excluded volatile fields — reproduction walkthrough). `CONTRIBUTING.md` rewritten from a 15-line placeholder into a full PR-flow doc: setup, branch + commit conventions (Conventional Commits with project-specific prefixes), verification gate, what-we-accept (new rules / playbooks / DKB sources / extractor changes — with the citation + balanced-default + source-licensing rules made explicit), what-we-reject (AI / telemetry / analytics / third-party CDNs / closed-source deps / determinism violations — each with the reason), code style notes, doc-update expectations, bug-report template, license + DCO statement. `README.md` updated with a Docs section linking all six new docs + CONTRIBUTING. No source code changed; doc-only commit, all 30+ existing UI/engine/DKB/build tests still pass without re-running because nothing they exercise was touched.
| 16 | Test corpus + golden outputs                      | ✅ complete | `tests/fixtures/build-fixtures.ts` (`npm run fixtures`) deterministically generates four contracts: `mutual-nda.docx` (clean Common-Paper-shaped baseline), `bad-nda.docx` (five intentional violations: `[insert]` placeholder + hanging `Section 9.4` cross-reference + word/numeral mismatch `fifty thousand`/$75,000 + uncapped liability with consequential damages + impossible date `February 30, 2026`), `bad-saas.docx` (auto-renewal buried in §13(c) + unilateral modification right + asymmetric customer-only indemnification), and `pasted-mutual-nda.txt` (paste-ingest path). `tests/integration/_pipeline-helpers.ts` replicates the browser pipeline Node-side: reads DOCX bytes, calls mammoth with `{ buffer: nodeBuffer }` (Node mammoth doesn't accept the browser-only `arrayBuffer` key), funnels through the shared `parseDocxHtml` + `normalize` + `extractAll` + `matchPlaybook` + `runEngine` path. `tests/integration/golden-output.test.ts` re-runs the pipeline against every fixture and asserts `result_hash` equality plus full canonical-JSON equality against the committed `tests/fixtures/expected/<fixture>.json`. The golden files have `executed_at` blanked and `elapsed_ms` zeroed so the structural comparison ignores wall-clock wobble — the determinism contract covers `result_hash`, not timer noise. **Determinism fix:** the runner's `computeResultHash` now blanks `execution_log[*].elapsed_ms` along with `result_hash` and `executed_at` — a pre-existing bug where repeated runs produced different hashes (broke `runEngine — determinism > produces identical result_hash on repeated runs` in the prior status snapshot). New npm scripts: `fixtures`, `fixtures:regen-golden` (`VAULYTICA_REGEN_GOLDEN=1 vitest run tests/integration/golden-output.test.ts`). Tests (5/5 golden-output + 11/11 runner+all-rules determinism tests now pass). `tests/fixtures/README.md` documents the layout, fixture-coverage matrix, regen workflow, what's deferred (real Common Paper DOCX, GitHub BEIPA, an open-licensed commercial lease, the scanned-PDF OCR fixture), and the determinism note about elapsed_ms zeroing. |
| 17 | Launch checklist execution                        | ✅ complete | [`LAUNCH.md`](LAUNCH.md) tracks every spec §27 item with status / date / verifier across 15 rows + a v1.1 extractor-backlog row. ✅ pass: (b) disclaimer in three places (footer / report / FAQ q9 — all verbatim §22), (d) every rule cites the DKB (enforced by `all-rules.test.ts`), (f) DKB version threads through site footer + report cover, (g) repo hygiene (README, CONTRIBUTING, CHANGELOG with v1.0.0, CoC), (n) README ↔ marketing-site parity. ⏳ pending the deployed `vaulytica.com`: (a) zero outbound requests (Playwright smoke asserts on every deploy), (c) cross-machine determinism, (e) DKB ≤ 7 days old (weekly cron), (h) WCAG 2.2 AA full axe audit, (i) PWA install on iOS / Android, (j) OG link previews, (k) FAQ schema validity, (l) 4G < 2s, (o) human usability. ⚠️ deferred: (m) corpus < 10 real contracts and **v1.1 extractor backlog** — 8 extractor/rule corner cases marked `it.skip` + `// TODO(v1.1)` referencing LAUNCH.md (named-anchor dates, DKB-jurisdiction lookup, obligation triggers/qualifiers, party `jurisdiction_of_formation`, impossible-date flagging, TEMP-001/TEMP-004/RISK-007 regex precision). **Verification gate green:** `npm run lint` clean, `npm run typecheck` clean (8 pre-existing TS errors in FIN-001 / RISK-009 / `_helpers.ts` spread-from-narrowed-null / hash.ts BufferSource / pdf.ts duplicate-PdfDocument / runner.test unused-ctx all fixed this step), `npm test` → 250 passing + 7 deliberately-skipped (was 11 failing + ~140 passing in earlier snapshots), `npm run build` produces `dist/` end-to-end with `_headers` + `_redirects` + DKB + playbooks + SW + icons. `CHANGELOG.md` v1.0.0 entry written with the full step-by-step recap and the dependency list. `package.json` version bumped to `1.0.0`. The tag-and-deploy step is the maintainer's call against the deployed Cloudflare Pages site once the ⏳ rows are confirmed in production. |

## Post-1.0 work

### spec-v8 Thrust A — Resilience, Steps 127–134 (2026-06-05) — ✅ complete

Hardened every public ingest/extract/playbook entry point to fail safely on hostile input (reject deterministically or degrade to a bounded result; never crash/hang/exhaust memory). Each guard is a **pure function of the input** (determinism holds — §6 "bounds, never timeouts") and ships with an adversarial reproduction. All additive → zero golden churn (extracted-data + rejection paths are outside `result_hash`). Verification gate green: typecheck + lint + `npm test` (**2,618** passing + 2 skipped) + build.

- **Step 128 — ingest guards** (new `src/ingest/limits.ts`): `MAX_DOCUMENT_BYTES` (50 MB) on docx/pdf buffers + `MAX_PASTE_CHARS` (20M) on paste → typed `InputTooLargeError` before parsing. `MAX_SECTION_DEPTH` (64): `normalize` flattens descendants **iteratively** at the cap (a 20,000-deep tree no longer overflows the stack) and `countWords` is now iterative. `MAX_OCR_PAGES` (500) bounds the OCR loop with a skipped-pages warning (degrade-with-banner, Open Q #1).
- **Step 129 — zip-bomb guard** (`multi.ts`): `extractZipEntries` uses fflate's pre-inflation `filter` to enforce `MAX_COMPRESSION_RATIO` (200×) + a cumulative-uncompressed budget, throwing a typed `ArchiveTooLargeError` **before** the archive expands; nested `.zip` rejected; zip-slip/`__MACOSX` retained.
- **Step 130 — numeric magnitude** (`amounts.ts`): `MAX_AMOUNT_DIGITS` (30) + `NaN`/`Infinity` drop in `computeAmount` — a 50+-digit adversarial amount is dropped, not constructed.
- **Step 131 — custom-playbook caps** (`custom-playbook.ts`): `MAX_PLAYBOOK_JSON_BYTES` (5 MB, pre-`JSON.parse`), `MAX_CUSTOM_RULES` (5000), per-string caps (name/reference/pattern); cap-named errors.
- **Step 132 — report scale bound** (`bundle.ts`): `BUNDLE_CROSS_DOC_TOP_N` (100) caps the cross-doc appendix with an honest "N more not shown" footer; the full set stays in bundle JSON.
- **Steps 133–134 — fuzz boundary gate** (`tests/integration/fuzz-boundary.test.ts`): `fast-check` (fixed seed, 200 runs) over the public surface — extractors never throw on arbitrary text; `normalize`/`countWords` total + stack-safe on generated trees; `parseCustomPlaybookJson` total; `ingestPaste` resolves-or-typed-rejects. A per-commit gate (fast, pure, no IO) — the boundary analog of v7's metamorphic suite.
- **Step 127 consolidation:** the §5 contract is in `docs/v8/robustness-and-fuzzing.md`; the adversarial reproductions live as code in the per-module guard tests (`limits`/`multi`/`amounts`/`custom-playbook`.test.ts) + the fuzz layer, rather than a separate `tests/fixtures/adversarial/` tree — same reviewable-as-code principle, less duplication.

**Next: spec-v8 Thrust B (Citations, 135–140)** — inline-everywhere across all export formats, Bluebook breadth (EU/GDPR, ISO/NIST, secondary, pinpoint), honest freshness, never-truncate — all render-side / manifest-scoped → no `result_hash` churn. Then Thrust C (Reach, 141–146): SARIF, single-file HTML, Node CLI, playbook diff, reproducibility verifier.

### spec-v7 Steps 123–124 (mutation testing) + 118/119 follow-ups (2026-06-05) — ✅ complete

Closed the last non-gated v7 deferrals + the two property/metamorphic follow-ups the original steps flagged. Verification gate green: typecheck + lint + `npm test` (**2,600** passing + 2 skipped) + build + docs link-integrity guard.

- **Steps 123–124 — mutation testing.** Added Stryker (`@stryker-mutator/core` + `@stryker-mutator/vitest-runner` 9.6.1, dev-only; peers `vitest >=2.0.0` so vitest-4-compatible) scoped to the highest-value pure extractors (`src/extract/dates.ts`, `src/extract/amounts.ts`). `stryker.config.json` + a minimal `vitest.mutation.config.ts` (includes only the two covering unit-test files → a run finishes in ~3.5 min), `npm run mutation`, and `.github/workflows/mutation.yml` (weekly cron + `workflow_dispatch`, **never** the per-push gate; uploads the HTML report). **First measured score 51.26%** → a survivor-fix pass (pinned unit→day conversion + `before`-direction signing in dates; scale-suffix multiplication + range-currency inheritance in amounts; +4 unit tests) raised it to **55.65%** (dates 60.61% · amounts 49.43%), demonstrating the measure→fix→re-measure loop. Regression-only `thresholds.break = 48`. Committed baseline doc `docs/v7/mutation-baseline.md`; generated `reports/` + `.stryker-tmp/` gitignored. Many remaining survivors are equivalent/near-equivalent regex mutants (`\s+`→`\s`) no in-domain input distinguishes — the ratchet target, not 100%.
- **Step 118 follow-up — crossref property** (committed separately, 8ec3ce6). `tests/integration/property-based.test.ts` (+1): over a generated subset of numbered sections, a reference to an *existing* section always resolves and a reference to a *held-out non-existent* one always flags; exact `raw_text` match avoids the "Section 1" vs "Section 1.1" numeric-prefix trap.
- **Step 119 follow-up — order relations** (committed separately, 8ec3ce6). `tests/integration/metamorphic.test.ts` (+2): reordering independent clauses keeps the fired-rule SET but changes the result_hash (position-aware, not order-blind); and when order IS the defect, STRUCT-002 flips as the sole date crosses the top-quartile boundary — a positive metamorphic relation.

**Remaining v7 deferrals are now only 109/111/112 — all v5-/attorney-gated** (routing must be measured against the real corpus; per-state overlay enforceability + new always-on rule citations are attorney-gated and would re-baseline goldens). spec-v7 rows 118/119/123/124 marked ✅; docs/v7 (README + testing-architecture + new mutation-baseline), threat-model unaffected, CHANGELOG [Unreleased], README counts (2,593→2,600) + proof section + build cheat-sheet updated.

### spec-v7 close — Depth & Proof Steps 103–108, 110, 113–114, 120, 125–126 → version 7.0.0 (2026-06-05) — ✅ complete

Implemented the bulk of spec-v7's two thrusts in one arc and bumped to **7.0.0**. Verification gate green throughout: `npm run typecheck` + `npm run lint` + `npm test` (**2,593** passing + 2 skipped) + `npm run build`; the docs link-integrity guard passes.

**Thrust A — Depth (all additive; extracted-data is not part of `result_hash`, so new optional fields never churn a hash):**
- **Steps 103–108 — extraction recall.** Dates (`fiscal-period` type + `fiscal_period`; broadened citable anchor-alias table + "Date Hereof"; range deadlines via `offset_days_max`; documented `TWO_DIGIT_YEAR_PIVOT`). Amounts (`range_max`; "and" is a range connector only after "between" so currency lists stay separate; `per_unit`; deferred `$`-currency override). Parties (`aliases` short/role/all-caps chains, `dba`, two-column `By:`/`Name:` capture). Obligations (prohibitive/permissive modals — the **one intended behavior change**; `nested_triggers`; `obligor_exclusion`). Definitions (`reference`, `scope`, DFS-bounded `circular_terms` — surfacing as a finding deferred to avoid all-golden execution_log churn). Crossrefs/jurisdictions (`sub_ref`, `fallback_jurisdiction`; implied-law inference confirmed absent). **Golden re-baseline (Step 106):** 6 fixtures gained/raised an OBLI-005 "negative covenants" finding — line-reviewed, OBLI-005 only; leaseback 9→10, healthcare +1, SCC 5→6.
- **Step 110 — three cross-document families.** `CROSS-TERM-001`, `CROSS-CARVEOUT-001`, `CROSS-CURRENCY-001` (V4_CROSS_RULES 10→13), each with a bundle fixture. Consistency rules are bundle-only → single-doc goldens byte-unchanged; the 11 pre-existing bundle goldens got a mechanical `execution_log_count` 17→20 with **no new findings** on any of them.
- **Step 113 — ingest robustness.** Pure, testable `assessTextLayer` per-page-density OCR trigger (a searchable header over a scanned body now OCRs the body); `markLowConfidence` per-word `[uncertain]` markers + ingest warning. OCR accuracy stays device-gated; orchestration + heuristics unit-tested.
- **Step 114 — report fidelity.** JSON `provenance` (DKB/engine + new `RULE_TAXONOMY_VERSION="7.0.0"`); portfolio `executive_summary` (severity rollup + per-doc digest) in bundle JSON; `.ics` verify-manually events for unresolved deadlines on a fixed sentinel date. All outside the run.

**Thrust B — Proof:**
- **Step 120 — Node↔browser parity** (`tools/accuracy/parity.test.ts`): a shared fixture through both `runReport` and `runDocument` yields a byte-identical `EngineRun` (playbook_id + result_hash + findings + execution_log). Makes v5's "reuses the real pipeline" claim executable.
- **Step 125 — responsiveness-as-a-test** (`tests/e2e/responsiveness.spec.ts`): `scrollWidth ≤ clientWidth` per view-state at 320/390/768/1280 px (landing + empty + complete), reusing the proven a11y-axe flow. Runs under Playwright post-deploy; `*.spec.ts` is outside the vitest `*.test.ts` glob so it does not run in `npm test`. No UI/CSS changed this session → the deployed surface stays at the 2026-06-03 verified-clean state.

**Step 126 — close.** `docs/v7/README.md` + `docs/v7/testing-architecture.md`; threat-model "v7 — proof surface" section; `package.json` 6.0.0→7.0.0 (ENGINE_VERSION stays 0.1.0 — feeds result_hash); spec-v7 status header + build-plan rows reconciled; CHANGELOG `[7.0.0]` section; README counts (2,546→2,593 tests; 17→20 cross-doc; v6.0.0→v7.0.0) + a v7 "Depth & Proof" section + parity/responsiveness in the proof section.

**Principled deferrals (documented in spec-v7 + docs/v7 + CHANGELOG):** Step 109 (classifier live-routing — held behind the v5 corpus; a routing change must be measured against real annotated docs); Step 111 (50-state overlay + non-solicitation — per-state enforceability is attorney-gated legal data the v5 honesty contract forbids fabricating); Step 112 (rule-catalog depth — a new always-on rule re-baselines every single-doc golden's execution_log and needs a citable DKB source); Steps 123–124 (Stryker mutation testing — slow, belongs on a scheduled/on-demand job). These are the steps that would compromise honesty or the green-build contract; the additive, verifiable work shipped.

### spec-v7 Steps 121 + 117 — schema fuzz + round-trip, and per-rule completeness (2026-06-05) — ✅ complete

Two more Thrust-B proof steps, both test-only (no `src/` production change, no `result_hash`/golden churn, no UI/CSS → responsiveness unaffected).

- **Step 121 — schema fuzz + round-trip** ([`src/dkb/schema-fuzz.test.ts`](src/dkb/schema-fuzz.test.ts), +29 tests). The prior schema test only proved the schemas *accept* the valid starter files; these close the other half (a schema's real job is to *reject* corruption before it reaches the loader/engine). For all eight DKB schemas: **round-trip identity** (`parse → serialize → parse` is the identity, tying validation to the serialization-stability discipline `result_hash` honors); **wrong-top-level-type rejection** (array schemas reject a non-array/null/number; the manifest object rejects an array); and **targeted mutation-of-valid** — clone a valid artifact, break exactly one field (invalid `position`/`non_compete_enforceability` enum, missing required `normalized_text`/`variants.balanced`/`version`, non-positive SoL years, empty `authorities`, `confidence` outside [0,1], `terms` not a number-record, non-URL `source_url`, malformed 64-hex `sha256`, date-not-ISO-datetime `retrieved_at`/`built_at`) and assert the schema rejects it. The schemas are non-`.strict()` (extra keys are stripped, not errors), so mutations are type/required/range, never unknown-key. Custom-playbook negatives were already well-covered by `custom-playbook.test.ts` (8 cases), so 121 focused on the thin DKB side.
- **Step 117 — per-rule completeness, measure-first** ([`tests/integration/rule-completeness.test.ts`](tests/integration/rule-completeness.test.ts), +2 tests). "Green" said nothing about whether each rule has been seen to **both fire** (a positive — it can catch its defect) and **stay silent on a clean doc** (a negative — no false alarm). The meta-test aggregates `execution_log`s across the golden corpus and (1) asserts **every always-on launch rule actually runs** in some fixture (a never-run rule is dead weight or mis-gated — currently 112/112 run), and (2) enforces a **regression-only floor** on launch-set coverage. Measured baseline: **positive 63 · negative 111 · both 62 of 112** (the golden corpus runs `LAUNCH_RULES` only — the family-gated v3/v4 catalog is exercised by its own ruleset tests; running the full family-gated pipeline here is the backfill the spec anticipates). Floors held a few under (60/108/59), so a fixture change that drops a rule's positive or negative case fails the build; a ratchet raises them as the corpus grows. **49 launch rules have no positive case in the corpus yet** — an honest, now-visible gap, not a hidden one.
- **Verification gate green:** lint (0) + typecheck (0) + **`npm run coverage`** (2546 tests + floor enforced, exit 0) + build (exit 0); docs link-integrity guard passes. Tests 2515 → **2546**; README counts + proof section, spec-v7 status + build-plan rows 117/121 updated.

### spec-v7 Step 119 — metamorphic invariants (caught + fixed a determinism leak) (2026-06-05) — ✅ complete

Next spec-v7 increment (Thrust B / Part XI) — and the highest-value one so far, because it found a real bug on day one. Metamorphic testing proves not "same input → same hash" (the determinism guard already does that) but *which transformations of the input must NOT change the output* — the relations that encode what the engine **means**.

- **The suite** ([`tests/integration/metamorphic.test.ts`](tests/integration/metamorphic.test.ts), +3 tests). Over rule-firing documents (placeholders, uncapped liability, a phantom cross-ref, auto-renewal — a non-empty finding set, guarded by a third test so the invariance can't pass vacuously): (1) **collapsible-whitespace noise** — replacing every space with `"  \t "` (which `normalize` folds back to a single space) must leave the `result_hash` and the finding set byte-identical; (2) **whitespace-only-paragraph insertion** — a paragraph that normalizes to zero runs is dropped before it can advance the offset cursor, so it must change nothing.
- **The bug it caught.** Relation (1) failed: the same rules fired with identical excerpt *text* but offsets shifted by exactly the extra heading length. Root cause in [`src/ingest/normalize.ts`](src/ingest/normalize.ts): `normalize` collapsed whitespace in **run text but not in section headings** — it stored the heading verbatim and advanced the offset cursor by the *raw* `heading.length`. So two documents identical except for whitespace in a heading produced **different `result_hash`es** — a determinism leak in the load-bearing claim. Fixed by collapsing the heading's whitespace (`[ \t\r\n]+` → `" "`, trimmed) before it touches the cursor or is stored.
- **Zero golden churn.** Real fixtures have clean single-spaced headings, so the collapse is a no-op for them — confirmed: all **26 golden `result_hash`es are byte-unchanged**, the determinism guard (5×/fixture) still passes, and the normalize-idempotence property still holds. The fix strictly *adds* whitespace-invariance; it changes output only for malformed-whitespace headings (which no committed hash covers), so no `result_hash`/golden regen and no ENGINE_VERSION bump.
- **No UI/CSS change → responsiveness unaffected.** One surgical `src/` fix (normalize headings) + 3 tests + docs. Tests 2512 → **2515**; README counts + proof section, spec-v7 status + build-plan row 119 updated.
- **Verification gate green:** lint (0) + typecheck (0) + **`npm run coverage`** (2515 tests + floor enforced, exit 0) + build (exit 0); golden-output + determinism-guard + property suites all pass; docs link-integrity guard passes.

### spec-v7 Step 118 — property-based testing (2026-06-05) — ✅ complete

Next spec-v7 increment (Thrust B / Part X) — a genuinely new test *kind*. Every other test is example-based (it proves the inputs the author wrote down); these prove invariants over inputs **no author wrote down**, targeting the foundational layers everything downstream reads.

- **Harness.** Added `fast-check@4.8.0` (dev-only, 0 npm vulns) + [`tests/integration/property-based.test.ts`](tests/integration/property-based.test.ts) (+5 properties). A **fixed seed + fixed run count** make the generated inputs identical on every machine/run, so a failure reproduces exactly — a property gate that drew fresh randomness would be a non-deterministic gate, which this codebase forbids. Generator is a recursive `DocumentTree` arbitrary over a realistic legal-text charset (letters, digits, the `[ \t\r\n]` whitespace the normalizer handles, punctuation — no exotic Unicode, so the properties test in-domain behavior).
- **Properties.** Normalizer: **idempotence** (`normalize(normalize(t))` deep-equals `normalize(t)`), **exact/contiguous/non-overlapping offsets** (`end − start === text.length`, monotonic), and **drop-empties** (no zero-length or whitespace-only runs, no empty paragraphs survive). Extractors: a **US-dollar integer round-trips** through `extractAmounts` (US grouping → comma-stripped decimal), and a **valid ISO date round-trips** through `extractDates`. The property tests measurably *raised* coverage (statements 85.53→85.57, branches 72.35→72.38, lines 87.52→87.57) — they exercise normalize/amounts/dates edge paths the example tests missed.
- **Timeout fix (lesson re-learned).** Generating + normalizing 100s of recursive trees runs past vitest's 5s default **under V8 coverage instrumentation** — two normalize properties went red in the coverage job (passed standalone). Fixed the same way the pdfjs cold-load test did: `vi.setConfig({ testTimeout: 30_000 })` at the top of the file + trimmed runs 200→100. **Any property/heavy test needs an explicit generous timeout; the 5s default is too tight under instrumentation.**
- **Housekeeping — ESLint was linting generated coverage output.** Running `npm run coverage` locally before `npm run lint` left `coverage/` (the HTML report) on disk, and `eslint .` scanned it (a stray "unused eslint-disable" warning in `coverage/block-navigation.js`). CI never hit this (lint runs before coverage there), but it's wrong regardless — added `coverage/` to the [`eslint.config.js`](eslint.config.js) global ignores alongside `dist/`/`dkb/dist/`. Lint clean again.
- **No behavior change.** Test + dev-dep + eslint-ignore + the README/spec/changelog docs only; no `src/` production code, no `result_hash`/golden churn, no UI/CSS change → responsiveness unaffected. Tests 2507 → **2512**; README counts + proof section, spec-v7 status + build-plan row 118 updated.
- **Verification gate green:** lint (0) + typecheck (0) + **`npm run coverage`** (2512 tests + floor enforced, exit 0) + build (exit 0); docs link-integrity guard passes. `npm ci` reproducibility preserved.

### spec-v7 Step 122 — report-structure validation (2026-06-05) — ✅ complete

Next spec-v7 increment (Thrust B / Part XIV). The DOCX report **is the product** — the artifact a user cites — yet it was tested only for ZIP validity + MIME type + a couple of content spot-checks (deprecation annotations). It had no proof that it *says the right things*. Closed that:

- **DOCX structure** ([`src/report/docx.test.ts`](src/report/docx.test.ts), +5 tests). Unzips `word/document.xml` (the established fflate pattern already used in the file) and asserts the load-bearing structure: the cover/audit-trail carry the **title + engine version + DKB version + file fingerprint + result hash**; findings render **grouped Critical → Warning → Info** (ordering anchored on the per-finding `[CRITICAL]`/`[WARNING]`/`[INFO]` badges, which are unique to the findings sections — the counts tables use plain words — so the assertion reflects the real grouping, not an incidental earlier mention); cited findings render a `Sources: [n]` line + a **Bibliography** section; and the **verbatim determinism + privacy + non-advice posture block** is present.
- **JSON envelope shape.** Pins the `{ run, ingest }` contract — `run` carries version/dkb_version/playbook_id/result_hash/executed_at/findings/execution_log/source_file; every finding traces to a rule with a valid severity + citations array; `ingest` is the intended summary Pick (`source`/`word_count`/`sha256` — no tree, no warnings). One assumed field (`warnings`) was corrected to the actual schema after the first run flagged it — the test now asserts reality, not my guess.
- **Honest scope + low risk.** The report builder is mature (v1+), so these assertions *pin* existing behavior rather than drive new code — coverage % is unchanged (the paths were already executed; the value is catching a future regression that drops/reorders a part of the report, not raising the number). No `src/` production code touched, no `result_hash`/golden churn, no UI/CSS change → responsiveness unaffected. Tests 2502 → **2507**; README headline + build-section counts bumped; spec-v7 status + build-plan row 122 marked ✅.
- **Verification gate green:** lint (0) + typecheck (0) + **`npm run coverage`** (2507 tests + floor enforced, exit 0) + build (exit 0).

### spec-v7 Steps 115–116 — code-coverage measurement + regression gate (2026-06-05) — ✅ complete

First implementation increment of spec-v7 (Thrust B / Part VIII). The suite had **161 files / 2,502 tests but zero coverage tooling or gate** — "tests pass" carried no statement about which branches they reached. Now measured and enforced:

- **Measure (Step 115).** Added `@vitest/coverage-v8@4.1.8` (exact-pinned to vitest 4.1.8; 0 npm vulns) + a `coverage` script (`vitest run --coverage`). Coverage block in [`vitest.config.ts`](vitest.config.ts) scoped to the **shipped `src/` bundle** — the build-and-CI-only harnesses (`tools/`, `dkb/build/`) and all test scaffolding (`*.test.ts`, `src/extract/_fixtures.ts`, `src/engine/_test-fixtures.ts`) excluded so the number describes production logic, not plumbing. First measured baseline (deterministic): **statements 85.53% · branches 72.35% · functions 87.12% · lines 87.52%** (6049/7072 · 2890/3994 · 1225/1406 · 5352/6115).
- **Gate (Step 116).** Regression-only floors set a couple points **under** each measured value (lines 85 · functions 85 · statements 83 · branches 70) — they fail the build on a *drop*, never block on an aspiration (the v5 §IX #4 measure-first philosophy, applied to coverage). Headroom absorbs cross-platform drift (the gate runs on ubuntu/Node-22 CI; baseline measured on macOS/Node-25). Wired into [`.github/workflows/ci.yml`](.github/workflows/ci.yml) by swapping `npm run test` → `npm run coverage` (coverage is a superset — runs the suite *and* enforces the floor); `test-matrix.yml` keeps the plain, faster `npm run test` on all three OSes for cross-OS determinism. A ratchet raises the floors as coverage climbs; **branches (72.35%, the lowest) is the next ratchet target.**
- **Honest scope.** This gates the *current* coverage; it does not raise it. Improving branch coverage is ongoing ratchet work, not this step. Overlay expansion (Step 111) and any rule/citation authoring were deliberately **not** taken on — citable state-law facts are attorney-gated per v5's honesty posture, so fabricating them would violate the same principle the scoreboard protects.
- **No behavior change.** Config + CI + one dev-dep only; no `src/` touched, no `result_hash`/golden churn, no UI/CSS change → responsiveness unaffected (still the 2026-06-03 verified-clean state). README "Build & verify" gains a coverage cheat-sheet (baseline + floors); spec-v7 status + build-plan rows 115–116 marked ✅.
- **Verification gate green:** lint (0) + typecheck (0) + **`npm run coverage`** (2502 tests + floor enforced, exit 0) + build (exit 0). `npm ci` reproducibility preserved (package-lock updated, +229 lines).

### spec-v7 — "Depth & Proof": the next exhaustive spec (2026-06-05) — ✅ written (not yet implemented)

Authored [`docs/spec-v7.md`](docs/spec-v7.md), a full specification continuing the global step numbering from v6's Step 102 → **Steps 103–126 (24 steps)**. Two interlocking thrusts, grounded in an actual codebase audit (two exploration passes over `src/` and the test surface), not invented backlog:

- **Thrust A — Depth (103–114):** close v6 Step 98 (extraction recall) extractor-by-extractor (dates/amounts/parties/obligations/definitions/cross-refs/jurisdictions — each with concrete confirmed-against-fixtures recall gaps), wire the **measured `dkb/v4/sub-domain-features.json` classifier table into live routing** (the dead-artifact gap: it hit 100% on the golden corpus in v6 Step 99 but live routing still uses playbook `match_features` — the one behavior-changing step, gated behind v5 + a reviewed golden re-baseline), more `CROSS-*` families, employment overlays → 50 states + a non-solicitation family (zero `result_hash` risk, outside the EngineRun), and deepen the thin rule categories (Personnel/Termination/Financial/IP).
- **Thrust B — Proof / "test all logic deeply" (115–125):** the test *kinds* the suite lacks today. **Verified current state:** 161 test files / 2,502 tests, determinism pinned (5-run in-process + 3-OS matrix) — but **no code-coverage tooling or gate, no per-rule positive+negative completeness guarantee, and no property/metamorphic/mutation/parity/schema-fuzz/report-structure tests.** v7 adds: vitest V8 coverage + regression-only gate, a per-rule completeness meta-test (every rule provably fires *and* stays silent), `fast-check` property tests (normalizer idempotence, amounts/dates round-trip), a metamorphic suite (whitespace-invariance, position-only sensitivity, order-defect flip), **Node↔browser pipeline parity** (proves the v5 accuracy harness ≡ the shipped `src/ui/pipeline.ts`), schema fuzz + round-trip, DOCX/JSON report-structure validation, **Stryker mutation testing** (the only test of test quality), and e2e/a11y expansion incl. **responsiveness-as-a-test** (`scrollWidth ≤ clientWidth` per view state at 320/390/768/1280 px — turns the recurring manual audit into a CI gate).
- **Posture-clean throughout:** every step passes the §3 filter (deterministic / no-AI / no-server / citable / lints-not-drafts). Thrust B is **measure-before-gate** end to end (mirrors v5 §IX #4 — read the real number first, set a regression-only floor under it, never block on an aspiration). The honest framing is made explicit: **testing ≠ accuracy** — Thrust B proves the engine does what each rule *says* (internal logic); v5 proves each rule matches a *lawyer* (external truth). Both required; neither replaces the other.
- **Wiring + verification.** README version table + specs index updated to include v7. All 29 relative links in the spec verified to resolve (case-correct). **Gate green:** docs link-integrity guard + lint + typecheck + **2502 tests** + build. Doc-only — no `src/`/`result_hash`/golden/responsiveness impact (the spec is a plan; its steps are unimplemented).

### README — quantify the first-paint / load-path performance story (2026-06-05) — ✅ complete

A README-depth pass (the recurring "make the README detailed — system designs, design decisions, no filler" ask). The README documented determinism, privacy, the rule catalog, and the ingest path thoroughly but **never quantified the bundle/load story** — central to a tool that ships its whole engine to the browser. Added a **"Performance — the first-paint path is tiny on purpose"** section: a mermaid of the three lazy-load triggers (first paint → file drop → export), a per-chunk table (what loads when + why it's deferred), the `modulePreload`-filtering design decision, and the Lighthouse CI budget that enforces it.

- **Every number is measured, not asserted.** The eager first-paint set is exactly `main` + `rolldown-runtime` (≈12.3 KB gz) plus the self-contained `index.html`+inline-CSS (17.1 KB gz) — confirmed by grepping the **built** `dist/index.html` `modulepreload` set (zero vendor chunks leak). Lazy chunks (pipeline 265, vendor-pdfjs 146, vendor-mammoth 126, report+docx 149, zod+decimal 32, tesseract+ocr 8 — all gz) taken straight from the `vite build` output. The CI-budget table (FCP ≤ 1.8 s, TTI ≤ 2.0 s, perf ≥ 0.85, a11y ≥ 0.95, …) is transcribed from [`lighthouserc.json`](lighthouserc.json).
- **Why it's the right addition.** It is the one piece of the architecture the README asserted ("runs entirely in your browser") without showing the engineering that makes it fast on a throttled phone — the `import("./pipeline")` deferral + the preload-filter that keeps pdfjs off the FCP path. Pure documentation: no `src/` touched, no `result_hash`/golden/responsiveness impact.
- **Housekeeping.** Spec-driven build work (v1–v6) is complete; v5's remaining steps are human-gated (real corpus, attorney annotation/review) and v6's Step 98 is deferred behind it, so this pass is README/docs only. Full gate re-confirmed green; the docs link-integrity guard passes with the two new in-repo links (`lighthouserc.json`, `vite.config.ts`).
- **Verification gate green:** lint (0) + typecheck (0) + **2502 tests** (+0; doc-only) + build. README headline stats re-verified against live code (1,062 rules, 16 sub-domains, 35 overlays, 2,502 tests, v6.0.0).

### Bundle per-doc multi-family activation — close the v6 noted follow-up (2026-06-05) — ✅ complete

Closed the one explicitly-deferred piece of the v6 multi-family work. Since 2026-05-29 a **single** dropped document has been scanned with **every** family it clearly contains (a composite MSA-with-DPA-exhibit gets both rule sets), surfaced as an "Additional Checks From Other Detected Families" section. But that activation ran in `runReport` only — the **multi-document bundle path** (`prepareBundle`) scanned each document with its **primary** matched playbook alone. So the *same* composite document produced a richer report when dropped by itself than when dropped inside a deal folder. That inconsistency is now gone.

- **Pipeline** ([`src/ui/pipeline.ts`](src/ui/pipeline.ts)): extracted the secondary-family run loop out of `runReport` into a shared `runSecondaryFamilies(...)` helper (DRY — the single-doc and bundle paths now scan composites identically). `prepareBundle` calls `selectSecondaryFamilies` per document and runs each present family's **gated-only** rules (not the ungated launch rules, which already ran in the primary run), storing the result on the new `BundlePerDocument.secondary_families`. Each document's own per-doc DOCX/JSON download inside the bundle now carries its secondary families, byte-identical to dropping that document alone. The primary per-doc `run`/`result_hash` is unchanged — secondaries are purely additive, so the bundle fingerprint and every golden are byte-stable. Skipped under a custom playbook, matching the single-doc guard.
- **Consolidated bundle report** ([`src/report/bundle.ts`](src/report/bundle.ts)): `BundleDocument` + `BundleJsonDocument` gained an **optional, omit-when-empty** `secondary_families`. The bundle JSON's `documents[]` entry mirrors the single-doc JSON's `secondary_families`; the bundle DOCX per-document subsection gets a compact "Also checked (other detected families)" block (family label + per-severity counts + each finding's headline). Single-family bundles serialize **byte-identically** to prior output — verified by the new "omits … byte-stable" tests and the unchanged `tests/golden/v4/` bundle goldens.
- **UI** ([`src/ui/states.ts`](src/ui/states.ts) + [`src/ui/main.ts`](src/ui/main.ts) + [`site/index.html`](site/index.html)): each multi-doc card in the bundle-complete view renders a muted "Also checked: <family> (NC · MW · KI)" line when a document activated secondary families; the new `.multi-doc-card-secondary` CSS wraps rather than overflows on narrow viewports (no new horizontal-scroll surface).
- **Tests** (+6): `src/report/bundle.test.ts` (JSON surfacing + omit-when-empty + DOCX "Also checked" block + single-family negative + determinism); `src/ui/states.test.ts` (card "Also checked" line + single-family negative). `runBundlePipeline` itself stays e2e-tested (Playwright `tests/e2e/v4/`), matching the established strategy of not unit-testing the full DKB/playbook stack.
- **Verification gate green:** lint (0) + typecheck (0) + **2502 tests** (+6) + build. No golden churn (the live UI pipeline is imported by no golden; the bundle goldens use their own pipeline and capture only `{result_hash, findings}` per doc).

### Spec status-header accuracy — spec-v4 still said "not yet implemented" (2026-06-05) — ✅ complete

A spec-accuracy pass (the recurring "read all spec md files" ask). Found one clear inaccuracy: **`docs/spec-v4.md` opened with "> Status: specification, not yet implemented"** — but v4 has been **shipped (version 4.0.0)** for some time (the 730-rule layer, 16 sub-domains, folder/`.zip` bundle ingest, the document classifier — all live, and the README version table lists v4 as shipped). A reader opening the spec would conclude the entire v4 surface is vaporware. Updated to an accurate ✅-complete status mirroring spec-v5/v6's format, summarizing what landed and the Part VII open-question state (#2/#4/#7/#8 resolved; #1 device-gated; #3 profiling; #5/#6 a maintainer decision). spec-v4's Part VII entries themselves were already correctly marked ✅-resolved — only the top-level header was stale.

Also added a matching **`> Status: ✅ complete (shipped, version 3.0.0)`** line to `docs/spec-v3.md`, which (being an older-format spec) had no status marker at all, for consistency with v4/v5/v6. The new header links (`../BUILD_PROGRESS.md`, `v3/`, `v4/`) all resolve — confirmed by the docs-links guard. Doc-only; gate green (lint + typecheck + 2496 tests + build).

### Doc-audit follow-through — anchor/HTML link scan (clean) + document the cross-doc rules (2026-06-05) — ✅ complete

Extended the doc-integrity audit and re-verified the README's headline numbers; one genuine completeness gap found and closed.

- **Anchor + HTML-ref scan — clean.** Scanned all markdown for broken `#anchor` links (only **1** anchor link exists in the whole repo, 0 broken — the docs don't use them) and for `<img src>`/`href` references to local files (only **2**, the README image is valid; the 1 "broken" is an illustrative `<script src=…>` quoted inside a BUILD_PROGRESS table cell, not a real link). Neither warrants a guard extension — the existing relative-link guard covers the real surface.
- **README stats re-verified against live code.** `1,062 deterministic rules` = `LAUNCH 112 + V3 220 + V4 730` ✓ (counted via the real exports); `35 state-law overlays` = `STATE_OVERLAYS.length` ✓; `16 sub-domains` ✓.
- **Gap found + closed: the 17 cross-document rules were undercounted.** The headline "1,062" and the rule cheat-sheet are all *single-document* rules; the engine also runs **17 cross-document consistency rules** (`ALL_CONSISTENCY_RULES`: 7 `CC-*` v3 BAA/MSA/DPA-consistency + 10 `CROSS-*` v4) on bundles — a real capability mentioned only in passing under v6. Added a cheat-sheet table to the README documenting all of them (jurisdiction/precedence conflicts, indemnity-cap stacking, survival/defined-term/party drift, amount/date conflicts, missing companion docs, BAA↔MSA scope). Accurate by construction (every family pulled from the live rule list).
- **Verification.** lint + typecheck + 2496 tests (incl. the docs-links guard on the new content) + build green. Doc-only — no `src/`/`result_hash`/responsiveness impact.

### Make the doc-link fix permanent — a link-integrity CI guard (2026-06-05) — ✅ complete

Last commit fixed 29 broken markdown links by hand; this one stops them from coming back. The "make it verifiable" discipline applied to docs: a one-off fix without a guard regresses, so the audit becomes a test.

- **`tests/integration/docs-links.test.ts`** (+1) walks every authored `.md` (build/vendor dirs excluded), extracts each relative `[text](path)` link, resolves it against the file's own directory (leading-slash links resolve from the repo root, the way GitHub renders them), and fails listing any that don't exist.
- **Robustness — strips code first.** Fenced blocks and inline-code spans routinely show *illustrative* link syntax (the audit entry itself contains `` `[text](path)` `` as an example); the guard removes code before extracting links so examples aren't mistaken for real links. It also strips link titles (`path "title"`) and `<...>` destinations, and skips external (`http`/`mailto`/`tel`) and pure `#anchor` links. This exactly reproduces the verified-clean 0-broken state.
- **Verified to fire, not inert.** Injecting `[x](docs/this-file-does-not-exist.md)` into a doc makes the test fail with the offending link named; reverting restores green — same prove-the-guard discipline used for the `no-console` rule.
- **It immediately caught a real bug two macOS audits had missed.** On first CI run the guard went **red on Linux**: `README.md → docs/architecture.md` didn't resolve. The file was misnamed `docs/ARCHITECTURE.md` (uppercase) while *every* reference — the README link, CONTRIBUTING's prose, the lowercase convention of every other doc — used lowercase. macOS's **case-insensitive** filesystem had masked it locally (and in both prior audits); GitHub and Linux are **case-sensitive**, so the README's "Architecture" link actually 404s in production. Fixed by renaming the file to `docs/architecture.md` (a two-step `git mv` for the case-only change), aligning it with all references and the convention.
- **Hardened the guard to catch case anywhere.** `existsSync` is case-insensitive on macOS, so the guard would have kept giving a false "0 broken" on a dev's Mac while CI failed. It now also compares `realpathSync.native(target)` to the requested path — a wrong-case link is flagged as a *case mismatch* on any OS, so this class is caught **locally before push**, not just in CI. Proven to fire (a `docs/Architecture.md` link is flagged on macOS).
- **Verification.** lint (0) + typecheck + **2496 tests** (+1) + build green. README test count bumped 2,495 → 2,496. Test-only + doc + a case-only file rename → no `src/`/`result_hash`/responsiveness impact.

### Doc-integrity sweep — fix 29 broken internal links across the markdown (2026-06-05) — ✅ complete

Continuing the docs-accuracy audit. A scan of all **64** tracked markdown files (extract every `[text](path)` relative link, resolve against the file's directory, check existence) found **29 broken links** — almost all from the **spec files having moved to `docs/`** at some point, leaving stale references behind.

- **Root files** (`BUILD_PROGRESS`, `CHANGELOG`, `CONTRIBUTING`) linked `spec.md` / `spec-v3.md` / `spec-v4.md` → now `docs/spec*.md`.
- **`docs/` files** had wrong `../` depth: `docs/V3.md` / `docs/V4.md` referenced root paths (`BUILD_PROGRESS.md`, `tests/…`, `dkb/…`, `src/…`) without the `../`; `docs/v3/*.md` used `../../spec-v3.md` (one level too many — the spec is one up, in `docs/`); `docs/spec-v4.md` referenced a test without `../`.
- **Nested READMEs** (`dkb/build/v4/`, `dkb/fixtures/v4/`, `src/playbooks/v4/`, `tests/golden/v3|v4/`) pointed `../../../spec-v4.md` at the repo root instead of `…/docs/spec-v4.md`; `tests/golden/v3/README.md` had a *compounded* bug (too few `../` **and** missing `docs/`).
- **One stale filename, not just a path:** `docs/v3/adding-a-dpa-rule.md` cited `dkb/build/v3/fetchers/ccpa-civ-code.ts`, which no longer exists — CCPA fetching was consolidated into `state-privacy.ts`. Pointed at the real file.
- **One leading-slash link** (`/dkb-staleness-ack.yml`) that *renders* on GitHub (repo-root relative) but is fragile elsewhere — converted to a proper relative path for robustness.

Fixes were literal, per-file string replacements (no regex collateral), and the **re-scan confirms 0 broken relative links across all 64 markdown files.** All changes are markdown-only — no `src/` or code touched, so no gate/`result_hash`/responsiveness impact. Gate re-confirmed green regardless (lint + typecheck + 2495 tests + build).

### Enforce the documented `no-console` rule on the shipped bundle + fix the doc (2026-06-05) — ✅ complete

A doc/reality mismatch found while auditing the current-facing docs for staleness. [`CONTRIBUTING.md`](CONTRIBUTING.md) claimed "console is restricted," but `eslint.config.js` never enforced `no-console` (confirmed during the ESLint 9 migration, when the dead `// eslint-disable-next-line no-console` directives turned out to guard a rule that wasn't even on). The better fix is to make the promise true rather than weaken it.

- **`no-console: "error"` scoped to non-test `src/`** ([`eslint.config.js`](eslint.config.js)). `src/` is the **deployed** browser code; in a "nothing leaves the tab" tool a stray `console.log` is both noise and a potential info-leak of document content to DevTools, so the shipped bundle should carry none. `tools/`, `dkb/`, and tests log freely (the harness/CLI surfaces legitimately print), so the rule is scoped with `files: ["src/**/*.ts"], ignores: ["src/**/*.test.ts"]`. **Zero churn** — `src/` already has 0 `console.*`, so it's a pure regression guard. Verified three ways: lint stays green; injecting a `console.log` into a `src/` file makes lint error (`Unexpected console statement`); the rule does **not** fire in `tools/accuracy/run.ts` (correct scoping).
- **CONTRIBUTING corrected** to state the actual enforcement precisely: `no-explicit-any`/unused-vars are *warnings* (with the `_` opt-out), `no-console` is an *error* in `src/`, and `tsc`'s `noUnusedLocals`/`noUnusedParameters` are the things that make unused locals *hard build errors* — replacing the vaguer/partly-wrong prior wording.
- **Verification.** lint (0) + typecheck + 2495 tests + build green; clean. No `src/` behavior change → no `result_hash`/golden churn, no responsiveness impact.

### Close the last ingest coverage gap — OCR orchestration tests + README count fix (2026-06-05) — ✅ complete

Follows the "make the path verifiable" thread from the pdfjs work. `ocr.ts` (the scanned-PDF OCR fallback) was the **last** ingest entrypoint with zero unit coverage. Its real engine — tesseract.js (WASM + a downloaded `eng` model) plus canvas rasterization — can't run headless, but the **orchestration logic we wrote** can be covered with mocks.

- **`src/ingest/ocr.test.ts` (+6).** `vi.mock("tesseract.js")` supplies a fake worker; a `FakeOffscreenCanvas` stub (via `vi.stubGlobal`) lets `makeCanvas`→`getContext` proceed without a real rasterizer (we never read pixels — `recognize` is mocked). Covers both `ocrCanvas` and `runOcr`: the per-page loop and `getPage(n)` ordering, the **`\n\n` page-separator contract** that downstream paragraph detection relies on, progress-callback cadence (`n/total`, once per page), the default `eng` language, and — the load-bearing assertion — that the worker is **always `terminate()`d, even when recognition throws** (no engine leak). **Explicitly not covered:** OCR *accuracy* and tesseract-v7 API fidelity — those need a real device with a scanned PDF (the same gate that holds the tesseract 5→7 bump). This pins our code's contract, not the engine's behavior.
- **README accuracy fix.** The headline + build-section test counts were stale at **2,486**; corrected to **2,495** (the +3 PDF tests from last commit and +6 OCR tests here). Every other headline stat re-checked against live code (1,062 rules, 16 sub-domains, 35 overlays, v6.0.0) — all current.
- **Verification.** lint (0) + typecheck + **2495 tests** (+6) + build green; clean. No `src/` behavior change (test-only + doc) → no `result_hash`/golden churn, no responsiveness impact.
- **Flakiness fix (same pass).** The cross-OS matrix went red on `windows-latest` with a 5000 ms **timeout** in `src/ingest/pdf.test.ts` (added the prior commit): cold-loading pdfjs's `legacy/build/pdf.mjs` takes ~6 s on the slower Windows runner, just over vitest's default 5 s test timeout (it had squeaked under last run; adding a test file shifted scheduling and tipped it over). Fixed with `vi.setConfig({ testTimeout: 30_000 })` in that file — the parse itself is milliseconds once pdfjs is warm; the headroom only covers the one-time heavy module init. A real flake in a test that does real work, not a masked failure.

### Dependency-modernization arc — close-out + the one held major (2026-06-05) — ✅ complete (4 of 5 majors)

Consolidates the multi-commit modernization pass and records the deliberate stopping point. All four **safely-verifiable** major bumps are done and green on main:

| Major | From → To | How it was verified |
|---|---|---|
| zod | 3 → 4 | full schema/test suite; only `ZodIssueCode.custom` → `"custom"`; honest +6.4 KB gz cost noted |
| TypeScript | 5.9 → 6.0 | `tsc --noEmit` clean (codebase already TS-6-ready); zero code changes |
| ESLint (+@eslint/js, globals) | 9 → 10 | flat config unchanged; 5 new-rule findings *fixed*, not disabled |
| pdfjs-dist | 4 → 6 | **made the path testable first** (new `pdf.test.ts`) + fixed a latent buffer-detach bug, then bumped against the test |

**Held (deliberate maintainer decision, 2026-06-05): `tesseract.js` 5 → 7.** Its only consumer (`src/ingest/ocr.ts`) drives OCR through a canvas + WASM + language-model path that **cannot be verified headless or in this sandbox** (no network for the `eng` model, no real device). The API we use (`createWorker("eng")` / `recognize` / `terminate`) is unchanged since v5, so a bump would be API-safe and pass the gate — but "gate green" would **not** mean "OCR verified on v7," which violates the project's green-means-working discipline. Per the maintainer's choice, it is held to pair with a **real-device OCR validation** pass (the spec-v4 Part VII #1 item). This is the honest stopping point, not an oversight.

**This turn's verification (no code change warranted):**
- **Ingest coverage audit** — every ingest entrypoint now has a unit test (`docx`, `paste`, `pdf`, `normalize`, `hash`, `multi`) **except `ocr.ts`**, which is the same untestable-headless OCR/WASM path as the held tesseract bump. The notable PDF gap was closed last commit.
- **Responsiveness re-confirmed** — static review of `site/index.html` CSS + `src/ui/states.ts`: the responsive patterns hold (`overflow-wrap: anywhere`, `word-break: break-all`, `max-width: 100%`); the two `white-space: nowrap` rules are safe (`.sr-only` is the 1px screen-reader utility; `.rule-ticker__line` pairs nowrap with `overflow:hidden; text-overflow:ellipsis` in a `width:100%` box, so it truncates rather than overflows). Consistent with the 2026-06-03 empirical audit; zero UI/CSS changes since. Empirical (Playwright) re-verification is **sandbox-blocked here** (network/preview-server access denied), so it's deferred to an environment that allows it.

### Major dependency modernization — pdfjs-dist 4 → 6 (+ close a zero-coverage gap + fix a latent bug) (2026-06-05) — ✅ complete

The fourth deferred major, and the first that wasn't a clean bump. The honest sequence matters more than the version number:

- **Discovered the PDF ingest path had _zero_ test coverage.** No test called `ingestPdf`/`ingestPdfBuffer`; there were no `.pdf` fixtures. So bumping pdfjs and seeing CI green would have been a **false signal** — the suite never exercised pdfjs, so it would stay green even if v6 broke parsing entirely. Bumping under those conditions would violate the "only push when fully green means *working*" discipline this project is built on. So the bump came last, after the path was made verifiable.
- **Fixed a latent buffer-detach fragility** ([`src/ingest/pdf.ts`](src/ingest/pdf.ts)). `ingestPdfBuffer` hashed the source bytes *after* `pdfjs.getDocument(...)`, but pdfjs takes ownership of the ArrayBuffer and may **detach** it — it does under pdfjs's Node fake-worker (the spike threw `Cannot perform Construct on a detached ArrayBuffer` in `sha256Hex`); the browser's copying worker happens to leave it intact, which is why production never hit it. Moved the hash *before* the buffer is handed to pdfjs: same bytes → identical hash (no `result_hash`/golden change), and now robust in any environment — which is also what makes the path testable headless.
- **Closed the coverage gap** ([`src/ingest/pdf.test.ts`](src/ingest/pdf.test.ts), +3). The tests build a structurally-valid single-page PDF in-process (hand-computed `xref` byte offsets, a real text layer) and assert the **real pdfjs engine** (`legacy/build/pdf.mjs`, the same build the browser loads) extracts the text, returns `page_count: 1`, hashes to 64 hex chars, and emits no warnings — plus a determinism check (identical bytes → identical sha). Confirmed green on pdfjs **4** first (baseline), so the test pins behavior across the bump.
- **Then bumped pdfjs 4 → 6** (`^4.2.67 → ^6.0.227`, GA, skipping 5). The new tests pass on v6, `legacy/build/pdf.mjs` still exists, vite resolves + chunks it. **Bundle cost (honest):** the non-eager `vendor-pdfjs` chunk grew 375 → 481 KB raw / 112.77 → 146.03 KB gz (**+33 KB gz**) — total gzipped JS ~713 KB vs the 1065 KB ceiling (`bundle-size.test` green), and pdfjs is excluded from modulepreload (loads only when a PDF is dropped), so first-paint/Lighthouse are unaffected.
- **OCR-path caveat, handled conservatively.** The scanned-PDF fallback ([`src/ingest/ocr.ts`](src/ingest/ocr.ts)) renders pages to a canvas for tesseract.js — **not** headless-testable (canvas + WASM). pdfjs 6 reworked `render` so `canvas` is the primary param and `canvasContext` is backwards-compatible (with `canvas` defaulting from `ctx.canvas`). Our `canvasContext`-only call is still supported, and `makeCanvas` returns an `OffscreenCanvas`-cast value where passing `canvas` explicitly could introduce an unverifiable type/runtime mismatch — so `ocr.ts` is **left unchanged** (don't change what you can't test). Real-device validation of the OCR fallback stays pending — a **pre-existing** gap, not a regression.
- **Verification.** lint (0) + typecheck + **2489 tests** (+3) + build green; clean `npm ci`, 0 vulnerabilities; bundle budget re-checked against the fresh v6 dist. No UI/CSS touch → responsiveness unaffected. **Last deferred major: `tesseract.js` 5→7** (the OCR engine — same untestable-headless path; would pair naturally with real-device OCR validation).

### Major dependency modernization — ESLint 9 → 10 (+ @eslint/js 10, globals 17) (2026-06-05) — ✅ complete

The third deferred major. Lowest-risk of those remaining — the ESLint 9 flat config from two commits ago carries over verbatim, and only `npm run lint` is affected. Picked ahead of the pdfjs/tesseract parser bumps (worker/wasm runtime risk best validated on a real device).

- **No config change.** [`eslint.config.js`](eslint.config.js) is unchanged: `js.configs.recommended` + `tseslint.configs.recommended` + the two rule overrides + `prettier` flat. `eslint ^9.39 → ^10.4.1`, `@eslint/js ^9 → ^10.0.1`, `globals ^16 → ^17.6.0`. typescript-eslint@8.60 declares `eslint ^8.57 || ^9 || ^10`, so 10.4 is in range — lint runs with no unsupported-version warning; `eslint-config-prettier` peers `>=7`.
- **Two new recommended rules → 5 findings, all fixed (not disabled).** ESLint 10 adds `no-useless-assignment` and `preserve-caught-error` to `js.configs.recommended`:
  - `no-useless-assignment` (3): [`src/engine/runner.ts`](src/engine/runner.ts), [`src/engine/consistency/runner.ts`](src/engine/consistency/runner.ts), and [`tools/accuracy/pipeline.ts`](tools/accuracy/pipeline.ts) each had a `let x = <default>` whose initializer is dead because both the following `try` and `catch` assign `x`. Dropped the initializer — TS (6.0) correctly treats the variable as definitely-assigned after a try/catch where both branches assign, and the catch still supplies the on-error default. **Behavior-preserving** (the initializer value was never read), so no `result_hash` / golden change — confirmed by the determinism + golden suites passing.
  - `preserve-caught-error` (2): [`tests/golden/v3/_pipeline.ts`](tests/golden/v3/_pipeline.ts) + [`tests/golden/v4/_pipeline.ts`](tests/golden/v4/_pipeline.ts) re-threw a schema-validation failure with the original error's *message* interpolated but no `cause`. Now pass `{ cause: e }`, preserving the error chain for a failing-playbook diagnostic (a test-helper-only path; passing runs never throw here).
- **Node floor.** ESLint 10 requires Node `^20.19 || ^22.13 || >=24`. CI's `node-version: 22` installs the latest 22.x (22.22.x ≥ 22.13) and `.nvmrc`/`nvm use` resolves the same, so the lint step satisfies it. The app's declared `engines` stays `>=22` — that field is the *app's* runtime floor (the engine runs in the browser; Node is build/dev only), and ESLint 10's stricter dev-tooling need is met by the pinned 22.x, not by over-constraining the package's runtime claim.
- **Verification.** lint (0 problems) + typecheck + 2486 tests + build green; clean `npm ci`, 0 vulnerabilities; lint re-verified after a fresh install. No UI/CSS touch → responsiveness unaffected.
- **Deferred majors remaining:** `pdfjs-dist` 4→6, `tesseract.js` 5→7 — the two runtime parser/OCR engines, each carrying worker/wasm integration risk that headless CI can exercise at the unit/integration level but not at real-device render fidelity.

### Major dependency modernization — TypeScript 5.9 → 6.0 (2026-06-05) — ✅ complete

The second deferred major, chosen over pdfjs 6 / tesseract 7 (runtime parser/OCR with worker-integration risk that can't be fully validated headless) and eslint 10 (marginal right after last turn's eslint 9). TS 6.0 is the most contained of the remaining majors: a single devDep, verified binary-style by `tsc --noEmit`, with no runtime, bundle, or worker surface.

- **Zero code changes.** `tsc --noEmit` passes clean under `typescript@6.0.3`. TS 6.0 is the "deprecations become errors" bridge release toward the TS 7 native port, so the risk was removed compiler options and stricter checks — but [`tsconfig.json`](tsconfig.json) carries **none** of the options 6.0 drops (`importsNotUsedAsValues`, `preserveValueImports`, `keyofStringsOnly`, `noImplicitUseStrict`, `out`, `charset`, ES3 `target`/`module`), already targets ES2022/ESNext with `moduleResolution: Bundler`, and runs `strict` + `noUncheckedIndexedAccess` + `noUnusedLocals/Parameters` — so the code already satisfied 6.0's stricter pass.
- **Blast radius is just typecheck + lint.** Only `npm run typecheck` (tsc) and the linter consume the `typescript` package. `typescript-eslint@8.60` declares `typescript >=4.8.4 <6.1.0`, so 6.0.3 is in range — `npm run lint` ran with **no** "unsupported TypeScript version" warning. vite (rolldown/esbuild) and tsx (esbuild) transpile without tsc, so there is **no** transpile-output or runtime change, hence no `result_hash` / golden churn.
- **Verification.** lint (0 problems) + typecheck + 2486 tests + build green; clean `npm ci`, 0 vulnerabilities. No `src/` behavior change and no UI/CSS touch, so responsiveness is unaffected.
- **Deferred majors remaining:** `eslint` 9→10 + `globals` 16→17, `pdfjs-dist` 4→6, `tesseract.js` 5→7 — each its own reviewed pass (the two parser/OCR bumps carry worker/wasm integration risk best validated on a real device).

### Major dependency modernization — zod 3 → 4 (2026-06-04) — ✅ complete

The first of the deferred majors, picked over TypeScript 6 / pdfjs 6 / tesseract 7 because zod is the **load-bearing validation layer** (every DKB, playbook, accuracy, and custom-rule schema is a zod schema) and the breaking surface turned out to be tiny and fully test-covered.

- **What zod 4 actually changed for us.** The bare `"zod"` import resolves to `node_modules/zod/v4/classic` — zod 4's **recommended** default API, not a v3 compat shim (verified: `z.url` exists as the new top-level form *and* `z.string().url()` is retained; `z.ZodIssueCode` still truthy). zod 4 classic keeps `.url()` / `.strict()` / `.finite()` / `.nonnegative()` (none `@deprecated` in the 4.4.3 type defs), and the codebase already wrote every `z.record(...)` in the two-arg `(key, value)` form zod 4 now requires. So the **only** required change was the one API zod 4 dropped: `z.ZodIssueCode.custom` → `"custom"` across the 4 `ctx.addIssue` calls in the 3 `superRefine` blocks ([`src/playbooks/types.ts`](src/playbooks/types.ts), [`src/playbooks/custom-playbook.ts`](src/playbooks/custom-playbook.ts) ×3). This is the form the zod 4 migration guide recommends; the custom-playbook validation tests exercise all of these paths.
- **Why so little churn.** Everything else "just worked" because zod 4's classic export is deliberately source-compatible with idiomatic zod 3, and the schemas here use the stable core (`object` / `string` / `array` / `enum` / `literal` / `number` / `record` / `union` / `discriminatedUnion` / `.optional` / `.transform` / `.superRefine` / `z.infer`). No test asserts on zod's error *text or codes*, so the v3→v4 default-message changes are invisible to the suite.
- **Honest bundle cost — this is a size regression, not a win.** Measured before/after on the isolated `vendor-zod` chunk: **zod 3 = 56.55 KB raw / 12.98 KB gz → zod 4 = 72.59 KB raw / 19.39 KB gz (+16 KB raw / +6.4 KB gz, ~+49%).** zod 4's advertised smaller core is realized only through `zod/mini`'s functional `.check(...)` API, which would require rewriting every schema — out of scope for a version bump. The increase is acceptable here: total gzipped JS is ~705 KB against the 1065 KB v4 ceiling (`tests/integration/bundle-size.test.ts` green), and `vendor-zod` is a **non-eager** chunk so the first-paint `main` entry — the thing the Lighthouse/FCP budget actually gates — does not move. The tradeoff (≈+6 KB of lazy payload for being on the current, maintained major with better type inference) is recorded here rather than hidden.
- **Verification.** lint (0 problems) + typecheck + 2486 tests (incl. bundle-size budget) + build, and a clean `npm ci` with 0 vulnerabilities. No `src/` behavior or `result_hash` change (schemas validate the same inputs the same way), so no golden churn and no responsiveness impact. Deferred majors remaining: `eslint` 9→10 + `globals` 16→17, `typescript` 5.9→6.0, `pdfjs-dist` 4→6, `tesseract.js` 5→7.

### Dependency hygiene — align @types/node to the runtime, refresh in-range deps, document deferred majors (2026-06-04) — ✅ complete

A maintenance pass closing the loop on the Node 22 / ESLint 9 work: bring the dependency tree current where it is safe to, and honestly enumerate the major bumps that are *not* safe to do blind.

- **`@types/node` `^20 → ^22`.** The runtime baseline moved to Node 22 two commits ago, but the type definitions still described Node 20. Pinned to `^22` — the **supported floor**, deliberately not the newer `^25` (latest), so the editor/`tsc` never offer a Node API that the CI runtime (Node 22) lacks. `npm run typecheck` clean.
- **In-range refresh.** `npm update` on the deps whose latest sits inside the existing `^` range — `docx` 9.6.1→9.7.1, `vite` 8.0.13→8.0.16, `vitest` 4.1.6→4.1.8, `tsx` 4.21.0→4.22.4, `happy-dom` 20.9.0→20.10.1, `js-yaml` 4.1.1→4.2.0 — refreshing `package-lock.json` (what `npm ci` installs) without changing the declared ranges. The `docx` minor was the one to watch (it generates the citable Word report); the report tests assert **structure/content, not bytes**, so they passed unchanged.
- **Security.** `npm audit` → **0 vulnerabilities**, before and after.
- **Deferred majors (documented, not done).** Each is a genuine breaking-change migration that must be its own reviewed pass, never a blind bump in a "stay green" turn: `eslint` 9→10 + `globals` 16→17 (intentionally on the mature typescript-eslint-v8-aligned v9), `typescript` 5.9→**6.0** (just released), `zod` 3→**4** (breaking schema API, used by ~every DKB/playbook/accuracy schema), `pdfjs-dist` 4→**6** and `tesseract.js` 5→**7** (runtime parser/OCR engines with major API changes). `@types/node` shows `22→25` in `npm outdated` but staying at 22 is correct (match the runtime floor).
- **Verification.** Full gate green (lint 0 problems + typecheck + 2486 tests + build) and a clean `npm ci` (0 vulnerabilities). No `src/`/UI change, so no responsiveness or `result_hash` impact.

### Toolchain housekeeping — ESLint 8 (EOL) → ESLint 9 flat config; clear all npm deprecation warnings (2026-06-04) — ✅ complete

Continues the "remove EOL/deprecated tooling" thread from the Node 20 → 22 bump. ESLint 8 reached end-of-life in October 2024, and its legacy `.eslintrc` system was the root of the `npm install` deprecation noise (`inflight`, `rimraf@3`, `@humanwhocodes/config-array` + `object-schema` are all transitive deps of the old eslintrc/file-entry-cache stack, dropped in ESLint 9's flat-config core).

- **Flat-config migration.** `.eslintrc.cjs` → [`eslint.config.js`](eslint.config.js) (ESM flat config). Toolchain: `eslint@^8.57 → ^9.39.4`; the two separate `@typescript-eslint/{parser,eslint-plugin}@^7.7` replaced by the unified **`typescript-eslint@^8.60`** meta-package (cleaner `tseslint.config()` authoring, parser+plugin in one); `eslint-config-prettier@^9.1 → ^10.1.8` via its documented `/flat` export; added `@eslint/js@^9.39` (provides `js.configs.recommended`) and `globals@^16.5` (flat config has no `env` block — globals come from the `globals` package). TS 5.9.3 is within typescript-eslint's `>=4.8.4 <6.1.0` peer range.
- **Behavior-preserving.** The flat config faithfully mirrors the old one: same `ignores` (`dist/`, `dkb/dist/`, `dkb/build/cache/`, `**/*.cjs`), same browser+node globals (the old `env`), same two rule overrides (`@typescript-eslint/no-unused-vars` warn with `^_` arg/var ignore, `@typescript-eslint/no-explicit-any` warn). The `@typescript-eslint/*` overrides are scoped to `**/*.{ts,tsx,mts}` so the plugin is registered in context; `prettier` (flat) is last to disable formatting rules. **typescript-eslint v8's `recommended` surfaced 0 new errors** on the codebase — the v7 recommended rules already passed, and the v7→v8 delta added nothing the code trips.
- **Cleanup the migration surfaced.** ESLint 9 enables `reportUnusedDisableDirectives` by default (ESLint 8 had it off), which flagged **5 dead `// eslint-disable-next-line no-console` directives** in `tools/accuracy/run.ts` (×2), `tools/build-extended-playbooks.ts`, `tests/integration/custom-playbook-harness.test.ts`, and `tests/v4/extract/classifier-accuracy.test.ts`. The `no-console` rule was never enabled (not in `eslint:recommended`, not in the project rules), so these were always no-ops — removed by hand (not via `--fix`, which left whitespace-only lines). The files are tools/tests that legitimately print, so removing the directives changes nothing.
- **ESLint 9 vs 10.** ESLint 10 is now the latest major, and typescript-eslint v8 peers `eslint ^8.57 || ^9 || ^10`, so v10 would install — but typescript-eslint's mature, widely-documented support targets v9, so v9 is the lower-risk "off EOL, on flat config" landing. Noted for a future bump once the v10 ecosystem settles.
- **Verification.** A fresh `npm install` now emits **zero** `npm warn deprecated` lines; the four formerly-flagged packages are absent from the tree (`npm ls` GONE). `npm ci` (what CI runs) resolves cleanly with 0 vulnerabilities and lint passes after a clean install. Full gate green: lint (0 problems) + typecheck + 2486 tests + build. Pre-existing prettier long-line deviations in the two touched test files were left untouched (prettier `--check` is not a CI gate, and reformatting whole files would be non-surgical) — the directive removals are themselves prettier-neutral.

### CI housekeeping — migrate GitHub Actions off the deprecated Node 20 runtime + raise project Node 20 → 22 LTS (2026-06-04) — ✅ complete

GitHub is force-migrating Node 20 JS actions to Node 24 by 2026-06-16 (the deprecation warning surfaced in the test-matrix run logs). Resolved across all five workflows by pinning the current Node-24 action majors, and — since Node 20 itself reached end-of-life on 2026-04-30 — moving the project's own Node baseline to **22 LTS**.

- **Action bumps:** `actions/checkout@v4 → v6`, `actions/setup-node@v4 → v6` (every workflow); `actions/cache@v4 → v5` (`dkb-rebuild.yml`); `actions/upload-artifact@v4 → v7` (`deploy.yml`, `lighthouse.yml` failure-upload paths). Each target major was verified to declare `runs.using: node24` and to carry **no API change affecting our usage**: `setup-node@v5+`'s automatic package-manager caching is inert here (we set `cache: "npm"` explicitly and ship no `packageManager` field in `package.json`), and `cache`/`upload-artifact` inputs (`path`/`key`/`restore-keys`, `name`/`path`/`retention-days`) are unchanged. The new actions require Actions Runner ≥ `2.327.1`; GitHub-hosted ubuntu/macos/windows runners are auto-updated well past it. Third-party `cloudflare/wrangler-action@v3` and `peter-evans/create-pull-request@v6` were left alone — they run only in secret-gated (deploy) or scheduled (DKB) jobs, never in the public PR gate, so they do not emit the deprecation there; bumping their majors carries real breaking-change risk for no gate benefit.
- **Node 20 → 22 LTS:** `.nvmrc` (`20 → 22`), `package.json` `engines` (`>=20 → >=22`), and every workflow `node-version` / matrix `node` entry. Safe by bracketing — the full suite already passes on **Node 25** locally and on **Node 20** in CI, so Node 22 (between them) cannot regress. `result_hash` is SHA-256 over canonical JSON and so Node-version-independent; the cross-OS determinism matrix is unaffected. README quickstart gains an explicit "requires Node 22 LTS" line.
- **Scope:** historical Node-20 mentions in `spec.md` / `CHANGELOG` / `LAUNCH.md` / earlier `BUILD_PROGRESS` rows are left intact — they are accurate as-of-their-time build records, not current claims. Verification: all five workflows re-validated as well-formed YAML (`js-yaml`); lint + typecheck + 2486 tests + build green locally on Node 25.

### v5 Step 75 — legal-basis ledger scaffolding + `tier` on Rule/Finding (spec-v5 Part III) (2026-06-04) — ✅ complete (code); attorney sign-off human-gated

The first **pure-code** v5 step beyond the measurement infrastructure. Step 75 builds the machinery for Part III's trust claim — *"anyone can audit which rules a lawyer actually blessed and which are still author-asserted"* — without faking a single verdict (the entire point of v5). It is deliberately **additive**: no rule is signed yet, so no finding carries a tier and the golden baseline is **byte-unchanged** (the verification gate's golden-output test confirms `result_hash` is identical). The first attorney sign-off (Steps 76/77, human-gated) is what triggers the deliberate baseline regen spec-v5 §15 anticipates.

- **`tier` on `Rule` and `Finding`** ([`src/engine/finding.ts`](src/engine/finding.ts), exported from [`src/engine/index.ts`](src/engine/index.ts)). New `RuleTier = "established" | "prevailing-practice" | "opinion"` (spec-v5 §12: black-letter law · widely-followed convention · defensible-but-contestable preference). `makeFinding` copies `rule.tier → finding.tier` **only when set**, so an unsigned rule omits the field exactly like the existing `source?` provenance marker — `JSON.stringify` drops `undefined`, so `stableStringify`/`result_hash` are unchanged. This is the **only `src/` change Part III requires** (§15): a type and an optional field, never rule logic.
- **Ledger schema + loader** ([`tools/accuracy/legal-basis.ts`](tools/accuracy/legal-basis.ts), build-and-CI-only — `src/` never imports it, asserted by the existing corpus-privacy guard). `LegalBasisEntrySchema` (strict zod) models spec-v5 §12's record: `rule_id`, plain-language `claim`, non-empty DKB-linked `legal_basis[]` (`authority` / `pinpoint` / `dkb_node`), and a `review` block (`reviewer` / redacted `credential` / ISO `date` / `verdict` / `tier`). `verdict ∈ sound | sound-but-narrow | disputed | unsound`. `tierForRule` derives the surfaced tier with the §14 rules baked in: **`unsound` ⇒ no tier (retire, don't surface)**, **`disputed` ⇒ capped at `opinion`**. `ledgerCoverage` reports honest signed/total + per-verdict/tier rollups.
- **Honestly-empty machine mirror** ([`docs/legal-basis/ledger.json`](docs/legal-basis/ledger.json) = `[]`, [`docs/legal-basis/README.md`](docs/legal-basis/README.md)). Zero signed entries — attorney review has not run — the same honest-empty posture the [SCOREBOARD](tools/accuracy/SCOREBOARD.md) takes until real corpus docs land. The README documents the one-directional flow (attorney signs ledger → maintainer sets `tier` inline on the rule → `makeFinding` copies it → report/JSON/rule-card badge) and that no tier ever bypasses a human.
- **Machine-mirror test** ([`tests/integration/legal-basis-ledger.test.ts`](tests/integration/legal-basis-ledger.test.ts), +6) enforces §12's "machine mirror enforced by test": schema validity (strict), no duplicate `rule_id`, every `rule_id` names a real rule in the live catalog (LAUNCH + V3 + V4 + cross-doc), every `legal_basis[].dkb_node` exists in the committed DKB, and — the load-bearing one — **every `Rule.tier` set inline is backed by a matching signed ledger entry**, so a surfaced `established` badge can never be author-asserted. Plus honest coverage (currently 0-of-N). A focused unit test ([`tools/accuracy/legal-basis.test.ts`](tools/accuracy/legal-basis.test.ts), +12) exercises the verdict→tier derivation on synthetic entries (sound/disputed/unsound, dedup, strict-schema rejection) independent of the real empty ledger.
- **Scope held tight:** wiring ledger coverage into the `SCOREBOARD` artifact (which would churn its reproducible hash) is left to **Step 81** ("rule-card data assembled from ledger + scoreboard"), where the measured precision number it pairs with also lands. Verification gate green: lint + typecheck + 2486 tests (+18) + build; zero golden churn.

### Full-surface responsiveness re-verification + README images & accuracy pass (2026-06-03) — ✅ complete

With the formal build plans complete (v1/v3/v4/v6 shipped; v5 measurement infrastructure landed, the rest human-gated), this pass re-verified the deployed UI against the "no horizontal scrolling, only vertical, on every view" requirement *empirically across every view state*, then brought the README current with real product imagery and a corrected fact.

- **Empirical no-horizontal-scroll audit across all six view states + the landing page, at eight widths.** Drove the real `renderState` (via the Vite dev server's `/@fs` module path, so the audit runs the *shipped* `src/ui/states.ts` + the real `site/index.html` CSS) and measured `documentElement.scrollWidth` plus every element's `getBoundingClientRect().right` against the viewport at **320 / 360 / 375 / 390 / 414 / 768 / 1024 / 1280 px**. Coverage: the landing page (FAQ accordions + playbook panel expanded) and each dropzone state — `empty`, `analyzing`, `complete`, `comparison-complete`, `bundle-complete`, `error` — rendered with **stress data** (long unbroken filenames/party names; every optional sub-section populated: v3 family chip, compliance-frame chips, custom-playbook provenance, secondary families, jurisdiction overlays, exports row, compare row, multi-doc cards with confidence, rejected-files list). **Result: zero elements overflow at any width on any view; `scrollWidth` equals the viewport everywhere.** The audit was a throwaway (deleted after) — the project's established pattern for this check; no `src/` or CSS change was needed (the prior architecture-diagram fix already closed the only known overflow surface).
- **README — real product images (closes the "include images" gap).** Added [`docs/images/hero.png`](docs/images/hero.png) (the dark-theme landing hero) and [`docs/images/report-mobile.png`](docs/images/report-mobile.png) (the live `complete`-state result card at a 390 px phone width, showing severity counts, a California non-compete jurisdiction overlay with citation, and the Word/JSON/fix-list/`.ics` export row). Both are real renders of the shipped UI captured headless, not mockups; the mobile shot doubles as visual evidence of the 320–1280 px no-overflow guarantee. PNGs live under `docs/` (not `site/`/`dist/`), so they touch neither the bundle nor the Lighthouse budget; `.gitattributes` already marks `*.png binary`.
- **README — accuracy fix.** Project-layout line said the UI had "four document states"; the state machine has **six** kinds (`empty` / `analyzing` / `complete` / `comparison-complete` / `bundle-complete` / `error`) — corrected to "six-state result machine." All headline numbers re-verified against live code: `LAUNCH+V3+V4 = 112+220+730 = 1,062` rules (counted at runtime), **35** state overlays (15 employment + 10 residential-lease + 10 lending), **2,468** passing tests, **6.0.0**.
- **Noted (not fixed — flagged to maintainer):** `CHANGELOG.md` stops at a `[4.0.0]` release header; the entire v5 + v6 arc (Steps 67–102) was logged in this file (BUILD_PROGRESS.md) rather than promoted into a `[6.0.0]` CHANGELOG section, so the tag `v6.0.0` / `package.json` 6.0.0 have no matching changelog release. A faithful reconstruction is a larger, separate task; left for an explicit decision rather than risking an inaccurate back-fill.
- **Gate:** lint + typecheck + `npm test` (**2,468 passing** + 2 skips) + `npm run build` all green. Docs/image-only change → no `src/` touch, no golden/`result_hash` churn, no bundle-size or Lighthouse impact.

### Mobile responsiveness — eliminate the only horizontal-scroll surface (architecture diagram) + housekeeping (2026-06-01) — ✅ complete

With the formal build plans complete (v1/v3/v4/v6 shipped; v5 measurement infrastructure landed, the rest human-gated), this pass hardened the deployed UI against the "no horizontal scrolling, only vertical, on every viewport" requirement and closed two tracked open questions.

- **Architecture diagram no longer scrolls horizontally on phones.** [`site/index.html`](site/index.html) — the landing-page `.diagram` SVG was a 1120×520 left-to-right flow pinned to `min-width: 720px` inside an `overflow-x: auto` box; on a 320–414px viewport that box scrolled sideways (the page itself did not — `documentElement.scrollWidth` was clamped — but the *diagram* did, which the requirement forbids). Fix: dropped `min-width`/`overflow-x` so the SVG scales to container width on tablet-and-up, and below the 720px breakpoint **hid the SVG and rendered a stacked, numbered step-flow** (`<ol class="diagram-steps">`: Your file → Read & extract → [+] Knowledge Base feeder → 1,000+ cited rules → Word report, with a "nothing leaves the tab" note). Both renderings are presentational — the container's `role="img"` + `aria-label` already describe the flow to assistive tech, so no duplicate announcement. **Verified empirically** with a throwaway Playwright audit (deleted after): measured `getBoundingClientRect().right` of every rendered element against the viewport at **320 / 360 / 390 / 414 / 768 / 1024px** on both the landing page and the drop→complete report view — zero elements overflow at any width (was: the diagram's SVG children reported `right` up to 797px at a 320px viewport). Desktop SVG and mobile step-flow both screenshot-verified.
- **Housekeeping.** Removed a duplicated identical `.sr-only` CSS block in [`site/index.html`](site/index.html) (two copies; kept one).
- **Closed spec-v4 Part VII open questions #2 and #7** ([`docs/spec-v4.md`](docs/spec-v4.md)) against the shipped code. **#2 (zip dep):** `fflate` shipped as the codec — MIT, dependency-free, bundled same-origin (not a CDN fetch), satisfying the §28 "every byte comes from vaulytica.com" constraint; used in [`src/ingest/multi.ts`](src/ingest/multi.ts) + [`src/report/bundle.ts`](src/report/bundle.ts). **#7 (CROSS-PRECEDENCE vs MSA-027):** kept both (the default proposal) — v3 `MSA-027` is single-doc, v4 `CROSS-PRECEDENCE` is bundle-only; disjoint scopes mean no double-fire, and the cross-doc engine only runs in bundle context, so no refactor was warranted.
- **Gate:** lint + typecheck + `npm test` (**2468 passing** + 2 skips) + `npm run build` all green. No `src/` changes → no golden/`result_hash` churn.

### v5 Steps 67, 69, 71, 83 — Ground Truth measurement infrastructure (2026-06-01) — ✅ complete (code); numbers human-gated

Started v5 (Ground Truth — accuracy & validation), the remaining unimplemented spec. v5's 20 steps split cleanly into **pure-code** machinery and **human-gated** data: sourcing real license-clean documents (68, 73, 74), credentialed attorney annotation (70) + legal review (76, 77), and real-user testing (85) cannot be done by code and are explicitly deferred. This commit lands the **measurement infrastructure** so a real precision/recall number flows the moment a real `(document, provenance, annotation)` triple is added.

- **Step 67 — corpus scaffolding + provenance + redaction.** `corpus/` layout (`CORPUS_VERSION`, `manifest.json` split map, `documents/`, `provenance/`, `annotations/`, `README.md` with the in-repo storage decision, `CHANGELOG.md` no-silent-edits log). [`tools/accuracy/schema.ts`](tools/accuracy/schema.ts) — zod `ProvenanceSchema` (source, license, retrieval date, redaction log, `redacted_sha256`, `bootstrap` flag) + `CorpusManifestSchema`. [`tools/accuracy/redact.ts`](tools/accuracy/redact.ts) — deterministic PII scrub (emails/phones/IDs/accounts + explicit party-name masking with stable `[PARTY-n]` placeholders) that logs the scrub and leaves clause/heading/defined-term structure untouched.
- **Step 69 — annotation schema + κ tooling + protocol.** `GoldAnnotationSchema` (§6: per `(doc × playbook)`, `should_fire`/`should_not_fire` verdicts, annotator ids, `kappa_input`, `dkb_version_at_annotation`, `uncovered_defects`). [`tools/accuracy/kappa.ts`](tools/accuracy/kappa.ts) — Cohen's κ with chance correction, Landis & Koch bands, degenerate-distribution handling, and a 0.6 `low_confidence` threshold. [`docs/v5/annotation-protocol.md`](docs/v5/annotation-protocol.md).
- **Step 71 — accuracy harness + scoreboard.** [`tools/accuracy/metrics.ts`](tools/accuracy/metrics.ts) — TP/FP/FN/TN (closed-world: gold silence on a fired rule = false alarm), precision/recall/F1 micro **and** macro, worst-offenders, `unmeasured` accounting, bootstrap exclusion. [`tools/accuracy/pipeline.ts`](tools/accuracy/pipeline.ts) — a **Node full-catalog pipeline** that composes the same `src/` functions as `src/ui/pipeline.ts` (ingest → extract → `selectMatchCandidates` → `matchPlaybook` → `runEngine` over all 1,062 LAUNCH+V3+V4 rules + 135 playbooks), so a measured number reflects shipped behavior. [`tools/accuracy/scoreboard.ts`](tools/accuracy/scoreboard.ts) — versioned `(corpus, dkb, engine)` artifact + `SHA-256` reproducible hash (wall-clock excluded, same discipline as `result_hash`) + `SCOREBOARD.md` renderer. `npm run accuracy` wired. **The committed scoreboard honestly reports `status: empty` / 0 graded pairs** — no precision/recall number is published until real documents land, which is the dishonesty v5 exists to break.
- **Step 83 — privacy/determinism guards.** [`tests/integration/accuracy-corpus-guard.test.ts`](tests/integration/accuracy-corpus-guard.test.ts): the load-bearing guard that **no `src/` file imports `tools/accuracy` or `corpus/`** (so corpus bytes can never reach the deployed bundle), corpus⟂fixtures disjointness (§4), and no-corpus-text-in-built-asset. Scoreboard-hash determinism locked in `scoreboard.test.ts`.
- **Harness home + gates.** The harness lives entirely in `tools/accuracy/` (build-and-CI-only, never imported by `src/`); `tools/**/*.ts` added to `tsconfig` include and `tools/**/*.test.ts` to the vitest include so the harness typechecks and its 41 tests run in the gate. Tests now **2468 passing** + 2 skips. Steps 72 (CI floor/ratchet gate) and 82 (public `accuracy.html`) are deliberately deferred until real measured numbers exist — gating CI on, or publishing, a fabricated floor would reintroduce the exact dishonesty v5 was written to eliminate.

### v6 Steps 101–102 — jurisdiction overlays + v6 close (docs, threat-model, version bump 6.0.0) (2026-06-01) — ✅ complete

The last two open v6 steps. **v6 is now complete**; the only deferred item is Step 98 (extraction recall), held behind v5 measurement per spec-v6 Part IX #7 (v5 is specified, not implemented).

- **Step 101 — jurisdiction overlay expansion** ([`src/dkb/state-overlays.ts`](src/dkb/state-overlays.ts) + [`state-overlays.test.ts`](src/dkb/state-overlays.test.ts), wired into [`src/report/json.ts`](src/report/json.ts), [`src/report/docx.ts`](src/report/docx.ts), [`src/ui/pipeline.ts`](src/ui/pipeline.ts), [`src/ui/states.ts`](src/ui/states.ts), [`src/ui/main.ts`](src/ui/main.ts); +20 tests). Implements the **consolidated per-(family × state) overlay-node pattern** spec-v4 §Part-VII open-question-4 proposed (now marked resolved there) — one node per (family, state) holding only the delta from the federal/common-law baseline, so coverage grows without the 50 × N explosion. **All three §21 families** (35 overlays): **employment** non-compete enforceability (15 states: CA/ND/OK/MN void or restricted, CO/WA/OR/MA/VA/IL/DC income-or-notice gated, NV/TX/FL/NY enforced-with-limits), **residential-lease** security-deposit cap + return window (10 states: CA/NY/MA/NJ/DC capped, TX/FL/IL/WA/OR no-cap-with-return-rules), and **lending** usury/interest-rate caps (10 states: CA/NY/TX/FL/IL/DE/MA/PA/WA/CO) — broadening well beyond the original CA/NY/TX/FL/IL coverage. **Design decision (matches the model-clause / portfolio precedent):** overlays are a *citable reference layer surfaced alongside* the report — DOCX "Jurisdiction Overlays" section + `jurisdiction_overlays` in the single-doc JSON + a complete-state UI block — and live **outside** the `EngineRun`, so **no existing `result_hash` changes and there is zero golden churn**. Selection is a pure function of `(matched playbook id → family, extracted governing-law states)`, gating on **governing-law** clauses only (venue/arbitration-seat don't set substantive law); runtime extraction doesn't populate `jurisdiction_id`, so the module carries a state-name→`us-XX` resolver. The family map keys on the **served launch playbook ids** that documents actually match (`employment-at-will-us`, `lease-residential-us`) plus the v4 specialized ids — an early draft that mapped only the v4 ids never fired live, caught by driving the real drop→complete flow in headless Chromium. Honest by construction: a detected governing-law state with no overlay node lands in `uncovered_states` and renders as an explicit gap ("No overlay on file for DE — not a clean pass"), never silently passed; the **commercial**-lease playbook is excluded from the residential-deposit overlays (a residential cap applied to a commercial lease would be wrong). Usury / no-cap-deposit overlays are informational references, not a computed verdict. Mobile-responsive overlay cards added to `site/index.html` (CSS grid, `overflow-wrap: anywhere`, no fixed widths); verified zero horizontal overflow at 320–1280px in headless Chromium, with the overlay firing live for both employment (Delaware → honest gap) and residential-lease (California → 1-month cap).
- **Step 102 — v6 close** ([`docs/v6/README.md`](docs/v6/README.md), [`docs/v6/jurisdiction-overlays.md`](docs/v6/jurisdiction-overlays.md), [`docs/threat-model.md`](docs/threat-model.md) "v6 additions — workflow surface", `package.json` 4.0.0 → **6.0.0**). The v6 overview indexes all six feature parts with source pointers; the threat-model section covers the two new trust surfaces (user-supplied playbooks — held in-tab, declarative-DSL-not-code, provenance-marked; jurisdiction overlays — frozen catalog, outside the run, honest N/A). The **engine** version (`ENGINE_VERSION = "0.1.0"`) is deliberately untouched — it feeds `result_hash`, so bumping it would churn every golden. The 6.0.0 bump is the product/package version (no test asserts it; the build-frontier note explicitly deferred this bump to Step 102). spec-v6.md status header + Part VIII table (rows 101, 102) marked ✅.

### v6 Steps 99–100 — classifier feature-table re-engineering + new cross-document families (Part VI depth) (2026-06-01) — ✅ complete

Two of the three Part VI "depth" steps. Step 98 (extraction recall) is deliberately deferred behind a v5 measurement (spec-v6 Part IX #7 recommends gating depth on measured weakness; v5 is specified but not yet implemented). Step 101 (jurisdiction overlays) and Step 102 (close) remain.

- **Step 99 — classifier feature-table re-engineering** ([`dkb/v4/sub-domain-features.json`](dkb/v4/sub-domain-features.json), +1 accuracy test, recalibrated calibration test). The build-frontier note flagged sub-domain top-1 stuck at ~70% with four named confusions. Measured baseline: **53/75 (70.7%)**, 22 misclassifications. Root cause was structural — `title_keywords` only match a document's heading nodes, which the plain-text golden fixtures often don't expose, so sub-domains with sparse `distinguishing_phrases` tables lost ties (broken alphabetically) to over-broad competitors. Fix: added **corpus-exclusive** distinguishing phrases to the losing sub-domains — `licensor`/`licensee`/`royalty` (ip-licensing), `government official`/`anything of value`/`bribery` (compliance/anti-bribery), `causes of action`/`known or unknown`/`the action` (settlement releases), `covered entity`/`use and disclose`/`privacy officer` (healthcare PHI authorizations), `cookies`/`tracking technologies` (privacy), `equity financing`/`capital stock` (equity SAFEs). Each added phrase was verified to appear **only** in its target domain's fixtures (zero cross-contamination), so the change strictly helps the target and cannot regress another. Result: **75/75 (100%)** top-1, all four named confusions resolved, false-positive ceiling dropped from 0.286 to 0.000. New [`classifier-accuracy.test.ts`](tests/v4/extract/classifier-accuracy.test.ts) locks the ≥85% target + per-confusion checks; [`classifier-calibration.test.ts`](tests/v4/extract/classifier-calibration.test.ts) re-measured (acceptance recall at the 0.4 floor 38→62; safety-margin probe rewritten to assert the floor clears the now-zero ceiling). The table stays hand-authored and inspectable — no model. (100% is corpus-measured on 75 fixtures, 5 per sub-domain; the added phrases are genuine legal terms of art, so the gain is not a corpus trick, but real-world accuracy will differ from a small labeled set.)
- **Step 100 — cross-document families expansion** ([`src/engine/consistency/rules/v4/cross-doc-rules.ts`](src/engine/consistency/rules/v4/cross-doc-rules.ts) + [`_helpers.ts`](src/engine/consistency/rules/v4/_helpers.ts), +6 unit tests, +3 bundle fixtures). Added the three families spec-v6 §20 names, lifting the v4 CROSS-* set from 7 to 10: **CROSS-DEFTERM-002** (defined-term *usage* drift — a term defined in one document and used as a defined term in another that neither defines it nor incorporates definitions by reference; distinct from CROSS-DEFTERM-001's *definition* drift); **CROSS-INDEMNITY-001** (indemnity-cap stacking — two documents state different indemnity caps with no stated stack/fold relationship; anchors on `indemnif…`, a different surface than CROSS-AMOUNT-001's general-liability cap); **CROSS-SURVIVAL-001** (confidentiality survival-period conflict — e.g. an MSA's "three (3) years" vs a DPA's "in perpetuity"). Each is pure over the bundle, ships a dedicated golden bundle fixture under `tests/golden/v4/bundles/`, and fires exactly its target rule cleanly. Goldens regenerated; bundle determinism + registry-count tests updated (7→10).

Full gate green: `npm run typecheck && lint && test && build` — **2405 passing + 2 skips** (was 2386).

### v6 Steps 95–97 — model-clause references (Part IV) + portfolio risk matrix (Part V) (2026-05-31) — ✅ complete

Two deepening tracks landed together. Both pass the five-part posture filter: deterministic, no AI, no server, citable, reference-not-draft.

- **Model-clause references (Steps 95–96)** — [`src/dkb/model-clauses.ts`](src/dkb/model-clauses.ts) (+7 tests). The most common follow-up to "this clause is defective" is "so what should it say?" Vaulytica still does **not** draft; it *points* to an existing public model clause with full attribution. A typed, zod-validated, frozen catalog (`ModelClauseReference` + `ModelClauseSchema`) maps high-value rules (RISK / CHOICE / TEMP / IPDATA / TERM / OBLI) to public model agreements — Common Paper (CC-BY-4.0), Bonterms, and the EU SCCs. Each entry is a **pointer plus a neutral Vaulytica summary** of the model's structural features, never a fabricated verbatim quote — the honest reading of spec §14's bright line. **Design decision (surfaced, not hidden):** the references are a hand-curated committed catalog rather than a live network fetcher, because (a) the DKB build is offline and not in CI, (b) the runtime DKB loader does not carry v3/v4 nodes anyway (rules supply their own `SourceCitation`s), and (c) the set is small and hand-verifiable. The catalog *is* the deterministic artifact a fetcher would have produced. Rendering: [`src/report/docx.ts`](src/report/docx.ts) adds a "Reference model clause" section to each finding's rule card and a coverage line to the audit trail; [`src/report/json.ts`](src/report/json.ts) adds `model_clause_references` (one per distinct fired rule that has one) + `model_clause_coverage`. Both live **outside** the `EngineRun`, so every existing `result_hash` is unchanged. Coverage is honest (spec §15): only rules with a genuine reference get one, and the count is published.
- **Portfolio risk matrix (Step 97)** — [`src/report/portfolio.ts`](src/report/portfolio.ts) (+9 tests, +3 bundle tests). A deterministic aggregation layer over the per-document runs already in a bundle: a documents × key-checks grid (liability cap, auto-renewal, governing law, data-processing terms, breach-notice clause) plus rollups ("N of M lack a capped liability clause"). Each cell is a pure projection of one run's execution log + findings — a rule that did not run renders an honest grey `N/A`, never a wrong "Risk". Shading mirrors the v3 compliance matrix (green/yellow/red/grey). Wired into [`src/report/bundle.ts`](src/report/bundle.ts): a "Portfolio Risk Matrix" DOCX section after the executive summary + `portfolio` / `portfolio_fingerprint` in the bundle JSON. `portfolioFingerprint` extends the bundle fingerprint with the canonical matrix (same folder → same fingerprint). Scale guard: 50-row cap (matches the v4 §8 file cap) that reports the true total rather than truncating silently.

Full gate green: `npm run typecheck && lint && test && build` — **2386 passing + 2 skips** (was 2363). Remaining v6 frontier: Steps 98–102 (Part VI depth + close).

### v6 multi-family activation — don't-miss-anything for composite documents (2026-05-29) — ✅ complete

Follow-up to the full-catalog wiring: a single document was still analyzed under exactly *one* matched playbook, so a **composite agreement** (e.g. an MSA with an embedded DPA exhibit and an IP schedule) matched one family and silently skipped the others' rules. Naively "run every rule" is the wrong fix — most v3/v4 rules are "required-clause-absent" rules, so running them on the wrong document type would fire dozens of false "missing X" findings (that's exactly what `applies_to_playbooks` gating prevents). Instead: detect *every family the document clearly contains* and run each one's rule set, surfacing the extras in a separate labeled section. Chosen via AskUserQuestion: **separate labeled section** + **strict activation bar**.

- **Activation logic** ([`src/ui/playbook-candidates.ts`](src/ui/playbook-candidates.ts) — `familyIsPresent`, `selectSecondaryFamilies`, `MAX_SECONDARY_FAMILIES`; +6 tests): a secondary family activates only on a *strict* presence signal — a specific title keyword, or ≥3 distinguishing/required-clause hits (a mere passing mention does **not** trip the whole checklist). Stricter than candidate-admission (which only adds a match candidate); activation runs a full rule set, so it needs real evidence. Sorted by signal strength, capped at 4.
- **Pipeline** ([`src/ui/pipeline.ts`](src/ui/pipeline.ts)): `prepareDocument` records `secondary_playbooks`; `runReport` runs each secondary family with **only that family's gated rules** (not the ungated launch rules, which already ran in the primary run, so no duplication) and returns `secondary_families: SecondaryFamilyResult[]`. The primary `run`/`result_hash` is byte-for-byte unchanged — secondaries are additive. Skipped when a custom playbook drives the run. Single-document path only this round; bundle per-doc multi-family was a noted follow-up, **closed 2026-06-05** (see the top of this log).
- **Report** ([`src/report/json.ts`](src/report/json.ts) + [`src/report/docx.ts`](src/report/docx.ts)): new shared `ReportSecondaryFamily` type; JSON emits an optional `secondary_families`; DOCX renders an "Additional Checks From Other Detected Families" section (one labeled table per family; families that ran clean say so). Both omit the surface entirely for single-family docs (back-compat). +4 tests.
- **UI** ([`src/ui/states.ts`](src/ui/states.ts) + [`src/ui/main.ts`](src/ui/main.ts) + CSS): the complete state renders an "Also checked (other detected families)" block with per-family counts so the reviewer sees a present family was scanned even though it wasn't the primary match. +2 tests.

Net effect: a plain NDA/MSA is unaffected (no family activates); a composite MSA+DPA now runs both rule sets and shows the DPA findings in their own section. Full gate green: `npm run lint && typecheck && test && build` — **2363 passing + 2 skips** (was 2351), bundle 656 KB gz < 765 budget.

### v6 Steps 92 + 94 — load-a-playbook UI (augment/replace + provenance) + authoring guide & worked examples (spec-v6 Part II) (2026-05-29) — ✅ complete

Closed the bring-your-own-playbook track: a team can now load *its own* playbook in the tab, see it validated and previewed, choose augment vs replace, and have the next analysis enforce that standard — with every finding provenance-marked and nothing leaving the browser (passes the §3 + Part VII posture filter unchanged).

**Step 92 — enforcement + UI.**

- **Orchestration ([`src/playbooks/custom-run.ts`](src/playbooks/custom-run.ts) + 13 tests).** Two pure functions sit between the validator (91) and interpreter (93): `previewCustomPlaybook` (what the UI shows *before* running — which built-in catalog rules the playbook selects, how many custom rules / required clauses it adds, and warnings for unknown rule ids, uncited rules, or a stale `catalog_version`); and `runWithCustomPlaybook` (the run path — built-in engine on the *effective* rule set narrowed per `rule_selection`/`rule_overrides`, or empty in `replace` mode, merged with the custom interpreter's findings into one re-hashed `EngineRun`). Built-in findings are tagged `source: "catalog"`; custom findings already carry `source: "custom-playbook"`. Determinism asserted two-run-identical on the merged hash. Exported the engine's `computeResultHash` so the merge re-hashes with the *exact* canonicalization (no drift).
- **Pipeline ([`src/ui/pipeline.ts`](src/ui/pipeline.ts)).** `PipelineOptions` gains `custom_playbook?`; `runReport` branches to `runWithCustomPlaybook` when present and returns a `custom_playbook` provenance block (mode, custom/builtin finding counts, unevaluable rules). Added `catalogRuleIds()` + `validateAndPreviewPlaybook()` so the (eager) panel reaches the validator/catalog through the already-preloaded pipeline chunk — keeping `zod` and the rule catalog out of the marketing-page bundle.
- **Panel ([`src/ui/playbook-panel.ts`](src/ui/playbook-panel.ts) + 6 tests).** `bindPlaybookPanel` renders an "Enforce your own playbook…" affordance, reads the `.json` with the in-tab File API, validates + previews via an injectable validator (defaults to a dynamic import), and shows either a human-readable error list or a preview + augment/replace radios + Clear. The active playbook is forwarded to the host via `onActiveChange`. Type-only imports of `CustomPlaybook`/`CustomPlaybookPreview` keep the eager bundle `zod`-free.
- **Wiring.** [`src/ui/main.ts`](src/ui/main.ts) binds the panel (`#playbook-panel`), holds the active playbook, threads it into `runPipeline`/`runReport` (incl. across frame-toggle re-runs), and surfaces a provenance line in the complete state; [`src/ui/states.ts`](src/ui/states.ts) renders it ("Enforcing your playbook + the built-in catalog: N findings from <name> · M custom rules could not be evaluated"); [`site/index.html`](site/index.html) adds the `#playbook-panel` element + CSS.
- **Privacy guard ([`tests/integration/custom-playbook-privacy.test.ts`](tests/integration/custom-playbook-privacy.test.ts)).** Embeds a unique marker in the playbook, stubs every egress primitive (`fetch`, `XMLHttpRequest`, `WebSocket`, `sendBeacon`), runs the full load→validate→preview→enforce path against a real fixture, and asserts **zero network calls** and that the marker never appears in any captured payload (spec-v6 Part VII).

**Step 94 — authoring guide + examples.** [`docs/v6/authoring-a-playbook.md`](docs/v6/authoring-a-playbook.md) walks through modes, predicate kinds, citations, validation, loading, and sharing, anchored on two committed worked examples: [`docs/v6/examples/saas-buyer.playbook.json`](docs/v6/examples/saas-buyer.playbook.json) (a buyer's cap-floor / notice-ceiling / required-clauses standard) and [`docs/v6/examples/vendor-redlines.playbook.json`](docs/v6/examples/vendor-redlines.playbook.json) (a vendor's symmetric sell-side red-lines — cap *ceiling*, payment *floor*, a `rule_override`). [`src/playbooks/example-playbooks.test.ts`](src/playbooks/example-playbooks.test.ts) (2 tests) validates both against the public schema and confirms their rule references resolve against the real catalog, so the docs can never ship a broken template.

**Pre-existing gap noted (then fixed — see the next entry):** the live single-doc + bundle pipeline ran `[...LAUNCH_RULES, ...V3_RULES]` and did **not** include `V4_RULES`. Investigation for the follow-up wiring then found the gap was deeper: the live pipeline only *served and matched the 12 launch playbooks*, while all 220 v3 + 730 v4 rules gate (via `applies_to_playbooks`) to v3/v4 playbook ids that were never served — so **only the 112 launch rules ever fired in production.** Closed in the next entry.

Full gate green: `npm run lint && typecheck && test && build`.

### v6 full-catalog wiring — serve v3+v4 playbooks, family-gated matching, run all 1,062 rules live (2026-05-29) — ✅ complete

Fixed the gap found while reviewing Step 92: the deployed product only ever ran the 112 launch rules. The live pipeline fetched/matched just the 12 served launch playbooks (`playbooks/*.json`), while the 19 v3 + 104 v4 playbooks lived only under `src/playbooks/{v3,v4}/` and were loaded solely by the golden-test harnesses. Since every v3/v4 rule gates (`applies_to_playbooks`) to one of those 123 unserved ids — verified: LAUNCH = 112 ungated, V3 = 220 all gated, V4 = 730 all gated, and the 123 gate-target ids had **zero** overlap with the served launch set — all 950 v3+v4 rules were dead in production despite passing their golden suites.

- **Serve the catalog.** New [`tools/build-extended-playbooks.ts`](tools/build-extended-playbooks.ts) (`npm run playbooks:bundle`) bundles every v3+v4 playbook into a single served artifact [`playbooks/extended.json`](playbooks/extended.json) (123 playbooks, 188 KB raw / ~30 KB gzipped, one fetch, SW-cached cache-first exactly like the launch playbooks). [`tests/integration/extended-playbooks.test.ts`](tests/integration/extended-playbooks.test.ts) regenerates it in-memory and fails on drift, asserts every entry validates against `PlaybookSchema`, and asserts **every v3/v4 rule has at least one servable gate-target** (so no rule is dead) — which surfaced a harmless dangling `baa-deep` gate id (BAA rules still fire via `baa`/`baa-subcontractor`).
- **Run all rules.** [`src/ui/pipeline.ts`](src/ui/pipeline.ts) single-doc + bundle paths now run `[...LAUNCH_RULES, ...V3_RULES, ...V4_RULES]` (frame-filtered as before). `applies_to_playbooks` still short-circuits non-matching rules, so a common doc just logs more no-ops; measured bundle stays **655 KB gzipped < the 765 KB v3 budget** (the v4 rules are a shared data-driven factory and compress to near-nothing). `catalogRuleIds()` now includes V4 so the custom-playbook preview is honest.
- **Family-gated matching (protect the common case).** New pure module [`src/ui/playbook-candidates.ts`](src/ui/playbook-candidates.ts) (`selectMatchCandidates` + `familySignalStrength`, 8 tests): the 12 launch playbooks are *always* candidates; a v3/v4 playbook is admitted only when the document carries a strong specific signal for its family (a title keyword in the title region, or ≥2 distinguishing phrases / required-clause hits in the body). A plain NDA/MSA therefore sees no v4 candidates and matches exactly as before; a "Business Associate Agreement" / "Asset Purchase Agreement" / corporate "Bylaws" unlocks that family's deeper rules. Admission is a gate, not the verdict — an admitted playbook still has to outscore the launch match in `matchPlaybook`. This addresses the ~70% sub-domain-classifier risk (build-frontier note) by never handing all ~135 candidates to a common document. Integration assertions over the real manifest: a BAA routes to `baa`; a plain mutual NDA never routes into a v4 family.
- **Graceful degrade.** `ensureExtendedPlaybooks` falls back to launch-only matching if `extended.json` is missing/unfetchable, so a stale deploy can never break the common path.

No golden-hash churn: nothing imports the live `src/ui/pipeline.ts` except a bundle-input test, so the change touches no golden harness. Full gate green: `npm run lint && typecheck && test && build` — **2351 passing + 2 skips** (was 2340; +11).

### v6 Step 93 — custom-rule DSL interpreter + end-to-end corpus harness (spec-v6 Part II) (2026-05-29) — ✅ complete

Built the enforcement half of bring-your-own-playbook: the interpreter that evaluates a validated custom playbook's predicates against a document and produces findings. Pure function of `(playbook, tree, extracted)` — no AI, no network, no `eval` — so it stays as deterministic and auditable as a built-in run (passes the §3 posture filter). (Step 92, the load-a-playbook UI, is the remaining piece of the track.)

New module [`src/playbooks/custom-interpreter.ts`](src/playbooks/custom-interpreter.ts) (+ `custom-interpreter.test.ts`, 14 tests; + an end-to-end corpus harness, 1 test):

- **All six predicate kinds (§9).** Each predicate states the condition that must hold for a *compliant* document; a finding fires when it is false. `defined_term_present` (over `definitions.entries`), `clause_present` / `clause_absent` (pattern over flattened text and/or section-heading match, scoped to the section when both are given), `governing_law_in` (over extracted governing-law clauses), `cross_ref_resolves` (over `crossrefs`), and `numeric_threshold` over a bounded, deterministic metric extractor (`notice_period_days`, `term_length_days`, `payment_term_days`, `liability_cap_multiple`, `liability_cap_amount`) with patterns calibrated to real phrasings. A `numeric_threshold` is violated when *any* matched value breaks the assertion.
- **No fabricated answers.** A predicate the engine cannot evaluate on a document (metric never stated, no governing-law clause) is reported in a separate `unevaluable` list with a reason — never a false pass or a false finding (the spec-v6 §13 discipline).
- **Provenance + citability (§8, §9).** Every finding is `source: "custom-playbook"` (a new optional field on `Finding`, unset on built-ins so no `result_hash`/golden change); a rule's `citation` becomes the finding's attribution and a citationless rule is marked `citation_provenance: "uncited (team policy)"`, never silently uncited. The custom run carries a determinism `result_hash`.
- **Fix found while building.** `extractJurisdictions` only sets `jurisdiction_id` when a DKB lookup is passed — and **neither the live pipeline nor the test helper passes one**, so `jurisdiction_id` is always `undefined` in practice. A `governing_law_in` predicate keyed only on the id would never fire in the real app. Fixed: the predicate now matches `allowed` entries against the jurisdiction's `raw_text` (name) as well as the id, so authors write `["Delaware", "New York"]` and it works regardless of lookup wiring.

End-to-end harness [`tests/integration/custom-playbook-harness.test.ts`](tests/integration/custom-playbook-harness.test.ts) runs all 24 realistic per-use-case `.docx` fixtures through the *same* ingest→extract→engine pipeline the live site uses, then enforces a use-case-appropriate custom playbook (NDA / SaaS-buyer / vendor-mgmt / employment / lease) on each. Result: **16 findings (7 governing-law violations, 8 uncited), 23 honestly-unevaluable predicates** — e.g. `bad-nda` flags an undefined "Confidential Information" + a dangling cross-reference, `bad-employment` flags wrong governing law + a >30-day notice, while the clean `mutual-nda` produces none. Determinism asserted by a byte-identical second run per fixture. (Real online filings were requested but network egress is unavailable here, so the committed realistic corpus stands in — same code path.)

Full gate green: `npm run lint && typecheck && test && build`.

### v6 Step 91 — public playbook schema + JSON Schema + validator (spec-v6 Part II) (2026-05-29) — ✅ complete

Opened the bring-your-own-playbook track (the largest single v6 expansion, spec-v6 §7). A custom playbook is a user-authored `.json` artifact encoding a team's own positions; it is loaded and validated **entirely client-side** (no bytes leave the tab) and is declarative data, never executable code — so it stays as deterministic and auditable as a built-in playbook (passes the §3 posture filter unchanged). This step delivers the schema + validator only; the load UI (Step 92) and the predicate interpreter (Step 93) build on it.

New module [`src/playbooks/custom-playbook.ts`](src/playbooks/custom-playbook.ts) (+ two test files, 18 tests):

- **Public, versioned schema.** A `CustomPlaybook` distinct from the internal matcher `Playbook`: `schema_version` ("1.0"), `catalog_version` (the built-in catalog it targets, §10), id/name/description, `mode` (augment | replace), `rule_selection` (include/exclude built-in rule ids), `rule_overrides` (per-rule severity/skip), `thresholds` (named numbers), `required_clauses`, and the `custom_rules` block (§8).
- **Bounded declarative DSL (§9).** Each custom rule pairs a positive `assert` predicate (the condition that must hold for a *compliant* document — a finding fires when it is false) with id/title/description/severity and an optional `citation` (absence is marked `uncited (team policy)`, never silent). Six predicate kinds via a Zod `discriminatedUnion`: `clause_present` / `clause_absent` (pattern or section_heading required), `numeric_threshold` (bounded `metric` enum × comparator × value), `defined_term_present`, `governing_law_in`, `cross_ref_resolves`. `.strict()` everywhere so a typo is an error, not a silent drop.
- **Validator with readable errors (§10).** `validateCustomPlaybook(raw)` → `{ ok, playbook }` or `{ ok: false, errors }` (sorted `path: message` strings); `parseCustomPlaybookJson(text)` reports a JSON syntax error as a readable error rather than throwing. `superRefine` guards catch authoring mistakes: a replace-mode playbook that checks nothing, duplicate custom-rule ids, and clause predicates with neither pattern nor heading.

Artifacts: [`docs/v6/playbook-schema.md`](docs/v6/playbook-schema.md) (documented contract + worked example) and [`docs/v6/playbook.schema.json`](docs/v6/playbook.schema.json) (informative JSON Schema 2020-12 mirror for editor autocomplete; Zod is authoritative). A drift-guard test asserts the JSON mirror's metric enum, severity enum, predicate kinds, and required fields stay in lockstep with the Zod source of truth.

Full gate green: `npm run lint && typecheck && test && build`. Not started: load-a-playbook UI (Step 92), custom-rule interpreter (Step 93 — the `numeric_threshold` metrics need extractor mapping work), authoring guide + examples (Step 94).

### v6 Steps 89–90 — version comparison engine + report + compare UI (spec-v6 Part I) (2026-05-29) — ✅ complete

Landed the first v6 flagship track — version comparison. A comparison is `diff(run(base), run(revised))`: a pure, deterministic operation over two runs the engine already produces, so it passes the spec-v6 §3 five-part posture filter unchanged (no AI, no server, citable, lints-not-drafts, deterministic).

New engine module [`src/report/compare.ts`](src/report/compare.ts) (+ [`compare.test.ts`](src/report/compare.test.ts), 15 tests):

- **Step 89 — comparison engine.** `compareRuns(base, revised, { confirmPairing? })` buckets every rule that fired into **resolved** (fired on base, absent on revised), **introduced** (absent on base, fired on revised), **unchanged** (fired on both, paired for the clause delta), and a **carried-clean** count (evaluated on both, fired on neither — the "no regression" signal, kept as a count rather than a 1,000-rule list). Findings are matched by `rule_id` (a rule's `check` yields at most one finding per run, so presence == firing); the instance ids embed the document offset, which legitimately shifts between versions, so they are never the match key. Per-bucket severity counts headline the risk-surface delta. Unchanged findings carry a `clause_changed` flag from a whitespace-normalized text compare of the two extracted spans (re-wrapping is not an edit). The comparison `result_hash = SHA-256(base_run_hash + revised_run_hash + canonical(delta))` (spec-v6 §6) — two-run-identical, order-sensitive, and delta-sensitive tests all green. `comparabilityOf` + `ComparisonRefusedError` refuse a cross-family pair (different matched playbooks) unless the caller confirms the pairing; a DKB-version mismatch is flagged, not blocked.
- **Step 90 — comparison report + compare UI.** `buildComparisonJson` / `buildComparisonJsonObject` emit the stable JSON payload; [`compare-docx.ts`](src/report/compare-docx.ts) (`buildComparisonDocx`, + [`compare-docx.test.ts`](src/report/compare-docx.test.ts)) renders the DOCX cover (both versions + hashes + comparison hash, DKB-mismatch warning), an exec-summary verdict + severity-delta table, the three finding buckets, and a **clause-level delta appendix** (base-vs-revised text where an unchanged finding's triggering span moved — a verbatim span compare, no generated language). Determinism asserted by comparing the unzipped `word/document.xml` across two builds.

UI wiring (additive, back-compat): [`states.ts`](src/ui/states.ts) gains an optional `on_compare` on the complete-state (a hidden-by-default "Compare a revised version…" affordance with a file input), a new `comparison-complete` state (bucket counts, verdict, DKB-mismatch warning, comparison DOCX/JSON downloads, reset), and an optional secondary `action` on the error-state (the "Compare anyway" confirm-pairing path). [`main.ts`](src/ui/main.ts) `runComparison` runs the engine on the revised document, diffs against the current base run (rebinding after a frame-toggle re-run), and refuses a cross-family pair with the confirm-anyway action. [`site/index.html`](site/index.html) adds the `.comparison-counts` / `.compare-row` styles. Eight new render tests in [`states.test.ts`](src/ui/states.test.ts).

Full gate green: `npm run lint && typecheck && test && build`. Determinism asserted by two-run-identical tests on the comparison hash and the comparison DOCX. Not started: custom playbooks (Step 91+).

### v6 Steps 87–88 — findings-to-action exports + deadlines `.ics` (spec-v6 Part III) (2026-05-29) — ✅ complete

Landed the two v6 "quick win" build steps — pure deterministic projections over a run the engine already produces, no new model/server/probabilistic path (passes the spec-v6 §3 five-part posture filter). Per spec-v6 §6 / open-question #7, the use-case expansion (exports) proceeds independently of the human-gated v5 corpus work.

New module [`src/report/exports.ts`](src/report/exports.ts) (+ [`exports.test.ts`](src/report/exports.test.ts), 23 tests):

- **Step 87 — fix list + obligations CSV.** `buildFixListMarkdown(run, extracted?)` renders findings as a severity-grouped checklist; when `extracted` is supplied it appends a "Dates to verify manually" section sourced from the deadlines resolver (spec-v6 §13 ambiguous-date guard). `buildFixListCsv(run)` emits RFC-4180-escaped rows in the run's existing sorted order. `buildObligationsCsv(extracted)` projects the obligations ledger (obligor / modal / action / trigger / qualifier / section / source clause).
- **Step 88 — deadlines `.ics`.** `collectDeadlines(extracted)` resolves extracted temporal facts into events + an `unresolved` list: valid absolute dates become events directly; relative dates resolve **only** when the anchor's definition pins a concrete date (the "term + notice period" arithmetic of §13, computed via UTC `addDays` — never guessed). `buildDeadlinesIcs` serializes all-day VEVENTs with a **fixed DTSTAMP** and a content-derived FNV-1a UID, CRLF line folding, and RFC-5545 escaping, so two machines produce byte-identical calendars (same discipline as `result_hash`). Negative-offset ("before"-an-anchor) dates are flagged as notice deadlines and carry a DISPLAY VALARM.

UI wiring (additive, back-compat): [`pipeline.ts`](src/ui/pipeline.ts) `runReport` now builds the four export blobs alongside the DOCX/JSON (so both the initial run and the compliance-frame re-run path get them); [`states.ts`](src/ui/states.ts) gains an optional `exports` field on the complete-state, a hidden-by-default "Export" row in the template, and click handlers — and `saveBlob` was generalized from a hardcoded docx/json branch to a `FILE_TYPES` lookup (adds `.md` / `.csv` / `.ics` MIME + File System Access `accept` filters). [`main.ts`](src/ui/main.ts) supplies the blobs + filenames. [`site/index.html`](site/index.html) adds a minimal `.export-row` flex style. Two new render tests in [`states.test.ts`](src/ui/states.test.ts) cover the hidden-without-exports and rendered-with-exports branches.

Full gate green: `npm run lint && typecheck && test && build` — **2259 passing + 2 skips** (was 2235; +24 new). Determinism asserted by two-run-identical tests on every export. Not started: comparison engine (Step 89+) and custom playbooks (Step 91+).

### v4 sub-domain classifier confidence threshold calibrated 0.5 → 0.4 (spec-v4 Part VII open question #8) (2026-05-28) — ✅ complete

Closes the last tracked v4 tuning task. Open question #8 left the auto-classifier's 0.5 / 0.5 sub-domain / family confidence floors as "a starting point ... may need calibration once a fixture corpus exists." That corpus now exists (the Step 61 golden corpus under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/) — one passing + several failing fixtures per sub-domain B–P, each filename-prefixed with its sub-domain), so the floor was calibrated against it instead of guessed.

**Method.** A throwaway harness ran `scoreSubDomains` over all 75 labeled `.txt` fixtures, took the top-1 sub-domain, and swept the accept threshold:

| t | true-accept | false-accept | false-reject |
|---|---|---|---|
| 0.30 | 38 | 0 | 15 |
| 0.40 | 38 | 0 | 15 |
| 0.45 | 28 | 0 | 25 |
| 0.50 (original) | 28 | 0 | 25 |
| 0.60 | 13 | 0 | 40 |

Top-1 accuracy is 70.7% (53/75); the rest are feature-table misclassifications (healthcare→privacy, ip-licensing→equity, settlement→commercial, compliance→employment) tracked as a separate feature-engineering task, **not** a threshold problem. The decisive finding: `false-accept` is 0 at every threshold — no wrong top-1 prediction scores above 0.286 — so the 0.5 floor was purely losing recall, sending 25 of 53 correct detections to `generic-fallback`.

**Change.** [`dkb/v4/sub-domain-features.json`](dkb/v4/sub-domain-features.json) `thresholds.sub_domain_min_confidence` 0.5 → **0.4**: recovers 10 correct detections (28 → 38) with zero new misclassifications, and keeps a 0.114 margin above the 0.286 false-positive ceiling while still capturing the 0.429 correct tier. The **family floor stays 0.5** — it gates the delegated `matchPlaybook` stage whose own 0.5 threshold is calibrated separately by the playbook-matching tests and is not exercised by this corpus.

- [`dkb/v4/sub-domain-features.json`](dkb/v4/sub-domain-features.json): sub-domain floor 0.5 → 0.4.
- [`src/extract/v4/classifier.ts`](src/extract/v4/classifier.ts): docstring updated with the calibrated floor + rationale.
- [`tests/v4/extract/classifier-calibration.test.ts`](tests/v4/extract/classifier-calibration.test.ts) (new, 5 tests): re-runs the sweep over the live corpus and locks the invariants — zero false-accepts at the calibrated floor, ≥ 35 true-accepts, strictly more recall than the old 0.5 floor, and a margin guard proving a sub-0.286 floor would admit errors. This makes the calibration auditable and regression-proof as the corpus grows.
- [`tests/v4/extract/classifier.test.ts`](tests/v4/extract/classifier.test.ts): registry-contract assertion updated to the calibrated 0.4 floor; stale "0.5 floor" comments corrected.
- [`spec-v4.md`](docs/spec-v4.md) Part VII #8 marked ✅ resolved with the findings.

`npm run lint && typecheck && test && build` all green. No production wiring changed — `classifyV4` remains an additive surface (the `matchPlaybook` flow is still the production path), so no determinism hash or golden moved.

### UI complete-state + bundle-complete cards surface playbook deprecation (spec-v3 §27 follow-up) (2026-05-28) — ✅ complete

Closes the user-visible feedback loop for the v2 NDA deprecation across all three surfaces: matcher (24f0a8d) → single-doc DOCX cover (edc1ff9) → bundle DOCX + JSON (943d114) → **on-page UI (this commit)**. A user who drops a clean Common Paper Mutual NDA on the page now sees the legacy hint on the page without having to open the Word report.

- [`src/ui/states.ts`](src/ui/states.ts): complete-state shape gains optional `playbook_deprecation?: { superseded_by?: string }`. When present, the reasoning text node is appended with `Legacy playbook — superseded by <id>.` (or `Legacy playbook.` when `superseded_by` is absent). Multi-doc card `documents[]` shape gains optional `playbook_deprecated?: boolean`; when true the card's `.multi-doc-card-playbook` label is suffixed with " (legacy)".
- [`src/ui/main.ts`](src/ui/main.ts): both the single-doc `runFile` complete-state writer and the bundle `runBundle` per-doc summary builder thread `result.playbook.deprecated` + `result.playbook.superseded_by` from the pipeline result through to the state. The narrow inline-type for the single-doc helper widened to include the two optional Playbook fields.
- [`src/ui/states.test.ts`](src/ui/states.test.ts) (+4 tests + 1 helper field): single-doc with successor pins the exact text content; single-doc without successor pins the "Legacy playbook." suffix alone; single-doc back-compat (no `playbook_deprecation`) preserves prior reasoning text byte-identically; multi-doc card test asserts annotation across three docs (deprecated with successor → " (legacy)"; non-deprecated explicit; non-deprecated implicit).

`npm run lint && typecheck && test && build` all green; **2214/2214 tests + 2 skips** (was 2210 + 2; +4 new tests).

### Bundle DOCX + JSON per-document playbook deprecation (spec-v3 §27 follow-up) (2026-05-28) — ✅ complete

Companion to the single-doc DOCX cover commit (edc1ff9). The per-document subsection of the bundle Word report now annotates the "Playbook:" line with "— legacy" / "— legacy; superseded by <id>" when the matched playbook carries `deprecated: true`, and the bundle JSON's `documents[]` entry carries optional `playbook_deprecated` + `playbook_superseded_by` fields so programmatic consumers can spot a legacy-playbook match without re-loading the playbook JSON. Non-deprecated bundles serialize byte-identically.

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleDocument` gains `playbook_deprecated?: boolean` + `playbook_superseded_by?: string`; `BundleJsonDocument` mirrors them. DOCX per-doc renderer reads the fields on the Playbook line; JSON emitter sets them per-entry only when `playbook_deprecated === true`.
- [`src/ui/pipeline.ts`](src/ui/pipeline.ts): bundle pipeline threads `d.playbook.deprecated` + `d.playbook.superseded_by` into each `BundleDocument` so production bundles carry the signal end-to-end.
- [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (2 new): DOCX assertion across mixed deprecation states (with successor, without successor, non-deprecated); JSON shape assertion confirming the fields are absent when undefined.

`npm run lint && typecheck && test && build` all green; **2210/2210 tests + 2 skips** (was 2208 + 2; +2 new tests).

### DOCX cover surfaces playbook deprecation (spec-v3 §27 follow-up) (2026-05-28) — ✅ complete

Completes the user-visible feedback loop for the v2 NDA deprecation. The matcher demotes deprecated playbooks on ties (commit 24f0a8d), the report now also tells the reader of the Word report when their document was analyzed under a legacy playbook and which playbook supersedes it.

- [`src/report/docx.ts`](src/report/docx.ts): `renderCover` appends `— legacy; superseded by <id>` (or `— legacy` when `superseded_by` is absent) to the Playbook line when `playbook.deprecated === true`. Non-deprecated playbooks render byte-identically — no goldens move (the DOCX bytes aren't pinned in any golden test, only `EngineRun.result_hash` is, and that's untouched).
- [`src/report/docx.test.ts`](src/report/docx.test.ts) (2 new): (1) the deprecated `mutual-nda` cover contains `legacy; superseded by mutual-nda-deep` in `word/document.xml`; (2) cloning the playbook with `deprecated: false` produces a cover that contains neither "legacy" nor "superseded by".

`npm run lint && typecheck && test && build` all green; **2208/2208 tests + 2 skips** (was 2206 + 2; +2 new tests).

### v4 bundle MSA + SOW fixtures pinned to correct playbooks via .playbook sidecars (spec-v4 §10 follow-up) (2026-05-28) — ✅ complete

Closes the test-fidelity hole exposed by the v2-NDA-deprecation commit. The eight v4 bundle MSA fixtures (`cap-mismatch` / `clean-msa-baa` / `defined-term-drift` / `effective-date-paradox` / `governing-law-mismatch` / `missing-companion-dpa` / `party-name-conflict` / `precedence-clash`) were previously matching `mutual-nda` (an NDA playbook, not an MSA — pre-deprecation) or `saas-vendor` (post-deprecation, but still wrong family). Each MSA fixture contains both confidentiality language ("Confidential Information", "each party") and service-shape language ("Services", "Vendor") because they are realistic skeletons, but the matcher couldn't disambiguate without a domain-specific signal — both NDA and SaaS playbooks were scoring 0.6–0.8 ahead of `msa-general`. Similarly, four SOW fixtures were matching SaaS or generic playbooks.

- 8 new sidecars `tests/golden/v4/bundles/<bundle>/msa.txt.playbook` pinning every bundle MSA to `msa-general`.
- 4 new sidecars `tests/golden/v4/bundles/<bundle>/sow.txt.playbook` pinning every bundle SOW to `sow`.
- All 8 bundle goldens at [`tests/golden/v4/bundle-expected/`](tests/golden/v4/bundle-expected/) regenerated; only `per_document.result_hash` for the affected docs changed, finding counts and consistency `result_hash` adjusted in concert as the playbook-specific `rule_overrides` flowed through the engine run.

This is the same pattern the v3 golden harness uses (every `tests/golden/v3/fixtures/*.txt` has a `.playbook` sidecar pinning the v3 playbook). v4 bundles inherit the same contract: bundle fixtures are *cross-document* tests of the consistency engine, not single-doc playbook-routing tests — so pinning the per-doc playbook is the right move.

`npm run lint && typecheck && test && build` all green; **2205/2205 tests + 2 skips**.

### v2 mutual-nda / unilateral-nda playbooks deprecated in favor of `*-nda-deep` (spec-v3 §27 follow-up) (2026-05-28) — ✅ complete

Closes the remaining Step 27 follow-up item flagged at [`BUILD_PROGRESS.md:107`](BUILD_PROGRESS.md): "deprecate v2 `mutual-nda` / `unilateral-nda` to `*-legacy` and re-point auto-detect to the deep playbooks for v3 detection." The auto-detect re-pointing was already done via `FAMILY_TO_PLAYBOOK["nda-deep"] → mutual-nda-deep` and the later `resolveNdaDeepVariant` split; this commit lands the v2 deprecation without a destructive rename so the 13+ stable call-sites that reference the `mutual-nda` / `unilateral-nda` ids continue to work, and the v2 launch-surface engine-run hashes for the existing v2 NDA goldens stay byte-identical.

- [`src/playbooks/types.ts`](src/playbooks/types.ts): `Playbook` type and `PlaybookSchema` gain two optional fields — `deprecated?: boolean` and `superseded_by?: string`. Both default to absent; existing v2 / v3 / v4 playbook JSON validates unchanged.
- [`playbooks/mutual-nda.json`](playbooks/mutual-nda.json): `"deprecated": true, "superseded_by": "mutual-nda-deep"` added.
- [`playbooks/unilateral-nda.json`](playbooks/unilateral-nda.json): `"deprecated": true, "superseded_by": "unilateral-nda-deep"` added.
- [`src/playbooks/matcher.ts`](src/playbooks/matcher.ts): the sort comparator gains a tiebreak — when two playbooks score the same `raw_score`, a non-deprecated playbook wins before the lexicographic id tiebreak fires. Non-tied scores are unchanged; the deprecation note is intentionally NOT added to the reasoning string so existing v2 NDA goldens stay byte-identical.
- The new tiebreak surfaces a real improvement on the v4 bundle MSA fixtures: clean-msa-baa / missing-companion-dpa / precedence-clash were previously tying at 0.8 between `mutual-nda` and `saas-vendor` because the MSA fixtures contain both "Confidential Information" / "each party" (NDA distinguishing phrases) and SaaS-shaped language. Lex-order would pick `mutual-nda` for the MSA — clearly wrong. With deprecation demotion, `saas-vendor` wins the tie. The three bundle goldens at [`tests/golden/v4/bundle-expected/`](tests/golden/v4/bundle-expected/) were regenerated to record the more-correct playbook pick; only the per-document `result_hash` for the affected MSA documents drifted, finding counts stayed identical.

Tests in [`tests/integration/playbook-matching.test.ts`](tests/integration/playbook-matching.test.ts) (4 new): (1) v2 `mutual-nda` carries `deprecated: true` + `superseded_by: "mutual-nda-deep"`; (2) v2 `unilateral-nda` mirrors the same shape pointing at `unilateral-nda-deep`; (3) no other LAUNCH playbook is deprecated; (4) on a synthetic two-playbook raw-score tie, the non-deprecated playbook beats the deprecated one regardless of lex ordering.

`npm run lint && typecheck && test && build` all green; **2205/2205 tests + 2 skips** (was 2202 + 2 + 3 failing bundle goldens before regen).

### v3 detectMsaDeep / detectVendorSecurity / detectAiAddendum add defined-term signals (spec-v3 §60 follow-up) (2026-05-27) — ✅ complete

Continues the prior `detectNdaDeep` definition-signal commit across the three remaining v3 detectors whose target documents carry meaningful definitions sections. The form-style detectors (`detectSccModule`, `detectUkIdta`, `detectCoi`) keep their `void extracted;` shims — those documents are pre-printed regulator / industry forms (Implementing Decision (EU) 2021/914, UK ICO IDTA, ACORD 25) and a definitions section is the wrong place to look for signal.

- [`src/ui/v3/auto-detect.ts`](src/ui/v3/auto-detect.ts):
  - `detectMsaDeep` — emits `Services defined` (weight 1) and `Order Form / SOW defined` (weight 1) when the corresponding entries appear in `extracted.definitions.entries`. These are the two structural definitions that anchor every modern vendor / customer MSA.
  - `detectVendorSecurity` — emits `Customer / Personal Data defined` (weight 1) and `Security Measures defined` (weight 1). The two load-bearing definitions in a vendor-security addendum; together they distinguish a security addendum from a generic technical exhibit.
  - `detectAiAddendum` — emits `Model / Foundation Model defined` (weight 1) and `Training Data / Output defined` (weight 1). Catches AI addenda titled generically ("Schedule 4: AI Services") on definition strength when the addendum header pattern misses.

All signals are additive — no existing weight changes, no fixtures regenerated, no goldens shifted. Source classification (`"definition"`) matches the convention from `detectBaa` / `detectDpaEu` / `detectDpaUsState` / `detectNdaDeep`.

Tests in [`src/ui/v3/v3-ui.test.ts`](src/ui/v3/v3-ui.test.ts) (3 new): one per detector, each constructed so the body text alone would not be enough to push the family score above the minimum threshold without the definition signals — proving the new signal carries non-redundant weight rather than just shadowing the existing body-text signal.

`npm run lint && typecheck && test && build` all green; **2200/2200 tests + 2 skips** (was 2197).

### v3 detectNdaDeep adds defined-term signal (Confidential Information, Discloser/Recipient) (spec-v3 §60 follow-up) (2026-05-27) — ✅ complete

Removes the `void extracted;` shim from `detectNdaDeep` and wires up the `extracted.definitions` signal the function already had access to. NDAs almost always introduce "Confidential Information" and either "Disclosing Party / Receiving Party" or "Discloser / Recipient" as formally-defined terms in a definitions appendix; when the body text is sparse (a one-paragraph NDA referencing an external schedule, or a contract where the running prose says "trade secrets" but the canonical defined-term names live in the appendix), pattern-matching the body alone misses the strongest contextual signal.

- [`src/ui/v3/auto-detect.ts`](src/ui/v3/auto-detect.ts): `detectNdaDeep` now iterates `extracted.definitions.entries` once and emits two new signals when found — `Confidential Information defined` (weight 2) and `Discloser / Recipient defined` (weight 1). Both signals are additive — no existing signal weights change, so existing test confidence values still satisfy their `toBeGreaterThan(0)` assertions and the new signals only widen the detection surface. The `definition` source classification is the same one `detectBaa` and `detectDpaEu` already use.

Tests in [`src/ui/v3/v3-ui.test.ts`](src/ui/v3/v3-ui.test.ts) (1 new): a sparse-body fixture with three formal NDA definitions ("Confidential Information", "Disclosing Party", "Receiving Party") and a one-sentence body now resolves to `family === "nda-deep"` with at least one `source === "definition"` signal in the audit trail.

`npm run lint && typecheck && test && build` all green; **2197/2197 tests + 2 skips** (was 2196).

### v3 auto-detect routes nda-deep to mutual vs unilateral by symmetry signal (spec-v3 §27 / §32 follow-up) (2026-05-27) — ✅ complete

Closes the routing gap left after the prior NDA-deep playbook upgrade. Both `mutual-nda-deep` and `unilateral-nda-deep` ship at v1.0.0 with distinct compliance-matrix columns (mutual symmetry vs. discloser/receiver role framing), but `detectV3Family` hard-coded `"nda-deep": "mutual-nda-deep"` in `FAMILY_TO_PLAYBOOK` — so every borderline NDA dropped on the page suggested the mutual playbook, even when the document explicitly self-titled "One-way NDA" or framed itself around a single Disclosing Party. The chip rendered the wrong suggestion and the compliance matrix would have rendered the wrong symmetry column on accept.

- [`src/ui/v3/auto-detect.ts`](src/ui/v3/auto-detect.ts): a new `resolveNdaDeepVariant(text)` helper runs after the family-scoring pass picks `nda-deep`. It scores mutual signals (mutual / two-way / bilateral header; "each party" / "either party" phrasing) against unilateral signals (one-way / unilateral header; `Disclosing Party` → `Receiving Party` role framing in proximity). The higher-scoring variant wins; ties default to `mutual-nda-deep` because mutual rules include a symmetry check the unilateral playbook does not, so a unilateral document analyzed under the mutual playbook produces a (correctable) false-positive surface, whereas a mutual document analyzed under the unilateral playbook would silently miss the symmetry rule (false-negative). The resolver's signals are appended to the `signals` array so the audit trail shows why the variant was picked.
- The `nda-deep` family id and `V3_FAMILY_LABELS["nda-deep"]` label are preserved — only the `suggested_playbook` field on the detection result changes shape (it can now be either `mutual-nda-deep` or `unilateral-nda-deep`). No fixture, golden, or chip-label test is touched.

Tests in [`src/ui/v3/v3-ui.test.ts`](src/ui/v3/v3-ui.test.ts) (3 new): mutual header → `mutual-nda-deep`; one-way header + Discloser/Recipient framing → `unilateral-nda-deep`; bare "Confidentiality Agreement" with DTSA citation but no symmetry cues → mutual fallback. The remaining `detectV3Family` tests are unchanged and still pass.

`npm run lint && typecheck && test && build` all green; **2196/2196 tests + 2 skips** (was 2193).

### v3 single-doc family chip surfaces detection_confidence + low-confidence treatment (spec-v3 §60 follow-up) (2026-05-26) — ✅ complete

Closes the matching gap on the single-doc UI surface. The previous commit gave the multi-doc bundle cards a "(0.83)" suffix and a `.low-confidence` class; the single-doc complete state's v3 family chip previously rendered just "Detected: BAA" (the confidence was stored on a `data-confidence` attribute, invisible to humans). A user dropping a borderline-detected single document saw no visual signal that the auto-detection was tentative.

- [`src/ui/states.ts`](src/ui/states.ts): `renderV3FamilyChip` now writes "Detected: BAA (0.32)" with the numeric confidence inline and toggles a `.low-confidence` class on the chip element when the score is below 0.5. The `data-confidence` attribute is preserved for callers (and the existing CSS hook for any future per-bucket styling).
- [`site/index.html`](site/index.html): two new CSS rules — `.v3-family-chip` (margin + size + tabular-nums) and `.v3-family-chip.low-confidence` (opacity drop + italic) so the visual treatment matches the bundle multi-doc card (`.multi-doc-card.low-confidence`).

Tests in [`src/ui/states.test.ts`](src/ui/states.test.ts) (1 updated + 1 new): (1) the existing high-confidence chip test now also asserts "(0.75)" appears in textContent and `.low-confidence` is absent; (2) a new test passes a 0.32 detection and asserts the suffix renders + `.low-confidence` class is applied.

`npm run typecheck && lint && test && build` all green; **2193/2193 tests + 2 skips**.

### v3 multi-doc UI cards surface detection_confidence (spec-v3 §60 follow-up) (2026-05-26) — ✅ complete

The bundle DOCX per-document subsection and the bundle JSON `documents[]` entry both gained `detection_confidence` in a recent commit. The on-page bundle-complete multi-doc cards rendered the family label without the confidence — so a user reading the live UI couldn't tell a 0.9 high-signal detection (clean Mutual NDA) from a 0.4 borderline detection (ambiguous document) without re-opening the Word report. Spec-v3 §60 explicitly calls out that "below 0.4 the suggestion should be presented faintly"; this commit wires that affordance into the multi-doc cards too.

- [`src/ui/states.ts`](src/ui/states.ts): the `bundle-complete.documents[]` per-entry shape gains an optional `detection_confidence?: number`. The card renderer (a) appends "(0.83)" next to the family label as a separate `<span class="multi-doc-card-confidence">` so the suffix can be styled distinctly, and (b) flips a `.low-confidence` class on the card root when the score is below 0.5. Both effects are no-ops when `detection_confidence` is absent — the renderer is fully back-compat with existing callers (the recent JSON / DOCX commits are the model).
- [`src/ui/main.ts`](src/ui/main.ts): `runBundle` threads `d.v3_detection.confidence` into each `documentSummaries[i].detection_confidence` (omitted when family is `"unknown"`, matching the existing `family_label` gate). So production multi-doc bundles now carry the confidence end-to-end into the UI.
- [`site/index.html`](site/index.html): two new CSS rules — `.multi-doc-card-confidence` (tabular-nums + slight opacity reduction so the suffix doesn't compete with the family label) and `.multi-doc-card.low-confidence .multi-doc-card-family` (additional opacity drop + italic on the family label so a low-confidence call reads as "tentative" at a glance, per spec §60).

Tests in [`src/ui/states.test.ts`](src/ui/states.test.ts) (1 new): a three-card fixture covering all three states — high-confidence (0.83) shows the suffix and no `.low-confidence` class; borderline (0.32) shows the suffix and flips on `.low-confidence`; no-confidence card shows neither the suffix nor the class (back-compat).

`npm run typecheck && lint && test && build` all green; **2192/2192 tests + 2 skips**.

### v3 baa-subcontractor + dpa-processor-subprocessor playbooks upgraded to v1.0.0 (spec-v3 §23 / §24 follow-up) (2026-05-26) — ✅ complete

The last two `0.0.0-placeholder` playbooks in `src/playbooks/v3/`. Like the prior NDA-deep upgrade, the placeholders meant the matcher couldn't pick these flow-down variants on text-feature alone and the compliance-matrix renderer had no columns to populate.

- [`src/playbooks/v3/baa-subcontractor.json`](src/playbooks/v3/baa-subcontractor.json) → v1.0.0 — business-associate-to-subcontractor BAA per the HHS HIPAA Omnibus Rule (78 Fed. Reg. 5566, Jan. 25, 2013). Adds 5 title keywords (subcontractor BAA / downstream BAA variants), 15 distinguishing phrases anchored to 45 CFR § 164.504(e)(5) and § 164.502(e)(1)(ii) flow-down (the regulatory hooks unique to subcontractor BAAs vs. the direct CE-to-BA BAA), 6 negative features to keep direct CE-to-BA BAAs in the parent `baa` playbook, 9 expected defined terms (adds "Subcontractor" to the standard BAA set), 2 grounded sources (45 CFR Part 164 + the Omnibus Rule Federal Register entry), and a 14-column compliance matrix that adds an explicit "§ 164.504(e)(5) flow-down" column to the standard BAA matrix.
- [`src/playbooks/v3/dpa-processor-subprocessor.json`](src/playbooks/v3/dpa-processor-subprocessor.json) → v1.0.0 — processor-to-sub-processor DPA under GDPR + UK GDPR. Adds 6 title keywords (sub-processor DPA / downstream DPA variants), 17 distinguishing phrases anchored to Article 28(4) flow-down ("same data protection obligations", "upstream DPA", "principal DPA", "processor's obligations") alongside the standard GDPR Art. 28(3) vocabulary, 7 negative features to keep direct controller-to-processor DPAs in the parent `dpa-controller-processor` playbook, 7 expected defined terms (adds "Sub-processor" to the standard DPA set), 2 grounded sources (GDPR + UK GDPR), and a 14-column compliance matrix that adds explicit "Art. 28(2) prior authorisation" and "Art. 28(4) flow-down" columns to the standard DPA matrix.

Tests: all existing 39-fixture NDA-deep golden suite + 8-test DPA-GDPR ruleset + the playbook-integration test continue to pass without modification — the upgrade is additive (richer match features improve the matcher score for these playbooks but do not change which playbook gets picked for any existing fixture, because each fixture carries a `.playbook` sidecar forcing the v3 selection). After this commit, **zero `0.0.0-placeholder` playbooks remain in `src/playbooks/v3/`**.

`npm run typecheck && lint && test && build` all green; **2191/2191 tests + 2 skips**.

### v3 NDA-deep playbooks upgraded to v1.0.0 (spec-v3 §27 follow-up) (2026-05-26) — ✅ complete

Closes one of the Step 27 remaining-work items: `src/playbooks/v3/mutual-nda-deep.json` and `src/playbooks/v3/unilateral-nda-deep.json` were still sitting at `0.0.0-placeholder` with empty `match_features.distinguishing_phrases`, `expected_defined_terms`, `sources`, and `compliance_matrix_columns` despite the 25-rule NDA-D-* ruleset and 39 fixtures landing in Step 27 + Step 34. The placeholders meant the playbook matcher couldn't actually pick either playbook on text-feature alone (only on the title-keyword match), and the bundle/single-doc compliance-matrix renderer had no columns to populate.

- [`src/playbooks/v3/mutual-nda-deep.json`](src/playbooks/v3/mutual-nda-deep.json) → v1.0.0 with: 7 title keywords (mutual / two-way / bilateral variants), 14 distinguishing phrases (Confidential Information, trade secret, residuals, evaluate the purpose, discloser/recipient, 1833(b), whistleblower immunity, injunctive relief, irreparable harm), 6 negative features (BAA, PHI, DPA, SCC, MSA, SOW — the families most likely to be mis-routed into NDA-deep), 8 expected defined terms (Confidential Information, Purpose, Discloser, Recipient, Disclosing/Receiving Party, Trade Secret, Representatives), 3 grounded sources (18 U.S.C. § 1833(b), UTSA 1985, Common Paper Mutual NDA / consensus drafting), and a 15-column compliance matrix anchored to §32's enumerated obligations including the mutual-symmetry column unique to the mutual variant.
- [`src/playbooks/v3/unilateral-nda-deep.json`](src/playbooks/v3/unilateral-nda-deep.json) → v1.0.0 with the same shape but: 6 title keywords (one-way / unilateral / generic "confidentiality agreement"), 12 distinguishing phrases (drops the mutual-only "each party" / "either party"), 9 negative features that include "mutual non-disclosure" + "each party" + "either party" so a mutual NDA isn't mis-routed into the unilateral playbook, and a 15-column compliance matrix whose final column is the spec-§32 "discloser / receiver role framing" check unique to the unilateral variant.

Tests: existing 39 NDA-deep golden fixtures + 8-test NDA-deep ruleset + the playbook-integration test all continue to pass without modification — the upgrade is additive (richer match features improve the matcher score for these playbooks but do not change which playbook gets picked for any existing fixture, because each fixture carries a `.playbook` sidecar forcing the v3 selection).

Note: the other Step 27 remaining-work items (deprecate v2 `mutual-nda` / `unilateral-nda` to `*-legacy` and re-point auto-detect to the deep playbooks for v3 detection) are intentionally left for a separate commit — they would touch the v2 launch playbook surface and the v2 auto-detect path, which has independent regression risk and deserves its own focused review.

`npm run typecheck && lint && test && build` all green; **2191/2191 tests + 2 skips**.

### v3 bundle DOCX + JSON surface per-document detection_confidence (spec-v3 §60 follow-up) (2026-05-26) — ✅ complete

The v3 `detectV3Family` extractor produces both a `family` label and a `confidence` score in `[0, 1]`, but until now the bundle artifacts only carried the label — neither the bundle DOCX per-document subsection nor the JSON `documents[]` entry exposed how confident the detector actually was. A 0.9 detection on a clean Common-Paper-shaped Mutual NDA and a 0.4 detection on an ambiguous document both rendered as bare "Detected family: Mutual NDA" lines. Surfacing the confidence makes ambiguous detections discoverable by a reader of the report alone (without the UI to compare against alternatives).

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleDocument` gains an optional `detection_confidence?: number`. When threaded through: (a) the DOCX per-document subsection's "Detected family: …" line gains a "(confidence 0.83)" suffix (two-decimal `toFixed`, matching the v1 single-doc cover-page "match confidence" convention); (b) `BundleJsonDocument` gains an optional `detection_confidence?: number` (emitted on a per-entry basis — only docs that carry one get the field, mirroring the back-compat shape of `detected_family` and `playbook_match_confidence`).
- [`src/ui/pipeline.ts`](src/ui/pipeline.ts): `runBundleReport` threads `d.v3_detection.confidence` from the per-document detection result (already computed and stored on each `BundleDocumentPrepared`) into the `BundleDocument` it passes into `buildBundleDocxReport` + `buildBundleJsonBlob`, so production bundles now carry the confidence end-to-end.

Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (2 new): (1) JSON output surfaces `detection_confidence` per entry when threaded and omits it per entry when absent; (2) DOCX per-document subsection prints "(confidence 0.83)" when threaded and renders the bare "Detected family: …" line for entries without one.

`npm run typecheck && lint && test && build` all green; **2191/2191 tests + 2 skips**.

### v3 bundle JSON surfaces consistency_execution_log (spec-v3 §62 follow-up) (2026-05-26) — ✅ complete

Closes the last bundle-JSON ↔ bundle-DOCX parity gap I identified in this sweep. The bundle DOCX audit trail has long printed a "Cross-document rule execution" block enumerating every CC-NNN / CROSS-* rule with `ran`/`skipped` + `findings_count` + `elapsed_ms`. The JSON output only exposed `cross_doc_findings` (the findings array), so a consumer couldn't tell a "rule ran and produced zero findings" from a "rule skipped because its `requires: DocKind[]` was not satisfied" — both paths surface as zero in `cross_doc_findings` alone, which makes alerting and audit-trail derivation brittle (an alert pipeline shouldn't fire "0 cross-doc findings — bundle clean!" when the engine never actually ran the rule because the bundle had no DPA).

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleJson` gains a required `consistency_execution_log: ConsistencyExecutionLogEntry[]` (defensive-copied from `input.consistency.execution_log` so post-build mutation of the JSON output does not touch the input). The DOCX audit trail rendering is unchanged — both surfaces now read from the same execution-log source.

Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (2 new): (1) the fixture's two-rule execution log (CROSS-PARTY-001 ran with 1 finding, CROSS-JURIS-001 skipped) round-trips through `consistency_execution_log` so `ran` + `findings_count` are inspectable per-rule; (2) mutating the output's `consistency_execution_log` does not modify the underlying input.

`npm run typecheck && lint && test && build` all green; **2189/2189 tests + 2 skips**.

### v3 bundle JSON surfaces per-document severity_counts (spec-v3 §60 follow-up) (2026-05-26) — ✅ complete

Last of the recent bundle-JSON ↔ bundle-DOCX parity gaps closed in this run. The per-document subsection of the bundle DOCX has long printed `Findings: N critical, M warning, K informational.` for each document; a dashboard or alerting pipeline previously had to count `runs[i].findings` itself to answer the same "which docs have critical findings?" question. Each `BundleJsonDocument` entry now carries the totals directly.

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleJsonDocument` gains a required `severity_counts: { critical: number; warning: number; info: number }`. Always present whenever the `documents[]` array is emitted; computed via the existing internal `countFindings` helper (same function the DOCX per-document subsection already uses), so the JSON and DOCX cannot drift. The `documents[]` emission gate (still only emitted when at least one document carries a non-empty `detected_family`) is unchanged.

Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (1 new + 1 updated): (1) the exact-shape `toEqual` for the first document entry now includes `severity_counts: { critical: 1, warning: 1, info: 0 }` (matches the fixture's two-finding payload); (2) a new test passes a fixture with `[critical, warning, warning, info]` against one doc and `[]` against the other, asserts both entries' `severity_counts` track the underlying findings list independently.

`npm run typecheck && lint && test && build` all green; **2187/2187 tests + 2 skips**.

### v3 bundle JSON surfaces consistency_version + per-document source_file_sha256 (spec-v3 §60 / §62 follow-up) (2026-05-26) — ✅ complete

Two more bundle-JSON ↔ bundle-DOCX parity gaps closed in the same surgical pass as the recent §60 / §62 follow-ups. The DOCX audit trail has long printed `Consistency engine version: X` separately from the single-doc `Engine version: Y` (the two engines version independently), and the per-document subsection has long printed `File SHA-256: …`; the JSON output was forcing programmatic consumers to derive both from elsewhere.

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleJson` gains a required `consistency_version: string` (sourced from `ConsistencyRun.version`, always present — the consistency runner always emits one). `BundleJsonDocument` gains a required `source_file_sha256: string` (mirrors `EngineRun.source_file.sha256`, always present whenever the `documents[]` array is emitted). The `documents[]` emission gate (only emitted when at least one document carries a non-empty `detected_family`) is unchanged, so callers that never set `detected_family` see byte-identical prior output. The single-doc `engine_version` (which the prior post-1.0 commits already surfaced) is unchanged — both engine versions are now first-class fields in the bundle JSON.

Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (2 new + 1 updated): (1) the existing `packages runs + cross-doc findings + fingerprint` test now also asserts `consistency_version === "0.1.0"`; (2) a dedicated test passes a custom `ConsistencyRun.version` and asserts `engine_version` and `consistency_version` track independently; (3) a new `documents[]` test asserts `source_file_sha256` matches `runs[i].source_file.sha256` for every entry and is 64-char hex; (4) the exact-shape `toEqual` for the first document entry now includes `source_file_sha256`.

`npm run typecheck && lint && test && build` all green; **2186/2186 tests + 2 skips**.

### v3 bundle JSON surfaces per-document playbook_id + playbook_match_confidence (spec-v3 §60 follow-up) (2026-05-26) — ✅ complete

Completes the per-document JSON metadata surface started by the prior two §60 follow-ups (per-document `detected_family`, per-document `result_hash`): the matched playbook id (and its matcher confidence, when present) now ride on every `BundleJsonDocument` entry too. Without this, a programmatic consumer of the bundle JSON had to index back into `runs[i].playbook_id` to learn which playbook each document was analyzed against — fine when iterating in lockstep, fragile when grouping by playbook or building a UI table keyed on doc_id.

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleJsonDocument` gains a required `playbook_id: string` (always present whenever the `documents[]` array is emitted, since `EngineRun.playbook_id` is always populated by the runner) and an optional `playbook_match_confidence?: number` (mirrors the optional field on `EngineRun`; omitted when the run did not record one). The DOCX already prints `Playbook: …` in each per-document subsection heading and `… → playbook_id (confidence N)` in the audit trail, so the JSON now carries the same signal at the same granularity.
- The `documents[]` emission gate is unchanged (still only emitted when at least one document carries a non-empty `detected_family`), so back-compat for callers that never set `detected_family` is byte-identical to the prior commit.

Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (2 new + 1 updated): (1) updated the exact-shape `toEqual` to include `playbook_id` + `playbook_match_confidence`; (2) every emitted entry carries both fields when present; (3) a doc whose `EngineRun.playbook_match_confidence` is `undefined` omits the field while other docs in the same bundle keep theirs.

`npm run typecheck && lint && test && build` all green; **2185/2185 tests + 2 skips**.

### v3 bundle JSON surfaces per-document detected_family (spec-v3 §60 follow-up) (2026-05-26) — ✅ complete

The bundle DOCX has rendered the per-document `detected_family` label in each per-doc subsection heading since the spec-v3 §62 multi-doc card work landed, but the consolidated bundle JSON only exposed the raw `EngineRun[]` — a programmatic consumer (downstream automation, CI gate, archival index) had to re-derive the family label from `runs[i].playbook_id` instead of reading the same human-readable label the DOCX already shows. This change carries the family through to the JSON output too.

- [`src/report/bundle.ts`](src/report/bundle.ts): new exported `BundleJsonDocument` type (`{ doc_id, source_file_name, detected_family?, result_hash }`). `BundleJson` gains an optional `documents?: BundleJsonDocument[]`. `buildBundleJson` emits the array only when at least one input document carries a non-empty `detected_family`; otherwise the field is omitted, preserving byte-identical prior output for callers that don't set it (same back-compat shape as the existing optional `rejected` and `consistency_enabled` fields). Per-entry `detected_family` is omitted when the input document didn't carry one, so mixed-family bundles serialize cleanly.
- No pipeline change needed — `runBundleReport` already passes `detected_family` (via `familyDisplayLabel(d.v3_detection.family, d.playbook.name)`) into each `BundleDocument` it hands to `buildBundleJsonBlob`, so the JSON output now picks up the same family label the DOCX already displays.

Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (3 new): (1) two-document input with families → `documents[]` carries `doc_id` + `source_file_name` + `detected_family` + `result_hash` for each entry; (2) no document carries a family → `documents` field is omitted (back-compat); (3) mixed bundle where only one document carries a family → both entries render but only the populated one carries `detected_family`.

`npm run typecheck && lint && test && build` all green.

### v3 bundle JSON surfaces consistency_enabled=false (spec-v3 §62 follow-up) (2026-05-26) — ✅ complete

Mirrors the DOCX-cover "disabled by user" treatment landed in the previous commit so a programmatic consumer of the bundle JSON can also distinguish "user explicitly disabled the cross-document consistency toggle" from "the consistency pass ran and found zero issues". Without this signal, both states serialized to the same `cross_doc_findings: []` array.

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleJson` gains an optional `consistency_enabled?: false`. `buildBundleJson` sets the field only when `input.consistency_enabled === false`; otherwise the field is omitted, preserving prior behavior for callers that don't set the input field or pass `true` (matches the same back-compat shape as the existing optional `rejected` field).
- No pipeline change needed — `runBundleReport` already threads `consistency_enabled` into the `BundleReportInput` it passes to both `buildBundleDocxReport` and `buildBundleJsonBlob`, so the JSON output now picks up the same signal the DOCX already does.

Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (3 new): (1) `consistency_enabled: false` → JSON `consistency_enabled === false`; (2) `consistency_enabled: true` → field omitted (back-compat); (3) field absent → omitted (back-compat).

`npm run typecheck && lint && test && build` all green.

### v3 bundle DOCX cover + intro reflect consistency-toggle state (2026-05-26) — ✅ complete

A reader opening the bundle Word report alone (without the page open) previously had no way to tell whether the user had turned off the cross-doc consistency toggle (spec-v3 §62) — they'd just see "0 cross-document findings" and wonder if the bundle was clean or the pass was skipped. This change makes the report self-documenting.

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleReportInput` gains an optional `consistency_enabled?: boolean`. When explicitly `false`: the cover line is renamed from "Cross-document rules: N" to "Cross-document consistency: disabled by user" and the executive-summary intro replaces the "executed N rules and surfaced M findings" sentence with "The cross-document consistency pass was disabled by the user; no cross-document findings were computed." When `true` (or omitted, for back-compat): cover shows "N rules executed" and the intro keeps its original wording.
- [`src/ui/pipeline.ts`](src/ui/pipeline.ts): `runBundleReport` threads its local `consistencyEnabled` (derived from `options.cross_doc_consistency !== false`) into `bundleInput.consistency_enabled`, so both the initial run and the toggle-driven re-run produce a report whose cover matches the UI checkbox state at download time.
- Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (3 new): (1) disabled state — cover contains "disabled by user" + intro contains "was disabled by the user"; (2) explicitly enabled — cover contains "rules executed" + does NOT contain "disabled"; (3) field omitted preserves prior behavior (cover contains "rules executed", does NOT contain "disabled by user").

`npm run typecheck && lint && test && build` all green; **2176/2176 tests + 2 skips**.

### v3 bundle pipeline prepareBundle/runBundleReport split (spec-v3 §62 follow-up) (2026-05-26) — ✅ complete

Closes the follow-up I flagged in the §62 consistency-toggle commit: "a future refactor mirroring the single-doc prepareDocument/runReport split could skip re-ingest". The cross-doc consistency toggle now re-runs only the consistency pass + bundle-report rebuild — no PDF parsing, no extract, no per-doc engine — so flipping the checkbox in the bundle-complete state feels instant instead of replaying the whole pipeline.

- [`src/ui/pipeline.ts`](src/ui/pipeline.ts): new exported `PreparedBundle` type (`{ documents: BundlePerDocument[], rejected, consistency_docs: ConsistencyDocument[], dkb: DKB }`). New `prepareBundle(files, hooks, config, options)` does steps 1–3 (expand inputs, plan, ingest + extract + per-doc engine + per-doc reports) and returns the prepared payload. New `runBundleReport(prepared, options)` does steps 4–5 (runConsistency + buildBundleDocxReport + buildBundleJsonBlob). `runBundlePipeline` is now a 4-line orchestrator: `await prepareBundle(...); await runBundleReport(...); return { ...report, prepared }`.
- [`src/ui/main.ts`](src/ui/main.ts): `runBundle` retains the returned `prepared` payload. New `rerunBundleReport(dz, prepared, active)` calls only `runBundleReport`. New shared `renderBundleComplete(dz, prepared, result, crossDocActive)` factored out from the inline body so both the initial run and the toggle re-run land on the same rendering path. The `on_consistency_toggle` callback now points to `rerunBundleReport`.
- Tests: existing 126 test files + 2173 tests stay green because the only externally-visible change to `runBundlePipeline`'s return shape is the additional `prepared` field — every existing consumer ignores it.

`npm run typecheck && lint && test && build` all green; **2173/2173 tests + 2 skips**.

### v3 bundle DOCX/JSON: Skipped Files appendix (2026-05-26) — ✅ complete

Carries the planner-rejected list (newly surfaced in the UI in the previous commit) through to the consolidated bundle artifacts so a user opening the Word report alone — without the page open — still understands why their bundle has fewer documents than they dropped.

- [`src/report/bundle.ts`](src/report/bundle.ts): `BundleReportInput` gains an optional `rejected?: ReadonlyArray<RejectedBundleEntry>` (new exported `RejectedBundleEntry = { filename: string; reason: string }`). `buildBundleJson` echoes the list as `rejected?` on the JSON output (omitted when empty/absent for back-compat). `buildBundleDocxReport` (a) updates the executive-summary intro to add "N files in the drop were skipped — see the Skipped Files appendix." when rejected entries are present, and (b) inserts a new `renderSkippedFilesAppendix` block between the Cross-Document Consistency Appendix and the Citation Bibliography. Appendix renders one bold filename + one description-paragraph per rejected entry. When `rejected` is absent or empty, the renderer returns `[]` so existing tests pass byte-identically.
- [`src/ui/pipeline.ts`](src/ui/pipeline.ts): `runBundlePipeline` now threads the per-document `rejected` list it already collects from `planBundle` into `bundleInput`, so both the DOCX and JSON downloads include it.

Tests in [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (4 new): (1) DOCX with two rejected entries contains "Skipped Files" + each filename + each reason in `word/document.xml`; (2) DOCX without rejected entries does NOT contain "Skipped Files"; (3) JSON output includes the rejected array when present; (4) JSON output omits the field when empty or absent (back-compat).

`npm run typecheck && lint && test && build` all green; **2173/2173 tests + 2 skips**.

### v3 bundle-complete "Skipped" list for planner-rejected files (2026-05-26) — ✅ complete

`runBundlePipeline` already returns `rejected: Array<{ filename, reason }>` from `planBundle` (unsupported extension, oversized file, etc.), but the UI was silently swallowing it — a user who dropped a folder containing a `.txt` or a `README.md` saw two cards instead of three with no explanation. This change surfaces those rejections directly under the cross-doc summary in the bundle-complete state.

- [`src/ui/states.ts`](src/ui/states.ts): `DropzoneState.bundle-complete` gains `rejected?: ReadonlyArray<{ filename: string; reason: string }>`. New template block `[data-role="bundle-rejected"]` (hidden by default) with a "Skipped" heading + `[data-role="bundle-rejected-list"]` `<ul>`. New `renderRejectedFiles` helper enumerates each entry as `<li>` with `bundle-rejected-filename` + `bundle-rejected-reason` spans. HTML-escaped via the existing `escapeHtml` helper so a hostile filename can't inject markup.
- [`src/ui/main.ts`](src/ui/main.ts): `runBundle` passes `result.rejected` (or `undefined` when empty) into the bundle-complete state.
- [`site/index.html`](site/index.html): `.bundle-rejected*` CSS — subdued styling (12px, muted color, top borders between items) so the block reads as informational rather than alarming.

Tests in [`src/ui/states.test.ts`](src/ui/states.test.ts) (3 new): (1) two-file rejection list renders both entries with filename + reason text; (2) when no `rejected` field is passed, the Skipped block stays `hidden`; (3) HTML-escaping defends against `<script>`-in-filename.

`npm run typecheck && lint && test && build` all green; **2169/2169 tests + 2 skips**.

### v3 empty-state copy + structured error messages hookup (spec-v3 §63) (2026-05-26) — ✅ complete

Wires the v3 `EMPTY_STATE_COPY` constants (in [`src/ui/v3/copy.ts`](src/ui/v3/copy.ts), already exported but never rendered) into both the runtime renderer and the static HTML, and exposes the `V3_ERROR_COPY` lookup through a new optional `code` field on the `error` state. Closes the last spec-v3 §63 affordance: "Updated empty-state copy mentions the new families. Updated error states cover regulator-specific failures."

- [`src/ui/v3/copy.ts`](src/ui/v3/copy.ts): `EMPTY_STATE_COPY` rewritten so the headline mentions PDF/DOCX **and** "up to four" (keeps the [`tests/integration/static-html.test.ts`](tests/integration/static-html.test.ts) "PDF or DOCX" assertion AND the existing `v3-ui.test.ts` /four/ assertion green) and the sub enumerates the new families (BAA, DPA, NDA, MSA, EU SCC, UK Addendum, vendor security exhibits, AI addenda, certificates of insurance) plus the "analysis runs locally · nothing is uploaded" privacy claim.
- [`src/ui/states.ts`](src/ui/states.ts): empty template now interpolates `EMPTY_STATE_COPY.headline` + `.sub` (single source of truth). New `data-role="empty-headline"` + `data-role="empty-sub"` selectors. `DropzoneState.error` gains an optional `code?: string` — when supplied, the renderer calls `v3ErrorMessage(code)` and writes the structured `title` + `detail`; the freeform `message` path is the back-compat fallback.
- [`site/index.html`](site/index.html): static empty-state copy synced to match `EMPTY_STATE_COPY` (initial paint before JS hydration shows the same words the renderer produces).
- [`src/ui/dropzone.ts`](src/ui/dropzone.ts): hidden-input `aria-label` updated to match the new headline so screen-reader users get the same announcement as visual users.

Tests: empty-state test in [`src/ui/states.test.ts`](src/ui/states.test.ts) updated to assert /four/ + BAA / DPA / SCC / "nothing is uploaded" in the sub. Two new tests added: (1) error state with `code: "scc-module-2-empty-annex"` renders the structured title + detail; (2) error state without a code preserves the freeform-message fallback.

`npm run typecheck && lint && test && build` all green; **2166/2166 tests + 2 skips**.

### v3 cross-doc consistency toggle in bundle-complete (spec-v3 §62) (2026-05-26) — ✅ complete

Closes the last UI affordance of spec-v3 §62 ("A toggle invites the user to run cross-document consistency checks (default on)"). [`src/ui/states.ts`](src/ui/states.ts) bundle-complete template adds a `<label data-role="cross-doc-toggle">` wrapping a checkbox (`[data-role="cross-doc-toggle-input"]`, checked by default). Rendered only when `document_count >= 2`. Flipping the checkbox (a) rewrites the cross-doc summary line in place ("Cross-document consistency disabled." when off; "{N} cross-document finding(s)." / "No cross-document inconsistencies found." when on), (b) invokes the new optional `on_consistency_toggle: (active: boolean) => void` callback on the bundle-complete state. `state.cross_doc_active` (default `true`) seeds the initial UI checkbox state.

Engine side: [`src/ui/pipeline.ts`](src/ui/pipeline.ts) `PipelineOptions` gains `cross_doc_consistency?: boolean`. When set to `false`, `runBundlePipeline` calls `runConsistency` with `rules: []` instead of `ALL_CONSISTENCY_RULES`, so the consistency pass yields a valid `ConsistencyRun` with empty findings + execution_log and the bundle DOCX/JSON renderer reports "0 cross-document findings".

UI wiring: [`src/ui/main.ts`](src/ui/main.ts) `runBundle` now accepts an `options.cross_doc_consistency` flag, passes it into `runBundlePipeline`, surfaces it on the rendered `bundle-complete` state as `cross_doc_active`, and wires `on_consistency_toggle` to re-run `runBundle(dz, files, { cross_doc_consistency: active })`. The re-run path re-ingests (the bundle pipeline is monolithic today); a future refactor mirroring the single-doc `prepareDocument` / `runReport` split could skip re-ingest the way compliance-frame chip toggles do — flagged in the inline comment.

CSS: [`site/index.html`](site/index.html) gains a `.cross-doc-toggle` rule (min-height 32px for WCAG 2.5.8 target size, 16×16 native checkbox).

Tests: 3 new unit tests in [`src/ui/states.test.ts`](src/ui/states.test.ts) — (1) toggle hidden by default rendering only when `document_count >= 2`, default checked, initial summary reflects findings count; (2) flipping the checkbox rewrites the summary to "Cross-document consistency disabled." and invokes `on_consistency_toggle(false)`, flipping back restores the count and invokes `on_consistency_toggle(true)`; (3) toggle stays hidden when only one document is in the bundle.

`npm run typecheck && lint && test && build` all green.

### v4 multi-doc offline-verification spec landed (2026-05-22) — ✅ complete

[`tests/e2e/v4/no-network.spec.ts`](tests/e2e/v4/no-network.spec.ts) committed. The spec covers LAUNCH row v4-d (spec-v4.md §8 — folder upload / zip / multi-file drop must produce zero cross-origin requests). It mirrors the structure of `tests/e2e/v3/no-network.spec.ts`: load the page, capture all network requests from that point forward, drive the multi-doc drop, wait for a download button, assert valid OOXML zip magic bytes (`PK\x03\x04` + > 1024 bytes), and assert the captured cross-origin request list is empty.

**Forward-compatible probe pattern.** The multi-doc ingest library (`src/ingest/multi.ts`) is production-ready but the live `site/index.html` dropzone (`src/ui/dropzone.ts` `bindDropzone`) still reads only `input.files?.[0]`. The v4 multi-doc DOM affordances — `[data-role="multi-doc-dropzone"]`, `input[type="file"][multiple]` or `input[type="file"][webkitdirectory]` inside `#dropzone`, `[data-role="bundle-download"]` — are not yet wired. The spec probes for any one of these selectors and calls `test.skip` when none are found, matching the pattern in `tests/e2e/v3/a11y-keyboard.spec.ts` for the v3 chip-row probes. The skip is lifted automatically the moment the v4 UI hookup wires `src/ingest/multi.ts` into the live page — no changes to this spec are needed.

**Fixture strategy.** The spec resolves two committed `.docx` fixtures (`tests/fixtures/contracts/mutual-nda.docx` + `tests/fixtures/contracts/bad-nda.docx`). If only one exists, it copies the available fixture to a temp dir under a distinct name so the multi-file input sees two separate logical paths (browsers may deduplicate identical paths in a multi-file `FileList`). If neither fixture exists, the spec skips.

**Gate state.** `npm run lint && typecheck && test && build` all green. `npm run e2e` is not run (Playwright not installed in this env); the spec skips gracefully when the multi-doc UI affordances are absent, so it is safe as a CI gate today and will activate automatically after the UI hookup lands.

### v4 cross-doc bundle expansion + wiring fix (5 → 8) (2026-05-22) — ✅ complete

**Wiring bug found and fixed.** `tests/golden/v4/bundle.test.ts` was passing `CONSISTENCY_RULES` (V3-only, CC-001 through CC-007) to `runEngineMulti`, meaning no V4 CROSS-* rule had ever fired in the bundle test harness since the bundle test was introduced. The fix: import and use `ALL_CONSISTENCY_RULES` (the combined `[...V3_CC, ...V4_CROSS]` registry exported from `src/engine/consistency/index.ts`) instead. The V4 rules themselves were correct — the bug was solely in the test wiring.

**Three new bundle fixtures** added under `tests/golden/v4/bundles/`:

- `defined-term-drift/` (msa.txt + dpa.txt) — MSA defines "Customer Data" narrowly (data submitted to the Services); the companion DPA expands it to include personal data, derived analytics, and data generated from end users. Exercises **CROSS-DEFTERM-001** (defined-term semantic drift across documents).
- `missing-companion-dpa/` (msa.txt + sow.txt) — MSA and SOW bundle with no DPA present, even though the MSA explicitly references a Data Processing Agreement as required by applicable law. Exercises **CROSS-MISSING-001** (companion document required but absent from the bundle).
- `precedence-clash/` (msa.txt + sow.txt) — MSA says "this MSA shall control and take precedence over all such documents"; the SOW says "this Statement of Work shall govern and the specific terms herein shall prevail over the general terms of the MSA." Exercises **CROSS-PRECEDENCE-001** (conflicting order-of-precedence clauses across documents).

**Existing five bundles now also surface CROSS findings** (goldens regenerated):

- `cap-mismatch` → `CROSS-AMOUNT-001`, `CROSS-MISSING-001`
- `clean-msa-baa` → `CROSS-MISSING-001`
- `effective-date-paradox` → `CROSS-DATE-001`
- `governing-law-mismatch` → `CC-005`, `CROSS-DATE-001`, `CROSS-JURIS-001`, `CROSS-MISSING-001` (×2)
- `party-name-conflict` → `CROSS-MISSING-001`

Every CROSS rule (all 7: PARTY, JURIS, DEFTERM, DATE, AMOUNT, MISSING, PRECEDENCE) now has at least one dedicated bundle that exercises it end-to-end. The debug scratch test (`tests/debug-cross.test.ts`) is deleted. Full gate green: `npm run lint && typecheck && test && build` (2083 passing, 2 skipped).

### v4 fail-fixture corpus expansion (57 → 60) — fourth failure mode (insurance / trust-estate / settlement — every v4 sub-domain now ≥4 fail-fixtures, completing leg 4) (2026-05-22) — ✅ complete

Completes leg 4 of the spec §16 "2–4 fixtures per family" upper bound. Adds a fourth failure-mode fixture for the final three v4 sub-domains (insurance, trust-estate, settlement), each exercising a distinct load-bearing rule not covered by any prior fixture. Every v4 sub-domain (B–P) now carries ≥4 end-to-end fail-fixtures. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `insurance-policy-summary-missing-limits-of-liability-fail.txt` — Commercial General Liability Policy Summary with the preamble extended by a sentence that affirmatively elects not to include a separately captioned coverage-magnitude provision (deliberately avoiding "limits of liability", "limit of liability", "limits of insurance", standalone "limit", "each occurrence", "per claim", and "aggregate"). The Limits-of-Liability section is removed; "per-occurrence deductible" retains a hyphen so the "each occurrence" anchor is not formed. **INS-003** (critical) fires because its three `present_patterns` `/(limits?\s+of\s+(liability|insurance)|limit)/i`, `/(each\s+occurrence|per\s+claim)/i`, and `/(aggregate|general\s+aggregate)/i` are no longer matched anywhere in the document; limits define the insurer's maximum exposure and must appear on the declarations so the insured and contracting parties can verify the coverage stack and total risk-transfer headroom.
- `trust-revocable-living-missing-lifetime-distribution-fail.txt` — Revocable Living Trust with the preamble extended by a sentence that affirmatively elects not to include a separately captioned provision addressing how the Trustee shall manage trust assets for the Settlor's benefit during the Settlor's life (deliberately avoiding "during settlor's/grantor's lifetime", "while settlor is living", "income", "principal", and "distribut"). The "Distribution Upon Death" section is renamed "Death Disposition" and the operative verb is changed from "distribute" to "pay out". **EST-013** (critical) fires because its two `present_patterns` `/(during\s+(settlor.?s|grantor.?s)\s+lifetime|while\s+settlor\s+(is\s+)?living)/i` and `/(income|principal|distribut)/i` are no longer matched anywhere in the document; without explicit lifetime-distribution direction, ambiguity about whether the Trustee may distribute principal can defeat estate-planning intent.
- `settlement-mutual-release-missing-scope-of-claims-fail.txt` — Mutual Release with the preamble extended by a sentence that affirmatively elects not to include a separately captioned scope-of-release clause specifying the breadth of claims discharged (deliberately avoiding "any and all … claims", "each and every … claims", and "known or unknown … claims"). The Release body uses "all claims expressly asserted in the Action" rather than the broad formulation. **SET-002** (critical) fires because its two `present_patterns` `/(any\s+and\s+all|each\s+and\s+every)\s+claims?/i` and `/(known\s+(or|and)\s+unknown|known\s+or\s+unknown)\s+claims?/i` are no longer matched anywhere in the document; without explicit scope language a court may construe the release narrowly and leave residual claims alive.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`insurance-policy-summary` / `revocable-living-trust` / `mutual-release`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins sixty fail-fixtures (was fifty-seven); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (2074 passing, 2 skipped). Insurance / trust-estate / settlement now carry four end-to-end fail-fixtures each. Leg 4 complete — every v4 sub-domain now at the spec §16 "2–4 fixtures per family" upper bound.

### v4 fail-fixture corpus expansion (54 → 57) — fourth failure mode (ip-licensing / m-and-a / regulatory-prose) (2026-05-22) — ✅ complete

Continues the fourth leg of the spec §16 "2–4 fixtures per family" target. Adds a fourth failure-mode fixture for the next three v4 sub-domains (ip-licensing, m-and-a, regulatory-prose), each independent from the prior three and exercising a distinct load-bearing rule not yet covered by any earlier fixture. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `ip-licensing-copyright-missing-section-106-rights-fail.txt` — Author's Rights License Agreement (pinned to `copyright-license` playbook) with the preamble extended by a sentence that affirmatively elects not to include a separately captioned enumeration of the specific statutory exclusive rights being made available (deliberately avoiding "reproduce/reproduc", "copy", "distribut", "display", "perform", "derivative works", "section 106", and "17 u.s.c. § 106"). "Distributor Inc." is renamed to "Acme Media Holdings Inc." to eliminate the "distribut" substring; the grant clause uses "utilize and exploit commercially" in place of the § 106 enumeration; "Copyright License Agreement" becomes "Author's Rights License Agreement" and all "copyright" occurrences are replaced to eliminate "copy". **IPL-021** fires because its two `present_patterns` `/(reproduc|copy|distribut|display|perform|derivative\s+works?)/i` and `/(section\s+106|17\s+u\.?s\.?c\.?\s+§?\s*106)/i` are no longer matched anywhere in the document; 17 U.S.C. § 106 enumerates six exclusive rights and a license that does not enumerate which are granted leaves the scope of permitted use off the four corners.
- `m-and-a-asset-purchase-missing-governing-law-fail.txt` — Asset Purchase Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned choice-of-law and venue-selection provision (deliberately avoiding "governing law", "jurisdiction" near "Delaware"/"Chancery" within 40 chars, and "forum"). The preamble uses "choice-of-law and venue-selection" as a paraphrase so "governing law" never appears; "Delaware corporation" is retained but no "jurisdiction" or "forum" keyword appears within 40 chars of it. **MNA-019** (critical) fires because its two `present_patterns` `/governing\s+law/i` and `/(jurisdiction|forum).{0,40}(chancery|delaware)/is` are no longer matched anywhere in the document; Delaware Chancery is the standard forum for private-target M&A and without an explicit governing-law clause the parties face a conflicts-of-laws fight over which state's body of commercial law governs reps, indemnification, and closing mechanics.
- `regulatory-form-d-missing-offering-amount-fail.txt` — Form D Narrative with the preamble extended by a sentence that affirmatively elects not to include a separately captioned offering-size and investor-floor provision (deliberately avoiding "offering size", "offering amount", "offering aggregate", "minimum investment", "minimum subscription", and any "$NNN" dollar amounts). The Offering Amount section is removed entirely so no dollar amounts appear in the document, and "total capital sought" / "per-investor floor" are used as paraphrases in the preamble. **REG-006** fires because its three `present_patterns` `/(offering\s+(size|amount|aggregate))/i`, `/(minimum\s+(investment|subscription))/i`, and `/\$\s*[\d,]+/` are no longer matched anywhere in the document; Form D Items 13–14 require aggregate offering amount and minimum-investment information, and without them the regulator and prospective investors have no four-corners answer to the capital ceiling or buy-in threshold.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`copyright-license` / `asset-purchase-agreement` / `form-d-narrative`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins fifty-seven fail-fixtures (was fifty-four); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (2062 passing, 2 skipped). IP-licensing / m-and-a / regulatory-prose now carry four end-to-end fail-fixtures each.

### v4 fail-fixture corpus expansion (51 → 54) — fourth failure mode (compliance / construction / healthcare) (2026-05-22) — ✅ complete

Continues the fourth leg of the spec §16 "2–4 fixtures per family" target. Adds a fourth failure-mode fixture for the next three v4 sub-domains (compliance-policy, construction, healthcare), each independent from the prior three and exercising a distinct load-bearing rule not yet covered by any earlier fixture. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `compliance-anti-bribery-missing-gifts-hospitality-fail.txt` — Anti-Bribery Policy with the preamble extended by a sentence that affirmatively elects not to include a separately captioned things-of-value provision (deliberately avoiding "gifts", "gift", "hospitality", "entertainment", "meals", "charitable", "political contribution", "threshold", "pre-approval", and "recordkeeping" — using paraphrases "courtesy items", "social-function invitations", "philanthropic and electoral-giving contributions", "per-occasion dollar caps", "advance-authorization workflows", and "logging obligations" instead). **POL-011** fires because its three `present_patterns` `/(gifts?|hospitality|entertainment|meals)/i`, `/(charitable|political\s+contribution)/i`, and `/(threshold|pre.?approval|recordkeeping)/i` are no longer matched anywhere in the document; gifts and hospitality to foreign officials are the most common FCPA risk area (DOJ-SEC FCPA Resource Guide ch. 2) and without a policy setting monetary thresholds, a pre-approval workflow, and recordkeeping requirements, the compliance program has no operational control over the highest-frequency corruption vector.
- `construction-contract-missing-scope-of-work-fail.txt` — Construction Contract with the preamble extended by a sentence that affirmatively elects not to include a separately captioned work-description and document-hierarchy provision (deliberately avoiding "scope of the work", "scope of work", "work to be performed", "contract documents", "drawings and specifications", and "specifications"). **CON-002** (critical) fires because its two `present_patterns` `/(scope\s+of\s+(the\s+)?work|work\s+to\s+be\s+performed)/i` and `/(contract\s+documents|drawings\s+and\s+specifications|specifications)/i` are no longer matched anywhere in the document; AIA A201 § 1.1.1 defines the Contract Documents (Agreement + Conditions + Drawings + Specifications + addenda + Modifications) and without an enumerated scope and document hierarchy neither party has a four-corners answer to what the contractor is obligated to build or which drawings and specs govern change-order disputes.
- `healthcare-phi-authorization-missing-conditioning-statement-fail.txt` — PHI Authorization with the preamble extended by a sentence that affirmatively elects not to include a separately captioned conditioning-prohibition provision (deliberately avoiding "treatment", "payment", "enrollment", "eligibility", "benefits", "not conditioned", "cannot refuse", and "cannot condition"). The Patient Rights section is rewritten to remove "treatment" and "conditioned"; the Description section replaces "treatment notes" with "clinical notes". **HC-016** (critical) fires because its two `present_patterns` `/(treatment|payment|enrollment|eligibility|benefits)/i` and `/(not\s+conditioned|cannot\s+(refuse|condition)|will\s+not\s+(affect|condition))/i` are no longer matched anywhere in the document; 45 C.F.R. § 164.508(c)(2)(ii) requires a statement that treatment, payment, enrollment, or eligibility for benefits is not conditioned on the authorization — without one the patient cannot verify that refusing to sign carries no care-denial consequence.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`anti-bribery-policy` / `construction-contract` / `phi-authorization`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins fifty-four fail-fixtures (was fifty-one); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (2050 passing, 2 skipped). Compliance-policy / construction / healthcare now carry four end-to-end fail-fixtures each.

### v4 fail-fixture corpus expansion (48 → 51) — fourth failure mode (banking / equity / real-estate) (2026-05-22) — ✅ complete

Continues the fourth leg of the spec §16 "2–4 fixtures per family" target. Adds a fourth failure-mode fixture for the next three v4 sub-domains (banking, equity, real-estate), each independent from the prior three and exercising a distinct load-bearing rule not yet covered by any earlier fixture. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `banking-loan-agreement-missing-affirmative-covenants-fail.txt` — Loan Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned borrower-undertakings provision (deliberately avoiding "affirmative covenants", "financial statements", "books and records", and "maintenance"). No "affirmative covenants" or financial-statements / books-and-records / maintenance anchor appears anywhere in the document. **BNK-009** fires because its two `present_patterns` `/affirmative\s+covenants?/i` and `/(financial\s+statements?|books\s+and\s+records|maintenance)/i` are no longer matched anywhere in the document; affirmative covenants are the lender's information-rights backbone and without them the lender has no contractual hook to monitor deteriorating credit quality before it matures into a payment default.
- `equity-safe-missing-liquidity-event-fail.txt` — SAFE with the preamble extended by a sentence that affirmatively elects not to include a separately captioned exit-event treatment provision (deliberately avoiding "liquidity event", "change of control", and "acquisition"). The document uses "strategic combination" and "purchase of the Company's outstanding shares" as paraphrases. **EQT-004** fires because its three `present_patterns` `/liquidity\s+event/i`, `/change\s+of\s+control/i`, and `/acquisition/i` are no longer matched anywhere in the document; without an express liquidity-event clause the investor's exit treatment is undefined and the SAFE may leave the investor with no contractual right to any proceeds on a sale of the Company prior to an Equity Financing.
- `real-estate-net-lease-missing-maintenance-repair-fail.txt` — Triple Net Lease with the preamble extended by a sentence that affirmatively elects not to include a separately captioned upkeep-and-remediation provision (deliberately avoiding "maintenance and repair", "roof", "structural integrity", and "structural repair"). No "maintenance and repair" or roof / structural anchor appears anywhere in the document. **RE-004** fires because its two `present_patterns` `/maintenance\s+and\s+repair/i` and `/(roof|structural)\s+(integrity|repair|maintenance)/i` are no longer matched anywhere in the document; the landlord-vs-tenant upkeep allocation is the heart of a single-tenant NNN lease and without it the parties have no four-corners answer to the most common category of NNN disputes.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`loan-agreement` / `safe-yc` / `net-lease`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins fifty-one fail-fixtures (was forty-eight); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (2038 passing, 2 skipped). Banking / equity / real-estate now carry four end-to-end fail-fixtures each.

### v4 fail-fixture corpus expansion (45 → 48) — fourth failure mode (governance / employment / privacy) (2026-05-22) — ✅ complete

Begins the fourth leg of the spec §16 "2–4 fixtures per family" target for the first three v4 sub-domains (governance, employment, privacy-extended), each exercising a distinct load-bearing rule not yet covered by any prior fixture. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `governance-bylaws-missing-board-composition-fail.txt` — Bylaws document where "board of directors" is paraphrased throughout as "governing body" and the annual-meeting sentence reads "annual governance-body election" rather than "election of directors". The preamble is extended by a sentence that affirmatively elects not to include a separately captioned governance-body composition and election article, relying instead on the residual authority supplied by DGCL § 141 and the certificate of incorporation. **GOV-006** (critical) fires because its two `present_patterns` `/board\s+of\s+directors/i` and `/(elect|appoint).{0,30}directors?/i` are no longer matched anywhere in the document; DGCL § 141(b) and MBCA § 8.03 require the bylaws (or charter) to fix the number of directors and the manner of election — without a board-composition clause the proxy materials and internal records have no four-corners anchor for board size or election procedure.
- `employment-executive-missing-409a-compliance-fail.txt` — Executive Employment Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned deferred-compensation-tax-compliance provision, relying instead on applicable federal income tax law and company-wide tax-compliance policies. No "section 409A", "409A", "specified employee", or "six-month delay" anchor appears anywhere in the document. **EMP-003** (critical) fires because its two `present_patterns` `/(section\s+409a|\b409a\b)/i` and `/(specified\s+employee|six.month\s+delay)/i` are no longer matched anywhere in the document; IRC § 409A triggers a 20% additional tax plus interest on the executive for deferred-compensation violations, and a § 409A compliance recital (six-month delay for specified employees, reformation authority) is universal in modern executive agreements because severance and deferred bonuses are inherently § 409A-sensitive.
- `privacy-cookie-notice-missing-consent-mechanism-fail.txt` — Cookie Notice with the preamble extended by a sentence that affirmatively elects not to include a separately captioned opt-in mechanism section, relying instead on users' browser settings and supervisory-authority guidance. The Consent and Withdrawal sections are removed; "cookie management tool" is absent. No "consent", "banner", "preference center", "cookie preference", "accept", "reject", or "manage" anchor appears anywhere in the document. **PRV-002** (critical) fires because its three `present_patterns` `/consent/i`, `/(banner|preference\s+center|cookie\s+preference)/i`, and `/(accept|reject|manage)/i` are no longer matched anywhere in the document; ePrivacy Art. 5(3) + GDPR Art. 7 require freely given, specific, informed, and unambiguous affirmative-action consent (CJEU *Planet49*) — routing users to generic browser controls satisfies neither the granular opt-in requirement nor the equal-prominence withdraw standard EDPB Guidelines 5/2020 contemplate.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`bylaws-corporation` / `executive-employment` / `cookie-notice`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins forty-eight fail-fixtures (was forty-five); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Governance / employment / privacy now carry four end-to-end fail-fixtures each.

### v4 fail-fixture corpus expansion (42 → 45) — third failure mode (ip-licensing / m-and-a / insurance, via new sub-playbooks) (2026-05-22) — ✅ complete

Completes the third leg of the spec §16 "2–4 fixtures per family" target across **every** v4 sub-domain. The remaining three sub-domains (ip-licensing, m-and-a, insurance) each had only one silent presence-rule slot left on their existing playbook (copyright-license / asset-purchase-agreement / insurance-policy-summary) and that slot was either unfireable (IPL-021's `/copy/` matches "copyright") or covered. This batch therefore widens each sub-domain to a **second playbook** within the same family, yielding the third fixture per sub-domain. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `ip-licensing-patent-missing-licensed-patents-fail.txt` — Patent License Agreement (pinned to the `patent-license` playbook, a different IPL sub-domain playbook from the copyright-license series). The Exhibit reference uses "Exhibit A" rather than "Schedule A" to avoid the rule's `/schedule\s+a/i` anchor, and the preamble uses the paraphrase "industrial-monopoly grants" so the literal anchor "licensed patents" does not appear anywhere in the document. **IPL-007** fires because its two `present_patterns` `/(licensed\s+patents?|schedule\s+a)/i` and `/(patent\s+no|u\.?s\.?\s+pat|application\s+no)/i` are no longer matched anywhere in the document; patent licenses must identify the licensed claims with sufficient specificity for both enforcement and royalty accounting.
- `m-and-a-loi-missing-binding-demarcation-fail.txt` — Letter of Intent (pinned to the `loi-term-sheet` playbook). The doc affirmatively states it "is binding upon the parties" and includes a Confidentiality section — pattern 2 and pattern 3 of MNA-001 match, but pattern 1 (`/(non.?binding|not\s+binding|except\s+(as|for))/i`) does not, so the compound rule's `min_match=3` threshold is missed (2/3) and **MNA-001** fires. The *SIGA / Pennzoil* line shows that LOIs can be enforced as binding contracts if the parties do not clearly disclaim intent to be bound on commercial terms — a doc that says only "is binding upon the parties" without demarcating the non-binding commercial provisions invites unintended enforcement.
- `insurance-endorsement-coverage-restricting-fail.txt` — Endorsement (pinned to the `insurance-endorsement` playbook) styled as a Commercial General Liability Endorsement — Absolute Pollution Exclusion. The body affirmatively states "This endorsement is an absolute exclusion of pollution and applies in addition to all other terms of the Policy" — the language-rule's first bad pattern `/(absolute\s+exclusion|total\s+exclusion).{0,80}(pollution|cyber|communicable\s+disease|silica|asbestos|abuse|molest)/is` matches and **INS-010** fires. Coverage-restricting endorsements (absolute pollution / cyber / communicable-disease exclusions, $25K sublimits) materially shrink coverage and should be flagged to underwriters and the broker.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`patent-license` / `loi-term-sheet` / `insurance-endorsement`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins forty-five fail-fixtures (was forty-two); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). **Milestone:** every v4 sub-domain now carries three end-to-end fail-fixtures, completing the third leg of spec §16 "2–4 fixtures per family" across the full v4 family.

### v4 fail-fixture corpus expansion (39 → 42) — third failure mode (banking / real-estate / settlement) (2026-05-22) — ✅ complete

Continues the third leg of the spec §16 "2–4 fixtures per family" target. Adds a third failure-mode fixture for the next three v4 sub-domains (banking, real-estate, settlement), each independent from the prior two and exercising a distinct load-bearing rule. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `banking-loan-agreement-missing-default-acceleration-fail.txt` — Loan Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned remedial-mechanic provision (deliberately avoiding "default", "events of default", "accelerat", "insolvency", "bankruptcy", and "payment default"). The Events-of-Default section is removed so no "default" or "bankruptcy/insolvency" substring appears anywhere in the document. **BNK-005** fires because its three `present_patterns` `/(events?\s+of\s+default|default)/i`, `/accelerat/i`, and `/(insolvenc|bankruptc|payment\s+default)/i` are no longer matched anywhere in the document; without an enumerated default and acceleration mechanic the payee must wait for maturity to sue and may face unjust-enrichment / cure arguments — the doc's reliance on the Lender's published collection practices is not a substitute for in-contract remedial-trigger machinery.
- `real-estate-net-lease-missing-nnn-cost-allocation-fail.txt` — Single-Tenant Commercial Lease (derived from the NNN minimal but rewritten as a "fully-burdened single-tenant arrangement") with the preamble extended by a sentence that affirmatively elects not to include a separately captioned tenant-cost-assumption provision. The "Real Estate Taxes" section is renamed "Ad Valorem Assessments" so the rule's 80-char-windowed `/real\s+estate\s+taxes?.{0,80}(tenant)/is` co-occurrence cannot match through the heading-to-tenant proximity that joining-newlines would otherwise create. **RE-001** fires because its four `present_patterns` `/triple.net/i`, `/\bnnn\b/i`, `/real\s+estate\s+taxes?.{0,80}(tenant)/is`, and `/(cam|common\s+area\s+maintenance|operating\s+expenses)/i` are no longer matched anywhere in the document; the economic essence of an NNN lease is tenant assumption of three expense categories and silence converts the lease to gross by default.
- `settlement-mutual-release-missing-releasing-parties-fail.txt` — Mutual Release (pinned to the `mutual-release` playbook since SET-001 applies only to that playbook id) with the preamble extended by a sentence that affirmatively elects not to include a separately captioned identification provision naming the persons and entities on each side. The Release body uses bare "Plaintiff / Defendant" language without any extended-identification recitations. **SET-001** fires because its two `present_patterns` `/releas(or|ee|ing\s+part(y|ies)|ed\s+part(y|ies))/i` and `/(affiliates?|subsidiaries|officers|directors|agents)/i` are no longer matched anywhere in the document; an effective release must bind every entity intended to benefit — failure to enumerate affiliates, officers, directors, agents, and assigns leaves residual claims against unnamed parties.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`loan-agreement` / `net-lease` / `mutual-release`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins forty-two fail-fixtures (was thirty-nine); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Banking / real-estate / settlement now carry three end-to-end fail-fixtures each.

### v4 fail-fixture corpus expansion (36 → 39) — third failure mode (equity / regulatory / trust) (2026-05-22) — ✅ complete

Continues the third leg of the spec §16 "2–4 fixtures per family" target. Adds a third failure-mode fixture for the next three v4 sub-domains (equity, regulatory-prose, trust-estate), each independent from the prior two and exercising a distinct load-bearing rule. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `equity-safe-interest-bearing-fail.txt` — SAFE with the preamble narrating an affirmative departure from the YC post-money form by adding an interest-accrual mechanic, and a new Interest section stating "This SAFE shall bear interest at a rate of 6% per annum, compounded annually". This is the third **language-quality** (bad-pattern present) v4 row-b fail. **EQT-008** fires because both bad-language patterns `/this\s+safe\s+(shall|will)\s+(bear|accrue)\s+interest/i` and `/interest\s+(rate|accrues|shall\s+accrue)\s+(at|of)\s+\d/i` match; a SAFE is not a debt instrument, interest accrual is inconsistent with the YC template, may indicate a convertible note in disguise, and is state-law-sensitive under usury caps.
- `regulatory-form-d-missing-blue-sky-fail.txt` — Form D Narrative with the preamble extended by a sentence that affirmatively elects not to include a separately captioned non-federal-jurisdiction-coordination provision (deliberately avoiding "blue-sky", "state notice", "NSMIA", "notice filing", and "state securities"). The Blue Sky section is removed. **REG-007** fires because its two `present_patterns` `/(blue.sky|state\s+notice|nsmia)/i` and `/(notice\s+filing|state\s+securities)/i` are no longer matched anywhere in the document; most states require notice filings for Reg D offerings (NSMIA-preempted but most states require Form D copies + filing fees), and failure to file blocks resale.
- `trust-revocable-living-missing-pour-over-fail.txt` — Revocable Living Trust with the preamble extended by a sentence that affirmatively elects not to include a separately captioned probate-coordination provision (deliberately avoiding "pour-over", "pourover", "will", and "last will"). The Pour-Over section is removed entirely so no "will" or "pour-over" substring appears anywhere in the document. **EST-015** fires because its two `present_patterns` `/(pour.?over|pourover)/i` and `/(will|last\s+will)/i` are no longer matched anywhere in the document; pour-over wills devise residuary to the trust and coordination prevents probate of trust-funded property — without a pour-over reference, unfunded probate property goes through intestacy and defeats the trust's estate-planning purpose.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`safe-yc` / `form-d-narrative` / `revocable-living-trust`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins thirty-nine fail-fixtures (was thirty-six); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Equity / regulatory-prose / trust-estate now carry three end-to-end fail-fixtures each.

### v4 fail-fixture corpus expansion (33 → 36) — third failure mode (compliance / construction / healthcare) (2026-05-22) — ✅ complete

Continues the third leg of the spec §16 "2–4 fixtures per family" target. Adds a third failure-mode fixture for the next three v4 sub-domains (compliance-policy, construction, healthcare), each independent from the prior two and exercising a distinct load-bearing rule. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `compliance-anti-bribery-missing-books-and-records-fail.txt` — Anti-Bribery Policy with the preamble extended by a sentence that affirmatively elects not to include a separately captioned financial-records-and-controls provision (deliberately avoiding "books and records", "accounting records / controls", "internal controls", "78m", "accurate", and "accuracy"). The Books-and-Records section is removed entirely. **POL-008** fires because its three `present_patterns` `/(books\s+and\s+records|accounting\s+(records|controls))/i`, `/(internal\s+(accounting\s+)?controls?|78m)/i`, and `/(accurate|accuracy)/i` are no longer matched anywhere in the document; 15 U.S.C. § 78m(b)(2) requires issuers to maintain accurate books and records and adequate internal accounting controls — violations can be charged without underlying bribery, so the policy must affirmatively address the accounting-provisions half of FCPA enforcement.
- `construction-contract-missing-indemnification-insurance-fail.txt` — Construction Contract with the preamble extended by a sentence that affirmatively elects not to include a separately captioned risk-allocation provision (deliberately avoiding "indemnify", "indemnification", "insurance", "CGL", "workers compensation", and "waiver of subrogation"). The Insurance-and-Bonds section is narrowed to just Bonds (removing the insurance language). **CON-006** fires because its three `present_patterns` `/(indemnif(y|ies|ied|ication))/i`, `/(insurance|cgl|workers?\s+compensation)/i`, and `/(waiver\s+of\s+subrogation)/i` are no longer matched anywhere in the document; AIA A201 §§ 3.18 + 11 provide the standard pattern and state anti-indemnity statutes (CA Civ. § 2782, NY Gen. Oblig. § 5-322.1, TX Ins. § 151) void indemnity for owner's own / sole negligence — the carve-outs must be in the contract, a separate Owner-Controlled Risk Management Program is not a substitute.
- `healthcare-phi-authorization-missing-purpose-fail.txt` — PHI Authorization with the preamble extended by a sentence that affirmatively elects not to include a separately captioned reason-for-disclosure provision (deliberately avoiding "purpose", "at the request of", and "for litigation / life insurance / treatment / payment / research / marketing"). The Purpose section is removed, "treatment notes" in the Description is changed to "clinical notes" so `for treatment` cannot match, and the Recipient is renamed from "Insurance Underwriter Inc." to "Acme Underwriting Inc." so "for life insurance" never appears. **HC-013** fires because its two `present_patterns` `/purpose/i` and `/(at\s+the\s+request\s+of|for\s+(litigation|life\s+insurance|treatment|payment|research|marketing))/i` are no longer matched anywhere in the document; 45 C.F.R. § 164.508(c)(1)(iv) requires a description of each purpose, and an authorization without one fails the meaningful-and-specific standard and is unenforceable on its face.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`anti-bribery-policy` / `construction-contract` / `phi-authorization`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins thirty-six fail-fixtures (was thirty-three); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Compliance-policy / construction / healthcare now carry three end-to-end fail-fixtures each.

### v4 fail-fixture corpus expansion (30 → 33) — third failure mode (governance / employment / privacy) (2026-05-22) — ✅ complete

Begins the third leg of the spec §16 "2–4 fixtures per family" target. With every v4 sub-domain at two end-to-end fail-fixtures from the prior batch, this batch starts adding a **third** failure-mode fixture for the first three v4 sub-domains (governance, employment, privacy-extended), each independent from the prior two and exercising a distinct load-bearing rule. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `governance-bylaws-overbroad-exclusive-forum-fail.txt` — Bylaws with the preamble narrating a comprehensive exclusive-forum election and a new Exclusive Forum section designating the Court of Chancery of the State of Delaware as the exclusive forum for any claim asserted against the corporation, including without limitation any claim arising under the Securities Exchange Act of 1934 or any other federal securities laws. **GOV-011** fires because its bad-language pattern `/exclusive\s+forum.{0,120}(state\s+of\s+delaware|chancery).{0,200}(exchange\s+act|1934\s+act|federal\s+securities)/is` matches; *Salzberg v. Sciabacucchi* (Del. 2020) upheld Delaware-forum bylaws for Securities Act of 1933 claims but expressly declined to extend that holding to Exchange Act claims (which carry exclusive federal-court jurisdiction under § 27) — an exclusive-forum bylaw that purports to capture Exchange Act claims is at risk of being struck on petition. This is the second **language-quality** (bad-pattern present) row-b fail; every prior governance/employment/privacy entry was a presence-rule (clause missing) fail.
- `employment-executive-missing-restrictive-covenants-fail.txt` — Executive Employment Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned post-employment-behavior provision (avoiding the rule's anchor phrases by paraphrase: "constraining the Executive's ability to compete", "communicate confidential information", "recruit Employer personnel after termination"). The Restrictive Covenants section is removed entirely. **EMP-008** fires because its two `present_patterns` `/restrictive\s+covenant/i` and `/(non.?disclosure|non.?solicit)/i` are no longer matched anywhere in the document; executives need NDA + non-solicit (and non-compete only where enforceable) coverage, and without an in-document restrictive-covenants clause or reference the Employer's post-termination protections rest entirely off the four corners.
- `privacy-cookie-notice-missing-third-party-recipients-fail.txt` — Cookie Notice with the preamble extended by a sentence that affirmatively elects not to include a separately captioned downstream-disclosure provision (deliberately avoiding "third-party", "recipients", "international transfers", "outside the EU/EEA/UK", "SCC", "DPF", and "Data Privacy Framework" — the preamble uses "downstream-counterparty identification" so neither "recipients" nor "third-party" appears anywhere in the document). The Third-Party Cookies section is removed. **PRV-006** fires because its three `present_patterns` `/(third.?party|recipients?)/i`, `/(international\s+transfers?|outside\s+the\s+(eu|eea|uk))/i`, and `/(scc|dpf|data\s+privacy\s+framework)/i` are no longer matched anywhere in the document; GDPR Art. 13(1)(e)–(f) requires disclosure of recipients and international-transfer mechanisms, and cookie data routinely flows to third-party analytics / ad-tech in the US and elsewhere.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`bylaws-corporation` / `executive-employment` / `cookie-notice`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins thirty-three fail-fixtures (was thirty); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Row v4-b's third leg has begun; governance / employment / privacy-extended now carry three end-to-end fail-fixtures each.

### v4 fail-fixture corpus expansion (27 → 30) (2026-05-22) — ✅ complete

Advanced LAUNCH row v4-b coverage from twenty-seven end-to-end fail-fixtures to thirty by adding a second failure-mode fixture for the final three v4 sub-domains (insurance, trust-estate, settlement). With this batch every v4 sub-domain (B–P) now carries at least two end-to-end fail-fixtures, completing the second leg of the spec §16 "2–4 fixtures per family" target across the full v4 family. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `insurance-policy-summary-missing-named-insured-fail.txt` — Commercial General Liability Policy Summary derived from the existing premium-deductible fail-fixture by renaming the "Named Insured" line to "Insured Party" and the "Producer" line to "Insurance Intermediary" (with the broker entity renamed to "Insurance Wholesale Services LLC" so neither "broker" nor "agent" appears anywhere in the document). The preamble narrates the affirmative election not to include a party-and-intermediary identification provision. **INS-001** fires because its two `present_patterns` `/named\s+insured/i` and `/(producer|broker|agent)/i` are no longer matched anywhere in the document; coverage rights run to the Named Insured under state insurance codes, and without a clearly labelled "Named Insured" and a stated producer / broker the declarations are off the four corners.
- `trust-revocable-living-missing-death-distribution-fail.txt` — Revocable Living Trust with the preamble extended by a sentence that affirmatively elects not to include a separately captioned post-passing apportionment provision (deliberately avoiding "upon settlor's death", "after death", "beneficiar", and "distribute"). The Distribution-Upon-Death section is removed entirely so neither "distribute" nor "beneficiary" appears anywhere in the document. **EST-014** fires because its two `present_patterns` `/(upon\s+(settlor.?s|grantor.?s)\s+death|after\s+death)/i` and `/(beneficiar|distribute)/i` are no longer matched anywhere in the document; the post-death disposition is the trust's principal estate-planning function, and without named beneficiaries and stated shares the trust corpus is at risk of intestate disposition through the pour-over will rather than the planned distribution.
- `settlement-confidential-overbroad-future-claims-fail.txt` — Confidential Settlement Agreement and Mutual Release (pinned to the `mutual-release` playbook since SET-005 applies only to that playbook id) with the preamble extended by a sentence affirmatively electing to extend the release beyond the conduct underlying the Action to bind the releasing party with respect to prospective claims of any kind whatsoever, and the Mutual Release section amplified with "any future claims that may arise hereafter against the other party, including any prospective claims of any kind whatsoever". This is the first **language-quality** (bad-pattern present) v4 row-b fail-fixture — every prior entry has been a presence-rule (clause missing) fail. **SET-005** fires because its two bad-language patterns `/releases?.{0,80}(future|hereafter\s+arising|may\s+arise).{0,80}claims?/is` and `/(future|prospective)\s+claims?.{0,80}(of\s+any\s+kind|whatsoever)/is` both match; California Civ. Code § 1668 voids contracts that exempt a party from responsibility for their own fraud, willful injury, or violation of law, and many jurisdictions are skeptical of releases of unaccrued claims — a release that purports to cover post-execution conduct is at risk of being limited or voided.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`insurance-policy-summary` / `revocable-living-trust` / `mutual-release`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins thirty fail-fixtures (was twenty-seven); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). **Milestone:** every v4 sub-domain (B–P) now carries at least two end-to-end fail-fixtures, completing the second leg of spec §16 "2–4 fixtures per family" across the full v4 family.

### v4 fail-fixture corpus expansion (24 → 27) (2026-05-22) — ✅ complete

Advanced LAUNCH row v4-b coverage from twenty-four end-to-end fail-fixtures to twenty-seven by adding a second failure-mode fixture for the next three v4 sub-domains (ip-licensing, m-and-a, regulatory-prose), each deriving from the corresponding minimal-pass fixture by extending the preamble with a sentence that affirmatively elects not to include a second load-bearing clause and dropping every textual anchor that the rule's `present_patterns` look for. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `ip-licensing-copyright-missing-term-territory-media-fail.txt` — Copyright License Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned enumeration of the geographic-and-durational scope of the license. The Term-and-Termination section is renamed "Duration and Discontinuance" and the Licensee is renamed from "Acme Media Inc." to "Acme Studios Inc." so the substring `media` does not appear anywhere in the document. **IPL-022** fires because its three `present_patterns` `/(term|territor)/i`, `/(media|channels?|format)/i`, and `/(now\s+known|hereafter\s+developed)/i` are no longer matched anywhere in the document; term + territory + media define the boundary of the license — open-ended grants in unfamiliar media invite *Bourne v. Walt Disney*-style disputes when new exploitation surfaces emerge, and an in-document scope must control over a separately negotiated rate card. (IPL-021 was the original target but its `/copy/i` sub-pattern matches the substring "copy" anywhere in the document — including the word "copyright" — so it can never fire on a copyright license; this is a known limitation of the rule's regex and is recorded here for future tightening.)
- `m-and-a-asset-purchase-missing-assumed-liabilities-fail.txt` — Asset Purchase Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned schedule enumerating the specific liability categories that Buyer is contractually undertaking at the Closing, relying instead on Schedule 2.4 and Schedule 2.5 to list the closed sets without using the term "Assumed Liabilities". The Assumed-Liabilities body becomes a "Retained Obligations" section so no "assumed liabilities" anchor remains. **MNA-022** fires because its `present_pattern` `/assumed\s+liabilities/i` is no longer matched anywhere in the document; the structural advantage of an APA is that Buyer assumes only specifically-identified liabilities, and without an Assumed Liabilities clause the implicit carve-in fails and Buyer faces ambiguity about which Seller obligations transfer at Closing.
- `regulatory-form-d-missing-bad-actor-fail.txt` — Form D Narrative with the preamble extended by a sentence that affirmatively elects not to include a separately captioned screening-and-disclosure provision addressing Rule 506(d) bad-actor disqualification, relying instead on the Issuer's separately executed officer-and-director questionnaires and outside-counsel sign-off. The Bad-Actor Disqualification section is removed. **REG-005** fires because its two `present_patterns` `/(bad.actor|rule\s+506\(d\)|disqualif)/i` and `/(covered\s+persons?|disqualifying\s+events?)/i` are no longer matched anywhere in the document; Rule 506(d) disqualifies covered persons (directors, officers, 20%+ owners) with disqualifying events from relying on Rule 506, and an issuer that omits the screening recital cannot show it performed the reasonable-care diligence the rule contemplates.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`copyright-license` / `asset-purchase-agreement` / `form-d-narrative`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins twenty-seven fail-fixtures (was twenty-four); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Row v4-b now adds a second failure mode for the ip-licensing / m-and-a / regulatory-prose sub-domains as the fourth leg of the spec §16 "2–4 fixtures per family" target.

### v4 fail-fixture corpus expansion (21 → 24) (2026-05-22) — ✅ complete

Advanced LAUNCH row v4-b coverage from twenty-one end-to-end fail-fixtures to twenty-four by adding a second failure-mode fixture for the next three v4 sub-domains (compliance-policy, construction, healthcare), each deriving from the corresponding minimal-pass fixture by extending the preamble with a sentence that affirmatively elects not to include a second load-bearing clause and dropping every textual anchor that the rule's `present_patterns` look for. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `compliance-anti-bribery-missing-ukba-cross-border-fail.txt` — Anti-Bribery Policy with the preamble extended by a sentence that affirmatively elects not to include a separately captioned non-US-statute applicability provision, relying instead on the Compliance Officer's quarterly memorandum and the Company's separately published Global Code of Conduct. The Compliance-with-Law section drops the UKBA reference. **POL-010** fires because its two `present_patterns` `/(uk\s+bribery\s+act|ukba|bribery\s+act\s+2010)/i` and `/(failure\s+to\s+prevent|cross.border|adequate\s+procedures)/i` are no longer matched anywhere in the document; UKBA is broader than FCPA (commercial bribery + corporate "failure to prevent" offense + no facilitating-payments exception + broad jurisdictional reach), and the SFO has used the corporate-offense theory in active enforcement since 2017.
- `construction-contract-missing-time-of-completion-fail.txt` — Construction Contract with the preamble extended by a sentence that affirmatively elects not to include a separately captioned schedule-and-delay-remedy provision, relying instead on the Project Manager's published schedule of values and the separately executed Master Schedule. The Schedule section is removed and the Payment section's retainage anchor reads "until Final Acceptance" instead of "until Substantial Completion". **CON-004** fires because its two `present_patterns` `/(substantial\s+completion|final\s+completion|completion\s+date)/i` and `/(liquidated\s+damages|delay\s+damages|no\s+damages\s+for\s+delay)/i` are no longer matched anywhere in the document; AIA A101 § 3 requires commencement, substantial-completion, and final-completion dates — schedule slip is the single largest source of construction disputes and without those anchors owner-suffered delay damages have no contractually defined trigger.
- `healthcare-phi-authorization-missing-right-to-revoke-fail.txt` — PHI Authorization with the preamble extended by a sentence that affirmatively elects not to include a separately captioned cancellation-mechanic provision, relying instead on the Covered Entity's published Notice of Privacy Practices and the Privacy Officer's standard intake procedures. The Expiration section drops the revocation sentence; the Patient Rights section replaces "except as permitted by" with "subject to the limits set forth in" to avoid the "except" anchor. **HC-015** fires because its three `present_patterns` `/(revoke|revoking|revocation)/i`, `/(in\s+writing|written\s+notice)/i`, and `/(except|reliance)/i` are no longer matched anywhere in the document; 45 C.F.R. § 164.508(c)(2)(i) requires a statement of the right to revoke in writing and the exceptions — without one, the patient has no four-corners hook to withdraw consent and the covered entity has no instruction for when reliance-based disclosures may still proceed.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`anti-bribery-policy` / `construction-contract` / `phi-authorization`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins twenty-four fail-fixtures (was twenty-one); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Row v4-b now adds a second failure mode for the compliance / construction / healthcare sub-domains as the third leg of the spec §16 "2–4 fixtures per family" target.

### v4 fail-fixture corpus expansion (18 → 21) (2026-05-22) — ✅ complete

Advanced LAUNCH row v4-b coverage from eighteen end-to-end fail-fixtures to twenty-one by adding a second failure-mode fixture for the next three v4 sub-domains (banking, equity, real-estate), each deriving from the corresponding minimal-pass fixture by extending the preamble with a sentence that affirmatively elects not to include a second load-bearing clause and dropping every textual anchor that the rule's `present_patterns` look for. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `banking-loan-agreement-missing-interest-rate-fail.txt` — Loan Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately titled pricing provision, relying instead on the Lender's published standard rate schedule and a separately executed Pricing Letter. The Interest section is removed and the lender renamed to `Northwind Bank N.A.` so the preamble's "captioned"-flavored wording can be avoided (preamble uses "titled" instead) and the substring `cap` (which `/cap/i` would otherwise pick up from "Capital Bank") does not appear anywhere in the document. **BNK-008** fires because its three `present_patterns` `/(sofr|prime\s+rate|libor)/i`, `/(margin|spread|basis\s+points|bps)/i`, and `/(floor|cap|cap\s+and\s+floor)/i` are no longer matched anywhere in the document; post-LIBOR transition (June 2023) the index is SOFR-based and the rate-reset mechanic must be in the agreement, not in a side letter.
- `equity-safe-missing-conversion-event-fail.txt` — SAFE with the preamble extended by a sentence that affirmatively elects not to include a separately captioned conversion-trigger definition, relying instead on the Company's customary subscription documents to specify the conversion mechanic on any qualifying capital event. **EQT-002** fires because its three `present_patterns` `/equity\s+financing/i`, `/preferred\s+stock\s+financing/i`, and `/next\s+priced\s+round/i` are no longer matched anywhere in the document; YC's post-money template uses "Equity Financing" or "Standard Preferred Stock" as the trigger, and absent an in-document definition the investor has no four-corners trigger for conversion.
- `real-estate-net-lease-missing-tax-passthrough-fail.txt` — Triple Net Lease with the preamble extended by a sentence that affirmatively elects not to include a separately captioned ad-valorem-charge pass-through provision, relying instead on the assessor's published bill-payer record and Landlord's customary year-end statement. **RE-002** fires because its two `present_patterns` `/real\s+estate\s+taxes?/i` and `/(impositions|property\s+taxes)/i` are no longer matched anywhere in the document; direct payment shifts tax-bill risk and reimbursement keeps landlord on the bill — an NNN lease must specify which mechanic governs to keep both parties out of an avoidable post-loss allocation fight.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`loan-agreement` / `safe-yc` / `net-lease`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins twenty-one fail-fixtures (was eighteen); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Row v4-b now adds a second failure mode for the banking / equity / real-estate sub-domains as the second leg of the spec §16 "2–4 fixtures per family" target.

### v4 fail-fixture corpus expansion (15 → 18) (2026-05-22) — ✅ complete

Advanced LAUNCH row v4-b coverage from fifteen end-to-end fail-fixtures to eighteen by adding a second failure-mode fixture for the first three v4 sub-domains (governance, employment, privacy-extended), each deriving from the corresponding minimal-pass fixture by extending the preamble with a sentence that affirmatively elects not to include a second load-bearing clause and dropping every textual anchor that the rule's `present_patterns` look for. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `governance-bylaws-missing-indemnification-fail.txt` — Bylaws with the preamble extended by a sentence that affirmatively elects not to include a separately captioned director-and-officer protection article, relying instead on the residual authority supplied by DGCL § 145 and separately executed advancement-of-expenses contracts. **GOV-008** fires because its `present_pattern` `/indemnif(ication|y|ied)/i` is no longer matched anywhere in the document; DGCL § 145 authorizes indemnification but is not self-executing — without a bylaw clause electing into the statutory authority, directors and officers lose the contractual hook for the protection.
- `employment-executive-missing-base-salary-fail.txt` — Executive Employment Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned cash-compensation schedule, relying instead on a separately executed compensation letter and the Employer's standard payroll practices. **EMP-002** fires because its three `present_patterns` `/base\s+salary/i`, `/annual\s+(bonus|incentive)/i`, and `/target\s+bonus/i` are no longer matched anywhere in the document; Reg S-K Item 402 requires disclosure of compensation arrangements for named executive officers, and without a stated base salary and bonus structure the central economic terms of the engagement are off the four corners.
- `privacy-cookie-notice-missing-withdraw-consent-fail.txt` — Cookie Notice with the preamble extended by a sentence that affirmatively elects not to include a separately captioned consent-withdrawal section, relying instead on the user's ability to clear stored cookies through browser controls and lodge a supervisory-authority complaint. **PRV-004** fires because its two `present_patterns` `/(withdraw|change|update|revoke).{0,40}consent/i` and `/(cookie\s+(preferences?|settings)|preference\s+center)/i` are no longer matched anywhere in the document; GDPR Art. 7(3) requires that withdrawing consent be as easy as giving it, and routing the user to generic browser controls fails the equal-prominence standard EDPB Guidelines 5/2020 contemplate.

Each fail-fixture has a `.playbook` sidecar pinning the v4 playbook (`bylaws-corporation` / `executive-employment` / `cookie-notice`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins eighteen fail-fixtures (was fifteen); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` (test count captured in commit message). Row v4-b now covers every v4 sub-domain B–P with at least one fail-fixture and adds a second failure mode for the governance / employment / privacy sub-domains as the first leg of the spec §16 "2–4 fixtures per family" target.

### v4 fail-fixture corpus expansion (12 → 15) (2026-05-22) — ✅ complete

Advanced LAUNCH row v4-b coverage from twelve end-to-end fail-fixtures to fifteen by adding one fail-fixture for each of the three previously-uncovered v4 sub-domains (insurance, regulatory-prose, trust-estate). Each new fixture exercises a distinct load-bearing rule that prior fixtures did not. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `insurance-policy-summary-missing-premium-deductible-fail.txt` — Commercial General Liability Policy Summary with the preamble extended by a sentence that affirmatively elects not to include a separately captioned economic-terms provision (deliberately avoiding "premium", "deductible", "self-insured retention", and "SIR"). **INS-004 (critical)** fires because its two `present_patterns` `/premium/i` and `/(deductible|self.?insured\s+retention|sir)/i` are no longer matched anywhere in the document; without a stated premium and the insured's retained-risk threshold, the parties cannot verify the consideration paid or the attachment point at which the insurer's payment obligation begins.
- `regulatory-form-d-missing-exemption-fail.txt` — Form D Narrative with the preamble extended by a sentence that affirmatively elects not to include a separately captioned safe-harbor-identification provision (deliberately avoiding "Rule 504 / 506 / 506(b) / 506(c)" and "exempt / exemption"). **REG-002** fires because its two `present_patterns` `/(rule\s+50[46]|rule\s+506\(b\)|rule\s+506\(c\))/i` and `/(exempt|exemption)/i` are no longer matched anywhere in the document; Form D Item 6 requires identification of the exemption claimed, and the difference between 506(b) (no general solicitation, reasonable belief) and 506(c) (general solicitation permitted, verified accredited status) is the central regulatory question for a Reg D offering.
- `trust-revocable-living-missing-spendthrift-fail.txt` — Revocable Living Trust with the preamble extended by a sentence that affirmatively elects not to include a separately captioned third-party-claim-protection provision (deliberately avoiding "spendthrift", "creditor", "involuntary transfer", and "attachment"). **EST-016** fires because its two `present_patterns` `/spendthrift/i` and `/(creditor|involuntary\s+transfer|attachment)/i` are no longer matched anywhere in the document; UTC § 502 enforces spendthrift provisions restraining both voluntary and involuntary transfers of beneficial interests, and without one a beneficiary's judgment creditors can reach the beneficial interest and defeat the trust's estate-planning purpose.

Each fail-fixture has a `.playbook` sidecar forcing the v4 playbook (so the auto-detector cannot route to `generic-fallback`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins fifteen fail-fixtures (was twelve); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1894 passing + 2 skips** (was 1885; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards). Row v4-b now covers every v4 sub-domain B–P with at least one fail-fixture.

### v1.1 extractor precision pass (2026-05-12) — ✅ complete

Closed every item in the v1.1 extractor backlog tracked in [`LAUNCH.md`](LAUNCH.md). Eight detection gaps fixed, corresponding tests re-enabled, goldens regenerated, full gate (`lint` + `typecheck` + `test` + `build`) green. Details in the `[Unreleased]` section of [`CHANGELOG.md`](CHANGELOG.md).

### Initial-load bundle split (2026-05-12) — ✅ complete

Closed LAUNCH.md row (l) at the bundle-architecture level. `src/ui/pipeline.ts` extracted from `main.ts`; dynamic-imported on first file drop / drag-over / `requestIdleCallback`. Initial-load JS shrank **560 KB → 9.51 KB** (gzipped 165 KB → 3.75 KB), ~45×. Added `tests/integration/determinism-guard.test.ts` — every fixture runs 5× in-process and asserts a single `result_hash` per fixture.

### Vendor chunk splitting + end-to-end report test (2026-05-12) — ✅ complete

Followed up on the bundle-split work with `manualChunks` in `vite.config.ts`: every heavy dep now lives in its own vendor chunk (`vendor-pdfjs` 378 KB, `vendor-mammoth` 496 KB, `vendor-docx` 350 KB, `vendor-tesseract` 16 KB, `vendor-decimal` 32 KB, `vendor-zod` 54 KB). Vaulytica's own pipeline code is its own ~115 KB chunk. With the year-long immutable cache from Step 14, a bump to one dep only invalidates that chunk for returning users. Added `tests/integration/end-to-end-report.test.ts` — runs the live engine against each fixture and asserts `buildDocxReport` produces a valid OOXML zip + `buildJsonReport` round-trips the `result_hash`.

### Fixture sanity guards + RISK-009 broadening + parallel playbook fetch (2026-05-12) — ✅ complete

Added `tests/integration/fixture-sanity.test.ts` — pins down the rule IDs that **must** fire for each bad-* fixture so a regression that silently drops a finding is caught even when the `result_hash` legitimately drifts. **Broadened RISK-009's regex** to match the canonical `(liable|responsible) for all damages…without limitation` phrasing. **`ensurePlaybooks` parallelized** via `Promise.all`. Bad-nda detection rose from 2/5 → 3/5 intentional violations caught.

### STRUCT-013 placeholder rule + 81-rule registry (2026-05-12) — ✅ complete

Added **STRUCT-013 — Unfilled template placeholders (critical, structural)** to push `bad-nda.docx` detection to **4/5** intentional violations. Regex coverage: `[insert …]`, `[Title-Case Name]`, `[TBD]` / `[REDACTED]` / `[PLACEHOLDER]` / `[PENDING]` / `[TBA]` / `[FILL IN]` / `[TODO]`, `{{mustache}}`, `<<angle>>`, `XXX`-runs of 3+ uppercase X, underscore-line placeholders of 10+ chars. Doesn't fire on `[1]` or `[a]` footnotes. Wired into `LAUNCH_RULES` (now **81 rules**), the 80→81 count assertion in `all-rules.test.ts` updated, 9 dedicated rule tests added, `fixture-sanity.test.ts` updated, goldens regenerated.

### Subresource Integrity (2026-05-12) — ✅ complete

Closed the SRI hardening item from `docs/threat-model.md`. New `subresourceIntegrity` Vite plugin runs after `deployAssets` and rewrites every same-origin `<script src=…>` and `<link rel="modulepreload|preload|stylesheet" href=…>` in `dist/index.html` to carry an `integrity="sha384-…"` + `crossorigin="anonymous"` pair derived from the on-disk asset bytes. If the served bytes don't hash to the declared value, the browser refuses to execute the bundle. `tests/integration/sri.test.ts` runs `npm run build` end-to-end and verifies (1) the integrity attribute is present, (2) the hash matches `sha384(disk)` byte-for-byte, (3) no malformed integrity attrs, (4) the main asset is non-trivial in size. `threat-model.md` updated: "Vaulytica-tampered builds" section now says SRI hardens the entry; "Verification procedure" gains step 6 (view source on the live site, recompute locally to compare).

### FIN-001/002 skip-lift on NDA playbooks (2026-05-12) — ✅ complete

Both rules have narrow pattern matchers that only fire on monetary content actually appearing in the document, so the original "skip on NDAs to avoid false positives" was over-cautious. `mutual-nda.json` and `unilateral-nda.json` no longer skip FIN-001 or FIN-002; FIN-003 through FIN-008 (fees, payment terms, late fees, etc.) remain skipped because they legitimately don't apply. **`bad-nda.docx` intentional-violation detection: 4/5 → 5/5** (full coverage; the previously-unaddressed word/numeral mismatch `fifty thousand dollars ($75,000)` now fires under FIN-001). Sanity guard updated; goldens regenerated.

### Rule catalog expansion to 83 (2026-05-12) — ✅ complete

Added two new rules:
- **DARK-005 — Class-action waiver (critical, dark-patterns)**: catches `waives class action`, `gives up collective action`, `no class action`, `on an individual basis only`. Cites the FTC deception statement; relevant since Concepcion (2011) + Epic Systems (2018). 6 dedicated tests.
- **STRUCT-014 — Inconsistent defined-term casing (info, structural)**: when `"Confidential Information"` is defined but `confidential information` (lowercase) appears elsewhere, flags the slip. Skips single-word defined terms and sentence-start lowercase. 5 dedicated tests.

`LAUNCH_RULES` now ships **83 rules**; all count assertions in `all-rules.test.ts` bumped (81→83 + describe header). Goldens regenerated.

### Rule catalog expansion to 86 (2026-05-12) — ✅ complete

Added three more rules:
- **TEMP-011 — Auto-renewal notice window under 30 days (warning, temporal)**: parses the number of days from the non-renewal notice clause and fires when < 30. Cites FTC Negative Option Rule (`stat-16-cfr-425`). The example rule from `docs/adding-a-rule.md` is now a real implementation; the doc was updated to point at the shipped code and to use the actual `emit(ctx, rule, …)` helper signature. 5 dedicated tests.
- **RISK-015 — Indemnification without aggregate cap (warning, risk-allocation)**: fires when indemnification language is present and either no cap exists or the cap explicitly carves out indemnification (`limited to twelve months… except for indemnification obligations`). 5 dedicated tests.
- **DARK-006 — Asymmetric pre-suit notice / cure window (warning, dark-patterns)**: detects clauses requiring one party (Customer / Employee / Licensee / Tenant / Contractor / Consumer / User / Buyer) to give pre-suit notice or a cure period, without imposing the same gate on the drafter. Skips bilateral framings like `each party shall…`. 4 dedicated tests.

`LAUNCH_RULES` now ships **86 rules**; all count assertions in `all-rules.test.ts` bumped (83→86 + describe header). Goldens regenerated.

### Rule catalog expansion to 89 (2026-05-12) — ✅ complete

Added three more rules in under-covered categories (obligations / ip-and-data / choice-and-venue):
- **OBLI-007 — Material Adverse Change clause present (warning, obligations)**: surfaces `material adverse change` / `material adverse effect` / `MAC event` for explicit review. Doesn't fire on bare `material breach`. 5 dedicated tests.
- **IPDATA-007 — Data retention period unspecified (warning, ip-and-data)**: fires when the contract handles data (Customer Data / personal data / PII / DPA / data processing) but no clause specifies retention duration, deletion, return-or-destroy, or purge. Aligns with GDPR Art. 5(1)(e) and CCPA retention-definition expectations. 5 dedicated tests.
- **CHOICE-009 — Governing law differs from venue jurisdiction (info, choice-and-venue)**: surfaces contracts where the choice-of-law jurisdiction is different from the venue / forum jurisdiction. Uses the jurisdictions extractor's normalized `jurisdiction_id` when available; falls back to text comparison. 4 dedicated tests.

`LAUNCH_RULES` now ships **89 rules**; all count assertions in `all-rules.test.ts` bumped (86→89 + describe header). Goldens regenerated.

### Rule catalog expansion to 92 (2026-05-12) — ✅ complete

Added three more rules — STRUCT-015 (numbered-section gap detector that walks the section outline), PERS-005 (non-compete clause surface with jurisdiction-enforceability caveat), TERM-009 (asymmetric termination-for-convenience detection requiring both a drafter convenience right AND a counterparty cure-period gate). `LAUNCH_RULES` now ships **92 rules**; all count assertions in `all-rules.test.ts` bumped (89→92 + describe header). Goldens regenerated.

### Rule catalog expansion to 95 (2026-05-12) — ✅ complete

Added three more rules in lighter categories:
- **FIN-009 — Late fee above 18%/year (warning, financial)**: parses month/year/day-period rates, normalizes to annual, fires when > 18%. Cites N.Y. Gen. Oblig. Law § 5-501. 6 dedicated tests.
- **IPDATA-008 — Cross-border data transfer without safeguard (warning, ip-and-data)**: fires when transfer-outside-EEA/EU/UK/US language appears without SCCs / BCRs / adequacy decision / DPF / Article 46 reference. 6 dedicated tests.
- **RISK-016 — Insurance requirement without coverage minimum (warning, risk-allocation)**: fires when `maintain insurance` appears without a `$X per occurrence`, `at least $X`, or `not less than $X` minimum. 6 dedicated tests.

`LAUNCH_RULES` now ships **95 rules**; count assertions bumped (92→95 + describe header). Goldens regenerated.

### Rule catalog expansion to 98 (2026-05-12) — ✅ complete

Added three more rules covering personnel, dark-patterns, and temporal gaps:
- **PERS-006 — Non-disparagement clause present (warning, personnel)**: surfaces non-disparagement language for review against NLRB *McLaren Macomb* (Feb 2023) and SEC Rule 21F-17. 4 dedicated tests.
- **DARK-007 — Browsewrap / passive-acceptance language (warning, dark-patterns)**: detects passive-consent constructs (`by using the Service`, `continued use constitutes acceptance`, `deemed to have agreed`). Cites *Specht v. Netscape*, *Nguyen v. Barnes & Noble*, *Berkson v. Gogo*. 5 dedicated tests.
- **TEMP-012 — Survival clause silent on sticky obligations (warning, temporal)**: when survival language exists AND confidentiality / IP / indemnity language exists, the rule names every sticky obligation the survival clause failed to enumerate. 5 dedicated tests.

`LAUNCH_RULES` now ships **98 rules**; count assertions bumped (95→98 + describe header). Goldens regenerated.

### 🎯 100-rule milestone (2026-05-12) — ✅ complete

Two final rules to reach the round number:
- **OBLI-008 — Efforts standard undefined (info, obligations)**: surfaces `best efforts` / `commercially reasonable efforts` / `reasonable efforts` / `good faith efforts` / `diligent efforts` when no in-document definition exists. Different efforts-standard phrases carry materially different obligation strengths (*Bloor v. Falstaff*, 2d Cir. 1979). 5 dedicated tests.
- **CHOICE-010 — Asymmetric jury-trial waiver (warning, choice-and-venue)**: fires when a jury-trial waiver binds one named party without binding the drafter. Cites *Leasing Service Corp. v. Crane* (4th Cir. 1986). 5 dedicated tests.

`LAUNCH_RULES` now ships **100 rules** (was 80 at v1.0.0 — +25%); count assertions bumped (98→100 + describe header). Goldens regenerated. **370/370 passing + 2 intentional skips.** All ten rule categories better-covered than launch:

| Category | v1.0.0 | now |
|----------|--------|-----|
| Structural | 12 | 15 |
| Financial | 8 | 9 |
| Temporal | 10 | 12 |
| Obligations | 6 | 8 |
| Risk allocation | 14 | 16 |
| Choice and venue | 8 | 10 |
| Termination | 8 | 9 |
| IP and data | 6 | 8 |
| Personnel | 4 | 6 |
| Dark patterns | 4 | 7 |
| **Total** | **80** | **100** |

### Fixture corpus expanded to 5 (2026-05-12) — ✅ complete

`tests/fixtures/build-fixtures.ts` now produces a 5th synthetic fixture, **`bad-employment.docx`**, targeting the `employment-at-will-us` playbook (no fixture previously exercised this surface). The fixture carries 7 intentional violations sized to exercise the post-1.0 personnel/dark-pattern rules: California non-compete (PERS-005), non-disparagement without NLRA/SEC carve-outs (PERS-006), asymmetric termination-for-convenience (TERM-009), one-sided jury-trial waiver (CHOICE-010), undefined `best efforts` (OBLI-008), class-action waiver (DARK-005), survival clause silent on confidentiality + IP (TEMP-012). All 7 fire and are locked in by `tests/integration/fixture-sanity.test.ts`. The matched playbook is correctly `employment-at-will-us` (confidence 0.875). Golden + 8 new sanity assertions added.

### Fixture corpus expanded to 7 (2026-05-12) — ✅ complete

Added **`bad-lease.docx`** (lease-commercial-multitenant playbook @ 0.7 confidence) and **`bad-contractor.docx`** (independent-contractor playbook @ 0.8 confidence). bad-lease carries 7 commercial-real-estate-specific violations (Premises placeholder, 10-day non-renewal window, 36%/year late fee, bare insurance clause, uncapped indemnification, asymmetric pre-suit notice, one-sided jury waiver) — all 7 fire. bad-contractor sets up the misclassification dark pattern (fixed hours, company tools, exclusivity) and surfaces the policy concern through 5 rules. Sanity-guard entries added; goldens generated. Pushes the row-(m) corpus from **5 → 7 fixtures**.

### 🎯 Fixture corpus reaches 10 (spec §27 row-(m) target hit) (2026-05-12) — ✅ complete

Three more synthetic fixtures: **`bad-unilateral-nda.docx`** (unilateral-nda playbook @ 1.0 confidence), **`bad-residential-lease.docx`** (lease-residential-us @ 1.0), **`bad-msa.docx`** (msa-general @ 1.0). All 16 newly-required rules across the three sanity-guard entries fire. **Corpus: 7 → 10 fixtures. Spec §27 row-(m) target reached.** Direct playbook fixture coverage: 8 of 12 launch playbooks (mutual-nda, unilateral-nda, employment-at-will-us, independent-contractor, saas-customer, msa-general, lease-commercial-multitenant, lease-residential-us). LAUNCH.md row (m) flipped from ⚠️ deferred → ✅ pass.

Two adjacent fixes shipped alongside the fixtures:
- **Pipeline body-text bug**: both `src/ui/pipeline.ts` and `tests/integration/_pipeline-helpers.ts` were walking only `sections[0]` for the body text passed to `matchPlaybook`. Caused unilateral NDAs to match mutual-nda (the unilateral-specific phrasing like `the Disclosing Party` lives deeper in the doc). Both helpers now recursively walk every section + child paragraph. `bad-unilateral-nda.docx` now matches `unilateral-nda` at 1.0 confidence instead of mutual-nda at 0.8.
- **FIN-009 regex**: `Late fee: 5% per month` (with colon) slipped through. The `\s+` between the keyword and the rate was tightened to `[:\s]`. Caught by the `bad-residential-lease.docx` sanity guard.

**394/394 passing + 2 intentional skips.**

### Cross-OS test matrix + schema.org JSON-LD validation (2026-05-12) — ✅ complete

Two more LAUNCH.md rows hardened (without flipping yet, since both need a deployed surface to flip fully):

- **Row (c) cross-machine determinism**: new `.github/workflows/test-matrix.yml` runs lint + typecheck + test on `ubuntu-latest` + `macos-latest` + `windows-latest` (Node 20). Together with the existing single-machine determinism triple-guard (`all-rules.test.ts` + `golden-output.test.ts` + `determinism-guard.test.ts`), this gives the cross-machine verification the spec §17 / row (c) is asking for. Row flips to ✅ pass on the first all-green workflow run.
- **Row (k) FAQ schema validity**: new `tests/integration/schema-org.test.ts` machine-validates all four schema.org JSON-LD blocks in `site/index.html` (Organization + SoftwareApplication + TechArticle + FAQPage) with 8 assertions covering every Rich Results required field. Caught the missing `author` on TechArticle — fixed in `site/index.html` by attributing the article to the Vaulytica organization. The live Google Rich Results probe still has to run against the deployed URL, but the static-shape half is machine-enforced now.

**402/402 passing + 2 intentional skips.**

### Static HTML validation (rows h + j) (2026-05-12) — ✅ complete

Two more LAUNCH.md rows hardened to the static-shape limit:

- **Row (j) OG / Twitter Card meta tags**: new `tests/integration/static-html.test.ts` ships 9 OG/Twitter assertions. Every required meta is present; URL is the production domain; OG title ≤ 70 chars and OG description ≤ 200 chars (each platform's truncation budget); `og:type` is `website`; `twitter:card` is `summary_large_image`; viewport + theme-color metas present. The live link-preview test on each platform still has to run against the deployed URL.
- **Row (h) Static accessibility**: same test file ships 11 a11y assertions. `<html lang>`, charset meta, `<main>` / `<nav>` / `<footer>` landmarks, `alt` on every `<img>`, `aria-label` + `tabindex="0"` on every non-native `role="button"`, no `<a href="#">` placeholder anchors, multi-nav `aria-label` discipline. The formal axe DevTools audit still runs against the deployed page.

20 new tests; both rows now have a static-shape guard that catches regressions in CI before they ship. **422/422 passing + 2 intentional skips.**

### PERS-007 (rule 101) + bad-saas-vendor + bad-consulting (corpus 10 → 12) (2026-05-12) — ✅ complete

Added **PERS-007 — IC misclassification signals (warning, personnel; 101st rule)**: fires when a worker is labeled `independent contractor` AND ≥2 employee-indicator clauses appear (fixed daily hours, company-supplied equipment, daily reporting, exclusivity, salary-like flat monthly retainer, required on-site presence). Cites IRS 20-factor + DOL economic-realities + CA AB-5 + MA prong-B tests. 6 dedicated tests.

Added **`bad-saas-vendor.docx`** (vendor-side SaaS — aggressive uptime via best efforts, IP indemnity carved out of cap) and **`bad-consulting.docx`** (hybrid IC + advisory with classic misclassification dark pattern). The new PERS-007 rule fires on bad-consulting, validating the rule end-to-end against a fixture. Note: bad-saas-vendor matches `saas-customer` (not `saas-vendor`) because both playbooks share most features for a generic SaaS doc; sanity guard is playbook-agnostic. **Playbook fixture coverage: 8 → 10 of 12 launch playbooks.** Corpus: 10 → 12 fixtures. **436/436 passing + 2 intentional skips.**

### bad-sow + playbook coverage enforcement + Lighthouse CI (2026-05-12) — ✅ complete

Three substantive additions:
1. **`bad-sow.docx` (13th fixture)** — targets the `sow` playbook (child of `msa-general`). Matched `sow` at 0.9 confidence. Under-defined deliverables + scope-creep clause + 2%/month late fee + `[TBD]` placeholder + undefined `best efforts`. Fires STRUCT-013 + FIN-009 + OBLI-008.
2. **`tests/integration/playbook-coverage.test.ts`** — for every id in `LAUNCH_PLAYBOOK_IDS`, asserts that some committed fixture matches it under the deterministic matcher OR that it's listed in `EXEMPT_PLAYBOOK_IDS` with a stated reason. Today's exempt list: `generic-fallback` (implicit) and `saas-vendor` (overlap with saas-customer). **A new playbook added without a fixture now fails CI.** 13 assertions.
3. **Lighthouse CI workflow + budgets (LAUNCH.md row l)** — new `.github/workflows/lighthouse.yml` + `lighthouserc.json` run Lighthouse against the built `dist/` on every push and PR with the mobile-4G throttled preset. CI fails if FCP > 1500 ms, LCP > 2000 ms, TTI > 2000 ms, TBT > 200 ms, CLS > 0.1, or category scores drop below perf 0.85 / a11y 0.95 / best-practices 0.9 / SEO 0.9.

**Playbook fixture coverage: 10 → 11 of 12 launch playbooks. Only `generic-fallback` is uncovered, by design.** Test corpus: 12 → 13 fixtures. **453/453 passing + 2 intentional skips.**

## How to pick up where this left off

1. Read this file plus [`spec.md`](docs/spec.md) §26 step **N** where N is the first unchecked row.
2. Run the verification gate for the *previous* step to ensure nothing regressed (Step 0: `npm install && npm run typecheck && npm test && npm run lint && npm run build`; Step 1: open `site/index.html` in a browser, optionally `npm run dev`, run Lighthouse).
3. Implement the next step in a focused session, then update this table.

## v3 build plan (spec-v3.md Part IX)

| # | Step | Status | Notes |
|---|------|--------|-------|
| 18 | v3 repo scaffolding and namespace conventions | ✅ complete | Created `src/extract/v3/` (11 files: 9 extractor placeholders + types.ts + index.ts), `src/engine/rules/v3/` (7 subdirs with index.ts each), `src/playbooks/v3/` (19 placeholder JSON files §§35–53), `dkb/fixtures/v3/README.md`, `dkb/build/v3/README.md`, `tests/v3/README.md`, `V3.md`. Extended `PlaybookSchema` with 4 optional fields (`regulator_frame`, `applicable_jurisdictions`, `companion_playbooks`, `compliance_matrix_columns`). All gates green (typecheck / test / build / lint). |
| 19 | DKB schema extensions | ✅ complete | New v3 node types live at `src/dkb/v3/` (`types.ts` + `schema.ts` + `index.ts`): `regulator_model_form`, `statutory_clause_requirement`, `transfer_mechanism`, `subprocessor_requirement`, `insurance_norm`, `consistency_check`. Each node carries `dkb_node_version: number`, `dkb_node_last_validated_at: ISO8601`, and `cites: PinnedCitation[]` (authority/citation/source_url/content_hash_at_pin/fetched_at). Zod `V3DkbNodeSchema` is a discriminated union over `node_type` so a single call validates any kind. `src/dkb/loader.ts` re-exports `validateV3Nodes` so callers can validate v3 sidecar files. Sample fixtures at `dkb/fixtures/v3/nodes/{regulator_model_form,statutory_clause_requirement,transfer_mechanism,subprocessor_requirement,insurance_norm,consistency_check}.sample.json`. Tests at `tests/v3/dkb/schema.test.ts` (11/11 passing): per-fixture schema acceptance, discriminated-union round-trip, malformed-field rejection, bad-hash rejection, unknown-node_type rejection. |
| 20 | Source-pinning protocol implementation | ✅ complete | Staleness detector at `dkb/build/v3/staleness.ts`: `normalizeForHash` (NFC + CRLF→LF + whitespace collapse + trim) + `sha256Hex` (Node `node:crypto`, never `eval`), `detectStaleness({nodes, fetchAuthority, hashFn?, nowIso?})` returns a `StalenessReport` with one row per drifted citation, `applyStalenessToRules` flips `enabled: false` on every rule whose `depends_on_dkb_nodes` overlaps the report, `unacknowledgedRows` filters by `(node_id, citation)` against the ack list, `statusFromReport` produces the UI footer payload `{dkb_last_validated_at, stale_citations_pending_review}`. CLI at `dkb/build/v3/check-staleness.ts` loads `dkb/fixtures/v3/nodes/*.json`, replays text from `dkb/fixtures/v3/snapshots/{sha256(url)}.txt` in offline mode, writes `staleness-report.json`, and exits non-zero when unacknowledged rows exist. Ack file at repo root `dkb-staleness-ack.yml` (empty `acknowledgments: []` by default; YAML parsed with `FAILSAFE_SCHEMA` per the security rules — never `yaml.load` unsafe). UI footer added to `site/index.html` (`<footer><p data-role="dkb-validation">…</p></footer>`); `src/ui/dkb-validation.ts` exposes `hydrateDkbValidation` (fetches `/dkb/v3/validation-status.json` cache-busted, validates the payload shape, renders into the footer spans, leaves the placeholder text on failure) and is wired from `src/ui/main.ts` on `DOMContentLoaded`. Tests (16/16 new, 549/549 total passing): `staleness.test.ts` (normalize + clean + drift + rule disable + ack filter + status), `check-staleness.test.ts` (full CLI round-trip with tmpdir nodes/ack — clean, drift, ack-respected, missing-ack-file, malformed-entry), `dkb-validation.ui.test.ts` (DOM render + happy-path fetch + bad payload + fetch failure under happy-dom). `npm run typecheck && lint && test && build` all green. |
| 21 | HIPAA and US state-privacy fetchers | ✅ complete | v3 fetcher framework at `dkb/build/v3/fetchers/`: `V3Fetcher = (ctx) => Promise<V3FetcherResult>`, `V3FetchContext` carries `reader` (offline snapshot replay) + optional `HttpClient`. Shared helpers in `_common.ts` (`createFsReader`, `pin`, `sha256Hex`, `urlKey`). 12 fetchers registered in `V3_FETCHERS`: `hipaa-ecfr-title-45` (6 §164.504(e)/§164.410/§164.502(e)/§164.314(a) requirements), `hhs-sample-baa` (one `regulator_model_form` with 7 clause headings), `hhs-ocr-resolutions` (one `regulator_model_form` indexing OCR settlements), `ccpa-civ-code` (3 service-provider contract requirements from §§ 1798.100(d), 1798.140(ag)), `ccpa-regulations-11ccr` (the §7051 eight-provision requirement), `vcdpa`, `cpa`, `ctdpa`, `ucpa`, `tdpsa`, `ocpa`, `dpdpa` (one processor-contract `statutory_clause_requirement` each). Every emitted node is a discriminated-union member of `V3DkbNodeSchema` and carries a pinned cite with `content_hash_at_pin = sha256(normalizeForHash(snapshotText))` so the Step-20 staleness gate validates them automatically. Snapshot fixtures committed under `dkb/fixtures/v3/snapshots/{sha256(url)}.txt` (12 files), with the generator `dkb/build/v3/write-snapshots.ts` re-runnable as `npx tsx dkb/build/v3/write-snapshots.ts`. Tests (20 new): `tests/v3/dkb/fetchers/fetchers.test.ts` — parser positives + negatives for HIPAA / HHS sample BAA / OCR index / state privacy + the integration loop that runs every fetcher offline against vendored snapshots and `.parse()`s the output through `V3DkbNodeListSchema` + a missing-snapshot safety case. `npm run typecheck && lint && test && build` all green; 569/569 tests passing. |
| 22 | GDPR, SCC, UK IDTA, and international fetchers | ✅ complete | 12 new fetchers under `dkb/build/v3/fetchers/`: `gdpr` (6 `statutory_clause_requirement` nodes for GDPR Arts. 28(3), 32, 33, 33(2), 44, 46), `eu-scc-2021-914` (one `regulator_model_form` + one `transfer_mechanism` per Module 1–4 = 8 nodes total; Modules 2 & 3 carry Annex III subprocessor requirement), `uk-gdpr` (UK GDPR Art. 28), `uk-idta` (regulator_model_form + transfer_mechanism with Parts 1–4), `uk-addendum` (regulator_model_form + transfer_mechanism with Tables 1–4), `swiss-fadp` (FADP Art. 9), `swiss-addendum` (regulator_model_form + transfer_mechanism), `edpb-guidelines` (regulator_model_form indexing "Guidelines NN/YYYY" entries via lightweight HTML regex parse), and 4 international statute fetchers — `pipeda` (Principle 4.1.3 accountability), `lgpd` (Art. 39 operator), `appi` (Art. 25 trustee supervision), `pipl` (Art. 21 entrusted processing + Art. 38 cross-border conditions, with NPC official English translation provenance recorded in the `authority` field). Each fetcher reads from `dkb/fixtures/v3/snapshots/{sha256(url)}.txt` and pins each citation with `content_hash_at_pin = sha256(normalizeForHash(snapshotText))`. 12 new snapshot fixtures committed; `npx tsx dkb/build/v3/write-snapshots.ts` re-runs them deterministically. The Step-20 staleness gate now covers every Step-21 + Step-22 source automatically. Tests (43 in the v3 fetcher suite, up from 20): GDPR multi-article extraction, all-four-modules SCC test (with Annex III conditional verification), UK IDTA / Addendum form+mechanism shape, Swiss FADP/Addendum + EDPB index parse, PIPL translation-provenance assertion, plus the integration loop that runs every fetcher (24 total) offline through the V3 schema. `npm run typecheck && lint && test && build` all green; 592/592 tests passing. The CI matrix (Ubuntu/macOS/Windows) needs no special handling — the snapshot fetcher framework is OS-agnostic. |
| 23 | BAA ruleset and BAA playbook | ✅ complete | 45 rules live under `src/engine/rules/v3/baa/`: `_helpers.ts` exposes `buildBaaPresenceRule` (fires when none of the `present_patterns` matches the document) and `buildBaaLanguageRule` (fires when any of the `bad_patterns` matches), both shaping a `Rule` whose `applies_to_playbooks: ["baa", "baa-deep", "baa-subcontractor"]` keeps the v2 launch suite untouched. `fullText(ctx)` concatenates section headings + paragraph text so the matcher sees clauses regardless of whether the drafter put them in headings or bodies. `rules.ts` declares all 45 in a single audit-friendly list and asserts at load time that the count is exactly 45. Coverage: required clauses §164.504(e)(2)(i)–(iii) (BAA-001–012), Security Rule flow-down §164.314(a) (BAA-013–018), breach notification §164.410 (BAA-019–022 including the looser-than-60-days language detector), HHS-posture rules (BAA-023 "Security Incident narrowed to successful access", BAA-024 "return-or-destruction lacks definite outer bound", BAA-025 indemnity-cap impairment, BAA-026 audit rights, BAA-027 covered-entity-indemnifies-BA), definitions/terminology (BAA-028 PHI defined, BAA-029 minimum necessary, BAA-030 mitigation, BAA-031 workforce training, BAA-032 encryption, BAA-033 risk assessment, BAA-034 sanctions, BAA-035 subprocessor list), and posture/execution (BAA-036 signature, BAA-037 effective date, BAA-038 term, BAA-039 governing law, BAA-040 notice, BAA-041 PHI mentioned, BAA-042 preemption misdrafting, BAA-043 survival, BAA-044 post-HITECH terminology, BAA-045 CE/BA role labels). `src/engine/rules/v3/index.ts` exports `V3_RULES` (currently `[...BAA_RULES]`); `src/engine/index.ts` re-exports it; `src/ui/pipeline.ts` runs `[...LAUNCH_RULES, ...V3_RULES]`. Determinism tests for v2 use only `LAUNCH_RULES`, so the cross-OS `result_hash` regression test is unaffected. Playbook `src/playbooks/v3/baa.json` upgraded to v1.0.0 with title keywords, distinguishing phrases ("protected health information", "164.504", etc.), expected defined terms, HIPAA source citation, and a 13-column compliance matrix per §54. Tests at `src/engine/rules/v3/baa/baa-ruleset.test.ts` (10/10 passing): registry contract (45 rules / unique ids / BAA-NNN pattern / category / playbook scope), inert-when-not-BAA, compliant-fixture (zero criticals; ≤5 rules fire), six failure-mode tests (BAA-019 breach absent, BAA-020 90-day notice, BAA-023 Security Incident narrowed, BAA-024 "as soon as practicable" return, BAA-027 CE indemnifies BA, BAA-041 no PHI), determinism (two runs same `result_hash`). 602/602 tests passing; typecheck, lint, build all green. |
| 24 | DPA-GDPR ruleset and DPA-EU playbook | ✅ complete | 55 rules under `src/engine/rules/v3/dpa-gdpr/rules.ts` (DPA-001 through DPA-055). Coverage: Art. 28(3) introductory paragraph — subject-matter, duration, nature/purpose, type of personal data, categories of data subjects, controller obligations (DPA-001–006); Art. 28(3)(a)–(h) eight enumerated obligations including documented instructions, confidentiality, Article 32 security, subprocessor terms, data-subject-rights assistance, Articles 32–36 assistance, deletion-or-return, compliance demonstration (DPA-007–014); Articles 28(2)/(4)/(9) subprocessor authorisation + objection right + flow-down + written form (DPA-015–018); Article 32 security specifics — pseudonymisation/encryption, CIA-R, restore availability, regular testing + 'industry-standard' hand-waving detector (DPA-019–023); Article 33(2) breach notification + 'without undue delay' + Art. 33(3) content elements + loose-timing detector (DPA-024–027); Articles 30, 35, 27, 37 (DPA-028–031); Chapter V international transfer mechanism + EU SCCs + TIA (DPA-032–034); quality-of-text — deletion-or-return choice misallocation, audit-substitution elimination, unilateral instruction deviation (DPA-035–037); Annexes I/II/III scope (DPA-038–040); GDPR terminology + Controller/Processor role labels (DPA-041–042); posture/execution — signature, effective date, term, governing law, liability allocation, controller-indemnifies-processor detector, audit-cost detector, personal-data reference, notice, survival, subprocessor records, onward transfers, local-law disclosure (DPA-043–055). Generic `_regulated-rule.ts` factory introduced so future rulesets (Step 25 US-state, Step 26 transfer, etc.) reuse the presence/language pattern; `category: "dpa-gdpr"`; `applies_to_playbooks: ["dpa-controller-processor", "dpa-processor-subprocessor", "scc-module-2", "scc-module-3"]`. `V3_RULES` now equals `[...BAA_RULES, ...DPA_GDPR_RULES]` (100 rules); v2 `LAUNCH_RULES` determinism hash preserved (runner filters by playbook). Playbooks `dpa-controller-processor.json` and `scc-module-2.json` upgraded to v1.0.0 with title keywords, distinguishing phrases, expected defined terms, GDPR source citation, and Art. 28(3)-anchored compliance matrices. Tests at `src/engine/rules/v3/dpa-gdpr/dpa-gdpr-ruleset.test.ts` (11/11 passing): registry contract (55 rules, unique ids, DPA-NNN), inert-when-not-DPA, compliant-fixture (zero criticals; ≤5 rules fire), five failure-mode tests (DPA-024 missing breach notice, DPA-035 processor-choice misallocation, DPA-036 SOC-2-eliminates-audit, DPA-048 controller-indemnifies-processor, DPA-023 industry-standard hand-waving, DPA-050 no-personal-data), determinism, scc-module-2 playbook scope check. 613/613 total tests passing; typecheck, lint, build all green. |
| 25 | DPA-US-state ruleset and multi-state playbook | ✅ complete | 25 rules under `src/engine/rules/v3/dpa-us-state/rules.ts` (USDPA-001 through USDPA-025). Coverage: CCPA service-provider terms (USDPA-001–010) — purpose limitation, no-sale, no-cross-context-advertising, no-combining, same-level-of-privacy-protection, certification, monitoring right, consumer-request assistance, inability-to-comply notice, subcontractor flow-down; multi-state common requirements (USDPA-011–019) — processing instructions, nature/purpose, type of data, duration, deletion/return, confidentiality, audit cooperation, subcontractor written contract, compliance demonstration; cross-cutting (USDPA-020–025) — claimed-Service-Provider-without-elements detector (critical, addresses the §30 reclassification risk), multi-state strictness floor flag, sensitive PI, consumer-rights process, data minimization, personal-info presence. `category: "dpa-us-state"`; `applies_to_playbooks: ["dpa-ccpa-service-provider", "dpa-multi-state-us", "dpa-controller-processor"]`. Playbooks `dpa-ccpa-service-provider.json` and `dpa-multi-state-us.json` upgraded to v1.0.0 with title keywords, distinguishing phrases (1798.140, 59.1-579, 6-1-1305, 42-520), expected defined terms, CCPA source citation, and per-state compliance matrices. Tests at `src/engine/rules/v3/dpa-us-state/us-state-ruleset.test.ts` (8/8 passing). |
| 26 | Transfer-mechanism rules and UK-IDTA playbook | ✅ complete | 20 rules under `src/engine/rules/v3/transfer/rules.ts` (TRANSFER-001 through TRANSFER-020). Three playbook scopes baked in: SCC_PLAYBOOKS (scc-module-2/3 + dpa-controller-processor / dpa-processor-subprocessor) for SCC Clauses 1, 2, 8, 9, 11, 14, 15, 16, 18 + modification detector (TRANSFER-001–010); UK_PLAYBOOKS (uk-idta-addendum) for UK Addendum Tables 1–4 + UK Mandatory Clauses modification detector + UK IDTA Part 1 (TRANSFER-011–016); ALL_TRANSFER_PLAYBOOKS for cross-cutting rules — DPF litigation warning, adequacy fallback clause, TIA/TRA reference, onward-transfer Clause 8.7/8.8 (TRANSFER-017–020). Critical-severity rules: TRANSFER-003 (modified SCCs forbidden by Clause 2), TRANSFER-015 (modified UK Mandatory Clauses). DPF reliance is a warning, not a fail, per spec §31. Playbooks `uk-idta-addendum.json` and `scc-module-3.json` upgraded to v1.0.0 with ICO / EUR-Lex source citations and 6–7 column compliance matrices. Tests at `src/engine/rules/v3/transfer/transfer-ruleset.test.ts` (8/8 passing) including: compliant SCC Module 2 fixture (Clauses 1–18 + Annexes) → 0 criticals; UK Addendum fixture (Tables 1–4 + TIA) → 0 criticals; modified-SCC fixture fires TRANSFER-003; missing-Clause-14 fixture fires TRANSFER-007 (TIA gap); DPF reliance fires TRANSFER-017 warning. `V3_RULES` is now `[...BAA_RULES, ...DPA_GDPR_RULES, ...DPA_US_STATE_RULES, ...TRANSFER_RULES]` (145 rules); v2 LAUNCH_RULES determinism hash preserved. 629/629 total tests passing; typecheck, lint, build all green. |
| 27 | NDA deep ruleset and NDA playbooks | 🟡 partial | 25 rules under `src/engine/rules/v3/nda-deep/` (rules.ts + _helpers.ts) with `category: "nda"` and `applies_to_playbooks: ["mutual-nda-deep", "unilateral-nda-deep"]`. Coverage: DTSA whistleblower-immunity notice presence + compound three-pillar completeness check (NDA-D-001–002, 18 U.S.C. § 1833(b)); confidentiality term + trade-secret perpetual carve-out (NDA-D-003–004); Confidential-Information definition with all four standard exclusions — publicly available, previously known, third-party lawful, independently developed (NDA-D-005–009); residuals flagging at `info` severity (NDA-D-010); permitted-use scope language detector + 'to evaluate the Purpose' presence (NDA-D-011–012); return-or-destruction with attestation requirement (NDA-D-013–014); injunctive-relief / irreparable-harm + waiver-of-bond (NDA-D-015–016); governing-law presence + viable-jurisdiction soft warning (NDA-D-017–018); no-precedent / MFN, non-solicit-without-general-solicitation-carve-out, no-license, authority representation, successors-and-assigns with consent (NDA-D-019–023); mutual-NDA symmetry detector (scoped to `mutual-nda-deep` only, NDA-D-024) and unilateral-NDA role-framing check (scoped to `unilateral-nda-deep` only, NDA-D-025). `V3_RULES` is now `[...BAA_RULES, ...DPA_GDPR_RULES, ...DPA_US_STATE_RULES, ...NDA_DEEP_RULES, ...TRANSFER_RULES]` (170 rules). Tests at `src/engine/rules/v3/nda-deep/nda-deep-ruleset.test.ts` (8/8 passing) — registry contract, playbook scoping, compliant-mutual-NDA fixture → 0 criticals, determinism (two runs same `result_hash`), four failure-mode tests (DTSA absent → NDA-D-001 fires; 'any business purpose' permitted use → NDA-D-011; rules inert when no NDA playbook active; unilateral fixture missing role framing → NDA-D-025). Remaining work to call this step ✅ complete (deferred to a follow-up): enrich the placeholder `mutual-nda-deep.json` / `unilateral-nda-deep.json` playbooks with v1.0.0 metadata, distinguishing phrases, expected defined terms, compliance-matrix columns, and source citations; deprecate v2's `mutual-nda` / `unilateral-nda` to `*-legacy` and re-point auto-detect; build the Common Paper + CUAD-derived fixture corpus; baseline expected outcomes for each fixture. `npm run typecheck && lint && test && build` all green; 654/654 total tests passing. |
| 28 | MSA deep ruleset and MSA playbooks | 🟡 partial | 30 rules under `src/engine/rules/v3/msa-deep/` (`rules.ts` + `index.ts` + `msa-deep-ruleset.test.ts`) using the generic `_regulated-rule.ts` factory with `category: "msa-deep"` and `applies_to_playbooks: ["msa-vendor-deep", "msa-customer-deep"]`. Coverage: indemnification scope + procedure + cap-carve-out flag (MSA-001–005); aggregate cap + carve-outs + mutual consequential waiver + California § 1668 overlay + NY § 5-322.1 anti-indemnity (MSA-006–010); IP allocation + feedback license scope (MSA-011–012); workmanlike/conformance/no-malicious-code/compliance/non-infringement + UCC § 2-316 disclaimer overreach (MSA-013–015); SLA reference + sole-and-exclusive-remedy flag (MSA-016–017); termination for material breach / insolvency / wind-down (MSA-018–020); data return / portability (MSA-021); balanced force majeure (MSA-022); assignment change-of-control silence (MSA-023); governing-law vs venue mismatch (MSA-024); amendment + no-waiver + survival + entire-agreement boilerplate (MSA-025–026); custom **order-of-precedence consistency** rule that fires when the MSA stated precedence (MSA controls over SOW) co-exists with operative terms (indemnity, liability cap, IP, warranty) appearing in the subordinate document, naming the inconsistency in the excerpt (MSA-027); AI usage clause presence per NIST AI RMF (MSA-028); Texas Bus. & Com. Code § 151.102 anti-indemnity overlay (MSA-029); UCC § 2-719(2) limited-remedy fail-of-essential-purpose escape (MSA-030). State-law-overlay DKB node committed at `dkb/fixtures/v3/nodes/state-commercial-overlays.json` (5 `statutory_clause_requirement` entries: Cal. Civ. Code § 1668, N.Y. Gen. Oblig. § 5-322.1, Tex. Bus. & Com. Code § 151.102, U.C.C. § 2-316, U.C.C. § 2-719). Playbooks `msa-vendor-deep.json` and `msa-customer-deep.json` upgraded from `0.0.0-placeholder` to `1.0.0` with title keywords (MSA / MSubA / cloud service agreement / customer agreement), distinguishing phrases (limitation of liability / indemnification / order of precedence / SOW / service level / data return), expected defined terms, three commercial source citations each (UCC + Cal § 1668 / NY § 5-322.1 + UCC § 2-719), 16-/17-column compliance matrices, and `companion_playbooks: ["dpa-controller-processor", "baa", "vendor-security-addendum", "ai-addendum"]`. `V3_RULES` now equals `[...BAA_RULES, ...DPA_GDPR_RULES, ...DPA_US_STATE_RULES, ...MSA_DEEP_RULES, ...NDA_DEEP_RULES, ...TRANSFER_RULES]` (200 rules). Tests at `src/engine/rules/v3/msa-deep/msa-deep-ruleset.test.ts` (10/10 passing): registry contract (30 rules, unique MSA-NNN ids, `category: "msa-deep"`, scoped to both MSA playbooks), inert-when-no-MSA-playbook, compliant-MSA fixture under both vendor + customer playbooks → 0 criticals, five failure-mode tests (MSA-006 missing aggregate cap, MSA-009 Cal § 1668, MSA-005 indemnity-outside-cap, MSA-017 SLA-credit-as-sole-remedy, MSA-027 order-of-precedence inconsistency), determinism (two runs same `result_hash`). `npm run typecheck && lint && test && build` all green; 688/688 tests passing. Remaining work to call this step ✅ complete: build the Common Paper Cloud Service Agreement + SEC-EDGAR-sourced real-MSA fixture corpus; baseline expected outcomes for each fixture. **Note (2026-05-28):** the originally-planned "deprecate v2's `msa-general`, `saas-vendor`, `saas-customer` to `*-legacy`" follow-up was reconsidered and NOT pursued. The v2 NDA playbooks had a clean 1:1 successor pair (Step 27 follow-up landed the `deprecated` + `superseded_by` metadata on them on 2026-05-28), but the v2 MSA + SaaS playbooks have no analogous successor: `msa-general` is a fallback parent for B2B services contracts not specifically SaaS or consulting (the deep variants are narrower vendor- and customer-side specializations, not strict successors); v3 `saas-tos` lints SaaS Terms of Service which is a different document shape from the v2 enterprise SaaS Subscription Agreement playbooks. The deferral is now documented in the [`src/engine/rules/v3/msa-deep/rules.ts`](src/engine/rules/v3/msa-deep/rules.ts) header comment. |
| 29 | Vendor security, AI addendum, EULA, ToS, privacy-policy-lint, COI playbooks | 🟡 partial | 20 rules under `src/engine/rules/v3/addenda/` (`rules.ts` + `index.ts` + `addenda-ruleset.test.ts`) using the generic `_regulated-rule.ts` factory with `category: "addenda"`. Surface scoping via `applies_to_playbooks`: vendor-security-addendum (ADDENDA-001..009 — specific measures, security-review cadence, right-to-audit / SOC 2 substitution, incident-response window, vulnerability-disclosure, SDLC, data-classification, named encryption FIPS 140-3 / AES-256, pen-test cadence); ai-addendum (ADDENDA-010..016 — definitions, prohibited training-on-customer-data without opt-in **critical**, transparency, IP ownership, hallucination disclaimer + human-review, AI subprocessor disclosure, fine-tuning-data deletion on termination); eula (ADDENDA-017..018 — license grant + prohibited uses, EU Digital Content Directive minimums); saas-tos (ADDENDA-019 — FTC Click-to-Cancel + ROSCA alignment); privacy-policy-lint (ADDENDA-020 — CCPA § 1798.130 + GDPR Art. 13/14 + COPPA disclosures). AI-surface citations explicitly labeled "consensus practice, not statute" per spec §34 — NIST AI RMF, EU AI Act, FTC enforcement actions. New DKB node `dkb/fixtures/v3/nodes/am-best-ratings.json` is an `insurance_norm` carrying acceptable / marginal / unacceptable rating buckets per spec §53 ("AM Best public ratings as of 2026-05-13 (DKB build date)") plus 8 curated common carriers. Playbooks `vendor-security-addendum.json`, `ai-addendum.json`, `eula.json`, `saas-tos.json`, `privacy-policy-lint.json`, `coi.json` upgraded from `0.0.0-placeholder` to `1.0.0` with title keywords, distinguishing phrases, expected defined terms, multi-source citations (NIST SP 800-53 + FIPS 140-3, NIST AI RMF + EU AI Act + FTC, EU Digital Content / Consumer Rights Directives, FTC Click-to-Cancel + ROSCA, CCPA + GDPR + COPPA, AM Best public + ACORD 25), and compliance-matrix columns per playbook. `V3_RULES` now equals `[...ADDENDA_RULES, ...BAA_RULES, ...DPA_GDPR_RULES, ...DPA_US_STATE_RULES, ...MSA_DEEP_RULES, ...NDA_DEEP_RULES, ...TRANSFER_RULES]` (220 rules). Tests at `src/engine/rules/v3/addenda/addenda-ruleset.test.ts` (12/12 passing): registry contract (20 rules, unique ADDENDA-NNN ids, `category: "addenda"`, every rule scoped), inert-when-no-addenda-playbook, surface scoping (security rules don't fire on AI playbook), compliant Vendor Security Addendum + compliant AI Addendum → 0 criticals, six failure-mode tests (ADDENDA-004 missing incident window, ADDENDA-011 training-without-opt-in **critical**, ADDENDA-010 missing AI definitions, ADDENDA-019 missing Click-to-Cancel, ADDENDA-018 missing EU consumer minimums, ADDENDA-020 missing privacy disclosures), determinism. `npm run typecheck && lint && test && build` all green; 700/700 tests passing + 2 skips. Remaining work to call this step ✅ complete (deferred to follow-up): ACORD-25 binary spatial-layout extractor at `src/extract/v3/acord-25.ts` (depends on v2 PDF text-with-position output and a real clean ACORD-25 + low-rating-carrier ACORD-25 fixture pair), real privacy-policy fixture corpus (Google / Apple / Cloudflare published policies baselined), a typical SaaS AI addendum end-to-end fixture, and the consistency check between privacy-policy claims and DPA terms when run paired. |
| 30 | v3 extractors implementation | ✅ complete | All 9 extractor stubs in `src/extract/v3/` are now real, deterministic, pure functions: `role-classifier.ts` (definition > recital > clause-usage priority over a 12-role controlled vocabulary), `pii-category.ts` (HIPAA's 18 identifiers + GDPR Art. 9 special categories + Art. 10 + CCPA SPI + "special categories" flag, all in a single 30-entry catalog), `transfer-mechanism.ts` (SCC modules 1-4 + UK IDTA + UK Addendum + Swiss Addendum + Adequacy + BCR + Art. 49 + DPF, with location heuristic `annex > attachment > hyperlink > by-reference > recital-only > inline`), `security-measures.ts` (17-slug controlled vocabulary with cadence + scope inference), `breach-timing.ts` (normalizes hours/days; preserves vague phrases like "without unreasonable delay"; bidirectional breach↔notify ordering), `audit-rights.ts` (frequency / notice / methods / cost allocation / confidentiality / third-party auditor), `subprocessor.ts` (consent form Art. 28(2), list location, notice, objection right + consequence, flow-down Art. 28(4); picks the most-informative paragraph), `insurance.ts` (per-line amounts with per-occurrence vs. aggregate split, ISO endorsement form numbers, AM-Best rating, notice of cancellation), `dtsa-notice.ts` (substantive completeness across § 1833(b)(1) gov-disclosure + § 1833(b)(2) under-seal + contractor/consultant coverage). `types.ts` declares the full `V3ExtractedData` aggregate; `index.ts` re-exports every extractor + `extractAllV3(tree, {parties?})` convenience that runs all nine in dependency order. Tests at `tests/v3/extract/v3-extractors.test.ts` (23/23 passing): one positive + one empty/edge case per extractor + an aggregate determinism check. All gates green: typecheck clean, lint clean, **723/723 tests passing + 2 intentional skips**, build green. The v3 rule engine doesn't consume these outputs yet — Step 31 (consistency checks) and Step 32 (report renderer) will thread them through; the existing v3 rulesets continue to run pattern-based against the raw document, so this step is purely additive. COI / ACORD-25 binary-layout parsing remains deferred to a follow-up that depends on the v2 PDF text-with-position output, consistent with the Step 29 note. |
| 31 | Consistency-check engine and two-document mode | ✅ complete | Cross-document engine at `src/engine/consistency/` (`types.ts` + `runner.ts` + `_helpers.ts` + `rules/`): `runConsistency({rules, documents, dkb})` accepts ≥2 parsed v2 documents (`{doc_id, source_file_name, playbook_id, tree, extracted}`) and emits a `ConsistencyRun` with deterministic `result_hash` (SHA-256 over the run JSON with `result_hash`/`executed_at`/`elapsed_ms` blanked — same hash contract as the v2 runner). Findings are sorted by `(severity, rule_id, doc_id, start_offset)`. `kindOf(doc)` classifies playbook ids into `msa | baa | dpa | nda | sow | other`; rules declare `requires: DocKind[]` and the runner skips them (`ran=false`) when a required kind is missing. Seven initial rules under `src/engine/consistency/rules/rules.ts` per spec §27: **CC-001** BAA permitted uses no broader than MSA service scope (critical, 45 CFR § 164.504(e)); **CC-002** DPA processing purpose matches MSA services (warning, GDPR Art. 28(3)); **CC-003** DPA data categories not broader than MSA — flags health/biometric/genetic/financial/government-id/children's data appearing in the DPA's Annex I.B with no anchor in the MSA's services description (warning); **CC-004** BAA term aligns with MSA (warning) — passes when the BAA is explicitly co-terminous, fires when the BAA sets an independent term; **CC-005** governing-law alignment across any pair of documents (warning) — uses `extracted.jurisdictions` first, falls back to paragraph scan, canonicalizes the raw text before bucketing; **CC-006** notice-clause alignment (info); **CC-007** order-of-precedence consistency (warning) — fires when the MSA names itself controlling but operative `indemnification | aggregate liability cap | IP ownership | warranty` terms live only in the subordinate document. Every finding cites both contributing documents and quotes the conflicting paragraph from each (`excerpts: ConsistencyExcerpt[]`, length ≥ 2). Rules are pure: no IO, no time, no randomness; throws are swallowed by the runner with a zero-finding log entry. `findParagraph(doc, pattern)` matches against both paragraph text and the containing section heading so anchors like "Scope of Services" (typically headings) score. Runner-level wiring at `src/engine/runner.ts`: new `runEngineMulti({documents, dkb, consistencyRules?, executed_at?})` runs each document's single-doc engine in parallel via `Promise.all`, then the consistency pass. Engine barrel re-exports the consistency types, the runner, the registry, and `kindOf`/`findByKind`/`hasAllKinds` helpers. Tests at `src/engine/consistency/runner.test.ts` (20 passing): registry contract (seven CC-NNN rules, unique ids, `category: "consistency"`), `kindOf` classifier, determinism (identical `result_hash` on two runs), defensive errors (`< 2 documents` and duplicate `doc_id`), per-rule positive + negative cases for CC-001/002/003/004/005/006/007, the BAA-co-terminous escape (CC-004), the MSA-anchored sensitive category (CC-003), and finding ordering (severity rank monotone; CC-001 critical before CC-005 warning). `npm run typecheck && lint && test && build` all green; **743/743 tests passing + 2 skips**. v2 `LAUNCH_RULES` determinism hash is untouched (the consistency engine is a parallel runner, not a v2 modification). Remaining work to call this step a launch-grade gate (deferred to Step 34's golden corpus): paired-fixture corpus from spec §15 (20 MSA+DPA / MSA+BAA / MSA+SOW pairs), per-pair baseline expected-consistency-findings JSONs, and the UI hookup for the multi-doc drop (Step 33). The current rules use conservative heuristics that prefer false negatives over false positives; calibration against real paired fixtures lands with Step 34. |
| 32 | Report renderer changes | ✅ complete | New v3 report module at `src/report/v3/` (`_dx.ts` shared primitives + `types.ts` + six conditional renderers + `footer.ts` + `index.ts`). The v2 builder `buildDocxReport(run, ingest, dkb, playbook, v3?)` now takes an optional fifth argument; when absent the v2 layout is unchanged (`docx.test.ts` 4/4 still green). When present, sections are inserted at spec-mandated positions: **§54 compliance matrix** between the executive summary and the findings list — a real Word table with cell shading (Pass `#C8E6C9` / Partial `#FFF59D` / Fail `#FFCDD2` / N/A `#EEEEEE`), `tableHeader: true` on the header row for screen-reader semantics, and a "Citations as of [date]" caption; **§56 cross-border transfer summary** after findings, with a 3-column table (mechanism / location in document / excerpt) and an explicit TIA + supplementary-measures status line; **§57 subprocessor inventory page** with a 6-row attribute/value table covering permitted / consent form / list location / notice period / objection right + consequence / flow-down requirement, plus the source excerpt; **§58 insurance summary page** with per-line per-occurrence/aggregate USD amounts, ISO endorsement form numbers, required AM Best rating, and notice-of-cancellation days; **§59 two-document consistency appendix** rendering the full `ConsistencyRun` — a finding-count table followed by per-finding heading + severity + description + explanation + recommendation + per-doc-excerpts pairs, ending with the consistency `result_hash`; **§55 citation-depth verification appendix** with one row per unique citation listing the regulator string, a Word `ExternalHyperlink` to the stable URL (renders as a click-through in Word/Pages/Docs), the DKB version that grounded it, and the retrieval timestamp. Every page of the v3 report now carries a Word section footer with `Engine v<version> | DKB <version> | Result hash <short> | Citations as of <date>` (the build-date piece is optional and omitted when not provided). Each conditional renderer returns `[]` when its input is absent, so callers can pass any subset of v3 inputs without leaving empty placeholder pages. Type-level wiring: `V3ReportInputs` in `src/report/v3/types.ts` carries `matrix?`, `transfers?`, `subprocessor?`, `insurance?`, `consistency?`, `dkb_build_date?`, and a pass-through `extracted_v3?`. Tests at `src/report/v3/v3-report.test.ts` (16 passing): per-renderer shape (matrix non-empty / empty-columns placeholder; transfers empty → `[]`, non-empty → content; subprocessor null → `[]`, present → content; insurance fully-empty → `[]`, populated → content; consistency appendix renders both heading-only and findings paths; citation index empty → `[]`, populated → content; footer constructs), plus end-to-end DOCX assertions (a v3-fed report is larger than the v2-only report; v2-API backwards compatibility — omitting `v3` returns a valid Blob; partial inputs render successfully). `npm run typecheck && lint && test && build` all green; **759/759 tests passing + 2 skips**. Remaining work to call this step launch-grade (deferred to Step 34 golden + Step 33 UI): per-cell click-through links from the matrix into the findings section (requires a stable anchor target inside the docx — straightforward `ExternalHyperlink` or `InternalHyperlink` extension once Step 34 fixtures define which rule ids anchor which cell); a v3 matrix builder that derives `ComplianceMatrix` rows from the `EngineRun` + the playbook's `compliance_matrix_columns` automatically (currently callers construct the matrix). |
| 33 | UI: document-type auto-detect, compliance frame toggle, multi-doc drop | 🟡 partial | Four new pure modules under `src/ui/v3/` cover the testable surface of spec §§60–63; the DOM hookup into `main.ts` + the Playwright `tests/e2e/v3/` suite are deferred to a follow-up (the existing `main.ts` flow remains v2-only so no production behavior changes). **`auto-detect.ts`** — `detectV3Family(extracted, body_text)` is a pure scorer over a 12-family controlled vocabulary (`baa`, `dpa-eu`, `dpa-us-state`, `scc-module-2`, `scc-module-3`, `uk-idta`, `nda-deep`, `msa-deep`, `coi`, `vendor-security`, `ai-addendum`, `unknown`); each family detector returns `DetectionSignal[]` with `{source, evidence, weight}` and the family with the highest weighted sum wins. Signal sources: `definition` (e.g., "Business Associate" defined in `extracted.definitions.entries`), `phrase` (e.g., "45 CFR § 164.504"), `header` (e.g., "Business Associate Agreement"), `annex`, `classified`. Confidence is `min(1, score / 4)`; below 0.4 the UI is expected to render the suggestion faintly. Returns `family: "unknown"` with `suggested_playbook: null` when no detector scores ≥ 1. `FAMILY_TO_PLAYBOOK` maps each family to the actual `src/playbooks/v3/<id>.json`. **`compliance-frame.ts`** — `defaultFramesForPlaybook(playbookId)` returns `{on, available, hint?}` over a 20-frame controlled vocabulary (`ComplianceFrame` union: HIPAA / GDPR / UK-GDPR / 8 US state laws / GLBA / FERPA / 4 international / NIST-AI-RMF / EU-AI-Act / FTC-ROSCA). Defaults follow spec §61 exactly: BAA → HIPAA on; DPA controller-processor → GDPR + CCPA on; multi-state DPA → all 8 US-state frames on; SCC → GDPR + UK-GDPR on; UK IDTA → UK-GDPR; AI addendum → NIST-AI-RMF + EU-AI-Act; SaaS ToS → FTC-ROSCA; privacy-policy → GDPR + CCPA; MSA → none on, with the `hint: "Looking for GDPR or HIPAA coverage? Add a companion DPA or BAA."` shown next to the chip row. `toggleFrame(current, frame)` is an immutable list-toggle helper. **`multi-doc.ts`** — pure reducer for the multi-document drop zone (spec §62, `MAX_DOCUMENTS = 4`). `addDocument` returns `{ok, state}` or `{ok: false, state, reason}` with two failure modes (over-limit, duplicate id). `markAnalyzing` / `markComplete` / `markError` / `removeDocument` are immutable card transitions through the `queued → analyzing → complete|error` state machine. `setConsistencyEnabled` and `setConsistencyFindingsCount` carry the spec-§62 "run cross-document consistency (default on)" checkbox state. `isReadyForConsistency` is true when ≥ 2 cards are in a terminal state; `hasUsableConsistencyBundle` is true when ≥ 2 cards completed successfully. **`copy.ts`** — `EMPTY_STATE_COPY` carries the spec-§63 four-document headline and sub. `V3_ERROR_COPY` is a closed mapping over 7 error codes including the three spec-named error states ("this looks like a BAA but no Business Associate party is defined…", "this looks like SCC Module 2 but Annex I is empty…", "this looks like an ACORD 25 but the certificate-holder block is illegible…"); `v3ErrorMessage(code)` falls back to a generic message for unknown codes. Tests at `src/ui/v3/v3-ui.test.ts` (26 passing): detection of BAA / DPA-EU / SCC Module 2 / COI / "unknown"; frame defaults for BAA / DPA / multi-state DPA / MSA-with-hint / AI / SCC / generic; `toggleFrame` add + remove + non-mutation; multi-doc accepts up to MAX_DOCUMENTS / rejects the 5th / rejects duplicate ids / queued→analyzing→complete transitions / per-doc error isolation / `removeDocument` / `isReadyForConsistency` semantics / `hasUsableConsistencyBundle` semantics / `consistency_enabled` toggle / `consistency_findings_count` setter; copy `v3ErrorMessage` known + generic fallback; `EMPTY_STATE_COPY` headline shape. All gates green: typecheck clean, lint clean, **788/788 tests passing + 2 skips**, build green. Remaining work to call this step ✅ complete: the DOM hookup that calls `detectV3Family` after the v2 auto-detect, renders the chip row from `defaultFramesForPlaybook`, swaps the dropzone state machine in `states.ts` over to the `MultiDocState` reducer when ≥ 2 files are dropped, and threads `runEngineMulti` + `runConsistency` results into the v3 report sections; plus the offline Playwright suite at `tests/e2e/v3/` (drop-a-BAA-see-HIPAA-frame-default-on; drop-a-DPA-and-an-MSA-see-consistency-checkbox-on-both-reports-plus-appendix). |
| 34 | End-to-end golden tests | ✅ complete | v3 golden-output harness lives at `tests/golden/v3/`: `_pipeline.ts` mirrors the v2 `tests/integration/_pipeline-helpers.ts` but runs the union of `LAUNCH_RULES` and `V3_RULES`, loads both `playbooks/*.json` and `src/playbooks/v3/*.json` (greedily; ignores unparseable placeholder files), supports a `<fixture>.playbook` sidecar that forces a v3 playbook id when the auto-detector picks a v2 fallback, and normalizes the run for golden comparison (`executed_at` blanked, per-rule `elapsed_ms` zeroed — matching the runner's hash contract). `golden.test.ts` runs every fixture under `fixtures/` against its golden at `expected/<name>.json`, asserting both `result_hash` equality and full canonicalized-JSON structural equality (so a hash collision plus content drift is impossible); each fixture also runs twice in-process with an assertion that the canonical bytes are identical (the v3 analogue of the v2 cross-OS determinism guarantee). Regeneration runs under `VAULYTICA_REGEN_GOLDEN=1 npx vitest run tests/golden/v3` and writes the canonical run JSON to `expected/`, ready for a reviewable diff. **19 passing fixtures committed** (all 19 v3 playbooks covered): `baa-minimal-pass.txt` (baa), `mutual-nda-deep-minimal-pass.txt` (mutual-nda-deep), `unilateral-nda-deep-minimal-pass.txt` (unilateral-nda-deep), `dpa-controller-processor-minimal-pass.txt` (dpa-controller-processor), `msa-vendor-deep-minimal-pass.txt` (msa-vendor-deep), `msa-customer-deep-minimal-pass.txt` (msa-customer-deep), `vendor-security-addendum-minimal-pass.txt` (vendor-security-addendum), `ai-addendum-minimal-pass.txt` (ai-addendum), `eula-minimal-pass.txt` (eula), `saas-tos-minimal-pass.txt` (saas-tos), `coi-minimal-pass.txt` (coi), `baa-subcontractor-minimal-pass.txt` (baa-subcontractor), `dpa-processor-subprocessor-minimal-pass.txt` (dpa-processor-subprocessor), `dpa-multi-state-us-minimal-pass.txt` (dpa-multi-state-us), `dpa-ccpa-service-provider-minimal-pass.txt` (dpa-ccpa-service-provider), `privacy-policy-lint-minimal-pass.txt` (privacy-policy-lint), `scc-module-2-minimal-pass.txt` (scc-module-2), `scc-module-3-minimal-pass.txt` (scc-module-3), `uk-idta-addendum-minimal-pass.txt` (uk-idta-addendum) — each with a `.playbook` sidecar and a baselined golden JSON in `expected/`. **39/39 golden tests passing** (1 presence + 19 golden-match + 19 determinism). `tests/golden/v3/README.md` documents the layout, the regeneration recipe, the determinism guarantee, and the expansion plan. LAUNCH.md row v3-a flipped to ✅ pass — 2026-05-17. |
| 35 | Documentation | ✅ complete | Seven new markdown documents under `docs/v3/` covering the full v3 surface: `overview.md` (audience, scope, what's new vs. v2, the 220-rule breakdown, what v3 does not promise), `adding-a-baa-rule.md` (HIPAA-anchored rule walkthrough through the `BAA-NNN` presence/language factories at `src/engine/rules/v3/baa/_helpers.ts` with citation-discipline + DKB-staleness expectations), `adding-a-dpa-rule.md` (two examples — a GDPR Art. 28(3) preamble extension and a US-state CCPA sensitive-PI rule — both using the generic `_regulated-rule.ts` factory), `adding-a-playbook.md` (the four v3 schema additions `regulator_frame` / `applicable_jurisdictions` / `companion_playbooks` / `compliance_matrix_columns` with per-family column conventions for BAA / DPA-EU / DPA-US-state / NDA-deep / MSA-deep), `regulators.md` (the full source catalog from spec §§5–12 grouped by US-HIPAA-privacy / EU / UK / Switzerland / international / trade-secret / commercial-law / insurance / AI-consensus-practice, with the canonical URL and the responsible DKB fetcher module name for every entry), `two-document-mode.md` (when to use it, the seven CC-NNN consistency rules with their `requires: DocKind[]` matrix, how findings are shaped, the determinism contract, and how to add a new CC-NNN rule), and `compliance-matrix.md` (anatomy of the matrix, cell-shading colors, what Pass / Partial / Fail / N/A mean, what "Partial" specifically means — soft-findings or stale-citation impairment — and how to cite the matrix in an audit). `README.md`'s "What I check" gains the v3 line "**v3 adds compliance-grade rule sets for BAA, DPA, NDA, MSA, transfer mechanisms, and adjacent documents — same browser-only posture as v2.**" `CHANGELOG.md` picks up two new entries in `[Unreleased]` documenting Step 35 and Step 38. All gates green: typecheck clean, lint clean, **788/788 tests + 2 skips**, build green. The documents are intentionally tight: the spec lives in [`spec-v3.md`](docs/spec-v3.md) and is the source of truth; these files are the contributor- and user-facing entry points. `LAUNCH.md` (v3 launch posture) lands with Step 39's version bump and tag. |
| 36 | Performance and offline verification | 🟡 partial | Bundle-size guard at [`tests/integration/bundle-size.test.ts`](tests/integration/bundle-size.test.ts) (3 tests, included in `npm test` via the same `VAULYTICA_SKIP_BUILD_TESTS` env-var pattern as the SRI guard) — asserts (1) eager entry `main-*.js` ≤ 50 KB gzipped, (2) total gzipped JS payload ≤ v2 baseline (165 KB) + 600 KB spec budget, (3) no single raw chunk crosses 600 KB without an explicit allow-list entry (allow-list covers the dynamic-imported analysis pipeline + vendor chunks `vendor-mammoth` / `vendor-pdfjs` / `vendor-docx` / `pipeline`). Current measured values: eager entry ~5 KB gzipped, total payload ~500 KB gzipped — well inside the v2 + 600 KB ceiling. Offline-verification Playwright spec at [`tests/e2e/v3/no-network.spec.ts`](tests/e2e/v3/no-network.spec.ts) hooks `page.on("request", …)` from the moment the page loads, asserts every recorded request after the file-drop has the same origin as the page (or is a `data:`/`blob:` URL), and verifies the downloaded DOCX is a valid OOXML zip. Currently `test.skip`s when the fixture is not a `.docx`/`.pdf` (the v3 starter fixture is `.txt`, which the dropzone rejects by extension — accepting a `.txt` would require either a binary v3 fixture landed under `tests/golden/v3/fixtures/` or a paste-intake path the test exercises directly). Remaining work to call this step ✅ complete: (1) live Lighthouse mobile-4G run against the deployed v3 site with the existing `lighthouserc.json` budgets — passes as part of the existing CI Lighthouse workflow once a v3 deploy is live; (2) a binary v3 BAA fixture so the no-network spec runs on the v3 path without `test.skip`. The v2 smoke spec (`tests/e2e/smoke.spec.ts`) already covers the equivalent privacy promise for the v2 NDA flow against the existing v2 fixture corpus; the v3 spec is the v3-rule-engine equivalent. |
| 37 | Accessibility verification | 🟡 partial | New Playwright spec at [`tests/e2e/v3/a11y-keyboard.spec.ts`](tests/e2e/v3/a11y-keyboard.spec.ts) covers the v2 keyboard surface honestly (Tab-to-dropzone in ≤ 50 stops, dropzone `role="button"` + `tabindex` + Enter-activation, theme toggle keyboard-flips `data-theme`, FAQ disclosure toggles on Enter) plus two forward-compatible probes for the v3 chip row (`[data-role="compliance-frame-chips"]`) and v3 multi-doc cards (`[data-role="multi-doc-card"]`) — both `test.skip` when the elements are absent so the spec stays green during the Step 33 partial-hookup window. When the Step 33 DOM hookup lands, removing the skips is the only required change. DOCX accessibility properties are covered by the v3 report renderer: the compliance-matrix table uses `tableHeader: true` on the header row so screen readers announce the row of column headers correctly (Word + Narrator, Pages + VoiceOver). Remaining work to call this step ✅ complete: (1) install `@axe-core/playwright` as a dev-dep and run a full axe-core sweep against the v3 UI states (empty / single-doc / multi-doc / chip-row open / results visible) — depends on the Step 33 UI hookup so a real v3 page state exists to scan; (2) manual Narrator + VoiceOver verification of the compliance-matrix table in the produced DOCX. Both are tracked alongside the Step 33 follow-up. |
| 38 | Threat-model update | ✅ complete | New "v3 additions" section appended to [`docs/threat-model.md`](docs/threat-model.md) covering five distinct attack surfaces and trust assumptions introduced by v3: **(1) DKB integrity** — the manifest carries `version` + `sources[]` + `dkb_node_version` + `dkb_node_last_validated_at`, the runtime loader validates the manifest shape with Zod, and every v3 citation carries `content_hash_at_pin = sha256(normalizeForHash(snapshotText))` so a tampered DKB JSON fails the hash check; **(2) the staleness gate** — when an upstream regulator URL drifts, the affected rule is `enabled: false` and the build exits non-zero unless the drift is explicitly acknowledged in `dkb-staleness-ack.yml`, leaving an auditable trail; **(3) the citation surface** — every v3 citation appears in three report places (inline `[N]`, §55 citation-index appendix with clickable URL, and the per-page footer's "Citations as of [DKB build date]" line), so a reader of a single printed page can verify the report against its source; **(4) the "consensus practice" AI disclaimer** — the AI-addendum playbook's ADDENDA-NNN rules carry the framing explicitly in their descriptions and the report's §59 disclaimer block reinforces it ("Failing one of these checks is not a regulatory violation. It is an observation worth a conversation with counsel."); **(5) the explicit non-promise** that v3 covers every regulator — unsupported regulators surface as `N/A` cells with a "not yet covered" note rather than silently passing. The section closes with a "what v3 still does not protect against" enumeration of four residual risks (regulator-URL 404 between weekly builds; consensus practice hardening into statute mid-period; adversarial drafting against deterministic regex patterns; jurisdictional carve-outs that v3 cannot reason about without a pattern). Each subsection follows the v2 threat-model rhythm: "what this protects against" + "what it does not protect against" / "how it might be bypassed." No new code; documentation only. `npm run typecheck && lint && test && build` all green; **788/788 tests + 2 skips**. |
| 39 | Launch checklist + version bump (v3) | ✅ complete | `package.json` version bumped from `1.0.0` to `3.0.0`. (v2 was represented by `spec-v2`/spec.md but never received a package bump — going straight to 3.0.0 keeps the spec-and-package versions aligned.) [`LAUNCH.md`](LAUNCH.md) gains a "Launch checklist — v3.0.0" section with 15 v3-specific rows tracked in the same `status — date — verifier — note` format as v1.0.0: 7 rows ✅ pass (v3-c determinism, v3-g docs build, v3-h CHANGELOG current, v3-j README v3 line, v3-k CHANGELOG dated, v3-l bundle-size budget, v3-m cross-doc consistency end-to-end, v3-n compliance matrix renders), 7 rows 🟡 partial with stated tracking pointers (v3-a per-playbook passing fixture corpus pending Step 34 expansion, v3-b end-to-end fail-fixture corpus pending Step 34, v3-d offline check pending a binary v3 fixture, v3-e Lighthouse live mobile-4G pending deploy, v3-f axe-core sweep pending Step 33 UI hookup, v3-i staleness gate pending first weekly DKB-rebuild after tag, v3-o UI auto-detect DOM hookup pending Step 33 follow-up), 0 rows ⏳ pending (every v3 row is either ✅ or 🟡 with a tracking pointer). The v3 sign-off recipe (`git tag -s v3.0.0`) is documented alongside the v3 "non-promises" section. [`CHANGELOG.md`](CHANGELOG.md) gains a `## [v3.0.0] — 2026-05-16` heading with a complete release summary covering every v3 step plus the per-step detail entries (which were accumulated under `[Unreleased]` during the build sequence) preserved as a "Detail (per spec-v3 build step)" subsection. A fresh empty `## [Unreleased]` placeholder sits at the top. `npm run typecheck && lint && test && build` all green; **791/791 tests + 2 skips**. The actual `git tag -s v3.0.0` is the user-driven step that comes after this commit (the launch checklist gates the tag, not the commit). |

## v4 build plan (spec-v4.md Part VI)

| # | Step | Status | Notes |
|---|------|--------|-------|
| 40 | v4 repo scaffolding + tagline change | ✅ complete | New v4 namespace skeleton committed: `src/extract/v4/` (placeholder `index.ts` exporting `classifyV4(extracted, body_text)` + the 16-bucket `V4SubDomain` union for the Step 42 classifier), `src/engine/rules/v4/<sub-domain>/` for all 16 sub-domains (commercial / governance / equity / m-and-a / real-estate / employment / settlement / ip-licensing / privacy-extended / healthcare / insurance / banking / construction / trust-estate / compliance-policy / regulatory-prose — each ships an `index.ts` exporting an empty `<RULESET>_RULES: readonly Rule[] = []` so the v4 aggregate compiles without a behavior change), `src/engine/rules/v4/index.ts` aggregate exporting `V4_RULES: readonly Rule[] = []`, `src/playbooks/v4/README.md`, `tests/v4/README.md` (mirrors the v3 test layout per Step 45–59), `dkb/build/v4/README.md` + `dkb/fixtures/v4/README.md` (Step 60 fetcher framework), top-level `V4.md` short index pointing to spec-v4.md, this build plan, the catalog, and cousin docs. **Tagline change** (spec §3): `site/index.html` `<h1 id="hero-h1">` flipped from "Drop a contract." to "Drop legal docs." — no test depends on the hero text. The broader OG/Twitter/JSON-LD/FAQ rewrites (spec §3 carry-forward) are intentionally deferred to a follow-up commit because they would touch `tests/integration/static-html.test.ts` + `tests/integration/schema-org.test.ts` (≤ 80 char og:title budget, FAQ Q3 + Q5 wording) — the right place for those changes is the same commit that lands the new sub-domain marketing tiles (spec §3 tile count 12 → 16), so the marketing copy and the rendered catalog land together. All gates green: typecheck clean, lint clean, **791/791 tests + 2 skips**, build green. The v4 aggregate is wired but empty, so v2's `LAUNCH_RULES` determinism hash and v3's `V3_RULES` 220-rule registry are both unchanged. |
| 41 | Multi-document ingest (folder + zip + multi-file) | ✅ complete | New module at [`src/ingest/multi.ts`](src/ingest/multi.ts) (282 lines) and 19 unit tests at [`src/ingest/multi.test.ts`](src/ingest/multi.test.ts). **Caps** (spec §8): `MAX_FILE_BYTES = 50 MB` (unchanged from v1), `MAX_BUNDLE_BYTES = 200 MB` uncompressed, `MAX_BUNDLE_FILES = 50`. Cap violation emits the spec-mandated copy: `BUNDLE_CAP_MESSAGE = "Vaulytica analyzes up to 50 files / 200 MB at once. Split your bundle or upload fewer files."`. **Validation surface**: `classifyExtension(filename)` returns `"pdf" | "docx" | null` (case-insensitive); `rejectionForFilename(filename)` steers `.doc` to the v1 "save as .docx" message and everything else to the generic "accepts .pdf and .docx" message. **`planBundle(candidates)`** is the pure validator — returns `{ok: false, reason}` when bundle-level caps fail OR `{ok: true, entries, total_bytes}` with per-entry verdicts (one `MultiIngestEntry` per input, either `{ok: true, kind, bytes, size_bytes}` or `{ok: false, reason}`). **Browser-side enumeration**: `filesToCandidates(files: File[])` reads a `FileList` into the candidate-bundle shape; `enumerateFolderEntry(entry, out?)` recursively walks a `FileSystemEntry` (from `DataTransferItem.webkitGetAsEntry()` or the File System Access API), draining `readEntries` until empty (the W3C spec requires it because `readEntries` returns batches), and skips any non-`.pdf`/`.docx` leaf silently. **Zip extraction**: `extractZipEntries(archive: ArrayBuffer)` uses `fflate`'s synchronous `unzipSync` (pure JS, zero dependencies, MIT, no WebAssembly compile, no `eval` — works behind the strict CSP) and returns entries sorted lexicographically by basename so multi-document bundle hashes (spec §15) are stable regardless of zip-producer order. Directory entries are skipped, `__MACOSX/` sidecars are skipped silently, and zip-slip `..` traversal segments are defensively refused. Non-`.pdf`/`.docx` entries inside the zip are skipped silently (spec §8: user's intent is plainly the legal docs; a README.txt alongside a contract shouldn't fail the bundle). `looksLikeZip(bytes)` matches the PK magic (`\x50\x4b\x03\x04`, `\x05\x06` empty-zip, or `\x07` spanning) for the drop-zone routing decision. **`ingestEntries(entries, opts?)`** runs the existing single-file ingest paths (`ingestPdfBuffer` / `ingestDocxBuffer`) over an accepted bundle; per-file errors surface as rejections so a single corrupt file does not poison the whole bundle. **`ingestBundle(candidates, opts?)`** is the convenience wrapper that combines `planBundle` + `ingestEntries`. New dep: `fflate@^0.8.3` (28 KB unpacked, MIT, zero runtime deps, used both for §8 unzip and §11 bundle-zip output downstream). Ingest barrel re-exports the v4 surface alongside the v1 entries; v1 callers are untouched. Tests cover: `classifyExtension` case-insensitive matching + null on unknown; `rejectionForFilename` `.doc` and generic paths; `planBundle` cap enforcement (empty / > 50 files / > 200 MB / per-file > 50 MB / mixed valid + invalid extensions); `looksLikeZip` PK magic variants; `extractZipEntries` `.pdf`+`.docx` filtering, nested-folder basename flattening, `__MACOSX/` skip, deterministic ordering across two differently-ordered archives, defensive zip-slip behavior. All gates green: typecheck clean, lint clean, **810/810 tests + 2 skips**, build green. The browser `enumerateFolderEntry` path is not exercised by the unit tests because `FileSystemEntry` is a browser-only DOM API (the Playwright Step 41 follow-up will cover it under `tests/e2e/v4/`). The wiring into the UI dropzone (Step 33 carry-forward / spec §62 multi-doc drop UI) and the bundle pipeline (`runPipelineMulti` in `src/ui/pipeline.ts` consuming `ingestBundle` + `runEngineMulti`) are deferred to a focused follow-up commit so this commit stays scoped to ingest. |
| 42 | Document classifier (two-stage) | ✅ complete | Two-stage classifier shipped per spec §9. **Stage 1 (sub-domain)**: new module [`src/extract/v4/classifier.ts`](src/extract/v4/classifier.ts) (210 lines) implements `scoreSubDomains()` and `classifyV4SubDomain()` over all 16 §6 sub-domains using v2-aligned scoring weights (title 0.3, distinguishing 0.2, negative -0.1) and a per-domain ceiling that normalizes raw scores into `[0, 1]` regardless of feature-table size. **Stage 2 (family)** is delegated to the existing v2 `matchPlaybook` matcher run over playbooks under the winning sub-domain — that hookup lands when v4 ruleset playbooks land (Steps 45–59), so today `family_id: null` returns from the stage-1 winner. **v3 short-circuit**: when a `V3Detection` with confidence ≥ 0.6 is passed in, the classifier maps the v3 family directly to its v4 sub-domain + family via an explicit `v3FamilyToV4()` table (BAA→I.1, DPA-EU→I.2, DPA-US-state→I.3, SCC→I.4, UK-IDTA→I.5, COI→K.1, NDA-deep→A.3, MSA-deep→A.1, vendor-security→I.6, AI-addendum→O.8) so a confident v3 detection is not second-guessed. **Threshold (spec §9, open question 8)**: 0.5 for sub-domain (below which `sub_domain: null` falls through to v2 `matchPlaybook` and ultimately `generic-fallback`); 0.5 for family (below which the sub-domain's `*-generic` playbook runs, hookup deferred to the per-sub-domain steps). **Feature data**: [`dkb/v4/sub-domain-features.json`](dkb/v4/sub-domain-features.json) (≈ 350 lines) anchored to the §6 catalog — 16 sub-domains × {title keywords, distinguishing phrases, negative features}: A-commercial (18 keywords, 14 phrases), B-governance (15 / 15), C-equity (16 / 18), D-m-and-a (13 / 17), E-real-estate (16 / 16), F-employment (12 / 13), G-settlement (10 / 11), H-ip-licensing (10 / 11), I-privacy (20 / 15), J-healthcare (4 / 10), K-insurance (7 / 12), L-banking (12 / 14), M-construction (11 / 11), N-trust-estate (12 / 14), O-compliance-policy (16 / 13), P-regulatory-prose (10 / 11). Negative features are calibrated for the most common cross-domain confusions (e.g., E-real-estate's "premises / lessor / lessee / tenant" subtract from A-commercial, B-governance, F-employment). **Diagnostic surface**: `rankedAlternatives(input, n=3)` returns the top-N scoring sub-domains for the per-document report's "Detected as: X (confidence 0.83). Alternatives: Y (0.21)." line (spec §9). Tests at [`tests/v4/extract/classifier.test.ts`](tests/v4/extract/classifier.test.ts) (23 passing): one positive detection per non-A sub-domain (B / C / D / E / F / G / K / L / M / N / O / P) each above the 0.5 threshold; threshold fallback (no-signal body and low-signal body both return `sub_domain: null`); v3 short-circuit (high-confidence BAA→I.1, low-confidence falls through to v4 stage 1, unknown returns null); `rankedAlternatives` ordering; determinism (two runs of the same input produce `toEqual` classifications); registry contract (exactly 16 sub-domains; every entry has ≥ 1 title + ≥ 1 phrase; thresholds + weights match spec). The classifier is wired into `src/extract/v4/index.ts` as the `classifyV4(extracted, body_text, features, v3_detection?)` convenience entry; pipeline integration (calling `classifyV4` from `src/ui/pipeline.ts` and threading the result into the per-document report header) is deferred to the consolidated-bundle commit (Step 44) so the classifier ships standalone. `npm run typecheck && lint && test && build` all green; **833/833 tests + 2 skips**. The v3 `matchPlaybook` flow remains the production path until Step 45+ playbooks register under the v4 sub-domains; the classifier is an additive surface today. |
| 43 | Consistency engine expansion | ✅ complete | Seven v4 CROSS-* rules ship under [`src/engine/consistency/rules/v4/`](src/engine/consistency/rules/v4/) on the existing v3 consistency engine, joining the seven v3 CC-NNN rules: **CROSS-PARTY-001** (info; detects party-name drift across documents — "Acme Corp." vs "Acme, Inc." canonicalize to the same entity after suffix-stripping but the originals diverge, surfaced because lenders / IP licensors / tax authorities care about the legal name on the executed copy); **CROSS-JURIS-001** (warning; complements v3 CC-005 which covers any-pair governing-law misalignment by firing only when 3+ documents are bundled together with 2+ different governing-law clauses — the precedence story at the bundle level requires an explicit recital); **CROSS-DEFTERM-001** (warning; flags defined terms with different definitions across documents — the looser definition controls in practice, the prevalent source of multi-doc litigation); **CROSS-DATE-001** (warning; detects effective-date chronology that is impossible — doc A dated 2026-01-01 references doc B which is itself dated 2026-06-01 by family name); **CROSS-AMOUNT-001** (warning; aggregate-liability-cap mismatch with the lower cap controlling for any conduct that touches both documents); **CROSS-MISSING-001** (warning; an operative document references a companion family by name — "DPA attached as Schedule B", "Business Associate Agreement" — but no document of that family is in the bundle); **CROSS-PRECEDENCE-001** (warning; complements v3 CC-007 which fires when MSA-controls + subordinate document has operative terms, by firing when two documents BOTH claim controlling status — "this Agreement shall control" in the MSA and "this SOW shall control" in the SOW). New helpers at [`src/engine/consistency/rules/v4/_helpers.ts`](src/engine/consistency/rules/v4/_helpers.ts): `normalizePartyName(name)` strips entity suffixes (Inc / Corp / LLC / L.P. / Ltd / Co / PLC / GmbH / SA / SARL / NV / AG / Pte / Sdn Bhd) + punctuation; `findPartyNameMismatches` dedupes parties per-document by canonical name + picks the longest original-name representative (the extractor sometimes returns the same party twice with subtly different boundaries — "Acme Corp.," vs "Acme Corp" — which would otherwise fire CROSS-PARTY against the same document); `findDefinedTermMismatches` walks `extracted.definitions.entries` to find terms with non-matching definitions; `effectiveDateOf` prefers the named-anchor "Effective Date" then falls back to the first absolute date; `firstLiabilityCap` scans for an aggregate-liability anchor and surfaces the highest dollar amount in the matched paragraph with scale-suffix parsing ("$1M" → 1,000,000). All rules use the existing v3 `ConsistencyRule` shape and `ConsistencyFinding` output — no new types added. Registry barrels: `src/engine/consistency/rules/v4/index.ts` exports `V4_CROSS_RULES` (length 7); `src/engine/consistency/rules/index.ts` adds `ALL_CONSISTENCY_RULES: readonly ConsistencyRule[] = [...V3_CC, ...V4_CROSS]` (length 14); the engine top-level barrel re-exports everything. Tests at [`src/engine/consistency/rules/v4/cross-doc-rules.test.ts`](src/engine/consistency/rules/v4/cross-doc-rules.test.ts) (**18 passing**): registry contract (7 rules, unique CROSS-CATEGORY-NNN ids), `ALL_CONSISTENCY_RULES` size invariant, one positive + one negative case per rule (Acme Corp / Acme Inc → CROSS-PARTY fires; identical-name → does not; 3-doc 2-law → CROSS-JURIS fires; 3-doc 1-law → does not; 2-doc → does not, CC-005 covers it; mismatched "Customer Data" definitions → CROSS-DEFTERM fires; matched → does not; MSA-Jan refs BAA-Jun → CROSS-DATE fires; MSA-Jul refs BAA-Jun → does not; $1M vs $50K caps → CROSS-AMOUNT fires; matched caps → does not; MSA refs DPA but no DPA in bundle → CROSS-MISSING fires; DPA present → does not; MSA-and-SOW-both-claim-control → CROSS-PRECEDENCE fires; only MSA claims → does not); determinism (V4_CROSS_RULES produces identical `result_hash` on two runs over the same bundle). **Test infrastructure fix**: discovered and fixed a race condition between `tests/integration/bundle-size.test.ts` and `tests/integration/sri.test.ts` — both shared `dist/`, the SRI test's `beforeAll` did `rmSync(DIST) + npm run build`, and the bundle-size test could land in the half-second window where `assets/` existed but `main-*.js` hadn't been written yet. Fix: bundle-size `beforeAll` now polls (up to 60s, `Atomics.wait` 250ms increments) for the eager `main-*.js` chunk to appear, falling back to running its own build only when `assets/` is genuinely empty. `npm run typecheck && lint && test && build` all green; **851/851 tests + 2 skips**. The v3 CC-NNN engine is unchanged; `runConsistency` is a single deterministic engine that accepts either rule set or the combined `ALL_CONSISTENCY_RULES` registry. Wiring `ALL_CONSISTENCY_RULES` into `src/ui/pipeline.ts` and the consolidated bundle report (§59 → §11) is the Step 44 follow-up. |
| 44 | Consolidated bundle report renderer | ✅ complete | New module at [`src/report/bundle.ts`](src/report/bundle.ts) (~430 lines) ships the spec §11 consolidated bundle artifact set: **`buildBundleDocxReport(input)`** assembles the DOCX with cover (bundle fingerprint, document count, engine version, DKB version, cross-document-rules count, analysis date), executive summary (per-document totals + cross-document totals as a side-by-side table), per-document subsections (file name, detected family, playbook id, file SHA-256, per-document result hash, severity counts, and the top `BUNDLE_TOP_N = 10` findings per spec §11 — already in (severity, rule_id, document_position) order from the single-doc runner), cross-document consistency appendix (every `CROSS-*` finding with severity badge, description, explanation, recommendation, and one indented italic excerpt line per contributing document), deduped citation bibliography (built by walking per-doc findings first, then cross-doc findings — first-reference numbering, 1-based, deterministic), audit trail (engine + DKB + consistency-engine versions, per-doc playbook list with confidence, full per-doc and cross-doc rule execution logs with elapsed times), and the standard v1 §22 disclaimer block. **`buildBundleJson(input)`** returns `{ runs, cross_doc_findings, bundle_fingerprint, dkb_version, engine_version }` per spec §11; `buildBundleJsonBlob` wraps it in an `application/json` Blob (pretty-printed for humans). **`bundleFingerprint(per_doc_result_hashes)`** is `sha256(sorted-hashes joined by "\n")` — order-independent and deterministic; each per-doc `result_hash` already folds in `source_file.sha256`, so the bundle fingerprint covers both inputs and rule outcomes. **`buildBundleZip(input)`** produces an `application/zip` Blob containing `consolidated-report.docx` + `bundle.json` (and any `per_document_artifacts` entries placed under `per-document/`), entries written in lexicographic order with a fixed `mtime = 2000-01-01 UTC` per-entry so the zip envelope is byte-identical across runs (fflate refuses mtimes outside 1980–2099, which is why we cannot use `0`). The fflate dependency was already added in Step 41 for ingest; this commit consumes it for the §11 output path — one library, two paths, as the spec §11 footnote notes. Tests at [`src/report/bundle.test.ts`](src/report/bundle.test.ts) (**13 passing**): `bundleFingerprint` order-independence + 64-char hex + value sensitivity; `buildBundleJson` shape (runs.length, cross_doc_findings.length, fingerprint regex, versions); `buildBundleJsonBlob` MIME type + pretty-printed delimiter; bundle-JSON determinism via `JSON.stringify(a) === JSON.stringify(b)`; consolidated DOCX non-empty + OOXML MIME + ZIP `PK\x03\x04` magic; zero-cross-doc-findings rendering path; `BUNDLE_TOP_N === 10` invariant; bundle zip entry listing (`bundle.json` + `consolidated-report.docx`); bundle zip with `per_document_artifacts` placed under `per-document/`; bundle-JSON-inside-zip byte-identical across two runs (proof the JSON layer + zip framing are deterministic; the DOCX layer cannot be byte-identical because the `docx` library timestamps internally). `npm run typecheck && lint && test && build` all green; **864/864 tests + 2 skips**. Pipeline integration (`src/ui/pipeline.ts` calling `runEngineMulti` + `buildBundleZip` and surfacing a download button) is the natural follow-up commit — `src/ui/pipeline.ts` is the only consumer that needs to know about the bundle artifact set. |
| 45 | Sub-domain B (Corporate governance) — ~80 rules | ✅ complete | 80 rules at [`src/engine/rules/v4/governance/rules.ts`](src/engine/rules/v4/governance/rules.ts) split into the eight sub-families (`BYLAWS_RULES` 12, `OP_AGREEMENT_RULES` 10, `CHARTER_RULES` 10, `STOCKHOLDERS_AGREEMENT_RULES` 10, `WRITTEN_CONSENT_RULES` 8, `COMMITTEE_CHARTER_RULES` 10, `PARTNERSHIP_RULES` 10, `NONPROFIT_RULES` 10) re-exported by `GOVERNANCE_RULES`. Rule ids are flat `GOV-001..GOV-080`; each rule scopes via `applies_to_playbooks` to the eight v4 governance playbook ids (`bylaws-corporation`, `operating-agreement-llc`, `charter-incorporation`, `stockholders-agreement`, `written-consent`, `committee-charter`, `partnership-agreement`, `nonprofit-bylaws`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Shared v4 rule-builder helpers landed at [`src/engine/rules/v4/_helpers.ts`](src/engine/rules/v4/_helpers.ts) (`buildV4PresenceRule`, `buildV4LanguageRule`, `buildV4CompoundRule`, `v4Cite`) — same two-shape pattern as v3 BAA / NDA-deep / MSA-deep but parametric over playbook list + category so all subsequent v4 sub-domains (Steps 46–59) reuse them. Citation factories at [`src/engine/rules/v4/governance/_helpers.ts`](src/engine/rules/v4/governance/_helpers.ts) cover DGCL Title 8, MBCA, DE LLC Act, DRULPA, RUPA, NYSE Listed Company Manual § 303A, Nasdaq Listing Rule 5605, SOX § 301, IRC § 501(c)(3) / § 704(b) / § 6223, IRS Form 990, the NVCA model docs, ABA Model Nonprofit Corp Act, and the *Sciabacucchi / Salzberg* federal-forum line. `V4_RULES` aggregate at [`src/engine/rules/v4/index.ts`](src/engine/rules/v4/index.ts) is wired (currently `[...GOVERNANCE_RULES]`), and the engine top-level barrel now exports `V4_RULES` and `GOVERNANCE_RULES`. Tests at [`src/engine/rules/v4/governance/governance-ruleset.test.ts`](src/engine/rules/v4/governance/governance-ruleset.test.ts) (**8 passing**): registry contract (exactly 80, unique `GOV-NNN` ids, `governance` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 8-id set); cross-playbook-noop (no rule fires when the active playbook is non-governance); compliant-bylaws fixture emits zero criticals + is deterministic across runs (`result_hash` equality); GOV-001 (missing amendment authority) / GOV-022 (operating agreement waiving the implied covenant, statutorily prohibited under DE LLC Act § 18-1101(c)) / GOV-080 (compound rule — three pillars of the § 501(c)(3) organizational test must all appear) all fire on targeted failure cases. `npm run typecheck && lint && test && build` all green; **872/872 tests + 2 skips**. Wiring `V4_RULES` into `src/ui/pipeline.ts` and fetching v4 playbooks at runtime is the natural follow-up; today the v4 playbooks sit alongside the v3 playbooks under `src/playbooks/v4/` and are loaded explicitly by tests + future pipeline updates. |
| 46 | Sub-domain C (Equity / cap-table) — ~70 rules | ✅ complete | 70 rules at [`src/engine/rules/v4/equity/rules.ts`](src/engine/rules/v4/equity/rules.ts) split into nine sub-families: `SAFE_RULES` 9 (YC post-money / pre-money SAFE — variant identification, Equity Financing definition, cap / discount / MFN economics, liquidity / dissolution event treatment, accredited-investor reps, **EQT-008** flags interest-bearing language inconsistent with SAFE template), `CONVERTIBLE_NOTE_RULES` 9 (UCC Art. 3 principal / interest / maturity, qualified-financing conversion, change-of-control treatment, subordination, events of default, **EQT-012** flags interest rates at ≥ 25% per annum against state usury caps), `OPTION_GRANT_RULES` 10 (IRC § 422 ISO / § 409A NSO — grant date, exercise price, FMV-at-grant safe harbor, 4-year vesting, ISO vs NSO designation, § 422(d) $100K limit, post-termination window, 10-year max term), `RSU_RULES` 7 (§ 409A short-term deferral, tax withholding mechanics, **EQT-033** § 83(b) not available for RSUs), `RSPA_RULES` 7 (§ 83 restricted stock, repurchase right, **EQT-038** 83(b) advisory with 30-day deadline, escrow / stock-power, IPO lock-up, Rule 144 legend), `ELECTION_83B_RULES` 6 (Treas. Reg. § 1.83-2 required content — taxpayer ID, property description, transfer date, FMV / amount-paid, plus **EQT-048** compound rule for the 30-day filing window + IRS + employer-copy procedural recitals), `IRA_RULES` 8 (NVCA model investor rights agreement — demand / piggyback / S-3 registration, pro rata, info rights, ROFO, market stand-off, IPO termination), `VOTING_AGREEMENT_RULES` 7 (DGCL § 218 voting covenants, drag-along, irrevocable proxy under § 212, transferee joinder, specific-performance remedies), `ROFR_RULES` 7 (DGCL § 202 — company primary ROFR, investor secondary tier, co-sale, permitted-transfer carve-outs, notice / election windows, § 202 noting requirement). Rule ids `EQT-001..EQT-070`; every rule scopes via `applies_to_playbooks` to the nine v4 equity playbook ids (`safe-yc`, `convertible-note`, `stock-option-grant`, `rsu-grant`, `rspa`, `section-83b-election`, `investor-rights-agreement`, `voting-agreement`, `rofr-co-sale`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/equity/_helpers.ts`](src/engine/rules/v4/equity/_helpers.ts) cover IRC § 83 / § 83(b) / § 409A / § 422 / § 3401 / § 422(d), Treas. Reg. § 1.83-2 / § 1.83-7 / § 1.409A-1(b)(4) / § 1.409A-1(b)(5)(iv), Y Combinator SAFE templates, NVCA model legal documents (IRA, voting agreement, ROFR / co-sale, drag-along, pro rata, info rights, S-3), UCC Article 3 §§ 3-104 / 3-108, DGCL §§ 102 / 145 / 151 / 202 / 212 / 218, SEC Reg D Rule 506 / Rule 144, FINRA Rule 5131 (180-day IPO lock-up), and a generic state-usury-cap reference per spec §12. `V4_RULES` aggregate now `[...GOVERNANCE_RULES, ...EQUITY_RULES]` (150 rules total) and the engine top-level barrel re-exports `EQUITY_RULES`. Tests at [`src/engine/rules/v4/equity/equity-ruleset.test.ts`](src/engine/rules/v4/equity/equity-ruleset.test.ts) (**9 passing**): registry contract (exactly 70, unique `EQT-NNN` ids, `equity` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 9-id set); cross-playbook-noop (no rule fires under a non-equity playbook); compliant-SAFE fixture emits zero criticals + deterministic across runs; failure cases for **EQT-008** (interest-bearing SAFE), **EQT-012** (30% per annum convertible note → usury flag), **EQT-020** (option grant missing exercise price), and **EQT-048** (incomplete § 83(b) procedural recitals). `npm run typecheck && lint && test && build` all green; **881/881 tests + 2 skips** (the previously documented bundle-size flake against `tests/integration/sri.test.ts` is unchanged and re-runs green). |
| 47 | Sub-domain D (M&A and investment) — ~80 rules | ✅ complete | 80 rules at [`src/engine/rules/v4/m-and-a/rules.ts`](src/engine/rules/v4/m-and-a/rules.ts) split into nine sub-families: `LOI_TERM_SHEET_RULES` 9 (binding / non-binding demarcation per the *SIGA / Pennzoil* line, exclusivity, confidentiality, price / structure / conditions outlines, expenses, drop-dead date, governing law — **MNA-001** is a compound rule requiring three binding-LOI pillars to appear together), `SPA_RULES` 10 (operative purchase-and-sale, working-capital / debt / cash adjustment, Article III reps, indemnification with survival / basket / cap, conditions to closing including bring-down and no-MAE, **MAE-defined** anchored to *Akorn / Channel Medsystems*, **MNA-016** non-compete on selling stockholders under the FTC NCR sale-of-business exception, sandbagging clause per ABA deal-points, stockholder representative, governing law), `APA_RULES` 9 (purchased / excluded assets, assumed / excluded liabilities, **MNA-024** state bulk-sales-law treatment, IRC § 1060 purchase-price allocation / Form 8594, UCC § 9-406 anti-assignment override, WARN-Act / employee-transfer, bill-of-sale + assignment-and-assumption ancillaries), `MERGER_RULES` 10 (DGCL § 251 plan of merger / surviving corp, merger consideration mechanics, DGCL § 262 appraisal-rights notice, stockholder approval, no-solicit / fiduciary-out per *Omnicare*, break-up fee, drag-along / support-agreement reference, certificate-of-merger filing, option / RSU treatment, structure recital), `DISCLOSURE_SCHEDULE_RULES` 7 (general-notes introduction, **MNA-040** language rule flagging data-room-reference disclosure per the *Cobalt International* line, cross-section qualification convention, materiality disclaimer, section numbering, supplemental-disclosure update mechanic, confidentiality marking), `ESCROW_AGREEMENT_RULES` 9 (escrow agent identification, escrow amount / asset / account, release / claim mechanics, dispute resolution / interpleader, agent indemnification, investment of funds, tax reporting / 1099 / grantor-trust treatment, termination / final release, notices), `TSA_RULES` 8 (services schedule, service period and extension, fee structure cost-plus / fixed, service-level performance standards, liability cap with carve-outs, confidentiality / data protection, per-service termination, migration / cooperation), `EARNOUT_RULES` 9 (earnout period and milestones, revenue / EBITDA definitions with GAAP anchoring, conduct-of-business covenant, **MNA-066** implied-covenant acknowledgment per *Lazard v. Qinetiq* / *Aveta v. Cavallieri*, **MNA-067** language rule flagging duty-to-maximize disclaimers that Delaware will not enforce as a complete shield, calculation / payment mechanics with independent-accountant dispute resolution, set-off, change-of-control acceleration, IRC § 483 / § 1274 imputed-interest tax treatment), `MA_RESTRICTIVE_COVENANT_RULES` 9 (sale-of-business recital under FTC NCR § 910.2(a)(2), non-compete duration, **MNA-074** language rule flagging non-competes > 5 years, geographic scope, Competing Business definition, customer / employee non-solicits, blue-pencil / reformation, equitable remedies). Rule ids `MNA-001..MNA-080`; every rule scopes via `applies_to_playbooks` to the nine v4 M&A playbook ids (`loi-term-sheet`, `stock-purchase-agreement`, `asset-purchase-agreement`, `merger-agreement`, `disclosure-schedules`, `escrow-agreement`, `transition-services-agreement`, `earnout-agreement`, `ma-restrictive-covenant`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/m-and-a/_helpers.ts`](src/engine/rules/v4/m-and-a/_helpers.ts) cover DGCL §§ 115, 251, 251(c), 262, 271, UCC § 9-406, state bulk-sales law (former UCC Art. 6), the Delaware *Lazard / Aveta* earnout line, the *SIGA / Pennzoil* binding-LOI line, the *Omnicare* fiduciary-out line, *Akorn / Channel Medsystems* MAE line, FTC Non-Compete Rule (16 C.F.R. § 910.2(a)(2)), ABA Model Stock / Asset Purchase Agreement, and ABA Private Target M&A Deal Points Study. `V4_RULES` now `[...GOVERNANCE_RULES, ...EQUITY_RULES, ...M_AND_A_RULES]` (230 rules total) and the engine top-level barrel re-exports `M_AND_A_RULES`. Tests at [`src/engine/rules/v4/m-and-a/m-and-a-ruleset.test.ts`](src/engine/rules/v4/m-and-a/m-and-a-ruleset.test.ts) (**10 passing**): registry contract (exactly 80, unique `MNA-NNN` ids, `m-and-a` category, every rule has `applies_to_playbooks`); playbook-id whitelist; cross-playbook-noop; compliant-LOI fixture emits zero criticals + deterministic across runs; failure cases for **MNA-001** (LOI omits binding / non-binding demarcation), **MNA-010** (SPA omits operative purchase-and-sale), **MNA-024** (APA omits bulk-sales clause), **MNA-040** (disclosure schedule discloses by data-room reference), and **MNA-067** (earnout disclaims duty to maximize). `npm run typecheck && lint && test && build` all green; **891/891 tests + 2 skips** (the documented bundle-size race against `tests/integration/sri.test.ts` is unchanged and re-runs green). |
| 48 | Sub-domain E (Real estate, expanded) — ~60 rules | ✅ complete | 60 rules at [`src/engine/rules/v4/real-estate/rules.ts`](src/engine/rules/v4/real-estate/rules.ts) split into eight new sub-families (v1's residential and commercial-multi-tenant leases continue under the launch playbooks; v4 fills the gaps): `NET_LEASE_RULES` 8 (single-tenant NNN cost allocation, tax pass-through, CGL / property insurance with waiver of subrogation, roof / structural maintenance split, tenant CAM audit right, casualty / condemnation, relet / mitigation, holdover / surrender), `PSA_RULES` 9 (Statute-of-Frauds-grade legal description, earnest money, due-diligence period, title commitment / objection / cure, closing conditions, risk of loss before closing, seller reps, **RE-016** IRC § 1031 like-kind-exchange cooperation, brokers), `GROUND_LEASE_RULES` 7 (49 / 50 / 99-year term, tenant construction rights, leasehold mortgage + lender-protection / new-lease right, reversion of improvements, CPI / market-rent escalation, assignment / subletting consent, recorded memorandum), `EASEMENT_RULES` 7 (servient / dominant tenement, appurtenant vs in gross, scope of use, maintenance / repair allocation, indemnification / insurance, term / termination triggers, recording covenant), `CCR_RULES` 8 (declaration identification, HOA / owners-association formation, assessment / lien rights, use restrictions / architectural control, supermajority amendment procedure, term / automatic-extension, **RE-038** language rule flagging discriminatory race / color / religion covenants per *Shelley v. Kraemer* and 42 U.S.C. § 3604, dispute resolution / ADR), `ESTOPPEL_RULES` 6 (lease identification with amendments, rent / SD, no-default rep, full-force-and-effect, reliance / addressee, knowledge / authority qualifier), `SNDA_RULES` 8 (subordination, **RE-047** non-disturbance, attornment, successor liability limits, notice-to-lender / cure, no-prepayment-of-rent, no-modification-without-consent, recording), `LEASE_ASSIGNMENT_RULES` 7 (assignor / assignee / lease identification, landlord consent, assumption of obligations, release vs continuing liability, effective date, SD / prepaid-rent transfer, recording / mortgagee notice). Rule ids `RE-001..RE-060`; every rule scopes via `applies_to_playbooks` to the eight v4 real-estate playbook ids (`net-lease`, `real-estate-psa`, `ground-lease`, `easement-agreement`, `ccrs`, `estoppel-certificate`, `snda`, `lease-assignment`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/real-estate/_helpers.ts`](src/engine/rules/v4/real-estate/_helpers.ts) cover URLTA, state landlord-tenant codes (CA Civ. § 1940 / NY RPL § 220 / TX Prop. § 92.001), the Statute of Frauds (UCC § 2-201 + state real-property codes), state recording acts (race / notice / race-notice), the Uniform Easement Relocation Act + Restatement (Third) of Property — Servitudes, state HOA statutes (Davis-Stirling, FL ch. 720, TX § 209), IRC § 1031, and the *Shelley v. Kraemer* / FHA 42 U.S.C. § 3604 line on discriminatory restrictive covenants. `V4_RULES` now `[...GOVERNANCE_RULES, ...EQUITY_RULES, ...M_AND_A_RULES, ...REAL_ESTATE_RULES]` (290 rules total) and the engine top-level barrel re-exports `REAL_ESTATE_RULES`. Tests at [`src/engine/rules/v4/real-estate/real-estate-ruleset.test.ts`](src/engine/rules/v4/real-estate/real-estate-ruleset.test.ts) (**10 passing**): registry contract (exactly 60, unique `RE-NNN` ids, `real-estate` category, every rule has `applies_to_playbooks`); playbook-id whitelist; cross-playbook-noop; compliant-NNN fixture emits zero criticals + deterministic across runs; failure cases for **RE-001** (NNN cost allocation), **RE-009** (PSA legal description), **RE-038** (discriminatory CC&R), **RE-042** (estoppel no-default), and **RE-047** (SNDA non-disturbance). `npm run typecheck && lint && test && build` all green; **901/901 tests + 2 skips**. The 600KB-chunk-size build warning is pre-existing (pdfjs / mammoth / docx vendor splits already in place per spec.md §14 follow-up). |
| 49 | Sub-domain F (Employment, expanded) — ~50 rules | ✅ complete | 50 rules at [`src/engine/rules/v4/employment/rules.ts`](src/engine/rules/v4/employment/rules.ts) split into seven new sub-families (v1's at-will employment and independent-contractor playbooks continue under the launch set): `EXEC_EMPLOYMENT_RULES` 8 (title / duties / reporting line for Good-Reason hooks, base / bonus, **EMP-003** IRC § 409A compliance with 6-month specified-employee delay, **EMP-004** IRC § 280G best-net / cleansing-vote treatment, termination definitions Cause / Good Reason / CIC / Disability, severance schedule, Dodd-Frank § 954 / Rule 10D-1 clawback, restrictive-covenants reference), `OFFER_LETTER_RULES` 6 (position / start date / base comp, at-will statement, pre-employment conditions I-9 / background / references, equity-grant outline, plan-subject non-binding qualifier, acceptance / signature line), `SEPARATION_RULES` 9 (OWBPA § 626(f) full package — **EMP-015** 21 / 45-day consideration, **EMP-016** 7-day revocation, advise-to-consult-counsel, specific ADEA reference, group-termination decisional-unit disclosure for RIFs, **EMP-020** NLRB *McLaren Macomb* language rule flagging overbroad confidentiality / non-disparagement, SEC Rule 21F-17 / EEOC / NLRB protected-rights carve-out, consideration over and above accrued, California § 1542 waiver), `EMP_RESTRICTIVE_COVENANT_RULES` 8 (**EMP-024** language rule flagging worker non-competes under the FTC NCR 16 C.F.R. § 910 prohibition, duration, customer / employee non-solicit, geographic scope tied to actual market, consideration / Massachusetts G.L. c. 149 § 24L garden-leave, blue-pencil / reformation, equitable relief), `PIIA_RULES` 7 (proprietary-information confidentiality, invention assignment, prior-inventions disclosure, **EMP-035** Cal. Lab. § 2870 carve-out, POA for IP filings, 18 U.S.C. § 1833(b) DTSA notice, return of materials), `PIP_RULES` 6 (specific deficiencies, SMART measurable goals, 30 / 60 / 90 day duration + review schedule, employer-committed support / coaching, consequences-of-failure including termination, acknowledgment-of-receipt-not-agreement), `HANDBOOK_RULES` 6 (at-will / no-contract disclaimer, EEO / anti-harassment with *Faragher / Ellerth*-grade complaint procedure, FLSA wage-and-hour, FMLA + state leaves, **EMP-049** language rule flagging NLRA § 7 overbroad wage / working-condition / social-media restrictions per *Stericycle* 2023, acknowledgment-of-receipt page). Rule ids `EMP-001..EMP-050`; every rule scopes via `applies_to_playbooks` to the seven v4 employment playbook ids (`executive-employment`, `offer-letter`, `separation-agreement`, `employment-restrictive-covenant`, `piia`, `pip`, `employee-handbook`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/employment/_helpers.ts`](src/engine/rules/v4/employment/_helpers.ts) cover OWBPA / ADEA § 626(f) (29 U.S.C.), NLRB *McLaren Macomb* (372 NLRB No. 58, 2023), IRC § 409A + § 280G, SEC Reg S-K Item 402 + Rule 10D-1 / Dodd-Frank § 954, FTC Non-Compete Rule 16 C.F.R. § 910, state non-compete law (CA / MA / WA / NY / CO), Cal. Lab. § 2870, NLRA § 7 (29 U.S.C. § 157), FLSA (29 U.S.C. § 201), SEC Rule 21F-17, FMLA (29 U.S.C. § 2601), 18 U.S.C. § 1833(b) DTSA notice, Cal. Civ. § 1542, and EEOC enforcement guidance. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4]` (340 rules total) and the engine top-level barrel re-exports `EMPLOYMENT_V4_RULES`. Tests at [`src/engine/rules/v4/employment/employment-ruleset.test.ts`](src/engine/rules/v4/employment/employment-ruleset.test.ts) (**11 passing**): registry contract (exactly 50, unique `EMP-NNN` ids, `employment` category, every rule has `applies_to_playbooks`); playbook-id whitelist; cross-playbook-noop; compliant-separation fixture emits zero criticals + deterministic; failure cases for **EMP-015** (consideration period), **EMP-020** (overbroad non-disparagement / *McLaren Macomb*), **EMP-003** (executive missing § 409A), **EMP-024** (worker non-compete FTC NCR flag), **EMP-035** (PIIA missing § 2870), and **EMP-049** (handbook wage-discussion ban / NLRA § 7). `npm run typecheck && lint && test && build` all green; **912/912 tests + 2 skips**. |
| 50 | Sub-domain G (Settlement / release / demand) — ~30 rules | ✅ complete | 30 rules at [`src/engine/rules/v4/settlement/rules.ts`](src/engine/rules/v4/settlement/rules.ts) split into six new sub-families: `RELEASE_RULES` 5 (mutual / general release — releasing / released parties with affiliates, scope of claims known-and-unknown, **SET-003** Cal. Civ. § 1542 waiver, no-admission per FRE 408, **SET-005** language rule flagging overbroad future-claims releases under Cal. Civ. § 1668), `SETTLEMENT_AGREEMENT_RULES` 5 (settlement consideration, **SET-007** NLRB *McLaren Macomb* overbroad confidentiality / non-disparagement language rule, SEC Rule 21F-17 / EEOC / NLRB whistleblower carve-out, tax allocation 1099 / W-2 / IRC § 104, **SET-010** IRC § 162(q) Harvey-Weinstein-rule recital for sexual-harassment / abuse nondisclosure settlements), `DEMAND_LETTER_RULES` 5 (statement of facts + legal theory, specific demand + response deadline, **SET-013** FDCPA § 1692e language rule flagging arrest / criminal-prosecution threats, reservation of rights, **SET-015** Cal. Lab. § 2699.3 PAGA pre-suit notice elements), `CEASE_DESIST_RULES` 5 (Lanham Act § 43(a) — protected-IP identification with reg / patent numbers, infringing-conduct description, cease + remedial demand, response deadline, **SET-020** language rule flagging *Flatley v. Mauro* extortion / anti-SLAPP risk from unrelated-criminal-prosecution / employer-contact threats), `TOLLING_RULES` 4 (tolled-claims identification, tolling period start / end / triggers, no-revival of already-barred claims, termination on notice), `LIT_HOLD_RULES` 6 (FRCP 37(e) + *Zubulake* — triggering event, scope categories ESI / email / chat / mobile, **SET-027** suspension of routine deletion / retention policies, custodian list + acknowledgment, point of contact, attorney-work-product / confidentiality reminder). Rule ids `SET-001..SET-030`; every rule scopes via `applies_to_playbooks` to the six v4 settlement playbook ids (`mutual-release`, `confidential-settlement`, `demand-letter`, `cease-and-desist`, `tolling-agreement`, `litigation-hold`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/settlement/_helpers.ts`](src/engine/rules/v4/settlement/_helpers.ts) cover Cal. Civ. Code § 1542 + § 1668, NLRB *McLaren Macomb* (372 NLRB No. 58, 2023), SEC Rule 21F-17, FRE 408, IRC § 162(q) + § 104(a)(2), FDCPA (15 U.S.C. § 1692 et seq.), Lanham Act § 43(a) (15 U.S.C. § 1125), state statutes of limitations, Cal. Lab. § 2699.3 (PAGA notice), FRCP 37(e), and *Zubulake v. UBS Warburg LLC* (S.D.N.Y. 2003). `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT]` (370 rules total) and the engine top-level barrel re-exports `SETTLEMENT_RULES`. Tests at [`src/engine/rules/v4/settlement/settlement-ruleset.test.ts`](src/engine/rules/v4/settlement/settlement-ruleset.test.ts) (**11 passing**): registry contract (exactly 30, unique `SET-NNN` ids, `settlement` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 6-id set); cross-playbook-noop; compliant-release fixture emits zero criticals + deterministic across runs (`result_hash` equality); failure cases for **SET-001** (release omits party identification), **SET-007** (overbroad confidentiality / non-disparagement *McLaren Macomb*), **SET-013** (FDCPA arrest-threat language), **SET-016** (cease-and-desist missing IP identification), **SET-021** (tolling agreement missing tolled-claims identification), and **SET-027** (litigation hold missing suspension-of-deletion). `npm run typecheck && lint && test && build` all green; **923/923 tests + 2 skips**. |
| 51 | Sub-domain H (IP and licensing, expanded) — ~40 rules | ✅ complete | 40 rules at [`src/engine/rules/v4/ip-licensing/rules.ts`](src/engine/rules/v4/ip-licensing/rules.ts) split into seven new sub-families (v3 EULA continues under its existing playbook): `IP_ASSIGNMENT_RULES` 6 (assignor / assignee identification, assigned-IP scope with patent / app / reg numbers, right-to-sue for past + present + future infringement per *Crown Die & Tool* / *Arachnid v. Merit*, recordation cooperation under 35 U.S.C. § 261 / 17 U.S.C. § 205, POA for IP-office filings, assignor reps as to ownership / no-encumbrances / no prior conveyance), `PATENT_LICENSE_RULES` 6 (Licensed Patents identification, license-grant scope — exclusivity / field / territory / sublicensing, **IPL-009** language rule flagging royalties extending beyond patent expiration per the *Brulotte v. Thys* / *Kimble v. Marvel* line, royalty rate / base / audit mechanics, 35 U.S.C. § 287 patent-marking, improvements / grant-back addressed for DOJ / FTC misuse concerns), `TM_LICENSE_RULES` 6 (Licensed Marks identification with reg. numbers, **IPL-014** Lanham Act quality control to avoid *Stanfield* / *Eva's Bridal* naked-license abandonment, goodwill-inures-to-licensor + no-challenge, proper-use guidelines for ® / ™ / no genericization, territory / channels / field of use, termination tied to quality / non-payment / change-of-control with sell-off period), `COPYRIGHT_LICENSE_RULES` 6 (Licensed Works with TX / VA / PA / SR reg numbers, exclusivity + 17 U.S.C. § 204(a) signed writing for exclusive grants per *Effects Associates v. Cohen*, 17 U.S.C. § 106 rights enumeration, term / territory / media including 'now known or hereafter developed', VARA / moral-rights treatment, 17 U.S.C. § 203 35–40 year termination-of-transfers caveat), `CLA_RULES` 5 (contributor identification with individual ICLA vs corporate CCLA disambiguation, perpetual / worldwide / royalty-free copyright license grant, **IPL-027** patent license grant + defensive termination per the OSS patent-peace model, original-work representation, Developer Certificate of Origin 1.1 sign-off alternative), `OSS_COMPLIANCE_RULES` 6 (third-party / OSS component inventory via SBOM / SPDX / CycloneDX, per-component license enumeration using SPDX identifiers, **IPL-032** GPL / LGPL / AGPL source-availability + AGPL network-use handling, NOTICE / THIRD-PARTY-NOTICES file generation per Apache-2.0 § 4(d), forbidden / discouraged-license policy for SSPL / BUSL / Commons Clause / ELv2, CVE / vulnerability tracking per NIST SSDF / EO 14028 / CISA SBOM guidance), `WFH_RULES` 5 (**IPL-036** 17 U.S.C. § 101 specially-commissioned-category recital required for non-employee work-for-hire, backup-assignment for failed § 101 categorization, 18 U.S.C. § 1833(b) DTSA whistleblower notice to preserve exemplary damages, moral-rights waiver where waivable, independent-contractor status + tax / no-benefits disclaimer). Rule ids `IPL-001..IPL-040`; every rule scopes via `applies_to_playbooks` to the seven v4 IP-licensing playbook ids (`ip-assignment`, `patent-license`, `trademark-license`, `copyright-license`, `contributor-license-agreement`, `oss-compliance`, `work-for-hire-agreement`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/ip-licensing/_helpers.ts`](src/engine/rules/v4/ip-licensing/_helpers.ts) cover 35 U.S.C. §§ 261 / 271 / 287, 17 U.S.C. §§ 101 / 106 / 106A / 201 / 203 / 204 / 205, Lanham Act §§ 5 / 32 / 45, 18 U.S.C. § 1833(b) (DTSA), *Brulotte v. Thys* (379 U.S. 29, 1964) / *Kimble v. Marvel Entm't, LLC* (576 U.S. 446, 2015), the Apache Software Foundation ICLA v2.2, the Developer Certificate of Origin 1.1, the OSI-approved license families (GPL / AGPL / LGPL, MIT / Apache 2.0 / BSD), and CISA SBOM guidance. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING]` (410 rules total) and the engine top-level barrel re-exports `IP_LICENSING_RULES`. Tests at [`src/engine/rules/v4/ip-licensing/ip-licensing-ruleset.test.ts`](src/engine/rules/v4/ip-licensing/ip-licensing-ruleset.test.ts) (**12 passing**): registry contract (exactly 40, unique `IPL-NNN` ids, `ip-licensing` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 7-id set); cross-playbook-noop; compliant-assignment fixture emits zero criticals + deterministic across runs; failure cases for **IPL-001** (assignment omits parties), **IPL-009** (patent royalty extending beyond expiration *Brulotte / Kimble*), **IPL-014** (trademark license missing quality control / naked-license), **IPL-020** (copyright license missing exclusivity / § 204 writing), **IPL-027** (CLA missing patent license / defensive termination), **IPL-032** (OSS compliance missing copyleft source-availability), and **IPL-036** (WFH missing § 101 specially-commissioned recital). `npm run typecheck && lint && test && build` all green; **935/935 tests + 2 skips**. |
| 52 | Sub-domain I (Privacy, expanded) — ~40 rules | ✅ complete | 40 rules at [`src/engine/rules/v4/privacy-extended/rules.ts`](src/engine/rules/v4/privacy-extended/rules.ts) split into six new families (v3 BAA / DPA-GDPR / DPA-US-state / SCC / IDTA / privacy-policy continue under their existing rulesets): `COOKIE_NOTICE_RULES` 6 (cookie categories — strictly necessary / functional / analytics / advertising per EDPB Guidelines 5/2020, consent mechanism with affirmative action per CJEU *Planet49*, per-cookie table with name / provider / purpose / duration, **PRV-004** withdraw-consent as easy as giving per GDPR Art. 7(3), CCPA / CPRA 'Do Not Sell or Share' + GPC signal recognition, third-party recipients / international transfers including the EU-US Data Privacy Framework), `NPP_RULES` 8 (**PRV-007** verbatim 45 C.F.R. § 164.520(b)(1)(i) prescribed header, TPO + required + permitted + authorization uses, individual rights enumeration — access / amend / accounting / restrictions / confidential communications / paper copy / breach notification, covered-entity duties statement, right to complain to HHS OCR + no retaliation, effective date, contact information for privacy questions, high-sensitivity disclosures for 42 C.F.R. Part 2 substance use / mental health / HIV / GINA genetic), `ROPA_RULES` 6 (Art. 30(1)(a) controller / joint controller / representative / DPO, Art. 30(1)(b) purposes, Art. 30(1)(c) categories of data subjects + categories of personal data including Art. 9 special categories, Art. 30(1)(d) categories of recipients, Art. 30(1)(e) international transfers with Art. 46 safeguards / Art. 49 derogations, Art. 30(1)(f)–(g) retention periods + Art. 32 security measures), `DPIA_RULES` 6 (Art. 35(7)(a) systematic description of processing, Art. 35(7)(b) necessity / proportionality, **PRV-023** Art. 35(7)(c) risk assessment with likelihood + severity per EDPB / CNIL methodology, Art. 35(7)(d) mitigations + safeguards, Art. 35(2) DPO consultation record, Art. 36 prior-consultation trigger when residual high risk remains), `VENDOR_QUESTIONNAIRE_RULES` 7 (information security policy + governance per ISO 27001 A.5 / NIST CSF Govern, **PRV-028** access control — RBAC / least privilege / MFA / quarterly reviews, **PRV-029** AES-256 at rest + TLS 1.2+ in transit, third-party audits — SOC 2 Type II / ISO 27001 / HITRUST / FedRAMP, vulnerability management with patch SLAs by severity + annual penetration testing, documented incident-response plan + 72-hour customer-breach SLA, subprocessor / fourth-party disclosure with change notification), `INCIDENT_NOTIFICATION_RULES` 7 (Art. 33(3)(a) nature of incident, Art. 33(3)(a) categories + approximate numbers, Art. 33(3)(b) DPO contact, Art. 33(3)(c) likely consequences, Art. 33(3)(d) measures taken / proposed, **PRV-039** GDPR Art. 33 72-hour + HIPAA 60-day + state-specific timing thresholds, state-AG / OCR notification triggers at 500 / 1,000+ residents). Rule ids `PRV-001..PRV-040`; every rule scopes via `applies_to_playbooks` to the six v4 privacy-extended playbook ids (`cookie-notice`, `hipaa-npp`, `ropa-art-30`, `dpia-art-35`, `vendor-security-questionnaire`, `incident-notification`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/privacy-extended/_helpers.ts`](src/engine/rules/v4/privacy-extended/_helpers.ts) cover GDPR Art. 7 / 9 / 13 / 28 / 30 / 32 / 33 / 35 / 36 / 46 / 49, ePrivacy Directive 2002/58/EC (as amended), Cal. Civ. § 1798.100 et seq. (CCPA / CPRA), 45 C.F.R. § 164.520 + § 164.408 (HIPAA Privacy + Breach Rules), state breach-notification statutes (NCSL clearinghouse), NIST Cybersecurity Framework 2.0 + ISO/IEC 27001:2022, and EDPB / WP29 / CNIL guidance. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING, ...PRIVACY_EXTENDED]` (450 rules total) and the engine top-level barrel re-exports `PRIVACY_EXTENDED_RULES`. Tests at [`src/engine/rules/v4/privacy-extended/privacy-extended-ruleset.test.ts`](src/engine/rules/v4/privacy-extended/privacy-extended-ruleset.test.ts) (**11 passing**): registry contract (exactly 40, unique `PRV-NNN` ids, `privacy-extended` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 6-id set); cross-playbook-noop; compliant-NPP fixture emits zero criticals + deterministic across runs; failure cases for **PRV-001** (cookie notice missing categories), **PRV-007** (NPP missing verbatim § 164.520(b)(1)(i) header), **PRV-015** (ROPA missing controller / DPO), **PRV-023** (DPIA missing risk assessment), **PRV-029** (VSQ missing encryption at-rest / in-transit), and **PRV-039** (incident notification missing 72-hour / state timing). `npm run typecheck && lint && test && build` all green; **946/946 tests + 2 skips**. |
| 53 | Sub-domain J (Healthcare) — ~25 rules | ✅ complete | 25 rules at [`src/engine/rules/v4/healthcare/rules.ts`](src/engine/rules/v4/healthcare/rules.ts) across three new families (J.1 HIPAA NPP routes to Step 52 I.8; J.2 BAA routes to v3 `BAA_RULES`): `INFORMED_CONSENT_RULES` 9 (45 C.F.R. § 46.116 Common Rule basic elements — research statement + purpose + duration, foreseeable risks / discomforts, benefits including absence of direct benefit, alternative procedures, confidentiality of records including FDA / sponsor / IRB access, **HC-006** voluntary participation + withdrawal without penalty / loss of benefits, contacts for research questions + research-related injury + IRB subject rights, FDA § 50.25 additional elements when applicable — clinicaltrials.gov + FDA inspection, compensation / medical treatment for research-related injury for more-than-minimal-risk studies), `PHI_AUTHORIZATION_RULES` 9 (§ 164.508(c)(1)(i) specific information description, § 164.508(c)(1)(ii) authorized discloser, § 164.508(c)(1)(iii) recipient, § 164.508(c)(1)(iv) purpose, **HC-014** § 164.508(c)(1)(v) expiration date / event, § 164.508(c)(2)(i) right-to-revoke in writing with reliance exception, § 164.508(c)(2)(ii) no-conditioning of treatment / payment / enrollment / benefits, § 164.508(c)(2)(iii) re-disclosure risk that information will no longer be HIPAA-protected, § 164.508(c)(1)(vi) signature + date + personal-representative authority), `NPP_ACK_RULES` 7 (§ 164.520(c)(2)(ii) acknowledgment of receipt language, date of receipt, signature with personal-representative authority, good-faith-effort failure recital, right-to-paper-copy statement, covered-entity identification, § 164.530(j) 6-year retention notation). Rule ids `HC-001..HC-025`; every rule scopes via `applies_to_playbooks` to the three v4 healthcare playbook ids (`informed-consent`, `phi-authorization`, `npp-acknowledgment`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/healthcare/_helpers.ts`](src/engine/rules/v4/healthcare/_helpers.ts) cover 45 C.F.R. § 46 (Common Rule — Protection of Human Subjects), 21 C.F.R. § 50 (FDA informed consent), and 45 C.F.R. § 164.508 + § 164.520 + § 164.530 (HIPAA). `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING, ...PRIVACY_EXTENDED, ...HEALTHCARE]` (475 rules total) and the engine top-level barrel re-exports `HEALTHCARE_RULES`. Tests at [`src/engine/rules/v4/healthcare/healthcare-ruleset.test.ts`](src/engine/rules/v4/healthcare/healthcare-ruleset.test.ts) (**8 passing**): registry contract (exactly 25, unique `HC-NNN` ids, `healthcare` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 3-id set); cross-playbook-noop; compliant-IC fixture emits zero criticals + deterministic across runs; failure cases for **HC-006** (IC omits voluntary / withdrawal), **HC-014** (PHI authorization omits expiration), and **HC-019** (NPP acknowledgment omits receipt language). `npm run typecheck && lint && test && build` all green; **954/954 tests + 2 skips**. |
| 54 | Sub-domain K (Insurance and risk) — ~25 rules | ✅ complete | 25 rules at [`src/engine/rules/v4/insurance/rules.ts`](src/engine/rules/v4/insurance/rules.ts) split into four new families (K.1 Certificate of Insurance / ACORD 25 continues under v3 `coi`): `POLICY_SUMMARY_RULES` 6 (Named Insured + producer, policy period inception + expiration, **INS-003** limits of liability — each-occurrence + aggregate / per-claim, premium + deductible / SIR, coverage parts / forms enumeration by ISO / AAIS form number + edition, claims-made vs occurrence trigger + retroactive date + ERP), `ENDORSEMENT_RULES` 6 (form number + edition identification, coverage being modified, effective date or 'at policy inception', **INS-010** language rule flagging absolute / total exclusions (pollution / cyber / communicable disease / silica / asbestos / abuse) and severe sublimits, Additional Insured form discipline tied to current ISO editions — CG 20 10 04 13 / CG 20 37 04 13 / CG 20 38 — vs older 1985 / 2001 forms with broader coverage, waiver-of-subrogation endorsement CG 24 04 where the contract requires waiver), `INDEMNIFICATION_AGREEMENT_RULES` 7 (indemnitor / indemnitee identification with affiliates / officers / agents, Type I / II / III scope recital with matching operative language, **INS-015** language rule flagging Type I broad-form indemnity covering indemnitee's sole / active / own negligence — void in construction contexts under Cal. Civ. § 2782 / N.Y. Gen. Oblig. § 5-322.1 / Tex. Ins. § 151, defense duty separately articulated per *Crawford v. Weather Shield* (44 Cal. 4th 541), notice + tender + cooperation procedure with consent-to-settle, insurance support — required limits + Additional Insured ISO form + waiver of subrogation, survival of indemnity through statute of limitations / repose), `HOLD_HARMLESS_RULES` 6 (parties identification — protected party + protecting party, activity / scope of risk identification, **INS-022** language rule flagging pre-dispute releases that purport to cover future claims, gross negligence, willful misconduct, or intentional acts — unenforceable in most states under the *Tunkl v. Regents* factor analysis, acknowledgment + assumption-of-risk language, signature + date + minor / guardian recital limited by *Atkins v. Swimwest*, severability for partial enforcement). Rule ids `INS-001..INS-025`; every rule scopes via `applies_to_playbooks` to the four v4 insurance playbook ids (`insurance-policy-summary`, `insurance-endorsement`, `indemnification-agreement`, `hold-harmless-agreement`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/insurance/_helpers.ts`](src/engine/rules/v4/insurance/_helpers.ts) cover ISO commercial-lines form library (CG 00 01 / CG 20 10 / CG 20 37 / CG 20 38 / CG 24 04 / CG 21 67), state insurance codes (CA Ins. § 380, NY Ins. L. § 3420, TX Ins. § 541 representative), Cal. Civ. § 2782 (CA construction anti-indemnity), N.Y. Gen. Oblig. § 5-322.1 (NY construction anti-indemnity), Tex. Ins. § 151 (Texas Anti-Indemnity Act), and the *Crawford / Tunkl / Atkins* lines. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING, ...PRIVACY_EXTENDED, ...HEALTHCARE, ...INSURANCE]` (500 rules total) and the engine top-level barrel re-exports `INSURANCE_RULES`. Tests at [`src/engine/rules/v4/insurance/insurance-ruleset.test.ts`](src/engine/rules/v4/insurance/insurance-ruleset.test.ts) (**9 passing**): registry contract (exactly 25, unique `INS-NNN` ids, `insurance` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 4-id set); cross-playbook-noop; compliant-policy fixture emits zero criticals + deterministic across runs; failure cases for **INS-003** (declarations omit limits), **INS-010** (absolute coverage-restricting endorsement), **INS-015** (Type I broad-form indemnity), and **INS-022** (pre-dispute release of gross negligence). `npm run typecheck && lint && test && build` all green; **963/963 tests + 2 skips**. |
| 55 | Sub-domain L (Banking and lending) — ~50 rules | ✅ complete | 50 rules at [`src/engine/rules/v4/banking/rules.ts`](src/engine/rules/v4/banking/rules.ts) split into eight new families: `PROMISSORY_NOTE_RULES` 6 (UCC § 3-104 negotiability — maker / payee / principal, **BNK-002** unconditional promise required for negotiability, interest rate with state-usury savings clause keyed to NY § 5-501 / Penal § 190.40 / CA Const. art. XV / TX Fin. § 302, definite time of payment, default + acceleration, UCC § 3-504 presentment / dishonor / protest waivers), `LOAN_AGREEMENT_RULES` 7 (loan amount + facility type + use of proceeds per LSTA model, SOFR-based interest with margin / floor / LIBOR-replacement, affirmative covenants, **BNK-010** negative covenants — debt / liens / restricted payments / asset sales / fundamental change, financial covenants — leverage / interest coverage / minimum liquidity, events of default + cross-default + cure periods, Reg Z disclosures for consumer-purpose loans), `SECURITY_AGREEMENT_RULES` 7 (UCC § 9-203 attachment — debtor / secured party, **BNK-015** granting clause creating security interest, § 9-108 collateral description with reasonable specificity, § 9-204 / § 9-315 after-acquired property + proceeds, § 9-509 authorization to file UCC-1, debtor reps as to ownership / no competing liens, § 9-601 Part-6 default remedies — collection / repossession / commercially reasonable disposition), `GUARANTY_RULES` 6 (guarantor / obligee / underlying obligation per Statute of Frauds, payment vs collection distinction, **BNK-023** suretyship-defenses waiver — release / modification / impairment of collateral, continuing-guaranty + cap recital, subrogation / contribution deferred until paid in full, reinstatement on Bankr. Code § 547 preference / § 548 fraudulent transfer clawback), `INTERCREDITOR_RULES` 6 (senior / junior identification with lien + payment priorities, payment / lien blockage + turnover, junior standstill 120–180 days, bankruptcy provisions — DIP financing / 363 sales / plan support / 1129(b) cramdown, junior buy-out option at par plus accrued, amendment / waiver consent rights), `SUBORDINATION_RULES` 6 (UCC § 9-339 subordinated-debt identification, senior debt + refinancing reach, **BNK-035** payment subordination + blockage + turnover, permitted payments outside default / blockage, standstill 120–180 days, 11 U.S.C. § 510(a) bankruptcy-enforceability invocation), `DEED_OF_TRUST_RULES` 6 (grantor / trustee / beneficiary identification — mortgage vs deed-of-trust state distinction, **BNK-040** Statute-of-Frauds-grade legal description with metes-and-bounds / lot-block / recorded-plat reference, granting / habendum operative conveyance, power-of-sale non-judicial foreclosure with state notice procedure, due-on-sale + Garn-St Germain 12 U.S.C. § 1701j-3 carve-outs, notarial acknowledgment + homestead / spousal-consent), `UCC1_RULES` 6 (§ 9-503 debtor exact-legal-name from public organic record, § 9-502 secured party + address, **BNK-047** § 9-504 collateral indication including all-assets supercollateral, § 9-307 filing-office identification by debtor's state of organization, § 9-515 5-year lapse + 6-month continuation window, § 9-509 debtor authorization). Rule ids `BNK-001..BNK-050`; every rule scopes via `applies_to_playbooks` to the eight v4 banking playbook ids (`promissory-note`, `loan-agreement`, `security-agreement`, `guaranty`, `intercreditor-agreement`, `subordination-agreement`, `deed-of-trust`, `ucc-1-financing-statement`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/banking/_helpers.ts`](src/engine/rules/v4/banking/_helpers.ts) cover UCC Articles 3 + 9 (§§ 3-104 / 3-504 / 9-108 / 9-203 / 9-204 / 9-307 / 9-315 / 9-339 / 9-502 / 9-503 / 9-504 / 9-509 / 9-515 / 9-601 / 9-620 / 9-625), Regulation Z (12 C.F.R. § 1026.18 closed-end credit disclosures), state usury statutes via the NCSL clearinghouse, state suretyship + Restatement (Third), state recording acts, Garn-St Germain Depository Institutions Act of 1982, 11 U.S.C. § 510(a), and LSTA / ABA model documents. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING, ...PRIVACY_EXTENDED, ...HEALTHCARE, ...INSURANCE, ...BANKING]` (550 rules total) and the engine top-level barrel re-exports `BANKING_RULES`. Tests at [`src/engine/rules/v4/banking/banking-ruleset.test.ts`](src/engine/rules/v4/banking/banking-ruleset.test.ts) (**13 passing**): registry contract (exactly 50, unique `BNK-NNN` ids, `banking` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 8-id set); cross-playbook-noop; compliant-note fixture emits zero criticals + deterministic across runs; failure cases for **BNK-002** (note omits unconditional promise), **BNK-010** (loan omits negative covenants), **BNK-015** (security agreement omits granting), **BNK-023** (guaranty omits suretyship-defenses waiver), **BNK-027** (intercreditor omits priorities), **BNK-035** (subordination omits payment subordination + blockage), **BNK-040** (deed of trust omits legal description), and **BNK-047** (UCC-1 omits collateral indication). `npm run typecheck && lint && test && build` all green; **976/976 tests + 2 skips**. |
| 56 | Sub-domain M (Construction) — ~30 rules | ✅ complete | 30 rules at [`src/engine/rules/v4/construction/rules.ts`](src/engine/rules/v4/construction/rules.ts) split into five new families: `CONSTRUCTION_CONTRACT_RULES` 7 (owner / contractor / architect identified, scope of work + AIA A201 § 1.1.1 Contract Documents enumeration, AIA A101 §§ 4–5 contract sum stipulated / GMP / cost-plus + progress payments, AIA A101 § 3 substantial / final completion + liquidated damages, AIA A201 § 3.7.4 differing site conditions / concealed unknown conditions, **CON-006** AIA A201 §§ 3.18 / 11 indemnification + insurance + waiver of subrogation aligned with Cal. Civ. § 2782 / NY Gen. Oblig. § 5-322.1 / Tex. Ins. § 151 anti-indemnity, AIA A201 § 14 termination for cause + convenience), `SUBCONTRACTOR_RULES` 6 (GC / sub / project identification, **CON-009** AIA A401 § 1.1 prime-contract flow-down, pay-when-paid vs pay-if-paid distinction with state-specific enforceability flag for CA / NC / NY / WI invalidation of pay-if-paid, schedule + coordination + cleanup, workmanship + materials warranty, dispute resolution + venue + governing law), `LIEN_WAIVER_RULES` 6 (**CON-014** waiver type — conditional vs unconditional, progress vs final — per Cal. Civ. §§ 8132 / 8134 / 8136 / 8138 statutory forms, claimant / property / through-date / amount identification, scope limited to amount received and through-date, conditional waiver payment-condition language, carve-outs for retention / disputed claims / change orders, signature + statutory-form recital), `BOND_RULES` 6 (principal / surety / obligee tri-party identification, payment vs performance vs dual-obligee bond type, **CON-022** penal sum stated as surety liability cap, underlying contract incorporated by reference, claimant definition + Miller Act 40 U.S.C. § 3133 90-day notice + 1-year suit limit, AIA A312 § 3 / § 5 performance-bond default declaration + surety options to complete / tender / pay / deny), `CHANGE_ORDER_RULES` 5 (AIA G701 § 5 original / revised contract-sum four-line summary, description of change in work — added / deleted / modified, **CON-028** AIA G701 § 4 contract-time adjustment with new substantial-completion date, owner + architect + contractor signature blocks, waiver of further claims for the changed work). Rule ids `CON-001..CON-030`; every rule scopes via `applies_to_playbooks` to the five v4 construction playbook ids (`construction-contract`, `subcontractor-agreement`, `construction-lien-waiver`, `payment-performance-bond`, `change-order`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/construction/_helpers.ts`](src/engine/rules/v4/construction/_helpers.ts) cover AIA Documents A101 / A201 / A312 / A401 / G701, Miller Act 40 U.S.C. §§ 3131–3134, state Little Miller Acts, state mechanic's-lien statutes (CA Civ. §§ 8000 et seq., Tex. Prop. ch. 53, NY Lien Law, Fla. § 713), Cal. Civ. § 8132 / § 8134 / § 8136 / § 8138 statutory lien-waiver forms, Cal. Civ. § 2782 construction anti-indemnity, and ConsensusDocs 750. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING, ...PRIVACY_EXTENDED, ...HEALTHCARE, ...INSURANCE, ...BANKING, ...CONSTRUCTION]` (580 rules total) and the engine top-level barrel re-exports `CONSTRUCTION_RULES`. Tests at [`src/engine/rules/v4/construction/construction-ruleset.test.ts`](src/engine/rules/v4/construction/construction-ruleset.test.ts) (**10 passing**): registry contract (exactly 30, unique `CON-NNN` ids, `construction` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 5-id set); cross-playbook-noop; compliant-contract fixture emits zero criticals + deterministic across runs; failure cases for **CON-006** (contract omits indemnity / insurance / waiver), **CON-009** (subcontract omits flow-down), **CON-014** (lien waiver omits waiver type), **CON-022** (bond omits penal sum), and **CON-028** (change order omits time adjustment). `npm run typecheck && lint && test && build` all green; **986/986 tests + 2 skips**. |
| 57 | Sub-domain N (Trust / estate / family) — ~60 rules | ✅ complete | 60 rules at [`src/engine/rules/v4/trust-estate/rules.ts`](src/engine/rules/v4/trust-estate/rules.ts) split into 8 new families plus the mandatory per-output execution-formality disclaimer: `WILL_RULES` 8 (UPC §§ 2-501/2-502/2-504/2-507/3-203/3-603 + §§ 2-606/5-202 — testator + domicile, revocation of prior wills, executor + successor nomination, fiduciary bond waiver, residuary clause, guardianship for minor children, self-proving affidavit, execution block with 2 witnesses), `REVOCABLE_TRUST_RULES` 8 (UTC §§ 103 / 401 / 502 / 602 / 704 / 813 — settlor identification, trustee + successors, **EST-011** revocability + amendment, initial funding / trust property, lifetime distributions, distributions on death, pour-over coordination with will, spendthrift / creditor-protection), `ADVANCE_DIRECTIVE_RULES` 7 (state advance-directive statutes including CA Prob. § 4670 / NY Pub. Health § 2980 — declarant + capacity, life-sustaining / artificial nutrition preferences, pain management / palliative care, Uniform Anatomical Gift Act donation, witness + notary, **EST-022** triggering condition — terminal / PVS / end-stage, revocation methods), `HEALTHCARE_POA_RULES` 7 (state healthcare-POA statutes — principal + agent + successor, scope of treatment / records / placement / end-of-life authority, 45 C.F.R. § 164.502(g) HIPAA personal-representative authorization, springing vs immediate activation, end-of-life / DNR cross-reference, limitations / restrictions, witness + notary), `DURABLE_POA_RULES` 7 (UPOAA §§ 104 / 105 / 109 / 110 / 114 / 119 / 120 / 201 — principal + agent, **EST-032** durability surviving incapacity, scope + § 201 'hot powers' specific grant for gifts / trust amendments / beneficiary changes, springing vs immediate effectiveness, agent fiduciary duties loyalty / good faith / no commingling, third-party reliance protection §§ 119–120, notarial acknowledgment + real-property recording), `PRENUP_RULES` 8 (UPMAA §§ 3 / 4 / 9 / 10 / 11 + UPAA — contemplation of marriage, **EST-039** fair and reasonable financial disclosure via Schedule A/B, separate vs marital / community property characterization including appreciation + transmutation, alimony / spousal-support waiver with state-specific enforceability flag for CA Fam. § 1612(c), elective-share / homestead / intestate-rights waiver under UPMAA § 9, independent counsel + opportunity to consult, choice of law + venue UPMAA § 11, execution + notary + adequate time before wedding), `POSTNUP_RULES` 7 (state postnup law plus heightened fiduciary scrutiny per CA Fam. § 721 — parties + during-marriage, consideration recital separate from the marriage itself, heightened fiduciary disclosure, property + transmutation per CA Fam. § 852 written waiver, spousal support / waiver, independent counsel + voluntariness, execution + notary), `FAMILY_MSA_RULES` 7 (state divorce codes — petitioner / respondent + date of separation per CA Fam. § 70, property + debt division, spousal support / alimony, **EST-056** child custody + parenting plan + child support including best-interests / state guidelines, post-TCJA tax provisions including IRC § 71 alimony repeal + Form 8332 dependency release, QDRO / DRO retirement plan division under ERISA 29 U.S.C. § 1056(d)(3) including FERS / CSRS / TSP for federal employees, incorporation / merger into judgment + execution); plus **EST-060** always-fires INFO disclaimer scoped to every N playbook reciting that execution formalities — witnesses, notarial acknowledgment, holographic state-specific requirements, age / competence at signing — cannot be verified from a docx or pdf alone (spec §6.N caveat enforced programmatically). Rule ids `EST-001..EST-060`; every rule scopes via `applies_to_playbooks` to the eight v4 trust-estate playbook ids (`last-will-and-testament`, `revocable-living-trust`, `advance-directive`, `healthcare-poa`, `durable-poa-financial`, `prenuptial-agreement`, `postnuptial-agreement`, `family-msa`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/trust-estate/_helpers.ts`](src/engine/rules/v4/trust-estate/_helpers.ts) cover Uniform Probate Code (UPC), Uniform Trust Code (UTC), Uniform Power of Attorney Act (UPOAA), Uniform Premarital and Marital Agreements Act (UPMAA), 45 C.F.R. § 164.502(g), Uniform Anatomical Gift Act, ERISA 29 U.S.C. § 1056(d)(3), and state advance-directive / family-law / probate / divorce codes. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING, ...PRIVACY_EXTENDED, ...HEALTHCARE, ...INSURANCE, ...BANKING, ...CONSTRUCTION, ...TRUST_ESTATE]` (640 rules total) and the engine top-level barrel re-exports `TRUST_ESTATE_RULES`. Tests at [`src/engine/rules/v4/trust-estate/trust-estate-ruleset.test.ts`](src/engine/rules/v4/trust-estate/trust-estate-ruleset.test.ts) (**17 passing**): registry contract (exactly 60, unique `EST-NNN` ids, `trust-estate` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 8-id set); cross-playbook-noop; parameterized test ensuring **EST-060** fires at `info` severity under every N playbook (will / trust / advance directive / healthcare POA / durable POA / prenup / postnup / family MSA); failure cases for **EST-002** (will omits revocation), **EST-011** (trust omits revocability), **EST-022** (advance directive omits triggering condition), **EST-032** (durable POA omits durability), **EST-039** (prenup omits financial disclosure), and **EST-056** (family MSA with children omits parenting plan). `npm run typecheck && lint && test && build` all green; **1003/1003 tests + 2 skips** (first run to cross the 1k-test threshold). |
| 58 | Sub-domain O (Compliance policies) — ~50 rules | ✅ complete | 50 rules at [`src/engine/rules/v4/compliance-policy/rules.ts`](src/engine/rules/v4/compliance-policy/rules.ts) split into 10 new families: `CODE_OF_CONDUCT_RULES` 5 (NYSE 303A / Nasdaq 5610 scope — directors / officers / employees, **POL-002** SOX § 406 senior-financial-officer elements honest / ethical / full-fair-accurate disclosures / compliance, NYSE 303A.10 + Nasdaq 5610 + Form 8-K Item 5.05 waiver disclosure within 4 business days, SOX § 301 audit-committee complaint procedure + Dodd-Frank § 922 reporting + non-retaliation, compliance-with-law baseline), `FCPA_RULES` 6 (15 U.S.C. § 78dd-1/-2/-3 anti-bribery prohibition, **POL-007** DOJ-SEC FCPA Resource Guide third-party / agent / intermediary due diligence, 15 U.S.C. § 78m(b)(2) books-and-records + internal-controls accounting provisions, facilitating-payments prohibition despite narrow FCPA § 78dd-1(b) exception, UK Bribery Act 2010 cross-border + 'failure to prevent', gifts / hospitality / charitable / political contribution thresholds + pre-approval), `AML_RULES` 6 (FinCEN 2018 five-pillar AML program — policies / officer / training / independent testing / CDD-beneficial-ownership, **POL-013** OFAC sanctions screening 31 C.F.R. Chapter V SDN + sectoral lists, FinCEN 31 C.F.R. § 1010.320 SAR within 30 days + confidentiality, FinCEN CDD final rule beneficial-ownership at 25% + Corporate Transparency Act 2024, 31 C.F.R. § 1010.311 CTR ≥ $10,000 + 31 U.S.C. § 5324 anti-structuring, 31 C.F.R. § 1010.430 5-year recordkeeping), `INSIDER_TRADING_RULES` 5 (SEC Rule 10b-5 MNPI prohibition with *Dirks / Salman* tipping liability, **POL-019** earnings blackout periods + Section 16 insider pre-clearance, SEC Rule 10b5-1 (Dec. 2022 amendments) cooling-off + good-faith certification + single-plan limit + Form 10-Q disclosure, tipping liability extending to family / friends / brokers, Item 407(i) Reg S-K / Dodd-Frank § 955 hedging + short-sales + pledging restrictions), `WHISTLEBLOWER_RULES` 5 (internal / SEC / CFTC / DOL reporting channels, SOX § 806 (18 U.S.C. § 1514A) + Dodd-Frank § 922 non-retaliation with reinstatement / back-pay / special damages, **POL-025** SEC Rule 21F-17 anti-impeding-whistleblower carve-out for NDAs / severance / handbooks, SOX § 301 confidentiality + anonymous reporting via third-party hotline, investigation timeline + independent investigator + corrective action), `DOC_RETENTION_RULES` 5 (retention schedule by category + period, **POL-029** Sedona Principles + FRCP 37(e) legal-hold override suspending routine destruction, ESI treatment for email / IM / Slack / Teams / cloud / mobile / ephemeral messaging, SEC Rule 17a-4 + IRC § 6501 + ERISA § 107 + HIPAA § 164.530(j) regulatory minimums — longest applicable controls, secure destruction NAID-AAA shred + NIST 800-88 sanitization + certificate of destruction), `COI_POLICY_RULES` 4 (**POL-033** definition of conflict covering direct / indirect financial + related-party + dual-board, IRS Form 990 Part VI annual + ongoing duty to disclose, IRC § 4958 intermediate-sanctions recusal + disinterested-director approval with rebuttable presumption, sanctions including board removal + employment discipline), `AI_AUP_RULES` 5 (NIST AI RMF Govern function approved-tools list + procurement gate against shadow AI, **POL-038** prohibited inputs covering PHI / PII / trade secrets / privileged / source code / confidential client data, EU AI Act Art. 14 + NIST AI RMF human-in-the-loop review for hiring / lending / legal / medical, USPTO + *Thaler v. Vidal* IP / attribution / hallucination disclaimer + verification, NIST AI RMF training + AI-incident reporting hotline), `SOCIAL_MEDIA_POLICY_RULES` 4 (**POL-042** NLRA § 7 + *Stericycle* (2023) overbroad wage / working-condition / company-related online activity restriction flagged, 16 C.F.R. Part 255 (2023) FTC Endorsement Guides material-connection / #ad disclosure, SEC Regulation FD compliance for material non-public information via social media, brand-voice / 'views are my own' personal-account disclaimer), `LOBBYING_POLICY_RULES` 5 (**POL-046** Lobbying Disclosure Act 1995 2 U.S.C. §§ 1601–1614 registration + quarterly LD-2 + semiannual LD-203 reports, pre-approval of lobbying activities + covered-official contacts, FECA 52 U.S.C. § 30118 corporate political-contribution ban + PAC + individual contributions, state / local lobbying matrix via NCSL clearinghouse, federal 5 C.F.R. § 2635 gift rules + state ethics codes — stricter controls). Rule ids `POL-001..POL-050`; every rule scopes via `applies_to_playbooks` to the ten v4 compliance-policy playbook ids (`code-of-conduct`, `anti-bribery-policy`, `aml-policy`, `insider-trading-policy`, `whistleblower-policy`, `document-retention-policy`, `coi-policy`, `ai-aup-policy`, `social-media-policy`, `lobbying-policy`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/compliance-policy/_helpers.ts`](src/engine/rules/v4/compliance-policy/_helpers.ts) cover NYSE 303A, Nasdaq 5610, SOX §§ 301 / 406 / 806, FCPA (15 U.S.C. §§ 78dd-1/2/3 + 78m), UK Bribery Act 2010, BSA (31 U.S.C. §§ 5311–5336) + FinCEN regulations + Corporate Transparency Act, OFAC (31 C.F.R. Chapter V), SEC Rule 10b-5 + Rule 10b5-1 (Dec. 2022) + Reg FD + Rule 21F-17 + Item 407(i) Reg S-K, Dodd-Frank § 922 + § 955, the Sedona Principles, SEC Rule 17a-4 + IRC § 6501 + ERISA § 107 + HIPAA § 164.530(j), IRS Form 990 Part VI + IRC § 4958, NIST AI RMF 1.0 + EU AI Act (Regulation (EU) 2024/1689), NLRA § 7 + *Stericycle*, 16 C.F.R. Part 255, Lobbying Disclosure Act + Honest Leadership Act, FECA (52 U.S.C. § 30118), and 5 C.F.R. § 2635. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING, ...PRIVACY_EXTENDED, ...HEALTHCARE, ...INSURANCE, ...BANKING, ...CONSTRUCTION, ...TRUST_ESTATE, ...COMPLIANCE_POLICY]` (690 rules total) and the engine top-level barrel re-exports `COMPLIANCE_POLICY_RULES`. Tests at [`src/engine/rules/v4/compliance-policy/compliance-policy-ruleset.test.ts`](src/engine/rules/v4/compliance-policy/compliance-policy-ruleset.test.ts) (**14 passing**): registry contract (exactly 50, unique `POL-NNN` ids, `compliance-policy` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 10-id set); cross-playbook-noop; compliant-code-of-conduct fixture emits zero criticals + deterministic across runs; failure cases for **POL-006** (FCPA prohibition), **POL-013** (OFAC screening), **POL-019** (blackout / pre-clearance), **POL-025** (Rule 21F-17 carve-out), **POL-029** (legal-hold override), **POL-033** (COI definition), **POL-038** (AI AUP prohibited inputs), **POL-042** (NLRA § 7 wage-discussion), and **POL-046** (LDA registration). `npm run typecheck && lint && test && build` all green; **1017/1017 tests + 2 skips**. |
| 59 | Sub-domain P (Regulatory prose) — ~40 rules | ✅ complete | 40 rules at [`src/engine/rules/v4/regulatory-prose/rules.ts`](src/engine/rules/v4/regulatory-prose/rules.ts) split into 5 distinct rulesets across 6 playbooks (P.3 / P.4 share `RISK_FACTORS_RULES` per spec §6.P note) plus the mandatory `REG-040` filing-schema disclaimer: `FORM_D_RULES` 7 (Form D Item 1 issuer identification, **REG-002** Rule 504 / 506(b) / 506(c) exemption, accredited-investor reps + 506(c) verification, Rule 502(c) general-solicitation policy, Rule 506(d) bad-actor disqualification, offering amount + minimum investment, state blue-sky notice filings per NSMIA preemption), `FORM_ADV_RULES` 8 (Form ADV Part 2A cover page + Item 2 material changes, Item 4 advisory business + AUM, **REG-010** Item 5 fees + compensation, Item 8 methods + strategies + risk of loss, Item 9 disciplinary, Item 11 code of ethics + personal trading, Item 12 brokerage practices + soft dollars, Item 15 custody + Rule 206(4)-2 qualified-custodian / surprise exam), `RISK_FACTORS_RULES` 9 (Reg S-K Item 105 risk-factor section + introduction, Item 105 (2020 revision) materiality threshold + 2-page summary trigger when section > 15 pages, **REG-018** language rule flagging generic boilerplate per SEC staff guidance, language rule flagging hypothetical / hedged risk factors for risks that have already materialized per SEC Pearson / First American enforcement, Rule 421 plain-English subheadings, PSLRA 15 U.S.C. §§ 77z-2 / 78u-5 safe-harbor cross-reference, 2023 cybersecurity disclosure rule Item 106 + Form 8-K Item 1.05, climate / ESG materiality, 10-K update vs prior period — last rule scoped to 10-K only), `PPM_RULES` 8 (PPM issuer + offering overview, risk-factor section, **REG-027** Rule 501 accredited-investor + Rule 506(b) sophistication suitability standards, subscription procedure + minimum / maximum, Rule 144 restricted-securities legend + transfer restrictions, conflicts-of-interest disclosure for 10b-5 protection, tax considerations + Circular 230, state blue-sky + selling-agent FINRA compliance), `REG_A_RULES` 7 (**REG-033** Tier 1 ($20M) vs Tier 2 ($75M post-2024) election, Form 1-A Part I / II / III structure, offering-specific risk factors, use of proceeds + plan of distribution, Tier 2 non-accredited 10% income / net-worth investment limitation per 17 C.F.R. § 230.251(d)(2)(i)(C), Tier 2 audited financials 2 years, Tier 2 ongoing reporting Form 1-K annual / Form 1-SA semi-annual / Form 1-U current); plus **REG-040** always-fires INFO disclaimer scoped to every P playbook reciting that v4 lints only the drafter's prose — not financial statements, accounting policies, EDGAR / Form D / Form ADV / Form 1-A filing schemas, financial calculations, or numerical disclosures (spec §6.P caveat enforced programmatically). Rule ids `REG-001..REG-040`; every rule scopes via `applies_to_playbooks` to the six v4 regulatory-prose playbook ids (`form-d-narrative`, `form-adv-brochure`, `s-1-risk-factors`, `10-k-risk-factors`, `ppm-narrative`, `reg-a-plus-circular`) registered at [`src/playbooks/v4/`](src/playbooks/v4/). Citation factories at [`src/engine/rules/v4/regulatory-prose/_helpers.ts`](src/engine/rules/v4/regulatory-prose/_helpers.ts) cover Regulation D Rules 501 / 502 / 504 / 506 (17 C.F.R. § 230), SEC Form D General Instructions, Form ADV Part 2A + Rule 204-3, Reg S-K Item 105 + Item 106, Securities Act Rule 421 plain-English rule, Reg A Tier 1 / Tier 2 + Form 1-A (17 C.F.R. § 230.251 et seq.), PSLRA 15 U.S.C. §§ 77z-2 + 78u-5, Rule 144, Circular 230, and state blue-sky / Uniform Securities Act variants. `V4_RULES` now `[...GOVERNANCE, ...EQUITY, ...M_AND_A, ...REAL_ESTATE, ...EMPLOYMENT_V4, ...SETTLEMENT, ...IP_LICENSING, ...PRIVACY_EXTENDED, ...HEALTHCARE, ...INSURANCE, ...BANKING, ...CONSTRUCTION, ...TRUST_ESTATE, ...COMPLIANCE_POLICY, ...REGULATORY_PROSE]` (730 rules total) and the engine top-level barrel re-exports `REGULATORY_PROSE_RULES`. Tests at [`src/engine/rules/v4/regulatory-prose/regulatory-prose-ruleset.test.ts`](src/engine/rules/v4/regulatory-prose/regulatory-prose-ruleset.test.ts) (**14 passing**): registry contract (exactly 40, unique `REG-NNN` ids, `regulatory-prose` category, every rule has `applies_to_playbooks`); playbook-id whitelist (every scope is in the 6-id set); cross-playbook-noop; parameterized test ensuring **REG-040** fires at `info` severity under every P playbook (Form D / Form ADV / S-1 / 10-K / PPM / Reg A+); failure cases for **REG-002** (Form D omits exemption), **REG-010** (Form ADV omits fees), **REG-018** (generic boilerplate risk factor), **REG-027** (PPM omits suitability), and **REG-033** (Reg A+ omits Tier election). `npm run typecheck && lint && test && build` all green; **1031/1031 tests + 2 skips**. With Step 59 landing, the catalog half of the v4 build plan (Steps 45–59) is complete — every sub-domain B–P has a ruleset + playbooks. Remaining: Steps 60 (DKB v4 fetchers) → 66 (launch checklist + v4.0.0 tag). |
| 60 | DKB build pipeline (v4 fetchers) | ✅ complete | Eight v4 source families wired under [`dkb/build/v4/fetchers/`](dkb/build/v4/fetchers/): `nvca` (NVCA Model Legal Documents — 6 `regulator_model_form` nodes for the C.* / D.* sub-domains: SPA, IRA, Voting, ROFR/Co-Sale, Certificate of Incorporation, Series Seed/A Term Sheet); `dgcl` (Delaware General Corporation Law Title 8 — `statutory_clause_requirement` nodes for §§ 102, 109, 141, 211, 251, 262 covering B.* governance + D.* M&A); `mbca` (ABA Model Business Corporation Act — vendored excerpts for §§ 2.02, 7.01–7.02, 8.01, 10.03 covering MBCA-aligned states); `ucc-article-2` / `ucc-article-3` / `ucc-article-9` (Cornell LII — § 2-201 statute of frauds, § 2-314 implied warranty, § 2-316 disclaimer; § 3-104 negotiable instrument, § 3-305 defenses; § 9-203 attachment, § 9-108 description, § 9-502 financing statement — covering A.* commercial + L.* lending); `aia` (American Institute of Architects contract documents catalog — A101 / A102 / A201 / A401 / G701 / G702-G703 surfaced as `regulator_model_form` citations for M.* construction without vendoring copyrighted clause text); `frcp` / `fre` (Cornell LII — FRCP 37(e) spoliation + Rule 41 dismissal; FRE 408 settlement-negotiations privilege + Rule 502(d) inadvertent waiver covering G.* settlement); `state-landlord-tenant` (CA Civ. § 1950.5 / §§ 1941-42, N.Y. RPL § 235-b + GOL § 7-103, Tex. Prop. § 92.103 + § 92.052, Fla. § 83.49, 765 ILCS 710 — 5 states × 1–2 requirements each, covering E.* residential lease); `state-trust-will` (Cal. Prob. § 6110 + § 15206, N.Y. EPTL § 3-2.1 + § 7-1.17, Tex. Est. § 251.051, Fla. § 732.502, 755 ILCS 5/4-3 — covering N.* will / trust execution formalities cited by the mandatory `TEF-NNN` disclaimer). **19 total fetcher ids** registered in `V4_FETCHERS` ↔ `V4_SOURCE_URLS`. Snapshot fixtures vendored at [`dkb/fixtures/v4/snapshots/{sha256(source_url)}.txt`](dkb/fixtures/v4/snapshots/) — every fixture exercises every per-fetcher `detect` regex so the parser fires every hand-keyed requirement. Each emitted node is a v3 `statutory_clause_requirement` or `regulator_model_form` (spec §12: v3 schema reused, no v4-specific node type), so the existing Step-20 staleness gate at [`dkb/build/v3/check-staleness.ts`](dkb/build/v3/check-staleness.ts) covers v4 nodes unmodified. Tests at [`tests/v4/dkb/fetchers/fetchers.test.ts`](tests/v4/dkb/fetchers/fetchers.test.ts) verify (a) the 19-id catalog + URL completeness; (b) each `parse*` extracts the expected node count from a synthetic input + returns empty for an unrelated input; (c) every registered fetcher runs end-to-end against its vendored snapshot and the result validates against `V3DkbNodeListSchema`; (d) the `detectStaleness` gate accepts v4 nodes and reports zero stale citations on a clean run, then fires when the authority text drifts. `npm run typecheck && lint && test && build` all green; **1062/1062 tests + 2 skips** (was 1031 + 2; +31 new). With Step 60 landing, the DKB-build half of v4 is complete; remaining steps are corpus expansion (61), perf / a11y / threat-model verification (62–64), docs (65), and the launch checklist + 4.0.0 tag (66). |
| 61 | Test corpus expansion | ✅ complete | v4 golden-test harness lands at [`tests/golden/v4/`](tests/golden/v4/) mirroring the [`v3 pattern`](tests/golden/v3/README.md): [`_pipeline.ts`](tests/golden/v4/_pipeline.ts) loads LAUNCH + v3 + every v4 playbook under [`src/playbooks/v4/`](src/playbooks/v4/) (105 ids) and runs `LAUNCH_RULES + V3_RULES + V4_RULES`; [`golden.test.ts`](tests/golden/v4/golden.test.ts) is the single-doc harness; [`bundle.test.ts`](tests/golden/v4/bundle.test.ts) is the multi-doc harness driving `runEngineMulti` + `CONSISTENCY_RULES` (spec-v4.md §§10–11). **15 synthetic single-doc fixtures** at [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/), one representative per v4 sub-domain B–P, each with a `.playbook` sidecar pinning the v4 playbook: governance→`bylaws-corporation`, equity→`safe-yc`, m-and-a→`asset-purchase-agreement`, real-estate→`net-lease`, employment→`executive-employment`, settlement→`confidential-settlement`, ip-licensing→`copyright-license`, privacy→`cookie-notice`, healthcare→`phi-authorization`, insurance→`coi-policy`, banking→`loan-agreement`, construction→`construction-contract`, trust-estate→`revocable-living-trust`, compliance→`anti-bribery-policy`, regulatory-prose→`form-d-narrative`. **5 multi-doc bundles** at [`tests/golden/v4/bundles/`](tests/golden/v4/bundles/) exercising the CROSS-* rule families: `party-name-conflict` (MSA + BAA — CROSS-PARTY-001), `governing-law-mismatch` (MSA + DPA + SOW each with different governing law — CROSS-JURIS-001 with 3 docs), `effective-date-paradox` (MSA effective 2026-01-01 referencing BAA effective 2026-06-01 — CROSS-DATE-001), `cap-mismatch` (MSA cap $2M + SOW cap $50K — CROSS-AMOUNT-001), and `clean-msa-baa` (sanity bundle with no inconsistency). **Three sanity guards per fixture**: (a) byte-identical golden match; (b) byte-identical canonical output across two in-process runs (determinism); (c) the playbook match is a v4 playbook (not `generic-fallback`) and at least one finding is produced. Bundle goldens at [`tests/golden/v4/bundle-expected/`](tests/golden/v4/bundle-expected/) record per-doc `result_hash` + finding count and the consistency-run summary (`result_hash`, finding count, finding rule_ids, execution-log count) — they do not duplicate per-doc engine runs already covered under `expected/`. Regeneration pattern: `VAULYTICA_REGEN_GOLDEN=1 npx vitest run tests/golden/v4`. `npm run typecheck && lint && test && build` all green; **1124/1124 + 2 skips** (was 1062 + 2; +62 new tests). Remaining: subsequent fixtures land per the recipe documented in [`tests/golden/v4/README.md`](tests/golden/v4/README.md). |
| 62 | Performance verification | ✅ complete | New `describe("v4 bundle-size guard")` block added to [`tests/integration/bundle-size.test.ts`](tests/integration/bundle-size.test.ts): total gzipped payload ≤ 1065 KB ceiling; v4-rule chunks are dynamic-imported or eager-entry stays under budget; pipeline chunk remains in the allow-list. |
| 63 | Accessibility verification (axe + a11y unit) | ✅ complete | New `describe("v4 surface a11y (LAUNCH row v4-h)")` block added to [`tests/integration/static-html.test.ts`](tests/integration/static-html.test.ts): tile count ≥ 6, every tile has `<h3>`, hero `<h1>` reads "Drop legal docs.", drop-zone aria-label mentions PDF/DOCX, footer wordmark has `aria-label="Vaulytica home"`. |
| 64 | Threat-model update | ✅ complete | `## v4 additions — multi-document surface` section appended to [`docs/threat-model.md`](docs/threat-model.md) covering multi-doc ingest posture, CROSS-* families, bundle fingerprint determinism, expanded catalog disclaimers, and what v4 still does not protect against. |
| 65 | Documentation | ✅ complete | Five new docs created under [`docs/v4/`](docs/v4/): `overview.md`, `adding-a-sub-domain.md`, `adding-a-family.md`, `cross-document-rules.md`, `the-document-classifier.md`. `## v4` section added to [`README.md`](README.md) linking all five docs. |
| 66 | Launch checklist + version bump | ✅ complete | `package.json` bumped to 4.0.0. `CHANGELOG.md` `[Unreleased]` → `[v4.0.0] — 2026-05-17` with summary paragraph; fresh `[Unreleased]` added. v4 launch checklist (rows v4-a through v4-o) + sign-off block + non-promises appended to [`LAUNCH.md`](LAUNCH.md). `BUILD_PROGRESS.md` rows 62–66 flipped to ✅ complete. |

## Post-4.0 work

### v4 fail-fixture corpus expansion (9 → 12) (2026-05-21) — ✅ complete

Advanced the v4 end-to-end fail-fixture corpus from nine to twelve fixtures, spanning three new v4 sub-domains (settlement, compliance-policy, construction), each deriving from the corresponding minimal-pass fixture by extending the preamble with a sentence that affirmatively elects not to include the load-bearing clause and dropping every textual anchor that the rule's `present_patterns` look for. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `settlement-confidential-missing-consideration-fail.txt` — Confidential Settlement Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned compensation clause stating the monetary amount to be paid for the releases set forth herein, relying instead on the delivery of non-monetary undertakings documented in a separately executed side letter. No "settlement payment / amount / sum", "consideration / in consideration", or "$NNN" dollar anchor appears anywhere in the document. **SET-006 (critical)** fires — its three present_patterns `/(settlement\s+payment|settlement\s+amount|settlement\s+sum)/i`, `/(consideration|in\s+consideration)/i`, and `/\$\s*[\d,]+/` are no longer matched anywhere in the document. Without recited consideration the agreement is vulnerable to a failure-of-consideration defense; the parties cannot verify the bargained-for exchange without a stated settlement payment amount.
- `compliance-anti-bribery-missing-third-party-diligence-fail.txt` — Anti-Bribery and Anti-Corruption Policy with the preamble extended by a sentence that affirmatively elects not to include a separately captioned counterparty-vetting section (deliberately avoiding "third party / parties", "agent(s)", "intermediary / intermediaries", "distributor", "due diligence", "screening", and "background"), relying instead on contractual representations and warranties obtained at onboarding. Neither third-party identification nor due-diligence review anchor appears anywhere in the document. **POL-007 (critical)** fires — its two present_patterns `/(third.?part(y|ies)|agents?|intermediar(y|ies)|distributor)/i` and `/(due\s+diligence|screening|background)/i` are no longer matched anywhere in the document. DOJ-SEC FCPA Resource Guide identifies third-party intermediaries as the most common FCPA channel; without a vetting obligation the policy has no textual hook to trigger risk-based review before a high-risk engagement.
- `construction-contract-missing-contract-sum-fail.txt` — Construction Contract with the preamble extended by a sentence that affirmatively elects not to include a separately captioned pricing clause and a separately captioned payment-cadence clause (deliberately avoiding "contract sum", "stipulated sum", "guaranteed maximum price", "GMP", "cost-plus", "progress payments", and "payment schedule / terms"), relying instead on a separately negotiated fee letter and invoicing protocol. Neither the pricing model anchor nor the payment-cadence anchor appears anywhere in the document. **CON-003 (critical)** fires — its two present_patterns `/(contract\s+sum|stipulated\s+sum|guaranteed\s+maximum\s+price|gmp|cost.plus)/i` and `/(progress\s+payments?|payment\s+(schedule|terms))/i` are no longer matched anywhere in the document. AIA A101 §§ 4–5 require the contract sum and progress-payment schedule; without a stated pricing model and draw cadence the pricing basis for any change-order dispute is undefined.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected v4 playbook (`confidential-settlement` / `anti-bribery-policy` / `construction-contract`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins twelve fail-fixtures; goldens regenerated. LAUNCH row v4-b updated to "12 end-to-end fail-fixtures across 12 sub-domains". `npm run lint && typecheck && test && build` all green (test count captured in commit message).

### v4 fail-fixture corpus expansion (6 → 9) (2026-05-21) — ✅ complete

Advanced the v4 end-to-end fail-fixture corpus from six to nine fixtures, spanning three new v4 sub-domains (m-and-a, ip-licensing, healthcare), each deriving from the corresponding minimal-pass fixture by extending the preamble with a sentence that affirmatively elects not to include the load-bearing clause and dropping every textual anchor that the rule's `present_patterns` look for. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `m-and-a-asset-purchase-missing-excluded-liabilities-fail.txt` — Asset Purchase Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned retained-obligations schedule enumerating the liabilities of Seller that Buyer is not assuming, relying instead on the general principle that Buyer assumes only those liabilities expressly set forth on the Assumed Liabilities schedule. No "excluded liabilities" anchor appears anywhere in the document. **MNA-023 (critical)** fires — its present_pattern `/excluded\s+liabilities/i` is no longer matched anywhere in the document. The structural advantage of an APA is that Buyer assumes only specifically-identified liabilities; without a broad Excluded Liabilities catch-all, Buyer faces unquantified successor-liability exposure for obligations it believed it had not assumed.
- `ip-licensing-copyright-missing-exclusivity-fail.txt` — Copyright License Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned grant-scope clause specifying whether the license is exclusive or non-exclusive (deliberately avoiding "exclusive", "non-exclusive", and "sole"). No "exclusive / non-exclusive / sole" anchor and no "in writing / signed / executed" anchor appears anywhere in the document. **IPL-020 (critical)** fires — its two present_patterns `/(exclusive|non.?exclusive|sole)/i` and `/(in\s+writing|signed|executed)/i` are no longer matched anywhere in the document. Under 17 U.S.C. § 204(a) an exclusive copyright transfer must be in a signed writing; without a stated exclusivity designation neither party has a four-corners answer to whether the licensee holds exclusive or shared rights.
- `healthcare-phi-authorization-missing-expiration-fail.txt` — PHI Authorization with the preamble extended by a sentence that affirmatively elects not to include a separately captioned time-limit clause (deliberately avoiding "expir*", "on [digit]", "by [digit]", "years from", "end of", and "conclusion of"), relying instead on the patient's right to cancel the authorization by written notice as the operative mechanism for terminating the authorized use. No expiration date or event appears anywhere in the document. **HC-014 (critical)** fires — its two present_patterns `/(expir(es?|ation|ing)|expir\b)/i` and `/((on|by)\s+\d|years?\s+from|end\s+of|conclusion\s+of)/i` are no longer matched anywhere in the document. 45 C.F.R. § 164.508(c)(1)(v) requires an expiration date or event; an authorization without one creates an open-ended consent that depends on patient revocation rather than natural lapse.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected v4 playbook (`asset-purchase-agreement` / `copyright-license` / `phi-authorization`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins nine fail-fixtures; goldens regenerated. LAUNCH row v4-b updated to "9 end-to-end fail-fixtures across 9 sub-domains". `npm run lint && typecheck && test && build` all green (test count captured in commit message).

### v4 fail-fixture corpus expansion (3 → 6) (2026-05-21) — ✅ complete

Advanced the v4 end-to-end fail-fixture corpus from three to six fixtures, spanning three new v4 sub-domains (equity, real-estate, banking), each deriving from the corresponding minimal-pass fixture by extending the preamble with a sentence that affirmatively elects not to include the load-bearing clause and dropping every textual anchor that the rule's `present_patterns` look for. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `equity-safe-missing-valuation-cap-fail.txt` — SAFE with the preamble extended by a sentence that affirmatively elects not to include a separately captioned economic-conversion-terms section setting out an investor-protection price ceiling, price reduction, or preferred-nation treatment, relying instead on the Company's next priced-round subscription documents as the operative mechanism for determining the conversion price. No "valuation cap", "discount rate", "most favored nation", or "MFN" anchor appears anywhere in the document. **EQT-003 (critical)** fires — its five present_patterns `/post.money\s+valuation\s+cap/i`, `/valuation\s+cap/i`, `/discount\s+rate/i`, `/most\s+favored\s+nation/i`, and `/\bmfn\b/i` are no longer matched anywhere in the document. Without at least one of cap, discount, or MFN the SAFE converts at the next-round price with no investor upside, defeating the economic purpose of the instrument.
- `real-estate-net-lease-missing-insurance-fail.txt` — Triple Net Lease with the preamble extended by a sentence that affirmatively elects not to include a separately captioned insurance-requirements section specifying tenant coverage types, minimum limits, additional-insured designations, and loss-payee arrangements, relying instead on a schedule of coverage certificates to be delivered before the Commencement Date. No "commercial general liability", "property insurance", or "waiver of subrogation" anchor appears anywhere in the document. **RE-003 (critical)** fires — its three present_patterns `/commercial\s+general\s+liability/i`, `/property\s+insurance/i`, and `/waiver\s+of\s+subrogation/i` are no longer matched anywhere in the document. NNN leases rely on tenant-carried insurance to protect both parties; without minimum CGL limits, additional-insured designation, and a waiver of subrogation, the landlord's coverage stack is exposed and recovery after a casualty is uncertain.
- `banking-loan-agreement-missing-events-of-default-fail.txt` — Loan Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned remedial-trigger schedule enumerating the circumstances under which the Lender may declare all outstanding principal and accrued interest immediately due and payable, relying instead on the Lender's rights under applicable law and the separate Credit Agreement. No "events of default", "cross-default", "cure period", "grace period", "insolvency", or "bankruptcy" anchor appears anywhere in the document. **BNK-012 (critical)** fires — its three present_patterns `/events?\s+of\s+default/i`, `/(cross.?default|cure\s+period|grace\s+period)/i`, and `/(insolvenc|bankruptc)/i` are no longer matched anywhere in the document. Without an enumerated EoD catalogue with cure periods and a cross-default trigger, the lender cannot accelerate until maturity and loses the early-warning protection the EoD framework is designed to provide.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected v4 playbook (`safe-yc` / `net-lease` / `loan-agreement`). [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) now pins six fail-fixtures; goldens regenerated. LAUNCH row v4-b updated to "6 end-to-end fail-fixtures across 6 sub-domains". `npm run lint && typecheck && test && build` all green (**1858 passing + 2 skips**).

### v4 fail-fixture corpus seed (0 → 3) (2026-05-21) — ✅ complete

Seeded the v4 end-to-end fail-fixture corpus with three fixtures spanning three v4 sub-domains, modeled on the v3 fail-fixture pattern. Each fixture extends a minimal-pass document with a preamble sentence that affirmatively elects not to include the load-bearing clause, and drops every textual anchor that the rule's `present_patterns` look for. New fixtures under [`tests/golden/v4/fixtures/`](tests/golden/v4/fixtures/):

- `governance-bylaws-missing-quorum-fail.txt` — Bylaws with the preamble extended by a sentence that affirmatively elects not to include a separately captioned meeting-threshold provision (deliberately avoiding the word "quorum"), relying instead on the residual default rule supplied by DGCL § 216 as the operative mechanism for determining the share-presence threshold at which a meeting of stockholders may transact business. No "quorum" anchor appears anywhere in the document. **GOV-005 (critical)** fires — its present_pattern `/quorum/i` is no longer matched anywhere in the document. DGCL § 216 supplies a majority-default if the bylaws are silent, but the bylaws themselves should restate the threshold explicitly; without a textual anchor, stockholders and proxy solicitors have no four-corners answer to what the meeting-threshold requirement is.
- `employment-executive-missing-severance-fail.txt` — Executive Employment Agreement with the preamble extended by a sentence that affirmatively elects not to include a separately captioned post-termination-pay schedule (deliberately avoiding the words "severance" / "salary continuation" / "COBRA"), relying instead on Employer's general termination-pay practices and the terms of any equity-plan documents as the operative mechanism for any post-employment compensation that may be payable. No "severance", "salary continuation", or "COBRA / continuation coverage" anchor appears anywhere in the document. **EMP-006** fires — its present_patterns `/severance/i`, `/salary\s+continuation/i`, and `/(cobra|continuation\s+coverage)/i` are no longer matched anywhere in the document. Severance terms are the central economic feature of an executive agreement; a generic at-will-with-no-severance contract leaves the executive with no contractual hook for post-termination compensation.
- `privacy-cookie-notice-missing-categories-fail.txt` — Cookie Notice with the preamble extended by a sentence that affirmatively elects not to include a separately captioned categories-of-cookies section (deliberately avoiding "essential / functional / analytics / advertising / performance / targeting" anchors), relying instead on a published cookie inventory and the consent-management tool referenced in the notice. No "strictly necessary / essential", "functional / preferences", or "analytics / performance / targeting / advertising" anchor appears anywhere in the document. **PRV-001** fires — its three present_patterns `/(strictly\s+necessary|essential)/i`, `/(functional|preferences?)/i`, and `/(analytics|performance|targeting|advertising)/i` are no longer matched anywhere in the document. ePrivacy Directive Art. 5(3) requires informed consent for non-strictly-necessary cookies; without a categorized inventory the user cannot give the granular consent the directive contemplates.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected v4 playbook (`bylaws-corporation` / `executive-employment` / `cookie-notice`). New test harness at [`tests/golden/v4/fixture-sanity.test.ts`](tests/golden/v4/fixture-sanity.test.ts) — mirrors the v3 sanity-test pattern — pins three fail-fixtures and asserts the load-bearing rule fires on each. Goldens generated; LAUNCH row v4-b updated from "15 per-family fail fixtures (pre-existing unit-level)" to "3 end-to-end fail-fixtures across 3 sub-domains (golden + sanity)" with the unit-level coverage statement preserved. `npm run lint && typecheck && test && build` all green (**1846 passing + 2 skips**).

### v3 fail-fixture corpus expansion (219 → 222) (2026-05-21) — ✅ complete

Advanced LAUNCH row v3-b coverage from two-hundred-and-nineteen end-to-end fail-fixtures to two-hundred-and-twenty-two by adding three new fixtures, each targeting one of the last three v3 rules that lacked a dedicated end-to-end fail-fixture (ADDENDA-010, ADDENDA-018, DPA-051). With this batch every shipping v3 rule in `src/engine/rules/v3/` now has a dedicated end-to-end fail-fixture pinned in [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts). New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `ai-addendum-missing-ai-definitions-fail.txt` — AI Addendum with the preamble extended by a sentence that affirmatively elects not to include a separately captioned definitions section setting out the meanings of the core AI terms relied on throughout this AI Addendum, instead relying on the ordinary commercial usage of those terms as understood in the industry from time to time, and on the descriptions of Services contained in the MSA and the applicable Statement of Work, as the operative mechanism for resolving any ambiguity. Throughout the operative text the "generative AI" / "foundation model" / "large language model" / "LLM" / "training data" anchors are dropped (replaced with neutral phrasings such as "AI systems" and "model inputs"); the "(Output|Outputs) … (means|shall mean)" anchor is likewise absent. **ADDENDA-010 (warning)** fires — its three present_patterns `/(generative\s+AI|foundation\s+model|large\s+language\s+model|\bLLM\b)/i`, `/(\b)(Output|Outputs)\b.{0,40}(?:means|shall\s+mean)/i`, and `/training\s+data/i` are no longer matched anywhere in the document. NIST AI RMF and practitioner posture both expect definitions to anchor the operative obligations of an AI Addendum; without textual definitions, every other AI-addendum clause is exposed to ordinary-meaning drift (e.g., disagreement over whether a retrieval-augmented system is a "foundation model" within the scope of the prohibition).
- `eula-missing-eu-consumer-law-fail.txt` — EULA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned EU-consumer-law-minimums clause referencing the European Union digital-content and consumer-rights directives by their formal citation, instead relying on a generic Section 10 consumer-protection-savings provision as the operative mechanism for any consumer-protection rights that may be asserted by an End User who happens to reside in the European Union. No "Digital Content Directive", "Directive (EU) 2019/770", "Consumer Rights Directive", or "Directive 2011/83" anchor appears anywhere in the document, and no "consumer rights cannot be limited / are not waived" phrasing appears either. **ADDENDA-018 (info)** fires — its present_patterns `/(Digital\s+Content\s+Directive|Directive\s+\(?EU\)?\s*2019\/770|Consumer\s+Rights\s+Directive|Directive\s+2011\/83)/i` and `/(consumer\s+rights\s+(?:cannot\s+be|are\s+not)\s+(?:limited|waived))/i` are no longer matched anywhere in the document. EU consumer law (Directive (EU) 2019/770 on digital content and digital services and Directive 2011/83/EU on consumer rights) cannot be contracted around for consumer-facing EULAs; a generic non-waiver savings clause does not put an EU consumer on textual notice of the directive-level minimums to which they are entitled (conformity, defects, remedies, withdrawal).
- `dpa-controller-processor-missing-notice-clause-fail.txt` — GDPR Controller-to-Processor DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned formal-communications clause stating how formal communications between the parties (including breach notices and other written communications) are to be delivered, instead relying on the corresponding mechanism in the MSA as the operative mechanism. No "notice shall be" / "notice must be" / "notices under" / "notices hereunder" / "notices shall" / "notice address" anchor appears anywhere in the document. Section 10 (Breach Notification under GDPR Art. 33) is preserved but uses "inform" rather than the trigger phrase, and Section 14 (Survival) is preserved so DPA-052 still passes where it was passing. **DPA-051 (warning)** fires — its present_pattern `/(notice\s+(shall|must)\s+be|notices\s+(under|hereunder|shall)|notice\s+address)/i` is no longer matched anywhere in the document. GDPR Art. 33(2) contemplates that the processor "shall notify" the controller of any personal data breach without undue delay; a DPA without a formal-communications clause leaves the parties without a textual anchor for the delivery method, addressee, and timing of breach notices and other communications that ride on top of that statutory duty.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`ai-addendum` / `eula` / `dpa-controller-processor`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins two-hundred-and-twenty-two fail-fixtures (was two-hundred-and-nineteen); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 222. `npm run lint && typecheck && test && build` all green (**1834 passing + 2 skips**).

### v3 fail-fixture corpus expansion (216 → 219) (2026-05-21) — ✅ complete

Advanced LAUNCH row v3-b coverage from two-hundred-and-sixteen end-to-end fail-fixtures to two-hundred-and-nineteen by adding three new fixtures, all targeting previously-uncovered rules within the AI Addendum playbook. None of the three load-bearing rules (ADDENDA-013, ADDENDA-015, ADDENDA-016) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `ai-addendum-missing-output-ownership-fail.txt` — AI Addendum with the preamble extended by a sentence that affirmatively elects not to include a separately captioned clause allocating, as between Customer and Vendor, the intellectual-property rights in the Outputs of any AI system, instead relying on the MSA's general intellectual-property provisions and Section 6 (Vendor Indemnity for AI-Generated IP Infringement) as the operative mechanism for handling any disputes arising in respect of such Outputs. No `(Output\w*).{0,40}(owns?|own|owned by|title to)` or reverse-direction anchor appears anywhere in the document. Section 2 (no-training-on-Customer-Data) is preserved so ADDENDA-011 still passes; Section 6 (IP indemnity) is preserved so the rest of the AI-addendum baseline still passes where it was passing. **ADDENDA-013 (warning)** fires — its present_patterns `/(Output\w*).{0,40}(?:owns?|own|owned\s+by|title\s+to)/i` and `/(?:owns?|own|owned\s+by).{0,40}Output\w*/i` are no longer matched anywhere in the document. NIST AI RMF and practitioner posture both expect an explicit ownership allocation of AI Outputs; relying on the MSA's general IP provisions alone does not resolve the foundation-specific ambiguity (vendors often claim derivative-work rights in Outputs by default).
- `ai-addendum-missing-subprocessor-disclosure-fail.txt` — AI Addendum with the preamble extended by a sentence that affirmatively elects not to include a separately captioned schedule identifying the third-party model providers Vendor relies on to deliver the AI features described in this AI Addendum, instead relying on the wholesale incorporation of the MSA's sub-contractor authorization mechanism and the MSA-level approved-vendor list as the operative mechanism for downstream model-provider approvals. No OpenAI / Anthropic / Google Gemini / Google Vertex / Cohere / Mistral / Hugging Face / Azure OpenAI / AWS Bedrock anchor and no "AI sub-processor", "model sub-processor", or "AI vendor" anchor appears anywhere in the document. **ADDENDA-015 (warning)** fires — its present_patterns `/(OpenAI|Anthropic|Google\s+(?:Gemini|Vertex)|Cohere|Mistral|Hugging\s*Face|Azure\s+OpenAI|AWS\s+Bedrock)/i` and `/(AI\s+sub[- ]?processors?|model\s+sub[- ]?processors?|AI\s+vendors?)/i` are no longer matched anywhere in the document. GDPR Art. 28(2) requires controller authorization (and disclosure) of every sub-processor in the chain; when an AI feature is powered by a third-party foundation model, the model provider itself is a sub-processor whose identity must be discoverable from the four corners of the AI Addendum.
- `ai-addendum-missing-fine-tuning-deletion-fail.txt` — AI Addendum with the preamble extended by a sentence that affirmatively elects not to include a separately captioned clause obligating Vendor to remove, on expiry of the MSA, any datasets derived from Customer Data that were used to adjust the parameters of a foundation model, instead relying on Section 2 (no-training-on-Customer-Data) and the MSA's general post-termination handling provisions as the operative mechanism for handling any such residual artifacts. No "delete fine-tuning data", "delete training data", "fine-tuning datasets … delete/destroy", or "model weights … delete/destroy" anchor appears anywhere in the document. **ADDENDA-016 (warning)** fires — its present_pattern `/(delete\s+(?:fine[- ]tun\w+|training)\s+data|fine[- ]tun\w+\s+(?:data|datasets?)[^.]{0,80}(?:delet|destroy)|model\s+weights?[^.]{0,80}(?:delet|destroy))/i` is no longer matched anywhere in the document. GDPR Art. 17 (right to erasure) reaches Customer Data that has been assimilated into model parameters or fine-tuning datasets; relying on a prohibition against training plus a generic MSA wind-down clause does not give Customer a direct contractual hook to compel removal of any residual artifacts the prohibition was breached to create.

Each fail-fixture has a `.playbook` sidecar pinning it to `ai-addendum`. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins two-hundred-and-nineteen fail-fixtures (was two-hundred-and-sixteen); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 219. `npm run lint && typecheck && test && build` all green (**1825 passing + 2 skips**).

### v3 fail-fixture corpus expansion (213 → 216) (2026-05-21) — ✅ complete

Advanced LAUNCH row v3-b coverage from two-hundred-and-thirteen end-to-end fail-fixtures to two-hundred-and-sixteen by adding three new fixtures, all targeting previously-uncovered rules within the Multi-State US DPA playbook (the last three uncovered USDPA-001..025 rules). None of the three load-bearing rules (USDPA-013, USDPA-014, USDPA-024) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-multi-state-us-missing-type-of-data-fail.txt` — Multi-State US DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned schedule enumerating the kinds of consumer records the Service Provider handles, instead relying on the description of Services in the MSA and the Statement of Work as the operative mechanism for identifying the subject-matter of the processing. No "type of personal data/information" or "category/categories of personal data/information" anchor appears anywhere in the document. Every other clause (Business-Purpose, prohibited acts, VCDPA/CPA/CTDPA/UCPA processor obligations, sub-processor flow-down, consumer rights assistance, annual assessment, breach notification, deletion on termination, certification) is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-013 (warning)** fires — its present_pattern `/(type|categor(?:y|ies))\s+of\s+(personal\s+)?(?:data|information)/i` is no longer matched anywhere in the document. Va. Code § 59.1-579 and Colo. Rev. Stat. § 6-1-1305 each require the processor contract to identify the type of personal data processed as a named element; without a textual anchor a regulator cannot determine the in-scope record set from the four corners of the DPA.
- `dpa-multi-state-us-missing-duration-fail.txt` — Multi-State US DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned clause stating how long the Service Provider may handle the consumers' personal information, instead relying on the term and termination provisions of the MSA as the operative mechanism for fixing the temporal scope. No "duration of processing", "duration of the processing", "processing shall continue", or "processing will continue" anchor appears anywhere in the document. **USDPA-014 (warning)** fires — its present_pattern `/duration\s+of\s+(?:the\s+)?processing|processing\s+(?:shall|will)\s+continue/i` is no longer matched anywhere in the document. Each state's processor-contract statute (Va. Code § 59.1-579, Colo. Rev. Stat. § 6-1-1305, Conn. Gen. Stat. § 42-520, Utah Code § 13-61-301) requires the duration of the processing to be set out; relying on the MSA term alone leaves the controller without a DPA-level anchor to time-box the service provider's authority to handle the personal information.
- `dpa-multi-state-us-missing-data-minimization-fail.txt` — Multi-State US DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned proportionality principle limiting the Service Provider's collection of personal information to what is required for the Business Purpose, instead relying on the Business-Purpose definition in Section 1 and the prohibited-acts enumeration in Section 2 as the operative mechanism for confining the Service Provider's collection to the disclosed scope. No "data minimization", "data minimisation", "reasonably necessary", or "necessary and proportionate" anchor appears anywhere in the document. **USDPA-024 (warning)** fires — its present_pattern `/(data\s+minim(?:ization|isation)|reasonably\s+necessary|necessary\s+and\s+proportionate)/i` is no longer matched anywhere in the document. Cal. Civ. Code § 1798.100(c), Va. Code § 59.1-578, Colo. Rev. Stat. § 6-1-1304, Conn. Gen. Stat. § 42-520(a), and Utah Code § 13-61-201 each incorporate a data-minimization principle requiring collection limited to what is reasonably necessary and proportionate; without a DPA-level minimization anchor the controller has no textual hook to enforce the statutory floor against the service provider.

Each fail-fixture has a `.playbook` sidecar pinning it to `dpa-multi-state-us`. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins two-hundred-and-sixteen fail-fixtures (was two-hundred-and-thirteen); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 216. `npm run lint && typecheck && test && build` all green (**1816 passing + 2 skips**).

### v3 fail-fixture corpus expansion (210 → 213) (2026-05-21) — ✅ complete

Advanced LAUNCH row v3-b coverage from two-hundred-and-ten end-to-end fail-fixtures to two-hundred-and-thirteen by adding three new fixtures, all targeting previously-uncovered rules within the GDPR Controller-to-Processor DPA playbook (continuing the DPA-001..055 sweep). None of the three load-bearing rules (DPA-053, DPA-054, DPA-055) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-subprocessor-records-fail.txt` — GDPR Controller-to-Processor DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned obligation requiring the Processor to keep a change-history of downstream processors available to Controller on request, instead relying on the Section 7 advance-notice mechanism as the operative mechanism for downstream-processor change communication. No "record of sub-processors", "sub-processor history", "sub-processor log", or "maintain … sub-processor" anchor appears anywhere in the document. Section 7's prior-authorization and advance-notice language is preserved so DPA-015 / DPA-016 still pass. **DPA-053 (warning)** fires — its present_patterns `/(record\s+of\s+sub[- ]?processors|sub[- ]?processor\s+(history|log)|maintain.{0,80}sub[- ]?processor)/is` are no longer matched anywhere in the document. GDPR Art. 28(2) contemplates that controllers retain visibility into the processor's downstream-processor changes over the life of the agreement; a single advance-notice obligation does not, by itself, give the controller after-the-fact access to the running change history that an audit or supervisory inspection requires.
- `dpa-controller-processor-missing-onward-transfer-fail.txt` — GDPR Controller-to-Processor DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned clause restating the SCC Module Two requirement that any further disclosure of Personal Data to a third country recipient be subject to the same data protection obligations as set out in this Agreement, instead relying on the wholesale incorporation of Commission Implementing Decision (EU) 2021/914, Module Two contained in Section 13 of this DPA as the operative mechanism. No "onward transfer", "onward-transfer", or "Clause 8.8" anchor appears anywhere in the document. Section 13 (Cross-Border Transfers and EU SCCs) is preserved so DPA-032 / DPA-033 / DPA-034 still pass where they were passing. **DPA-054 (warning)** fires — its present_patterns `/(onward\s+transfer|clause\s+8\.8|onward[- ]transfer)/i` are no longer matched anywhere in the document. Commission Implementing Decision (EU) 2021/914, Clause 8.8 requires onward transfers to be subject to the same data protection obligations as the underlying transfer; relying on wholesale incorporation of the SCC text without a DPA-level restating clause leaves the controller without a textual anchor to enforce against the processor in respect of further disclosures.
- `dpa-controller-processor-missing-local-law-disclosure-fail.txt` — GDPR Controller-to-Processor DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned clause restating the SCC Module Two requirement that the Processor notify the Controller of legally-binding requests for disclosure of the Personal Data received from official bodies (and challenge such requests where lawfully permitted), instead relying on the wholesale incorporation of Commission Implementing Decision (EU) 2021/914, Module Two contained in Section 13 of this DPA as the operative mechanism. No "public authority", "government request", "law enforcement request", "Clause 14", or "Clause 15" anchor appears anywhere in the document. Section 13 is preserved so DPA-032 / DPA-033 / DPA-034 still pass where they were passing. **DPA-055 (warning)** fires — its present_pattern `/(public\s+authority|government\s+request|law\s+enforcement\s+request|clause\s+14|clause\s+15)/i` is no longer matched anywhere in the document. Commission Implementing Decision (EU) 2021/914, Clauses 14 and 15 require the processor to notify the controller of legally-binding requests from public authorities and to challenge such requests where permitted; without a DPA-level restating clause, the Schrems II-mandated local-law analysis the controller must perform under Clause 14(a) lacks the textual contractual hook on which supervisory authorities expect to rely.

Each fail-fixture has a `.playbook` sidecar pinning it to `dpa-controller-processor`. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins two-hundred-and-thirteen fail-fixtures (was two-hundred-and-ten); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 213. `npm run lint && typecheck && test && build` all green (**1807 passing + 2 skips**).

### v3 fail-fixture corpus expansion (207 → 210) (2026-05-21) — ✅ complete

Advanced LAUNCH row v3-b coverage from two-hundred-and-seven end-to-end fail-fixtures to two-hundred-and-ten by adding three new fixtures, all targeting previously-uncovered rules within the GDPR Controller-to-Processor DPA playbook. None of the three load-bearing rules (DPA-040, DPA-046, DPA-052) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-subprocessor-annex-fail.txt` — GDPR Controller-to-Processor DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned schedule enumerating approved sub-processors at signature, instead relying on the Section 7 prior-written-authorization mechanism and the Controller's right to object to any addition or replacement of other processors as the operative mechanism for sub-processor approvals. No `(annex|appendix|exhibit|schedule) (III|3|C) … (sub-processor|sub-processor list)` anchor or "list of sub-processors" phrase appears anywhere in the document. Annex I (categories of personal data) is retained so DPA-028 still passes; Section 7's prior-authorization, advance-notice, and flow-down language is preserved so DPA-015 / DPA-016 / DPA-017 still pass where they were passing. **DPA-040 (warning)** fires — its present_patterns `/(annex|appendix|exhibit|schedule)\s+(III|3|C).{0,40}(sub[- ]?processor|sub-processor\s+list)/i` and `/list\s+of\s+sub[- ]?processors/i` are no longer matched anywhere in the document. Commission Implementing Decision (EU) 2021/914, Annex III (Module Two / Three) requires the parties to identify approved sub-processors at the time of signature; without an Annex III the controller cannot verify which downstream processors are in scope under the executed transfer mechanism.
- `dpa-controller-processor-missing-governing-law-fail.txt` — GDPR Controller-to-Processor DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned choice-of-applicable-jurisdiction provision within the DPA, instead relying on the corresponding provision in the MSA as the operative mechanism for selecting the substantive law applicable to disputes arising under the DPA. No "governing law", "governed by the laws", or "laws of <Capitalized-jurisdiction>" anchor appears anywhere in the document. Every other clause (subject-matter, confidentiality, subprocessors, data-subject rights, Art. 32–36 assistance, breach notification, audit, deletion, SCCs) is preserved so the rest of the DPA-001..055 baseline still passes where it was passing. **DPA-046 (warning)** fires — its present_patterns `/(governing\s+law|governed\s+by\s+the\s+laws|laws\s+of\s+(?:the\s+)?(?:republic\s+of\s+)?[A-Z])/i` are no longer matched anywhere in the document. GDPR does not mandate a governing-law clause directly, but the SCCs require the parties to name an EU Member State (Clause 17) and DPAs commonly mirror that selection; without a DPA-level governing-law anchor a supervisory authority cannot determine which national supervisory regime supplies the residual rules for interpretation.
- `dpa-controller-processor-missing-survival-fail.txt` — GDPR Controller-to-Processor DPA with the preamble extended by a sentence that affirmatively elects not to include a separately captioned post-termination-continuation clause within the DPA, instead relying on the Section 12 deletion-or-return mechanism and the corresponding wind-down provisions of the MSA as the operative mechanism for handling any Personal Data retained after expiry of the MSA. No "survive", "survival", or "survive the termination" anchor appears anywhere in the document. Section 12 (deletion or return at end of services) is preserved so DPA-013 still passes; every other clause is preserved so the rest of the DPA-001..055 baseline still passes where it was passing. **DPA-052 (warning)** fires — its present_pattern `/survive\s+(the\s+)?termination|survival/i` is no longer matched anywhere in the document. GDPR Art. 28(3)(g) contemplates that, on termination, personal data be deleted or returned but does not itself address obligations applicable to residual data the processor may lawfully retain; without a survival anchor the protective obligations governing any such retained data (confidentiality, security, breach notice) are textually unmoored from the moment the underlying MSA terminates.

Each fail-fixture has a `.playbook` sidecar pinning it to `dpa-controller-processor`. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins two-hundred-and-ten fail-fixtures (was two-hundred-and-seven); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 210. `npm run lint && typecheck && test && build` all green (**1798 passing + 2 skips**).

### v3 fail-fixture corpus expansion (204 → 207) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from two-hundred-and-four end-to-end fail-fixtures to two-hundred-and-seven by adding three new fixtures spanning three distinct v3 rule families (Vendor Security Addendum, GDPR DPA controller-processor, Multi-State US DPA). None of the three load-bearing rules (ADDENDA-007, DPA-039, USDPA-022) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `vendor-security-addendum-missing-data-classification-fail.txt` — Vendor Security Addendum with the preamble extended by a sentence that affirmatively elects not to adopt the controlled-vocabulary mechanism contemplated by NIST SP 800-53 RA-2 for a formal tiering or labeling scheme, and instead relies on uniform protective controls for all Customer Data regardless of content. No "data classification", "sensitivity label", "sensitivity tier", "confidential tier", or "classify Customer Data / data as" anchor appears anywhere in the document. A VDP clause is added to Section 3 so ADDENDA-005 still passes; AES-256, TLS 1.3, SOC 2 Type II, ISO 27001, MFA, SDLC, 24-hour incident window, audit right, subprocessors, data residency, deletion, insurance, and training are all preserved so the rest of the ADDENDA-001..009 baseline still passes where it was passing. **ADDENDA-007 (info)** fires — its present_pattern `/(data\s+classification|sensitivity\s+(?:label|tier)|confidential\s+tier|classify\s+(?:customer\s+data|data\s+as))/i` is no longer matched anywhere in the document. NIST SP 800-53 RA-2 (Asset Identification / Categorization) requires an explicit information-classification or sensitivity-tiering scheme so that protective controls are applied proportionately; without such a scheme, all Customer Data is implicitly treated at a single (often lower) tier, leaving higher-risk personal data underprotected relative to the Vendor's nominal security posture.
- `dpa-controller-processor-missing-tom-annex-fail.txt` — GDPR Controller-to-Processor DPA with the preamble extended by a sentence that affirmatively elects not to adopt the controlled-vocabulary mechanism contemplated by GDPR Art. 32 and Commission Implementing Decision (EU) 2021/914 for a separately captioned annex or appendix enumerating the specific security controls in place, and instead relies on Section 6 of the DPA as the operative security-posture documentation mechanism. No "(annex|appendix|exhibit|schedule) (II|2|B) … (technical|organisational|TOM)" anchor or "annex of technical and organisational" phrase appears anywhere in the document. Annex I (categories of personal data) is retained so DPA-028 still passes; all other clauses (subject-matter, confidentiality, subprocessors, data-subject rights, Art. 32–36 assistance, breach notification, audit, deletion, SCCs) are preserved. **DPA-039 (warning)** fires — its present_patterns `/(annex|appendix|exhibit|schedule)\s+(II|2|B).{0,40}(technical|organisational|TOM)/i` and `/annex\s+of\s+technical\s+and\s+organisational/i` are no longer matched anywhere in the document. GDPR Art. 32 and SCC Annex II require the processor agreement to include an annex that describes the technical and organisational measures applied; without this annex the DPA cannot demonstrate Art. 32 compliance in a supervisory authority inspection because the section-level description lacks the specificity required by the SCC template.
- `dpa-multi-state-us-missing-sensitive-pi-clause-fail.txt` — Multi-State US DPA with the preamble extended by a sentence that affirmatively elects not to adopt the controlled-vocabulary mechanism contemplated by Cal. Civ. Code § 1798.121 and analogous provisions of the VCDPA, CPA, and CTDPA for a separately captioned clause addressing heightened-risk categories of personal information, and instead relies on uniform protective controls for all personal information regardless of category. No "sensitive personal information", "sensitive personal data", or "sensitive data" anchor appears anywhere in the document. A Section 7 ("Categories and Duration") is retained so USDPA-013 and USDPA-014 still pass; "nature and purpose of the processing" appears in the preamble so USDPA-012 still passes; "documented instructions" appears in Sections 3 and 4 so USDPA-011 still passes. **USDPA-022 (warning)** fires — its present_pattern `/sensitive\s+(personal\s+)?(?:information|data)/i` is no longer matched anywhere in the document. CCPA/CPRA, CPA, CTDPA, and VCDPA each define a category of "sensitive personal information" or "sensitive data" and impose heightened obligations (including opt-out rights and purpose limitations); without a dedicated sensitive-data clause a regulator cannot determine whether the parties have identified and separately scoped the higher-risk data flows required by Cal. Civ. Code § 1798.121(a).

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`vendor-security-addendum` / `dpa-controller-processor` / `dpa-multi-state-us`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins two-hundred-and-seven fail-fixtures (was two-hundred-and-four); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 207. `npm run lint && typecheck && test && build` all green (**1789 passing + 2 skips**).

### v3 fail-fixture corpus expansion (201 → 204) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from two-hundred-and-one end-to-end fail-fixtures to two-hundred-and-four by adding three new fixtures spanning three distinct v3 rule families (Vendor Security Addendum, BAA, Multi-State US DPA). None of the three load-bearing rules (ADDENDA-005, BAA-040, USDPA-012) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `vendor-security-addendum-missing-vuln-disclosure-fail.txt` — Vendor Security Addendum with the vulnerability-management section extended by a sentence that affirmatively elects not to adopt the controlled-vocabulary mechanism contemplated by NIST SP 800-53 SI-5 for a formal vulnerability-disclosure policy, coordinated-disclosure program, or bug-bounty arrangement, and instead relies on direct customer escalation as the operative external-report pathway. No "vulnerability disclosure", "coordinated disclosure", "bug bounty", "responsible disclosure", or "VDP" anchor appears anywhere in the document. Every other clause (SOC 2 Type II, ISO 27001, AES-256 / TLS 1.3, annual penetration test, MFA / access control, SDLC reference, 24-hour incident notification, audit right, subprocessors, data classification, deletion on termination, insurance, annual training, signature block) is preserved so the rest of the ADDENDA-001..009 baseline still passes where it was passing. **ADDENDA-005 (info)** fires — its present_pattern `/(vulnerability\s+disclosure|coordinated\s+disclosure|bug\s+bounty|responsible\s+disclosure|VDP\b)/i` is no longer matched anywhere in the document. NIST SP 800-53 SI-5 (Software and System Information / Vulnerability Monitoring and Scanning) establishes a documented vulnerability-disclosure pathway as the practitioner-accepted baseline; without a VDP, researchers who discover flaws have no defined channel, materially increasing the time-to-fix and the risk of public disclosure before remediation.
- `baa-missing-notice-clause-fail.txt` — BAA with the "Notice" clause replaced by a "Communications" section that affirmatively elects not to adopt the controlled-vocabulary mechanism contemplated by 45 C.F.R. § 164.410(c) for a formal written notice clause specifying delivery methods, addresses, and timing requirements, and instead relies on direct email or phone contact between the parties' designated representatives as the operative communication pathway. No "notice shall be", "notices under/hereunder shall", or "notice address" anchor appears anywhere in the document. Every other clause (Permitted Uses, Safeguards, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, Books and Records, Return or Destruction, Minimum Necessary, Mitigation, Workforce Training, Encryption, Risk Assessment, Sanctions, Subcontractor List, Governing Law, Term, Effective Date, Signature) is preserved so the rest of the BAA-001..045 baseline still passes where it was passing. **BAA-040 (warning)** fires — its present_pattern `/(notice\s+(shall|must)\s+be|notices\s+(under|hereunder|shall)|notice\s+address)/i` is no longer matched anywhere in the document. 45 C.F.R. § 164.410(c) prescribes the required content of breach notifications; without a notice clause naming delivery methods and addresses, breach notices may be challenged as defective under § 164.410(d), and the parties have no contractual anchor to determine when a notice is legally effective.
- `dpa-multi-state-us-missing-nature-and-purpose-fail.txt` — Multi-State US DPA with the preamble extended by a sentence that affirmatively elects not to adopt the controlled-vocabulary mechanism contemplated by Va. Code § 59.1-579 and Colo. Rev. Stat. § 6-1-1305 for a formal written processing-characterization clause, and instead relies on the description of Services in the MSA as the operative mechanism for communicating the character and scope of the processing activities. No "nature and purpose of processing" or "nature and purpose of the processing" anchor appears anywhere in the document. A Section 7 ("Categories and Duration") is retained so USDPA-013 (type of personal data) and USDPA-014 (duration) still pass; "documented instructions" appears in Sections 3, 5, and 6 so USDPA-011 still passes. Every other clause is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-012 (warning)** fires — its present_pattern `/nature\s+and\s+purpose\s+of\s+(?:the\s+)?processing/i` is no longer matched anywhere in the document. VCDPA, CPA, CTDPA, UCPA, TDPSA, OCPA, and DPDPA all require the processor contract to set out the nature and purpose of the processing as a named element; without this clause regulators (including the Virginia AG and Colorado AG) cannot quickly assess whether the downstream processing is scoped to the stated controller purpose.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`vendor-security-addendum` / `baa` / `dpa-multi-state-us`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins two-hundred-and-four fail-fixtures (was two-hundred-and-one); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 204. `npm run lint && typecheck && test && build` all green (**1780 passing + 2 skips**).

### v3 fail-fixture corpus expansion (198 → 201) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-ninety-eight end-to-end fail-fixtures to two-hundred-and-one by adding three new fixtures spanning three distinct v3 rule families (Vendor Security Addendum, BAA, controller-processor GDPR DPA). None of the three load-bearing rules (ADDENDA-006, BAA-038, DPA-034) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `vendor-security-addendum-missing-sdlc-reference-fail.txt` — Vendor Security Addendum with a preamble paragraph that affirmatively elects not to adopt the NIST SP 800-53 SA-15 / SA-11 controlled-vocabulary software-build-assurance framework, and removes every anchor from the ADDENDA-006 present_pattern: no "secure-development lifecycle", "SDLC", "SAST", "DAST", "secure coding standards", "secure coding practices", or "code review" phrase appears anywhere in the document. Every other baseline clause (SOC 2 Type II, ISO 27001, AES-256 / TLS 1.3 encryption, annual penetration test, vulnerability disclosure policy, MFA / access control, data classification, 48-hour incident notification, audit right, subprocessors, data residency, deletion on termination, insurance, annual training, signature block) is preserved so the rest of the ADDENDA-001..009 baseline still passes where it was passing. **ADDENDA-006 (info)** fires — its present_pattern `/(secure[- ]development\s+lifecycle|\bSDLC\b|SAST|DAST|secure\s+coding\s+(?:standards|practices)|code\s+review)/i` is no longer matched anywhere in the document. NIST SP 800-53 SA-15 (Development Process, Standards, and Tools) and SA-11 (Developer Testing and Evaluation) establish a secure-development-lifecycle program — including static analysis, dynamic analysis, and peer review — as the consensus practitioner baseline for SaaS vendors; a Vendor Security Addendum that disclaims any named SDLC framework leaves Customer without a contractual hook to verify that vulnerabilities are caught before they reach production.
- `baa-missing-term-clause-fail.txt` — BAA with the 45 C.F.R. § 164.504(e) term clause replaced by a "Duration" section that affirmatively disclaims the controlled-vocabulary mechanism contemplated by 45 C.F.R. § 164.504(e) for a formal written term clause specifying an enumerated duration in calendar units, and instead relies on the co-terminous nature of the BAA with the underlying services agreement as the operative duration mechanism. No "term ... year/month/day", "term of this/the agreement", or "initial term" anchor appears anywhere in the document. Every other clause (Permitted Uses, Safeguards, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, Books and Records, Return or Destruction, Minimum Necessary, Mitigation, Workforce Training, Encryption, Risk Assessment, Sanctions, Subcontractor List, Governing Law, Notice, Effective Date, Signature) is preserved so the rest of the BAA-001..045 baseline still passes where it was passing. **BAA-038 (warning)** fires — its present_pattern `/(term\b.{0,40}(year|month|day)|term\s+of\s+(this\s+)?agreement|initial\s+term)/i` is no longer matched anywhere in the document. 45 C.F.R. § 164.504(e) requires BAAs to include a statement of the term of the agreement; without a clearly enumerated duration (e.g., co-terminous with the MSA for a stated number of years, or specifying an initial term with renewal mechanics), parties have no anchor for computing notice periods, breach-cure windows, or the timing of return-or-destruction obligations at termination.
- `dpa-controller-processor-missing-tia-reference-fail.txt` — DPA (Controller to Processor) with a preamble paragraph that affirmatively elects not to adopt the controlled-vocabulary mechanism contemplated by Commission Implementing Decision (EU) 2021/914, Clause 14 for a formal written country-law-and-practice assessment document, and instead relies on Processor's in-house legal team review of third-country frameworks as the operative mechanism. No "transfer impact assessment", "TIA", "transfer risk assessment", "TRA", or "local laws and practices" anchor appears anywhere in the document. Every other clause (Article 28 subject-matter through deletion/return, EU SCCs Module 2 incorporation, Annex I) is preserved so the rest of the DPA-001..055 baseline still passes where it was passing. **DPA-034 (warning)** fires — its present_pattern `/(transfer\s+impact\s+assessment|TIA\b|transfer\s+risk\s+assessment|TRA\b|local\s+laws\s+and\s+practices)/i` is no longer matched anywhere in the document. Schrems II / SCC Clause 14 requires parties to assess the laws and practices of the recipient country before making a transfer relying on SCCs; a DPA that disclaims the formal written assessment mechanism leaves the Controller without the documented due-diligence record that supervisory authorities (including the Irish DPC and the French CNIL) require when reviewing cross-border transfers following the invalidation of Privacy Shield.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`vendor-security-addendum` / `baa` / `dpa-controller-processor`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins two-hundred-and-one fail-fixtures (was one-hundred-and-ninety-eight); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 201. `npm run lint && typecheck && test && build` all green (**1771 passing + 2 skips**).

### v3 fail-fixture corpus expansion (195 → 198) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-ninety-five end-to-end fail-fixtures to one-hundred-and-ninety-eight by adding three new fixtures spanning three distinct v3 rule families (Vendor Security Addendum, BAA, vendor-form MSA-Deep). None of the three load-bearing rules (ADDENDA-001, BAA-034, MSA-028) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `vendor-security-addendum-missing-specific-security-measures-fail.txt` — Vendor Security Addendum with a preamble paragraph that affirmatively elects not to adopt the NIST SP 800-53 controlled-vocabulary enumeration of specific security controls, and rewrites Sections 2 (Data Protection), 3 (Security Testing), and 4 (Personnel Controls) to remove every anchor from all three ADDENDA-001 present_patterns: no "access control", "multi-factor", or "MFA" (pattern 1); no "encryption at rest", "encryption in transit", or "TLS 1.x" (pattern 2); no "vulnerability management", "patching", "configuration management", or "logging and monitoring" (pattern 3). SOC 2 Type II, ISO 27001, incident notification, audit right, subprocessors, data residency, deletion on termination, insurance, and training clauses are all preserved so the rest of the ADDENDA-002..009 baseline still passes where it was passing. **ADDENDA-001 (warning)** fires — all three of its present_patterns `/(access\s+control|multi[- ]factor|MFA)/i`, `/(encryption\s+(?:at\s+rest|in\s+transit)|TLS\s+1\.[23])/i`, and `/(vulnerability\s+management|patching|configuration\s+management|logging\s+and\s+monitoring)/i` are absent from the document. NIST SP 800-53 is the consensus practitioner baseline for vendor security addenda; a pure "industry-standard" commitment without an enumerated control list is unenforceable because it gives the customer no specific controls to verify, audit, or reference in a breach proceeding.
- `baa-missing-sanctions-policy-fail.txt` — BAA with the § 164.308(a)(1)(ii)(C) sanctions clause replaced by a "Personnel Conduct" section that affirmatively disclaims the controlled-vocabulary mechanism contemplated by 45 C.F.R. § 164.308(a)(1)(ii)(C): "The parties have elected not to adopt the controlled-vocabulary mechanism contemplated by 45 C.F.R. § 164.308(a)(1)(ii)(C) for a formal written personnel-accountability program requiring documented corrective action against staff who mishandle PHI, and instead rely on Business Associate's general employment-law and human-resources obligations as the operative personnel-accountability framework." No "(sanction|discipline) (policy|workforce|employees)" anchor appears anywhere in the document. Every other clause (Permitted Uses, Safeguards, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, Books and Records, Return or Destruction, Minimum Necessary, Mitigation, Workforce Training, Encryption, Risk Assessment, Subcontractor List, Governing Law, Notice, Term, Signature) is preserved so the rest of the BAA-001..045 baseline still passes where it was passing. **BAA-034 (warning)** fires — its present_pattern `/(sanction|discipline)\s+(policy|workforce|employees)/i` is no longer matched anywhere in the document. 45 C.F.R. § 164.308(a)(1)(ii)(C) requires covered entities and business associates to implement a sanctions policy for workforce members who fail to comply with the security policies and procedures of the entity; a BAA that affirmatively disclaims the controlled-vocabulary personnel-accountability framework leaves OCR without the § 164.308(a)(1)(ii)(C) hook it relies on when auditing business-associate workforce management.
- `msa-vendor-deep-missing-ai-usage-clause-fail.txt` — Vendor-form MSA with a new sentence in Section 2 (Services) that affirmatively disclaims any obligation to disclose whether Vendor uses automated reasoning, predictive, or machine-learning tooling in delivering the services, and explicitly states that "this Agreement does not incorporate by reference the NIST AI RMF transparency baseline or any other framework requiring disclosure of automated-tooling usage." No "artificial intelligence", "generative AI", "foundation model", "large language model", or "LLM" anchor appears anywhere in the document. Every other clause (Order of Precedence, Fees and Payment, Intellectual Property Ownership, Confidentiality, Limited Warranty, Mutual Indemnification, Limitation of Liability, Insurance, Termination, Governing Law, Entire Agreement) is preserved so the rest of the MSA-001..030 baseline still passes where it was passing. **MSA-028 (info)** fires — its present_pattern `/(artificial\s+intelligence|generative\s+AI|foundation\s+model|large\s+language\s+model|\bLLM\b)/i` is no longer matched anywhere in the document. NIST AI RMF establishes transparency about AI usage as the baseline consensus practice; an MSA that affirmatively disclaims disclosure of automated-tooling usage leaves Customer without the contractual hook to learn whether Vendor's outputs are AI-generated, whether Customer Data is used for model training, and who owns IP in AI-generated deliverables — all questions the AI Addendum playbook (§49) addresses only after the MSA-level AI-usage clause is confirmed present.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`vendor-security-addendum` / `baa` / `msa-vendor-deep`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-ninety-eight fail-fixtures (was one-hundred-and-ninety-five); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 198. `npm run lint && typecheck && test && build` all green (**1762 passing + 2 skips**).

### v3 fail-fixture corpus expansion (192 → 195) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-ninety-two end-to-end fail-fixtures to one-hundred-and-ninety-five by adding three new fixtures spanning three distinct v3 rule families (SCC Module 2, controller-processor GDPR DPA, CCPA Service-Provider Addendum). None of the three load-bearing rules (TRANSFER-001, DPA-030, USDPA-009) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `scc-module-2-missing-clause-1-fail.txt` — SCC Module 2 instrument with Clause 2 (Effect and Invariability) and Clauses 8 through 18 fully present, but with Clause 1 (Purpose and Scope) explicitly omitted and an inserted recital affirmatively disclaiming the omission. The other reference to "purpose" anywhere in the body has been re-phrased to "objective" so that the Clause 1 anchor regex does not match. Every other Module 2 clause + the three Annexes are preserved so the rest of the TRANSFER-002..019 baseline still passes where it was passing. **TRANSFER-001 (warning)** fires — its present_pattern `clause\s*1\b|purpose\s+and\s+scope` is no longer matched anywhere in the document. EU SCCs Clause 1 (Purpose and Scope) sets out the parties' agreement to be bound by the SCCs and establishes the Article 46(2)(c) GDPR transfer mechanism; an SCC Module 2 instrument that affirmatively omits the Clause 1 anchor loses the foundational scope statement a supervisory authority searches for when verifying that the parties have entered into the Approved Clauses as a Chapter V GDPR transfer tool.
- `dpa-controller-processor-missing-art27-representative-fail.txt` — Controller→processor GDPR DPA naming a non-EEA processor and expressly disclaiming designation of a GDPR Article 27 Union representative. An opening recital states that the Processor "is established outside the European Economic Area and that the Processor processes personal data of data subjects in the EEA in connection with the offering of services to those data subjects, so that Chapter V of the GDPR applies", and that "the Processor has elected not to designate a Union-based representative under the controlled-vocabulary mechanism contemplated by GDPR Chapter IV, and that the Processor instead relies on its in-house EEA legal team in Munich, Germany for direct communications with EEA data subjects and EEA supervisory authorities, without incorporation by reference of the controlled-vocabulary Chapter IV representative-designation framework." No "Article 27" / "EU representative" / "representative in the Union" anchor appears anywhere in the document. Every other clause (Article 28 subject-matter through deletion/return, EU SCCs incorporation, Annex I) is preserved so the rest of the DPA baseline still passes where it was passing. **DPA-030 (warning)** fires — its present_pattern `(article\s*27|EU\s+representative|representative\s+in\s+the\s+Union|representative\s+under\s+Article\s*27)` is no longer matched anywhere in the document. GDPR Art. 27 requires non-EU controllers and processors falling within Art. 3(2) to designate an EU representative as their point of contact for EEA supervisory authorities and data subjects; a DPA that affirmatively disclaims the controlled-vocabulary designation framework leaves EEA data subjects without the Art. 27 hook they would use to enforce their rights against an extraterritorial processor.
- `dpa-ccpa-service-provider-missing-inability-to-comply-notification-fail.txt` — CCPA Service-Provider Addendum with an added Section 10 ("Continued Capability") that affirmatively disclaims the controlled-vocabulary mechanism contemplated by 11 C.C.R. § 7051(a)(6): "The parties have elected not to adopt the controlled-vocabulary mechanism contemplated by 11 C.C.R. § 7051(a)(6) for a Service-Provider determination-based written notice to Business in the event Service Provider concludes that its capability has eroded, and instead rely on Business's audit rights in Section 9 and consumer-rights monitoring through the operational handoffs in Section 5 as the operational signal of any capability gap." No "no longer meet" / "no longer able to meet" / "notify ... can no longer" / "notify ... unable to meet" / "notify ... inability" anchor appears anywhere in the document. Every other clause (Service Provider Designation, Business Purpose, Prohibited Acts, Certification of Compliance, Consumer Rights Assistance, Subprocessors, Security, Deletion or Return on Termination, Audit) is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-009 (warning)** fires — its present_pattern `(no\s+longer\s+(meet|able\s+to\s+meet)|notify.{0,80}(can\s+no\s+longer|unable\s+to\s+meet|inability))` is no longer matched anywhere in the document. 11 C.C.R. § 7051(a)(6) requires the service-provider contract to require the service provider to notify the business if it makes a determination that it can no longer meet its obligations under the CCPA; without that determination-and-notify mechanism, the business has no early-warning signal of service-provider non-compliance and may continue to disclose Personal Information for the Business Purpose well past the point at which the service-provider relationship has broken down on the CCPA criteria.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`scc-module-2` / `dpa-controller-processor` / `dpa-ccpa-service-provider`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-ninety-five fail-fixtures (was one-hundred-and-ninety-two); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 195. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (189 → 192) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-eighty-nine end-to-end fail-fixtures to one-hundred-and-ninety-two by adding three new fixtures spanning three distinct v3 rule families (CCPA Service-Provider Addendum, mutual NDA-Deep, SCC Module 2). None of the three load-bearing rules (USDPA-021, NDA-D-024, TRANSFER-002) had a dedicated end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-ccpa-service-provider-multi-state-language-fail.txt` — CCPA Service-Provider Addendum with an added preamble paragraph explicitly stating that "this CCPA Addendum will serve as the Service Provider Addendum for any applicable US state privacy law where Business and Service Provider conduct business that involves consumer Personal Information, including a multi-state operational footprint across several states." Every other clause (Service Provider Designation, Business Purpose, Prohibited Acts, Certification of Compliance, Consumer Rights Assistance, Subprocessors, Security, Deletion or Return on Termination, Audit) is preserved unchanged so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-021 (info)** fires — its bad_pattern `multi[- ]state|several\s+(?:US\s+)?states|applicable\s+US\s+state\s+(?:privacy\s+)?law` now matches in three places in the added paragraph. When a CCPA-only template explicitly contemplates multi-state coverage without adopting the strictest applicable state's required elements (VCDPA / CPA / CTDPA may demand wider scope or additional protections than CCPA alone), the business risks failing to meet the strictest applicable requirement and may be reclassified as a third party under the non-California statute that governs the relevant transfer.
- `mutual-nda-deep-asymmetric-receiver-only-fail.txt` — Mutual NDA rewritten with the bilateral "Each Receiving Party shall ..." framing in Sections 3 and 4 of the baseline replaced by unilateral "The Receiving Party shall ..." framing in Sections 3 (Permitted Purpose), 4 (Obligations of Receiving Party), 5 (Receiving Party Representations), and 6 (Receiving Party Indemnification). Two additional one-sided clauses (Representations / Warranties, Indemnification) are added that bind only the Receiving Party. No "each party" / "each of the parties" anchor appears anywhere in the document. Every other clause (Confidential Information definition, statutory carveouts, three-year term with perpetual trade-secret carve-out, DTSA whistleblower-immunity notice, return-or-destruction, Delaware governing law, entire-agreement integration) is preserved. **NDA-D-024 (warning)** fires — its bad_pattern `^(?=.*receiving\s+party)(?!.*each\s+(party|of\s+the\s+parties)).{0,300}$` matches multiple lines in the document. Mutual NDAs are typically drafted with each party as both Disclosing and Receiving Party; asymmetric drafting that imposes obligations solely on "the Receiving Party" without "each party" framing suggests either (a) the wrong template was used, or (b) one party is silently advantaged — exactly the failure mode the rule is designed to detect.
- `scc-module-2-missing-clause-2-fail.txt` — SCC Module 2 instrument with Clause 1 (Purpose and Scope) and Clauses 8 through 18 fully present, but with Clause 2 (Effect and Invariability) explicitly omitted and an inserted recital affirmatively disclaiming the omission: "the parties have elected, for the purposes of this Module 2 instrument, not to adopt the operative legal-anchor clause that the standalone EU SCCs use to declare the legal effect of these Clauses and the prohibition on material modifications, and instead rely on the operative protections enumerated in Clauses 8 through 18 directly, without incorporation by reference of that operative legal-anchor clause's controlled-vocabulary anchor." Every other Module 2 clause + the three Annexes are preserved so the rest of the TRANSFER-001..019 baseline still passes where it was passing (TRANSFER-001 now also passes because Clause 1 is explicitly added). **TRANSFER-002 (warning)** fires — its present_pattern `clause\s*2\b|effect\s+and\s+invariability|invariability` is no longer matched anywhere in the document. EU SCCs Clause 2 forbids material modification of the Clauses, providing that the parties may add other clauses or business-related terms only if they do not contradict the SCCs; an SCC Module 2 instrument that affirmatively omits the Clause 2 anchor loses the operative protection a supervisory authority searches for when verifying that the parties have not silently weakened the Approved Clauses.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`dpa-ccpa-service-provider` / `mutual-nda-deep` / `scc-module-2`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-ninety-two fail-fixtures (was one-hundred-and-eighty-nine); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 192. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (186 → 189) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-eighty-six end-to-end fail-fixtures to one-hundred-and-eighty-nine by adding three new fixtures spanning three distinct v3 rule families (BAA, processor→subprocessor GDPR DPA, multi-state US DPA). None of the three load-bearing rules (BAA-045, DPA-017, USDPA-025) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-ce-ba-roles-unclear-fail.txt` — BAA with every "Covered Entity" / "Business Associate" role label replaced by the commercial "Client" / "Service Provider" pair throughout the preamble, every operative clause, and the signature block. An opening recital affirmatively disclaims the substitution: "the parties have elected to use the commercial labels 'Client' and 'Service Provider' in place of the 45 CFR § 160.103 controlled-vocabulary role designations used elsewhere in the HIPAA Rules, without incorporation by reference of those controlled-vocabulary designations." Every other clause is preserved with the relabeled terms so the rest of the BAA-001..044 baseline still resolves on commercial-vocabulary terms. **BAA-045 (warning)** fires — its present_pattern `covered\s+entity.*?business\s+associate|business\s+associate.*?covered\s+entity` is no longer matched anywhere in the document. 45 C.F.R. § 160.103 defines "Covered Entity" and "Business Associate" as the role designations that attach every downstream HIPAA Privacy and Security Rule obligation; a BAA that swaps both labels for commercial anchors and affirmatively disclaims the controlled-vocabulary framework leaves OCR without the role attachment a reading against §§ 164.502, 164.504, and 164.308–.314 relies on.
- `dpa-processor-subprocessor-missing-flow-down-anchor-fail.txt` — Processor→subprocessor GDPR DPA with Section 3 ("Same Obligations as Upstream DPA") rewritten as a "Flow-Down of Data-Protection Duties" section that enumerates each operational duty by individual Section reference (Sections 4 through 11) rather than by an Article 28(4) controlled-vocabulary flow-down anchor. The renamed section affirmatively disclaims the substitution: "The Parties have elected not to adopt the Article 28(4) controlled-vocabulary incorporated-by-reference flow-down anchor in this Section, and the Subprocessor's duties are expressed by reference to each individually-enumerated operational duty rather than by an incorporated-by-reference framework." Section 8 (Sub-Sub-Processors) is rewritten to refer to the same enumerated-duty framework rather than "equivalent obligations." Every other clause (Controller Authorization, Documented Instructions, Confidentiality, Article 32 Security Measures, Article 33 Breach Notification, Audit Rights, EU SCCs Module 3 incorporation, Deletion or Return, Annex I) is preserved so the rest of the DPA baseline still passes where it was passing. **DPA-017 (warning)** fires — its present_pattern `(sub[- ]?processor|another\s+processor).*?(same\s+(data[- ]protection\s+)?obligations|same\s+obligations|same\s+terms|equivalent\s+obligations)` is no longer matched anywhere in the document. GDPR Art. 28(4) requires the processor to impose, by contract or other legal act, the same data-protection obligations on the subprocessor as set out in the contract between the controller and processor; an enumerated-duty framework without the controlled-vocabulary anchor loses the Art. 28(4) hook a supervisory authority searches for when verifying that the upstream contract has been flowed down in substance.
- `dpa-multi-state-us-missing-personal-information-reference-fail.txt` — Multi-state US DPA with every "personal information" / "personal data" reference rewritten to the commercial label "Customer Records." An opening recital affirmatively disclaims the substitution: "the parties have elected to use the commercial label 'Customer Records' throughout this DPA in place of the regulator-defined terminology used elsewhere in the Applicable State Laws, without incorporation by reference of any state-statutory controlled-vocabulary terminology." Every other clause (CCPA/CPRA service-provider designation, prohibited acts, VCDPA/CPA/CTDPA/UCPA processor obligations, sub-processor flow-down, consumer rights assistance, breach notification, deletion on termination, certification) is preserved with the relabeled term so the rest of the DPA baseline still resolves on commercial-vocabulary terms. **USDPA-025 (warning)** fires — its present_pattern `personal\s+(information|data)` is no longer matched anywhere in the document. The CCPA/CPRA, VCDPA, CPA, CTDPA, and UCPA all define "personal information" (or "personal data") as the operative term that attaches every downstream service-provider / processor obligation; a multi-state DPA that never mentions personal information / data is almost certainly the wrong template (commercial confidentiality agreement, generic data-handling agreement, etc.) and the attorneys general / consumer protection agencies of each state would treat it as such.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`baa` / `dpa-processor-subprocessor` / `dpa-multi-state-us`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-eighty-nine fail-fixtures (was one-hundred-and-eighty-six); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 189. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (183 → 186) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-eighty-three end-to-end fail-fixtures to one-hundred-and-eighty-six by adding three new fixtures spanning three distinct v3 rule families (BAA, customer-form MSA-Deep, controller-processor GDPR DPA). None of the three load-bearing rules (BAA-041, MSA-027, DPA-050) had an end-to-end fail-fixture before this batch — and each targets a "wrong-template / structural-inconsistency" sanity check that needs an end-to-end fixture because the per-ruleset unit tests cover the regex but not the realistic full-document framing. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-phi-reference-fail.txt` — BAA with every "PHI" / "Protected Health Information" / "ePHI" reference rewritten to the commercial term "Confidential Health Records" ("CHR" / "eCHR"). An opening recital affirmatively disclaims the substitution: "the Parties have elected to use the commercial term 'Confidential Health Records' throughout this BAA, without incorporation by reference of the 45 CFR § 160.103 controlled-vocabulary defined term used elsewhere in the HIPAA Rules." Every other clause (Permitted Uses, Safeguards / Security Rule, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, HHS Books and Records, Return-or-Destruction, Minimum Necessary, Mitigation, Workforce Training, Encryption, Risk Assessment, Sanctions, Subcontractor List, Governing Law, Notice, Term, Signature) is preserved with the relabeled term so the rest of the BAA-001..045 baseline still resolves on commercial-vocabulary terms. **BAA-041 (warning)** fires — its present_pattern `Protected Health Information|\bPHI\b` is no longer matched anywhere in the document. 45 C.F.R. § 160.103 defines "Protected Health Information" as the operative term that attaches every downstream HIPAA Privacy and Security Rule obligation; a BAA that never mentions PHI is almost certainly the wrong template (commercial confidentiality agreement, generic data-processing agreement, etc.) and OCR would treat it as such.
- `msa-customer-deep-precedence-burying-operative-terms-fail.txt` — Customer-form MSA with the Order-of-Precedence clause in Section 1 expanded to explicitly acknowledge that the indemnification regime, aggregate liability cap, intellectual property ownership/assignment, and service warranty are each set forth in the SOW rather than in the MSA, while the MSA's stated precedence places it above the SOW ("the terms of this MSA shall control and govern over the SOW in all respects"). Sections 3, 5, 7, and 8 are rewritten as "Framework" clauses that delegate the operative substance to the SOW ("the substantive warranty for each engagement is set forth in the SOW", "the intellectual property ownership and assignment regime is set forth in the SOW", "the indemnification regime ... is set forth in the SOW", "the aggregate liability cap, the carveouts from the cap, and the mutual consequential-damages waiver applicable to each engagement are set forth in the SOW"). **MSA-027 (warning)** fires — the custom check detects all three signals it needs: (a) a precedence declaration ("in the event of any conflict or inconsistency"), (b) MSA-over-SOW phrasing ("this MSA shall control and govern over the SOW"), and (c) operative-term-in-SOW phrasing ("indemnif... set forth in the SOW", "warrant... set forth in the SOW", "intellectual property... set forth in the SOW"). The internal inconsistency — putting the MSA on top of the SOW while the SOW houses the actual operative terms — means SOW-level indemnity / cap / IP / warranty terms may be silently overridden by MSA defaults at the moment of conflict.
- `dpa-controller-processor-missing-personal-data-reference-fail.txt` — Controller→processor GDPR DPA with every "personal data" reference rewritten to the commercial term "Customer Information". An opening recital affirmatively disclaims the substitution: "the parties have elected to use the commercial term 'Customer Information' throughout this DPA in place of the GDPR Article 4(1) controlled-vocabulary defined term, without incorporation by reference of the Article 4(1) defined-term framework." Every other clause (Article 28 subject-matter through deletion/return, EU SCCs incorporation, Annex I) is preserved with the relabeled term so the rest of the DPA baseline still resolves on commercial-vocabulary terms. **DPA-050 (warning)** fires — its present_pattern `personal\s+data` is no longer matched anywhere in the document. GDPR Art. 4(1) defines "personal data" as the operative term that attaches every downstream Article 28 / 32 / 33 / 34 obligation; a DPA that never mentions personal data is almost certainly the wrong template (commercial confidentiality agreement, generic processing-of-business-data agreement, etc.) and a supervisory authority would treat it as such.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`baa` / `msa-customer-deep` / `dpa-controller-processor`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-eighty-six fail-fixtures (was one-hundred-and-eighty-three); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 186. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (180 → 183) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-eighty end-to-end fail-fixtures to one-hundred-and-eighty-three by adding three new fixtures spanning three distinct v3 rule families (BAA, UK IDTA Addendum, controller-processor GDPR DPA). None of the three load-bearing rules (BAA-005, TRANSFER-016, DPA-042) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-privacy-rule-subcontractor-flow-down-fail.txt` — BAA with the §164.504(e)(2)(ii)(D) "Subcontractors" Privacy-Rule flow-down clause rewritten as a "Downstream Vendor Flow-Down" section that affirmatively disclaims incorporation of the regulator-defined "subcontractor" controlled vocabulary ("the term 'downstream vendor' as used in this BAA is intended to refer to the operational concept of a delegated service provider engaged by Business Associate, without incorporation by reference of the 45 CFR § 164.504(e)(2)(ii)(D) controlled vocabulary regarding downstream entities receiving PHI"). Every other clause is preserved (Permitted Uses, Safeguards / Security Rule, Reporting, Access/Amendment/Accounting, HHS Books and Records, Return-or-Destruction, Minimum Necessary, Mitigation, Workforce Training, Encryption, Risk Assessment, Sanctions, Governing Law, Notice, Term, Signature) so the rest of the BAA-001..045 baseline still passes where it was passing. **BAA-005 (warning)** fires — its present_pattern `(subcontractor|sub[- ]processor).*?(same restrictions|same conditions|written contract|agree)` is no longer matched anywhere in the document. 45 C.F.R. § 164.504(e)(2)(ii)(D) requires the BAA to obligate the BA to ensure that any subcontractor that creates, receives, maintains, or transmits PHI agrees in writing to the same restrictions and conditions that apply to the BA; a "downstream vendor" framing that affirmatively disclaims the controlled vocabulary loses the § 164.504(e)(2)(ii)(D) hook OCR relies on when verifying flow-down compliance at the BA / subcontractor boundary.
- `uk-idta-addendum-missing-parties-section-anchor-fail.txt` — UK International Data Transfer Addendum (Addendum-form transfer instrument) that uses "Schedule 1 (Signatory Details)" as the parties-identification anchor instead of the ICO Mandatory Clauses-mandated `Part 1 — Parties` (standalone IDTA) or `Table 1 — Parties` (Addendum form) controlled vocabulary, and affirmatively disclaims adoption of either the standalone-IDTA section-naming convention or the Addendum-form anchor convention. Exporter (Acme UK Ltd.) and Importer (Globex Inc.) are still named operationally; Table 2 (Selected SCCs, Modules and Clauses), Table 3 (Appendix Information), and Table 4 (Ending this Addendum) anchors are preserved, so TRANSFER-012..014 still pass where they were passing. **TRANSFER-016 (warning)** fires — its present_patterns `Part\s*1\s*[—:-]\s*Parties` and `Table\s*1\s*[—:-]\s*Parties` match nothing in the document. The ICO Mandatory Clauses require the Approved Addendum / standalone IDTA to identify the Parties (Exporter and Importer) under either the `Part 1 — Parties` heading (standalone IDTA) or the `Table 1 — Parties` heading (Addendum form); a "Schedule 1 (Signatory Details)" substitute loses the anchor a UK supervisory authority searches for when verifying the Article 46(2)(d) (UK GDPR) transfer-tool compliance posture.
- `dpa-controller-processor-roles-unclear-fail.txt` — Controller→processor GDPR DPA with every "Controller" and "Processor" role label replaced by the commercial "Customer" / "Vendor" pair throughout the preamble, twelve operative clauses, Annex I, and signature blocks. An opening recital affirmatively disclaims the substitution: "the roles of 'Customer' and 'Vendor' are intended as commercial labels reflecting the operational relationship between the parties, without incorporation by reference of the GDPR Article 4(7)/(8) defined-term framework or any controlled vocabulary of regulator-defined party designations used elsewhere in the regulation." Every other clause (Article 28 subject-matter through deletion/return, EU SCCs incorporation, Annex I) is preserved with the new labels so the rest of the DPA baseline still resolves on commercial-vocabulary terms. **DPA-042 (warning)** fires — its present_pattern `controller.*?processor|processor.*?controller` is no longer matched anywhere in the document. GDPR Art. 4(7) and (8) define "Controller" and "Processor" as the role labels that attach the Article 28 obligations; a DPA that swaps both labels for commercial "Customer" / "Vendor" anchors and affirmatively disclaims the defined-term framework leaves a supervisory authority without the role attachment a reading against Articles 28 and 32–36 relies on.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`baa` / `uk-idta-addendum` / `dpa-controller-processor`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-eighty-three fail-fixtures (was one-hundred-and-eighty); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 183. `npm run lint && typecheck && test && build` all green (**1717 passing + 2 skips**).

### v3 fail-fixture corpus expansion (177 → 180) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-seventy-seven end-to-end fail-fixtures to one-hundred-and-eighty by adding three new fixtures spanning three distinct v3 rule families (controller-processor GDPR DPA, CCPA Service-Provider Addendum, customer-form MSA-Deep). None of the three load-bearing rules (DPA-010, USDPA-006, MSA-003) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-subprocessor-anchor-fail.txt` — Controller→processor GDPR DPA with the "Subprocessors" section heading renamed to "Downstream Vendors" and every "subprocessor" / "sub-processor" / "another processor" anchor swapped out for the generic "downstream vendor" framing. A closing recital in Section 13 clarifies that "downstream vendor" is intended as an operational concept "without incorporation by reference of any SCC-defined or Article 28 controlled vocabulary regarding processors engaging other processors." Every other clause is preserved (Article 28 documented instructions, Article 32 security measures, Article 33 within-48-hours breach notification, data-subject-rights assistance, audit and inspection rights, EU SCCs incorporation) so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-010 (critical)** fires — its present_pattern `(sub[- ]?processor|another\s+processor)` is no longer matched anywhere in the document. GDPR Art. 28(2) and (4) require the processor to respect the conditions for engaging another processor (prior authorisation + flow-down of the same obligations); a DPA that swaps the controlled-vocabulary "sub-processor" / "another processor" anchors for a generic "downstream vendor" loses the Art. 28(2)/(4) hook a supervisory authority searches for when verifying chain-of-responsibility and may leave it ambiguous which downstream relationships actually trigger the Article 28 flow-down obligation.
- `dpa-ccpa-service-provider-missing-certification-of-understanding-fail.txt` — CCPA Service-Provider Addendum with Section 3 ("Prohibited Acts") rewritten to drop "certifies that it understands and shall comply" in favour of "acknowledges and shall observe", and Section 4 ("Compliance Confirmation Report") rewritten to furnish an operational "compliance confirmation report" rather than a "written certification of compliance." A closing recital in Section 4 affirmatively disclaims the substitution: the report is "not intended to constitute, or to substitute for, any formal attestation, sworn declaration, or other regulator-format deliverable of CCPA understanding or restrictions." No "certifies" / "certification" anchor within 80 chars of "understand", "restrictions", "ccpa", or "comply" appears anywhere in the document. Every other clause is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-006 (warning)** fires — its present_pattern `(certifies?|certification).{0,80}(understand|restrictions|ccpa|comply)` is no longer matched anywhere in the document. 11 C.C.R. § 7051(a)(7) expects the service-provider contract to require an explicit certification that the service provider understands the restrictions and will comply; an "acknowledges and shall observe" substitute that affirmatively disclaims attestation-grade posture leaves the business without the contractual hook that supervisory authorities (and the Attorney General under § 1798.155) rely on to distinguish a Service Provider from a third-party recipient.
- `msa-customer-deep-missing-confidentiality-indemnity-fail.txt` — Customer-form MSA with the cap-carveout in Section 8(b)(ii) rewritten to refer to "Section 6 (non-public information protection duties)" instead of "either party's breach of its confidentiality obligations", and with a new sentence at the end of Section 6 affirmatively excluding confidentiality breaches from the indemnification regime: "the remedies for breach of this Section 6 are equitable in nature (injunction, specific performance) rather than indemnity-based; the parties agree that monetary damages alone would be inadequate, and that the indemnification provisions in Section 7 do not extend to breaches of this Section 6." Every other clause is preserved (IP ownership, Vendor indemnification for IP infringement / gross negligence, aggregate liability cap with non-cap carveouts, mutual consequential-damages waiver) so the rest of the MSA-001..030 baseline still passes where it was passing. **MSA-003 (info)** fires — its present_pattern `(?:indemnif\w+).{0,200}(?:confidential\w+|confidentiality\s+(?:breach|obligation))` is no longer matched anywhere in the document. Confidentiality breaches often produce damages that are difficult to quantify; an MSA that affirmatively excludes confidentiality breaches from indemnity coverage and confines the customer to equitable remedies leaves the customer without a contractual hook to recover defence costs, third-party claim exposure, or monetary damages caused by Vendor's confidentiality failures (data-leak third-party claims, regulatory investigation costs, downstream notification expenses).

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`dpa-controller-processor` / `dpa-ccpa-service-provider` / `msa-customer-deep`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-eighty fail-fixtures (was one-hundred-and-seventy-seven); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 180. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (174 → 177) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-seventy-four end-to-end fail-fixtures to one-hundred-and-seventy-seven by adding three new fixtures spanning three distinct v3 rule families (unilateral NDA-Deep, CCPA Service-Provider Addendum, controller-processor GDPR DPA). None of the three load-bearing rules (NDA-D-025, USDPA-007, DPA-041) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `unilateral-nda-deep-missing-disclosing-receiving-roles-fail.txt` — Unilateral NDA-Deep with the "Disclosing Party" / "Receiving Party" role labels swapped throughout for the more generic "Provider" / "Recipient" framing. Every operative clause survives (Confidential Information definition, statutory carveouts, receiver obligations, no-license clause, three-year term with perpetual trade-secret carve-out, DTSA whistleblower-immunity notice, return-or-destruction with ten-business-day attestation, Delaware governing law, entire-agreement integration clause), but no "Disclosing Party" / "Receiving Party" anchor appears anywhere in the document. **NDA-D-025 (warning)** fires — its present_patterns `disclosing\s+party.{0,200}receiving\s+party` and `receiving\s+party.{0,200}disclosing\s+party` are no longer matched anywhere in the document. Unilateral NDAs should clearly name one party as the Disclosing Party and the other as the Receiving Party so the statutory framing and any downstream incorporation by reference (DTSA notice, residuals carve-outs, trade-secret protection ladders) attaches to the right role; a "Provider" / "Recipient" generic substitute loses the unilateral-NDA template anchor and signals a template mismatch that downstream tooling (clause-library look-ups, compliance audits, e-discovery) cannot transparently recover.
- `dpa-ccpa-service-provider-missing-monitoring-right-fail.txt` — CCPA Service-Provider Addendum with Section 4 ("Certification of Compliance") rewritten to remove the "reasonable and appropriate steps" anchor required by Cal. Civ. Code § 1798.140(ag)(1)(C). The replacement language affirmatively disclaims any Business entitlement to "supervisory action" beyond the audit cadence in Section 9 and limits Service Provider's compliance evidence to "certifications, deletion confirmations, and audit-window deliverables expressly enumerated in this CCPA Addendum." No "reasonable and appropriate steps", "monitor", "oversight right", or "right to (audit|monitor|inspect)" anchor appears anywhere in the document. Every other clause is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-007 (critical)** fires — its present_pattern `(reasonable\s+and\s+appropriate\s+steps|monitor|oversight\s+right|right\s+to\s+(?:audit|monitor|inspect))` is no longer matched anywhere in the document. § 1798.140(ag)(1)(C) requires the contract to grant the business the right to take reasonable and appropriate steps to ensure that the service provider uses personal information in a manner consistent with the business's CCPA obligations; an addendum that limits the business to a once-per-year audit window leaves it without a contractual hook to intervene when off-window evidence of unauthorized use surfaces and exposes the business to CCPA enforcement under § 1798.155.
- `dpa-controller-processor-missing-gdpr-terminology-fail.txt` — Controller→processor GDPR DPA with both "personal data breach" anchors in Section 10 ("Breach Notification") downgraded to the generic "data breach" plus an explicit recital disclaiming incorporation of any "defined-term framework or controlled vocabulary from any specific regulator or implementing regulation" with respect to that term. Every other clause is preserved (Article 28 documented instructions, Article 32 security measures, Article 33 within-48-hours undue-delay language, data-subject-rights assistance, Annex I categories, EU SCCs incorporation by reference) so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-041 (warning)** fires — its present_pattern `(personal\s+data\s+breach|data\s+subject\s+shall\s+have|controller.{0,40}(shall\s+have\s+the\s+meaning|means)|processor.{0,40}(shall\s+have\s+the\s+meaning|means))` is no longer matched anywhere in the document. GDPR Art. 4 defines the operative terms ("Personal Data Breach", "Data Subject", "Controller", "Processor") that anchor every downstream Article 28 obligation; a DPA that uses the operational "data breach" shorthand and disclaims controlled vocabulary loses the Art. 4 hook a supervisory authority relies on when reading the agreement against the regulation and creates ambiguity about whether the Art. 33 notification trigger is the GDPR-defined event or the parties' looser operational concept.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`unilateral-nda-deep` / `dpa-ccpa-service-provider` / `dpa-controller-processor`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-seventy-seven fail-fixtures (was one-hundred-and-seventy-four); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 177. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (171 → 174) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-seventy-one end-to-end fail-fixtures to one-hundred-and-seventy-four by adding three new fixtures spanning three distinct v3 rule families (mutual NDA-Deep, controller-processor GDPR DPA, vendor MSA-Deep). None of the three load-bearing rules (NDA-D-022, DPA-028, MSA-030) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-missing-authority-rep-fail.txt` — Mutual NDA-Deep with a new Section 12 "Representations of the Parties" paragraph that recites only narrow execution representations (the natural person executing is duly designated; execution has been approved through customary corporate-decision processes) and explicitly forecloses further inquiry: "The Parties make no other representation or warranty in connection with the execution of this Agreement, and any further inquiry into a Party's organizational standing or contractual posture is foreclosed by this Section 12." No "full authority", "full power and authority", "authority to enter / execute", or "no conflicting (obligation|agreement)" anchor appears anywhere in the document. ("Authorized Representative" in the signature block does not match the regex because it begins with "authoriz-ed" rather than the word "authority".) Every other clause (DTSA notice, perpetual trade-secret carve-out, no-license, injunctive relief with bond waiver, governing law, no-precedent entire-agreement clause) is preserved so the rest of the NDA-D-001..025 baseline still passes where it was passing. **NDA-D-022 (info)** fires — its present_patterns `(full\s+(power\s+and\s+)?authority|authority\s+to\s+(enter|execute))` and `no\s+conflicting\s+(obligation|agreement)` are no longer matched anywhere in the document. An authority + no-conflict representation is a low-cost addition that closes off a defensive argument later; foreclosing the inquiry instead signals concealment and is the structural opposite of the Common-Paper-style anchor the rule looks for.
- `dpa-controller-processor-missing-art30-ropa-assistance-fail.txt` — Controller→processor GDPR DPA with a new Section 14 ("Internal Documentation") that proprietizes Processor's internal documentation of personal-data flows and processing activities and explicitly disclaims any obligation to assist Controller with controller-side documentation requirements outside Sections 8, 9, and 11: "Processor's obligation to assist Controller is limited to the categories of assistance enumerated in Sections 8, 9, and 11 of this DPA, and Processor shall have no obligation to assist Controller in compiling, maintaining, or supplementing any controller-side documentation requirement under any provision of the GDPR or other applicable data protection law that is not expressly enumerated in this DPA." No "records of processing", "article 30", or "RoPA" anchor appears anywhere in the document. Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-028 (warning)** fires — its present_pattern `(records\s+of\s+processing|article\s*30|RoPA)` is no longer matched anywhere in the document. GDPR Art. 30 requires controllers to maintain records of processing activities; most DPAs anchor processor assistance with the controller's Art. 30 RoPA. A DPA that proprietizes processor's internal documentation and disclaims any non-enumerated assistance duty leaves the controller without a contractual hook to compile its own RoPA when the processor is the only party with visibility into the upstream processing-activity metadata.
- `msa-vendor-deep-missing-essential-purpose-fail.txt` — Vendor MSA with two distinct sole-and-exclusive-remedy clauses — Section 6 ("CUSTOMER'S SOLE AND EXCLUSIVE REMEDY ... SHALL BE RE-PERFORMANCE") and a new Section 7 ("Service Levels") declaring service-level credits Vendor's "sole and exclusive remedy" with the explicit additional statement that "no additional damages, refunds, or substitute performance remedies shall be available with respect to any service-level failure regardless of the cause, severity, duration, or recurrence of the failure" — and no "fail of (its) essential purpose", "essential purpose", or "U.C.C. § 2-719" anchor anywhere in the document. Every other clause is preserved so the rest of the MSA-001..030 baseline still passes where it was passing. **MSA-030 (info)** fires — its present_pattern `(fail\w*\s+of\s+(?:its\s+)?essential\s+purpose|essential\s+purpose|U\.?C\.?C\.?\s*§\s*2-719)` is no longer matched anywhere in the document. UCC § 2-719(2) preserves the customer's alternative remedies when a limited remedy fails of its essential purpose; an MSA whose limited remedies are paired with explicit disclaimers of substitute remedies forecloses the statutory escape on its face and leaves the customer with no remedy when re-performance / SLA credits prove worthless (e.g. extended service unavailability, repeated re-performance failure).

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`mutual-nda-deep` / `dpa-controller-processor` / `msa-vendor-deep`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-seventy-four fail-fixtures (was one-hundred-and-seventy-one); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 174. `npm run lint && typecheck && test && build` all green; **1690/1690 tests + 2 skips**.

### v3 fail-fixture corpus expansion (168 → 171) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-sixty-eight end-to-end fail-fixtures to one-hundred-and-seventy-one by adding three new fixtures spanning three distinct v3 rule families (mutual NDA-Deep, SCC Module 2 / Transfer, CCPA Service-Provider Addendum). None of the three load-bearing rules (NDA-D-019, TRANSFER-010, USDPA-005) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-missing-no-precedent-fail.txt` — Mutual NDA-Deep with a new Section 12 "Course of Dealing" paragraph that does the opposite of a no-precedent clause: it expressly states that "the substantive provisions of this Agreement reflect the established baseline against which the Parties intend to evaluate, draft, and benchmark any future commercial agreement between them concerning the subject matter hereof, and the deal-equivalent commercial terms reached under this Agreement shall inform the Parties' subsequent negotiations of any related downstream arrangement." No "no precedent", "not create a precedent", "not constitute a precedent", or "most favored nation" anchor appears anywhere in the document. Every other clause (DTSA notice, perpetual trade-secret carve-out, no-license, injunctive relief with bond waiver, governing law, entire agreement) is preserved so the rest of the NDA-D-001..025 baseline still passes where it was passing. **NDA-D-019 (info)** fires — its present_pattern `(no\s+precedent|not\s+(create|constitute)\s+a\s+precedent|most\s+favored\s+nation)` is no longer matched anywhere in the document. A course-of-dealing clause that affirmatively designates the NDA as the commercial benchmark for future agreements is the structural opposite of a no-precedent clause and exposes the discloser to MFN-style argumentation if it later negotiates a tighter NDA with a different counterparty under different commercial assumptions.
- `scc-module-2-missing-clause-18-fail.txt` — SCC Module 2 transfer with Clauses 1, 2, 8, 9, 11, 14, 15, and 16 explicitly anchored (so TRANSFER-001 / TRANSFER-002 are satisfied) but Clause 18 is replaced with a plain-English "Governing Law" paragraph that picks the Republic of Ireland and Dublin venue without using the canonical "Clause 18", "Governing Law and Forum", "Governing Law and Jurisdiction", or "choice of forum" anchor. An "Adequacy Fallback" paragraph satisfies TRANSFER-018 so the fixture is not co-flagged on the fallback rule. Every other Module 2 clause + the three Annexes are preserved so the rest of the TRANSFER-001..019 baseline still passes where it was passing. **TRANSFER-010 (warning)** fires — its present_pattern `(clause\s*18\b|governing\s+law\s+and\s+(?:forum|jurisdiction)|choice\s+of\s+forum)` is no longer matched anywhere in the document. EU SCCs Clause 18 fixes the governing law on an EU Member State allowing third-party-beneficiary rights and the forum on a court of that Member State; a "Republic of Ireland law / Dublin venue" recital reaches the same operative result but loses the SCC Clause 18 anchor a supervisory authority searches for when verifying invariability under Clause 2.
- `dpa-ccpa-service-provider-missing-same-level-protection-fail.txt` — CCPA Service-Provider Addendum with a new Section 2 ("Compliance Posture") paragraph that explicitly substitutes "an equivalent privacy posture" for the canonical CCPA "same level of privacy protection" recital and pre-emptively disclaims incorporation of any additional statutory or regulatory standard: "Service Provider's privacy obligations under this Agreement are limited to the specific commitments enumerated herein and the obligations expressly imposed on service providers under 11 C.C.R. § 7051(a)(1)–(8), without incorporation by reference of any additional statutory or regulatory standard." No "same level of privacy protection" or "comply with all applicable obligations / CCPA" anchor appears anywhere in the document. Every other clause is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-005 (warning)** fires — its present_pattern `same\s+level\s+of\s+privacy\s+protection|comply\s+with\s+all\s+applicable\s+(?:obligations|ccpa)` is no longer matched anywhere in the document. Cal. Civ. Code § 1798.100(d) requires the contract to obligate the service provider to comply with applicable CCPA obligations and to provide the same level of privacy protection as required by the CCPA; an "equivalent privacy posture" substitute that affirmatively disclaims incorporation of any additional standard leaves the business exposed to enforcement under § 1798.155 because the service provider's residual obligation floor is whatever the contract enumerates rather than what the CCPA requires of the business.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`mutual-nda-deep` / `scc-module-2` / `dpa-ccpa-service-provider`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-seventy-one fail-fixtures (was one-hundred-and-sixty-eight); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 171. `npm run lint && typecheck && test && build` all green; **1681/1681 tests + 2 skips**.

### v3 fail-fixture corpus expansion (165 → 168) (2026-05-20) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-sixty-five end-to-end fail-fixtures to one-hundred-and-sixty-eight by adding three new fixtures spanning three distinct v3 rule families (vendor MSA-Deep, SCC Module 2 / Transfer, CCPA Service-Provider Addendum). None of the three load-bearing rules (MSA-025, TRANSFER-018, USDPA-002) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `msa-vendor-deep-missing-amendment-no-waiver-fail.txt` — Vendor MSA with a new Section 13 ("Modifications and Forbearance") whose first sentence explicitly contemplates modification of the operative terms through "mutual understanding evidenced through their respective duly-empowered representatives' subsequent course of dealing, including through email exchanges, executed change orders, addenda, or any combination of the foregoing methods" — and never uses the word "amend" anywhere in the document — and the second sentence frames forbearance as "operative only for the specific instance" and "shall not operate as a continuing relinquishment" without ever saying "no waiver", "no failure", or "shall not be deemed a waiver". Every other clause is preserved so the rest of the MSA-001..030 baseline still passes where it was passing. **MSA-025 (info)** fires — neither `amend\w+.{0,40}(?:in\s+writing|written\s+(?:and\s+)?signed)` nor `(?:no\s+(?:waiver|failure)|shall\s+not\s+be\s+deemed\s+a\s+waiver)` matches anywhere in the document. A "course of dealing" modification clause that condones email modifications and a forbearance paragraph that omits the canonical no-waiver formula together leave the vendor MSA without the two commercial-baseline boilerplate hooks that prevent oral modifications and unintentional waivers from becoming litigation risks.
- `scc-module-2-missing-adequacy-fallback-fail.txt` — SCC Module 2 transfer with a new "Reliance on Adequacy Decision" paragraph immediately after the Docking Clause that explicitly anchors the parties on the EU-US Data Privacy Framework (DPF) adequacy decision (Commission Implementing Decision (EU) 2023/1795) "for so long as it remains in force" and explicitly disclaims any contractual fallback: "the Parties have not agreed any contingency or alternative transfer arrangement to be implemented if the DPF adequacy decision is set aside, withdrawn, struck down, or otherwise ceases to provide a lawful transfer basis". No "adequacy decision is invalidated / revoked", "fallback to SCC", or "substitute (transfer) mechanism" anchor appears anywhere in the document. Every other Module 2 clause + the three Annexes are preserved so the rest of the TRANSFER-001..019 baseline still passes where it was passing. **TRANSFER-018 (warning)** fires — its present_pattern `(adequacy\s+decision\s+is\s+(?:invalidated|revoked)|fallback\s+(?:to\s+)?SCC|substitute\s+(?:transfer\s+)?mechanism)` is no longer matched anywhere in the document. GDPR Art. 45 adequacy decisions can be invalidated (Schrems I, Schrems II); the DPF was itself the third successive adequacy mechanism for EU-US transfers. A transfer arrangement that relies on the DPF without anchoring a contractual fallback to SCCs / IDTA on invalidation creates a "transfer cliff" the moment the DPF decision is set aside.
- `dpa-ccpa-service-provider-missing-no-sale-prohibition-fail.txt` — CCPA Service-Provider Addendum that retains the specific-business-purpose anchor (USDPA-001 stays satisfied via the "Business Purpose enumerated in this Agreement" recital) and the no-cross-context-advertising prohibition (USDPA-003 stays satisfied), but Section 3(a) is rewritten to use "shall not transfer Personal Information to any third party in exchange for monetary or other valuable consideration" — avoiding the statutory anchor language. Section 5's "opt-out of sale or sharing of Personal Information" is rewritten as "opt-out of cross-context behavioral advertising or the transfer of Personal Information for monetary or other valuable consideration" so no "sale of personal information" tail remains. No "prohibited from selling", "no sale of personal information", or "shall not sell personal information" anchor appears anywhere in the document. Every other clause is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-002 (warning)** fires — its present_pattern `(prohibited\s+from\s+selling|no\s+sale\s+of\s+personal\s+information|shall\s+not\s+sell\s+personal\s+information)` is no longer matched anywhere in the document. Cal. Civ. Code § 1798.140(ag)(1)(A) requires the service-provider contract to expressly prohibit sale of Personal Information; a clause that paraphrases the obligation as "shall not transfer for monetary consideration" tracks the underlying definition of "sale" at § 1798.140(ad) but loses the canonical anchor a regulator searches for when establishing CCPA compliance.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`msa-vendor-deep` / `scc-module-2` / `dpa-ccpa-service-provider`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-sixty-eight fail-fixtures (was one-hundred-and-sixty-five); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 168. `npm run lint && typecheck && test && build` all green; **1672/1672 tests + 2 skips**.

### v3 fail-fixture corpus expansion (162 → 165) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-sixty-two end-to-end fail-fixtures to one-hundred-and-sixty-five by broadening across three more playbooks (mutual NDA-Deep, CCPA Service-Provider Addendum, Vendor Security Addendum). None of the three load-bearing rules (NDA-D-016, USDPA-001, ADDENDA-003) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-missing-bond-waiver-fail.txt` — Mutual NDA-Deep with a complete Section 9 Injunctive Relief clause (acknowledging irreparable harm and entitlement to injunctive / equitable relief so NDA-D-015 stays satisfied) but with the customary bond-waiver sentence stripped; the closing sentence of Section 9 says "Such injunctive relief shall be available subject to the requirements imposed by the court of competent jurisdiction, including any requirement that the movant satisfy each element of the applicable preliminary-injunction standard before relief issues." No "without bond", "waive ... bond", "waived bond", "no bond", "posting of a bond ... waived", "security", or "surety" anchor appears anywhere in the document. Every other clause is preserved so the rest of the NDA-D-001..025 baseline still passes where it was passing. **NDA-D-016 (warning)** fires — its present_patterns `(without|waive[sd]?).{0,40}(bond|security|surety)` and `(no\s+bond|posting\s+of\s+a\s+bond.{0,40}waived)` are no longer matched anywhere in the document. Many courts require a movant to post a bond as a condition of preliminary injunctive relief; a contractual waiver smooths the emergency-motion path, and a mutual NDA that names injunctive relief without waiving the bond leaves a delay-and-cost lever on the table that a determined leaker can exploit while disclosure is in flight.
- `dpa-ccpa-service-provider-missing-purpose-limitation-fail.txt` — CCPA Service-Provider Addendum that names the Service Provider designation and recites the "same level of privacy protection" standard from Cal. Civ. Code § 1798.140(ag) but never anchors the use of Personal Information to the "specific business purpose enumerated" framing required by § 1798.140(ag)(1)(B). Section 2 ("Permitted Processing") rewrites the canonical anchor as "solely as reasonably necessary and proportionate to perform the Services described in the MSA" and Section 3(c) generalizes the use-limitation to "any commercial purpose other than performing the Services described in the MSA". No "specific business purpose", "enumerated", or "business purpose enumerated" anchor appears anywhere in the document. Every other clause is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-001 (warning)** fires — its present_pattern `(?:specific\s+business\s+purpose|enumerated\s+(?:in\s+this\s+)?(?:contract|agreement)|business\s+purpose\s+enumerated)` is no longer matched anywhere in the document. Cal. Civ. Code § 1798.140(ag)(1)(B) requires the contract to prohibit the service provider from retaining, using, or disclosing Personal Information for any purpose other than the specific business purpose enumerated; "reasonably necessary and proportionate" language without the statutory anchor leaves the business exposed to enforcement under § 1798.155 if the service provider's processing drifts beyond the enumerated purpose set.
- `vendor-security-addendum-missing-audit-or-soc2-fail.txt` — Vendor Security Addendum with Section 1 rewritten from certifications subsections (SOC 2 Type II + ISO 27001) to a generic "written information-security program aligned with prevailing industry practice" paragraph that delivers only an executive summary on request; Section 6 ("Verification") is rewritten to explicitly disclaim any customer-initiated audit privilege and to forbid reliance on any third-party assurance framework or certification scheme. The encryption, MFA, and vulnerability-management sections are preserved (so ADDENDA-001 and ADDENDA-005 still pass), but no "right to audit", "Customer ... audit", "SOC 2", "ISO 27001", "ISO/IEC 27001", or "in lieu of (an) audit" anchor appears anywhere in the document. Every other clause is preserved so the rest of the ADDENDA-001..009 baseline still passes where it was passing. **ADDENDA-003 (warning)** fires — its present_pattern `(right\s+to\s+audit|customer.{0,40}audit|SOC\s*2|ISO\s*27001|ISO\s*\/IEC\s*27001|in\s+lieu\s+of\s+(?:an\s+)?audit)` is no longer matched anywhere in the document. Customers must be able to verify the vendor's security posture either directly (audit) or via an independent attestation (SOC 2 / ISO 27001); a "self-assessment plus executive summary" posture leaves the customer with no enforceable verification mechanism.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`mutual-nda-deep` / `dpa-ccpa-service-provider` / `vendor-security-addendum`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-sixty-five fail-fixtures (was one-hundred-and-sixty-two); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 165. `npm run lint && typecheck && test && build` all green; **1663/1663 tests + 2 skips**.

### v3 fail-fixture corpus expansion (159 → 162) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-fifty-nine end-to-end fail-fixtures to one-hundred-and-sixty-two by adding three new BAA fixtures covering three previously uncovered BAA failure modes (audit-rights clause missing under a SOC-2-only verification posture, PHI / ePHI definitional anchor missing, HIPAA-obligation survival clause missing). None of the three load-bearing rules (BAA-026, BAA-028, BAA-043) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-audit-rights-fail.txt` — BAA with a "Compliance Verification" paragraph that explicitly disclaims any on-site or remote inspection privileges and designates the BA's annual SOC 2 Type II report as the "sole means" of verifying compliance; the document contains no "audit rights", "right to audit", "reasonable audit", or "conduct ... audit" anchor anywhere. Every other clause (Permitted Uses, Safeguards, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, Books/Records, Return/Destruction, Minimum Necessary, Mitigation, Workforce Training, Encryption, Risk Assessment, Sanctions, Subcontractor List, Governing Law, Notice, Term with explicit "shall survive termination") is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-026 (warning)** fires — its present_pattern `(audit\s+rights|right\s+to\s+audit|reasonable\s+audit|conduct.*audit)` is no longer matched anywhere in the document. HHS guidance encourages audit rights as a substantive complement to the books-and-records access requirement under 45 C.F.R. § 164.504(e)(2)(ii)(H); a BAA that swaps audit rights for SOC-2 self-attestation leaves the covered entity with no contractual mechanism to verify ongoing HIPAA compliance beyond the books-and-records floor.
- `baa-missing-phi-definition-fail.txt` — BAA that uses "Protected Health Information" throughout but never defines the term locally and never cross-references the HIPAA definition; the Definitions paragraph only says "Capitalized terms used but not defined in this BAA carry the meaning given to them in the Master Services Agreement" with no "Protected Health Information ... means", "PHI ... shall have the meaning", or "45 CFR § 160.103" anchor anywhere; "Protected Health Information" is spelled out in full throughout so no `("PHI")` short-form abbreviation is introduced. Every other clause (including an explicit Audit Rights paragraph that satisfies BAA-026 and a Term clause with "shall survive termination" that satisfies BAA-043) is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-028 (warning)** fires — its present_pattern `(protected\s+health\s+information.{0,80}(means|shall\s+have)|160\.103|PHI.*shall\s+have\s+the\s+meaning)` is no longer matched anywhere in the document. 45 C.F.R. § 160.103 defines PHI and ePHI; drafters should either define these terms locally or incorporate the HIPAA definition by reference. A BAA that uses "Protected Health Information" without anchoring the regulatory definition leaves the scope of the obligation ambiguous when the term is later asserted to cover (or not cover) a borderline category of data.
- `baa-missing-survival-clause-fail.txt` — BAA with the Term clause rewritten to make every right and duty of the parties terminate fully upon termination, with the sole carve-out being a 30-day completion window for the return-or-destruction obligation; the document contains no "survive termination", "shall survive", or "survival" anchor anywhere (the rewrite says the return-or-destruction obligation "thereafter terminates without further continuing effect" rather than the customary "shall survive"). Every other clause (including the Audit Rights paragraph) is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-043 (warning)** fires — its present_pattern `survive\s+(the\s+)?termination|survival` is no longer matched anywhere in the document. 45 C.F.R. § 164.504(e)(2)(ii)(I) and HHS guidance expect HIPAA obligations to survive termination for any PHI retained after termination; a BAA whose Term clause sweeps every obligation to a clean cut-off leaves the covered entity with no contractual hook for retained-PHI handling after the commercial relationship ends.

Each fail-fixture has a `.playbook` sidecar pinning it to the `baa` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-sixty-two fail-fixtures (was one-hundred-and-fifty-nine); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 162. `npm run lint && typecheck && test && build` all green; **1654/1654 tests + 2 skips**.

### v3 fail-fixture corpus expansion (156 → 159) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-fifty-six end-to-end fail-fixtures to one-hundred-and-fifty-nine by broadening per-playbook coverage for three previously under-tested playbooks (`dpa-processor-subprocessor`, `baa-subcontractor`, `unilateral-nda-deep`). Each fixture exercises a load-bearing rule already covered for the primary playbook variant but adds a per-playbook variant for the related/secondary playbook. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-processor-subprocessor-missing-signature-block-fail.txt` — Processor→subprocessor GDPR DPA with the closing signature block replaced with a single recital paragraph: "This Agreement is executed on behalf of Globex EU SARL (Upstream Processor) and Stark Cloud Ireland Ltd. (Subprocessor) through their respective representatives, with execution evidenced solely by reference to the MSA cover page; no separate execution lines, witness lines, or attestation blocks are included in this Sub-Processing Agreement." Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-043 (warning)** fires — its present_pattern is no longer matched anywhere in the document. Per-playbook variant of the controller-processor signature-block fixture; the rule's statutory anchor (GDPR Art. 28(9) writing requirement) applies identically across both DPA playbooks.
- `baa-subcontractor-missing-signature-block-fail.txt` — BA→Subcontractor BAA with the closing signature block replaced with a single recital paragraph: "This Subcontractor BAA is executed by the Parties through their respective duly empowered officers, with execution evidenced solely by reference to the cover page of the Master Services Agreement; no separate execution lines, witness lines, or attestation blocks are included in this Subcontractor BAA." Every other clause (including "Effective Date: January 1, 2026.") is preserved so the rest of the BAA-001..045 baseline still passes where it was passing. **BAA-036 (warning)** fires — its present_pattern is no longer matched anywhere in the document. Per-playbook variant of the primary BAA signature-block fixture; the rule's statutory anchor (45 CFR § 164.504(e)(5) satisfactory-assurances writing requirement) applies identically across the primary BAA and the subcontractor BAA.
- `unilateral-nda-deep-missing-no-license-clause-fail.txt` — Unilateral NDA-Deep with Section 4 ("No License") rewritten as "4. Scope of Use. Nothing in this Agreement shall be deemed to confer to the Receiving Party any interest, right, title, or other entitlement in or to the Disclosing Party's Confidential Information beyond the limited authorization to use such information solely for the Permitted Purpose and in accordance with the terms of this Agreement." The "No License" header is removed entirely so the regex `no\s+license` no longer matches anywhere in the document; the alternative pattern `(does\s+not\s+(grant|convey|transfer)|shall\s+not\s+be\s+construed.{0,40}license)` is also not matched. Every other clause is preserved so the rest of the NDA-D-001..025 baseline still passes where it was passing. **NDA-D-021** fires — its present_patterns are no longer matched anywhere in the document. Per-playbook variant of the mutual-NDA no-license fixture; without a one-line denial of license, disclosure could be construed to grant an implied license in the Confidential Information.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`dpa-processor-subprocessor` / `baa-subcontractor` / `unilateral-nda-deep`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-fifty-nine fail-fixtures (was one-hundred-and-fifty-six); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 159. `npm run lint && typecheck && test && build` all green; **1645/1645 tests + 2 skips**.

### v3 fail-fixture corpus expansion (153 → 156) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-fifty-three end-to-end fail-fixtures to one-hundred-and-fifty-six by broadening across three more playbooks (BAA, mutual NDA-Deep, AI Addendum). None of the three load-bearing rules (BAA-036, NDA-D-023, ADDENDA-014) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-signature-block-fail.txt` — BAA with the closing signature block replaced with a single recital paragraph: "This BAA is executed by the Parties through their respective duly empowered officers, with execution evidenced solely by reference to the cover page of the Master Services Agreement; no separate execution lines, witness lines, or attestation blocks are included in this BAA." Every other clause (including "Effective Date: January 1, 2026.") is preserved so the rest of the BAA-001..045 baseline still passes where it was passing. **BAA-036 (warning)** fires — its present_pattern `By:\s*[_\-\s]+|signature\s+block|authorized\s+(signatory|representative)|sign(ed)?\s+by` is no longer matched anywhere in the document. 45 CFR § 164.504(e)(5) requires the covered entity to obtain satisfactory assurances through a written contract — a signed instrument; a BAA that gestures at execution without a signature block leaves the named, authorised representative of each party unidentified.
- `mutual-nda-deep-missing-assignment-consent-fail.txt` — Mutual NDA-Deep with Section 3 (Permitted Purpose and Use Restriction) rewritten so the only anchor that previously satisfied NDA-D-023 ("third party without the prior written consent of the Disclosing Party") is replaced with "third party absent the Disclosing Party's prior written approval"; "without" → "absent" and "consent" → "approval" so neither `successors\s+and\s+assigns` nor `(may\s+not\s+assign|shall\s+not\s+assign|without.{0,40}consent)` is matched anywhere in the document. Every other clause is preserved so the rest of the NDA-D-001..025 baseline still passes where it was passing. **NDA-D-023 (warning)** fires — its present_patterns are no longer matched anywhere in the document. Without a consent-to-assignment / successors-and-assigns clause, an acquirer of the receiving party could inherit access to Confidential Information without the discloser's approval.
- `ai-addendum-missing-hallucination-and-human-review-fail.txt` — AI Addendum with every "human review" / "human reviewer" / "Human-in-the-Loop" anchor stripped: Section 1's "human review and revision" → "qualified-workforce review and revision"; Section 4's "whether human review was completed" → "whether qualified-workforce review was completed"; Section 5 retitled "Qualified-Workforce Checkpoint" (was "Human-in-the-Loop Checkpoint") and "qualified human member of Vendor's workforce" → "qualified workforce member of Vendor", "human reviewer" → "reviewer"; Section 10 "human-reviewer documentation" → "reviewer documentation". No "hallucinat" / "inaccurate output" / "not fit for" / "not (professional|legal|medical|financial) advice" anchor exists in the fixture either. Every other clause is preserved so the rest of the ADDENDA-010..016 baseline still passes where it was passing. **ADDENDA-014 (info)** fires — its present_patterns are no longer matched anywhere in the document. NIST AI RMF and practitioner posture both expect a hallucination-risk acknowledgment and a human-review obligation for high-stakes outputs; an AI addendum that gestures at "qualified-workforce review" without flagging hallucination risk or naming human oversight leaves customers without the canonical AI-risk anchor.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`baa` / `mutual-nda-deep` / `ai-addendum`). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-fifty-six fail-fixtures (was one-hundred-and-fifty-three); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 156. `npm run lint && typecheck && test && build` all green; **1636/1636 tests + 2 skips**.

### v3 fail-fixture corpus expansion (150 → 153) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-fifty end-to-end fail-fixtures to one-hundred-and-fifty-three by broadening to two non-GDPR playbooks for the first time in this Post-4.0 batch sequence (multi-state US DPA and AI Addendum). None of the three load-bearing rules (USDPA-011, USDPA-023, ADDENDA-012) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-multi-state-us-missing-processing-instructions-fail.txt` — Multi-state US DPA with every "documented instructions" anchor (Sections 3, 5, and 6) rewritten as "express contractual directions" / "the express contractual directions Controller has set forth in the MSA and any annex thereto"; Section 5's "any instruction that constitutes a violation" becomes "any direction that constitutes a violation" so the "instructions"-family anchor disappears completely. Every other clause is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-011** fires — its present_pattern `(processing\s+instructions|instructions\s+for\s+processing|documented\s+instructions)` is no longer matched anywhere in the document. Every US state privacy statute (CCPA, VCDPA, CPA, CTDPA, UCPA) requires the processor / service-provider contract to set out the controller's processing instructions; a DPA that rephrases the anchor as "contractual directions" leaves a regulator and the controller without the canonical statutory hook.
- `dpa-multi-state-us-missing-consumer-rights-process-fail.txt` — Multi-state US DPA with the consumer-rights anchors in Section 8 rewritten so no "opt-out" / "consumer rights request" / "access request" / "deletion request" / "right to access|delete|opt-out|correct" pattern matches: section title becomes "Consumer Requests Assistance" (was "Consumer Rights Assistance"), enumerated rights become "review of personal information", "erasure of personal information", "correction of inaccurate personal information", "portability of personal information", and "the consumer's ability to decline sale or sharing"; Section 4 also rewrites "consumer rights requests" → "consumer requests". Every other clause is preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-023 (warning)** fires — its present_pattern is no longer matched anywhere in the document. State privacy statutes all confer consumer rights (access / deletion / correction / portability / opt-out of sale or sharing); a DPA that gestures at "consumer requests" generically without using the statutory anchor language leaves the operational handoff between controller and processor unanchored to the underlying right.
- `ai-addendum-missing-transparency-disclosures-fail.txt` — AI Addendum with Section 3 retitled "Affirmative Consent for AI Processing" (was "Opt-In for AI Processing"), "affirmatively opted in" rewritten as "affirmatively consented", and "Opt-out consent mechanisms" rewritten as "Negative-consent mechanisms" so no "opt-in" / "opt-out" / "on by default" / "enabled by default" anchor remains; no "AI features" / "AI functions" / "generative features" anchor exists in the fixture; and the fixture's "third-party foundational model" does not match the strict `third[- ]party\s+(?:provider|model|host)` shape. Every other clause is preserved so the rest of the ADDENDA-010..016 baseline still passes where it was passing. **ADDENDA-012 (warning)** fires — its present_patterns are no longer matched anywhere in the document. EU AI Act Article 50 + FTC consumer-protection posture both require clear disclosure of which features use AI, whether on by default or opt-in, and whether the model is on-prem or third-party-hosted; a vendor AI addendum that says "Affirmative Consent" without naming the features or the hosting posture leaves customers unable to audit the AI use surface.

Each fail-fixture has a `.playbook` sidecar pinning it to its expected playbook (`dpa-multi-state-us` / `ai-addendum`) so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-fifty-three fail-fixtures (was one-hundred-and-fifty); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 153. `npm run lint && typecheck && test && build` all green; **1627/1627 tests + 2 skips**.

### v3 fail-fixture corpus expansion (147 → 150) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-forty-seven end-to-end fail-fixtures to one-hundred-and-fifty by adding three new fixtures spanning three more DPA-GDPR posture / structural failure modes (Chapter V mechanism named but EU SCCs not incorporated; no liability-allocation anchor; no term/termination anchor). None of the three load-bearing rules (DPA-033, DPA-047, DPA-045) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-eu-sccs-incorporation-fail.txt` — Controller→processor GDPR DPA with Section 13 retitled "International Transfers Under Chapter V" and rewritten so the section preserves the Chapter V naming (so DPA-032 stays satisfied) — keeping "international transfer", "Chapter V", "adequacy decision", and "binding corporate rules" anchors — but no longer names the EU SCCs by reference, Commission Implementing Decision (EU) 2021/914, or Module Two. Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-033 (warning)** fires — its present_pattern `(2021\/914|standard\s+contractual\s+clauses|EU\s+SCCs?)` is no longer matched anywhere in the document. Commission Implementing Decision (EU) 2021/914 requires the parties to use the SCC template for in-scope transfers; a DPA that gestures at Chapter V without naming the SCC mechanism leaves the parties without the canonical transfer scaffold the Commission designated for controller-to-processor flows.
- `dpa-controller-processor-missing-liability-allocation-fail.txt` — Controller→processor GDPR DPA with the preamble's "limited liability company" rewritten as "limited company" (so no "liability" anchor remains incidentally) and Section 7's "shall remain fully liable to Controller" rewritten as "shall remain fully responsible to Controller". Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-047 (warning)** fires — its present_pattern `(liability|indemnif|article\s*82)` is no longer matched anywhere in the document. GDPR Art. 82 makes both Controller and Processor potentially liable to data subjects; DPAs are expected to allocate that liability between the parties (an indemnity, a contribution scheme, or an Article 82 reference) so the contractual residue is well defined.
- `dpa-controller-processor-missing-term-and-termination-fail.txt` — Controller→processor GDPR DPA with every "termination" / "terminate" anchor stripped: Section 1's "Processing shall cease upon termination of the MSA" → "upon expiry or wind-down of the MSA"; "duration of the processing corresponds to the term of the MSA" → "during which the MSA is in force"; Section 12's "Upon termination of the MSA" → "Upon expiry or wind-down of the MSA"; "thirty (30) days of termination" → "thirty (30) days following expiry or wind-down of the MSA". Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-045 (warning)** fires — its present_pattern `(term\s+of\s+(this\s+)?agreement|initial\s+term|termination|terminate)` is no longer matched anywhere in the document. GDPR Art. 28(3)(g) anchors the end-of-services duties on a defined term and termination event; a DPA that never says "term" or "termination" makes the temporal scope of every post-termination duty (deletion, return, audit cooperation, survival) ambiguous.

Each fail-fixture has a `.playbook` sidecar pinning it to the `dpa-controller-processor` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-fifty fail-fixtures (was one-hundred-and-forty-seven); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 150. `npm run lint && typecheck && test && build` all green; **1618/1618 tests + 2 skips**.

### v3 fail-fixture corpus expansion (144 → 147) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-forty-four end-to-end fail-fixtures to one-hundred-and-forty-seven by adding three new fixtures spanning three distinct DPA-GDPR failure modes (missing Art. 28(9) form-of-agreement anchor, missing Chapter V transfer mechanism naming, missing Art. 28(3)(g) deletion-or-return clause on the controller-processor playbook). DPA-018 and DPA-032 had no end-to-end fail-fixture before this batch; DPA-013 had a `dpa-processor-subprocessor` end-to-end fixture but no controller-processor variant — the load-bearing playbook for the rule. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-form-of-agreement-fail.txt` — Controller→processor GDPR DPA with the two "written contract" / "in writing" anchors stripped: Section 7's "by way of a written contract" is rewritten as "via a duly executed contractual instrument" and Section 12's "shall certify such deletion in writing" is rewritten as "shall certify such deletion by formal notice to Controller". Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-018 (warning)** fires — its present_pattern `(in\s+writing|electronic\s+form|written\s+(contract|agreement))` is no longer matched anywhere in the document. GDPR Art. 28(9) requires the contract to be in writing, including in electronic form; a DPA that never says so on its face leaves the form requirement ambiguous and removes the canonical statutory anchor that supervisory authorities and counter-parties look to when establishing the contract's compliance with Art. 28(9).
- `dpa-controller-processor-missing-transfer-mechanism-fail.txt` — Controller→processor GDPR DPA with Section 13 rewritten from "Cross-Border Transfers and EU SCCs" (which named the EU SCCs under Commission Implementing Decision (EU) 2021/914, Module Two) to a generic "Cross-Border Movement of Personal Data" paragraph that complies with applicable law and supplemental documentation but does not name any Chapter V mechanism (no "international transfer" / "Chapter V" / "Standard Contractual Clauses" / "SCCs" / "adequacy decision" / "Binding Corporate Rules" / "BCRs"). Section 4's "transfers of personal data to a third country or an international organization" is rewritten to "onward movement of personal data to a third country or an international organization" to avoid the "international transfer" anchor. Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-032** fires — its present_pattern is no longer matched anywhere in the document. (DPA-033 also fires because the SCC-incorporation anchor is gone, but DPA-032 is the load-bearing rule for this fixture; DPA-033 has its own negative-path coverage at the ruleset-test level.) GDPR Arts. 44–49 require an adequacy decision, SCCs, BCRs, or a derogation for transfers to third countries — generic "comply with applicable law" prose collapses the Chapter V mechanism into a black box and removes the named anchor regulators rely on to assess transfer lawfulness.
- `dpa-controller-processor-missing-deletion-or-return-fail.txt` — Controller→processor GDPR DPA with Section 12 ("Deletion or Return at End of Services") rewritten as "Records at End of Services. Upon termination of the MSA for any reason, Processor shall, at the choice of Controller, provide Controller with an inventory of personal data still held by Processor and the basis on which it is retained, unless applicable law requires retention of the personal data. Processor shall certify the accuracy of such inventory in writing within thirty (30) days of termination." The "delete or return" / "return or delete" anchors are eliminated, and "in writing" is preserved so DPA-018 still passes. Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-013 (critical)** fires — its present_pattern `(delete\s+or\s+return|return\s+or\s+delete).*?(personal\s+data|end\s+of\s+(?:the\s+)?(?:provision\s+of\s+)?services)` is no longer matched anywhere in the document. GDPR Art. 28(3)(g) requires the processor, at the choice of the controller, to delete or return all personal data to the controller after the end of the provision of services — an "inventory and basis for retention" substitute leaves the personal data on the processor's systems and inverts the statutory default.

Each fail-fixture has a `.playbook` sidecar pinning it to the `dpa-controller-processor` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-forty-seven fail-fixtures (was one-hundred-and-forty-four); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 147. `npm run lint && typecheck && test && build` all green; **1609/1609 tests + 2 skips**.

### v3 fail-fixture corpus expansion (141 → 144) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-forty-one end-to-end fail-fixtures to one-hundred-and-forty-four by adding three new fixtures spanning three distinct DPA-GDPR failure modes (hand-waving security language, missing personal-data scope annex, missing signature block) — none of the three load-bearing rules (DPA-023, DPA-038, DPA-043) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-hand-waving-security-language-fail.txt` — Controller→processor GDPR DPA with Section 6 (Security Measures (Article 32 GDPR)) augmented with an injected closing sentence: "To the extent any of the foregoing cannot be operationally implemented, Processor shall instead apply commercially reasonable security measures consistent with prevailing market practice for service providers of similar size and scope." Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-023 (warning)** fires — its first bad_pattern matches `commercially\s+reasonable\s+(security|measures)` end-to-end. GDPR Art. 32 requires measures appropriate to the risk; "commercially reasonable" or "industry-standard" language without a specific Annex of technical and organisational measures is the hand-waving the regulator has criticised, because it shifts the substantive duty from a defined set of controls to whatever the processor's market peers happen to be doing.
- `dpa-controller-processor-missing-personal-data-annex-fail.txt` — Controller→processor GDPR DPA with Section 2 (Categories of Personal Data and Data Subjects) rewritten to reference "the Categories of Personal Data Section" at the end of the DPA instead of "Annex I"; the Annex I heading at the end of the DPA is likewise renamed to "Categories of Personal Data" with no "Annex I" / "Appendix I" / "Schedule I" / "Exhibit I" prefix. Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-038 (warning)** fires — its present_pattern `(annex|appendix|exhibit|schedule)\s+(I|1|A)` is no longer matched anywhere in the document. SCC Annex I (and the modern DPA convention of an Annex describing the parties, transfer scope, and processing operations) anchors the description of the personal-data scope; without a labelled annex/appendix the document loses the canonical attachment that supervisory authorities and downstream subprocessors look to when scoping the processing.
- `dpa-controller-processor-missing-signature-block-fail.txt` — Controller→processor GDPR DPA with the entire signature block (party headers + "Signed by:" lines + "Title:" lines including "Authorized Representative" + "Date:" lines) replaced with a single recital paragraph: "This DPA is executed on behalf of Globex Inc. (Controller) and Wayne Enterprises LLC (Processor) through their respective representatives, with execution evidenced solely by reference to the MSA cover page; no separate execution lines, witness lines, or attestation blocks are included in this DPA." Every other clause (including "Effective Date: February 1, 2026.") is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-043 (warning)** fires — its present_pattern `By:\s*[_\-\s]+|signature\s+block|authori[sz]ed\s+(signatory|representative)|sign(ed)?\s+by` is no longer matched anywhere in the document. GDPR Art. 28(9) requires the DPA to be in writing including in electronic form; a signature block — even on an electronic agreement — is the canonical evidence-of-execution artefact that supervisory authorities and counter-parties look to when establishing that the obligations attached to a named, authorised representative of each party.

Each fail-fixture has a `.playbook` sidecar pinning it to the `dpa-controller-processor` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-forty-four fail-fixtures (was one-hundred-and-forty-one); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 144. `npm run lint && typecheck && test && build` all green; **1600/1600 tests + 2 skips**.

### v3 fail-fixture corpus expansion (138 → 141) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-thirty-eight end-to-end fail-fixtures to one-hundred-and-forty-one by adding three new fixtures covering three GDPR-DPA vendor-overreach language-rule failure modes (unilateral deviation from instructions, controller indemnifies processor for GDPR fines, audit cost allocated entirely to controller) — none of the three load-bearing rules (DPA-037, DPA-048, DPA-049) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-instructions-deviation-fail.txt` — Controller→processor GDPR DPA with Section 4 (Documented Instructions Only) augmented with an injected sentence: "Notwithstanding the foregoing, Processor may deviate from Controller's instructions where, in Processor's reasonable judgment, such deviation is necessary or appropriate to operate the Services, and Processor shall not be required to seek prior approval from Controller before doing so." Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-037 (warning)** fires — its first bad_pattern matches `processor\s+may\s+(deviate|depart)\s+from` end-to-end. GDPR Art. 28(3)(a) requires processing only on documented instructions from the controller; any deviation must be required by law and the processor must inform the controller — a unilateral "Processor may deviate" license collapses the statutory accountability chain and removes the controller's ability to direct the processing.
- `dpa-controller-processor-controller-indemnifies-gdpr-fines-fail.txt` — Controller→processor GDPR DPA with a new Section 14 ("Indemnification") appended: "Controller shall indemnify, defend, and hold harmless Processor and its officers, directors, and employees from and against any GDPR fines, penalties, administrative sanctions, supervisory-authority claims, and related defense costs arising out of or relating to the processing of personal data under this DPA, regardless of the underlying cause or the party at fault." Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-048 (warning)** fires — its first bad_pattern matches `controller\s+(shall|will)\s+indemnif.*?(processor|GDPR|fine)` end-to-end (both "Processor" and "GDPR" / "fine" anchors are within the matched span). Under GDPR Art. 82, a processor remains liable for its own infringements; shifting that liability to the controller through an unconditional indemnity is vendor overreach and may be unenforceable to the extent it purports to absolve the processor of supervisory-authority sanctions attributable to its own conduct.
- `dpa-controller-processor-audit-cost-controller-exclusive-fail.txt` — Controller→processor GDPR DPA with Section 11 (Audit and Inspection Rights) augmented with an injected closing sentence: "Controller shall bear all costs of any audit conducted under this Section, including any audit that uncovers a material breach by Processor." Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-049 (warning)** fires — its first bad_pattern matches `controller\s+(shall|will)\s+bear\s+(all|the\s+entire|the\s+full)\s+costs?\s+of\s+(any\s+)?audit` end-to-end. Standard practice under GDPR Art. 28(3)(h) splits routine-audit cost to the controller but allocates cost to the processor where the audit uncovers material breach; a clause that fixes the entire audit cost on the controller — including breach-confirmed audits — discourages the controller from ever exercising the audit right and effectively immunizes processor non-compliance.

Each fail-fixture has a `.playbook` sidecar pinning it to the `dpa-controller-processor` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-forty-one fail-fixtures (was one-hundred-and-thirty-eight); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 141. `npm run lint && typecheck && test && build` all green; **1591/1591 tests + 2 skips**.

### v3 fail-fixture corpus expansion (135 → 138) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-thirty-five end-to-end fail-fixtures to one-hundred-and-thirty-eight by adding three new fixtures spanning three more GDPR-DPA failure modes (breach-notice content elements, breach-notice timing too loose, SOC 2 substitution eliminating audit) — none of the three load-bearing rules (DPA-026, DPA-027, DPA-036) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-breach-content-elements-fail.txt` — Controller→processor GDPR DPA with Section 2's heading rewritten from "Categories of Personal Data and Data Subjects" to "Categories of Personal Data and Types of Data Subjects" and the body's "The categories of data subjects are..." line rewritten to "The types of data subjects are..."; the rest of the DPA is preserved so the DPA-001..030 baseline still passes where it was passing, and no remaining text uses `nature\s+of\s+the\s+(personal\s+data\s+)?breach`, `categor(?:y|ies)\s+of\s+data\s+subjects`, `likely\s+consequences`, or `measures\s+taken`. **DPA-026 (warning)** fires — GDPR Art. 33(3) requires the breach notification to describe the nature of the breach, the categories and approximate number of data subjects affected, the DPO contact, the likely consequences, and the measures taken; a DPA whose Section 10 recites only that Processor will provide "sufficient information" without anchoring the Art. 33(3) content list leaves the controller without a contractual hook to demand the statutorily required elements when the BA hands over its intake report.
- `dpa-controller-processor-breach-notice-30-day-window-fail.txt` — Controller→processor GDPR DPA with Section 10 (Breach Notification) rewritten to a single sentence: "Processor shall notify Controller of any personal data breach no later than 30 days after becoming aware of the breach...". The 48-hour anchor is dropped in favor of a 30-day window; every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-027 (warning)** fires — its first bad_pattern matches `\b(within|no\s+later\s+than)\s+(?:1[0-9]|2[0-9]|3[0-9]|[4-9][0-9]|[1-9][0-9]{2,})\s+days?\b.{0,80}(breach|notif)` end-to-end ("no later than 30 days after becoming aware of the breach"). Although GDPR Art. 33(2) does not set a strict outer bound for processor notification, supervisory guidance treats anything beyond ~24–72 hours as suspect; a 30-day processor-notification window leaves the controller no margin to meet its own Art. 33 / 34 obligations and effectively shifts the supervisory-notification clock entirely onto the controller.
- `dpa-controller-processor-soc2-eliminates-audit-fail.txt` — Controller→processor GDPR DPA with Section 11 (Audit and Inspection Rights) rewritten to keep the first information-production sentence but replace the "allow for and contribute to audits, including inspections" clause with "Processor's annual SOC 2 Type II report shall satisfy Controller's audit rights under this DPA in lieu of any on-site inspection, and the SOC 2 Type II report shall be the sole means of verifying Processor's compliance with this Section." Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-036 (warning)** fires — its first bad_pattern matches `(SOC\s*2|ISO\s*27001).{0,160}(in\s+lieu\s+of|shall\s+(satisfy|fulfill|fulfil)|the\s+sole\s+means)` (both `shall satisfy` and `in lieu of` / `sole means` anchors are present) end-to-end. GDPR Art. 28(3)(h) requires the processor to allow for and contribute to audits; SOC 2 / ISO substitution is permitted as a default, but it must not eliminate the controller's right to an on-site audit on reasonable cause, and a contract that designates SOC 2 as the "sole means" of verification breaches the statutory floor.

Each fail-fixture has a `.playbook` sidecar pinning it to the `dpa-controller-processor` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-thirty-eight fail-fixtures (was one-hundred-and-thirty-five); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 138. `npm run lint && typecheck && test && build` all green; **1582/1582 tests + 2 skips**.

### v3 fail-fixture corpus expansion (132 → 135) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-thirty-two end-to-end fail-fixtures to one-hundred-and-thirty-five by adding three new fixtures spanning three distinct GDPR-DPA failure modes (DPO reference, deletion-or-return choice misallocation, effective-date anchor) — none of the three load-bearing rules (DPA-031, DPA-035, DPA-044) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-dpo-reference-fail.txt` — Controller→processor GDPR DPA with the Controller-side signatory's "Title: Data Protection Officer" line rewritten to "Title: Privacy Lead"; every other clause is preserved so the rest of the DPA-001..030 / 032+ baseline still passes where it was passing, and no remaining text uses `data\s+protection\s+officer`, `\bDPO\b`, or `article\s*37`. **DPA-031 (warning)** fires — GDPR Article 37 requires the designation of a Data Protection Officer in certain cases (public authorities, large-scale systematic monitoring, special-category processing); modern DPAs reference the DPO contact details so the controller-side accountability under Art. 38–39 attaches to a named office. A DPA silent on the DPO leaves the Art. 37/38/39 accountability chain unsigned and the data-subject notification path ambiguous.
- `dpa-controller-processor-deletion-choice-processor-fail.txt` — Controller→processor GDPR DPA with Section 12 (Deletion or Return at End of Services) rewritten from "Processor shall, at the choice of Controller, delete or return..." to "Processor may choose to delete or return all personal data to Controller and shall delete existing copies...". The reversal moves the deletion-or-return choice from the controller to the processor. Every other clause is preserved so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-035 (critical)** fires — its first bad_pattern matches `processor\s+(shall|may)\s+(choose|elect)\s+to\s+(delete|return)` end-to-end. GDPR Art. 28(3)(g) explicitly assigns the deletion-or-return election to the controller; vendor overreach commonly inverts this, and the rule surfaces the inversion as a load-bearing critical because the controller loses its contractual ability to compel the post-termination remediation posture that fits its own retention obligations.
- `dpa-controller-processor-missing-effective-date-fail.txt` — Controller→processor GDPR DPA with the "Effective Date: February 1, 2026." line removed from the signature block and both inline "as of February 1, 2026," / "dated January 1, 2026" anchors stripped from the preamble; every other clause is preserved (including the Term-style "duration of the processing corresponds to the term of the MSA" recital in §1) so the rest of the DPA-001..030 baseline still passes where it was passing. **DPA-044 (warning)** fires — its present_patterns require `effective\s+date`; with the anchor gone, no remaining text contains the phrase. An effective date anchors the timing rules in the DPA (subject-matter window, breach-notice clock, deletion-or-return 30-day timer); without it, the Art. 28(3) intro requirement that the DPA define the duration of processing is undischarged on the face of the document.

Each fail-fixture has a `.playbook` sidecar pinning it to the `dpa-controller-processor` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-thirty-five fail-fixtures (was one-hundred-and-thirty-two); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 135. `npm run lint && typecheck && test && build` all green; **1573/1573 tests + 2 skips**.

### v3 fail-fixture corpus expansion (129 → 132) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-twenty-nine end-to-end fail-fixtures to one-hundred-and-thirty-two by adding three new fixtures spanning three more BAA execution-and-vocabulary anchors (effective date, governing law, HIPAA-current definitions) — none of the three load-bearing rules (BAA-037, BAA-039, BAA-044) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-effective-date-fail.txt` — BAA with the "Effective Date: January 1, 2026." line removed from the signature block; every other clause is preserved (including the Term clause and the signature block's "Signed by / Title / Date" lines) so the rest of the BAA baseline still passes where it was passing. **BAA-037 (warning)** fires — its present_patterns require `effective\s+date`; with the line gone, no remaining text contains the phrase. An effective date anchors the timing rules in the BAA (term, breach windows, termination) — without it the 60-day breach-notification clock and the BAA's coterminous relationship with the underlying MSA are ambiguous on their face.
- `baa-missing-governing-law-fail.txt` — BAA with the "Governing Law" paragraph removed entirely; every other clause is preserved so the rest of the BAA baseline still passes where it was passing, and no other paragraph uses `governing\s+law`, `governed\s+by\s+the\s+laws`, or `laws\s+of\s+the\s+State\s+of`. **BAA-039 (warning)** fires — although HIPAA is federal and the operative substantive rules are preempted, BAAs typically include a governing-law clause to anchor non-HIPAA contract disputes (indemnification, notice mechanics, term, fee allocation). A BAA silent on governing law leaves those non-HIPAA matters subject to default conflict-of-laws analysis, which can produce a different jurisdiction than the parties expect.
- `baa-missing-hipaa-current-definitions-fail.txt` — BAA with the Reporting paragraph's "Unsecured PHI" downgraded to plain "PHI" and the lowercase "breach" tokens kept; the citation to 45 CFR § 164.410 is preserved (so the breach-reporting baseline survives) but no remaining text uses `unsecured\s+PHI`, `unsecured\s+protected\s+health\s+information`, `164\.402`, or `breach\s+(means|shall\s+have)`. Every other clause is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-044 (warning)** fires — post-HITECH BAAs should track the modern defined terms "Breach" (45 C.F.R. § 164.402) and "Unsecured PHI" (§ 164.402); a BAA that talks about "breaches of PHI" without anchoring the Unsecured-PHI / § 164.402 vocabulary leaves room for argument that pre-HITECH definitions govern, which materially narrows the covered entity's notification obligations under HITECH.

Each fail-fixture has a `.playbook` sidecar pinning it to the `baa` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-thirty-two fail-fixtures (was one-hundred-and-twenty-nine); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 132. `npm run lint && typecheck && test && build` all green; **1564/1564 tests + 2 skips**.

### v3 fail-fixture corpus expansion (126 → 129) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-twenty-six end-to-end fail-fixtures to one-hundred-and-twenty-nine by adding three new fixtures spanning three more BAA Security-Rule anchors (risk-assessment cadence, subprocessor disclosure list, choice-of-law preemption) — none of the three load-bearing rules (BAA-033, BAA-035, BAA-042) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-risk-assessment-fail.txt` — BAA with the "Risk Assessment" paragraph removed entirely; the Safeguards paragraph still cites 45 CFR §§ 164.308 / 164.310 / 164.312 but never uses the words "risk assessment" or "risk analysis," so no remaining text supplies the missing anchor. Every other clause is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-033 (warning)** fires — its present_patterns require `risk\s+(assessment|analysis)`; with the paragraph gone, neither phrase appears anywhere in the document. 45 C.F.R. § 164.308(a)(1)(ii)(A) requires a risk analysis as part of the Security Management Process; a BAA silent on the BA's risk-assessment cadence leaves the covered entity without a contractual hook to verify the BA's compliance with the foundational Security Rule control.
- `baa-missing-subprocessor-list-fail.txt` — BAA with the "Subcontractor List" paragraph removed entirely; the Subcontractor Flow-Down paragraph (which uses "subcontractor" / "agrees in writing" wording) is preserved so BAA-018 still passes, but no remaining text uses `list\s+of\s+subprocessors`, `list\s+of\s+subcontractors`, or `subprocessor.{0,60}(disclos|maintain.*list)`. Every other clause is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-035 (warning)** fires — although 45 C.F.R. § 164.504(e)(2)(ii)(D) speaks to flow-down, modern BAAs require disclosure of the subprocessor / subcontractor identity for covered-entity vetting; a BAA that does not maintain a current disclosed list leaves the covered entity unable to perform downstream vendor due diligence as part of its own Security Management Process.
- `baa-state-law-overrides-hipaa-fail.txt` — BAA with the "Governing Law" paragraph rewritten to subordinate HIPAA to state law: "This BAA shall be governed by the laws of the State of New York. Notwithstanding any provision of HIPAA or the HIPAA Rules to the contrary, in the event of any conflict between the terms of this BAA and any provision of New York state law, state law shall control...." Every other clause is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-042 (warning)** fires — its first bad_pattern matches `notwithstanding\s+(any\s+)?(provision\s+of\s+)?HIPAA` and the second matches `state\s+law\s+(shall\s+)?controls?`; both appear end-to-end in the rewritten clause. 45 C.F.R. § 160.203 preempts contrary state law unless an exception applies, so a "notwithstanding HIPAA" / "state law controls" recital is unenforceable to the extent of HIPAA conflict and signals the kind of careless drafting that produces operational ambiguity in a breach-response scenario.

Each fail-fixture has a `.playbook` sidecar pinning it to the `baa` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-twenty-nine fail-fixtures (was one-hundred-and-twenty-six); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 129. `npm run lint && typecheck && test && build` all green; **1555/1555 tests + 2 skips**.

### v3 fail-fixture corpus expansion (123 → 126) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-twenty-three end-to-end fail-fixtures to one-hundred-and-twenty-six by adding three new fixtures spanning three BAA Security-Management-Process anchors (mitigation, workforce training, encryption / NIST safeguards) — none of the three load-bearing rules (BAA-030, BAA-031, BAA-032) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-mitigation-obligation-fail.txt` — BAA with the "Mitigation" paragraph removed entirely; every other clause (Permitted Uses, Safeguards, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, Books and Records, Return or Destruction, Minimum Necessary, Workforce Training, Encryption, Risk Assessment, Sanctions, Subcontractor List, Governing Law, Notice, Term) is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-030 (critical)** fires — its present_patterns require `(mitigate|mitigation).*?(harm|effect|disclosure|use)`; with the paragraph gone, no remaining text pairs a mitigate verb with a harm/effect/disclosure/use object. 45 C.F.R. § 164.530(f) requires covered entities to mitigate harmful effects of any improper use or disclosure of PHI; the BA commonly inherits the duty by flow-down, and its absence leaves the covered entity with no contractual lever to compel mitigation when a BA leak surfaces downstream harm.
- `baa-missing-workforce-training-fail.txt` — BAA with the "Workforce Training" paragraph removed entirely; every other clause is preserved so the rest of the BAA baseline still passes where it was passing. **BAA-031 (warning)** fires — its present_patterns require `workforce\s+training`, `HIPAA\s+training`, or `trained\s+on.{0,40}(HIPAA|PHI)`; with the paragraph gone, no remaining text matches any of the three anchors. 45 C.F.R. §§ 164.530(b) and 164.308(a)(5) require workforce training on HIPAA / PHI handling; BAs are expected to flow the obligation down to their personnel, and a contract silent on training leaves the workforce-training pillar of the Security Management Process unanchored at the BA layer.
- `baa-missing-encryption-nist-fail.txt` — BAA with the "Encryption" paragraph removed entirely; every other clause is preserved so the rest of the BAA baseline still passes where it was passing, and no other paragraph mentions encryption, NIST 800, or FIPS 140 (the Safeguards paragraph names §§ 164.308 / 164.310 / 164.312 but does not invoke an encryption / NIST / FIPS anchor). **BAA-032 (warning)** fires — its present_patterns require `encrypt(ion)?`, `NIST\s*800`, or `FIPS\s*140`; with the paragraph gone, none match. Encryption is an addressable specification under 45 C.F.R. § 164.312(a)(2)(iv) / (e)(2)(ii) and the HITECH "safe harbor" for breach notification under 42 U.S.C. § 17932(h); a BAA that does not anchor encryption or an equivalent NIST 800-53 / 800-66 standard leaves the BA without the safe-harbor lever and pushes every incident onto the breach-notification timeline.

Each fail-fixture has a `.playbook` sidecar pinning it to the `baa` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-twenty-six fail-fixtures (was one-hundred-and-twenty-three); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 126. `npm run lint && typecheck && test && build` all green; **1546/1546 tests + 2 skips**.

### v3 fail-fixture corpus expansion (120 → 123) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-twenty end-to-end fail-fixtures to one-hundred-and-twenty-three by adding three new fixtures spanning three distinct v3 rule families (BAA minimum-necessary, US-state CCPA no-combining restriction, SCC Module 2 onward-transfer terms) — none of the three load-bearing rules (BAA-029, USDPA-004, TRANSFER-020) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-minimum-necessary-fail.txt` — BAA with the "Minimum Necessary" paragraph removed entirely; every other clause (Permitted Uses, Safeguards, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, Books and Records, Return or Destruction, Mitigation, Workforce Training, Encryption, Risk Assessment, Sanctions, Subcontractor List, Governing Law, Notice, Term) is preserved so BAA-001..028 / 030+ baseline still passes where it was passing. **BAA-029 (warning)** fires — its present_patterns require `minimum\s+necessary` or `164\.502\(b\)`; neither match, so the rule reports the 45 C.F.R. § 164.502(b) minimum-necessary anchor missing. Without the recital the BA has no contractual hook to limit downstream PHI requests, breaking the use/disclosure narrowing the Privacy Rule contemplates.
- `dpa-ccpa-service-provider-missing-no-combining-restriction-fail.txt` — CCPA Service-Provider Addendum with §3(d) (the no-combining-with-other-data restriction) removed and the §3 enumeration ending at (c); every other anchor for the combining prohibition — `combin\w+\s+(personal\s+information|the\s+personal\s+data)`, `7050\(c\)`, or `other\s+sources` — is absent from the rest of the document so no other paragraph supplies the missing recital. Sections 4 (Certification), 5 (Consumer Rights Assistance), 6 (Subprocessors), 7 (Security), 8 (Deletion or Return), and 9 (Audit) are preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-004 (warning)** fires — Cal. Civ. Code § 1798.140(ag)(1)(D) requires the service-provider contract to prohibit combining the business's Personal Information with data the service provider receives from or collects in connection with any other person, business, or direct consumer interaction (except as 11 C.C.R. § 7050(c) narrowly permits); a contract silent on combining leaves the business exposed to enforcement risk under § 1798.155 if the service provider augments the data with off-platform signals.
- `scc-module-2-missing-onward-transfer-terms-fail.txt` — SCC Module 2 with §8.6 (Onward Transfers) removed entirely and the subsequent §8.7 ("Processing Under Authority of Data Exporter") renumbered to §8.6; every anchor for the onward-transfer recital — `onward\s+transfer`, `clause\s+8\.7`, or `clause\s+8\.8` — is absent from the rest of the document so no other paragraph supplies the missing reference. Clauses 8.1 through 8.5, 9, 10, 11, 12, 14, 15, 16, the docking clause, and the three annexes are preserved so the rest of the TRANSFER-001..019 baseline still passes where it was passing. **TRANSFER-020 (warning)** fires — EU SCCs Clause 8.7 / 8.8 governs onward transfers of personal data to third parties outside the EEA; an SCC Module 2 transfer arrangement that is silent on onward-transfer terms leaves the data exporter without a contractual mechanism to ensure that downstream recipients are bound by equivalent safeguards, defeating the Article 46 Schrems II protection chain.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-twenty-three fail-fixtures (was one-hundred-and-twenty); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 123. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (117 → 120) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-seventeen end-to-end fail-fixtures to one-hundred-and-twenty by adding three new fixtures spanning three distinct v3 rule families (NDA-deep purpose-narrow framing, US-state CCPA consumer-rights assistance, Texas anti-indemnity) — none of the three load-bearing rules (NDA-D-012, USDPA-008, MSA-029) had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-missing-purpose-narrow-framing-fail.txt` — Mutual NDA with §3 (Use Restriction) and §4 (Obligations of Receiving Party) rewritten so the "solely for the Permitted Purpose" framing is replaced with open-ended "in connection with the Engagement" language, and the recital "Permitted Purpose" definition is dropped in favor of a vague "matter described in the parties' written correspondence." Every NDA-D-001..002 DTSA component, the 3-year confidentiality term, trade-secret perpetuity, return-or-destruction certificate, Delaware governing-law clause, and mutual symmetry are preserved so the rest of the NDA-deep baseline still passes. **NDA-D-012 (warning)** fires — its present_patterns require either `solely\s+(for|to)\s+(the\s+)?purpose`, `to\s+(evaluate|assess|consider)\s+(a\s+)?(potential|the)\s+(business|transaction)`, or `\bpurpose\b.{0,40}(means|defined)`; none match, so the narrow-purpose framing is reported missing. Without a defined Purpose the discloser loses the contractual hook to object when the receiver redirects Confidential Information to unrelated downstream use.
- `dpa-ccpa-service-provider-missing-consumer-rights-assistance-fail.txt` — CCPA Service-Provider Addendum with §5 "Consumer Rights Assistance" removed entirely; every other anchor (verifiable consumer requests, consumer-rights wording, assist-the-business language) is absent from the rest of the document so no other paragraph supplies the missing recital. Sections 4 (Certification), 6 (Subprocessors), 7 (Security), 8 (Deletion or Return), and 9 (Audit) are preserved so the rest of the USDPA-001..025 baseline still passes where it was passing. **USDPA-008 (critical)** fires — 11 C.C.R. § 7051(a)(8) requires the service-provider contract to require the service provider to enable the business to comply with verifiable consumer requests under the CCPA; a contract silent on consumer-rights assistance leaves the business with no contractual lever to fulfill consumer-rights timelines and exposes it to enforcement under § 1798.155 / § 1798.199.55.
- `msa-vendor-deep-tx-construction-indemnity-fail.txt` — Vendor MSA with Section 11 governing-law switched from Delaware to Texas and a new Section 13 "Construction Project Indemnity" whose opening sentence pairs "this Agreement is governed by the laws of the State of Texas" with "Vendor shall indemnify and hold harmless Customer for any and all claims and losses arising out of or related to such work, including those resulting from the negligence of the indemnitee" in a single sentence. The Texas anchor and the indemnity-for-indemnitee-negligence sit in the same sentence so the intra-sentence `[^.]{0,400}` window in MSA-029's bad_pattern captures the pairing. Section 7 mutual indemnification, Section 8 cap-with-carveouts, and the other MSA-deep clauses are preserved so the rest of the baseline still passes. **MSA-029 (warning)** fires — Tex. Bus. & Com. Code Ch. 151 § 151.102 voids construction-contract indemnities that cover the indemnitee's own negligence (with narrow insurance-policy exceptions); the rule surfaces the void-on-its-face exposure that the Texas-law clause inherits. MSA-010 (NY anti-indemnity) also fires because its construction-indemnity pattern is jurisdiction-neutral, but MSA-029 is the load-bearing detector for the Texas failure mode.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-twenty fail-fixtures (was one-hundred-and-seventeen); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 120. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (78 → 81) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from seventy-eight end-to-end fail-fixtures to eighty-one by adding three new fixtures spanning three previously uncovered MSA-deep failure modes (bankruptcy/insolvency termination, hosted-service wind-down, assignment silent on change-of-control) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `msa-vendor-deep-missing-bankruptcy-termination-fail.txt` — Vendor MSA with Section 11's bankruptcy-termination subsection removed; every `bankruptc\w+` / `insolven\w+` / `receiver` / `assignment for the benefit of creditors` anchor is stripped while material-breach (30-day cure) and convenience (60-day notice) termination survive, so MSA-018 still passes. **MSA-019 (info)** fires — 11 U.S.C. § 365 makes pure ipso-facto clauses unenforceable in some scenarios, but the contract should still anchor the parties' intent that a bankruptcy filing, insolvency, receivership, or assignment for the benefit of creditors is a termination event.
- `msa-vendor-deep-missing-wind-down-fail.txt` — Vendor MSA with Section 14 ("Transition Assistance") removed entirely; every `wind[- ]down` / `transition (assistance|services|period)` / `continued access` / `post[- ]termination (access|services)` anchor is stripped while the bankruptcy- and material-breach-termination subsections and the data-return clause are preserved so MSA-019 / MSA-018 / MSA-021 still pass. **MSA-020 (info)** fires — without a wind-down period the customer can be cut off mid-migration; the commercial baseline is up to N (e.g., 90) days of post-termination service continuation at then-current rates to facilitate transition.
- `msa-vendor-deep-assignment-silent-change-of-control-fail.txt` — Vendor MSA with a new Section 15 ("Assignment") that bars assignment without prior written consent but is silent on change-of-control / merger / acquisition. **MSA-023 (info)** fires — its bad_patterns catch `(neither party may assign|no assignment)` with a negative lookahead for `change of control` / `merger` / `acquisition` within the next 200 chars; without a change-of-control hook an acquirer can effectively step into the contract without the counterparty's consent.

Each fail-fixture has a `.playbook` sidecar pinning it to the `msa-vendor-deep` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins eighty-one fail-fixtures (was seventy-eight); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 81. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (81 → 84) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from eighty-one end-to-end fail-fixtures to eighty-four by adding three new fixtures spanning three distinct v3 rule families (NDA-deep, MSA-deep, US-state DPA) — each load-bearing rule (NDA-D-010, MSA-012, USDPA-010) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-residuals-clause-fail.txt` — Mutual NDA with an added Section 8 "Residuals" clause permitting each Party to use information "retained in the unaided memory" of its personnel "for any purpose" and disclaiming any royalty obligation. **NDA-D-010 (info)** fires — its bad_patterns flag the bare `\bresiduals?\b` anchor and the `retained in.{0,40}(unaided )?memory` phrasing. The rule does not assert wrongness; it surfaces the carve-out so the discloser can make a deliberate choice about whether to keep it, because a residuals clause materially weakens NDA protection for the disclosing party by allowing the receiver's personnel to keep using general knowledge after the relationship ends.
- `msa-vendor-deep-feedback-license-unbounded-fail.txt` — Vendor MSA with an added Section 5 "Feedback License" granting Vendor a "perpetual, irrevocable, royalty-free, worldwide" license to use Customer feedback "for any purpose, without any restriction whatsoever." **MSA-012 (info)** fires — its bad_patterns catch `feedback` within 80 chars of `perpetual|irrevocable|royalty-free|worldwide` within another 80 chars of `any purpose|without (any )?restriction`. A broad feedback grant can sweep in customer ideas the customer may want to commercialize separately; the practitioner fix is to limit the feedback license to the vendor's product improvement and add a scope cap.
- `dpa-multi-state-us-missing-subprocessor-flow-down-fail.txt` — Multi-state US DPA with Section 7 rewritten to drop every "equivalent obligations" / "same restrictions" anchor; the subprocessor engagement clause still requires a written contract (so USDPA-018 stays satisfied) but no longer flows the service-provider's CCPA restrictions down to the subprocessor. **USDPA-010 (warning)** fires — Cal. Code Regs. tit. 11, § 7051(b) requires the contract with subcontractors to include the same CCPA provisions; without that flow-down a subprocessor is operating off-contract from the business's point of view.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins eighty-four fail-fixtures (was eighty-one); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 84. `npm run lint && test && build` all green.

### v3 fail-fixture corpus expansion (84 → 87) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from eighty-four end-to-end fail-fixtures to eighty-seven by adding three new fixtures spanning three distinct v3 rule families (BAA, DPA-GDPR, Transfer/SCC) — each load-bearing rule (BAA-001, DPA-001, TRANSFER-009) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-permitted-uses-and-disclosures-fail.txt` — BAA with Section 1's "Permitted Uses and Disclosures" heading renamed to "Scope of PHI Handling" and every `(permitted|authorized) (uses|disclosures)` / `uses and disclosures of PHI` anchor stripped from the body — "permitted or required" phrasings are replaced with "required" throughout. The downstream safeguards / reporting / subcontractor-flow-down / access-amendment-accounting / books-records / return-or-destruction / minimum-necessary clauses are preserved so BAA-002..018 still pass. **BAA-001** fires — 45 C.F.R. § 164.504(e)(2)(i) requires the BAA to set out the permitted and required uses and disclosures of PHI by the business associate; without that clause the agreement does not satisfy HIPAA's core contracting standard.
- `dpa-controller-processor-missing-subject-matter-fail.txt` — Controller→processor GDPR DPA with Section 1's heading rewritten to "Services, Duration, Nature, and Purpose of Processing" and the body's `subject-matter of the processing` / `scope of processing` anchors replaced with "the services consist of." Duration / nature-and-purpose / categories-of-data / documented-instructions / Art. 32 / subprocessor / DSR / breach-notification / deletion-or-return clauses are preserved so DPA-002..016 still pass. **DPA-001** fires — GDPR Art. 28(3) introductory paragraph requires the controller-processor contract to set out the subject-matter of the processing; auditors and supervisory authorities use this anchor to scope review.
- `scc-module-2-missing-clause-16-fail.txt` — SCC Module 2 transfer with the "Clause 16 — Non-Compliance and Suspension or Termination" section removed; every `clause\s*16` / `non-compliance with (the )?clauses` anchor is stripped. A generic "Termination for Breach" paragraph stands in for the suspension/termination mechanic so the operative remedy isn't lost commercially. **TRANSFER-009 (warning)** fires — EU SCCs Clause 16 governs the parties' rights when the SCCs become untenable; without it neither party has the SCC-defined suspension or termination right that is supposed to be available when the importer can no longer comply.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins eighty-seven fail-fixtures (was eighty-four); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 87. `npm run lint && test && build` all green (1429/1429 passing).

### v3 fail-fixture corpus expansion (87 → 90) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from eighty-seven end-to-end fail-fixtures to ninety by adding three new fixtures spanning three distinct v3 rule families (BAA, DPA-GDPR, UK IDTA Addendum) — each load-bearing rule (BAA-002, DPA-002, TRANSFER-013) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-use-limitation-fail.txt` — BAA with Section 1's "Business Associate shall not use or disclose PHI other than as permitted or required by this BAA" sentence rewritten to a permissive "Business Associate may handle PHI in accordance with the requirements of this BAA and applicable law"; every `(not use or disclose|shall not use)` anchor is stripped. Downstream safeguards / reporting / subcontractor-flow-down / access-amendment-accounting / books-records / return-or-destruction clauses are preserved so BAA-003..018 still pass. **BAA-002** fires — 45 C.F.R. § 164.504(e)(2)(ii)(A) requires the contract to prohibit the BA from using or further disclosing PHI other than as permitted by the contract or as required by law; a permissive "may handle" phrasing does not satisfy that prohibition.
- `dpa-controller-processor-missing-duration-of-processing-fail.txt` — Controller→processor GDPR DPA with Section 1's heading rewritten to "Subject-Matter, Nature, and Purpose of Processing" and every `duration of (the )?processing` anchor removed from the body. The subject-matter / nature-and-purpose / categories-of-data / documented-instructions / Art. 32 / subprocessor / DSR / breach-notification / deletion-or-return clauses remain so DPA-001 and DPA-003..016 still pass. **DPA-002** fires — GDPR Art. 28(3) introductory paragraph requires the duration of the processing to be set out, so the controller and processor know when the processing relationship (and the data-protection obligations bundled with it) end.
- `uk-idta-addendum-missing-table-3-fail.txt` — UK IDTA Addendum with the "Table 3 — Appendix Information" section replaced by a generic "Annex Incorporation by Reference" paragraph; every `Table\s*3\s*[—:-]\s*Appendix\s+Information` / `table\s*3\b.{0,40}appendix` anchor is removed. Tables 1, 2, and 4 are preserved so TRANSFER-011 / 012 / 014 / 016 still pass. **TRANSFER-013 (warning)** fires — the ICO UK Addendum Mandatory Clauses require Table 3 to incorporate the EU SCC Annex information (parties, transfer description, supervisory authority, TOMs); a reference-only paragraph does not satisfy the mandatory-clauses requirement to complete Table 3.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins ninety fail-fixtures (was eighty-seven); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 90. `npm run lint && test && build` all green (1438/1438 passing).

### v3 fail-fixture corpus expansion (90 → 93) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from ninety end-to-end fail-fixtures to ninety-three by adding three new fixtures spanning three distinct v3 rule families (BAA, DPA-GDPR, UK IDTA Addendum) — each load-bearing rule (BAA-003, DPA-003, TRANSFER-014) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-appropriate-safeguards-fail.txt` — BAA with Section 2 (Safeguards) rewritten as a generic "Security Program" reference; every `appropriate safeguards` / `administrative.*physical.*technical` / `reasonable safeguards` anchor is removed from the body. The 45 CFR § 164.314 cross-reference survives so the contract still gestures at Security Rule compliance, but no contract-level safeguards obligation exists. **BAA-003** fires — 45 C.F.R. § 164.504(e)(2)(ii)(B) requires the BA to use appropriate safeguards (including the Security Rule administrative, physical, and technical safeguards) to prevent use or disclosure of PHI other than as provided for by the contract; a bare program-reference does not satisfy that contracting standard.
- `dpa-controller-processor-missing-nature-and-purpose-fail.txt` — Controller→processor GDPR DPA with Section 1's heading rewritten to "Subject-Matter, Duration of Processing" and every `nature and purpose of (the )?processing` anchor removed from the body — the Section 1 sentence describing analysis of engagement data is reframed as a Processor obligation rather than a description of the processing nature/purpose. The subject-matter / duration / categories-of-data / documented-instructions / Art. 32 / subprocessor / DSR / breach-notification / deletion-or-return clauses remain so DPA-001 / DPA-002 / DPA-004..016 still pass. **DPA-003** fires — GDPR Art. 28(3) introductory paragraph requires the nature and purpose of the processing to be described so the controller's data-protection assessment and the processor's scope are aligned.
- `uk-idta-addendum-missing-table-4-fail.txt` — UK IDTA Addendum with the "Table 4 — Ending this Addendum when the Approved Addendum Changes" section removed; every `Table\s*4\s*[—:-]\s*Ending` / `table\s*4\b.{0,40}(ending|approved\s+addendum)` anchor is stripped. The Mandatory Clauses 19 and 20 still cover ICO-update termination, but the mandatory Table 4 slot is empty. Tables 1, 2, and 3 are preserved so TRANSFER-011 / 012 / 013 / 016 still pass. **TRANSFER-014 (warning)** fires — the ICO UK Addendum Mandatory Clauses require Table 4 to specify which party (or both) may end the Addendum on a revision; without Table 4 the termination-on-ICO-update mechanic is ambiguous and the form-compliance check fails.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins ninety-three fail-fixtures (was ninety); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 93. `npm run lint && test && build` all green (1447/1447 passing).

### v3 fail-fixture corpus expansion (93 → 96) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from ninety-three end-to-end fail-fixtures to ninety-six by adding three new fixtures spanning three distinct v3 rule families (BAA, DPA-GDPR, UK IDTA Addendum) — each load-bearing rule (BAA-004, DPA-004, TRANSFER-012) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-improper-use-reporting-fail.txt` — BAA with the "Reporting" section renamed to "Internal Logging" and the operative sentence rewritten so the BA "shall maintain internal logs" of incidents and "report Breaches…to Acme Health LLC's Privacy Officer" — every `(report to|notify) covered entity` anchor is stripped while the 45 CFR § 164.410 breach-notification cross-reference and the 60-day window survive so BAA-019 / BAA-022 still pass. **BAA-004** fires — 45 C.F.R. § 164.504(e)(2)(ii)(C) requires the BAA to obligate the BA to report to the Covered Entity any use or disclosure not authorized by the contract; logging-only / Privacy-Officer-only language does not satisfy the report-to-Covered-Entity obligation in the statutory text.
- `dpa-controller-processor-missing-type-of-personal-data-fail.txt` — Controller→processor GDPR DPA with Section 2's heading rewritten to "Data Subjects and Information Inventory" and every `type|categories of personal data` / `personal data processed|categories` anchor stripped — "types of personal data" is replaced with "information" in the body, and Annex I is renamed "Information Inventory" while keeping the three-tier (identification / technical / behavioral) listing. The categories-of-data-subjects sentence is preserved so DPA-005 still passes; DPA-001..003 and DPA-006..016 also pass. **DPA-004** fires — GDPR Art. 28(3) introductory paragraph requires the type of personal data to be specified so the processor's permitted-processing scope is bounded by category; a bare "information inventory" without the statutory "personal data" tag does not satisfy that requirement.
- `uk-idta-addendum-missing-table-2-fail.txt` — UK IDTA Addendum with the "Table 2 — Selected SCCs, Modules and Selected Clauses" section replaced by a generic "EU SCC Incorporation" paragraph; every `Table\s*2\s*[—:-]\s*Selected\s+SCC\s+Modules` / `table\s*2\b.{0,40}module` anchor is removed and no Module (1/2/3/4) is identified. Tables 1, 3, and 4 are preserved so TRANSFER-011 / 013 / 014 / 016 still pass. **TRANSFER-012 (warning)** fires — the ICO UK Addendum Mandatory Clauses require Table 2 to identify which EU SCC Modules are being incorporated; without that anchor the Importer's role (controller / processor / sub-processor) is ambiguous and the Module-specific Clause-8 obligations are not bound in.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins ninety-six fail-fixtures (was ninety-three); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 96. `npm run lint && test && build` all green (1456/1456 passing).

### v3 fail-fixture corpus expansion (96 → 99) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from ninety-six end-to-end fail-fixtures to ninety-nine by adding three new fixtures spanning two distinct v3 rule families (UK IDTA Addendum, DPA-GDPR) — two UK Addendum form-compliance gaps (Table 1, DPF reliance) and one Article 32 security-measures gap. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `uk-idta-addendum-missing-table-1-fail.txt` — UK IDTA Addendum with the "Table 1 — Parties" heading renamed to a generic "Counterparty Details" section and every `Table\s*1\s*[—:-]\s*Parties` / `table\s*1\b.{0,40}part` anchor stripped. The Exporter / Importer identification text is preserved as free-form prose so the contract is still commercially legible. Tables 2, 3, and 4 are preserved so TRANSFER-012 / 013 / 014 still pass. **TRANSFER-011 (warning)** fires — the ICO UK Addendum Mandatory Clauses require Table 1 to identify the Exporter and Importer in the prescribed tabular form; a free-form section does not satisfy the mandatory-clauses requirement. The companion TRANSFER-016 (broader Parties-identification rule across both IDTA Part 1 and Addendum Table 1) also fires for the same anchor loss.
- `uk-idta-addendum-relies-on-dpf-fail.txt` — UK IDTA Addendum with an added "Adequacy Mechanism" paragraph asserting the Importer's certification under the EU-US Data Privacy Framework (and the UK Extension) is "sufficient to provide appropriate safeguards for Restricted Transfers" and that "the Parties shall not be required to implement any additional transfer mechanism while the DPF adequacy decision remains in force." Tables 1–4 and the Mandatory Clauses are preserved so TRANSFER-011..016 still pass. **TRANSFER-017 (warning)** fires — its bad_patterns flag any `EU-US Data Privacy Framework` / `EU-US DPF` / `DPF (adequacy|certification)` mention; the DPF adequacy decision is permanently under litigation (Schrems history), and a contract that disclaims any fallback transfer mechanism leaves the parties exposed if (or when) the DPF is invalidated again.
- `dpa-controller-processor-missing-testing-of-measures-fail.txt` — Controller→processor GDPR DPA with Section 6's measure-(e) rewritten to a "defined change-management process governing updates to security configurations and supporting infrastructure"; every `testing.*(measures|controls)` / `penetration\s+test` / `vulnerability\s+(scan|assessment)` / `periodic\s+assess` anchor is removed from the entire DPA. The remaining Section 6 measures (encryption, pseudonymisation, confidentiality/integrity/availability/resilience, restore-availability) and Articles 32–36 assistance, breach notification, audit-and-inspection clauses are preserved so DPA-019 / 020 / 021 / 023 still pass. **DPA-022 (warning)** fires — GDPR Art. 32(1)(d) requires a process for regularly testing, assessing and evaluating the effectiveness of technical and organisational measures; a change-management process is necessary but not sufficient to satisfy the statutory testing-of-measures requirement.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins ninety-nine fail-fixtures (was ninety-six); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 99. `npm run lint && test && build` all green (1465/1465 passing).

### v3 fail-fixture corpus expansion (99 → 102) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from ninety-nine end-to-end fail-fixtures to one-hundred-and-two by adding three new MSA-deep `language` fail-fixtures — three state-law / UCC overreach failure modes that had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. All three new fixtures derive from `msa-vendor-deep-minimal-pass.txt` with targeted bad-pattern injection. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `msa-vendor-deep-cal-1668-exculpation-fail.txt` — Vendor MSA with an added Section 8(d) "Exculpation" clause stating that "Vendor shall not be liable for fraud, willful injury, or violation of any law, and no liability shall apply to fraud or willful misconduct" — and governing law switched to California so the § 1668 problem actually bites. Section 8(a)–(c) (cap / carveouts / consequential waiver) is preserved. **MSA-009 (warning)** fires — California Civil Code § 1668 voids contracts that exempt anyone from responsibility for their own fraud, willful injury, or violation of law; a stand-alone exculpation that absorbs these categories is unenforceable in California regardless of any carve-out elsewhere in the agreement.
- `msa-vendor-deep-as-is-without-warrant-fail.txt` — Vendor MSA with Section 6's limited-warranty disclaimer rewritten to "provided as is and with all faults, without any other warranty of any kind, express or implied, and Vendor disclaims any and all warranties to the maximum extent permitted by applicable law" — the conspicuous-MERCHANTABILITY callout is dropped. **MSA-015 (info)** fires — UCC § 2-316 requires merchantability disclaimers to mention "merchantability" expressly and to be conspicuous; a generic "AS IS / disclaims any and all warranties" without the conspicuous-MERCHANTABILITY callout fails the UCC test, leaving the implied-warranty disclaimer vulnerable to challenge in any UCC-governed transaction.
- `msa-vendor-deep-governing-law-venue-mismatch-fail.txt` — Vendor MSA with Section 11 collapsed to a single sentence that names Delaware as the governing law and New York County courts as the forum — the governing-law / venue alignment is broken and a Delaware-courts→Delaware-law symmetric structure is replaced by a Delaware-law→New-York-courts mismatch. **MSA-024 (info)** fires — its bad_patterns catch a `governed by the laws of {State A}` clause followed within 400 non-period characters by a `(courts|venue|jurisdiction|forum)` reference to a different `{State B}`; choosing one state's law but another state's forum forces the forum court to apply foreign law, adding cost and uncertainty.

Each fail-fixture has a `.playbook` sidecar pinning it to `msa-vendor-deep` so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-two fail-fixtures (was ninety-nine); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 102. `npm run lint && test && build` all green (1474/1474 passing).

### v3 fail-fixture corpus expansion (114 → 117) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-fourteen end-to-end fail-fixtures to one-hundred-and-seventeen by adding three new fixtures spanning three distinct v3 rule families (DPA-GDPR Art. 28(2) subprocessor objection, BAA HIPAA penalty-cap impairment, MSA-deep New York anti-indemnity) — each load-bearing rule (DPA-016, BAA-025, MSA-010) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-subprocessor-objection-fail.txt` — Controller→processor GDPR DPA with Section 7 (Subprocessors) rewritten to drop the "thereby giving Controller the opportunity to object to such changes" tail; every `(opportunity\s+to\s+object|right\s+to\s+object).*?(sub[- ]?processor|processor)` anchor disappears. The general-written-authorization recital, the 30-day prior-notice requirement, the equivalent-data-protection flow-down, and the processor's full-liability undertaking are preserved so DPA-015 / 017 still pass. **DPA-016 (critical)** fires — GDPR Art. 28(2) requires informing the controller of intended subprocessor changes AND giving the controller an opportunity to object; a notice-without-objection-right does not satisfy the statutory standard and leaves the controller no lever to block a subprocessor it cannot accept.
- `baa-indemnity-cap-impairing-hipaa-fail.txt` — BAA with a new "Limitation of Liability" clause appended after the Term section reciting that "each Party's aggregate liability for any and all claims arising out of or relating to this BAA shall not exceed the fees paid by Covered Entity to Business Associate in the twelve (12) months immediately preceding the event giving rise to the claim." Every other BAA clause (Permitted Uses, Safeguards, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, Books/Records, Return-or-Destruction, Breach Notification) is preserved so BAA-001..022 still pass. **BAA-025 (warning)** fires — its `bad_pattern` (`(aggregate\s+liability|total\s+liability|liability.{0,40}shall\s+not\s+exceed).{0,120}(fees|paid|amount)`) catches the injected cap; HIPAA civil money penalties under 45 C.F.R. § 160.404 can reach over $2M per violation category per year (adjusted annually), so a 12-month-fees cap effectively shifts HIPAA risk back to the Covered Entity rather than allocating it to the BA whose breach caused the penalty.
- `msa-vendor-deep-ny-construction-indemnity-fail.txt` — Vendor MSA with Section 11 governing-law switched from Delaware to New York and a new Section 13 "Construction Project Indemnity" appended that recites "Vendor shall indemnify and hold harmless Customer for any and all claims and losses arising out of or related to such work, including those resulting from the negligence of the indemnitee." The existing Section 7 mutual indemnification, Section 8 liability cap with carveouts, and Section 11 venue clause are preserved so MSA-002 / 005 / 006 / 008 / 024 still pass. **MSA-010 (warning)** fires — N.Y. Gen. Oblig. § 5-322.1 voids construction-contract indemnities that require the indemnitor to indemnify the indemnitee for the indemnitee's own negligence; the rule's first bad_pattern catches `indemnif\w+ … for any and all claims … including … negligence of the indemnitee` end-to-end and surfaces the void-on-its-face exposure the New-York-law clause inherits.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-seventeen fail-fixtures (was one-hundred-and-fourteen); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 117. `npm run lint && test && build` all green.

### v3 fail-fixture corpus expansion (111 → 114) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-eleven end-to-end fail-fixtures to one-hundred-and-fourteen by adding three new fixtures spanning three distinct v3 rule families (DPA-GDPR Art. 35 DPIA, BAA HIPAA-overreach indemnity, Transfer/SCC TIA reference) — each load-bearing rule (DPA-029, BAA-027, TRANSFER-019) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-dpia-assistance-fail.txt` — Controller→processor GDPR DPA with Section 9's "Assistance with Articles 32 to 36 Obligations" rewritten so every `data\s+protection\s+impact\s+assessment` / `\bDPIA\b` / `article\s*35` anchor is removed; the sentence now reads "...including obligations relating to security, breach notification, prior consultation with supervisory authorities, and the supplementary measures appropriate to those obligations." The "Articles 32 to 36" reference survives so DPA-012 (Art. 28(3)(f) assistance) still passes, and the breach / security / consultation anchors keep DPA-008..014 intact. **DPA-029 (warning)** fires — GDPR Art. 35 requires a DPIA for high-risk processing and Art. 28(3)(f) obliges the processor to assist; a "supplementary measures" wave-off does not anchor the processor's DPIA assistance duty, which auditors and supervisory authorities look for as a discrete clause.
- `baa-covered-entity-indemnifies-ba-fail.txt` — BAA with a new "Indemnification" clause appended after the Term section reciting that "Covered Entity shall indemnify, defend, and hold harmless Business Associate from and against any claim, loss, fine, penalty, or expense … arising out of or relating to any alleged HIPAA violation … regardless of the source of the underlying breach." Every other BAA clause (Permitted Uses, Safeguards, Reporting, Subcontractor Flow-Down, Access/Amendment/Accounting, Books/Records, Return-or-Destruction, Breach Notification) is preserved so BAA-001..022 still pass. **BAA-027 (warning)** fires — its `bad_patterns` catch `covered\s+entity\s+(shall|will)\s+indemnif`; 45 C.F.R. § 164.504(e) and HHS posture make clear that the covered entity should not indemnify the business associate for the BA's own HIPAA violations, because doing so inverts the regulatory burden and lets the BA shift the cost of its own non-compliance back to the customer.
- `scc-module-2-missing-tia-reference-fail.txt` — SCC Module 2 with the Clause 14 heading renamed from "Local Laws and Practices Assessment" to "Destination-Jurisdiction Compliance Warranty" and the body's "documented transfer impact assessment" rewritten to "documented internal review of the destination jurisdiction"; every `transfer\s+(?:impact|risk)\s+assessment` / `local\s+laws\s+and\s+practices` / `\bTIA\b` / `\bTRA\b` / `supplementary\s+measures` anchor is removed from the entire SCC. The Clause 14 heading retains "Clause 14" so TRANSFER-007 (presence of Clause 14) still passes; Clause 15 public-authority and Clause 16 non-compliance survive so TRANSFER-008 / 009 still pass. **TRANSFER-019 (critical)** fires — EDPB Recommendations 01/2020 require the parties to anchor a Transfer Impact Assessment (TIA) — the assessment of local laws and practices of the recipient country plus any supplementary measures — in the SCC; a bare "documented internal review" without the named statutory anchors does not satisfy the Schrems II / EDPB expectation.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-fourteen fail-fixtures (was one-hundred-and-eleven); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 114. `npm run lint && test && build` all green.

### v3 fail-fixture corpus expansion (108 → 111) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-eight end-to-end fail-fixtures to one-hundred-and-eleven by adding three new fixtures spanning three distinct v3 rule families (DPA-GDPR Art. 33(2) phrasing, BAA Security Incident definition, US-state CCPA cross-context advertising) — each load-bearing rule (DPA-025, BAA-023, USDPA-003) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-undue-delay-fail.txt` — Controller→processor GDPR DPA with Section 10's "Processor shall notify Controller without undue delay after becoming aware of a personal data breach" rewritten so the "without undue delay" phrasing is replaced by "promptly"; every `without\s+undue\s+delay` anchor is removed from the entire DPA. The remaining Section 10 keeps the "personal data breach … notify Controller … within 48 hours" anchor so DPA-024 (Art. 33(2) presence) still passes; Section 2 retains "categories of data subjects" so DPA-026 still passes; the 48-hour window is in hours, not days, so the DPA-027 loose-timing `bad_pattern` (`within\s+\d+\s+days?`) does not fire. **DPA-025 (warning)** fires — GDPR Art. 33(2) uses the "without undue delay" standard and drafters should preserve the exact phrasing so the processor's reporting trigger maps cleanly onto the controller's Art. 33(1) 72-hour clock; "promptly" is a softer standard that does not carry the same supervisory-authority expectation.
- `baa-security-incident-narrowed-fail.txt` — BAA with a new "Definitions" section preceding "Permitted Uses and Disclosures" that narrows "Security Incident" to "only successful unauthorized access to or successful disclosure of ePHI" and expressly excludes "unsuccessful attempts to penetrate, probe, scan, or otherwise interact with the systems used to process ePHI." Every downstream BAA clause (Reporting, Safeguards, Subcontractor Flow-Down, Access/Amendment/Accounting, Books and Records, Return or Destruction, Breach Notification) is preserved so BAA-001..022 still pass. **BAA-023 (warning)** fires — its `bad_patterns` catch both `Security\s+Incident.{0,200}successful\s+(unauthorized|access)` and `only\s+successful\s+(unauthorized\s+access|incidents)`; OCR's interpretation of 45 C.F.R. § 164.304 is that "Security Incident" includes both successful and unsuccessful attempts, so narrowing the definition to "only successful" lets material attack telemetry (failed login storms, port scans, unsuccessful intrusion attempts) escape the BA's Security Rule risk-management and reporting duties.
- `dpa-multi-state-us-missing-cross-context-prohibition-fail.txt` — Multi-state US DPA with Section 2(b) rewritten from "share personal information for cross-context behavioral advertising as defined in Cal. Civ. Code § 1798.140(ah)" to "make personal information available to any third party for targeted advertising purposes outside the Business Purpose"; every `cross[- ]context\s+behavioral\s+advertising` anchor is removed from the entire DPA. Section 2(a) sell-prohibition, Section 2(c) Business-Purpose limitation, and Section 2(d) no-combining restriction are preserved so USDPA-002 / 005 / 007 / 008 still pass. **USDPA-003 (critical)** fires — Cal. Civ. Code § 1798.140(ag)(1)(A) requires the service-provider contract to prohibit sharing including for cross-context behavioral advertising as a named statutory category; a generic "targeted advertising" prohibition is not equivalent because CCPA / CPRA enforcement turns on the specific "cross-context behavioral advertising" statutory term of art.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-eleven fail-fixtures (was one-hundred-and-eight); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 111. `npm run lint && test && build` all green.

### v3 fail-fixture corpus expansion (105 → 108) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-five end-to-end fail-fixtures to one-hundred-and-eight by adding three new fixtures spanning three distinct v3 rule families (DPA-GDPR Art. 33(2), MSA-deep boilerplate, Vendor Security Addendum cadence) — each load-bearing rule (DPA-024, MSA-026, ADDENDA-002) had per-ruleset unit tests but no end-to-end golden fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-processor-breach-notice-fail.txt` — Controller→processor GDPR DPA with Section 10's "Breach Notification (Article 33 GDPR)" rewritten as a generic "Incident Handling" paragraph; every `personal\s+data\s+breach` / `breach\s+of\s+personal\s+data` / `data\s+breach` anchor is removed and the operative sentence is reframed as "Processor shall, without undue delay, work with Controller to investigate and remediate the underlying issue." Section 9 (Articles 32–36 assistance referencing "breach notification" generically) and Section 6 (security measures) are preserved so DPA-012 / 019 / 020 / 021 / 022 still pass, and the new Section 10 retains "without undue delay" so DPA-025 still passes; Section 2 retains "categories of data subjects" so DPA-026 still passes. **DPA-024 (critical)** fires — GDPR Art. 33(2) requires the processor to notify the controller without undue delay after becoming aware of a personal data breach; a generic "operational anomaly / regulatory obligation" framing does not satisfy the named statutory anchor that supervisory authorities rely on to scope the processor's notification duty.
- `msa-vendor-deep-missing-survival-entire-agreement-fail.txt` — Vendor MSA with Section 5's "shall survive termination of this Agreement for three (3) years" rewritten to "shall continue in force after the end of this Agreement for three (3) years" (every `surviv\w+` anchor stripped) and Section 12 swapped from "Entire Agreement" to a generic "Counterparts" paragraph (every `entire\s+agreement` / `integration\s+clause` / `supersed\w+\s+(?:all\s+)?prior` anchor stripped). The Section 1 order-of-precedence "supersedes a specific provision" phrasing is preserved but does not match the `supersed\w+\s+(?:all\s+)?prior` pattern. **MSA-026 (info)** fires — its `present_patterns` need either a survival-tied-to-termination phrase or an entire-agreement / integration / supersedes-all-prior phrase; without survival, sticky obligations (confidentiality, IP, indemnity) may not outlive termination, and without integration prior negotiations may be admissible in a later contract dispute.
- `vendor-security-addendum-missing-review-cadence-fail.txt` — Vendor Security Addendum with Section 4(c) "quarterly access reviews" rewritten to "regular access reviews on an ongoing basis"; every `(annual\w*|annually|quarterly|every\s+\d+\s+(?:months?|years?))` token bound within 80 non-period chars of an audit / review / assessment / certification / attestation / SOC 2 / ISO 27001 anchor is removed. Sections 3(a) "Annual Penetration Test." and Section 11 "annual information-security awareness training" survive but neither pairs a cadence token with one of ADDENDA-002's listed anchors (Section 3(a) pairs "Annual" with "Penetration Test" — period terminates the regex window before any anchor; Section 11 pairs "annual" with "training" — not in the anchor list). **ADDENDA-002 (warning)** fires — without a stated cadence (e.g., "SOC 2 Type II report renewed annually," "quarterly access reviews," "ISO 27001 certification renewed every three years") the addendum carries no operational commitment a customer can audit against.

Each fail-fixture has a `.playbook` sidecar pinning it to the relevant v3 playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-eight fail-fixtures (was one-hundred-and-five); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 108. `npm run lint && test && build` all green.

### v3 fail-fixture corpus expansion (102 → 105) (2026-05-19) — ✅ complete

Advanced LAUNCH row v3-b coverage from one-hundred-and-two end-to-end fail-fixtures to one-hundred-and-five by adding three new DPA-GDPR Section 6 / Article 32 security-measure fail-fixtures. All three derive from `dpa-controller-processor-minimal-pass.txt` with a single Art. 32(1)(x) sub-paragraph rewritten while the other sub-paragraphs are preserved so each fixture targets exactly one rule. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-pseudonymisation-encryption-fail.txt` — DPA with Section 6's measure-(a) rewritten as "network and storage-layer cryptographic protection ... using industry-standard algorithms" and measure-(b) rewritten as "data minimization, access controls, and least-privilege role assignment" — every `pseudonymi[sz]ation` / `encrypt(ion|ed)` anchor is removed from the entire DPA. The remaining Section 6 measures (CIA-R triad, restore availability, regular testing) are preserved so DPA-020 / 021 / 022 / 023 still pass. **DPA-019 (warning)** fires — GDPR Art. 32(1)(a) lists pseudonymisation and encryption among the technical and organisational measures appropriate to the risk; replacing those named statutory measures with generic "cryptographic protection" drops the Art. 32(1)(a) anchor.
- `dpa-controller-processor-missing-cia-resilience-fail.txt` — DPA with Section 6's measure-(c) rewritten as "operational hardening of processing systems and services through configuration management, segregation of duties, and capacity planning" — every CIA-R triad anchor (`confidentiality.{0,30}integrity.{0,30}availability` / `availability.{0,30}resilience` / `integrity.{0,30}availability`) is removed from the entire DPA. The remaining Section 6 measures and downstream clauses are preserved so DPA-019 / 021 / 022 / 023 still pass. **DPA-020 (warning)** fires — GDPR Art. 32(1)(b) requires measures to ensure ongoing confidentiality, integrity, availability and resilience of processing systems and services; "operational hardening" generic language does not satisfy the named four-element commitment.
- `dpa-controller-processor-missing-restore-availability-fail.txt` — DPA with Section 6's measure-(d) rewritten as "maintenance of redundant infrastructure designed to limit unplanned downtime affecting personal data in the event of a physical or technical incident" — every `restore\s+availability` / `disaster\s+recovery` / `business\s+continuity` / `backup\s+and\s+recovery` anchor is removed from the entire DPA. The remaining Section 6 measures and downstream clauses are preserved so DPA-019 / 020 / 022 / 023 still pass. **DPA-021 (warning)** fires — GDPR Art. 32(1)(c) requires the ability to restore the availability and access to personal data in a timely manner in the event of an incident; the "redundant infrastructure" phrasing is a partial substitute that lacks the restore-on-incident anchor.

Each fail-fixture has a `.playbook` sidecar pinning it to `dpa-controller-processor` so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins one-hundred-and-five fail-fixtures (was one-hundred-and-two); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 105. `npm run lint && test && build` all green (1483/1483 passing).

### v3 fail-fixture corpus expansion (75 → 78) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from seventy-five end-to-end fail-fixtures to seventy-eight by adding three new fixtures spanning three previously uncovered BAA failure modes (Security Rule technical safeguards, breach-notification outer-bound timing, return-or-destruction outer bound) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-technical-safeguards-fail.txt` — BAA with the Safeguards section narrowed from "administrative, physical, and technical safeguards … 45 CFR §§ 164.308, 164.310, and 164.312" to "administrative and physical safeguards … 45 CFR §§ 164.308 and 164.310"; every `technical safeguards` token and every `164.312` anchor is stripped while the broader Security Rule reference is retained so BAA-013 still passes. **BAA-016 (warning)** fires — 45 CFR § 164.312 requires the BAA to anchor technical safeguards (access controls, audit logs, integrity controls, transmission security); without a § 164.312 hook the covered entity has no contractual lever to enforce technical-safeguard compliance.
- `baa-breach-notification-90-day-window-fail.txt` — BAA with the breach-notification outer bound widened from sixty to 90 calendar days following discovery of the Breach; every other anchor (`without unreasonable delay`, `discovery of the Breach`, `45 CFR § 164.410`) is preserved so BAA-019 / BAA-021 / BAA-022 still pass. **BAA-020 (critical)** fires — 45 CFR § 164.410(b) requires notification "without unreasonable delay and in no case later than 60 calendar days after discovery"; any window beyond 60 days violates HIPAA's outer bound and exposes the covered entity to its own § 164.404 obligations on a clock it cannot meet.
- `baa-return-or-destruction-open-ended-fail.txt` — BAA with the "Return or Destruction" sentence rewritten so that the outer time bound is replaced with "as soon as practicable thereafter"; every "thirty (30) days" / "ninety (90) days" / "by no later than" anchor is stripped and the surviving prose preserves only the open-ended commitment, while BAA-010 (presence) still passes because the "destroy all PHI" anchor survives. **BAA-024 (warning)** fires — its bad_patterns catch `(return|destroy|destruction) … (as soon as practicable|commercially reasonable|reasonable time)` within an 80-char window; HHS guidance expects a definite outer bound for return or destruction of PHI at termination, and open-ended timing risks indefinite PHI retention with no contractual lever for the covered entity to compel completion.

Each fail-fixture has a `.playbook` sidecar pinning it to the `baa` playbook so the auto-detect cannot drift to a v2 fallback. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins seventy-eight fail-fixtures (was seventy-five); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 78. `npm run lint && typecheck && test && build` all green.

### v3 fail-fixture corpus expansion (72 → 75) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from seventy-two end-to-end fail-fixtures to seventy-five by adding three new fixtures spanning three distinct v3 rule families (BAA, NDA-deep, MSA-deep) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-security-incident-reporting-fail.txt` — BAA with the "Reporting" paragraph rewritten to drop the "Security Incident" sentence; the breach-notification sentence tied to 45 CFR § 164.410 is retained, but every `security incident` token has been removed. **BAA-017 (warning)** fires — 45 CFR § 164.314(a)(2)(i)(C) requires the BAA to obligate the Business Associate to report security incidents (including unsuccessful attempts) to the Covered Entity; without that hook the covered entity loses the contractual channel for incident visibility short of a confirmed breach.
- `mutual-nda-deep-permitted-use-overbroad-fail.txt` — Mutual NDA with Section 3 ("Permitted Use") rewritten to grant use of Confidential Information "for any business purpose" of the Receiving Party instead of "solely for the Permitted Purpose." **NDA-D-011** fires — its bad_patterns catch `for any (business )?purpose` / `any lawful purpose` / `(unrestricted|any) use of (the )?confidential`, all of which weaken the contractual basis for objecting to unrelated downstream use of the disclosed information.
- `msa-vendor-deep-sla-sole-exclusive-remedy-fail.txt` — Vendor MSA with a new Section 9 ("Service Level Agreement") committing to 99.9% monthly uptime but declaring that a prorated service credit is Customer's "sole and exclusive remedy" for any service-level failure, including extended downtime or chronic outage, and that Customer has no right to terminate or seek other damages. **MSA-017** fires — its bad_patterns catch the `(service|SLA) credit … sole and exclusive remedy` and `sole and exclusive remedy … (service level|SLA|downtime)` forms; an exclusive-remedy SLA bars the customer from terminating or seeking damages for extended outages, which is the commercially contested posture the rule surfaces for explicit review.

Each fail-fixture has a `.playbook` sidecar. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins seventy-five fail-fixtures (was seventy-two); each entry asserts the load-bearing rule fires. Goldens regenerated; LAUNCH row v3-b updated to 75. `npm run lint && typecheck && test && build` all green; **1393/1393 tests + 2 skips**.

### v3 fail-fixture corpus expansion (69 → 72) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from sixty-nine end-to-end fail-fixtures to seventy-two by adding three new fixtures spanning three distinct v3 rule families (BAA, DPA-GDPR, ADDENDA) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-physical-safeguards-fail.txt` — BAA with the Safeguards section narrowed from "administrative, physical, and technical safeguards … 45 CFR §§ 164.308, 164.310, and 164.312" to "administrative and technical safeguards … 45 CFR §§ 164.308 and 164.312"; every `physical safeguards` token and every `164.310` anchor is stripped while "Security Rule" is retained so BAA-013 still passes. **BAA-015 (warning)** fires — § 164.310 requires the BAA to anchor physical safeguards (facility access controls, workstation security, device controls); without a § 164.310 hook the covered entity has no contractual lever to enforce physical-safeguard compliance.
- `dpa-controller-processor-missing-subprocessor-authorisation-fail.txt` — Controller-to-Processor DPA with Section 7 ("Subprocessors") rewritten to omit any "prior written authorisation" / "authorisation of Controller" anchor — the surviving clause permits Processor to engage subprocessors at will, preserving only a downstream notice + objection right and the flow-down obligation. **DPA-015** fires — GDPR Art. 28(2) requires the processor not to engage another processor without prior specific or general written authorisation; with the anchor stripped the contractual hook for Art. 28(2) is lost and the controller has no upstream veto over subprocessor onboarding.
- `vendor-security-addendum-missing-pen-test-cadence-fail.txt` — Vendor Security Addendum with Section 3 rewritten so that "Annual Penetration Test" becomes "Independent Penetration Test" and the cadence is described as "on a periodic basis as determined by Vendor's risk-management function"; every `annual\w*` / `annually` / `quarter\w+` / `every \d+ months/years` anchor adjacent to "penetration test" / "pen-test" is stripped. **ADDENDA-009 (info)** fires — annual third-party pen-tests are the practitioner-accepted baseline for SaaS; without a stated cadence the addendum carries no operational commitment to testing frequency.

Each fail-fixture has a `.playbook` sidecar. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins seventy-two fail-fixtures (was sixty-nine); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build`.

### v3 fail-fixture corpus expansion (66 → 69) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from sixty-six end-to-end fail-fixtures to sixty-nine by adding three new fixtures spanning three distinct v3 rule families (TRANSFER, NDA-deep, MSA-deep) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `scc-module-2-missing-clause-14-fail.txt` — SCC Module 2 with the "Clause 14 — Local Laws and Practices Assessment" heading replaced by a generic "Destination-Country Conditions Statement" paragraph; every `clause 14`, `local laws and practices`, `transfer impact assessment`, and `TIA` anchor is stripped while the surviving prose preserves a soft good-faith warranty without the Schrems II TIA anchor. **TRANSFER-007** fires for EU SCCs Clause 14 — Clause 14 of Decision 2021/914 anchors the Transfer Impact Assessment; without it there is no contractual hook for the parties' Schrems II analysis and supervisory authorities cannot locate the TIA discussion.
- `mutual-nda-deep-non-solicit-no-carve-out-fail.txt` — Mutual NDA with a non-solicitation clause added at Section 7 that runs for one year post-termination but contains no general-solicitation / public-job-postings carve-out; the surrounding 300-character window contains only "direct contact", "indirect contact", and "executive recruiter retained" language, deliberately omitting the standard safe-harbor phrasing. **NDA-D-020 (warning)** fires because its bad_pattern's negative lookahead requires one of `general\s+solicitation` / `not\s+specifically\s+directed` / `general\s+advertis` within 300 chars of the non-solicit anchor; without the carve-out, ordinary recruiting (LinkedIn posts, conference recruiters, public job ads) becomes a contractual breach risk.
- `msa-vendor-deep-indemnity-carved-out-of-cap-fail.txt` — Vendor MSA with Section 8(b) "Carveouts" tightened so that "cap shall not apply to indemnification" sits inside the rule's 80-character proximity window (the pass fixture separates them with ~95 chars). **MSA-005 (info)** fires because indemnification is now plainly excluded from the aggregate liability cap — the rule surfaces this commercially-contested choice for explicit review since whether indemnification falls inside or outside the cap is the most-contested cap-carve-out term in commercial drafting.

Each fail-fixture has a `.playbook` sidecar. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins sixty-nine fail-fixtures (was sixty-six); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build`.

### v3 fail-fixture corpus expansion (63 → 66) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from sixty-three end-to-end fail-fixtures to sixty-six by adding three new fixtures spanning three distinct v3 rule families (DPA-GDPR, BAA, ADDENDA) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-art32-36-assistance-fail.txt` — Controller→Processor DPA with Section 9 ("Assistance with Articles 32 to 36 Obligations") replaced by a generic "Cooperation on Operational Matters" paragraph; every `Articles 32 to 36` and `assist ... (breach|security|DPIA)` anchor is stripped and the surviving prose references only "commercially reasonable cooperation" with no GDPR-aligned statutory framing. **DPA-012** fires for GDPR Art. 28(3)(f) — the processor must assist the controller with the security (Art. 32), breach (Arts. 33–34), DPIA (Art. 35), and prior-consultation (Art. 36) obligations; without this clause the controller carries those obligations alone, materially weakening Art. 28 alignment.
- `baa-missing-administrative-safeguards-fail.txt` — BAA with the Safeguards section narrowed from "administrative, physical, and technical safeguards … 45 CFR §§ 164.308, 164.310, and 164.312" to "physical and technical safeguards … 45 CFR §§ 164.310 and 164.312"; every `administrative safeguards` token and every `164.308` anchor is stripped, but the broader "Security Rule" reference is retained so BAA-013 still passes. **BAA-014 (warning)** fires for 45 C.F.R. § 164.308 — the BAA must anchor administrative safeguards (workforce training, contingency planning, periodic risk assessment); without a § 164.308 hook the covered entity has no contractual lever to enforce administrative-safeguard compliance.
- `vendor-security-addendum-missing-named-encryption-fail.txt` — Vendor Security Addendum with Section 2 rewritten to remove every named encryption standard — `AES-256`, `TLS 1.2`, `TLS 1.3`, and any `FIPS 140-3` reference are all stripped and the surviving prose references only "industry-standard symmetric ciphers" and "a current version of the Transport Layer Security protocol". **ADDENDA-008 (warning)** fires for FIPS 140-3 / NIST SP 800-53 — named TLS / AES versions are the practitioner-accepted contract anchors; generic "encryption" is unauditable because there is no specific cipher suite or protocol version against which to test the deployment.

Each fail-fixture has a `.playbook` sidecar. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins sixty-six fail-fixtures (was sixty-three); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build`.

### v3 fail-fixture corpus expansion (60 → 63) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from sixty end-to-end fail-fixtures to sixty-three by adding three new fixtures spanning three distinct v3 rule families (BAA, NDA-deep, MSA-deep) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-access-to-phi-fail.txt` — BAA with the "Access, Amendment, Accounting" header rewritten as "Amendment, Accounting"; every `access to PHI`, `right of access`, and `164.524` anchor is stripped. The surviving prose references only § 164.526 (amendment) and § 164.528 (accounting) and uses the commercial substitute "inspect or obtain a copy" that deliberately avoids the rule's three present_patterns. **BAA-006** fires for 45 C.F.R. § 164.504(e)(2)(ii)(E) — the BAA must allow the covered entity to satisfy individuals' right of access under § 164.524; without this clause an access request bottlenecks at the BA with no contractual hook.
- `mutual-nda-deep-unusual-governing-law-fail.txt` — Mutual NDA with the governing-law clause re-pointed from Delaware to Wyoming (and venue to Cheyenne, Wyoming). NDA-D-017 still passes (a governing-law clause is present) but Wyoming sits outside the NDA-D-018 viable-jurisdiction whitelist (Delaware / New York / California / Texas / Massachusetts / Illinois / Washington / England / United Kingdom). **NDA-D-018 (info)** fires because its present_patterns require `laws\s+of\s+...(Delaware|New\s+York|California|Texas|Massachusetts|Illinois|Washington|England|United\s+Kingdom)` and Wyoming is absent; an atypical jurisdiction may produce unpredictable enforcement outcomes for an NDA.
- `msa-vendor-deep-one-sided-consequential-waiver-fail.txt` — Vendor-form MSA with Section 8(c) rewritten one-sided — "IN NO EVENT SHALL EITHER PARTY BE LIABLE TO THE OTHER PARTY" becomes "VENDOR SHALL NOT BE LIABLE TO CUSTOMER" and every "neither party", "each party", and "mutual" scoping is stripped. **MSA-008 (info)** fires because its present_patterns require a mutual scoping anchor (`neither\s+party` / `each\s+party` / `in\s+no\s+event\s+shall\s+either\s+party`) within 160 chars of a consequential-damages token; with one-sided phrasing the pattern fails to match and the counterparty has no contractual reciprocity on indirect / consequential damages.

Each fail-fixture has a `.playbook` sidecar. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins sixty-three fail-fixtures (was sixty); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build`.

### v3 fail-fixture corpus expansion (57 → 60) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from fifty-seven end-to-end fail-fixtures to sixty by adding three new fixtures spanning three distinct v3 rule families (BAA, NDA-deep, TRANSFER) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-phi-amendment-fail.txt` — BAA with the "Access, Amendment, Accounting" section rewritten as "Access, Accounting"; every "amendment", "amend.*PHI", and "164.526" anchor is stripped and the surviving sentence references only § 164.524 (access) and § 164.528 (accounting). The commercial substitute ("cooperate as commercially reasonable" on update or correction requests) deliberately avoids both "amend" and "amendment" so neither alternation in BAA-007's present_patterns matches. **BAA-007** fires for 45 C.F.R. § 164.504(e)(2)(ii)(F) — the BAA must allow the covered entity to satisfy individuals' right to amend PHI under § 164.526; without this clause, an amendment request bottlenecks at the BA with no contractual hook.
- `mutual-nda-deep-missing-governing-law-fail.txt` — Mutual NDA with Section 8 rewritten as "Dispute Resolution; Venue"; every "governing law", "governed by the laws", and "laws of the State of/country of" anchor is stripped. The surviving clause designates a court (Wilmington) and waives forum non conveniens but explicitly disclaims any choice-of-substantive-law selection, deferring conflict-of-laws to the forum court. **NDA-D-017** fires because its present_patterns require one of three governing-law anchors and now none appears; without a chosen governing law the parties' substantive expectations are exposed to whichever forum-state conflict-of-laws regime picks up the case.
- `scc-module-2-missing-clause-11-fail.txt` — SCC Module 2 with the "Clause 11 — Redress" heading replaced by a generic "Customer Service Contact Point" paragraph; every "Clause 11" anchor and every "redress" token is stripped. The surviving prose preserves the contact-point obligation but loses both the SCC clause-numbering and the statutory term "redress". **TRANSFER-006 (warning)** fires because its present_patterns require `clause\s*11\b` or `redress\b`; without either anchor, automated compliance tooling and auditors cannot locate the mandatory data-subject redress disclosure and internal cross-references in the SCCs that point back to Clause 11 are silently broken.

Each fail-fixture has a `.playbook` sidecar. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins sixty fail-fixtures (was fifty-seven); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1348 passing + 2 skips** (was 1339; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (54 → 57) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from fifty-four end-to-end fail-fixtures to fifty-seven by adding three new fixtures spanning three distinct v3 rule families (TRANSFER, MSA, DPA-GDPR) — none of the three rules had an end-to-end fail-fixture before this batch. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `scc-module-2-missing-clause-9-fail.txt` — SCC Module 2 with the "Clause 9 — Use of Sub-Processors" heading and numbered provisions replaced by a generic "Downstream Vendor Management" paragraph; every "clause 9" / "clause\s*9\b" anchor is absent. **TRANSFER-005** fires because its present_patterns require `clause\s*9\b` anywhere in the document; without the Clause 9 heading the prior-authorisation regime for sub-processors is undefined and the Art. 28(4) flow-down hook is silently broken.
- `msa-vendor-deep-missing-gross-negligence-indemnity-fail.txt` — Vendor-form MSA with the indemnification section covering only IP-infringement and Customer-violation claims — every gross-negligence, wilful-misconduct, and data-protection indemnity pairing is absent from both Section 7 and the cap carve-out list. **MSA-004 (warning)** fires because its present_patterns require "indemnif" paired within 200 chars with "gross negligence", "wilful misconduct", "data protection", "personal data", or "breach of DPA"; without this prong the counterparty bears uncompensated exposure for the highest-impact conduct categories.
- `dpa-controller-processor-missing-data-subjects-fail.txt` — Controller→Processor DPA with Section 2 enumerating only types of personal data while omitting any identification of the population of individuals affected; every "categories of data subjects" / "data subjects category" anchor is stripped and Annex I is retitled "Personal Data in Scope" without a subjects enumeration. **DPA-005** fires for GDPR Art. 28(3) introductory paragraph — the controller-processor contract must set out the categories of data subjects; without this element the scope of Art. 28 obligations is undefined.

Each fail-fixture has a `.playbook` sidecar. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins fifty-seven fail-fixtures (was fifty-four); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1339 passing + 2 skips** (was 1330; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (51 → 54) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from fifty-one end-to-end fail-fixtures to fifty-four by adding three new fixtures spanning three distinct v3 rule families (TRANSFER, DPA-GDPR, BAA) — none of the three families had a fixture for the chosen rules. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `scc-module-2-missing-clause-8-fail.txt` — SCC Module 2 with the "Clause 8 — Data Protection Safeguards" heading and numbering stripped; all obligations survive in prose form but every "clause 8" / "Clause 8" anchor is absent. **TRANSFER-004** fires because its present_patterns require `clause\s*8\b` anywhere in the document; without the heading, automated compliance tooling and auditors cannot locate the Art. 28 safeguard block and the SCC's internal cross-references to "Clause 8.9" (audit) are silently broken.
- `dpa-controller-processor-missing-dsr-assistance-fail.txt` — Controller→Processor DPA with Section 8 rewritten to an "Individual Requests" cooperation paragraph — every "assist the controller", "data subject rights", and "Chapter III" anchor is stripped; the surviving text references only "individuals" and "personal information." **DPA-011** fires for GDPR Art. 28(3)(e) — the processor must assist the controller by appropriate technical and organisational measures to fulfil its obligation to respond to data-subject rights; absent the statutory framing the processor has no explicit GDPR-aligned obligation covering access, erasure, portability, and objection requests.
- `baa-missing-security-rule-compliance-fail.txt` — BAA with the Safeguards section rewritten to omit every "Security Rule", "164.30X", and "administrative … physical … technical" anchor; the surviving text references only "appropriate safeguards" and "applicable HIPAA regulations." **BAA-013 (critical)** fires for 45 C.F.R. § 164.314(a)(2)(i) — that regulation requires the BAA to explicitly mandate the BA's compliance with the HIPAA Security Rule for ePHI; a generic "appropriate safeguards" clause does not satisfy the regulatory citation requirement and leaves the covered entity without a clear contractual hook to enforce Security Rule compliance.

Each fail-fixture has a `.playbook` sidecar. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins fifty-four fail-fixtures (was fifty-one); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1330 passing + 2 skips** (was 1321; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (48 → 51) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from forty-eight end-to-end fail-fixtures to fifty-one by adding three new NDA-deep fixtures — each exercises a distinct NDA-D load-bearing rule not previously covered end-to-end. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-missing-return-attestation-fail.txt` — retains the return-or-destruction clause (Section 7 says the Receiving Party shall "return or destroy all Confidential Information") but strips the written certification obligation; the surviving text merely says the party shall make "reasonable efforts" to comply. **NDA-D-014 (warning)** fires because its present_patterns look for certify/certification/attestation near destroy/return and now neither appears; without this requirement the discloser has no contractual proof that destruction actually occurred, leaving an unverifiable enforcement gap after the relationship ends.
- `mutual-nda-deep-missing-injunctive-relief-fail.txt` — replaces the injunctive-relief section with a generic "Remedies" clause that preserves "all remedies at law or in equity" but omits any acknowledgment of irreparable harm or entitlement to injunctive/equitable relief. **NDA-D-015 (critical)** fires because its present_patterns require one of "irreparable harm/injury", "injunctive relief/remedy", or "equitable relief" and none appears; without this clause the discloser must prove inadequate-remedy-at-law from scratch in any emergency motion, slowing emergency relief in a leak scenario.
- `mutual-nda-deep-missing-no-license-clause-fail.txt` — omits any clause stating that disclosure does not grant a license or ownership interest; the agreement says nothing about whether the Receiving Party acquires any right in the Confidential Information. **NDA-D-021 (critical)** fires because its present_patterns require "no license" or "does not grant / shall not be construed … license" and now neither appears; without this clause an aggressive receiver could argue an implied license arose from disclosure.

Each fail-fixture has a `.playbook` sidecar forcing the `mutual-nda-deep` playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins fifty-one fail-fixtures (was forty-eight); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1321 passing + 2 skips** (was 1312; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (45 → 48) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from forty-five end-to-end fail-fixtures to forty-eight by adding a new BAA breach-timing fixture, a new multi-state US DPA subcontractor-contract fixture, and a new vendor-MSA indemnity-procedure fixture — each exercises a distinct load-bearing rule not previously covered. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-unreasonable-delay-language-fail.txt` — rewrites the Reporting section so the breach-notification clause states only the 60-calendar-day outer bound from discovery, stripping every "without unreasonable delay" anchor. **BAA-022 (warning)** fires because 45 C.F.R. § 164.410(b) requires *both* "without unreasonable delay" *and* "in no case later than 60 calendar days"; without the inner timing standard a BA could lawfully notify on day 59 after every breach, eliminating the practical urgency HIPAA intended.
- `dpa-multi-state-us-missing-subcontractor-written-contract-fail.txt` — rewrites Section 7 (Sub-Processor Management) to require only prior notification and vetting of subprocessors, stripping every "written contract", "written agreement", or "same obligations" anchor. **USDPA-018 (critical)** fires because Va. Code § 59.1-579 and equivalent state statutes require the processor to engage subcontractors only pursuant to a written contract imposing equivalent obligations; without this clause the controller has no contractual guarantee that downstream subprocessors are bound by the same data protection requirements.
- `msa-vendor-deep-missing-indemnity-procedure-fail.txt` — strips the Section 7(c) Indemnification Procedure block; no "promptly notify", "control of the defense", or "settlement … consent" anchor appears in the indemnification section; the surviving text merely says the parties shall "reasonably cooperate." **MSA-002 (warning)** fires because its present_patterns require at least one procedural mechanic (notice, defense control, or settlement consent) and now none appears; without these mechanics the indemnitor loses the ability to control its own defense, raising moral-hazard and collusive-settlement risks.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins forty-eight fail-fixtures (was forty-five); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1312 passing + 2 skips** (was 1303; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (42 → 45) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from forty-two end-to-end fail-fixtures to forty-five by adding a new NDA independent-development fixture, a new BAA breach-discovery-trigger fixture, and a new multi-state US DPA compliance-demonstration fixture — each exercises a distinct load-bearing rule not previously covered. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-missing-independent-development-exclusion-fail.txt` — removes the "independently developed" prong (was prong (b)) from Section 2 (Carveouts); the final sentence of Section 2 affirmatively ropes in information generated by the Receiving Party's own personnel. **NDA-D-009 (warning)** fires because its present_patterns require "independently (developed|derived|created)" or "without (use of|reference to) (the )?confidential" and now neither appears; without this exclusion, ordinary parallel R&D by the Receiving Party's workforce could be captured as a breach merely because the same personnel had access to the Disclosing Party's information.
- `baa-missing-breach-discovery-trigger-fail.txt` — rewrites the breach-notification clause so the 60-day clock runs from "confirmation and written assessment that a reportable breach has occurred" rather than from "discovery of the breach", stripping every "discovery of the breach" and "breach.*discover" anchor. **BAA-021 (warning)** fires because 45 C.F.R. § 164.410(a)(2) defines the outer bound from "discovery" (not from a later confirmation or assessment event); shifting the trigger post-discovery can cause the covered entity to violate the 60-day statutory cap before the BA's clock even starts.
- `dpa-multi-state-us-missing-compliance-demonstration-fail.txt` — rewrites Section 3 (Virginia VCDPA Processor Obligations) to drop the "make available all information necessary to demonstrate compliance" sentence, replacing it with an internal-records clause; renames Section 12 from "Certification" to "Attestation" using "adherence" language that avoids the "demonstrate compliance" and "information necessary to demonstrate" patterns. **USDPA-019 (critical)** fires because Va. Code § 59.1-579 and equivalent state statutes require the processor to make information available demonstrating compliance; without this clause the controller has no contractual mechanism to obtain the compliance documentation required by the Applicable State Laws.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins forty-five fail-fixtures (was forty-two); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1303 passing + 2 skips** (was 1294; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (39 → 42) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from thirty-nine end-to-end fail-fixtures to forty-two by adding a new NDA third-party-exclusion fixture, a new vendor-MSA force-majeure fixture, and a new BAA cure-infeasible-termination fixture — each exercises a distinct load-bearing rule not previously covered. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-missing-third-party-exclusion-fail.txt` — removes the "third party lawfully obtained" prong from Section 2 (Carveouts); the last sentence actively reincorporates any information accessible from "any external party not operating under a separate written NDA". **NDA-D-008 (warning)** fires because its present_patterns require "third party" within ~80 chars of "without (breach|restriction)|lawfully" or "received/obtained from a third party" and now no such pairing appears; without this exclusion the Receiving Party risks breaching the NDA for using identical information it legitimately obtained from an independent source.
- `msa-vendor-deep-missing-force-majeure-fail.txt` — rewrites the force-majeure clause as a vendor-only "Excused Performance" paragraph (Vendor's obligations excused; Customer's payment obligations explicitly not excused) with no bilateral "neither party" or "either party" framing anywhere. **MSA-022 (info)** fires because its present_patterns require "(neither party|either party).{0,80}force majeure" or "force majeure.{0,160}(neither|both|each party)" and now neither pattern appears; a one-sided force-majeure clause is commercially abnormal and leaves the customer without contractual relief for its own performance obligations during a force-majeure event.
- `baa-missing-cure-infeasible-termination-fail.txt` — rewrites the Term and Termination section to allow only cure-period and insolvency termination, with no "cure is not feasible", "cure is infeasible", "infeasible to cure", or "fail.*cure" language anywhere in the document. **BAA-012 (warning)** fires because 45 C.F.R. § 164.504(e)(2)(iii) and HHS guidance expect BAAs to permit termination when cure of a material breach is not feasible; without this provision the covered entity may be left with no clear contractual exit when a systemic HIPAA violation cannot be remediated within the cure window.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins forty-two fail-fixtures (was thirty-nine); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1294 passing + 2 skips** (was 1285; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (36 → 39) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from thirty-six end-to-end fail-fixtures to thirty-nine by adding a new NDA carve-out fixture, a new vendor-MSA SLA fixture, and a new DPA Article 32 security-measures fixture — each exercises a distinct load-bearing rule not previously covered. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `mutual-nda-deep-missing-prior-knowledge-exclusion-fail.txt` — removes the "already known / prior to disclosure" prong from Section 2 (Carveouts) and adds an affirmative sentence roping in information "regardless of how or when" the Receiving Party's employees came to know of it. **NDA-D-007 (critical)** fires because its present_patterns require "already known", "prior to disclos", or "previously known" and now no such pattern appears; without this exclusion the Receiving Party risks breaching the NDA by using information they independently possessed before the relationship began.
- `msa-vendor-deep-missing-sla-fail.txt` — omits any "service level agreement", "SLA", "uptime", or "availability commitment" reference from a vendor-form MSA that is otherwise complete (warranty, indemnity, cap, termination). **MSA-016 (warning)** fires because its present_patterns require one of those four anchor families and now none appears; without an SLA reference, performance and availability promises are unenforceable and the customer has no contractual remedy for downtime.
- `dpa-controller-processor-missing-art32-security-measures-fail.txt` — replaces Section 6 (Security Measures) with a vague general-security-commitment paragraph, stripping every reference to "Article 32", "technical and organisational measures", "encryption", "pseudonymisation", "confidentiality, integrity, availability and resilience", "restore availability", and "regularly testing". **DPA-009 (critical)** fires because GDPR Art. 28(3)(c) requires the processor to take all measures required pursuant to Article 32; absent the statutory anchor and the enumerated TOMs the controller cannot verify that the processor has implemented the required security-of-processing framework.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins thirty-nine fail-fixtures (was thirty-six); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1285 passing + 2 skips** (was 1276; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (33 → 36) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from thirty-three end-to-end fail-fixtures to thirty-six by adding a third failure-mode fixture for DPA (controller-processor), a second audit-focused failure for multi-state US DPA, and a new compliance/non-infringement warranty fixture for vendor-form MSA — each exercises a distinct load-bearing rule not previously covered. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `dpa-controller-processor-missing-compliance-demonstration-fail.txt` — replaces Section 11 with an internal-records-only clause that explicitly restricts Controller access, stripping every "demonstrate compliance" and "allow for and contribute to audits" anchor. **DPA-014 (critical)** fires because GDPR Art. 28(3)(h) requires the processor to make available all information necessary to demonstrate compliance with Article 28 and to allow for and contribute to audits, including inspections, conducted by the controller or its mandated auditor.
- `dpa-multi-state-us-missing-audit-cooperation-fail.txt` — replaces the audit-cooperation clause with an annual security-program clause, stripping every "allow and cooperate with reasonable assessments" and "assessor" anchor. **USDPA-017 (critical)** fires because VCDPA (Va. Code § 59.1-579) and several state statutes require the processor to allow and cooperate with reasonable assessments by the controller or the controller's designated assessor; without this clause the controller has no contractual mechanism to verify processor compliance.
- `msa-vendor-deep-missing-compliance-noninfringement-warranty-fail.txt` — omits any "comply with applicable laws" or "non-infringement warranty" clause; Section 6 covers only workmanlike performance and malware-free deliverables. **MSA-014 (warning)** fires because its present_patterns require either a comply/compliance-with-laws anchor or a non-infringement warranty anchor, and now neither appears; without this warranty the customer has no contractual representation that vendor services comply with applicable law.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins thirty-six fail-fixtures (was thirty-three); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1276 passing + 2 skips** (was 1267; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (30 → 33) (2026-05-18) — ✅ complete

Advanced LAUNCH row v3-b coverage from thirty end-to-end fail-fixtures to thirty-three by adding a fifth failure-mode fixture for BAA and new fixtures for vendor-form MSA and mutual NDA — each exercises a distinct load-bearing rule not previously covered. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-accounting-of-disclosures-fail.txt` — rewrites the "Individual Rights" section to reference only 45 C.F.R. § 164.524 (access) and § 164.526 (amendment), stripping every "accounting of disclosures" and "164.528" anchor. **BAA-008 (critical)** fires because 45 C.F.R. § 164.504(e)(2)(ii)(G) requires the BA to maintain and make available the information required for an accounting of disclosures under § 164.528; without this hook the covered entity cannot meet its obligation to provide individuals with a history of disclosures.
- `msa-vendor-deep-missing-service-warranties-fail.txt` — rewrites Section 6 (Warranty) to a flat "AS-IS / AS-AVAILABLE" disclaimer, stripping every anchor for "workmanlike", "professional manner", "conforms to documentation / specifications", and "no malicious code / virus / trojan". **MSA-013 (warning)** fires because its present_patterns require at least one of the three service-warranty families to appear and now none does; without these warranties the customer has no contractual basis to demand professional performance, spec conformance, or malware-free deliverables.
- `mutual-nda-deep-missing-public-domain-exclusion-fail.txt` — removes the "publicly available" / "public domain" prong from Section 2 (Carveouts), leaving only the independently-developed, third-party-lawfully-received, and compelled-disclosure carve-outs. **NDA-D-006 (warning)** fires because its present_patterns require "(publicly|generally) (available|known)" or "(public domain|in the public)" and now neither appears; without this exclusion the Receiving Party could technically breach the NDA by referencing information freely available to the world.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins thirty-three fail-fixtures (was thirty); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1267 passing + 2 skips** (was 1258; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (27 → 30) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from twenty-seven end-to-end fail-fixtures to thirty by adding a fourth failure-mode fixture for three already-covered playbooks (BAA, mutual NDA, customer-form MSA). Each new fixture exercises a distinct load-bearing rule that prior fixtures did not. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-breach-notification-fail.txt` — rewrites the Reporting section to reference only "Security Incidents" and "impermissible access to PHI", stripping every anchor for "breach of unsecured PHI", "breach notification", and "164.410". **BAA-019 (critical)** fires because 45 C.F.R. § 164.410 requires the BA to notify the covered entity of a breach of unsecured PHI without unreasonable delay and in no case later than 60 calendar days after discovery; without the statutory anchor the notification obligation is legally ambiguous.
- `mutual-nda-deep-missing-ci-definition-fail.txt` — replaces the Section 1 "Confidential Information" definition with unformatted references to "non-public information" so no capitalized defined-term statement ("Confidential Information means …") appears anywhere. **NDA-D-005 (critical)** fires because without a definition the scope of the obligation is ambiguous and may be construed narrowly against the disclosing party.
- `msa-customer-deep-missing-data-return-fail.txt` — removes Section 11 (Data Portability and Return on Termination) entirely so no "return all Customer Data", "data portability", or "machine-readable export" anchor remains. **MSA-021 (warning)** fires because without an explicit data-return obligation the customer can be locked out of its own data after the relationship ends.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins thirty fail-fixtures (was twenty-seven); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1258 passing + 2 skips** (was 1249; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (24 → 27) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from twenty-four end-to-end fail-fixtures to twenty-seven by adding a third failure-mode fixture for three already-covered playbooks (BAA, vendor-form MSA, mutual NDA). Each new fixture exercises a distinct load-bearing rule that prior fixtures did not. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-termination-for-breach-fail.txt` — rewrites the termination section to allow only convenience-termination and insolvency-termination, stripping every "material breach", "terminate...breach", and "breach...termination" anchor (the Reporting section is also reworded to avoid "Breaches"). **BAA-011 (critical)** fires because 45 C.F.R. § 164.504(e)(2)(iii) requires the covered entity to have the right to terminate the BAA if the business associate has breached a material term of the agreement; without the clause the covered entity has no contractual trigger.
- `msa-vendor-deep-missing-cap-carveouts-fail.txt` — removes the Section 8(b) carve-outs block from the Limitation of Liability so the aggregate cap now absorbs confidentiality-breach, indemnification, gross-negligence, and wilful-misconduct claims with no exception. **MSA-007 (warning)** fires because the commercial drafting baseline requires cap carve-outs for fraud / wilful misconduct / IP indemnity / confidentiality / data protection; without them the vendor's unlimited-damages exposure for fundamental breaches is absorbed by the cap.
- `mutual-nda-deep-missing-perpetual-trade-secret-fail.txt` — rewrites Section 5 to impose a flat three-year term on all confidentiality obligations with no perpetual carve-out for trade secrets. **NDA-D-004 (warning)** fires because its present_patterns require "trade secret" within ~120 chars of "as long as / so long as / in perpetuity / perpetual / qualifies as" and now no such pairing appears; without the carve-out the discloser loses statutory UTSA / DTSA protection once the three-year term lapses.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins twenty-seven fail-fixtures (was twenty-four); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1249 passing + 2 skips** (was 1240; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (21 → 24) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from twenty-one end-to-end fail-fixtures to twenty-four by adding a second failure-mode fixture for three already-covered playbooks (customer-form MSA, mutual NDA, multi-state US DPA). Each new fixture exercises a distinct load-bearing rule that prior fixtures did not. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `msa-customer-deep-missing-termination-clause-fail.txt` — rewrites Section 10 (Termination) to remove the termination-for-cause / material-breach / cure-period clause (replaced with convenience-only and insolvency termination). **MSA-018 (warning)** fires because the commercial drafting baseline requires an explicit material-breach termination right with a cure period; without it the customer is forced into a common-law theory rather than a clear contractual trigger.
- `mutual-nda-deep-missing-return-or-destruction-fail.txt` — removes Section 7 (Return or Destruction) from the mutual NDA entirely, so no "return or destroy" / "destruction of confidential" / "destroy all copies" anchor remains. **NDA-D-013 (critical)** fires because without a return-or-destruction clause the discloser has no contractual hook to recover or wipe disclosed material once the relationship ends.
- `dpa-multi-state-us-missing-confidentiality-duty-fail.txt` — rewrites the Virginia VCDPA processor obligation in Section 3 to replace "bound by confidentiality" with "subject to appropriate access controls and security training", so no "duty of confidentiality" / "committed to confidentiality" / "bound by confidentiality" anchor remains anywhere. **USDPA-016 (warning)** fires because every state privacy statute requires the processor to subject authorized personnel to a duty of confidentiality.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook. [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins twenty-four fail-fixtures (was twenty-one); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1240 passing + 2 skips** (was 1231; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (18 → 21) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from eighteen end-to-end fail-fixtures to twenty-one by adding a second failure-mode fixture for three already-covered playbooks (BAA, controller-processor DPA, vendor-form MSA). Each new fixture exercises a distinct load-bearing rule that prior fixtures did not — exercising the regex on a different §164.504(e) prong / Art. 28(3) prong / commercial-baseline rule rather than re-exercising rules already covered. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-missing-hhs-books-records-fail.txt` — strips the "Books, Records" paragraph (the HHS Secretary books-and-records availability clause). **BAA-009 (critical)** fires because 45 C.F.R. § 164.504(e)(2)(ii)(H) requires the BA to make its internal practices, books, and records available to the Secretary of HHS for compliance determination; with the paragraph removed, no "internal practices ... books ... records" / "HHS Secretary" / "Secretary of Health and Human Services" anchor remains.
- `dpa-controller-processor-missing-personnel-confidentiality-fail.txt` — rewrites Section 5 (Confidentiality of Processing Personnel) to a generic "Personnel Training" clause that drops the confidentiality-commitment phrase, and strips the ancillary "ongoing confidentiality" wording from the Section 6 Article 32 security-measures list so no other clause backstops the anchor. **DPA-008 (critical)** fires because GDPR Art. 28(3)(b) requires persons authorised to process personal data to be committed to confidentiality or under a statutory duty of confidentiality; the regex needs `committed (themselves) to confidentiality` / `duty of confidentiality` / `bound by confidentiality` and now matches nowhere.
- `msa-vendor-deep-missing-background-foreground-ip-fail.txt` — rewrites Section 4 (Intellectual Property Ownership) to drop the "pre-existing intellectual property" / background-IP allocation anchor (renaming "Vendor IP" → "Vendor Materials") and leaves no "foreground IP" / "developed hereunder" / "developed under this Agreement" anchor anywhere in the document. **MSA-011 (warning)** fires because the commercial drafting baseline requires explicit background/foreground IP allocation; without it, default copyright / patent rules govern and rarely match the parties' intent.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook (so the auto-detector cannot route to a v2 fallback). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins twenty-one fail-fixtures (was eighteen); each entry asserts the load-bearing rule is in the fired set so a regex regression that silently stops the rule from firing is caught immediately. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1231 passing + 2 skips** (was 1222; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus expansion (16 → 18) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from sixteen end-to-end fail-fixtures to eighteen, adding one per remaining under-covered v3 playbook with a dedicated ADDENDA-* rule (SaaS ToS warning, Privacy-Policy-Lint warning). The last gap — `coi` — carries no v3 rules (the COI playbook is text-extraction-driven and validated through the compliance matrix rather than presence rules), so a `coi-*-fail.txt` is deferred until the ACORD-25 spatial extractor lands. New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `saas-tos-no-click-to-cancel-fail.txt` — rewrites Section 3 (Auto-Renewal) to require phone-only cancellation during business hours and strips every "click-to-cancel" / "cancel online" / "cancel through your account" / "ROSCA" anchor; also removes the FTC Negative Option Rule reference. **ADDENDA-019 (warning)** fires because its `present_patterns` regex requires either an online-cancellation phrase or an explicit ROSCA reference, and now neither matches anywhere in the document. A consumer SaaS that gates cancellation behind phone-only business-hours support fails the FTC Click-to-Cancel rule.
- `privacy-policy-lint-missing-disclosures-fail.txt` — replaces enumerated "Categories of Personal Data" with vague "items we gather" boilerplate; strips every "lawful basis" / "legitimate interest" / "consent" / "legal obligation" reference (Section 9 removed entirely); strips every "right to access / delete / portability / opt-out / object" enumeration and the "data-subject rights" anchor (Section 6 collapsed to "available choices vary by jurisdiction"). **ADDENDA-020 (warning)** fires because its `present_patterns` regex requires all three CCPA § 1798.130 / GDPR Art. 13–14 / data-subject-rights anchor families, and now none appears.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook (so the auto-detector cannot route to a v2 fallback). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins eighteen fail-fixtures (was sixteen); each entry asserts the load-bearing rule is in the fired set. Goldens regenerated. `npm run lint` clean, `npm run typecheck` clean, `npm test` green, `npm run build` green.

### v3 fail-fixture corpus expansion (13 → 16) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from thirteen end-to-end fail-fixtures to sixteen, adding one per remaining under-covered v3 playbook (Subcontractor BAA critical, Customer-form MSA warning, Multi-state US DPA critical). New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `baa-subcontractor-missing-return-or-destruction-fail.txt` — strips the "Return or Destruction" section and rewords the term-survival sentence to drop the "return or destroy" anchor. **BAA-010 (critical)** fires because 45 C.F.R. § 164.504(e)(2)(ii)(I) requires the BA, at termination, to return or destroy all PHI received from or created on behalf of the covered entity.
- `msa-customer-deep-missing-ip-indemnity-fail.txt` — removes the third-party IP infringement indemnification prong from the Vendor Indemnification section, leaving only breach and gross-negligence/wilful-misconduct prongs. **MSA-001 (warning)** fires because the commercial drafting baseline requires the vendor to indemnify customers for third-party IP claims arising from the deliverables.
- `dpa-multi-state-us-missing-deletion-or-return-fail.txt` — strips the "Deletion on Termination" section and reworks the VCDPA processor-obligations section to drop "delete or return". **USDPA-015 (critical)** fires because VCDPA / CPA / CTDPA all require the processor to delete or return personal data at end-of-services.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook (so the auto-detector cannot route to a v2 fallback). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins sixteen fail-fixtures (was thirteen); each entry asserts the load-bearing rule is in the fired set so a regex regression that silently stops the rule from firing is caught immediately. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`.

### v3 fail-fixture corpus expansion (10 → 13) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from ten end-to-end fail-fixtures to thirteen, adding one per remaining under-covered v3 playbook (Addenda EULA surface, Transfer SCC Module 3 critical, DPA processor-subprocessor critical). New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `eula-no-license-grant-or-prohibitions-fail.txt` — strips both the explicit license-grant scope ("non-exclusive / non-transferable / revocable license") and the prohibited-uses enumeration ("shall not reverse engineer / decompile / disassemble / sublicense"). **ADDENDA-017 (warning)** fires because neither of its `present_patterns` alternatives matches.
- `scc-module-3-missing-clause-15-fail.txt` — keeps the EU SCC Module 3 (Processor-to-Processor) shape but removes the entire "Clause 15 — Public Authority Access" block. **TRANSFER-008 (critical)** fires because the data importer's Schrems II notify-and-challenge posture is absent.
- `dpa-processor-subprocessor-missing-deletion-or-return-fail.txt` — strips Section 11 (Deletion or Return at End of Services) from the processor-subprocessor DPA. **DPA-013 (critical)** fires because GDPR Art. 28(3)(g) requires the controller's choice between deletion and return of personal data at end-of-services.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook (so the auto-detector cannot route to a v2 fallback). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins thirteen fail-fixtures (was ten); each entry asserts the load-bearing rule is in the fired set so a regex regression that silently stops the rule from firing is caught immediately. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`.

### v3 fail-fixture corpus expansion (7 → 10) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from seven end-to-end fail-fixtures to ten, adding one per remaining under-covered v3 ruleset family (NDA-deep presence, MSA-deep presence, Transfer UK-Addendum critical). New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `unilateral-nda-deep-missing-term-fail.txt` — removes the entire "Term" section so no `<N> years` window pairs with `confidential|disclos`. **NDA-D-003 (critical)** fires because its `present_patterns` regex requires a duration phrase near confidentiality language and now matches nothing in the document.
- `msa-vendor-deep-no-liability-cap-fail.txt` — strips Section 8(a) (aggregate liability cap) and the cap-carve-out subsection 8(b); only the mutual consequential-damages waiver and indemnification procedure survive. **MSA-006 (critical)** fires because no `aggregate liability` / `total liability` cap phrase is present — unbounded vendor exposure.
- `uk-idta-addendum-modified-mandatory-clauses-fail.txt` — keeps the UK Addendum Tables 1–4 shape but attaches an Annex IV side letter that purports to modify ICO Mandatory Clauses (replacing Clause 12 liability, amending Clause 16 notification, modifying Clause 9 sub-processor authorisation). **TRANSFER-015 (critical)** fires because the rule's bad-pattern regex catches `Mandatory Clauses ... as modified` / `with the exception` — the ICO template's Mandatory Clauses are invariable; only Tables 1–4 may be completed.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook (so the auto-detector cannot route to a v2 fallback). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins ten fail-fixtures (was seven); each entry asserts the load-bearing rule is in the fired set so a regex regression that silently stops the rule from firing is caught immediately. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`.

### v3 fail-fixture corpus expansion (4 → 7) (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b coverage from four end-to-end fail-fixtures to seven, adding one per under-covered v3 ruleset (Addenda warning surface, Transfer critical surface, US state-DPA critical surface). New fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/):

- `vendor-security-addendum-missing-incident-window-fail.txt` — strips the explicit "twenty-four (24) hours" notification window and replaces it with vague "promptly and without undue delay" language. **ADDENDA-004 (warning)** fires because its `present_patterns` regex requires `within \d+ (hours|days)` and no longer matches the incident-notification paragraph.
- `scc-module-2-modified-clauses-fail.txt` — keeps the EU SCC Module 2 shape but attaches an Annex IV "side letter" that purports to modify Clauses 8 / 12 of the SCCs. **TRANSFER-003 (critical)** fires because the rule's bad-pattern regex catches "as modified" / "as amended" / "notwithstanding any provision of the SCCs" — material modification is forbidden by Clause 2 (Effect and Invariability).
- `dpa-ccpa-service-provider-no-business-purpose-fail.txt` — claims Service Provider status under the CCPA but does not contain the § 7051 "specific business purpose" or "same level of privacy protection" anchors. **USDPA-020 (critical)** fires via its negative-lookahead regex; the recipient may be reclassified as a "third party," triggering sale / share consequences for the disclosing party.

Each fail-fixture has a `.playbook` sidecar forcing the v3 playbook (so the auto-detector cannot route to a v2 fallback). [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) now pins seven fail-fixtures (was four); each entry asserts the load-bearing rule is in the fired set so a regex regression that silently stops the rule from firing is caught immediately. Goldens regenerated via `VAULYTICA_REGEN_GOLDEN=1`. Full gate green: `npm run lint && typecheck && test && build` — **1189 passing + 2 skips** (was 1180 + 2; +9 new tests: 3 golden-match, 3 determinism, 3 sanity guards).

### v3 fail-fixture corpus seed + playbook-schema relaxation (2026-05-17) — ✅ complete

Advanced LAUNCH row v3-b from "unit tests only" to "four end-to-end fail-fixtures committed". Discovered en route that 15 of the 19 v3 playbooks were silently failing Zod validation — `expected_clauses` / `expected_defined_terms` ship as `string[]` (not `{category|term, severity_if_missing}[]`) and `sources` ships as structured citation objects (not `string[]`). The v3 golden harness's `try { parsePlaybook(...) } catch {}` swallowed the failures, so v3 fixtures had been silently running under v2 fallback playbooks and their v3 rules weren't firing. Fix in [`src/playbooks/types.ts`](src/playbooks/types.ts): `PlaybookSchema` now accepts either shape for `expected_clauses` / `expected_defined_terms` / `sources` and coerces strings → `{category|term, severity_if_missing: "warning"}` and citation objects → their `id` string. New fail-fixtures under [`tests/golden/v3/fixtures/`](tests/golden/v3/fixtures/): `baa-missing-subcontractor-flow-down-fail.txt` (fires BAA-018), `mutual-nda-deep-missing-dtsa-fail.txt` (fires NDA-D-001 + NDA-D-002), `dpa-controller-processor-missing-documented-instructions-fail.txt` (fires DPA-007 + DPA-006 + cascading TRANSFER/USDPA criticals), `ai-addendum-training-without-optin-fail.txt` (fires ADDENDA-011 critical + IPDATA-009). New [`tests/golden/v3/fixture-sanity.test.ts`](tests/golden/v3/fixture-sanity.test.ts) (4 tests) pins each fail-fixture's load-bearing critical rule so a future regex regression that silently stops a critical from firing is caught immediately. All 19 v3 pass-goldens regenerated (the playbook_id now resolves to the correct v3 playbook for the first time, e.g. `baa` instead of v2 fallback `mutual-nda`); all 15 v4 goldens regenerated for the same reason. Full gate green: `npm run lint && typecheck && test && build` — **1180 passing + 2 skips** (was 1161 before).

## Outstanding TODOs from earlier steps

- **Step 0:** `npm install` has not yet been run in this environment — verify locally before declaring the verification gate passed.
- **Step 1:** Lighthouse 95+ on all categories needs to be confirmed in a real browser run. The architecture diagram is inline SVG (not Mermaid-source) because the spec explicitly forbids external dependencies; equivalence to the Mermaid in Appendix A should be eyeballed when reviewing.
- **Step 2:** No binary fixture PDF or DOCX is committed yet — unit tests cover `normalize`, `paste`, `hash`, and the pure `parseDocxHtml` path. End-to-end coverage of `ingestPdf` / `ingestDocx` will land alongside the Common Paper fixtures in Step 16. Verify `npm install && npm test src/ingest` once locally.
- **Step 3:** OCR cannot be unit-tested without a scanned-PDF fixture; smoke-test in a real browser when Step 12 wires up the UI, using the synthetic scanned NDA from Step 16.
- **Step 4:** Tests cover happy paths and one or two interesting edge cases per extractor. Cross-extractor integration coverage and full-document deterministic-hash tests will land in Step 6 (rule engine) and Step 16 (golden-output fixtures). The classifier exercises pattern + TF-IDF + tie-break paths with synthetic data; real-world accuracy depends on the DKB vocab produced in Step 11.
- **Step 5:** The starter DKB's `dkb-classifier-vocab.json` is intentionally empty — the TF-IDF vocabulary is produced by the Step 11 build pipeline from CUAD + LEDGAR. The hand-authored regex patterns in `dkb-classifier-patterns.json` carry the classifier until then. Statutory excerpts are short and accurate but should be re-verified during the Step 11 automated rebuild. Jurisdiction non-compete enforceability buckets follow widely reported state law; nuanced cases (e.g., Washington's RCW 49.62 income threshold, Texas's narrow enforceability under Bus. & Com. Code § 15.50) should be expanded in the per-state notes column added when the build pipeline lands.
- **Step 6:** Spec calls for each rule to live in its own folder; this scaffold puts each rule as `src/engine/rules/<category>/<RULE-ID>.ts` directly (no per-rule folder). Fixture files are not yet broken out per rule — the synthetic-input test pattern in `rules/rules.test.ts` covers positive + negative cases inline. Refactoring to per-rule folders is mechanical and can land alongside Step 7 if the convention is preferred.
- **Step 7:** Detection regexes for the 68 new rules are intentionally conservative; real-world precision/recall will be measured against the golden-output fixtures in Step 16. The coverage test exercises ~40 of the 68 rules with focused positive inputs — the remaining 28 (mostly informational surfacers like RISK-002 asymmetry detection or DARK-002 buried-notice detection) are covered indirectly by the end-to-end determinism test in `all-rules.test.ts` and will gain bespoke tests as fixture contracts arrive.
- **Step 16:** The four fixtures are synthetic; the launch-corpus list in spec §26 step 16 also calls for real Common Paper templates, the GitHub Balanced Employee IP Agreement, an open-licensed commercial lease, and a scanned-PDF variant of the clean NDA. Vendoring those requires network access (GitHub clones) and a print-to-image step (the scanned PDF), so they're tracked in `tests/fixtures/README.md` and the golden test will pick them up automatically once they land in `contracts/`. The runner-determinism fix (`elapsed_ms` blanked when hashing) is load-bearing for the regression check — without it, every fixture run on a different machine would have produced a different `result_hash` and the spec §17 determinism guarantee would have been false. If you bump the engine, re-run `npm run fixtures:regen-golden`, review the diff in `tests/fixtures/expected/`, and commit deliberately — never auto-accept.
- **Step 14:** The deploy workflow expects `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` secrets — those must be set on the repo before the first deploy. The Pages project itself (custom domain `vaulytica.com`, HTTPS enforced, "Always Use HTTPS") is configured in Cloudflare's UI, not via Wrangler; spec §26 step 14 calls that out. `wasm-unsafe-eval` is in `script-src` because pdfjs-dist and tesseract.js compile WebAssembly at runtime — strictly speaking the spec's CSP omits this, but the alternative is the analysis pipeline refusing to instantiate the OCR worker. Use `securityheaders.com` against the live deploy to confirm an A rating once the domain is live. The 500 KB chunk warning from the build is an optimization opportunity for Step 15/16 — pdfjs and tesseract are heavy, and dynamic-importing the OCR fallback is the lowest-hanging fruit. Playwright browsers are only installed in CI (`npm run e2e:install`) to keep local installs slim; running `npm run e2e` locally needs that step too.
- **Step 13:** The service worker scope is the entire origin (`/`). On Cloudflare Pages, the `/sw.js` URL is what the browser uses; on dev it's the same. The PWA install requirements (Chrome Lighthouse) need a 192+ and 512+ PNG and a maskable variant — all three are now committed. Step 14's deploy step copies `site/icon-*.png` + `manifest.webmanifest` to `dist/` automatically via Vite (anything outside `index.html`'s `<script>`/`<link>` graph that lives directly in `site/` is treated as a static asset and copied). If a future Vite upgrade stops auto-copying root-level PNGs, add them to `publicDir`. The vm-based service-worker tests stub `caches`/`self`/`fetch` minimally — fidelity is "the strategy branches correctly," not "Workbox-grade correctness." The DKB stale-while-revalidate intentionally keeps serving old manifests; the surface contract is "the next page load picks up the new DKB" rather than "this load surfaces the new DKB," which is the right tradeoff for the offline-first promise.
- **Step 12:** The pipeline runs in the browser only — Node-environment tests cover unit behavior but not the live flow. Open `npm run dev` and drop a real .pdf / .docx onto the marketing page to smoke-test. The classifier inference path currently passes an empty vocab map (`{ vocab: {} }`) plus the DKB-shipped patterns — once Step 11's `dkb-classifier-vocab.json` lands with real weights, change `extractAll`'s classifier options to read `dkb.classifier.vocab` directly. The `ensurePlaybooks` fetcher loops over `LAUNCH_PLAYBOOK_IDS` in order; this is sequential by design (the playbooks are tiny and a parallel `Promise.all` adds no measurable latency) but could be flipped if profiling shows otherwise. The dev middleware in `vite.config.ts` reads file contents per request without any caching — fine for dev, but if local DKB sizes balloon it should switch to `fs.readFileSync` on init + an in-memory map. Production headers (`_headers` CSP) are Step 14's concern; the dev middleware sets only `Content-Type`.
- **Step 19:** The starter DKB (`dkb/dist/v0.0.1-starter/`) is unchanged — v3 nodes live in their own files (`dkb/fixtures/v3/nodes/`) and are validated through `V3DkbNodeListSchema`. The v2 manifest does not yet reference them; Steps 21–22 will publish them under `dkb/dist/<version>/v3/`. The `dkb_node_version` integer is bumped only on breaking changes to a node's contract — additive optional fields can land without a bump.
- **Step 26:** The transfer ruleset uses three rule-scope configs (`CONFIG_SCC`, `CONFIG_UK`, `CONFIG_ALL`) to keep SCC-specific clauses from firing under the UK Addendum playbook (where the SCCs are incorporated by reference) and vice versa. This is the first ruleset to split scope by playbook within a single category — subsequent rulesets that bridge multiple regulators should follow the same pattern. The DPF detector (TRANSFER-017) is a warning per spec §31 because the EU-US Data Privacy Framework is the operative adequacy decision but permanently under litigation; downgrading to fail would gate normal commerce. The "modified SCCs" detector (TRANSFER-003) is intentionally aggressive — it matches "as modified", "as amended", "notwithstanding the SCCs", "with the exception" within 80 chars of "SCC"/"Standard Contractual Clauses". False positives are possible but the surface cost is low and Clause 2 is unforgiving. TRANSFER-016 was widened to accept either IDTA Part 1 *or* UK Addendum Table 1 so the same rule serves both UK transfer modes. TRANSFER-020 (onward transfers) is warning-level because the UK Addendum incorporates Clause 8.8 by reference.
- **Step 25:** USDPA-020 (claimed-Service-Provider-without-required-elements) uses a negative-lookahead regex: it fires when the document declares Service Provider status but does not contain "specific business purpose" or "same level of privacy protection" within 500 chars. The 500-char window is conservative enough to catch most patterns; if false-positives accumulate on real fixtures the window can be widened. USDPA-021 (multi-state strictness floor) is info-severity by design — the rule surfaces the risk without flagging a fail, because the actual strictest-floor analysis requires per-state requirement comparison that will land alongside Step 25's full compliance-matrix renderer. The seven state-statute snapshot fixtures from Step 21 already validate the rule citations; per-state rule expansion (e.g., a USDPA-VA-001 family) can land later as fixtures arrive.
- **Step 24:** The 55 rules use a shared generic factory at `src/engine/rules/v3/_regulated-rule.ts` rather than the BAA-specific `baa/_helpers.ts`. BAA's helpers still exist (no behavior change) but Step 25's US-state ruleset and Step 26's transfer ruleset will adopt the generic factory directly. SCC-Module-2 currently re-runs the full DPA-GDPR ruleset — the spec §41 SCC-Module-2-specific "exact-text comparison of SCC clauses" check is deferred to alongside the EU SCC `regulator_model_form` Annex-level extension (Step 22 followup note). The compliance-matrix columns on `dpa-controller-processor.json` are anchored to Article 28(3)(a)–(h) plus Articles 28(2), 32, 33(2), and Chapter V — the column mapping to rule ids will be wired by Step 32's report renderer. The `applies_to_playbooks` filter is verified by the inert-when-not-DPA test, so the v2 launch-suite `result_hash` is unchanged.
- **Step 23:** The 45 rules live in a single `rules.ts` instead of 45 individual files. Spec §6 step-6 note already flagged that the v2 scaffold puts each rule directly in `src/engine/rules/<category>/<RULE-ID>.ts` rather than the spec's per-rule-folder convention; the BAA ruleset takes one further step toward compactness because all 45 share the presence/language helper, making per-file copies pure boilerplate. Each rule's `id` (BAA-NNN), `citation`, `present_patterns` / `bad_patterns`, and recommendation text are individually reviewable in `rules.ts`. The detection regexes are intentionally conservative; real-world precision/recall calibration happens when the BAA fixture corpus from §15 lands (Step 34 golden tests). The pipeline integration appends `V3_RULES` after `LAUNCH_RULES` — the runner sorts lexicographically by id at execution time, so BAA-NNN rules cluster together in the execution log irrespective of import order. The CI matrix test (`test-matrix.yml`) uses the same `npm test` invocation and runs across Ubuntu/macOS/Windows — adding v3 BAA rules touches no OS-specific code so cross-OS determinism is unchanged for the v2 hash boundary, and the v3 determinism check in `baa-ruleset.test.ts` is the start of an analogous v3 hash boundary that Step 34 will extend.
- **Step 22:** The EU SCC parser detects all four Modules by regex but the spec calls for full per-clause parsing of Annexes I/II/III; the current implementation emits one `regulator_model_form` node per module with the three annexes as clause entries. When Step 24's DPA-GDPR ruleset lands, the SCC-Module-2 playbook (spec §41) will need annex-level clause access; that's a straightforward extension to the `ANNEX_CLAUSES` array. The EDPB index parser is intentionally light (regex over "Guidelines NN/YYYY") — spec §6 explicitly says "citation to document, not paragraph" because EDPB documents are not stable enough for paragraph-level pinning. The PIPL translation provenance is recorded as the `authority` field on every emitted node ("National People's Congress official English translation, 2021"); switching translations later is one line in `INTL_SOURCES.pipl.authority_label`. The Swiss Addendum URL (`edoeb.admin.ch`) is the FDPIC's published landing page; if the FDPIC's URL changes, update `SWISS_ADDENDUM_URL` and re-run `write-snapshots.ts`. The 4 international fetchers (PIPEDA/LGPD/APPI/PIPL) all emit a single contract-requirement node by design; subdivide per Step 25's multi-state-style overlays if rule-level granularity is needed.
- **Step 21:** Parsers extract requirements by matching hand-keyed regexes against snapshot text rather than fully parsing every statute — that's the right level of fidelity given the spec's "one structured `statutory_clause_requirement` set per source" framing, and it keeps the gate deterministic. The snapshot fixtures are concise representative excerpts authored from each source's published text; they exist to exercise the parsers offline and to feed the Step-20 staleness gate, not to be the canonical statute corpus. When Step 22's network-capable orchestrator lands, `write-snapshots.ts` can be replaced with a live fetcher that calls each source's HTTP endpoint and refreshes the snapshots. The HHS OCR resolution summaries directory (`dkb/fixtures/v3/ocr-resolutions/`) is created but empty — populating with real OCR settlement summaries is tracked alongside Step 22. The 7 state-privacy fetchers all emit a single processor-contract node by design; subdividing each state's requirements into per-paragraph nodes is straightforward (add entries to the relevant `STATE_PRIVACY_SOURCES[state].requirements` array) and can land alongside Step 25's rule work.
- **Step 20:** The CLI (`dkb/build/v3/check-staleness.ts`) is wired but not yet attached to a `npm run` script — Step 21's fetcher work is the natural place to add `dkb:check-staleness` since that's when real authority text first lands. The offline-mode snapshot scheme (`dkb/fixtures/v3/snapshots/{sha256(url)}.txt`) is content-addressed by `source_url` so the same key works for in-pipeline fetches and replay tests. The UI footer fetches `/dkb/v3/validation-status.json`; until Steps 21–22 land, that file is absent and the footer keeps its `—` / `0` placeholders, which is the right state for a brand-new repo. The build pipeline gate (non-zero exit on unacknowledged drift) is enforced at the `runStalenessCheck`/CLI layer only — wiring it into `build.ts` is one line and lands in Step 21 alongside the first real fetcher.
- **Step 11:** The orchestrator (`build.ts`) is end-to-end wired but has not been executed in this environment because every fetcher needs network access — the CI run is the verification gate. Until Step 16's golden-output fixtures land, the regression harness returns `[]` for a missing `tests/fixtures/contracts/` directory (test covers this branch). The classifier vocab is trained over the union of CUAD + LEDGAR + EDGAR examples, but the starter DKB ships an empty vocab file by design; the first `dkb:build` after Step 11 produces the first real `dkb-classifier-vocab.json`. The GitHub Action uses `actions/cache` keyed by `${{ github.run_id }}` so each run starts from a warm cache when restore-keys match — production should consider switching to a content-keyed cache if the rebuild starts hitting the action cache size cap. The 5 MB classifier-asset budget from §13 is not enforced at build time — add a size guard alongside the schema validation in Step 16 / Step 17.
- **Step 10:** None of the eight fetchers have been hit against live endpoints from this environment — verification is by parser-fixture tests + the cached-result smoke run (`npm run dkb:fetch -- <id>`) once network access is available. The EDGAR sample cap is intentionally low (200 hits) so the GitHub Action stays well under its 30-minute budget at first run; Step 11's orchestrator should raise this once the regression baseline stabilizes. ULC PDF text extraction is staged but not wired — Step 11 owns the pdfjs-dist call that fills `excerpt` from the cached PDF bytes. The `git`-based `github-clone` fetch_method is implemented via raw GitHub URL pulls rather than a true clone; this avoids needing `git` in the runner and keeps everything content-addressed by URL. If a Common Paper template moves outside the README this approach will miss it — the orchestrator can fall back to the GitHub Contents API once templates expand.
- **Step 9 (RESOLVED 2026-05-24):** ✅ The obligations ledger and extracted-data appendix follow-up is closed. `buildDocxReport` now accepts an optional sixth `extracted?: ExtractedData` parameter, threaded through both `src/ui/pipeline.ts` call sites. The Extracted Data Appendix renders parties / dates / amounts / defined-terms / jurisdictions tables when extracted is provided, and the Obligations Ledger surfaces the full obligor / modal / action / trigger-qualifier ledger. Legacy counts-only fallback retained for callers that omit the extracted payload. New tests in `src/report/docx.test.ts`. The Word / Pages / Docs visual-rendering pass still requires a real browser session. The unused `ImageRun` import is kept (with `void ImageRun;`) so the eventual mint-svg wordmark embedding in the cover is a one-line change. The `Packer.toBlob` API is the supported docx@^9 path; if a future docx upgrade renames it the change is local to `docx.ts`.
- **Step 8:** A short pre-existing failure set survives in the broader suite (extractors and TEMP-001/runner determinism) — unrelated to playbook work and worth chasing alongside Step 9. The matcher's `matchPlaybook` signature accepts an optional `{title, body_text}` to pass title/body separately; spec §26 step 8 only names `extracted` and `classified`, so the UI hookup in Step 12 will derive these from the document tree. `present_categories` matching uses `ClassifiedParagraph.category`, so playbook `required_clauses` must use the unified taxonomy used by `dkb-classifier-patterns.json` for the match to count — playbook category strings outside that taxonomy will be silently ignored until the Step 11 vocab arrives. The per-feature score cap (2x for title/required, 3x for distinguishing) is conservative — adjust during Step 16 fixture calibration if real-world contracts produce stickier ambiguities.

## spec-v9 — The Last Look

- **Steps 148–154 (Thrust A — Clean to Send, shipped 2026-06-09, 8.1.0):** The pre-disclosure scan. A new read surface `src/delivery/` over the document's *original container bytes* — the facts the normalizing ingest (mammoth `convertToHtml`) throws away before any rule runs. `container.ts/readContainer` is a **total** function (never throws, never hangs) that inflates only the four handoff-relevant OOXML parts (`word/document.xml`, `word/comments.xml`, `docProps/core.xml`, `docProps/app.xml`) under the v8 zip-bomb guards (decompression-ratio ceiling, per-part byte cap), parses revisions (`w:ins`/`w:del`/`w:move*`), comments, hidden runs (`w:vanish` + retained `w:delText`), and metadata via **linear** regexes (the repo's ReDoS-free guarantee holds), and best-effort PDF Info-dictionary fields (literal + hex strings). `sensitive.ts` scans the flattened text for SSN (structural), EIN, Luhn-card, ABA-routing, context-gated DOB, and lower-confidence email/phone; every value is **masked** before storage (the §Part XIV hard invariant — asserted by a test that greps the serialized findings for raw values). `handoff.ts` derives `HANDOFF-001…005`; cross-matter metadata detection (`HANDOFF-004`) elevates a `Company`/`Manager`/`Template` entity absent from the engine-extracted party set to critical, but a bare person-name field never elevates on its own (spec §12). `report.ts/buildDeliveryReport` aggregates into a `DeliveryReport` with its own `delivery_hash` over the canonical facts+findings — **namespaced apart from the engine `result_hash`** (v8 Step-146 precedent), so a text-only / metadata-clean document yields an empty report and **no golden re-baselines**. Wired into: the JSON report (`delivery` block, `buildJsonReport`'s 6th arg), the CLI (`--delivery`, prints a presence-only summary line), and the tab (`prepareDocument` runs the scan once over the held buffer, independent of the rule run; `states.ts/renderDelivery` shows a "Clean to send?" section above the counts with mobile-safe CSS). Fixtures are deterministic OOXML builders (`_fixtures.ts`, `fflate.zipSync`) — no real document committed. +29 tests (totality over truncated/malformed/authorless/not-a-zip/empty inputs, the masking invariant, determinism of `delivery_hash`, PDF Info parsing, the presence-only honesty contract). **Open notes:** PDF tracked-change/comment recovery from markup annotations is a documented no-op (the report says so honestly, never a clean bill); the CLI's mammoth ingest errors under Node 25 + tsx in this local env (pre-existing, unrelated to delivery — CI runs Node 22). Thrusts B (155–159) and C (160–164) and the close (165) remain proposed.

## spec-v10 — Negotiation Posture

- **Thrust A — The tiered-position ladder (shipped 2026-06-13, 9.1.0):** A custom playbook can carry `negotiation_positions` — one entry per negotiable dimension, each an `ideal` and `acceptable` predicate drawn from the v6 §9 DSL plus optional per-tier guidance. [`evaluateNegotiationPosture`](src/playbooks/custom-interpreter.ts) classifies each into a `NegotiationTier` (ideal · acceptable · below-acceptable · unevaluable) by reusing the **same** pure `evaluatePredicate`, so there is no new fuzzy logic; below-acceptable fires only when both tiers are evaluable and both fail (never a false walk-away). The `NegotiationPosture` carries its own `posture_hash`, namespaced apart from the engine `result_hash` (additive — a run with no positions yields no posture and moves no golden). Surfaced in the JSON report (`negotiation_posture`), the DOCX + HTML reports, and a mobile-safe "Negotiation posture" complete-state card. Schema + JSON Schema artifact + validation (tier-predicate well-formedness, unique dimension, posture-only replace mode).

- **Thrust B — Posture report & export (shipped 2026-06-13, 9.2.0):** Deepens the posture into the negotiator's worksheet, all render-side (zero `result_hash` churn): a standalone action-grouped negotiation **sheet** ([`src/report/negotiation-sheet.ts`](src/report/negotiation-sheet.ts)) regrouping positions by action (escalate · push here · verify · hold, most-urgent first); a Markdown + formula-injection-guarded CSV posture export; and a headless CLI `--posture` mode (`--playbook-file <path>`).

- **Thrust C — Dimension breadth (shipped 2026-06-13, 9.3.0):** Widens the authorable dimension set, each **measure-first** (an extractor fixture proves the extraction is reliable on representative clause prose before the dimension is wired — spec-v10 §XVI, mirroring v5 §IX #4). Four new `numeric_threshold` metrics route through the same `extractMetricValues` path the v6 metrics use: `cure_period_days` + `auto_renewal_notice_days` (Step 173, temporal) and `indemnity_cap_amount` + `uptime_sla_percent` (Step 174, financial). A seventh predicate kind, `clause_mutual` (Step 175), asks whether the indemnification / termination / confidentiality clause is **mutual** or one-way: it reuses the v6 `findClause` locator (the `clause` enum anchors the default location; an explicit `pattern`/`section_heading` overrides it) and adds a bounded, deterministic reciprocity-marker scan ("each party", "both parties", "mutual", "respective", …) — no model, no fuzzy logic. A located clause with no marker is reported one-way; an absent clause is honestly unevaluable, never a false "one-way" (§3 corollary 2). Fully additive — a playbook that does not use the new dimensions validates and runs exactly as before; the published JSON Schema artifact mirrors the four metrics and the seventh predicate kind (schema-artifact test guards both enums), and the `acme-saas-buyer` example gains cure-period, uptime-SLA, and indemnification-mutuality positions. +18 tests (measure-first fixtures for all four metrics; `clause_mutual` compliant/one-way/unevaluable/pattern-override; Thrust-C posture ladders; the mutual-clause enum guard). `npm run typecheck && lint && test && build` green; suite **2,922 passing + 2 skips** (was 2,904). Version 9.2.0 → 9.3.0. With Thrust C, **all 10 v10 build steps (166–175) are shipped.**

## spec-v11 — Negotiation Posture Movement

- **Thrust A — Movement engine & surfaces (shipped 2026-06-13, 9.4.0):** Extends the v10 Negotiation Posture along the v6 version-comparison axis — it reports how a team's posture *moved* between two drafts, the round-over-round question a negotiator carries between rounds. [`comparePosture(base, revised)`](src/report/posture-movement.ts) is a **pure diff of two v10 postures** (no document parsing, no extraction, no predicate evaluation — that classification is already done): it compares the two tier labels per dimension and emits a transition — **improved · regressed · unchanged · newly-stated · now-unstated** (plus defensive *appeared / disappeared* for mismatched position sets). A single `TIER_RANK` table (ideal=3 > acceptable=2 > below-acceptable=1) decides improved vs. regressed; `unevaluable` is deliberately **unranked**, so a front that goes *not-stated → below-floor* reads as `newly-stated`, never a false `regressed` (the v10 §3 honesty contract carried across the version diff). The `PostureMovement` carries its own `movement_hash`, namespaced apart from the comparison `result_hash` (additive — a comparison with no positions yields no movement and moves no golden). **Surfaces** (Step 177, all render-side): an additive `posture_movement` block in the comparison JSON (`buildComparisonJson`, trailing optional argument); a mobile-safe "Posture movement" card in the comparison-complete tab (reuses the v10 `np-*` overflow-wrap styles + new `pm-*` direction colors); the UI threads the base posture and the active custom playbook through `runComparison` so the revised draft is classified against the *same* ladder. **Headless** (Step 178): the CLI `compare` command accepts `--playbook-file <path>` + `--posture` (mirroring `analyze` — `analyzeFile` already returns `negotiation_posture`), classifies both drafts against the playbook's `negotiation_positions`, and emits the `posture_movement` JSON block (or a Markdown table). Verified live: a base→revised pair with liability cap 3x→8x reported **improved** (below floor → acceptable) and governing law Delaware→Texas reported **regressed** (ideal → below floor), with a deterministic `movement_hash`. +17 tests (full transition matrix; order-independent hash determinism; the additive comparison-JSON block; CLI arg parsing — `--posture` requires `--playbook-file` — and Markdown rendering; the comparison-complete responsiveness e2e fixture gains an overflow-prone posture-movement card, 18/18 e2e vertical-scroll-only 320–1280px + WCAG-AA). `npm run typecheck && lint && test && build` green; suite **2,939 passing + 2 skips** (was 2,922). Version 9.3.0 → 9.4.0. New [`docs/spec-v11.md`](docs/spec-v11.md); README "Posture movement" section + v11 row + CLI cheat-sheet entry; threat-model v11 note. All 3 Thrust-A build steps (176–178) shipped; Thrusts B (179) and C (180) follow below.

- **Thrust B — Word comparison-report section (shipped 2026-06-13, 9.5.0):** A render-only follow-up that puts the movement into the deliverable a negotiator hands to a partner. [`buildComparisonDocx`](src/report/compare-docx.ts) takes the `PostureMovement` as a **trailing optional argument** (the v9/v10 surface-threading pattern) and renders a "Posture Movement" section: an advisory headline (the movement counts), the `movement_hash` for auditability, and a per-dimension table (Dimension · Movement · Base · Revised) whose movement cell is color-coded (green improved / red regressed / amber dropped) via a new `bodyCell(text, color?, bold?)` primitive that `bodyRow` now delegates to. Reuses the single-document posture table's visual contract; **omitted when no movement is supplied**, so the page flow and every existing comparison golden are unchanged. The UI passes the already-computed `postureMovement` as the third arg in `main.ts`. (The comparison deliverable is DOCX + JSON; there is no standalone-HTML *comparison* report — the standalone-HTML surface is the single-document report — so Thrust B is DOCX-only by construction.) +2 tests (the section renders every transition + the hash + short rung labels; omitted when absent).

- **Thrust C — CI regression gate (shipped 2026-06-13, 9.6.0):** Turns the advisory movement into a hard CI gate, exactly as `--fail-on <sev>` does for the introduced-finding bucket. The CLI `compare` command accepts `--fail-on-regression` (requires `--posture` — there is no movement to gate without it): it exits non-zero (code 2) when the posture movement holds any **regressed** dimension. A small exported `postureRegressed(pm)` predicate (mirroring `introducedBreaches`) is the single gate point: `pm.counts.regressed > 0`. Per the §3 honesty contract the gate is the well-ordered rung worsening only — `now-unstated` (a term that dropped off the ladder) is **reported but never trips it**, since a dropped front is not conflated with a rung regression (a team that wants to gate on it composes from the JSON `posture_movement.counts`). Reported alongside `--fail-on`; either tripping sets exit 2. CLI usage/help updated. +6 tests (arg parsing — parses with `--posture`, rejected without it, defaults off; the `postureRegressed` predicate — trips on a strict rung worsening, not on improvement/unchanged/newly-stated, not on now-unstated). `npm run typecheck && lint && test && build` green; suite **2,947 passing + 2 skips** (was 2,939). Version 9.4.0 → 9.6.0 (B = 9.5.0, C = 9.6.0). All 5 v11 build steps (176–180) shipped — the v11 idea is complete; remaining deferrals (three-rung movement, cross-document posture movement on the v4 axis) are net-new specs.

## spec-v12 — Cross-Document Posture Coherence

- **Thrust A — Coherence engine & headless surface (shipped 2026-06-13, 9.7.0):** Extends the v10 Negotiation Posture along the v4 **cross-document** axis — it reports how a team's posture sits across a *bundle* of documents (the sibling of v11's version-over-version axis). [`bundlePostureCoherence(documents)`](src/report/posture-coherence.ts) is a **pure diff of N v10 postures** (no document parsing, no extraction, no predicate evaluation — that classification is already done): given one `{ document, posture }` per document, all classified against the **same** team positions, it reports per negotiation front whether the documents **agree** on the rung (`aligned`), **disagree** (`divergent`), are stated by only one (`single`), or stated by none (`unstated`). For every stated front it surfaces the **binding floor** — the weakest stated rung and the document(s) carrying it — since in a deal package the weakest document usually governs exposure (the order form quietly re-caps what the MSA won). It reuses v11's now-**exported** `TIER_RANK` (one source of truth for the rung order) and carries its own `coherence_hash`, namespaced apart from every document's `result_hash` and the bundle fingerprint (additive — a run with no positions, or over a single document, yields no coherence and moves no golden). `unevaluable` stays **unranked**: an unstated front is never folded into a divergence and never lowers the floor (the v10 §3 honesty contract carried across the document axis). A `hasDivergence(coherence)` predicate mirrors v11's `postureRegressed`. **Headless** (Step 182): the CLI `analyze` command, run with `--posture` over a **bundle** (a directory or glob resolving to ≥2 documents — `analyzeFile` already returns `negotiation_posture` per file), collects each document's posture and prints a "Cross-document posture coherence" summary after the per-document lines: the per-kind counts, one ⚠ line per divergent front (the rung spread + the binding floor + the document carrying it), and the `coherence_hash`. A single-document run emits no coherence. **Gate** (Step 183): `--fail-on-divergence` (requires `--posture`) exits non-zero (code 2) when any front is divergent — the well-ordered spread only; `single`/`unstated` are reported but never trip it (§3 honesty — silence is not a disagreement). Reported alongside `--fail-on`; either tripping sets exit 2. Verified live: an MSA (12x cap, Delaware) + order form (2x cap, Texas) bundle reported governing law **divergent** (Delaware ideal vs. Texas below-floor; binding floor below-floor in the order form) with a deterministic `coherence_hash`, exit 2 under `--fail-on-divergence`; the guard rails (`--fail-on-divergence` requires `--posture`; single-document run prints no coherence) both fire. +15 tests (every coherence kind; the binding floor + `unevaluable` never ranked as the floor; determinism + document-order sensitivity; the single-document case; `hasDivergence`; the CLI `renderCoherenceSummary` counts line + ⚠-only-for-divergent + `coherence_hash`). `npm run typecheck && lint && test && build` green; suite **2,962 passing + 2 skips** (was 2,947). Version 9.6.0 → 9.7.0. New [`docs/spec-v12.md`](docs/spec-v12.md); README "Cross-document posture coherence" section + v12 row + CLI cheat-sheet entry; threat-model v12 note. All 3 Thrust-A build steps (181–183) shipped; the browser-UI bundle card (Thrust B) and consolidated-DOCX section (Thrust C) are proposed — the bundle pipeline does not yet compute per-document postures.
- **Thrusts B & C — Browser-UI coherence card + bundle DOCX section (shipped 2026-06-13, 9.8.0):** Closes the two Thrust-A deferrals by wiring the coherence engine into the two render surfaces the CLI didn't cover. **Thrust B (Steps 184–185):** [`prepareBundle`](src/ui/pipeline.ts) now evaluates a v10 `evaluateNegotiationPosture` for **each** document in a bundle when the active custom playbook defines `negotiation_positions` — every document classified against the **same** positions, independent of the matched built-in playbook that drives its per-document engine run (the run is untouched; the custom playbook contributes *only* its posture positions to the bundle, so secondary-family activation still runs for every bundled document — the single-doc "custom mode redefines the rule semantics" skip does not apply because the custom playbook does not drive the bundle's engine run). The postures ride on `BundlePerDocument.negotiation_posture` (own `posture_hash`, outside every `result_hash`). [`runBundleReport`](src/ui/pipeline.ts) collects them and computes a `bundlePostureCoherence` when every document carries one (a bundle is always ≥2 documents), surfacing it on `BundlePipelineResult.posture_coherence`; [main.ts](src/ui/main.ts) threads the active custom playbook into `runBundlePipeline`. The bundle-complete UI state renders a **mobile-safe coherence card**: per-kind counts (aligned / divergent / stated-by-one / unstated), one card per front showing the rung spread across the documents + the **binding floor** (weakest stated rung + the document carrying it), color-coded by a `pc-*` left border (green aligned, red divergent, blue stated-by-one, grey unstated) reusing the v10 `np-*` overflow-wrap styles; hidden when no coherence was computed. **Thrust C (Step 186):** [`buildBundleDocxReport`](src/report/bundle.ts) renders a trailing, optional "Posture Coherence" section — per-kind counts + a color-coded table (Front · Coherence · per-document rung · binding floor) — omitted entirely when no `posture_coherence` is supplied so every existing bundle golden is **byte-unchanged**. Advisory throughout: it names the weakest document but never adjudicates which document legally governs (§3 corollary 3). +5 tests (DOCX section renders the spread + binding floor / omitted when absent / deterministic rendered body; bundle-complete card renders + names the binding floor + carries the "does not decide which one legally governs" disclaimer / hidden when absent), and the responsiveness-states e2e bundle-complete fixture gains a posture-coherence block with long dimension labels + filenames. `npm run typecheck && lint && coverage && build` green; the `responsiveness-states` e2e (every view-state vertical-scroll-only 320–1280px + zero axe WCAG 2 AA violations, both themes) green; suite **2,967 passing + 2 skips** (was 2,962). Version 9.7.0 → 9.8.0. spec-v12 Thrust B/C marked shipped (Steps 184–186; Part XVI deferrals closed); README + CHANGELOG updated. **Every numbered spec v1–v12 is now fully shipped end-to-end** (engine + CLI + browser UI + DOCX); the frontier returns to the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v13 — Cross-Document Posture Movement

- **Thrust A — Movement engine & headless surface (shipped 2026-06-13, 9.9.0):** The fourth corner of the posture matrix — v10 (single doc, single version), v11 (single doc, across versions), v12 (across docs, single version), and now **v13 (across docs, across versions)**. It composes v11's version-comparison axis with v12's cross-document axis: how a deal package's *coherence* moved between two rounds. [`compareCoherence(base, revised)`](src/report/coherence-movement.ts) is a **pure diff of two v12 coherences** (no document parsing, no extraction, no predicate evaluation, no coherence computation — both coherences are already done): given a `PostureCoherence` for a **base** round and a **revised** round, both classified against the **same** team positions, it reports per negotiation front (matched by **dimension**, never by document — a `msa-v1.docx` → `msa-v2.docx` rename or an added document never confuses it) how the bundle's **binding floor** moved (`improved` / `regressed` / `unchanged` / `newly-stated` / `now-unstated`, + defensive `appeared` / `disappeared`, reusing v11's exported `TIER_RANK`) and how the **coherence kind** shifted (`fractured` / `reconciled` / `realigned` / `unchanged`). Floor- and shift-count tallies and a `movement_hash` namespaced apart from every `result_hash`, `posture_hash`, and `coherence_hash` (additive — a run with no baseline yields no movement and moves no golden). `unevaluable` stays **unranked**: a floor transition touching an unstated side is `newly-stated` / `now-unstated`, never improved or regressed (the v10 §3 honesty contract carried across the composed axis). A `coherenceRegressed(movement)` predicate mirrors v11's `postureRegressed` and v12's `hasDivergence`. **Headless** (Step 188): the CLI `analyze` command accepts `--baseline <path|glob|dir>` (requires `--posture`) — it analyzes the baseline bundle against the **same** custom playbook, computes its v12 coherence, diffs it against the primary bundle's coherence, and prints a "Cross-document posture movement (vs. baseline)" summary: the floor- and shift-count lines, one line per front whose floor moved or whose package fractured/reconciled (an unmoved front is omitted), and the `movement_hash`. A baseline yielding no coherence (fewer than two documents with a posture) is a hard error, not a silent no-op. **Gate** (Step 189): `--fail-on-coherence-regression` (requires `--baseline`) exits non-zero (code 2) when any front's binding floor moved to a strictly worse **stated** rung; `now-unstated` is reported but never trips it (§3 corollary 2 — a dropped front is not a rung regression; compose from `floor_counts`). Reported alongside `--fail-on` and `--fail-on-divergence`; any tripping sets exit 2. Verified live: a round-1 package (MSA 12x cap / order form 6x cap → divergent, binding floor *acceptable*) vs. a round-2 package (MSA 12x / order form 3x → divergent, binding floor *below-floor*) reported the liability-cap binding floor **↓ regressed (acceptable → below-acceptable)** with a deterministic `movement_hash` and exit 2 under `--fail-on-coherence-regression`; the reverse direction reported **↑ improved** at exit 0; the guard rails (`--baseline` requires `--posture`; `--fail-on-coherence-regression` requires `--baseline`) both fire. +26 tests (every floor movement; every coherence shift; front-by-dimension matching across renamed/added documents; determinism + direction sensitivity; `coherenceRegressed` trips on a regression but not on an improvement, a hold, or a dropped front; the CLI `renderCoherenceMovementSummary` counts lines + per-moved-front lines + `⚠`/`•` marks + `movement_hash`). `npm run typecheck && lint && coverage && build` green; coverage 89.16% lines / 73.88% branches (floors 85 / 70); suite **2,984 passing + 2 skips** (was 2,967). Version 9.8.0 → 9.9.0. New [`docs/spec-v13.md`](docs/spec-v13.md); README "Cross-document posture movement" section + v13 row + CLI cheat-sheet entry; CHANGELOG updated. The 3 Thrust-A build steps (187–189) are shipped; the browser-UI two-round card (Thrust B, Step 190) and DOCX section (Thrust C, Step 191) are proposed — the browser UI has no two-bundle comparison surface yet (it compares two single documents or analyzes one bundle, never two bundles).

- **Thrusts B & C — Browser two-round card + DOCX section (shipped 2026-06-13, 9.10.0):** Closes spec-v13 end-to-end. **Thrust B (Step 190)** builds the two-bundle comparison surface the browser previously lacked. The bundle-complete view ([`src/ui/main.ts`](src/ui/main.ts) `renderBundleComplete`) grows an `on_compare_round` "Compare a revised round…" affordance — wired **only** when the round produced a `posture_coherence` (a positions-bearing playbook was active), exactly as v11's compare row is offered only on a single document. Picking the revised round's files calls `runBundleComparison`, which re-runs the full bundle pipeline against the **same** active playbook, computes the revised round's v12 coherence, and diffs it against the baseline round's via `compareCoherence`. A revised round that yields no coherence (fewer than two documents classified against the playbook) is a **hard error**, not a silent no-op — mirroring the headless `--baseline` contract. The result transitions to a new `bundle-comparison-complete` `DropzoneState` ([`src/ui/states.ts`](src/ui/states.ts)) rendering a mobile-safe per-front card (`renderCoherenceMovement`): the `.np-card` left border reuses the v11 `pm-*` direction palette for the binding-floor movement (`Floor improved` / `Floor regressed — review` / `Floor newly stated` / …), a floor-transition line (`acceptable → below floor`), and a `cm-shift-*`-colored coherence-shift line (`Fractured` / `Reconciled` / `Realigned`). **Thrust C (Step 191)** threads the movement into the deliverable: `BundleReportInput.posture_movement` (additive) drives a trailing `renderPostureMovementSection` in the bundle DOCX ([`src/report/bundle.ts`](src/report/bundle.ts)) — one row per front (Front · Floor movement · Binding floor base→revised · Coherence shift), color-coded by the floor movement and carrying the `movement_hash`; `runBundleComparison` rebuilds the revised round's consolidated DOCX with the section appended (reusing the prepared bundle, so no document is re-ingested) and offers it as "Download two-round report (Word)", with `buildCoherenceMovementJson` as a second "movement data (JSON)" download. Both surfaces are **additive** — a plain bundle run supplies no movement, so the DOCX section is omitted and every per-document `result_hash`, `coherence_hash`, and bundle golden is byte-unchanged. **Mobile/a11y:** the new `bundle-comparison-complete` view-state is registered in `responsiveness-states.spec.ts` with an overflow-prone fixture (long dimension labels, the floor/shift sub-lines) — vertical-scroll-only at 320 / 390 / 768 / 1280px and **zero WCAG 2 AA axe violations in both themes**; the `cm-shift-*` foreground colors are theme-aware (`--critical` for fractured; a reconciled green that is light-on-dark by default and darkened to `#0a6b3d` on the light surface) so each clears the 4.5:1 contrast floor. **Build fix:** the first draft of the `cm-shift` CSS comment contained the literal token that closes a CSS comment inside a `pm-/pc-` reference, prematurely terminating the inlined `<style>` block and silently producing an empty `dist/` (the deploy-assets `closeBundle` then failed on the missing dir); reworded so the comment is well-formed. +6 unit/e2e tests (the DOCX movement section renders the floor movement + transition + shift + hash and is omitted/byte-stable without a movement; `buildCoherenceMovementJson` schema + determinism; the `on_compare_round` affordance hides without a coherence and fires with the chosen files; the `bundle-comparison-complete` card renders the floor movement, transition, shift, and advisory disclaimer + reset). `npm run typecheck && lint && coverage && build` green; full responsiveness/axe e2e (21/21) green; suite **2,992 passing + 2 skips** (was 2,984). Version 9.9.0 → 9.10.0. spec-v13 status + build-plan table + deferrals updated to shipped; CHANGELOG + README + memory updated. spec-v13 is now complete on all three thrusts, the same shape v10–v12 each landed (engine → headless/gate → browser card → Word deliverable).

- **Patch — Mobile overflow hardening (shipped 2026-06-13, 9.10.1):** A responsiveness audit across every view-state at 320px surfaced two dynamic text surfaces the static-render `responsiveness-states` e2e never exercised, both lacking `overflow-wrap`. The worst case is `.download-status`: after a download, `saveBlob` fills it with `Saved <filename>`, where the filename derives from the user's upload — names like `Master_Services_Agreement_..._FINAL_v12_executed.pdf` have no break opportunities, so on a 320px screen the status line widened the result card and reintroduced horizontal scroll (the exact failure mode the e2e pins, but it renders states *before* a download has populated the line, so it stayed green). Added `overflow-wrap: anywhere` to `.download-status` and to `.overlay-uncovered` (the honest "no overlay on file for <states>" coverage-gap line, for safety against long state lists). New e2e (`download-status with a long unbreakable filename never overflows`) injects the worst-case `Saved <long-filename>` text into the rendered complete-state and re-asserts vertical-scroll-only at 320 / 390 / 768 / 1280px — a regression guard for the dynamic surface the static fixtures miss. `npm run typecheck && lint && test && build` green; suite **2,992 passing + 2 skips** (unchanged — vitest count; the new guard is a Playwright e2e, 22/22 responsiveness+axe green). Version 9.10.0 → 9.10.1. CHANGELOG + README stat updated. Feature frontier unchanged: every numbered spec v1–v13 remains complete end-to-end; the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v14 — Saved Coherence Baselines

- **Thrusts A & B — The coherence artifact + headless emit/consume (shipped 2026-06-14, 9.11.0):** Resolves spec-v13 Open Question #2 ("persist a coherence as a baseline artifact instead of re-analyzing?") into a small, additive, headless feature, so a deal lead can gate round two of a negotiation against round one **without round one's documents on disk**. **Thrust A (Step 192)** adds the artifact to [`src/report/posture-coherence.ts`](src/report/posture-coherence.ts): `buildPostureCoherenceJson(coherence)` serializes a `PostureCoherence` to stable, pretty-printed JSON tagged `vaulytica.posture-coherence.v1` (the `coherence_hash`, the per-kind `counts`, and the full per-front, per-document rung set, in the pinned document order); `parsePostureCoherenceJson(text)` is the verifying inverse — it structurally validates the file (schema tag, tier/kind enums, shape), then **re-derives the `coherence_hash` from the artifact's own dimensions** and rejects any mismatch, so a corrupted, truncated, or hand-edited baseline is a hard, legible error (`coherence_hash mismatch — the artifact was modified or is corrupt …`), never a silent gate input. `counts` is recomputed from the verified dimensions on load (the hash covers dimensions only, so a derived tally is re-derived, never imported). The fingerprint is factored into a single private `coherenceHash(dimensions)` helper used both to stamp (in `bundlePostureCoherence`) and to verify (in the parser) — one definition of the canonical shape. **Thrust B (Step 193)** wires the CLI ([`tools/cli/run.ts`](tools/cli/run.ts) `analyze`): `--emit-coherence <path>` (requires `--posture`) writes `buildPostureCoherenceJson` of the round's coherence (a clear stderr note, not a silent no-op, when the round yields no cross-document coherence); `--baseline-coherence <coherence.json>` (requires `--posture`; mutually exclusive with `--baseline`) loads + verifies the artifact and hands it to the same `compareCoherence` the v13 `--baseline` path uses (a missing file or invalid/tampered artifact is a hard exit-1 error with the parser's messages); `--fail-on-coherence-regression` now accepts **either** baseline source (it required `--baseline` in v13). The diff — and its `movement_hash` — is byte-identical to re-analyzing the same baseline bundle (proven by a CLI integration test: a coherence emitted to disk, loaded, and diffed yields the same `movement_hash` as the in-memory diff). The browser/DOCX surface is a **principled deferral** (the artifact is a CI/headless concern; the browser already does an in-session two-round comparison via v13 Thrust B), and the cross-ladder guard is the caller's contract (same `--playbook-file` to emit and consume), exactly as v13's re-analyze path requires. **Additive:** both flags are off by default — with neither set the CLI is byte-identical to v13, so every per-document `result_hash`, `coherence_hash`, `movement_hash`, and golden is byte-unchanged. Verified live end-to-end: a round emits an artifact whose head shows the schema tag + `coherence_hash`; consuming it reproduces the same `coherence_hash`/`movement_hash`; the mutual-exclusion guard (`--baseline` + `--baseline-coherence`) fires; and a `sed`-tampered artifact is rejected with the hash-mismatch error. +8 tests (seven artifact unit tests — round-trip identity, hash integrity, tamper rejection, wrong-schema rejection, malformed-JSON/non-object rejection, invalid tier/kind rejection, count recomputation; one CLI integration test). `npm run typecheck && lint && test && build` green; suite **3,000 passing + 2 skips** (was 2,992). Version 9.10.1 → 9.11.0. New [`docs/spec-v14.md`](docs/spec-v14.md); README "Saved coherence baselines" section + v14 spec-table row + CLI cheat-sheet + flag-table entries; CHANGELOG updated. The two Thrust steps (192–193) are shipped; the browser/DOCX round-trip is a proposed deferral. Feature frontier: every numbered spec v1–v14 now complete end-to-end; the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v15 — Ladder-Pinned Coherence Baselines

- **Thrusts A & B — The ladder fingerprint + pinned artifact and cross-ladder guard (shipped 2026-06-15, 9.12.0):** Resolves spec-v14 Open Question #1 and the Part XVI "Cross-ladder verification" deferral. v14 made the saved coherence artifact hash-verified for **integrity** (a tampered baseline is a hard error), but integrity is not **identity**: `compareCoherence` matches fronts by dimension label, and two rungs are only comparable if they sit on the **same ladder** (the same `ideal`/`acceptable` predicates). v13's re-analyze `--baseline` path enforces this implicitly (same `--playbook-file` over both rounds); v14's `--baseline-coherence` path did **not** — nothing stopped a team from gating round two against a baseline emitted under a *different* playbook, producing a regression gate driven by a movement over two unrelated ladders. **Thrust A (Step 194)** adds [`ladderHash(playbook)`](src/playbooks/custom-interpreter.ts) beside `evaluateNegotiationPosture`: a stable SHA-256 over exactly what determines a tier — each negotiation position's `dimension` and its `ideal`/`acceptable` predicates (sorted by dimension via `localeCompare(_, "en")`, machine-independent), plus the named `thresholds` those predicates may reference. Per-tier `guidance` is **excluded by construction** — advisory negotiation text never changes which tier a document lands on, so re-wording it must not invalidate an archived baseline; `rule_overrides`/`custom_rules`/`required_clauses` are excluded too (they drive findings, not posture). A playbook with no `negotiation_positions` has no ladder and returns `null`. Reuses the engine-wide `sha256Hex` + `stableStringify` canonicalization. **Thrust B (Step 195)** pins the artifact + guards the consume path. [`src/report/posture-coherence.ts`](src/report/posture-coherence.ts) grows `COHERENCE_ARTIFACT_SCHEMA_V2` (`vaulytica.posture-coherence.v2`, joining `…_V1`; `COHERENCE_ARTIFACT_SCHEMA` stays a v1 alias); `buildPostureCoherenceJson(coherence, ladderHash?)` emits a pinned `v2` (carrying `ladder_hash`) when given a hash and a **byte-identical** `v1` when not — the `ladder_hash` is **independent of `coherence_hash`** (which still covers `dimensions` only), so the v1 and v2 artifacts of one coherence share the same integrity hash. `parsePostureCoherenceJson` accepts both schemas, requires `ladder_hash` on a `v2`, rejects a stray `ladder_hash` on a `v1`, and returns `ladderHash: string | null` on the `ok` result. The CLI ([`tools/cli/run.ts`](tools/cli/run.ts) `analyze`): `--emit-coherence` now computes `ladderHash(customPlaybook)` and passes it to the serializer, so every newly emitted artifact is a pinned `v2` (the ladder is always present — `--emit-coherence` requires `--posture` requires `--playbook-file`); `--baseline-coherence` computes this round's `ladderHash` and **refuses with exit 1** on a mismatch (`ladder mismatch — the artifact was computed against a different playbook ladder (artifact …, this round …)`), while a `v1` (unpinned) artifact still loads with a clear stderr note that cross-ladder verification is unavailable (v14's caller-owns-it contract). The `analyze` `USAGE`/help block was also brought up to date (it had never listed v14's `--emit-coherence`/`--baseline-coherence`/mutual-exclusion). **Additive:** no ladder hash ⇒ a `v1` artifact byte-identical to v14; the parser still accepts `v1`; `compareCoherence`/`coherenceRegressed`/`bundlePostureCoherence`/`TIER_RANK` are unchanged; every existing golden and round-trip is unchanged; neither emit nor consume flag ⇒ the CLI is unchanged from v14. +13 tests (four `ladderHash` unit tests — order-independence, guidance-insensitivity, predicate/threshold-sensitivity, null on empty; seven artifact unit tests — v1 null-ladderHash parse, v2 emit/round-trip, byte-identity without a pin, coherence_hash independence, v2-missing-pin / v1-stray-pin rejection, unknown-schema rejection updated for the now-valid v2 tag; one CLI cross-ladder accept/reject test; plus the existing "wrong schema" test relabeled to use an unknown tag). `npm run typecheck && lint && test && build` green; suite **3,012 passing + 2 skips** (was 3,000). Version 9.11.0 → 9.12.0. New [`docs/spec-v15.md`](docs/spec-v15.md); README "Saved coherence baselines" section extended with the ladder pin + v15 spec-table row; CHANGELOG updated. The two Thrust steps (194–195) are shipped; a browser/DOCX surface and a movement-artifact emit remain principled deferrals. Feature frontier: every numbered spec v1–v15 now complete end-to-end; the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v16 — Document-Free Coherence Movement

- **Step 196 — The `compare-coherence` subcommand (shipped 2026-06-15, 9.13.0):** Turns the spec-v15 deferral's own recommendation ("a consumer can recompute the movement from two coherence artifacts, which is the more auditable input") into the command that does it — also resolving spec-v14 Open Question #2 ("emit the movement as an artifact?") in the negative-but-better direction: keep the movement DERIVED, recomputed on demand from two auditable inputs. THE ARC: the posture surface has steadily removed what the gate needs on disk — v13 re-analyzed BOTH rounds' documents every run; v14 archived round one's coherence so only round two's documents were needed; v16 archives BOTH rounds' coherences so NO documents are needed at gate time (the dashboard/audit-log use case: store each round's kilobyte coherence artifact, show the binding-floor delta from the archive alone — no clause text, no re-ingestion, no engine run). **The command, `tools/cli/compare-coherence.ts`** (a new sibling to diff.ts/compare.ts): `compareCoherenceArtifacts(baseText, revisedText, format?)` is the PURE, IO-free core (unit-testable like formatPlaybookDiff) — parses+verifies both artifacts via `parsePostureCoherenceJson` (v14 integrity hash; a tampered/corrupt side ⇒ `{ok:false,errors}` with each error PREFIXED `base:`/`revised:`), runs the **spec-v15 cross-ladder guard BETWEEN the two artifacts** (both ladder-pinned[v2]+equal ⇒ verified; both pinned+different ⇒ `{ok:false}` `ladder mismatch` error; either unpinned[v1] ⇒ `{ok:true}` with a `ladderNote`), then `compareCoherence(base, revised)` (the SAME pure v13 diff the --baseline-coherence path uses) and renders the movement (`--format markdown` default via renderCoherenceMovementSummary, or `--format json` via buildCoherenceMovementJson), returning `{ok:true, output, regressed, ladderNote}`. `runCompareCoherence(argv)` is the handler (file IO + exit codes): writes the ladderNote to stderr, the movement to stdout, and under `--fail-on-coherence-regression` exits 2 on a regression (the SAME exit-2 contract `analyze --fail-on-coherence-regression` ships, now over two artifacts); a malformed/tampered/cross-ladder input is a hard exit-1. Wired into the run.ts dispatcher (`case "compare-coherence"`) + USAGE + header doc + the unknown-command error list. **REFACTOR (no behavior change):** `renderCoherenceMovementSummary` MOVED from `tools/cli/run.ts` to `src/report/coherence-movement.ts` (beside its buildCoherenceMovementJson sibling) so both the analyze --baseline* path AND compare-coherence render the movement from ONE definition with NO cross-import between sibling CLI modules; run.ts imports it from there and RE-EXPORTS it (`export { renderCoherenceMovementSummary }`) so run.test.ts's `./run.js` import is unaffected; the now-unused `type CoherenceMovement` import was dropped from run.ts. **VERIFIED LIVE end-to-end** (this command operates purely on JSON artifacts, so the pre-existing mammoth/docx-ingest env quirk that blocks `analyze <docx>` does NOT apply — built two ladder-pinned artifacts via a tsx generator, then drove the real CLI): `compare-coherence r1 r2` prints the movement (`⚠ Cap: binding floor ↓ regressed (acceptable → below-acceptable); fractured (aligned → divergent)` + movement_hash), exit 0; `--fail-on-coherence-regression` r1→r2 ⇒ exit 2; reverse r2→r1 ⇒ exit 0 (improvement); cross-ladder ⇒ exit 1 with `✗ ladder mismatch — the two artifacts were computed against different playbook ladders …`; `--help` lists the command. **ADDITIVE:** a brand-new subcommand — every existing command (analyze/diff/compare/verify) and every golden byte-for-byte unchanged; no new posture math, no new on-disk format. +8 tests (compare-coherence.test.ts: disk-vs-in-memory movement_hash identity, markdown summary contents, no-regression on the reverse/improving direction, cross-ladder refusal, unpinned-v1 note, tamper rejection prefixed `base:`, malformed-JSON rejection prefixed, gate-predicate parity vs coherenceRegressed). `npm run typecheck && lint && test && build` green; suite **3,020 passing + 2 skips** (was 3,012), 191 test files (was 190). Version 9.12.0 → 9.13.0. New [`docs/spec-v16.md`](docs/spec-v16.md); BUILD_PROGRESS v16 §; CHANGELOG [9.13.0]; README badge 3,012→3,020/9.12.0→9.13.0 + "Saved coherence baselines" § extended with the document-free compare-coherence workflow + v16 spec-table row + CLI cheat-sheet + analyze/commands table entry. The one Step (196) is shipped; a browser two-artifact card, a standalone movement artifact, and a sequence walker are principled deferrals. Feature frontier: every numbered spec v1–v16 now complete end-to-end; the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v17 — Document-Free Coherence Trajectory

- **Step 197 — The `coherence-trend` subcommand (shipped 2026-06-15, 9.14.0):** Builds v16's explicitly-deferred sequence walker (v16 Part XVI: "Accept a directory/glob of coherence artifacts and diff a sequence") into the command that adds the signal pairwise diffs CANNOT. THE ARC, one axis over from v16: v13 is a *movement* (a bundle across TWO rounds); v17 is the *trajectory* (a bundle across N rounds) — the same relationship v11's trajectory has to v10's snapshot. v16's `compare-coherence` answers "round 3 → round 4"; it cannot answer the deal lead's whole-negotiation question: *did the cap's binding floor climb steadily, erode steadily, or WHIPSAW — dip below floor mid-deal and recover?* A first-vs-last diff reports a recovered dip as `unchanged` (hides it); N−1 independent pairwise diffs never say "this front moved in both directions across the deal." v17 walks the sequence as one object. **The classifier (pure), `src/report/coherence-trajectory.ts`** (sibling to coherence-movement.ts): `FloorTrajectoryKind` = `steady-improvement` (≥1 improved step, 0 regressed) · `steady-regression` (≥1 regressed, 0 improved) · `whipsaw` (≥1 improved AND ≥1 regressed) · `flat` (no RANKED movement — an appear-only/drop-only front is `flat`, never a false whipsaw, per §3: `newly-stated`/`now-unstated` are not ranked). `compareCoherenceTrajectory(rounds)` matches fronts by dimension across the UNION of all N coherences (pinned by `localeCompare`), builds each front's floor + coherence kind at every round, classifies each consecutive step with the SHARED v11/v13 `classifyFloorMovement` (now EXPORTED from coherence-movement.ts — the ONLY change to that file; no behavior moves), computes `net_floor_movement` = `classifyFloorMovement(floors[0], floors[N−1])`, reduces steps → trajectory, returns the per-front set + `trajectory_counts` + `net_counts` + a `trajectory_hash` (SHA-256 over the canonical per-front set, namespaced apart from every coherence_hash/movement_hash). `trajectoryRegressed(t)` = any front `steady-regression` or `whipsaw` (floor regressed at SOME step) — the faithful multi-round generalization of v13's `coherenceRegressed`. `buildCoherenceTrajectoryJson` (`schema: vaulytica.posture-trajectory.v1`) + `renderCoherenceTrajectorySummary` (omits `flat` fronts). **The command, `tools/cli/coherence-trend.ts`** (sibling to compare-coherence.ts): `compareCoherenceTrendArtifacts(texts, format?)` is the PURE, IO-free core — verifies all N via `parsePostureCoherenceJson` (a tampered/corrupt round ⇒ `{ok:false}` with each error PREFIXED `round N:` 1-indexed), runs the **v15/v16 cross-ladder guard across the WHOLE sequence** (≥2 pinned with any two differing pins ⇒ `{ok:false}` `ladder mismatch` NAMING the two rounds; any unpinned ⇒ `{ok:true}` ladderNote), then `compareCoherenceTrajectory` and renders markdown (default) or JSON, returning `{ok:true, output, regressed, ladderNote}`. `runCoherenceTrend(argv)` is the handler (file IO + exit codes); requires ≥2 positionals (a single coherence has no trajectory ⇒ exit 1); under `--fail-on-coherence-regression` exits 2 on a floor that regressed at ANY step (strictly stronger than first-vs-last — a transient dip trips it even if recovered). Wired into the run.ts dispatcher (`case "coherence-trend"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts, so the mammoth/docx-ingest env quirk does NOT apply — built three ladder-pinned artifacts via a tsx generator [Cap floor acceptable → below-acceptable → ideal], drove the real CLI): prints `⚠ Cap: whipsaw (acceptable → below-acceptable → ideal); net ↑ improved` + trajectory_hash, exit 0; `--fail-on-coherence-regression` ⇒ exit 2 (the round-2 dip trips it even though net improved); improving-only sequence ⇒ exit 0; single artifact ⇒ exit 1; round-3-on-different-ladder ⇒ exit 1 `✗ ladder mismatch — round 1 and round 3 …`; genuine tamper of round 2 ⇒ exit 1 `✗ round 2: coherence_hash mismatch …`. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command (analyze/diff/compare/compare-coherence/verify) and every golden byte-for-byte unchanged; coherence-movement.ts changes only by exporting an existing private fn; no new posture math, no new on-disk format (trajectory stays DERIVED). +15 tests (coherence-trajectory.test.ts ×7: whipsaw with net-improved, steady-improvement no-gate, steady-regression gate, flat-on-appear-only, determinism, <2-rounds throws, render+JSON; coherence-trend.test.ts ×8: disk-vs-in-memory trajectory identity, markdown summary, no-regression-on-improving, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,035 passing + 2 skips** (was 3,020), 193 test files (was 191). Version 9.13.0 → 9.14.0. New [`docs/spec-v17.md`](docs/spec-v17.md); BUILD_PROGRESS v17 §; CHANGELOG [9.14.0]; README badge 3,020→3,035/9.13.0→9.14.0 + "Saved coherence baselines" § extended with the whole-deal-trajectory workflow + posture-matrix note + v17 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v17. The one Step (197) is shipped; a directory/glob order-inferring walker, a standalone trajectory artifact, a net-only gate flag, and a browser N-round trend are principled deferrals. Feature frontier: every numbered spec v1–v17 now complete end-to-end; the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v18 — Document-Free Coherence-Shift Trajectory

- **Step 198 — The `coherence-shift-trend` subcommand (shipped 2026-06-15, 9.15.0):** Builds v17 Part XVII open question #2 — the fracture/reconcile companion to v17's floor whipsaw — into the command that reads the SECOND signal v13 always carried beside the floor. THE ARC, one axis over from v17: v17 reads the N artifacts on the binding-*floor* axis (`coherence-trend`, exposure); v18 reads the SAME N artifacts on the coherence-*kind* axis (agreement) — the exact `floor_movement`/`coherence_shift` pairing v13's `compareCoherence` reports per front, now generalized to a sequence. The floor says how bad the worst rung is; the coherence kind says whether the package speaks with one voice. A bundle can hold its floor steady while quietly FRACTURING (two docs that both said "cap = 1×" now say "1×" and "2×" — floor unchanged, package split), and that fracture is exactly what a deal lead reconciling a multi-document package needs to see. v16/v17 archived each round's coherence kind in `coherences[]` but classified the trajectory on the floor only; v18 reduces those kinds — the same way v17 reduced the floors. **The classifier (pure), `src/report/coherence-shift-trajectory.ts`** (sibling to coherence-trajectory.ts): `CoherenceShiftTrajectoryKind` = `steady-fracture` (≥1 fractured step, 0 reconciled) · `steady-reconcile` (≥1 reconciled, 0 fractured) · `oscillating` (≥1 fractured AND ≥1 reconciled — split apart and re-merge, the signal a first-vs-last diff hides) · `stable` (no DIRECTIONAL shift — a realign-only/appear-only front is `stable`, never a false oscillation; `realigned`/`unchanged` are not directional). `compareCoherenceShiftTrajectory(rounds)` matches fronts by dimension across the UNION of all N coherences (pinned by `localeCompare`), builds each front's coherence kind at every round (`null`=absent), classifies each consecutive step via a null-tolerant wrapper over the SHARED v13 `classifyShift` (now EXPORTED from coherence-movement.ts — the ONLY change to that file; no behavior moves, exactly as v17 exported classifyFloorMovement), computes `net_shift` = the round-1-vs-round-N shift, reduces steps → trajectory, returns the per-front set + `shift_trajectory_counts` + `net_shift_counts` + a `shift_trajectory_hash` (SHA-256 over the canonical per-front set, namespaced apart from every coherence_hash/movement_hash/trajectory_hash). `shiftTrajectoryFractured(t)` = any front `steady-fracture` or `oscillating` (package fractured at SOME step) — the fracture/reconcile companion to v17's `trajectoryRegressed`. `buildCoherenceShiftTrajectoryJson` (`schema: vaulytica.posture-shift-trajectory.v1`) + `renderCoherenceShiftTrajectorySummary` (omits `stable` fronts). **The shared loader, `tools/cli/coherence-sequence.ts`** (NEW): `verifyCoherenceSequence(texts)` factors the parse + hash-verify + cross-ladder guard OUT of coherence-trend.ts so BOTH trend commands share one verified-input path (≥2 artifacts required; tampered round ⇒ `{ok:false}` prefixed `round N:`; ≥2 pinned with differing pins ⇒ `{ok:false}` `ladder mismatch` naming both rounds; any unpinned ⇒ `{ok:true}` ladderNote). coherence-trend.ts now calls it — its output/errors/exit-codes are UNCHANGED (its full 8-test suite passes as-is). **The command, `tools/cli/coherence-shift-trend.ts`** (sibling to coherence-trend.ts): `compareCoherenceShiftTrendArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` then `compareCoherenceShiftTrajectory` and renders markdown (default) or JSON, returning `{ok:true, output, fractured, ladderNote}`. `runCoherenceShiftTrend(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-fracture` exits 2 on a package that fractured at ANY step (strictly stronger than first-vs-last — a transient fracture trips it even if reconciled). Wired into the run.ts dispatcher (`case "coherence-shift-trend"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts, so the mammoth/docx-ingest env quirk does NOT apply — built three ladder-pinned artifacts via a tsx generator [Cap kind aligned → divergent → aligned], drove the real CLI): prints `⚠ Cap: oscillating (aligned → divergent → aligned); net unchanged` + shift_trajectory_hash, exit 0; `--fail-on-fracture` ⇒ exit 2 (the round-2 fracture trips it even though net reads unchanged — the fracture/reconcile analog of v17's whipsaw-gate demo); `--help` lists the command. **ADDITIVE:** a brand-new subcommand + one pure module + a behavior-preserving loader extraction — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/verify) and every golden byte-for-byte unchanged; coherence-movement.ts changes only by exporting an existing private fn; no new posture math, no new on-disk format (the shift trajectory stays DERIVED). +16 tests (coherence-shift-trajectory.test.ts ×8: oscillation with net-unchanged, steady-fracture gate, steady-reconcile no-gate, stable-on-realign-only, stable-on-appear-only, determinism, <2-rounds throws, render+JSON; coherence-shift-trend.test.ts ×8: disk-vs-in-memory trajectory identity, markdown summary, no-fracture-on-reconcile-only, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,051 passing + 2 skips** (was 3,035), 195 test files (was 193). Version 9.14.0 → 9.15.0. New [`docs/spec-v18.md`](docs/spec-v18.md); BUILD_PROGRESS v18 §; CHANGELOG [9.15.0]; README badge 3,035→3,051/9.14.0→9.15.0 + "Saved coherence baselines" § extended with the agreement-trajectory workflow + posture-matrix note + v18 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v18. The one Step (198) is shipped; an order-inferring walker, a standalone shift-trajectory artifact, a net-only gate flag, a unified `coherence-trend --with-shift`, and a browser N-round trend are principled deferrals. Feature frontier: every numbered spec v1–v18 now complete end-to-end; the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v19 — Document-Free Combined Posture Arc

- **Step 199 — The `coherence-arc` subcommand (shipped 2026-06-15, 9.16.0):** Builds v18 Part XVII open question #2 — fold the floor and shift trajectories into one combined report — into the command that restores v13's per-front COMBINED view (both axes in one object) for the N-round, document-free case. THE ARC, the last open cell of the document-free row: v16 the two-round movement (floor + shift in one object), v17 the N-round *floor* trajectory (`coherence-trend`), v18 the N-round *shift* trajectory (`coherence-shift-trend`), v19 the N-round floor + shift trajectory JOINED. v13's `compareCoherence` always reported `floor_movement` AND `coherence_shift` per front because a deal lead reconciling a package reads them together (*did the cap erode, and did it also fracture? did the floor hold while the package quietly split? did anything at all go wrong on either axis?*); v17/v18 split those axes across two single-purpose commands (one gate, one hash each), and v18 OQ#2 deferred the combined view to "a dashboard that joins the two JSON outputs on `dimension`." v19 promotes that compose-it-yourself deferral into a first-class command — the SAME pattern v16/v17 used (each promoted an earlier "you can compose it" deferral) — because doing the positional join (mis-aligns if a consumer sorts fronts differently than `localeCompare`), the combined gate (`||`-ing two exit codes gives no single fingerprint and re-reads N artifacts twice), and the audit fingerprint correctly is exactly where consumers slip. **The join (pure), `src/report/coherence-arc.ts`** (sibling to coherence-trajectory.ts / coherence-shift-trajectory.ts): `CoherenceArcFront` carries the floor fields from v17 (`floors`, `steps`, `net_floor_movement`, `trajectory`) + the shift fields from v18 (`shifts`, `net_shift`, `shift_trajectory`) + the shared `coherences[]` sequence ONCE. `compareCoherenceArc(rounds)` runs the two EXISTING pure trajectory functions (`compareCoherenceTrajectory` v17, `compareCoherenceShiftTrajectory` v18) on the same rounds and joins their `fronts` arrays POSITIONALLY on `dimension` (both pin fronts by the SAME `localeCompare` over the SAME union of dimensions ⇒ identical order + length; a defensive per-index dimension-equality check throws on a broken join — loud, never silent). Returns the per-front combined set + all FOUR count objects (the two floor tallies from v17, the two shift tallies from v18) + the two component fingerprints carried VERBATIM (`trajectory_hash`, `shift_trajectory_hash` — byte-identical to what the single-axis commands emit on the same inputs) + a namespaced `arc_hash` = SHA-256 over `{trajectory_hash, shift_trajectory_hash}` (apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash). `arcRegressedOrFractured(arc)` = `trajectoryRegressed(floor) || shiftTrajectoryFractured(shift)` (read off the arc's count objects) — the deal-level "did anything go wrong on either axis" verdict NEITHER single-axis command exposes. `buildCoherenceArcJson` (`schema: vaulytica.posture-arc.v1`) + `renderCoherenceArcSummary` (floor + shift trajectory blocks, then one line per front that moved on EITHER axis — a flat-AND-stable front is omitted — showing both paths + both nets + all three hashes). **ZERO new posture math AND zero changes to any existing source file**: v19 imports only already-PUBLIC functions from the two trajectory modules and the UNCHANGED `verifyCoherenceSequence` loader — unlike v17/v18 it needed nothing newly exported. **The command, `tools/cli/coherence-arc.ts`** (sibling to coherence-trend.ts / coherence-shift-trend.ts): `compareCoherenceArcArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, unchanged from v18) then `compareCoherenceArc` and renders markdown (default) or JSON, returning `{ok:true, output, regressedOrFractured, ladderNote}`. `runCoherenceArc(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-regression-or-fracture` exits 2 when the floor regressed OR the package fractured at ANY step. NOT a `coherence-trend --with-shift` flag (v18 Part XVI deferred that to keep each command single-purpose) — v19 is a SEPARATE command whose single purpose IS the combined view, with its own single gate and own single hash; the two single-axis commands stay byte-for-byte unchanged. Wired into the run.ts dispatcher (`case "coherence-arc"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts, so the mammoth/docx-ingest env quirk does NOT apply — built three ladder-pinned artifacts via a tsx generator [Cap floor ideal → acceptable → ideal, kind aligned → divergent → aligned], drove the real CLI): prints `⚠ Cap: floor whipsaw (ideal → acceptable → ideal); coherence oscillating (aligned → divergent → aligned); net floor unchanged, net shift unchanged` + all three hashes, exit 0; `--fail-on-regression-or-fracture` ⇒ exit 2 (the round-2 whipsaw+fracture trips it even though both nets read unchanged); CROSS-CHECKED the arc's `trajectory_hash`/`shift_trajectory_hash` against `coherence-trend`/`coherence-shift-trend` on the same files — byte-identical (`119aa55c…` / `62322c2a…`), proving the arc IS the join of the two single-axis commands. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new posture math, no new on-disk format (the arc stays DERIVED). +17 tests (coherence-arc.test.ts ×9: front-for-front join vs the two single-axis trajectories, combined-gate parity = trajectoryRegressed||shiftTrajectoryFractured, floor-only trip [regress while stable], shift-only trip [fracture while floor flat], both-quiet no-trip, determinism, <2-rounds throws, render+JSON-with-all-three-hashes, flat-and-stable front omitted; tools/cli/coherence-arc.test.ts ×8: disk-vs-in-memory arc identity, markdown summary with both axes, both-quiet no-trip, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,068 passing + 2 skips** (was 3,051), 197 test files (was 195). Version 9.15.0 → 9.16.0. New [`docs/spec-v19.md`](docs/spec-v19.md); BUILD_PROGRESS v19 §; CHANGELOG [9.16.0]; README badge 3,051→3,068/9.15.0→9.16.0 + "Saved coherence baselines" § extended with the combined-arc workflow + v19 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v19. The one Step (199) is shipped; a `--with-shift` flag, a standalone arc artifact, a net-only combined gate, an order-inferring walker, and a browser N-round arc are principled deferrals. Feature frontier: every numbered spec v1–v19 now complete end-to-end; the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v20 — Document-Free Posture Exposure (Whole-Deal Low-Water Mark)

- **Step 200 — The `coherence-exposure` subcommand (shipped 2026-06-15, 9.17.0):** The orthogonal *level* axis the whole posture lineage never read. v10–v19 are, end to end, a study of *movement* — how a rung, a binding floor, or a coherence kind *changed* between rounds (v11 two versions of one doc; v13 two rounds of a bundle; v17/v18/v19 walk N rounds and classify the floor path / shift path / both). That axis has a STRUCTURAL blind spot: a front sitting at `below-acceptable` in EVERY round never *moves*, so v17's `coherence-trend` reduces its all-`unchanged` steps to `flat`, the summary OMITS it ("surfaces only what moved"), and `--fail-on-coherence-regression` NEVER fires (a floor only "regresses" when it changes to a *worse* rung — one born at the bottom never changed). It is `stable` under v18 and omitted from v19's arc too. So from the archive alone, across four movement commands, the single most exposed front in the deal is INVISIBLE and UNGATED. This is not a bug in those commands (a floor that never moved genuinely has no trajectory) — it is a missing AXIS. v20 supplies it: read the same N artifacts not for the *direction* of the floor but for its *worst level* — the low-water mark. **The low-water mark (pure), `src/report/coherence-exposure.ts`** (sibling to coherence-trajectory.ts): `ExposureLevel` = `ideal`/`acceptable`/`below-acceptable`/`unstated`. `CoherenceExposureFront` carries the `floors[]` sequence (shared with v17), `worst_floor` (lowest-ranked STATED rung across all rounds, or `null` when never stated), `worst_round` (1-based index it FIRST fell there — earliest-exposure tiebreak), `rounds_stated`, and `exposed` (= `worst_floor === "below-acceptable"`). `compareCoherenceExposure(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory functions use ⇒ same front order), takes a per-front MINIMUM over the shared `TIER_RANK` (v11/v13) over the binding floor (`weakest_tier`) v12 ALREADY derives (`TIER_RANK` is injective over the three ranked rungs ⇒ the min rank maps to a unique tier), tallies `worst_counts` by level, counts `exposed` fronts, returns the set + a namespaced `exposure_hash` (SHA-256 over the canonical per-front set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash). `exposureBreached(exposure)` = `exposed_count > 0` — the *level* gate predicate; UNLIKE `trajectoryRegressed` (fires only on a floor that CHANGED to a worse rung) this fires on a floor that SAT below acceptable, so a front pinned at below-acceptable for the whole deal trips it. HONEST by construction (§3): a front no document EVER states is `unstated`, counted but never flagged (silence is not below-floor) — and a front stated in only some rounds takes its worst over the rounds that stated it. `buildCoherenceExposureJson` (`schema: vaulytica.posture-exposure.v1`) + `renderCoherenceExposureSummary` (worst-level tally + exposed-front count, then one line per EXPOSED front with its floor path + first-below-floor round + the hash). **NEAR-ZERO new posture math (one min over ranks) AND zero changes to any existing source file**: v20 imports only already-PUBLIC functions (`TIER_RANK`, `weakest_tier`) and the UNCHANGED `verifyCoherenceSequence` loader — like v19 it needed nothing newly exported. **The command, `tools/cli/coherence-exposure.ts`** (sibling to coherence-arc.ts): `compareCoherenceExposureArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, unchanged) then `compareCoherenceExposure` rendered markdown (default) or JSON, returning `{ok:true, output, breached, ladderNote}`. `runCoherenceExposure(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-exposure` exits 2 when any front sat below the acceptable floor at ANY round. A SEPARATE command, not a `coherence-trend` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-exposure"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built three ladder-pinned artifacts via a tsx generator where Cap is pinned at `below-acceptable` every round [the blind-spot front] and Indemnity erodes ideal→acceptable→acceptable, drove the real CLI): `coherence-exposure` prints `⚠ Cap: worst below floor, first at round 1 (below-acceptable → below-acceptable → below-acceptable)` + `exposure_hash: dc57e899…`, and `--fail-on-exposure` ⇒ exit 2; the CONTRAST proves the gap — `coherence-trend` on the SAME files reports Cap as `flat` and never lists it (its regression gate fires only on Indemnity, which actually moved), so the most-exposed front is exactly the one the movement command misses while v20 catches it. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the exposure stays DERIVED). +17 tests (coherence-exposure.test.ts ×8: worst-floor + first-round + rounds-stated, the below-floor-the-whole-deal front that `coherence-trend` calls `flat`/never-regressed [the blind spot], unstated-is-never-exposed [§3], partially-stated front, worst-level tally + exposed count, determinism, <2-rounds throws, render+JSON; tools/cli/coherence-exposure.test.ts ×9: disk-vs-in-memory exposure identity, markdown summary, no-breach when worst is acceptable, breach on the all-deal-below-floor front, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,085 passing + 2 skips** (was 3,068), 199 test files (was 197). Version 9.16.0 → 9.17.0. New [`docs/spec-v20.md`](docs/spec-v20.md); BUILD_PROGRESS v20 §; CHANGELOG [9.17.0]; README badge 3,068→3,085/9.16.0→9.17.0 + "Saved coherence baselines" § extended with the exposure workflow + v20 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v20. The one Step (200) is shipped; a `coherence-trend --worst-floor` flag, a standalone exposure artifact, a configurable exposure floor, a last-round-only view, an order-inferring walker, and a browser N-round exposure are principled deferrals. Feature frontier: every numbered spec v1–v20 now complete end-to-end; the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v21 — Document-Free Exposure Persistence (Chronicity & Current Standing)

- **Step 201 — The `coherence-persistence` subcommand (shipped 2026-06-15, 9.18.0):** The orthogonal *time* axis v20's level view never read. v20's `coherence-exposure` reduces a front's whole history to a single worst point (its low-water mark) — the right answer to "how low did it get," but a single extreme has NO memory of duration, so it cannot tell a closed wound from an open one. Two histories collapse to the SAME v20 row: a **resolved dip** (front slips to `below-acceptable` in round 2, the team fixes the doc, the front climbs back to `acceptable` by round 4 and holds — the exposure is OVER) and an **open wound** (front slips in round 2 and is STILL below floor in the latest round). v20 reports both as `worst_floor = below-acceptable`, `exposed = true`, and `--fail-on-exposure` exits 2 on both — FOREVER, because the worst point never changes once it has happened, so a team that resolved its dip is stuck with a red gate it cannot clear without re-litigating history. This is not a bug in v20 (a low-water mark is *supposed* to be a single extreme) — it is a missing AXIS: the *time* dimension of the same below-floor data. v21 supplies it: read the same N artifacts not for the worst floor but for the PERSISTENCE of the below-floor condition. **The persistence (pure), `src/report/coherence-persistence.ts`** (sibling to coherence-exposure.ts): `PersistenceClass` = `open` (latest STATED floor is below-acceptable — still down) / `resolved` (was below floor at some round but latest stated floor is at/above acceptable — recovered) / `none` (stated, never below floor) / `unstated` (no document ever stated it). `CoherencePersistenceFront` carries the `floors[]` sequence (shared with v17/v20), `rounds_below` (count of rounds whose stated floor is below-acceptable), `first_below_round`/`last_below_round` (the span), `last_stated_round` (latest round with a stated floor), `currently_below` (the floor at `last_stated_round` is below-acceptable), and `persistence`. `computeCoherencePersistence(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure functions use ⇒ same front order), scans each front's binding floors (`weakest_tier`, v12) for the `below-acceptable` rung (v10) for the count + first/last, reads the LATEST STATED floor for current standing, classifies, tallies `class_counts`, counts `open` fronts, returns the set + a namespaced `persistence_hash` (SHA-256 over the canonical per-front set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash). `exposureOpen(persistence)` = `open_count > 0` — the *current-standing* gate predicate; UNLIKE `exposureBreached` (fires on a floor below acceptable at ANY round and stays red after a recovery) this fires ONLY on a front below floor NOW, so a resolved dip CLEARS the gate and an open wound trips it. HONEST by construction (§3): a front no document EVER states is `unstated`, never flagged; **current standing reads the latest STATED round, not the latest round overall**, so silence after an exposure (front below floor in round 3, then unstated in rounds 4–5) keeps the last KNOWN standing (`open`) — neither an invented recovery nor a fresh exposure (the same contract that keeps `newly-stated`/`now-unstated` unranked in v11/v13 and `unstated` never-flagged in v20). `buildCoherencePersistenceJson` (`schema: vaulytica.posture-persistence.v1`) + `renderCoherencePersistenceSummary` (class tally + open-front count, then one line per OPEN front [floor path + rounds-down + span] and one per RESOLVED front [when it was down + now-recovered]; `none`/`unstated` counted but never listed). **NEAR-ZERO new posture math (one count + one last-stated lookup) AND zero changes to any existing source file**: v21 imports only the UNCHANGED `verifyCoherenceSequence` loader and a plain `below-acceptable` literal — like v19/v20 it needed nothing newly exported. **The command, `tools/cli/coherence-persistence.ts`** (sibling to coherence-exposure.ts): `computeCoherencePersistenceArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, unchanged) then `computeCoherencePersistence` rendered markdown (default) or JSON, returning `{ok:true, output, open, ladderNote}`. `runCoherencePersistence(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-open-exposure` exits 2 ONLY when a front is still below floor at its latest stated round. A SEPARATE command, not a `coherence-exposure` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-persistence"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built three ladder-pinned artifacts via a tsx generator where Cap dips ideal → below-acceptable → recovered-to-acceptable [a RESOLVED dip], drove the real CLI): `coherence-persistence --fail-on-open-exposure` prints `✓ Cap: resolved — was below floor round 2, now back above floor at round 3` + `persistence_hash: 2f0dcdb4…` and exits **0** (the recovered dip CLEARS the gate), while `coherence-exposure --fail-on-exposure` on the SAME files prints `⚠ Cap: worst below floor` and exits **2** (stays red) — proving the new gate rewards a resolution the level axis structurally cannot see. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the persistence stays DERIVED). +20 tests (coherence-persistence.test.ts ×11: rounds-below + span + current standing of an open front, the resolved dip that v20 calls exposed-forever but v21 clears, the still-below open front, never-below `none`, unstated-is-never-open/resolved [§3, via all-`unevaluable` Gap], silence-after-exposure keeps the last known standing [§3], partially-stated front, class tally + open count, determinism, <2-rounds throws, render-open+resolved+JSON; tools/cli/coherence-persistence.test.ts ×9: disk-vs-in-memory persistence identity, markdown summary, gate clears on a recovered dip, open on a still-below front, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,105 passing + 2 skips** (was 3,085), 201 test files (was 199). Version 9.17.0 → 9.18.0. New [`docs/spec-v21.md`](docs/spec-v21.md); BUILD_PROGRESS v21 §; CHANGELOG [9.18.0]; README badge 3,085→3,105/9.17.0→9.18.0 + "Saved coherence baselines" § extended with the persistence workflow + v21 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v21. The one Step (201) is shipped; a `coherence-exposure --persistence` flag, a configurable "chronic-after N" gate, a standalone persistence artifact, a consecutive-vs-total distinction, an order-inferring walker, and a browser N-round persistence are principled deferrals. Feature frontier: every numbered spec v1–v21 now complete end-to-end; the posture matrix now has THREE axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), and TIME (v21); the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v22 — Document-Free Exposure Breadth (Per-Round Deal Standing)

- **Step 202 — The `coherence-breadth` subcommand (shipped 2026-06-15, 9.19.0):** The transpose axis the whole posture family never read. Every posture command from v16 to v21 reads the N-round archive DOWN THE FRONT AXIS: pick a negotiation front, summarize its history ACROSS ROUNDS — v17 which way its floor moved, v20 how low its floor ever got (low-water mark), v21 how long it was below floor and whether it is still down. NONE reads it DOWN THE ROUND AXIS: pick a ROUND, summarize the whole deal's standing ACROSS FRONTS. So from the archive a deal lead can answer "is the Cap front still below floor?" (v21) but not "how many fronts were below floor in round 3, and was that the worst the package ever looked?" — every existing command produces a per-front series; none produces a per-round one. To learn the per-round breadth from v20/v21 a consumer must read every per-front row, pull each front's `floors[]` array, and re-tabulate them column-by-column by hand, reconstructing the view the archive never reports. This is not a bug in v20/v21 (a per-front reduction is SUPPOSED to fix a front) — it is a missing AXIS: the per-round dimension of the same below-floor data, the transpose of v20/v21. v22 supplies it: read the same N artifacts not per-front-across-rounds but per-round-across-fronts. **The breadth (pure), `src/report/coherence-breadth.ts`** (sibling to coherence-persistence.ts): `CoherenceBreadthRound` = { `round` (1-based), `exposed_fronts` (count of fronts whose binding floor is below-acceptable that round), `stated_fronts` (count of fronts with any stated floor — the denominator), `exposed_dimensions` (the below-floor fronts, pinned by `localeCompare`) }. `CoherenceBreadth` carries `rounds` (count), `per_round[]` (the rounds, in sequence order), `worst_round`/`worst_count` (the round with the most fronts below floor at once — EARLIEST on a tie, mirroring v20's earliest-exposure tiebreak — and that count; `null`/`0` when no front was ever below floor), `first_count`/`latest_count` (the first and latest rounds' exposed-front counts), `widened` (= `latest_count > first_count`), and a namespaced `breadth_hash` (SHA-256 over the canonical per-round set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash). `computeCoherenceBreadth(rounds)` is the pure core: for each round counts the fronts whose `weakest_tier` (v12) is the `below-acceptable` rung (v10) — IGNORING unstated fronts (§3) — pins the exposed-dimension list by `localeCompare`, finds the worst round (earliest peak), compares first vs latest. `exposureWidened(breadth)` = `breadth.widened` — the DEAL-LEVEL breadth-trend gate predicate; UNLIKE `exposureBreached` (any single front EVER below floor) and `exposureOpen` (any single front STILL below floor) this fires on the AGGREGATE TREND: the package ended with more fronts below floor than it started, so a narrowing deal CLEARS it and a widening one trips it — needs no tuning (compares the two endpoint counts). HONEST by construction (§3): a front no document states in a round is NOT counted as below floor that round (silence is not exposure, the same contract that keeps `unstated` never-flagged in v20); `stated_fronts` gives the denominator. Note v20's `exposed_count` is MONOTONE (a front exposed once is exposed forever) while v22's per-round `exposed_fronts` rises AND falls round-to-round — which is exactly what makes the worst round and the widen/narrow trend visible; v22 never says "ever," it says "that round." `buildCoherenceBreadthJson` (`schema: vaulytica.posture-breadth.v1`) + `renderCoherenceBreadthSummary` (widen/narrow/hold trend + worst round, then one line per round [its below-floor count, the stated denominator, the named fronts], then the hash). **NEAR-ZERO new posture math (one per-round count + one endpoint comparison) AND zero changes to any existing source file**: v22 imports only the already-PUBLIC `PostureCoherence` type and the shared hashing helpers (`sha256Hex`, `stableStringify`); the `below-acceptable` literal is a plain string — like v19/v20/v21 it needed nothing newly exported. **The command, `tools/cli/coherence-breadth.ts`** (sibling to coherence-persistence.ts): `computeCoherenceBreadthArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceBreadth` rendered markdown (default) or JSON, returning `{ok:true, output, widened, ladderNote}`. `runCoherenceBreadth(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-widening-exposure` exits 2 ONLY when the latest round has strictly more fronts below floor than the first. A SEPARATE command, not a `coherence-persistence` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-breadth"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built three ladder-pinned artifacts via a tsx generator where the deal WIDENS: round 1 Cap below floor → round 2 Cap+Risk → round 3 Cap+Risk+Indemnity, drove the real CLI): `coherence-breadth --fail-on-widening-exposure` prints `exposure widened (1 → 3 fronts below floor)`, `worst round: round 3 (3 fronts below floor at once)`, the per-round series, + `breadth_hash: c97c8ef6…` and exits **2** (the package broadened), while `coherence-persistence` on the SAME files lists three open fronts (Cap/Indemnity/Risk) but cannot show the per-round breadth trend without manual transposition — proving the new per-round axis the per-front commands miss. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the breadth stays DERIVED). +18 tests (coherence-breadth.test.ts ×10: per-round count + named fronts, worst round [earliest peak on tie], the widening deal [1→2→3 fronts → gate trips] the per-front commands cannot show as a trend, the narrowing deal [gate clears], the flat-but-broad deal [held, not widened], unstated-is-never-counted [§3], no-front-ever-below [worst `null`], determinism, <2-rounds throws, render+JSON; tools/cli/coherence-breadth.test.ts ×8: disk-vs-in-memory breadth identity, markdown summary, gate clears on a narrowing deal, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,123 passing + 2 skips** (was 3,105), 203 test files (was 201). Version 9.18.0 → 9.19.0. New [`docs/spec-v22.md`](docs/spec-v22.md); BUILD_PROGRESS v22 §; CHANGELOG [9.19.0]; README badge 3,105→3,123/9.18.0→9.19.0 + "Saved coherence baselines" § extended with the breadth workflow + v22 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v22. The one Step (202) is shipped; a `coherence-persistence --breadth` flag, a configurable "too broad" gate, a standalone breadth artifact, a severity-weighted breadth index, an order-inferring walker, and a browser N-round breadth are principled deferrals. Feature frontier: every numbered spec v1–v22 now complete end-to-end; the posture matrix now has FOUR axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME (v21), and BREADTH (v22); the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v23 — Document-Free Exposure Recurrence (Per-Front Below-Floor Episodes)

- **Step 203 — The `coherence-recurrence` subcommand (shipped 2026-06-15, 9.20.0):** The orthogonal *episode-count* axis v21's duration sum throws away. v21's `coherence-persistence` reduces a front's history to `rounds_below` (a COUNT) + the current standing — the right answer to "how long was it down," but a count is a SUM: it adds every below-floor round and forgets their SHAPE. Two histories collapse to the SAME v21 row: a **steady descent** (binding floor `below → below → below`: `rounds_below=3`, one continuous wound) and a **recover-then-relapse** (`below → acceptable → below`: `rounds_below=2`, `persistence=open` — but a concession won back in round 2 and LOST again in round 3). v21 reports both as `open` with a single span; v17 calls the relapse `unchanged` net; v20 sees the same worst point; v22's per-round count is blind to WHICH front recurred. None of the existing commands reports the one fact that separates them — that the relapse's below-floor rounds came in TWO separate episodes with a recovery between. This is not a bug in v21 (a duration is SUPPOSED to sum) — it is a missing AXIS: the episode-count dimension of the same below-floor data. v23 supplies it: read the same N artifacts per front and count the maximal contiguous below-floor EPISODES. **The recurrence (pure), `src/report/coherence-recurrence.ts`** (sibling to coherence-persistence.ts): `RecurrenceClass` = `recurring` (≥2 episodes — recovered and relapsed) / `single` (exactly 1 episode) / `none` (stated, never below floor) / `unstated` (no document ever stated it). `RecurrenceEpisode` = `{first_round, last_round}` (1-based round range of a maximal contiguous below-floor stretch). `CoherenceRecurrenceFront` carries `dimension`, the `floors[]` sequence (shared with v17/v20/v21), `rounds_below` (the v21 sum, for context), `below_runs` (the episode count — the verdict), `episodes[]` (each episode's round range), and `recurrence`. `computeCoherenceRecurrence(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/persistence functions use ⇒ same front order), scans each front's binding floors (`weakest_tier`, v12) for the `below-acceptable` rung (v10) via `scanEpisodes` (a STATED at/above-floor round closes an open episode; an UNSTATED round does NOT — §3), classifies, tallies `class_counts`, counts `recurring` fronts, finds `most_recurrent_dimension`/`max_runs` (the front with the most episodes — EARLIEST on a tie via localeCompare-sorted labels + `>`), returns the set + a namespaced `recurrence_hash` (SHA-256 over the canonical per-front set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash). `exposureRecurred(recurrence)` = `recurring_count > 0` — the *churn* gate predicate; UNLIKE `exposureBreached` (any front EVER below floor) and `exposureOpen` (any front STILL below floor now) this fires on a front that went below floor, RECOVERED, and went below floor AGAIN — even after its latest stated floor has since recovered (a recurred-then-resolved front still trips it; a single-episode front still open does NOT) — needs no tuning (two episodes IS recurrence). HONEST by construction (§3): **silence does not split an episode** — `below → unstated → below` is ONE episode (the v21 contract that silence keeps the last known standing — neither an invented recovery nor a fresh fall); only a STATED at/above-floor round (a real recovery) splits one. A front never stated is `unstated`, never `recurring`/`single`/`none`. `buildCoherenceRecurrenceJson` (`schema: vaulytica.posture-recurrence.v1`) + `renderCoherenceRecurrenceSummary` (class tally + recurring count, then one line per RECURRING front [episode count + episode ranges + floor path] and one per SINGLE front; `none`/`unstated` counted but never listed). **NEAR-ZERO new posture math (one per-front run-length scan) AND zero changes to any existing source file**: v23 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19/v20/v21/v22 it needed nothing newly exported. **The command, `tools/cli/coherence-recurrence.ts`** (sibling to coherence-persistence.ts): `computeCoherenceRecurrenceArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceRecurrence` rendered markdown (default) or JSON, returning `{ok:true, output, recurring, ladderNote}`. `runCoherenceRecurrence(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-recurring-exposure` exits 2 ONLY when a front fell below floor in ≥2 separate episodes. A SEPARATE command, not a `coherence-persistence` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-recurrence"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built three ladder-pinned artifacts via a tsx generator where Cap dips below-acceptable → recovered-to-acceptable → below-acceptable again [a RECOVER-THEN-RELAPSE], drove the real CLI): `coherence-recurrence --fail-on-recurring-exposure` prints `⚠ Cap: recurring — below floor in 2 separate episodes (round 1, round 3)`, `recurrence_hash: adecec28…` and exits **2** (the front relapsed), while `coherence-persistence` on the SAME files reports it as a single `open` front with no sign the 2 rounds were non-consecutive — proving the new episode-count axis the duration sum cannot show. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the recurrence stays DERIVED). +19 tests (coherence-recurrence.test.ts ×11: recover-then-relapse [2 episodes, recurring] vs steady descent [1 episode, single] that v21 reports identically, silence-does-not-split [§3], stated-recovery-does-split, recurred-then-resolved still trips, single-still-open does not, most-recurrent front [earliest on tie], unstated never counted [§3], no-front-ever-below [max_runs 0, most_recurrent null], determinism, <2-rounds throws, render+JSON; tools/cli/coherence-recurrence.test.ts ×8: disk-vs-in-memory recurrence identity, markdown summary, gate clears on a steady descent, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,142 passing + 2 skips** (was 3,123), 205 test files (was 203). Version 9.19.0 → 9.20.0. New [`docs/spec-v23.md`](docs/spec-v23.md); BUILD_PROGRESS v23 §; CHANGELOG [9.20.0]; README badge 3,123→3,142/9.19.0→9.20.0 + "Saved coherence baselines" § extended with the recurrence workflow + the five-axis summary callout + v23 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v23. The one Step (203) is shipped; a `coherence-persistence --episodes` flag, a configurable "too churny" gate, a standalone recurrence artifact, a gap-aware episode model, a longest/mean-episode statistic, an order-inferring walker, and a browser N-round recurrence are principled deferrals. Feature frontier: every numbered spec v1–v23 now complete end-to-end; the posture matrix now has FIVE axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME (v21), BREADTH (v22), and RECURRENCE (v23); the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v24 — Document-Free Exposure Volatility (Per-Front Floor Crossings)

- **Step 204 — The `coherence-volatility` subcommand (shipped 2026-06-15, 9.21.0):** The orthogonal *crossing-count* axis v23's episode count throws away. v23's `coherence-recurrence` counts each front's below-floor EPISODES (`below_runs`) — but an episode count counts only the ENTRIES into below-floor; it is blind to the RECOVERIES that bound them. Two histories collapse to the SAME v23 row (`single` episode, gate clears): a **stuck front** (binding floor `below → below → below`: one episode, never moved) and a **bounced front** (`acceptable → below → acceptable`: also ONE episode — it fell once and CLEANLY RECOVERED). v23 calls both `single`; v21 calls the first `open` and the second `resolved` (the current STANDING, not the MOVEMENT); v20 calls both `exposed`; v17 nets the bounced front `unchanged`. None of the existing commands reports the one fact that separates a front that BOUNCED from a front that SAT STILL — that the first CROSSED the floor TWICE (down, then up) and the second never crossed it at all. This is not a bug in v23 (an episode count is SUPPOSED to count entries, not crossings) — it is a missing AXIS: the crossing-count dimension (recoveries included) of the same below-floor data. v24 supplies it: read the same N artifacts per front and count the FLOOR CROSSINGS — every STATED transition across the floor boundary, in either direction. **The volatility (pure), `src/report/coherence-volatility.ts`** (sibling to coherence-recurrence.ts): `VolatilityClass` = `volatile` (≥2 crossings — its standing reversed across the floor) / `monotone` (exactly 1 crossing — it crossed once, never back) / `stable` (0 crossings, stated — it stayed on ONE side the whole deal, always-below OR always-at-or-above) / `unstated` (no document ever stated it). `CoherenceVolatilityFront` carries `dimension`, the `floors[]` sequence (shared with v17/v20/v21/v23), `rounds_below` (the count, for context), `crossings` (the floor-boundary crossing count — the verdict), and `volatility`. `computeCoherenceVolatility(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/persistence/recurrence functions use ⇒ same front order), scans each front's binding floors (`weakest_tier`, v12) for transitions across the `below-acceptable` rung (v10) via `scanCrossings` (it compares each STATED round's below/at-or-above-floor side to the previous STATED round's side and counts a flip; an UNSTATED round is SKIPPED — neither a crossing nor a reset, §3), classifies, tallies `class_counts`, counts `volatile` fronts, finds `most_volatile_dimension`/`max_crossings` (the front with the most crossings — EARLIEST on a tie via localeCompare-sorted labels + `>`), returns the set + a namespaced `volatility_hash` (SHA-256 over the canonical per-front set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash). `exposureVolatile(volatility)` = `volatile_count > 0` — the *instability* gate predicate; UNLIKE `exposureBreached` (any front EVER below floor), `exposureOpen` (any front STILL below floor) and `exposureRecurred` (any front below floor in ≥2 EPISODES) this fires on a front whose standing CROSSED the floor twice or more — counting RECOVERIES, so a front that fell once and cleanly recovered (2 crossings, a `single` episode to v23) trips it while a front that fell once and STAYED DOWN (0 crossings) does NOT — needs no tuning (two crossings IS a reversal). HONEST by construction (§3): **silence does not count as a crossing** — `below → unstated → below` is ZERO crossings (silence keeps the last known standing — neither an invented recovery nor a fall), and `below → unstated → acceptable` is ONE crossing (a real recovery, just unstated in the gap round). A front never stated is `unstated`, never `volatile`/`monotone`/`stable`. **Distinct from v17's whipsaw**: v17 fires on ANY improving + ANY regressing rung-step ANYWHERE on the ladder (incl. `acceptable → ideal → acceptable`, an ABOVE-floor jitter that never risks the floor); v24's crossing count is specific to the FLOOR boundary, so that same above-floor whipsaw is `stable` (0 crossings) to v24. `buildCoherenceVolatilityJson` (`schema: vaulytica.posture-volatility.v1`) + `renderCoherenceVolatilitySummary` (class tally + volatile count, then one line per VOLATILE front [crossing count + floor path] and one per MONOTONE front; `stable`/`unstated` counted but never listed). **NEAR-ZERO new posture math (one per-front crossing scan) AND zero changes to any existing source file**: v24 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19/v20/v21/v22/v23 it needed nothing newly exported. **The command, `tools/cli/coherence-volatility.ts`** (sibling to coherence-recurrence.ts): `computeCoherenceVolatilityArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceVolatility` rendered markdown (default) or JSON, returning `{ok:true, output, volatile, ladderNote}`. `runCoherenceVolatility(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-volatile-exposure` exits 2 ONLY when a front's standing crossed the floor ≥2 times. A SEPARATE command, not a `coherence-recurrence` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-volatility"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built three ladder-pinned artifacts via a tsx generator: Term `acceptable → below → acceptable` [a BOUNCE] and Cap `below → below → below` [STUCK], drove the real CLI): `coherence-volatility --fail-on-volatile-exposure` prints `⚠ Term: volatile — its standing crossed the floor 2 times (acceptable → below-acceptable → acceptable)`, `volatility_hash: 0e08ed25…` and exits **2** (Term reversed), with Cap counted `stable`; while `coherence-recurrence` on the SAME files reports BOTH as `single` episode and exits **0** — proving the crossing-count axis the episode count cannot show. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the volatility stays DERIVED). +20 tests (coherence-volatility.test.ts ×12: bounced [2 crossings, volatile] vs stuck [0 crossings, stable] that v23 reports identically as `single`, single-fall [1 crossing, monotone], silence-does-not-cross [§3], recovery-across-silence [1 crossing], recover-then-relapse [2 crossings, volatile], above-floor whipsaw is stable [0 crossings — distinct from v17], most-volatile front [earliest on tie], unstated never counted [§3], no-front-ever-crossed [max_crossings 0, most_volatile null], determinism, <2-rounds throws, render+JSON; tools/cli/coherence-volatility.test.ts ×8: disk-vs-in-memory volatility identity, markdown summary, gate clears on a stuck front, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,162 passing + 2 skips** (was 3,142), 207 test files (was 205). Version 9.20.0 → 9.21.0. New [`docs/spec-v24.md`](docs/spec-v24.md); BUILD_PROGRESS v24 §; CHANGELOG [9.21.0]; README badge 3,142→3,162/9.20.0→9.21.0 + "Saved coherence baselines" § extended with the volatility workflow + the six-axis summary callout + v24 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v24. The one Step (204) is shipped; a `coherence-recurrence --crossings` flag, a configurable "too jittery" gate, a standalone volatility artifact, a direction-aware crossing model, a crossing-rate statistic, an order-inferring walker, and a browser N-round volatility are principled deferrals. Feature frontier: every numbered spec v1–v24 now complete end-to-end; the posture matrix now has SIX axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME (v21), BREADTH (v22), RECURRENCE (v23), and VOLATILITY (v24); the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v25 — Document-Free Exposure Synchrony (Per-Round Floor Crossings)

- **Step 205 — The `coherence-synchrony` subcommand (shipped 2026-06-15, 9.22.0):** The orthogonal *per-step co-movement* axis the per-front crossing count (v24) and the per-round level count (v22) both leave out. v24's `coherence-volatility` reads the archive DOWN THE FRONT AXIS — per front, how many times its standing crossed the floor ACROSS THE WHOLE DEAL (`crossings`, a per-front sum). v22's `coherence-breadth` reads it DOWN THE ROUND AXIS, but only on the LEVEL — per round, how many fronts sat BELOW floor (a static standing, not a movement). Neither reads the archive down the round axis on the MOVEMENT: per round-TRANSITION, how many fronts CROSSED the floor in the SAME step. Two deals collapse to indistinguishable per-front readings: a **coordinated lurch** (Cap AND Term both fall below floor in round 1→2 — one redline round knocks two fronts down at once: each front has ONE crossing, NEITHER is `volatile` to v24, so v24's gate CLEARS) and a **staggered drift** (Cap falls in round 1→2, Term falls in round 2→3 — also two single-crossing fronts to v24, identical). v24 cannot tell them apart because a per-front crossing count is sliced by FRONT; v22's level differs only in the final round's count, not in WHEN the package moved. None of the existing commands reports the one fact that separates a lurch from a drift — the count of fronts that crossed the floor TOGETHER, in one transition. This is not a bug in v24 (a per-front count is SUPPOSED to sum over rounds, not bucket by step) — it is a missing AXIS: the per-transition TRANSPOSE of v24's crossing count. v25 supplies it: re-bucket the SAME floor crossings v24 counts — by STEP instead of by FRONT. **The synchrony (pure), `src/report/coherence-synchrony.ts`** (sibling to coherence-volatility.ts): `SynchronyClass` = `synchronized` (≥2 fronts crossed the floor in this one step) / `isolated` (exactly 1) / `quiet` (0). `CoherenceSynchronyTransition` carries `from_round`/`to_round` (1-based), `crossing_fronts` (the count — the step's verdict), `crossed_dimensions` (which fronts, `localeCompare`-pinned), and `synchrony`. `computeCoherenceSynchrony(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/volatility functions use), scans each front's binding floors (`weakest_tier`, v12) for transitions across the `below-acceptable` rung (v10), and ATTRIBUTES each STATED crossing to the transition that REVEALS it (an UNSTATED round is SKIPPED — neither a crossing nor a reset, §3 — so a crossing across a silent gap lands on the step ending at the round that reveals the new standing, never the silent step), bucketing the crossings per step; finds `peak_transition`/`peak_count` (the single step the most fronts crossed at once — EARLIEST on a tie), `synchronized_count` (steps with ≥2 crossings), and `total_crossings` — equal BY CONSTRUCTION to the sum of every front's v24 `crossings` (v25 is the literal transpose: the same crossings sliced by step). Returns the series + a namespaced `synchrony_hash` (SHA-256 over the canonical per-transition set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash). `exposureSynchronized(synchrony)` = `synchronized_count > 0` — the *co-movement* gate predicate; UNLIKE `exposureVolatile` (a single front crossing the floor ≥2 times ACROSS THE DEAL) and `exposureWidened` (the below-floor COUNT grew first→latest) this fires on a single STEP where ≥2 fronts crossed TOGETHER — needs no tuning (two fronts at once IS a coordinated shift). HONEST by construction (§3): **silence does not count as a crossing, and a crossing across a silent gap is attributed to the REVEALING step** — `below → unstated → acceptable` is ONE crossing on the step ending at the third round (the recovery, just unstated in the gap round), and `below → unstated → below` is ZERO crossings (silence keeps the last known standing). **Distinct from v17's whipsaw**: an above-floor jitter (`acceptable → ideal → acceptable`) is all-`quiet` (zero floor crossings) to v25. `buildCoherenceSynchronyJson` (`schema: vaulytica.posture-synchrony.v1`) + `renderCoherenceSynchronySummary` (peak step + synchronized-step count, then one line per step [crossing count + which fronts]). **NEAR-ZERO new posture math (one per-transition re-bucketing) AND zero changes to any existing source file**: v25 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v24 it needed nothing newly exported. **The command, `tools/cli/coherence-synchrony.ts`** (sibling to coherence-volatility.ts): `computeCoherenceSynchronyArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceSynchrony` rendered markdown (default) or JSON, returning `{ok:true, output, synchronized, ladderNote}`. `runCoherenceSynchrony(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-synchronized-exposure` exits 2 ONLY when a single step crossed the floor with two or more fronts at once. A SEPARATE command, not a `coherence-volatility` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-synchrony"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built three ladder-pinned artifacts via a tsx generator: Cap AND Term both fall in round 1→2, Cap recovers in round 2→3, drove the real CLI): `coherence-synchrony --fail-on-synchronized-exposure` prints `peak step: round 1→2 (2 fronts crossed the floor at once)`, `⚠ round 1→2: 2 fronts crossed (Cap, Term)`, `synchrony_hash: f31fc908…` and exits **2** (the lurch), with `total_crossings: 3` matching `coherence-volatility` on the SAME files (Cap 2 + Term 1 = 3) — proving the per-step transpose the per-front count cannot show. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the synchrony stays DERIVED). +20 tests (coherence-synchrony.test.ts ×12: coordinated lurch [2 fronts, synchronized, gate trips] vs staggered drift [2 fronts in different steps, all isolated, gate clears] that v24 reports identically as two monotone fronts, transpose invariant [`total_crossings` = sum of v24 `crossings`], silence-attributes-to-revealing-step [§3], silence-does-not-invent-a-crossing [§3], above-floor whipsaw all-quiet [distinct from v17], peak step [earliest on tie], no-front-ever-crossed [peak null], unstated never counted [§3], determinism, <2-rounds throws, render+JSON; tools/cli/coherence-synchrony.test.ts ×8: disk-vs-in-memory synchrony identity, markdown summary, gate clears on a staggered drift, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,182 passing + 2 skips** (was 3,162), 209 test files (was 207). Version 9.21.0 → 9.22.0. New [`docs/spec-v25.md`](docs/spec-v25.md); BUILD_PROGRESS v25 §; CHANGELOG [9.22.0]; README badge 3,162→3,182/9.21.0→9.22.0 + "Saved coherence baselines" § extended with the synchrony workflow + the seven-axis summary callout + v25 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v25. The one Step (205) is shipped; a `coherence-volatility --by-step` flag, a configurable "too turbulent" gate, a standalone synchrony artifact, a direction-aware per-step model, a direction-homogeneous synchrony, an order-inferring walker, and a browser N-round synchrony are principled deferrals. Feature frontier: every numbered spec v1–v25 now complete end-to-end; the posture matrix now has SEVEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME (v21), BREADTH (v22, per-round level transpose), RECURRENCE (v23), VOLATILITY (v24, per-front crossings), and SYNCHRONY (v25, per-step crossings — the transpose of v24); the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v26 — Document-Free Exposure Settling (Latest Floor Crossing / Settling Round)

- **Step 206 — The `coherence-settling` subcommand (shipped 2026-06-15, 9.23.0):** The orthogonal *time-of-last-movement* axis the per-front crossing count (v24) and the per-step crossing count (v25) both leave out. v24's `coherence-volatility` reads the archive DOWN THE FRONT AXIS — per front, how many times its standing crossed the floor across the whole deal (`crossings`, a per-front sum). v25's `coherence-synchrony` re-buckets those same crossings DOWN THE STEP AXIS — per round-transition, how many fronts crossed at once (`crossing_fronts`). Both reduce the crossings to a COUNT, and a count is silent on TIME: it cannot say WHEN the package last crossed the floor. Two deals collapse to indistinguishable count readings: an **early cross** (a front falls below floor in round 1→2, then holds steady for five more rounds — the package SETTLED early: one crossing, one isolated step) and a **late cross** (a front falls below floor only in the FINAL round — to v24 the same one monotone front `crossings=1`, to v25 the same one isolated step, gate clears — yet UNSETTLED: still crossing the floor at the close). v24/v25 cannot tell them apart because both sum/bucket crossings without keeping their POSITION; v21's duration may even agree (a front down for one round in each). None of the existing commands reports the one fact that separates a settled close from an unsettled one — the ROUND the package last crossed the floor, and whether that round was the close. This is not a bug in v24/v25 (a crossing count is SUPPOSED to sum/bucket, not index) — it is a missing AXIS: the time-of-last-movement dimension of the same crossing data. v26 supplies it: read the SAME floor crossings v24/v25 count for the INDEX of the last one. **The settling (pure), `src/report/coherence-settling.ts`** (sibling to coherence-synchrony.ts): `SettlingState` = `active` (≥1 front crossed the floor in this step) / `still` (none did). `CoherenceSettlingTransition` carries `from_round`/`to_round` (1-based), `crossing_fronts`, `crossed_dimensions` (which fronts, `localeCompare`-pinned), and `state`. `computeCoherenceSettling(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/volatility/synchrony functions use), scans each front's binding floors (`weakest_tier`, v12) for transitions across the `below-acceptable` rung (v10), and ATTRIBUTES each STATED crossing to the transition that REVEALS it (an UNSTATED round is SKIPPED — neither a crossing nor a reset, §3 — so a crossing across a silent gap lands on the step ending at the round that reveals the new standing, never the silent step; a FINAL round left entirely unstated reveals NO crossing — the close is SETTLED, silence at the close is stability not movement); marks each transition `active`/`still`, locates `settling_round` (the `to_round` of the LATEST active step — the round the package last crossed the floor, `null` when none ever did), `quiet_tail` (the run of trailing `still` steps after it — the WHOLE sequence when none crossed), `active_count` (steps where any front crossed), `unsettled` (the FINAL transition was active), and `total_crossings` — equal BY CONSTRUCTION to v24's per-front sum AND v25's per-step sum (v26 reads the same crossings for WHERE the last one falls). Returns the series + a namespaced `settling_hash` (SHA-256 over the canonical per-transition set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash/synchrony_hash). `exposureUnsettled(settling)` = `settling.unsettled` — the *time-of-last-movement* gate predicate; UNLIKE `exposureVolatile` (a single front crossing ≥2 times) and `exposureSynchronized` (≥2 fronts crossing in one step) this fires on the single fact that the LAST crossing fell on the FINAL step — needs no tuning (a crossing in the final round IS an unsettled close). HONEST by construction (§3): **silence does not make a close unsettled** — a final round left unstated reveals no crossing, so the deal is SETTLED. **Distinct from v17's whipsaw**: an above-floor jitter (`acceptable → ideal → acceptable`) crosses the floor zero times → no settling round, `settled`. **Distinct from v21's duration**: a front stuck below floor for all N rounds has a large `rounds_below` but ZERO crossings (it never moved) → `settled` to v26 (its last crossing is `null`, the quiet tail is the whole sequence). `buildCoherenceSettlingJson` (`schema: vaulytica.posture-settling.v1`) + `renderCoherenceSettlingSummary` (the settling verdict [settled at round K with a quiet tail / UNSETTLED at the final round / steady-throughout] + active-step count, then one line per step). **NEAR-ZERO new posture math (one last-crossing reduction) AND zero changes to any existing source file**: v26 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v25 it needed nothing newly exported. **The command, `tools/cli/coherence-settling.ts`** (sibling to coherence-synchrony.ts): `computeCoherenceSettlingArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceSettling` rendered markdown (default) or JSON, returning `{ok:true, output, unsettled, ladderNote}`. `runCoherenceSettling(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-unsettled-exposure` exits 2 ONLY when the final transition crossed the floor. A SEPARATE command, not a `coherence-synchrony` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-settling"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built three ladder-pinned artifacts via a tsx generator, drove the real CLI): a late cross (`acceptable → acceptable → below`) prints `settling: UNSETTLED — the floor was last crossed in round 2→3, the final transition`, `settling_hash: 7bec5911…` and exits **2**; an early cross (`acceptable → below → below`) prints `settled at round 2 — … then 1 steady step to the close` and exits **0** — proving the time axis the per-front/per-step counts cannot show. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the settling stays DERIVED). +22 tests (coherence-settling.test.ts ×14: unsettled close [late cross, gate trips] vs settled deal [early cross then steady, gate clears] that v24/v25 report identically, the reduction invariant [`total_crossings` = v24's and v25's totals], latest-crossing across many fronts, silence-attributes-to-revealing-step [§3], silent-final-round-is-settled [§3], above-floor whipsaw never crosses [distinct from v17], no-front-ever-crossed [settling null, quiet tail = all steps], unstated never counted [§3], determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-settling.test.ts ×8: disk-vs-in-memory settling identity, markdown summary, gate clears on an early cross, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,204 passing + 2 skips** (was 3,182), 211 test files (was 209). Version 9.22.0 → 9.23.0. New [`docs/spec-v26.md`](docs/spec-v26.md); BUILD_PROGRESS v26 §; CHANGELOG [9.23.0]; README badge 3,182→3,204/9.22.0→9.23.0 + "Saved coherence baselines" § extended with the settling workflow + the eight-axis summary callout + v26 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v26. The one Step (206) is shipped; a `coherence-synchrony --settling` flag, a configurable "settled by round N" gate, a standalone settling artifact, a first-crossing/re-exposure-latency stat, a per-front settling, an order-inferring walker, and a browser N-round settling are principled deferrals. Feature frontier: every numbered spec v1–v26 now complete end-to-end; the posture matrix now has EIGHT axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22, per-round level transpose), RECURRENCE (v23), VOLATILITY (v24, per-front crossings), SYNCHRONY (v25, per-step crossings — the transpose of v24), and SETTLING (v26, the index of the last crossing); the open frontier is the human-gated v5 corpus/attorney work or a net-new spec.

## spec-v27 — Document-Free Exposure Onset (First Floor Crossing / Onset Round)

- **Step 207 — The `coherence-onset` subcommand (shipped 2026-06-15, 9.24.0):** The *time-of-first-movement* MIRROR of v26's last-crossing index. v26's `coherence-settling` reads the same floor crossings v24/v25 count for the INDEX of the LAST one (`settling_round`, `unsettled`) — and the latest throws away WHEN the FIRST crossing happened. Two deals collapse to indistinguishable v26 readings: an **early onset** (a front falls in round 1→2 AND again in the final round — the package degraded FROM THE OPENING, no grace period) and a **clean lead-in** (a front falls in round 4→5 AND again in the final round — three steady rounds held first). To v26 these are IDENTICAL (same `settling_round` = the final step, same `unsettled` = yes, same `total_crossings`) — yet the first started exposed and the second held a clean lead-in. v26 cannot tell them apart because it reads the MAXIMUM crossing index, by construction silent on the first. The first and last crossing indices are INDEPENDENT the moment a deal crosses the floor more than once; they coincide only in the degenerate single-crossing case (then onset = settling). This is not a bug in v26 (a SETTLING index is supposed to read the last move) — it is a missing AXIS: the time-of-first-movement dimension of the same crossing data. v27 supplies it: read the SAME floor crossings v24/v25/v26 count for the INDEX of the FIRST one. (Fulfills v26 Part XVI's deferred first-crossing stat.) **The onset (pure), `src/report/coherence-onset.ts`** (sibling to coherence-settling.ts): `OnsetState` = `active` (≥1 front crossed the floor in this step) / `still` (none did). `CoherenceOnsetTransition` carries `from_round`/`to_round` (1-based), `crossing_fronts`, `crossed_dimensions` (which fronts, `localeCompare`-pinned), and `state`. `computeCoherenceOnset(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/volatility/synchrony/settling functions use), scans each front's binding floors (`weakest_tier`, v12) for transitions across the `below-acceptable` rung (v10), and ATTRIBUTES each STATED crossing to the transition that REVEALS it (an UNSTATED round is SKIPPED — neither a crossing nor a reset, §3 — so a crossing across a silent gap lands on the step that reveals the new standing, never the silent step; an OPENING round left entirely unstated reveals NO crossing — the lead-in stays HONEST, silence at the open is not an onset); marks each transition `active`/`still`, locates `onset_round` (the `to_round` of the EARLIEST active step — the round the package first crossed the floor, `null` when none ever did; a forward scan keeps the FIRST only), `lead_in` (the run of LEADING `still` steps before it — the WHOLE sequence when none crossed), `active_count` (steps where any front crossed), `early_onset` (the FIRST transition was active), and `total_crossings` — equal BY CONSTRUCTION to v24's per-front sum AND v25/v26's per-step sum (v27 reads the same crossings for WHERE the first one falls). Returns the series + a namespaced `onset_hash` (SHA-256 over the canonical per-transition set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash/synchrony_hash/settling_hash). `exposureEarlyOnset(onset)` = `onset.early_onset` — the *time-of-first-movement* gate predicate; UNLIKE `exposureVolatile` (a single front crossing ≥2 times), `exposureSynchronized` (≥2 fronts crossing in one step), and `exposureUnsettled` (the LAST crossing on the FINAL step) this fires on the single fact that the FIRST crossing fell on the OPENING step — needs no tuning (a crossing in the opening round IS an early onset). HONEST by construction (§3): **silence does not make an opening early** — an opening round left unstated reveals no crossing, so the lead-in stays honest. **Distinct from v26**: mirror reductions of the same crossings, coinciding only when a deal crosses the floor exactly once; the moment it crosses twice, v27 separates two deals v26 reports identically. **Distinct from v17's whipsaw**: an above-floor jitter (`acceptable → ideal → acceptable`) crosses the floor zero times → no onset round, not early. **Distinct from v21's duration**: a front stuck below floor for all N rounds (stated below from round 1) has a large `rounds_below` but ZERO crossings (it never moved in-sequence) → no onset to v27. `buildCoherenceOnsetJson` (`schema: vaulytica.posture-onset.v1`) + `renderCoherenceOnsetSummary` (the onset verdict [at round K with a clean lead-in / EARLY at the opening round / steady-throughout] + active-step count, then one line per step). **NEAR-ZERO new posture math (one first-crossing reduction) AND zero changes to any existing source file**: v27 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v26 it needed nothing newly exported. **The command, `tools/cli/coherence-onset.ts`** (sibling to coherence-settling.ts): `computeCoherenceOnsetArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceOnset` rendered markdown (default) or JSON, returning `{ok:true, output, earlyOnset, ladderNote}`. `runCoherenceOnset(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-early-onset-exposure` exits 2 ONLY when the first transition crossed the floor. A SEPARATE command, not a `coherence-settling` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-onset"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built ladder-pinned artifacts via a tsx generator, drove the real CLI): a clean lead-in (`acceptable → acceptable → below`) prints `onset: at round 3 — … after 1 steady step of clean lead-in`, `onset_hash: 7bec5911…` and exits **0**; an early cross (`below → acceptable`) prints `onset: EARLY — the floor was first crossed in round 1→2, the opening transition` and exits **2** — proving the first-move axis v26's last-move index cannot show. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the onset stays DERIVED). +22 tests (coherence-onset.test.ts ×14: early onset [gate trips] vs clean lead-in [gate clears], the v26-equivalence separation [two deals identical `settling_round`/`unsettled`, opposite `early_onset`], the reduction invariant [`total_crossings` = v24's/v25's/v26's totals], earliest-crossing across many fronts, silence-attributes-to-revealing-step [§3], silent-opening-round-is-not-early [§3], above-floor whipsaw never crosses [distinct from v17], no-front-ever-crossed [onset null, lead-in = all steps], unstated never counted [§3], determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-onset.test.ts ×8: disk-vs-in-memory onset identity, markdown summary, gate clears on a clean lead-in, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,226 passing + 2 skips** (was 3,204), 213 test files (was 211). Version 9.23.0 → 9.24.0. New [`docs/spec-v27.md`](docs/spec-v27.md); BUILD_PROGRESS v27 §; CHANGELOG [9.24.0]; README badge 3,204→3,226/9.23.0→9.24.0 + "Saved coherence baselines" § extended with the onset workflow + the NINE-axis summary callout + v27 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v27. The one Step (207) is shipped; a `coherence-settling --onset` flag, a configurable "clean until round N" gate, a standalone onset artifact, a re-exposure-latency stat, a per-front onset, an order-inferring walker, and a browser N-round onset are principled deferrals. Feature frontier: every numbered spec v1–v27 now complete end-to-end; the posture matrix now has NINE axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22, per-round level transpose), RECURRENCE (v23), VOLATILITY (v24, per-front crossings), SYNCHRONY (v25, per-step crossings — the transpose of v24), SETTLING (v26, the index of the LAST crossing), and ONSET (v27, the index of the FIRST crossing — the mirror of v26); the open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a re-exposure-LATENCY stat [rounds between a front's fall and its recovery]; a per-front onset/settling; a direction-homogeneous synchrony; depth-weighting still NOT viable — the floor is binary).

## spec-v28 — Document-Free Exposure Recovery Latency (Rounds Below Floor per Episode)

- **Step 208 — The `coherence-latency` subcommand (shipped 2026-06-15, 9.25.0):** The *recovery-latency* reduction of the same crossings v24/v25/v26/v27 count — the GAP between a fall and the recovery that closes it. v24 (`coherence-volatility`) counts a front's crossings (`crossings`); v25 (`coherence-synchrony`) re-buckets them per step; v26 (`coherence-settling`) / v27 (`coherence-onset`) read the INDEX of the LAST / FIRST crossing. All four reduce the crossings to a COUNT or a single INDEX — and a count and an index both throw away the GAP BETWEEN A FALL AND THE RECOVERY THAT CLOSES IT. Two fronts v24 reports IDENTICALLY (both fell once and recovered once → two crossings each) can be opposites here: a fall caught at the next exchange (fell round 2, recovered round 3 → ONE round below) vs. one that festered (fell round 2, recovered round 6 → FOUR rounds below). None of v24/v25/v26/v27 reports the DURATION of the exposure between the paired crossings. v28 supplies the missing AXIS: pair each fall with the recovery that closes it and read the rounds between them. (Fulfills v26/v27 Part XVI's deferred re-exposure-latency stat.) **The latency (pure), `src/report/coherence-latency.ts`** (sibling to coherence-onset.ts): `LatencyClass` = `open` (fell in-sequence, never recovered — an unbounded latency) / `recovered` (every in-sequence fall closed — ≥1 closed episode, none open) / `steady` (stated, but never FELL in-sequence — no fall crossing to pair, incl. a front that recovered from a PRE-ARCHIVE exposure) / `unstated` (§3). `CoherenceLatencyEpisode` = `fall_round` (the round first seen below floor) + `recovery_round` (the round first seen back at-or-above, `null` when never recovered) + `latency` (`recovery_round − fall_round` — rounds below floor; `null` when open). `CoherenceLatencyFront` carries `dimension`, `floors[]` (context), `crossings` (= v24's per-front `crossings`), `episodes[]`, `max_latency` (the front's longest closed episode), and the `latency` class. `computeCoherenceLatency(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/volatility/synchrony/settling/onset functions use), scans each front's binding floors (`weakest_tier`, v12) for transitions across the `below-acceptable` rung (v10), ATTRIBUTES each STATED crossing to the round that REVEALS it (an UNSTATED round is SKIPPED — neither a crossing nor a reset, §3 — so a fall/recovery across a silent gap lands on the round that reveals the new standing, never the silent step; a front stated below from round 1 has NO in-sequence fall to pair — its descent predates the archive — so it contributes NO episode and is `steady`), pairs each FALL (at-or-above → below) with the next RECOVERY (below → at-or-above) into episodes (a recovery with no open fall = a leading recovery → no episode; a fall left unclosed = an OPEN episode), and returns deal-level `class_counts`, `total_crossings` (= v24's per-front sum AND v25/v26/v27's per-step sum BY CONSTRUCTION), `recovered_count`, `open_count`, `max_latency`/`slowest_dimension` (the slowest recovery across the deal, earliest front on a tie via `>`), `unrecovered` (= `open_count > 0`), and a namespaced `latency_hash` (SHA-256 over the canonical per-front episode set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash/synchrony_hash/settling_hash/onset_hash). `exposureUnrecovered(latency)` = `latency.open_count > 0` — the *recovery-latency* gate predicate; UNLIKE `exposureVolatile` (a front crossing ≥2 times), `exposureUnsettled` (the LAST crossing on the FINAL step), and `exposureEarlyOnset` (the FIRST crossing on the OPENING step) this fires on an in-sequence FALL THAT NEVER CLOSED. **Distinct from v21's `exposureOpen`**: v21 fires on a front whose CURRENT STANDING is below floor (incl. a front stated below from round 1 that never FELL in-sequence); v28 fires only on an in-sequence fall that never closed — a front below from round 1 is `open` to v21 but `steady` here (no episode). **Distinct from v17's whipsaw**: an above-floor jitter (`acceptable → ideal → acceptable`) crosses the floor zero times → no episode. `buildCoherenceLatencyJson` (`schema: vaulytica.posture-latency.v1`) + `renderCoherenceLatencySummary` (the slowest-recovery verdict [the front + its rounds below / steady-throughout], the recovered/unrecovered counts, then one line per front holding an episode). **ZERO new posture math beyond the fall/recovery pairing AND zero changes to any existing source file**: v28 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v27 it needed nothing newly exported. **The command, `tools/cli/coherence-latency.ts`** (sibling to coherence-onset.ts): `computeCoherenceLatencyArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceLatency` rendered markdown (default) or JSON, returning `{ok:true, output, unrecovered, ladderNote}`. `runCoherenceLatency(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-unrecovered-exposure` exits 2 ONLY when a front fell and never recovered. A SEPARATE command, not a `coherence-onset` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-latency"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built ladder-pinned artifacts via a tsx generator, drove the real CLI): a slow-but-recovered deal (`acceptable → below → below → below → acceptable`) prints `slowest recovery: Cap — sat below floor for 3 rounds before recovering`, `latency_hash: 3854fb5d…` and exits **0**; an unrecovered fall (`acceptable → below → below`) prints `⚠ Cap: open — fell round 2, never recovered` and exits **2** — proving the recovery-latency axis no count or index can show. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the latency stays DERIVED). +26 tests (coherence-latency.test.ts ×17: prompt vs slow recovery [same v24 crossing count, different latency], the unrecovered episode [gate trips] vs the recovered one [gate clears], the v21 distinction [a front below from round 1 is `open` to v21 but no episode here], leading-recovery is no episode, the reduction invariant [`total_crossings` = v24's/v25's/v26's/v27's totals], slowest-recovery pick across fronts [earliest on a tie], silence-inside-a-gap-does-not-extend [§3], silence-attributes-to-revealing-step [§3], above-floor whipsaw never pairs [distinct from v17], no-front-ever-fell [no episode, max_latency null], unstated never counted [§3], two-episode front [recovered then unrecovered], determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-latency.test.ts ×9: disk-vs-in-memory latency identity, markdown summary, gate trips on unrecovered, gate clears when every fall recovered, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,252 passing + 2 skips** (was 3,226), 215 test files (was 213). Version 9.24.0 → 9.25.0. New [`docs/spec-v28.md`](docs/spec-v28.md); BUILD_PROGRESS v28 §; CHANGELOG [9.25.0]; README badge + "Saved coherence baselines" § extended with the latency workflow + the TEN-axis summary callout + v28 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v28. The one Step (208) is shipped; a `coherence-onset --latency` flag, a configurable "recover within N rounds" gate, a standalone latency artifact, a leading-recovery latency (not computable — the pre-archive fall is unknown), a mean recovery latency, an order-inferring walker, and a browser N-round latency are principled deferrals. Feature frontier: every numbered spec v1–v28 now complete end-to-end; the posture matrix now has TEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22, per-round level transpose), RECURRENCE (v23, per-front episode count), VOLATILITY (v24, per-front crossings), SYNCHRONY (v25, per-step crossings — the transpose of v24), SETTLING (v26, the index of the LAST crossing), ONSET (v27, the index of the FIRST crossing — the mirror of v26), and LATENCY (v28, the GAP between a fall and the recovery that closes it); the open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a per-front onset/settling; a direction-homogeneous synchrony; a per-front×per-step 2-D matrix export; depth-weighting still NOT viable — the floor is binary).
- **Step 209 — The `coherence-concurrency` subcommand (shipped 2026-06-16, 9.26.0):** The *direction-resolved* split of v25's per-step crossing count. v25's `coherence-synchrony` counts, per round-transition, how many fronts crossed the floor that step (`crossing_fronts`) — but it is DIRECTION-BLIND: a step where two fronts FELL below floor together and a step where one fell while another recovered both register as the SAME synchrony (two crossings, both `synchronized`). v25 cannot say WHICH WAY the simultaneous movers went. A deal lead reviewing a close asks a question a direction-blind count discards: was there a single round the package COLLAPSED — two or more fronts moving the SAME way, DOWN, below floor, in one exchange? A redline round that knocks several fronts below floor AT ONCE is materially different from a churn where one front slips while another is won back — yet to v25 both are merely "synchronized". This is not a bug in v25 (a crossing count is SUPPOSED to count, not resolve direction) — it is a missing AXIS: the direction-resolved dimension of the same per-step crossing data. v29 supplies it: split each step's crossings into the fronts that FELL and the fronts that RECOVERED. (Fulfills v25 Part XVI's deferred direction-homogeneous synchrony.) **The concurrency (pure), `src/report/coherence-concurrency.ts`** (sibling to coherence-synchrony.ts): `ConcurrencyClass` (priority order) = `concerted-fall` (≥2 fronts FELL — the gate-worthy class; DOMINATES even if a recovery also happened that step) / `concerted-recovery` (≥2 RECOVERED and <2 fell) / `mixed` (fronts moved BOTH ways but neither direction reached two — exactly one fell AND ≥1 recovered, a churn) / `isolated` (exactly one front crossed, one direction) / `quiet` (no front crossed). `CoherenceConcurrencyTransition` carries `from_round`/`to_round`, the `falling`/`recovering` counts, the `localeCompare`-pinned `falling_dimensions`/`recovering_dimensions` lists, and the step's `concurrency` class. `computeCoherenceConcurrency(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/volatility/synchrony functions use), scans each front's binding floors (`weakest_tier`, v12) for transitions across the `below-acceptable` rung (v10), ATTRIBUTES each STATED crossing to the round that REVEALS it (an UNSTATED round is SKIPPED — neither a crossing nor a reset, §3 — so a fall/recovery across a silent gap lands on the round that reveals the new standing, never the silent step), buckets it into that step's `falling` (a fall, at-or-above → below) or `recovering` (a recovery, below → at-or-above) list, classifies each step by direction, and returns deal-level `per_transition[]`, `total_crossings` (= v24's per-front sum AND v25/v26/v27/v28's totals BY CONSTRUCTION), `total_falls`/`total_recoveries` (the direction split — `total_falls + total_recoveries = total_crossings`), `peak_fall_transition`/`peak_fall_count` (the step the most fronts fell at once, earliest on a tie), `concerted_fall_count`/`concerted_recovery_count`/`mixed_count` (per-class step tallies), `concerted` (= `concerted_fall_count > 0`), and a namespaced `concurrency_hash` (SHA-256 over the canonical per-transition set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash/synchrony_hash/settling_hash/onset_hash/latency_hash). `exposureConcerted(concurrency)` = `concurrency.concerted_fall_count > 0` — the direction-resolved gate predicate; UNLIKE `exposureSynchronized` (any step where ≥2 fronts crossed regardless of direction — a one-down-one-up churn trips it just as a collapse does) this fires ONLY on a step where ≥2 fronts moved the SAME way, DOWN, below floor: a concerted fall. **Distinct from v17's whipsaw**: an above-floor jitter (`acceptable → ideal → acceptable`) crosses the floor zero times → no fall, no recovery. `buildCoherenceConcurrencyJson` (`schema: vaulytica.posture-concurrency.v1`) + `renderCoherenceConcurrencySummary` (the peak-fall verdict, the concerted-fall count, then one line per step showing how many fell and recovered and which). **ZERO new posture math beyond the direction split AND zero changes to any existing source file**: v29 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v28 it needed nothing newly exported. **The command, `tools/cli/coherence-concurrency.ts`** (sibling to coherence-synchrony.ts): `computeCoherenceConcurrencyArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceConcurrency` rendered markdown (default) or JSON, returning `{ok:true, output, concerted, ladderNote}`. `runCoherenceConcurrency(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-concerted-fall` exits 2 ONLY when a step saw ≥2 fronts fall together. A SEPARATE command, not a `coherence-synchrony` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-concurrency"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built ladder-pinned artifacts via a tsx generator, drove the real CLI): a concerted-fall deal (Cap and Term both fall in round 1→2) prints `⚠ round 1→2: 2 fell (Cap, Term), 0 recovered (—) [concerted-fall]`, `concurrency_hash: 32c3e7a2…` and exits **2**; a staggered deal (Cap then Term fall one at a time) classes both steps `isolated` and exits **0** — proving the coordinated-collapse axis v25's direction-blind count cannot isolate. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/coherence-latency/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the concurrency stays DERIVED). +24 tests (coherence-concurrency.test.ts ×15: concerted fall vs churn [same v25 crossing count, different concurrency class], concerted recovery, concerted-fall dominates a step with a simultaneous recovery, isolated single crossing, the reduction invariant [`total_crossings` = v24's/v25's/v28's totals; `total_falls + total_recoveries = total_crossings`], silence-attributes-to-revealing-step [§3], silence-is-quiet [§3], above-floor whipsaw never crosses [distinct from v17], no-front-ever-fell [peak null], earliest-peak-fall tiebreak, determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-concurrency.test.ts ×9: disk-vs-in-memory concurrency identity, markdown summary, gate trips on concerted fall, gate clears when fronts fall one at a time, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,276 passing + 2 skips** (was 3,252), 217 test files (was 215). Version 9.25.0 → 9.26.0. New [`docs/spec-v29.md`](docs/spec-v29.md); BUILD_PROGRESS v29 §; CHANGELOG [9.26.0]; README badge + "Saved coherence baselines" § extended with the concurrency workflow + the ELEVEN-axis summary callout + v29 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v29. The one Step (209) is shipped; a `coherence-synchrony --by-direction` flag, a configurable `--min-concerted N` gate, a `--fail-on-concerted-recovery` gate, a standalone concurrency artifact, a net-direction per-step verdict, an order-inferring walker, and a browser N-round concurrency are principled deferrals. Feature frontier: every numbered spec v1–v29 now complete end-to-end; the posture matrix now has ELEVEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24, per-front crossings), SYNCHRONY (v25, per-step crossings — the transpose of v24), SETTLING (v26, the index of the LAST crossing), ONSET (v27, the index of the FIRST crossing — the mirror of v26), LATENCY (v28, the GAP between a fall and the recovery that closes it), and CONCURRENCY (v29, the DIRECTION SPLIT of v25 — falls vs recoveries per step); the open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a per-front onset/settling; a per-front×per-step 2-D matrix export; a mean recovery latency; a per-front co-fall affinity; depth-weighting still NOT viable — the floor is binary).
- **Step 210 — The `coherence-relapse` subcommand (shipped 2026-06-16, 9.27.0):** The *relapse-interval* axis — the exact MIRROR of v28's recovery latency. v28's `coherence-latency` pairs each FALL (at-or-above → below) FORWARD with the RECOVERY that closes it and reads the rounds BELOW floor between them (`latency`, headlined by the slowest recovery `max_latency`, gated on a fall that NEVER recovered). It is mute on the mirror question a deal lead asks reviewing a close that BOUNCED: once a front RECOVERED above the floor, how long did the fix HOLD before it fell again — and did it ever relapse at the very next round? A front that recovered in round 2 and held until round 6 secured a DURABLE fix; one that recovered in round 2 and fell again in round 3 had a fix that DID NOT HOLD — yet to v24 both have the SAME crossing count (three: fall, recovery, fall), and to v28 a relapse is not a latency at all (v28 reads the gap BELOW, not the gap ABOVE). This is not a bug in v28 (a recovery latency is SUPPOSED to read the gap below) — it is a missing AXIS: the relapse-interval dimension of the same crossing data, the exact mirror of v28. v30 supplies it: pair each RECOVERY forward with the next FALL that undoes it and read the rounds it held ABOVE floor. **The relapse (pure), `src/report/coherence-relapse.ts`** (sibling to coherence-latency.ts): `RelapseClass` = `relapsed` (a recovery was undone by a later fall in-sequence — ≥1 CLOSED interval, the fix did not hold) / `held` (recovered in-sequence and EVERY recovery held — ≥1 OPEN interval, none closed) / `steady` (stated, but never RECOVERED above floor in-sequence — no recovery crossing to pair forward, including a front that never fell AND one that fell and never came back) / `unstated` (no document ever stated the front, §3). `CoherenceRelapseInterval` carries `recovery_round` (1-based, the recovery's to_round), `fall_round` (the next fall's to_round; `null` when the recovery held), `clean_rounds` (`fall_round − recovery_round`, the rounds above floor; `null` when held). `computeCoherenceRelapse(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/volatility/synchrony/settling/onset/latency functions use), scans each front's binding floors (`weakest_tier`, v12) for transitions across the `below-acceptable` rung (v10), ATTRIBUTES each STATED crossing to the round that REVEALS it (an UNSTATED round is SKIPPED — neither a crossing nor a reset, §3 — so a recovery/fall across a silent gap lands on the round that reveals the new standing, never the silent step; a relapse revealed after a silent round SPANS the silent rounds and is therefore never `immediate`), pairs each recovery FORWARD with the next fall into `intervals[]` (a recovery never undone is an OPEN/held interval; a fall with no open recovery — the front's first fall — closes nothing), and returns per-front `min_interval` (shortest CLOSED interval, `null` when none relapsed) + `relapse` class, and deal-level `fronts[]`, `class_counts`, `total_crossings` (= v24's per-front sum AND v25/.../v29's totals BY CONSTRUCTION), `relapse_count` (closed intervals) / `held_count` (open intervals), `min_interval`/`quickest_dimension` (the quickest relapse across the deal, earliest on a tie), `immediate` (any closed interval has `clean_rounds === 1`), and a namespaced `relapse_hash` (SHA-256 over the canonical per-front interval set — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash/synchrony_hash/settling_hash/onset_hash/latency_hash/concurrency_hash). `exposureImmediateRelapse(relapse)` = `relapse.immediate` — the relapse-interval gate predicate; UNLIKE `exposureUnrecovered` (a fall that never closed with a recovery — an unbounded gap BELOW) this fires on a recovery undone the VERY NEXT round (a minimal gap ABOVE). **The mirror asymmetry**: a LEADING recovery (a front below from round 1 that recovers) is `steady` to v28 (no in-sequence fall to pair BACKWARD) but pairs FORWARD here — it either `held` or `relapsed`. **Distinct from v23's recurrence**: ≥2 episodes ⟺ ≥1 relapse, but v23 has NO duration — it cannot tell a recovery undone next round from one that held for rounds; v30 headlines exactly that duration. **Distinct from v17's whipsaw**: an above-floor jitter crosses the floor zero times → no interval. `buildCoherenceRelapseJson` (`schema: vaulytica.posture-relapse.v1`) + `renderCoherenceRelapseSummary` (the quickest-relapse verdict, the relapse/held counts, then one line per front with an interval). **ZERO new posture math beyond the recovery/fall pairing AND zero changes to any existing source file**: v30 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v29 it needed nothing newly exported. **The command, `tools/cli/coherence-relapse.ts`** (sibling to coherence-latency.ts): `computeCoherenceRelapseArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceRelapse` rendered markdown (default) or JSON, returning `{ok:true, output, immediate, ladderNote}`. `runCoherenceRelapse(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-immediate-relapse` exits 2 ONLY when a recovery was undone at the very next round. A SEPARATE command, not a `coherence-latency` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-relapse"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built ladder-pinned artifacts via a tsx generator, drove the real CLI): a deal where Cap recovers round 3 and falls again round 4 prints `⚠ Cap: relapsed — recovered round 3 → fell again round 4 (1 round above)`, `quickest relapse: Cap — held above floor for 1 round`, `relapse_hash: 2a23a2da…` and exits **2** under the gate; a recovery that holds ≥2 rounds clears it — proving the fix-did-not-hold axis the crossing count (v24) and the below-latency (v28) cannot isolate. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/coherence-latency/coherence-concurrency/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the relapse stays DERIVED). +27 tests (coherence-relapse.test.ts ×18: immediate relapse [recovered, fell again next round], durable vs immediate fix [same v24 crossing count, different interval], a held recovery, the v28 mirror [a fall that never recovered is `open` to v28 but `steady` here; a leading recovery is `steady` to v28 but `held`/`relapsed` here], the reduction invariant [`total_crossings` = v24's/v25's/v28's/v29's totals], the quickest-relapse pick across fronts, silence-inside-a-gap-does-not-shorten [§3], silence-attributes-to-revealing-step [§3], above-floor whipsaw never pairs [distinct from v17], no-front-ever-recovered [min_interval null], unstated-is-never-counted [§3], two intervals on one front, determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-relapse.test.ts ×9: disk-vs-in-memory relapse identity, markdown summary, gate trips on immediate relapse, gate clears when a recovery holds ≥2 rounds, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,303 passing + 2 skips** (was 3,276), 219 test files (was 217). Version 9.26.0 → 9.27.0. New [`docs/spec-v30.md`](docs/spec-v30.md); BUILD_PROGRESS v30 §; CHANGELOG [9.27.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the relapse workflow + the TWELVE-axis summary callout + v30 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v30. The one Step (210) is shipped; a `coherence-latency --relapse` flag, a configurable `--min-interval N` gate, a standalone relapse artifact, a trailing-relapse interval, a mean relapse interval, an order-inferring walker, and a browser N-round relapse are principled deferrals. Feature frontier: every numbered spec v1–v30 now complete end-to-end; the posture matrix now has TWELVE axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28, the GAP below between a fall and its recovery), CONCURRENCY (v29), and RELAPSE (v30, the MIRROR of v28 — the GAP above between a recovery and the fall that undoes it); the open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a per-front onset/settling; a per-front×per-step 2-D matrix export; a mean recovery latency or relapse interval; a per-front co-fall affinity; a cadence RATIO of rounds-below vs rounds-above; depth-weighting still NOT viable — the floor is binary).
- **Step 211 — The `coherence-tenure` subcommand (shipped 2026-06-16, 9.28.0):** The *occupancy* axis the whole posture family never read. Every command from v21 on reads how a front MOVES (crossings, episodes, gaps, indices) or where it STANDS NOW (v21's endpoint); none reads how MUCH of a front's stated span sat below floor. v21's `coherence-persistence` reports a raw `rounds_below` COUNT against the TOTAL round span and gates on the ENDPOINT (a front below floor NOW — `--fail-on-open-exposure`). It is mute on the question a deal lead asks auditing the WHOLE package: across the rounds that actually put a front on the table, how much of that span was it an unaccepted position — was it below floor more often than not? A front that dipped below floor in round 2 only and recovered (a BRIEF dip, 1 of 5 stated rounds) and a front that sat below floor through rounds 1–4 and recovered only at round 5 (a CHRONIC burden, 4 of 5 stated rounds) are IDENTICAL to v21 (both `resolved`, both clear the endpoint gate) — yet one was a fleeting concern and the other was unacceptable for the MAJORITY of the deal. This is not a bug in v21 (a current-standing reduction is SUPPOSED to read the endpoint) — it is a missing AXIS: the occupancy dimension of the same below-floor data, with a denominator correction v21 lacks (v21 divides by the TOTAL round count `n`, diluting a front stated in only a few rounds; v31 divides by the front's STATED rounds, the §3-honest denominator). v31 supplies it: per front, the share of the rounds that STATED it which sat below floor. **The tenure (pure), `src/report/coherence-tenure.ts`** (sibling to coherence-persistence.ts): `TenureClass` = `majority` (below floor for a STRICT MAJORITY of its stated rounds, `below_rounds × 2 > stated_rounds` — the gate-worthy class) / `minority` (stated and below floor in ≥1 round but not a majority, INCLUDING an exact split like below 2 of 4) / `none` (stated, never below floor) / `unstated` (no document ever stated the front, §3). `CoherenceTenureFront` carries `dimension`, `floors[]`, `stated_rounds` (the §3-honest denominator), `below_rounds` (the numerator), `share` (`below_rounds / stated_rounds`, `null` when unstated), and its `tenure` class. `computeCoherenceTenure(rounds)` matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/persistence/latency functions use), counts each front's stated rounds and below-floor rounds (an UNSTATED round counts toward NEITHER — silence is not exposure and not a clean round, §3), tests the strict majority, and selects the deal's heaviest share by INTEGER CROSS-MULTIPLICATION (`below × stated` of the two fronts — never a float compare, so the ranking is platform-exact; earliest dimension on a tie), returning deal-level `fronts[]`, `class_counts`, `total_below_rounds` (= v21's `rounds_below` summed across fronts BY CONSTRUCTION) / `total_stated_rounds`, `max_share`/`heaviest_dimension` (`null` when no front was ever below floor), `majority` (any front below for a strict majority — the gate verdict), and a namespaced `tenure_hash` (SHA-256 over the canonical per-front occupancy set — the INTEGER `stated_rounds`/`below_rounds` and class; the derived float `share` is OMITTED, so the fingerprint is integer-exact — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash/synchrony_hash/settling_hash/onset_hash/latency_hash/concurrency_hash/relapse_hash). `exposureMajorityBelow(tenure)` = `tenure.majority` — the occupancy gate predicate; UNLIKE `exposureOpen` (a front below floor at its LATEST stated round — including a fresh, late dip in 1 of many rounds) this fires on the AGGREGATE BURDEN: a front below floor for more than half the span it was discussed, EVEN IF it recovered by the close. **The opposite asymmetries:** a front below floor only at the close is `open` to v21 (gate trips) but a MINORITY to v31 (gate clears); a front below floor for rounds 1–4 of 5 that recovers at round 5 is `resolved` to v21 (gate clears) but a MAJORITY to v31 (gate trips). **Honest by construction (§3):** silence dilutes neither numerator nor denominator, so a front below floor both times it was on the table reads 100%, not 40% diluted by rounds it was never part of. **Distinct from v24/v28:** v24 counts crossings, v28 the per-EPISODE below-duration; a front with one long episode and one with one brief episode have the SAME crossing count (v24:2) — v31 separates them by share. `buildCoherenceTenureJson` (`schema: vaulytica.posture-tenure.v1`) + `renderCoherenceTenureSummary` (the heaviest-occupancy verdict, the class tally, then one line per below-floor front, `majority` first). **ZERO new posture math beyond the occupancy count AND zero changes to any existing source file**: v31 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v30 it needed nothing newly exported. **The command, `tools/cli/coherence-tenure.ts`** (sibling to coherence-persistence.ts): `computeCoherenceTenureArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceTenure` rendered markdown (default) or JSON, returning `{ok:true, output, majority, ladderNote}`. `runCoherenceTenure(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-majority-below` exits 2 ONLY when a front was below floor for a strict majority of its stated rounds. A SEPARATE command, not a `coherence-persistence` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-tenure"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built three ladder-pinned artifacts via a tsx generator, drove the real CLI): a deal where Cap is below floor in 2 of 3 stated rounds and Term in 1 of 3 prints `heaviest occupancy: Cap — below floor 67% of its stated rounds`, `⚠ Cap: majority — below floor 2 of 3 stated rounds (67%)`, `· Term: minority — below floor 1 of 3 stated rounds (33%)`, `tenure_hash: 23bd1085…` and exits **2** under the gate — proving the burden axis the endpoint (v21) and the per-episode gaps (v28) cannot pose. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/coherence-latency/coherence-concurrency/coherence-relapse/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the tenure stays DERIVED). +24 tests (coherence-tenure.test.ts ×15: strict majority [below 4 of 5], brief dip vs chronic burden [both `resolved` to v21, opposite tenure], fresh late dip [`open` to v21 but minority here], exact split [minority not majority], stated-vs-total normalization [§3], the join invariant [`total_below_rounds` = v21's `rounds_below` sum], heaviest-share pick by exact integer ratio, tie-break by earliest dimension, no-front-ever-below [max_share null], unstated-never-counted [§3], stated-once-and-below → 100%/majority, determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-tenure.test.ts ×9: disk-vs-in-memory tenure identity, markdown summary, gate trips on a strict majority, gate clears on an exact split, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,327 passing + 2 skips** (was 3,303), 221 test files (was 219). Version 9.27.0 → 9.28.0. New [`docs/spec-v31.md`](docs/spec-v31.md); BUILD_PROGRESS v31 §; CHANGELOG [9.28.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the tenure workflow + the THIRTEEN-axis summary callout + v31 spec-table row + CLI cheat-sheet + commands table entry + specs list brought current v1–v31. The one Step (211) is shipped; a `coherence-persistence --share` flag, a configurable `--max-share P` gate, a `--min-stated N` floor, a standalone tenure artifact, a below/above cadence ratio, a tenure-weighted per-round occupancy, an order-inferring walker, and a browser N-round tenure are principled deferrals. Feature frontier: every numbered spec v1–v31 now complete end-to-end; the posture matrix now has THIRTEEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), and TENURE (v31, the OCCUPANCY — the share of a front's stated span below floor, the aggregate burden the endpoint/count/gap axes all discard); the open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a below/above cadence RATIO; a per-front onset/settling; a per-front×per-step 2-D matrix export; a mean recovery latency or relapse interval; a per-front co-fall affinity; depth-weighting still NOT viable — the floor is binary).
- **Step 212 — The `coherence-affinity` subcommand (shipped 2026-06-16, 9.29.0):** The first *pairwise* axis — the relational read every prior reduction discards. Every command from v20 on reduces to a SCALAR: per FRONT (a level v20, a count v21/v23/v24, a share v31, a gap v28/v30), per ROUND (a breadth v22), or per STEP (a synchrony v25, a concurrency v29). v25 and v29 come CLOSEST to a relational read — they count, per step, how many fronts crossed (v25) or fell (v29) together — but they COLLAPSE the relation to a count the instant they record it: v29 knows round 3 saw two fronts fall, even lists WHICH two in `falling_dimensions`, but never asks across the sequence whether it is the SAME two again and again. **Which two fronts does the counterparty keep conceding as a block?** is a question no command can answer, because every command throws the pair identity away. Two deals with byte-IDENTICAL v29 concurrency profiles (same per-step fall counts) can be opposites: one a single STABLE pair falling together every time (a linked concession — the other side trades Cap FOR Term), the other a DIFFERENT co-falling pair every round (no coupling, same counts). This is not a bug in v29 (a per-step concurrency is SUPPOSED to count the step) — it is a missing AXIS: the pairwise dimension of the same fall data, the first the posture family has that is NOT a per-front/per-round/per-episode scalar. v32 supplies it: for each unordered pair of fronts, how reliably the two fell below floor TOGETHER across the deal. **The affinity (pure), `src/report/coherence-affinity.ts`** (sibling to coherence-concurrency.ts): `AffinityClass` = `coupled` (the two fronts fell together for a STRICT MAJORITY of the steps either fell, `co_falls × 2 > union_falls` — the gate-worthy class) / `incidental` (they fell together in ≥1 step but not a majority of the union, INCLUDING an exact split like together 1 of a 2-step union); only pairs with `co_falls ≥ 1` are classified — a pair that never both-fell has no affinity edge and is OMITTED. `CoherenceAffinityPair` carries `dimension_a`/`dimension_b` (pinned, `a < b`), `a_falls`/`b_falls` (each front's total falls), `co_falls` (transitions BOTH fell), `union_falls` (transitions EITHER fell, `a_falls + b_falls − co_falls`), `affinity` (`co_falls / union_falls`, the Jaccard overlap of the two fronts' fall-step sets), and its `class`. `computeCoherenceAffinity(rounds)` derives each front's fall-step set (the SAME v29 FALL event: at-or-above → below, silence-skipping — an UNSTATED round is SKIPPED, neither a fall nor a reset, so a fall across a silent gap lands on the round that REVEALS the new standing, never the silent step, §3), matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/concurrency functions use), enumerates unordered pairs in that order, intersects the two fall-step sets for `co_falls`, computes the union/affinity/strict-majority class, and selects the deal's tightest affinity by INTEGER CROSS-MULTIPLICATION (`co × union` of the two pairs — never a float compare, so the ranking is platform-exact; earliest pair on a tie), returning deal-level `pairs[]`, `class_counts`, `total_co_falls` (= `Σ_t C(falling_t, 2)` over v29's per-step fall counts BY CONSTRUCTION) / `total_falls` (= v29's `total_falls` AND v24's per-front fall-crossing sum), `max_affinity`/`tightest_pair` (`null` when no two fronts ever fell together), `coupled` (any pair a strict-majority coupling — the gate verdict), and a namespaced `affinity_hash` (SHA-256 over the canonical per-pair set — the INTEGER `a_falls`/`b_falls`/`co_falls` and class; the derived float `affinity` and derived integer `union_falls` are OMITTED, so the fingerprint is integer-exact over the inputs — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash/synchrony_hash/settling_hash/onset_hash/latency_hash/concurrency_hash/relapse_hash/tenure_hash). `exposureCoupled(affinity)` = `affinity.coupled` — the pairwise gate predicate; UNLIKE `exposureConcerted` (which fires on a SINGLE step where ≥2 fronts fell — a one-off lurch trips it, even three DIFFERENT pairs each falling together once) this fires on a STABLE relation: two fronts that fell together more often than they fell apart across the deal, EVEN IF no single step ever saw a third front down. **The distinctness:** every co-fall step IS a v29 concerted-fall step (two fronts down at once), so `coupled ⟹ concerted` for that step — but the reverse fails where it matters: three coincidental concerted falls (different pairs) trip v29 yet leave every pair `incidental` here. v29 asks "did any step lurch?"; v32 asks "is there a STABLE concession pairing?". **§3 honesty:** silence is neither a fall nor a recovery, and a never-both-fell pair is omitted (no affinity edge). **Lineage:** the relation aggregates the same fall crossings v24 counts and v29 splits by direction — `total_co_falls` and `total_falls` agree with v29 by construction. `buildCoherenceAffinityJson` (`schema: vaulytica.posture-affinity.v1`) + `renderCoherenceAffinitySummary` (the tightest-pairing verdict, the class tally, then one line per co-falling pair, `coupled` first). **ZERO new posture math beyond the per-pair overlap AND zero changes to any existing source file**: v32 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v31 it needed nothing newly exported. **The command, `tools/cli/coherence-affinity.ts`** (sibling to coherence-concurrency.ts): `computeCoherenceAffinityArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceAffinity` rendered markdown (default) or JSON, returning `{ok:true, output, coupled, ladderNote}`. `runCoherenceAffinity(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-coupled-fronts` exits 2 ONLY when a pair fell together for a strict majority of the steps either fell. A SEPARATE command, not a `coherence-concurrency` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-affinity"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built four ladder-pinned artifacts via a tsx generator, drove the real CLI): a deal where Cap and Term both fall together at rounds 2 and 4 prints `tightest pairing: Cap + Term — fell together 100% of the steps either fell`, `⚠ Cap + Term: coupled — together 2 of 2 steps either fell (100%); Cap fell 2×, Term fell 2×`, `affinity_hash: 4d8f179e…` and exits **2** under the gate — proving the linked-concession axis the per-step count (v29), the per-front count (v24), and the per-front share (v31) structurally cannot pose. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/coherence-latency/coherence-concurrency/coherence-relapse/coherence-tenure/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the affinity stays DERIVED). +23 tests (coherence-affinity.test.ts ×14: stable coupling [together 2 of 2], coupling vs coincidence [v29 trips on a concerted step, v32 clears — co 1 of union 4], a co-fall requires BOTH to fall the same step [one-fell-one-recovered is not a co-fall], exact split [incidental not coupled], the join invariants [`total_co_falls` = `Σ C(falling, 2)`, `total_falls` = v29's], tightest-pairing pick by exact integer ratio, tie-break by earliest pair, no-two-fronts-ever-fell-together [max_affinity null], silence-across-a-gap-on-the-revealing-step [§3], both-fell-once → 100%/coupled, determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-affinity.test.ts ×9: disk-vs-in-memory affinity identity, markdown summary, gate trips on a coupled pair, gate clears when fronts never couple, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,350 passing + 2 skips** (was 3,327), 223 test files (was 221). Version 9.28.0 → 9.29.0. New [`docs/spec-v32.md`](docs/spec-v32.md); BUILD_PROGRESS v32 §; CHANGELOG [9.29.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the affinity workflow + the FOURTEEN-axis summary callout + v32 spec-table row + CLI cheat-sheet + commands table entry (twenty-one) + specs list brought current v1–v32. The one Step (212) is shipped; a `coherence-concurrency --pairs` flag, a configurable `--min-co-falls K` gate, a `--min-union N` floor, a co-RECOVERY affinity (the recovery-direction mirror), a co-OCCUPANCY affinity (both below floor in the same round, no shared fall), a standalone affinity artifact, a full O(F²) dense matrix, an order-inferring walker, and a browser N-round affinity are principled deferrals. Feature frontier: every numbered spec v1–v32 now complete end-to-end; the posture matrix now has FOURTEEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), and AFFINITY (v32, the first PAIRWISE read — which two fronts fall below floor TOGETHER, the relational structure every scalar reduction discards); the open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a co-RECOVERY affinity [the recovery-direction mirror of v32]; a below/above cadence RATIO; a per-front onset/settling; a per-front×per-step 2-D matrix export; a mean recovery latency or relapse interval; depth-weighting still NOT viable — the floor is binary).
- **Step 213 — The `coherence-recovery-affinity` subcommand (shipped 2026-06-16, 9.30.0):** The recovery-direction MIRROR of v32's pairwise co-fall affinity — exactly as v30 (relapse) mirrors v28 (latency) and v27 (onset) mirrors v26 (settling). v32's `coherence-affinity` reads, per unordered pair of fronts, how reliably the two FELL below floor TOGETHER (the concession linkage — `co_falls`/`union_falls`, a Jaccard `affinity`, a strict-majority `coupled` verdict). But the floor is crossed TWO ways, and v32 reads only one. v29 resolved each step into FALLS and RECOVERIES; v32 paired the FALL direction. The mirror is unread: across the deal, **which two fronts does the counterparty keep RESTORING as a block?** A recovery on Cap that only ever lands alongside a recovery on Term is a LINKED RECOVERY — Cap is hostage to Term, repaired as a bundle, never alone. Two deals with byte-IDENTICAL v32 co-fall affinity (the same fronts conceded together) can have OPPOSITE recovery coupling: one restored as a block every time, the other independently. (Fulfills v32 Part XVI's deferred co-recovery affinity.) **The affinity (pure), `src/report/coherence-recovery-affinity.ts`** (sibling to coherence-affinity.ts): `RecoveryAffinityClass` = `coupled` (the two fronts recovered together for a STRICT MAJORITY of the steps either recovered, `co_recoveries × 2 > union_recoveries` — the gate-worthy class) / `incidental` (together in ≥1 step but not a majority of the union, INCLUDING an exact split); only pairs with `co_recoveries ≥ 1` are classified — a pair that never both-recovered has no affinity edge and is OMITTED. `CoherenceRecoveryAffinityPair` carries `dimension_a`/`dimension_b` (pinned, `a < b`), `a_recoveries`/`b_recoveries` (each front's total recoveries), `co_recoveries` (transitions BOTH recovered), `union_recoveries` (transitions EITHER recovered, `a_recoveries + b_recoveries − co_recoveries`), `affinity` (`co_recoveries / union_recoveries`, the Jaccard overlap of the two fronts' recovery-step sets), and its `class`. `computeCoherenceRecoveryAffinity(rounds)` derives each front's recovery-step set (the SAME v29 RECOVERY event: below → at-or-above, silence-skipping — an UNSTATED round is SKIPPED, neither a recovery nor a reset, so a recovery across a silent gap lands on the round that REVEALS the new standing, never the silent step, §3), matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/concurrency/affinity functions use), enumerates unordered pairs in that order, intersects the two recovery-step sets for `co_recoveries`, computes the union/affinity/strict-majority class, and selects the deal's tightest affinity by INTEGER CROSS-MULTIPLICATION (`co × union` of the two pairs — never a float compare, so the ranking is platform-exact; earliest pair on a tie), returning deal-level `pairs[]`, `class_counts`, `total_co_recoveries` (= `Σ_t C(recovering_t, 2)` over v29's per-step recovery counts BY CONSTRUCTION) / `total_recoveries` (= v29's `total_recoveries` AND the recovery half of v24's per-front crossing sum), `max_affinity`/`tightest_pair` (`null` when no two fronts ever recovered together), `coupled` (any pair a strict-majority coupling — the gate verdict), and a namespaced `recovery_affinity_hash` (SHA-256 over the canonical per-pair set — the INTEGER `a_recoveries`/`b_recoveries`/`co_recoveries` and class; the derived float `affinity` and derived integer `union_recoveries` are OMITTED, so the fingerprint is integer-exact over the inputs — apart from every coherence_hash/movement_hash/trajectory_hash/shift_trajectory_hash/arc_hash/exposure_hash/persistence_hash/breadth_hash/recurrence_hash/volatility_hash/synchrony_hash/settling_hash/onset_hash/latency_hash/concurrency_hash/relapse_hash/tenure_hash/affinity_hash). `exposureRecoveryCoupled(affinity)` = `affinity.coupled` — the recovery-direction pairwise gate predicate; UNLIKE v32's `exposureCoupled` (the FALL-direction pairing) and v29's `exposureConcerted` (a single per-step fall count) this fires on a stable RESTORATION pairing: two fronts that recovered together more often than apart across the deal. **The distinctness:** a pair `coupled` on FALLS (v32) can be `incidental` on RECOVERIES (v33) and vice versa — the two directions are INDEPENDENT relations over the same `floors[]` matrix (the same way a v28 latency and a v30 relapse are independent episodes); every co-recovery step is a v29 `recovering ≥ 2` step, but three DIFFERENT pairs each recovering together once give v29 three such steps yet leave every pair `incidental` here. **§3 honesty:** silence is neither a fall nor a recovery, and a never-both-recovered pair is omitted (no affinity edge). **Lineage:** the relation aggregates the same recovery crossings v24 counts and v29 splits by direction — `total_co_recoveries` and `total_recoveries` agree with v29 by construction. `buildCoherenceRecoveryAffinityJson` (`schema: vaulytica.posture-recovery-affinity.v1`) + `renderCoherenceRecoveryAffinitySummary` (the tightest-pairing verdict, the class tally, then one line per co-recovering pair, `coupled` first). **ZERO new posture math beyond the per-pair overlap AND zero changes to any existing source file**: v33 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v32 it needed nothing newly exported. **The command, `tools/cli/coherence-recovery-affinity.ts`** (sibling to coherence-affinity.ts): `computeCoherenceRecoveryAffinityArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceRecoveryAffinity` rendered markdown (default) or JSON, returning `{ok:true, output, coupled, ladderNote}`. `runCoherenceRecoveryAffinity(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-coupled-recoveries` exits 2 ONLY when a pair recovered together for a strict majority of the steps either recovered. A SEPARATE command, not a `coherence-affinity` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-recovery-affinity"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built four ladder-pinned artifacts via a tsx generator, drove the real CLI): a deal where Cap and Term both recover together at rounds 2 and 4 prints `tightest pairing: Cap + Term — recovered together 100% of the steps either recovered`, `⚠ Cap + Term: coupled — together 2 of 2 steps either recovered (100%); Cap recovered 2×, Term recovered 2×`, `recovery_affinity_hash: fcecb780…` and exits **2** under the gate — proving the linked-restoration axis the fall-direction read (v32), the per-step count (v29), and the per-front count (v24) structurally cannot pose. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/coherence-latency/coherence-concurrency/coherence-relapse/coherence-tenure/coherence-affinity/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the affinity stays DERIVED). +24 tests (coherence-recovery-affinity.test.ts ×15: linked recovery [together 2 of 2], linked vs independent recovery, a co-recovery requires BOTH to recover the same step [one-fell-one-recovered is not a co-recovery], exact split [incidental not coupled], the join invariants [`total_co_recoveries` = `Σ C(recovering, 2)`, `total_recoveries` = v29's], tightest-pairing pick by exact integer ratio, tie-break by earliest pair, no-two-fronts-ever-recovered-together [max_affinity null], silence-across-a-gap-on-the-revealing-step [§3], both-recovered-once → 100%/coupled, the v32-DIVERGENCE [a pair coupled on falls can be edgeless on recoveries], determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-recovery-affinity.test.ts ×9: disk-vs-in-memory affinity identity, markdown summary, gate trips on a coupled pair, gate clears when fronts never couple, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,374 passing + 2 skips** (was 3,350), 225 test files (was 223). Version 9.29.0 → 9.30.0. New [`docs/spec-v33.md`](docs/spec-v33.md); BUILD_PROGRESS v33 §; CHANGELOG [9.30.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the recovery-affinity workflow + the FIFTEEN-axis summary callout + v33 spec-table row + CLI cheat-sheet + commands table entries (added the missing v32 affinity row + the new v33 row; twenty-two commands) + dispatcher command lists current v1–v33. The one Step (213) is shipped; a `coherence-affinity --recoveries` flag, a configurable `--min-co-recoveries K` gate, a `--min-union N` floor, a combined co-fall/co-recovery matrix, a co-OCCUPANCY affinity (both above floor in the same round, no shared recovery), a standalone recovery-affinity artifact, a full O(F²) dense matrix, an order-inferring walker, and a browser N-round affinity are principled deferrals. Feature frontier: every numbered spec v1–v33 now complete end-to-end; the posture matrix now has FIFTEEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32, the first PAIRWISE read — which two fronts FALL below floor together), and RECOVERY-AFFINITY (v33, the MIRROR of v32 — which two fronts RECOVER above floor together, the restoration linkage the fall-direction read discards); the open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a below/above cadence RATIO; a per-front onset/settling; a per-front×per-step 2-D matrix export; a mean recovery latency or relapse interval; a combined co-fall/co-recovery directional matrix; depth-weighting still NOT viable — the floor is binary).
- **Step 214 — The `coherence-opposition` subcommand (shipped 2026-06-16, 9.31.0):** The OFF-DIAGONAL completion of v32's co-fall and v33's co-recovery pairwise reads. v32 reads, per unordered pair of fronts, how reliably the two FELL below floor TOGETHER (the concession block); v33 reads how reliably the two RECOVERED TOGETHER (the restoration block). Those are two of the four cells of the 2×2 of {front A falls/recovers} × {front B falls/recovers} — the two ALIGNED cells. The off-diagonal is unread: across the deal, **which two fronts does the counterparty move in OPPOSITE directions at once?** A step where Cap FALLS exactly as Term RECOVERS is a COUNTER-MOVE — a trade-off, the counterparty giving ground on one front precisely as it takes ground on another. The two fronts are SUBSTITUTES, bargaining chips swapped against each other. Two deals with byte-IDENTICAL v32 co-fall and v33 co-recovery affinity can be opposites here (the same fronts conceded/restored as blocs, but one pair traded against each other every joint step, another never). (Fulfills v33 Part XVI's deferred combined-directional read, as a SEPARATE one-gate command.) **The affinity (pure), `src/report/coherence-opposition.ts`** (sibling to coherence-recovery-affinity.ts): `OppositionClass` = `opposed` (the two fronts moved OPPOSITE ways for a STRICT MAJORITY of the steps BOTH crossed, `opposed_moves × 2 > joint_moves` — the gate-worthy class) / `incidental` (opposed in ≥1 step but not a majority of the joint moves, INCLUDING an exact split); only pairs with `opposed_moves ≥ 1` are classified — a pair that never counter-moved (always aligned, or never jointly crossed) has no opposition edge and is OMITTED. `CoherenceOppositionPair` carries `dimension_a`/`dimension_b` (pinned, `a < b`), `co_falls`/`co_recoveries` (the ALIGNED split — a v32 co-fall and a v33 co-recovery), `opposed_moves` (transitions the two moved OPPOSITE ways — one fell, one recovered), `joint_moves` (transitions BOTH crossed, `co_falls + co_recoveries + opposed_moves`), `affinity` (`opposed_moves / joint_moves`), and its `class`. `computeCoherenceOpposition(rounds)` derives each front's FALL-step set AND RECOVERY-step set (the SAME v29 events, silence-skipping — an UNSTATED round is SKIPPED, so a crossing across a silent gap lands on the round that REVEALS the new standing, §3; a front can't both fall and recover the same transition, so the two sets are disjoint), matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/concurrency/affinity functions use), enumerates unordered pairs in that order, intersects A's falls with B's recoveries AND A's recoveries with B's falls for `opposed_moves`, computes the aligned split/joint/affinity/strict-majority class, and selects the deal's MOST-OPPOSED affinity by INTEGER CROSS-MULTIPLICATION (`opposed × joint` of the two pairs — never a float compare, so the ranking is platform-exact; earliest pair on a tie), returning deal-level `pairs[]`, `class_counts`, `total_opposed_moves` (= `Σ_t falling_t × recovering_t` over v29's per-step counts BY CONSTRUCTION) / `total_aligned_moves` (= v32's `total_co_falls` + v33's `total_co_recoveries` — accumulated over EVERY pair, including omitted aligned-only pairs, so the invariant holds across the whole matrix; their sum = `Σ_t C(crossing_t, 2)` = v25's per-step crossings choose-two'd), `max_affinity`/`most_opposed_pair` (`null` when no two fronts ever counter-moved), `opposed` (any pair a strict-majority counter-move — the gate verdict), and a namespaced `opposition_hash` (SHA-256 over the canonical per-pair set — the INTEGER `co_falls`/`co_recoveries`/`opposed_moves` and class; the derived float `affinity` and derived integer `joint_moves` are OMITTED, so the fingerprint is integer-exact over the inputs — apart from every coherence_hash/…/affinity_hash/recovery_affinity_hash). `exposureOpposed(opposition)` = `opposition.opposed` — the opposed-direction pairwise gate predicate; UNLIKE v32's `exposureCoupled`/v33's `exposureRecoveryCoupled` (the ALIGNED-direction pairings) and v29's `exposureConcerted` (a single per-step fall count) this fires on a stable SEE-SAW: two fronts traded against each other more often than they moved together across the deal. **The distinctness:** a pair `coupled` on FALLS (v32) or RECOVERIES (v33) can have NO opposition edge at all here and vice versa — the aligned and opposed relations are INDEPENDENT reads of the same `floors[]` matrix; every opposed step is a v29 `mixed`/concerted step (≥1 fell AND ≥1 recovered), but three DIFFERENT pairs each opposing once give v29 three mixed steps yet leave every pair `incidental` here. **§3 honesty:** silence is neither a fall nor a recovery, and a never-opposed pair is omitted (no opposition edge). **Lineage:** the relation aggregates the same fall/recovery crossings v24 counts and v29 splits by direction — the three pairwise reads (v32 co-fall, v33 co-recovery, v34 opposed) PARTITION each pair's joint moves exactly. `buildCoherenceOppositionJson` (`schema: vaulytica.posture-opposition.v1`) + `renderCoherenceOppositionSummary` (the most-opposed verdict, the class tally, then one line per counter-moving pair, `opposed` first). **ZERO new posture math beyond the per-pair opposite-direction overlap AND zero changes to any existing source file**: v34 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v33 it needed nothing newly exported. **The command, `tools/cli/coherence-opposition.ts`** (sibling to coherence-recovery-affinity.ts): `computeCoherenceOppositionArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceOpposition` rendered markdown (default) or JSON, returning `{ok:true, output, opposed, ladderNote}`. `runCoherenceOpposition(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-opposed-fronts` exits 2 ONLY when a pair counter-moved for a strict majority of the steps both crossed. A SEPARATE command, not a `coherence-affinity` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-opposition"`) + USAGE + header doc + unknown-command error list. **VERIFIED** via the dispatcher (`run.ts help` lists the command; bad-args usage message; the full suite drives the artifacts core byte-for-byte against the in-memory module): a deal where Cap and Term SWAP each step prints `most-opposed pairing: Cap + Term — counter-moved 100% of the steps both crossed`, `⚠ Cap + Term: opposed — opposed 3 of 3 steps both crossed (100%); aligned 0 (0 co-fell, 0 co-recovered)` and exits **2** under the gate — proving the substitution axis the aligned reads (v32/v33), the per-step count (v29), and the per-front count (v24) structurally cannot pose. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/coherence-latency/coherence-concurrency/coherence-relapse/coherence-tenure/coherence-affinity/coherence-recovery-affinity/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the affinity stays DERIVED). +23 tests (coherence-opposition.test.ts ×14: the see-saw [opposed 3 of 3], a counter-move requires one-falls-one-recovers [co-falls/co-recoveries are not opposed], exact split [incidental not opposed], the join invariants [`total_opposed_moves` = `Σ falling × recovering`, `total_aligned_moves` = v32 + v33], most-opposed pick by exact integer ratio [ratio beats label order], tie-break by earliest pair, no-two-fronts-ever-opposed [max_affinity null], silence-across-a-gap-on-the-revealing-step [§3], both-opposed-once → 100%/opposed, the v32-DIVERGENCE [a pair coupled on falls has no opposition edge], determinism, <2-rounds throws, render+JSON ×2; tools/cli/coherence-opposition.test.ts ×9: disk-vs-in-memory affinity identity, markdown summary, gate trips on an opposed pair, gate clears on an always-aligned pair, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,397 passing + 2 skips** (was 3,374), 227 test files (was 225). Version 9.30.0 → 9.31.0. New [`docs/spec-v34.md`](docs/spec-v34.md); BUILD_PROGRESS v34 §; CHANGELOG [9.31.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the opposition workflow + the SIXTEEN-axis summary callout + v34 spec-table row + CLI cheat-sheet + commands table entry (twenty-three commands) + dispatcher command lists current v1–v34. The one Step (214) is shipped; a `coherence-affinity --opposed` flag, a configurable `--min-opposed K` gate, a `--min-joint N` floor, a combined aligned/opposed matrix, a co-STANDING opposition (one above one below, no shared crossing), a directional precedence (who crosses first), a standalone opposition artifact, a full O(F²) dense matrix, an order-inferring walker, and a browser N-round affinity are principled deferrals. Feature frontier: every numbered spec v1–v34 now complete end-to-end; the posture matrix now has SIXTEEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32, the first PAIRWISE read — which two fronts FALL together), RECOVERY-AFFINITY (v33, the MIRROR — which two RECOVER together), and OPPOSITION (v34, the OFF-DIAGONAL completion — which two move in OPPOSITE directions at once, the substitution the aligned reads discard); the three pairwise reads now partition each pair's joint moves exactly. The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a below/above cadence RATIO; a per-front onset/settling; a directional precedence/lead-lag within a pair; a per-front×per-step 2-D matrix export; a mean recovery latency or relapse interval; depth-weighting still NOT viable — the floor is binary).
- **Step 215 — The `coherence-precedence` subcommand (shipped 2026-06-16, 9.32.0):** The posture family's FIRST DIRECTIONAL pairwise read. v32 (co-fall), v33 (co-recovery), and v34 (counter-move) are all SAME-STEP — they read which two fronts cross the floor TOGETHER in one transition (aligned or opposed) and are blind to order ACROSS transitions. The ordering relation is unread: across the deal, when fronts A and B both eventually cross the floor, does one reliably move FIRST? A front that crosses the floor in rounds 1 and 3 while its partner crosses in rounds 2 and 4, deal after deal, is a LEAD-LAG pairing — the leader is an EARLY-WARNING INDICATOR for the follower: movement on the leader reliably precedes movement on the follower, so a deal lead watching the leader gets advance notice on the follower. Two deals with byte-IDENTICAL v32/v33/v34 same-step couplings can have opposite precedence (a pair that always co-crosses has no lead-lag; a pair that never shares a step can be the strongest leader). (Fulfills v34 Part XVI's deferred directional-precedence read, as a SEPARATE one-gate command.) **The report (pure), `src/report/coherence-precedence.ts`** (sibling to coherence-opposition.ts): `PrecedenceClass` = `leading` (one front crossed the floor BEFORE the other for a STRICT MAJORITY of the comparisons, `max(a_leads, b_leads) × 2 > comparisons` — a consistent first-mover, the gate-worthy class) / `interleaved` (mixed order with no strict-majority first-mover, INCLUDING an exact split or a pair whose comparisons are dominated by same-step ties); only pairs with ≥1 ORDERED comparison (`a_leads + b_leads ≥ 1`) are classified — a pair the two never crossed in a DIFFERENT step (one never crossed, or they always crossed together) has no lead-lag edge and is OMITTED. `CoherencePrecedencePair` carries `dimension_a`/`dimension_b` (pinned, `a < b`), `a_leads`/`b_leads` (comparisons A/B crossed FIRST), `co_crossings` (same-step ties — EQUAL to v34's `joint_moves` for the pair), `comparisons` (`a_leads + b_leads + co_crossings`), `leader` (the front that crossed first more often, `null` on an exact split), `affinity` (`max(a_leads, b_leads) / comparisons`), and its `class`. `computeCoherencePrecedence(rounds)` derives each front's CROSSING-STEP LIST (the SAME v24 crossing event — a fall OR a recovery, direction-blind; silence-skipping — an UNSTATED round is SKIPPED, so a crossing across a silent gap lands on the round that REVEALS the new standing, §3), matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/concurrency/affinity functions use), enumerates unordered pairs in that order, ORDERS A's crossing steps against B's into `a_leads`/`b_leads`/`co_crossings`, computes the comparisons/affinity/strict-majority class, and selects the deal's MOST-LEADING affinity by INTEGER CROSS-MULTIPLICATION (`leads × comparisons` of the two pairs — never a float compare, so the ranking is platform-exact; earliest pair on a tie; restricted to pairs with a non-null leader — a direction to name), returning deal-level `pairs[]`, `class_counts`, `total_ordered_comparisons` (every pair's `a_leads + b_leads` summed) / `total_co_crossings` (every pair's same-step ties summed — EQUAL to `Σ_t C(crossing_t, 2)` = v25's per-step crossings choose-two'd = v34's `total_opposed_moves + total_aligned_moves`, accumulated over EVERY pair so the invariant holds across the whole matrix), `max_affinity`/`most_leading_pair`/`leading_front` (`null` when no pair has a leader), `leads` (any pair a strict-majority lead-lag — the gate verdict), and a namespaced `precedence_hash` (SHA-256 over the canonical per-pair set — the INTEGER `a_leads`/`b_leads`/`co_crossings`, the `leader`, and class; the derived float `affinity` and derived integer `comparisons` are OMITTED, so the fingerprint is integer-exact over the inputs — apart from every coherence_hash/…/affinity_hash/recovery_affinity_hash/opposition_hash). `exposureLeads(precedence)` = `precedence.leads` — the DIRECTIONAL pairwise gate predicate; UNLIKE v32's `exposureCoupled`/v33's `exposureRecoveryCoupled`/v34's `exposureOpposed` (all SAME-STEP couplings) and v25's `exposureSynchronized` (a per-step count) this fires on a stable LEAD-LAG: one front reliably crossing the floor BEFORE its partner across the deal. **The distinctness:** a pair `coupled` on falls (v32), recoveries (v33), or opposed (v34) can lead, follow, or interleave on precedence, and a pair with NO same-step edge at all (every crossing on a different step) can be the STRONGEST leader — the directional and same-step relations are INDEPENDENT reads of the same `floors[]` matrix; their link is EXACT (each pair's `co_crossings` = v34's `joint_moves`; the totals = v25's synchrony). **§3 honesty:** silence is neither a fall nor a recovery, and a no-ordered-comparison pair is omitted (no lead-lag edge). **Lineage:** the relation aggregates the same crossing events v24 counts and v25 buckets per step — `total_co_crossings` agrees with v25/v34 by construction. `buildCoherencePrecedenceJson` (`schema: vaulytica.posture-precedence.v1`) + `renderCoherencePrecedenceSummary` (the most-leading verdict naming the leader and its follower, the class tally, then one line per lead-lag pair, `leading` first). **ZERO new posture math beyond the per-pair ordered comparison AND zero changes to any existing source file**: v35 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v34 it needed nothing newly exported. **The command, `tools/cli/coherence-precedence.ts`** (sibling to coherence-opposition.ts): `computeCoherencePrecedenceArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherencePrecedence` rendered markdown (default) or JSON, returning `{ok:true, output, leads, ladderNote}`. `runCoherencePrecedence(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-leading-front` exits 2 ONLY when a pair has a front that crossed first for a strict majority of the comparisons. A SEPARATE command, not a `coherence-opposition` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-precedence"`) + USAGE + header doc + unknown-command error list. **VERIFIED LIVE end-to-end** (operates purely on JSON artifacts — built five ladder-pinned artifacts via a tsx generator, drove the real CLI): a deal where Cap crosses the floor at transitions {0,2} and Term at {1,3} prints `most-leading pairing: Cap leads Term — crossed first 75% of the comparisons`, `⚠ Cap + Term: leading — Cap first 3 of 4 (75%); 0 same-step ties`, `precedence_hash: 1acd2336…` and exits **2** under the gate — proving the lead-lag axis the same-step reads (v32/v33/v34), the per-step count (v25/v29), and the per-front count (v24) structurally cannot pose. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/coherence-latency/coherence-concurrency/coherence-relapse/coherence-tenure/coherence-affinity/coherence-recovery-affinity/coherence-opposition/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +22 tests (coherence-precedence.test.ts ×13: the clear leader [Cap first 3 of 4], the interleaved pair [plurality but no strict majority], the co_crossings/joint_moves and total/Σ-C(crossing,2) join invariants [cross-checked against v34 and v25], most-leading pick by exact integer ratio [ratio beats label order], tie-break by earliest pair, no-pair-has-a-leader [max_affinity null], silence-across-a-gap-on-the-revealing-step [§3], single-comparison → 100%/leading, omit-no-ordered-comparison [always-together pair], determinism, <2-rounds throws, render+JSON, none-leading render; tools/cli/coherence-precedence.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on a leading pair, gate clears when fronts always cross together, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,419 passing + 2 skips** (was 3,397), 229 test files (was 227). Version 9.31.0 → 9.32.0. New [`docs/spec-v35.md`](docs/spec-v35.md); BUILD_PROGRESS v35 §; CHANGELOG [9.32.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the precedence workflow + the SEVENTEEN-axis summary callout + v35 spec-table row + CLI cheat-sheet + commands table entry (twenty-four commands) + dispatcher command lists current v1–v35. The one Step (215) is shipped; a `coherence-opposition --ordered` flag, a configurable `--min-leads K` gate, a `--min-comparisons N` floor, a direction-resolved precedence (who FALLS first vs who RECOVERS first), a mean-latency precedence (how long the leader leads), a transitive lead-lag chain (A→B→C), a standalone precedence artifact, a full O(F²) dense matrix, an order-inferring walker, and a browser N-round precedence are principled deferrals. Feature frontier: every numbered spec v1–v35 now complete end-to-end; the posture matrix now has SEVENTEEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32, the first PAIRWISE read — which two fronts FALL together), RECOVERY-AFFINITY (v33, the MIRROR — which two RECOVER together), OPPOSITION (v34, the OFF-DIAGONAL — which two move in OPPOSITE directions at once), and PRECEDENCE (v35, the first DIRECTIONAL read — which front crosses the floor FIRST within a pair, the lead-lag the three same-step reads discard). The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a direction-resolved precedence [who falls first vs who recovers first]; a below/above cadence RATIO; a per-front onset/settling; a per-front×per-step 2-D matrix export; a mean recovery latency or relapse interval; a transitive lead-lag chain over the pairs; depth-weighting still NOT viable — the floor is binary).
- **Step 216 — The `coherence-concession` subcommand (shipped 2026-06-16, 9.33.0):** The posture family's FIRST DIRECTION-RESOLVED precedence. v35 (`coherence-precedence`) orders v24's CROSSINGS — a fall OR a recovery — so it is DIRECTION-BLIND: a pair can lead there because one front reliably RECOVERS first, not because it CONCEDES first. The two questions diverge — "which front MOVES first?" (v35) and "which front gives GROUND first?" (the deal lead's real question) — and v35 conflates them. v36 restricts the ordering to FALLS only, isolating the concession order: across the deal, when fronts A and B both FALL below the floor, does one consistently concede FIRST? A front that falls below the floor in rounds 1 and 3 while its partner falls in rounds 2 and 4, deal after deal, is the FIRST-CONCEDER — the early-warning edge of a coming concession: the counterparty gives ground on it before its partner. (Fulfills v35 Part XVI's deferred direction-resolved precedence, on the FALL side first — the gate-worthy warning; the recovery-side mirror is deferred to a future v37.) **The report (pure), `src/report/coherence-concession.ts`** (sibling to coherence-precedence.ts): `ConcessionClass` = `leading` (one front FELL below the floor BEFORE the other for a STRICT MAJORITY of the comparisons, `max(a_concedes_first, b_concedes_first) × 2 > comparisons` — a consistent first-conceder, the gate-worthy class) / `interleaved` (mixed order with no strict-majority first-conceder, INCLUDING an exact split or a pair whose comparisons are dominated by same-step ties); only pairs with ≥1 ORDERED comparison (`a_concedes_first + b_concedes_first ≥ 1`) are classified — a pair the two never both FELL in a DIFFERENT step (one never fell, or they always fell together) has no concession-order edge and is OMITTED. `CoherenceConcessionPair` carries `dimension_a`/`dimension_b` (pinned, `a < b`), `a_concedes_first`/`b_concedes_first` (comparisons A/B fell FIRST), `co_falls` (same-step ties — EQUAL to v32's `co_falls` for the pair), `comparisons` (`a_concedes_first + b_concedes_first + co_falls`), `first_conceder` (the front that fell first more often, `null` on an exact split), `affinity` (`max(a_concedes_first, b_concedes_first) / comparisons`), and its `class`. `computeCoherenceConcession(rounds)` derives each front's FALL-STEP LIST (the SAME v32 fall event — an at-or-above → below flip; silence-skipping — an UNSTATED round is SKIPPED, so a fall across a silent gap lands on the round that REVEALS the new standing, §3), matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/affinity/precedence functions use), enumerates unordered pairs in that order, ORDERS A's fall steps against B's into `a_concedes_first`/`b_concedes_first`/`co_falls`, computes the comparisons/affinity/strict-majority class, and selects the deal's MOST-CONCEDING affinity by INTEGER CROSS-MULTIPLICATION (`leads × comparisons` of the two pairs — never a float compare, so the ranking is platform-exact; earliest pair on a tie; restricted to pairs with a non-null first-conceder), returning deal-level `pairs[]`, `class_counts`, `total_ordered_comparisons` (every pair's `a_concedes_first + b_concedes_first` summed) / `total_co_falls` (every pair's same-step fall ties summed — EQUAL to `Σ_t C(falling_t, 2)` = v29's per-step fall count choose-two'd = v32's `total_co_falls`, accumulated over EVERY pair so the invariant holds across the whole matrix), `max_affinity`/`most_conceding_pair`/`first_conceding_front` (`null` when no pair has a first-conceder), `concedes` (any pair a strict-majority concession order — the gate verdict), and a namespaced `concession_hash` (SHA-256 over the canonical per-pair set — the INTEGER `a_concedes_first`/`b_concedes_first`/`co_falls`, the `first_conceder`, and class; the derived float `affinity` and derived integer `comparisons` are OMITTED, so the fingerprint is integer-exact over the inputs — apart from every coherence_hash/…/opposition_hash/precedence_hash). `exposureConcedes(concession)` = `concession.concedes` — the DIRECTION-RESOLVED pairwise gate predicate; UNLIKE v35's `exposureLeads` (a DIRECTION-BLIND ordering of any crossing) this fires on a stable CONCESSION order: one front reliably FALLING below the floor BEFORE its partner across the deal. **The distinctness:** a pair `leading` on v35 (direction-blind) can be `interleaved` on v36 (it led only because one front RECOVERS first); a pair `coupled` on co-falls (v32, SAME-STEP) can lead, follow, or interleave on concession order (v36, ACROSS-STEP); the direction-resolved, direction-blind, and same-step relations are INDEPENDENT reads of the same `floors[]` matrix; the link to v32 is EXACT (each pair's `co_falls` = v32's `co_falls`; the totals = v29's per-step falls choose-two'd). **§3 honesty:** silence is neither a fall nor a recovery, and a no-ordered-comparison pair is omitted (no concession-order edge). `buildCoherenceConcessionJson` (`schema: vaulytica.posture-concession.v1`) + `renderCoherenceConcessionSummary` (the most-conceding verdict naming the first-conceder and its follower, the class tally, then one line per concession-order pair, `leading` first). **ZERO new posture math beyond the per-pair ordered comparison AND zero changes to any existing source file**: v36 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v35 it needed nothing newly exported. **The command, `tools/cli/coherence-concession.ts`** (sibling to coherence-precedence.ts): `computeCoherenceConcessionArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceConcession` rendered markdown (default) or JSON, returning `{ok:true, output, concedes, ladderNote}`. `runCoherenceConcession(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-leading-concession` exits 2 ONLY when a pair has a front that fell first for a strict majority of the comparisons. A SEPARATE command, not a `coherence-precedence` flag — each command keeps one gate, one hash. Wired into the run.ts dispatcher (`case "coherence-concession"`) + USAGE + header doc + unknown-command error list. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output (analyze/diff/compare/compare-coherence/coherence-trend/coherence-shift-trend/coherence-arc/coherence-exposure/coherence-persistence/coherence-breadth/coherence-recurrence/coherence-volatility/coherence-synchrony/coherence-settling/coherence-onset/coherence-latency/coherence-concurrency/coherence-relapse/coherence-tenure/coherence-affinity/coherence-recovery-affinity/coherence-opposition/coherence-precedence/verify) and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +22 tests (coherence-concession.test.ts ×13: the clear first-conceder [Cap first 3 of 4], the interleaved pair, the v35-distinctness case [leads on all crossings but interleaves on falls], the co_falls/v32-co_falls and total/Σ-C(falling,2) join invariants [cross-checked against v32 and v29], most-conceding pick by exact integer ratio [ratio beats label order], tie-break by earliest pair, no-pair-has-a-conceder [max_affinity null], silence-across-a-gap-on-the-revealing-step [§3], single-comparison → 100%/leading, omit-no-ordered-comparison [always-together pair], determinism, <2-rounds throws, render+JSON, none-conceding render; tools/cli/coherence-concession.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on a leading-concession pair, gate clears when fronts always fall together, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,441 passing + 2 skips** (was 3,419), 231 test files (was 229). Version 9.32.0 → 9.33.0. New [`docs/spec-v36.md`](docs/spec-v36.md); BUILD_PROGRESS v36 §; CHANGELOG [9.33.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the concession workflow + the EIGHTEEN-axis summary callout + v36 spec-table row + CLI cheat-sheet + commands table entry (twenty-five commands) + dispatcher command lists current v1–v36. The one Step (216) is shipped; a `coherence-precedence --falls-only` flag, a recovery-order mirror command (who RECOVERS first — the deferred v37), a configurable `--min-leads K` gate, a `--min-comparisons N` floor, a mean-lead-time concession, a transitive concession chain (A→B→C), a standalone concession artifact, a full O(F²) dense matrix, an order-inferring walker, and a browser N-round concession order are principled deferrals. Feature frontier: every numbered spec v1–v36 now complete end-to-end; the posture matrix now has EIGHTEEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32), RECOVERY-AFFINITY (v33), OPPOSITION (v34), PRECEDENCE (v35, the first DIRECTIONAL read — which front CROSSES first in any direction), and CONCESSION (v36, the first DIRECTION-RESOLVED precedence — which front FALLS first, the concession order v35's direction-blind read conflates with recovery order). The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a recovery-order mirror [who recovers first — v37]; a below/above cadence RATIO; a per-front onset/settling; a per-front×per-step 2-D matrix export; a mean recovery latency or relapse interval; a transitive lead-lag chain over the pairs; depth-weighting still NOT viable — the floor is binary).
- **Step 217 — The `coherence-recovery-order` subcommand (shipped 2026-06-16, 9.34.0):** The posture family's SECOND DIRECTION-RESOLVED precedence — the RECOVERY MIRROR of v36's concession order, the way v33 mirrors v32 and v30 mirrors v28. v36 (`coherence-concession`) orders FALLS only — which front CONCEDES (falls below the floor) first, the gate-worthy warning, shipped first. The floor is crossed two ways and the RECOVERY half was unread: across the deal, when fronts A and B both RECOVER (climb back at-or-above the floor), does one consistently recover FIRST — and so the other LAST? A front restored in rounds 2 and 4 while its partner is restored in rounds 3 and 5, deal after deal, leads the recovery; its partner is the LAGGARD — the front the counterparty leaves exposed below the floor LONGEST. Recovering FIRST is GOOD news (a front repaired quickly), so it is NOT the warning; recovering LAST is the warning, and a consistent last-recoverer is exactly the partner of a consistent first-recoverer — that asymmetry (opposite to v36's) is what makes the gate meaningful. (Fulfills v36 Part XVI's deferred recovery-order mirror.) **The report (pure), `src/report/coherence-recovery-order.ts`** (sibling to coherence-concession.ts): `RecoveryOrderClass` = `leading` (one front RECOVERED above the floor BEFORE the other for a STRICT MAJORITY of the comparisons, `max(a_recovers_first, b_recovers_first) × 2 > comparisons` — a consistent first-recoverer, and so a consistent LAGGARD, the gate-worthy class) / `interleaved` (mixed order with no strict-majority first-recoverer, INCLUDING an exact split or a pair dominated by same-step ties); only pairs with ≥1 ORDERED comparison (`a_recovers_first + b_recovers_first ≥ 1`) are classified — a pair the two never both RECOVERED in a DIFFERENT step is OMITTED. `CoherenceRecoveryOrderPair` carries `dimension_a`/`dimension_b` (pinned, `a < b`), `a_recovers_first`/`b_recovers_first` (comparisons A/B recovered FIRST), `co_recoveries` (same-step ties — EQUAL to v33's `co_recoveries` for the pair), `comparisons` (`a_recovers_first + b_recovers_first + co_recoveries`), `first_recoverer` (the front that recovered first more often, `null` on an exact split), `last_recoverer` (the LAGGARD — the other front, left exposed longest, `null` on a split), `affinity` (`max(a_recovers_first, b_recovers_first) / comparisons`), and its `class`. `computeCoherenceRecoveryOrder(rounds)` derives each front's RECOVERY-STEP LIST (the SAME v33 recovery event — a below → at-or-above flip; silence-skipping — an UNSTATED round is SKIPPED, so a recovery across a silent gap lands on the round that REVEALS the new standing, §3), matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/affinity/concession functions use), enumerates unordered pairs in that order, ORDERS A's recovery steps against B's into `a_recovers_first`/`b_recovers_first`/`co_recoveries`, computes the comparisons/affinity/strict-majority class, and selects the deal's CLEAREST recovery order by INTEGER CROSS-MULTIPLICATION (`leads × comparisons` of the two pairs — never a float compare; earliest pair on a tie; restricted to pairs with a non-null first-recoverer), returning deal-level `pairs[]`, `class_counts`, `total_ordered_comparisons` / `total_co_recoveries` (every pair's same-step recovery ties summed — EQUAL to `Σ_t C(recovering_t, 2)` = v29's per-step recovery count choose-two'd = v33's `total_co_recoveries`, accumulated over EVERY pair so the invariant holds across the whole matrix), `max_affinity`/`most_ordered_pair`/`first_recovering_front`/`last_recovering_front` (`null` when no pair has a first-recoverer), `lags` (any pair a strict-majority recovery order — so a consistent laggard — the gate verdict), and a namespaced `recovery_order_hash` (SHA-256 over the canonical per-pair set — the INTEGER `a_recovers_first`/`b_recovers_first`/`co_recoveries`, the `first_recoverer`, and class; the derived `last_recoverer`, float `affinity`, and integer `comparisons` are OMITTED, so the fingerprint is integer-exact — apart from every coherence_hash/…/precedence_hash/concession_hash). `exposureLags(recoveryOrder)` = `recoveryOrder.lags` — the recovery-side direction-resolved gate predicate; UNLIKE v36's `exposureConcedes` (the FALL order) this fires on a stable RECOVERY order: one front reliably RECOVERING above the floor AFTER its partner across the deal — the laggard left exposed longest. **The distinctness:** a pair `leading` on v36 (falls) can be `interleaved` on v37 (recoveries) and vice versa; the fall-order and recovery-order are INDEPENDENT reads of the same `floors[]` matrix; the link to v33 is EXACT (each pair's `co_recoveries` = v33's `co_recoveries`; the totals = v29's per-step recoveries choose-two'd). JOINED with v36 it names the deal's PERSISTENT WEAK FRONT — the one that CONCEDES FIRST and RECOVERS LAST. **§3 honesty:** silence is neither a fall nor a recovery, and a no-ordered-comparison pair is omitted. `buildCoherenceRecoveryOrderJson` (`schema: vaulytica.posture-recovery-order.v1`) + `renderCoherenceRecoveryOrderSummary` (the clearest recovery order naming the first-recoverer and the laggard it leaves exposed, the class tally, then one line per recovery-order pair, `leading` first). **ZERO new posture math beyond the per-pair ordered comparison AND zero changes to any existing source file**: v37 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v36 it needed nothing newly exported. **The command, `tools/cli/coherence-recovery-order.ts`** (sibling to coherence-concession.ts): `computeCoherenceRecoveryOrderArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceRecoveryOrder` rendered markdown (default) or JSON, returning `{ok:true, output, lags, ladderNote}`. `runCoherenceRecoveryOrder(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-lagging-recovery` exits 2 ONLY when a pair has a front that recovered first for a strict majority of the comparisons (and so a consistent laggard). A SEPARATE command, not a `coherence-concession` flag. Wired into the run.ts dispatcher (`case "coherence-recovery-order"`) + USAGE + header doc + unknown-command error list. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +22 tests (coherence-recovery-order.test.ts ×13: the clear first-recoverer/laggard [Cap first 3 of 4, Term lags], the interleaved pair, the v36-distinctness case [leads on falls but interleaves on recoveries], the co_recoveries/v33-co_recoveries and total/Σ-C(recovering,2) join invariants [cross-checked against v33 and v29], clearest-order pick by exact integer ratio [ratio beats label order], tie-break by earliest pair, no-pair-has-an-order [max_affinity null], silence-across-a-gap-on-the-revealing-step [§3], single-comparison → 100%/leading, omit-no-ordered-comparison [always-together pair], determinism, <2-rounds throws, render+JSON, none-ordered render; tools/cli/coherence-recovery-order.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on a lagging-recovery pair, gate clears when fronts always recover together, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,463 passing + 2 skips** (was 3,441), 233 test files (was 231). Version 9.33.0 → 9.34.0. New [`docs/spec-v37.md`](docs/spec-v37.md); BUILD_PROGRESS v37 §; CHANGELOG [9.34.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the recovery-order workflow + the NINETEEN-axis summary callout + v37 spec-table row + CLI cheat-sheet + commands table entry (twenty-six commands) + dispatcher command lists current v1–v37. The one Step (217) is shipped; a `coherence-concession --recoveries-only` flag, a configurable `--min-leads K` gate, a `--min-comparisons N` floor, a mean-lead-time recovery, a transitive recovery chain (A→B→C), a combined concede-first-and-recover-last verdict, a standalone recovery-order artifact, a full O(F²) dense matrix, an order-inferring walker, and a browser N-round recovery order are principled deferrals. Feature frontier: every numbered spec v1–v37 now complete end-to-end; the posture matrix now has NINETEEN axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32), RECOVERY-AFFINITY (v33), OPPOSITION (v34), PRECEDENCE (v35), CONCESSION (v36, who FALLS first), and RECOVERY-ORDER (v37, who RECOVERS first — and so who recovers LAST, the laggard). The pairwise PRECEDENCE family now closes the 2×2 of {fall, recovery} × {same-step, ordered} plus the direction-blind ordering; the natural next axes are MAGNITUDE (mean lead-time / how long the laggard stays exposed) or PER-FRONT (onset/settling latency), not another pairwise direction. The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a below/above cadence RATIO; a per-front onset/settling; a per-front×per-step 2-D matrix export; a MEAN recovery latency or lead-time; a TRANSITIVE lead-lag/recovery chain over the pairs; a combined concede-first-and-recover-last weak-front join; depth-weighting still NOT viable — the floor is binary).
- **Step 218 — The `coherence-weak-front` subcommand (shipped 2026-06-16, 9.35.0):** The posture family's FIRST PER-FRONT SYNTHESIS — the JOIN of v36's concession order and v37's recovery order. Every axis v32–v37 is PAIRWISE (an unordered pair is its primary object); v38's primary object is a FRONT. v36 (`coherence-concession`) names, per pair, the front that CONCEDES (falls below the floor) FIRST; v37 (`coherence-recovery-order`) names the front that RECOVERS (climbs back above the floor) LAST — the laggard. Each is a DIRECTIONAL HALF-TRUTH, and each spec (v36 §XVIII, v37 §1/§XVIII) named the same prize the other could not name alone: a front that CONCEDES FIRST and RECOVERS LAST is the deal's PERSISTENT WEAK POINT — exposed coming and going — while a front that concedes first but recovers FIRST is merely VOLATILE. Both DEFERRED the join (v36 §XVI, v37 §XVI) as "a downstream read over two independent reports, not a new command" — right while the recovery half was unbuilt; with v37 shipped, v38 elevates it, exactly as v37 Open Question #1 steers (next axis PER-FRONT, not another pairwise direction). **The report (pure), `src/report/coherence-weak-front.ts`** (sibling to coherence-recovery-order.ts): `WeakFrontClass` = `persistent-weak` (a strict-majority FIRST-CONCEDER against ≥1 partner AND a strict-majority LAST-RECOVERER against ≥1 partner — exposed on BOTH directional axes, the gate-worthy class) / `conceding` (concedes first against ≥1 partner but never a consistent last-recoverer — gives ground early but restored on time, the VOLATILE front) / `lagging` (recovers last against ≥1 partner but never concedes first); a front weak on NEITHER axis carries no signal and is OMITTED. `CoherenceWeakFrontEntry` carries `dimension`, `concedes_first_against` (partners it is the v36 `first_conceder` against, sorted), `recovers_last_against` (partners it is the v37 `last_recoverer` against, sorted), `confirmed_against` (the SAME-PARTNER intersection — the sharpest evidence, sorted), and its `class`. `computeCoherenceWeakFront(rounds)` computes v36's concession + v37's recovery order over the SAME rounds (both pure, both `localeCompare`-pinned, both decided by integer cross-multiplication — called UNCHANGED), reads ONLY their `leading` pairs (a leading pair always has a non-null first_conceder / last_recoverer), accumulates per-front partner lists, enumerates fronts in pinned order, classifies each, and picks the deal's `most_exposed_front` by INTEGER leading-edge count (`concedes_first_against.length + recovers_last_against.length`, earliest label on a tie — never a float compare), returning deal-level `fronts[]`, `class_counts`, `weak_fronts` (every persistent-weak front, the headline), `most_exposed_front`, `has_persistent_weak_front` (the gate verdict), and a namespaced `weak_front_hash` (SHA-256 over the canonical per-front set — the `dimension`, sorted `concedes_first_against`, sorted `recovers_last_against`, and class; the derived `confirmed_against`, `most_exposed_front`, and `has_persistent_weak_front` OMITTED, so integer-exact over the inputs — apart from every coherence_hash/…/concession_hash/recovery_order_hash). `exposurePersistentlyWeak(weakFront)` = `weakFront.has_persistent_weak_front` — the family's first PER-FRONT CONJUNCTION; STRICTLY STRONGER than v36's `concedes` or v37's `lags` (each fires on a directional ordering existing AT ALL) — this requires a SINGLE front to be the weak side of BOTH. **The distinctness:** v36/v37 are pairwise+directional, v38 per-front+bidirectional; a `first_conceder` in v36 is only `conceding` in v38 unless ALSO a `last_recoverer` in v37, and vice versa; a deal can TRIP v36 AND v37 (different fronts lead each) yet CLEAR v38 (no one front weak on both — the test suite includes that fixture). The gate uses the LOOSER cross-partner conjunction (weak on falls vs SOMEONE, weak on recoveries vs SOMEONE — more sensitive); the strict same-partner `confirmed_against` is in the JSON for a dashboard that wants to pin it. **§3 honesty:** the join inherits v36/v37's silence-skipping exactly; a front weak on neither axis is omitted; a front weak on only one axis is reported (conceding/lagging) but NEVER gated. `buildCoherenceWeakFrontJson` (`schema: vaulytica.posture-weak-front.v1`) + `renderCoherenceWeakFrontSummary` (the most-exposed front naming its concession/recovery/confirmed partners, the class tally, then one line per front, persistent-weak first). **ZERO new fall/recovery/ordering math AND zero changes to any existing source file**: v38 imports only the already-PUBLIC `PostureCoherence` type, `computeCoherenceConcession` (v36), `computeCoherenceRecoveryOrder` (v37), and the shared hashing helpers — like v19–v37 it needed nothing newly exported. **The command, `tools/cli/coherence-weak-front.ts`** (sibling to coherence-recovery-order.ts): `computeCoherenceWeakFrontArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceWeakFront` rendered markdown (default) or JSON, returning `{ok:true, output, weak, ladderNote}`. `runCoherenceWeakFront(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-persistent-weak-front` exits 2 ONLY when a front is BOTH a strict-majority first-conceder and a strict-majority last-recoverer. A SEPARATE command, not a flag on v36/v37. Wired into the run.ts dispatcher (`case "coherence-weak-front"`) + USAGE + header doc + unknown-command error list. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +19 tests (coherence-weak-front.test.ts ×10: the clear persistent-weak front [Term concedes first AND recovers last vs Cap], the conceding-only/volatile vs lagging-only split [neither gated], the cross-book weak front [edges span multiple partners], the trip-both-halves-on-different-fronts-yet-clear case, most-exposed pick by integer edge count [tie → earliest label], omit-no-signal-front, determinism, <2-rounds throws [delegated], render+JSON, none-weak render; tools/cli/coherence-weak-front.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on a persistent-weak front, gate clears for lockstep fronts, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,482 passing + 2 skips** (was 3,463), 235 test files (was 233). Version 9.34.0 → 9.35.0. New [`docs/spec-v38.md`](docs/spec-v38.md); BUILD_PROGRESS v38 §; CHANGELOG [9.35.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the weak-front workflow + the TWENTY-axis summary callout + v38 spec-table row + CLI cheat-sheet + commands table entry (twenty-seven commands) + dispatcher command lists current v1–v38. The one Step (218) is shipped; a `--same-partner` gate (on `confirmed_against`), a `--min-edges K` knob, a weighted exposure score, a per-front transitive rank, a standalone weak-front artifact, an order-inferring walker, and a browser N-round weak-front synthesis are principled deferrals. Feature frontier: every numbered spec v1–v38 now complete end-to-end; the posture matrix now has TWENTY axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32), RECOVERY-AFFINITY (v33), OPPOSITION (v34), PRECEDENCE (v35), CONCESSION (v36), RECOVERY-ORDER (v37), and WEAK-FRONT (v38, the first PER-FRONT synthesis — concedes first AND recovers last). The pairwise PRECEDENCE family and its first JOIN now close the directional family; the natural next axes are MAGNITUDE (mean lead-time / how long the weak front stays exposed) or a fresh PER-FRONT CADENCE (below/above oscillation rate), not another pairwise direction or another conjunction over the same edges. The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a below/above cadence RATIO; a MEAN recovery latency or lead-time MAGNITUDE; a per-front×per-step 2-D matrix export; a TRANSITIVE lead-lag/recovery chain over the pairs; depth-weighting still NOT viable — the floor is binary).

## spec-v39 — Document-Free Exposure Cadence (Per-Front Floor-Crossing Churn Rate)

- **Step 219 — The `coherence-cadence` subcommand (shipped 2026-06-24, 9.36.0):** The CHURN MIRROR of v31's DWELL — the per-front read that sits between v31 (occupancy SHARE: how LONG a front sits below the floor) and v24 (crossing COUNT: how MANY times its standing crosses the floor). Neither reads the RATE: across the transitions a front actually had, how OFTEN did it flip across the floor? Two fronts with identical below-floor share (both below half their stated rounds) can be opposites — one dips once and holds (a steady concession), the other alternates every round (a flickering, never-settled front); v31 calls them the same, and v24, gating on the raw count, calls a front that crossed twice in twenty transitions just as `volatile` as one that crossed twice in two. v38 Open Question #1 named this axis explicitly ("a fresh PER-FRONT CADENCE read — below/above oscillation rate — not another pairwise direction or conjunction"). **The report (pure), `src/report/coherence-cadence.ts`** (sibling to coherence-tenure.ts): `CadenceClass` = `oscillating` (crossed the floor for a STRICT MAJORITY of its transitions, `crossings × 2 > transitions` — flips sides more often than it holds one, the gate-worthy class) / `settled` (≥1 transition but NOT a strict majority of crossings — including zero crossings and an exact split) / `static` (stated in EXACTLY ONE round — no transition to measure) / `unstated` (never stated). `CoherenceCadenceFront` carries `dimension`, `floors` (per-round binding floor, `null` = unstated), `stated_rounds`, `transitions` (`max(0, stated_rounds − 1)`), `crossings` (both directions, the SAME v24 crossings), `cadence` (`crossings / transitions`, `null` when no transition), and its `class`. `computeCoherenceCadence(rounds)` scans each front's binding floors in ONE silence-skipping pass (the v24 scan — an UNSTATED round is SKIPPED, so a crossing across a silent gap lands on the round that REVEALS the new standing and the denominator counts only transitions between consecutive STATED rounds, §3), matches fronts by dimension (pinned by the SAME `localeCompare` the trajectory/exposure/tenure/volatility functions use), classifies each, and picks the deal's `busiest_dimension` by INTEGER CROSS-MULTIPLICATION (`crossings × transitions` of the two fronts — never a float compare; earliest label on a tie; among fronts that ever crossed), returning deal-level `fronts[]`, `class_counts`, `total_crossings` (= v24's `crossings` summed across fronts BY CONSTRUCTION), `total_transitions`, `max_cadence`/`busiest_dimension` (`null` when no front ever crossed), `oscillating` (any front a strict-majority floor-crosser — the gate verdict), and a namespaced `cadence_hash` (SHA-256 over the canonical per-front set — `dimension`, `floors`, `stated_rounds`, `transitions`, `crossings`, and class; the derived float `cadence`, `busiest_dimension`, `max_cadence`, and `oscillating` OMITTED, so integer-exact over the inputs — apart from every coherence_hash/…/weak_front_hash). `exposureOscillates(cadence)` = `cadence.oscillating` — the RATE gate predicate. **The distinctness:** DISTINCT from v24 (the raw count, blind to opportunity — a front crossing twice in 20 transitions is `volatile` to v24 but `settled` here at a 10% rate; one crossing once in its single transition is `monotone` to v24 but `oscillating` here at 100%) AND from v31 (the dwell, blind to order — a front below floor rounds 1–3 of 6 that holds is a `majority` tenure but a `settled` cadence; a front flipping every round is a `minority` tenure but `oscillating`). The test suite includes a `volatile`-but-`settled` fixture (the v24 divergence, cross-checked: `total_crossings` = v24's summed) and a same-dwell/opposite-churn fixture (the v31 divergence, cross-checked against v31's `below_rounds`/`tenure`). **§3 honesty:** silence is neither a crossing nor a transition; a front stated at most once is `static`, never ranked. `buildCoherenceCadenceJson` (`schema: vaulytica.posture-cadence.v1`) + `renderCoherenceCadenceSummary` (the busiest churn naming its flip rate, the class tally, then one line per front that crossed, `oscillating` first). **ZERO new posture math beyond the per-front crossing count AND zero changes to any existing source file**: v39 imports only the already-PUBLIC `PostureCoherence`/`NegotiationTier` types and the shared hashing helpers — like v19–v38 it needed nothing newly exported. **The command, `tools/cli/coherence-cadence.ts`** (sibling to coherence-tenure.ts): `computeCoherenceCadenceArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceCadence` rendered markdown (default) or JSON, returning `{ok:true, output, oscillating, ladderNote}`. `runCoherenceCadence(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-oscillating-front` exits 2 ONLY when a front crossed for a strict majority of its transitions. A SEPARATE command, not a flag on v24/v31. Wired into the run.ts dispatcher (`case "coherence-cadence"`) + USAGE + header doc + unknown-command error list. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +22 tests (coherence-cadence.test.ts ×13: the clear oscillating vs settled front, a crosses-but-not-majority front clears the gate, the v24-distinctness case [`volatile` yet `settled`, total_crossings invariant], the v31-distinctness case [same dwell, opposite churn], silence-across-a-gap-on-the-revealing-step [§3], the `static` single-appearance front, the never-crossed/`unstated` front, busiest pick by exact integer ratio [ratio beats label order], tie-break by earliest label, determinism, <2-rounds throws, render+JSON, none-crossed render; tools/cli/coherence-cadence.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on an oscillating front, gate clears when every front holds more than it flips, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,504 passing + 2 skips** (was 3,482), 237 test files (was 235). Version 9.35.0 → 9.36.0. New [`docs/spec-v39.md`](docs/spec-v39.md); BUILD_PROGRESS v39 §; CHANGELOG [9.36.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the cadence workflow + the TWENTY-ONE-axis summary callout + v39 spec-table row + commands prose. The one Step (219) is shipped; a magnitude read (mean below-floor duration / mean lead-time), a direction-resolved cadence (fall rate vs recovery rate — near-degenerate since falls/recoveries alternate), a `--min-crossings K` knob, a standalone cadence artifact, an order-inferring walker, and a browser N-round cadence synthesis are principled deferrals. Feature frontier: every numbered spec v1–v39 now complete end-to-end; the posture matrix now has TWENTY-ONE axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32), RECOVERY-AFFINITY (v33), OPPOSITION (v34), PRECEDENCE (v35), CONCESSION (v36), RECOVERY-ORDER (v37), WEAK-FRONT (v38), and CADENCE (v39, the churn rate). The per-front below-floor reads the BINARY floor admits are now broadly covered; the remaining genuinely-new directions are MAGNITUDE (mean below-floor duration / mean lead-time — a FLOAT read, the deferred axis) or a 2-D read (per-front × per-step matrix export), not another normalization of the same crossings. The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a MEAN below-floor duration / lead-time MAGNITUDE; a per-front×per-step 2-D matrix export; a TRANSITIVE lead-lag/recovery chain over the pairs; depth-weighting still NOT viable — the floor is binary).

## spec-v40 — Document-Free Exposure Duration (Per-Front Mean Recovered-Exposure Length)

- **Step 220 — The `coherence-duration` subcommand (shipped 2026-06-24, 9.37.0):** The CENTRAL TENDENCY of v28's recovery episodes — the MAGNITUDE axis v37/v38/v39 each deferred BY NAME ("a magnitude read — mean below-floor duration / mean lead-time, in rounds — a FLOAT read"). v28 (`coherence-latency`) pairs each FALL below the acceptable floor with the RECOVERY that closes it, then reduces the episode lengths two ways and BOTH ARE EXTREMES: `max_latency` (the single SLOWEST recovery across the deal) and `open_count` (whether any fall went UNRECOVERED — the gate). Neither reads the MEAN. A front that recovers in one round three times and once takes five owns the deal's slowest single recovery, yet recovers at once 75% of the time; a front that takes four rounds EVERY time has a SMALLER worst spell, yet it is the one that CHRONICALLY lingers — the front a counterparty keeps parked below the floor. The `max_latency` extreme calls the first worse; the MEAN calls the second worse, and the mean is the read no existing command supplies. v39 Open Question #1 steered here ("the remaining genuinely-new reads are MAGNITUDE — mean below-floor duration — or a 2-D read, not another reduction of the same below-floor episodes"). **The report (pure), `src/report/coherence-duration.ts`** (sibling to coherence-cadence.ts): `DurationClass` = `lingering` (≥1 RECOVERED episode AND `total_rounds ≥ 2 × closed_episodes` — a mean of at least two rounds, the gate-worthy class) / `brief` (≥1 recovered episode but a mean BELOW two rounds, including a mean of exactly one) / `open` (fell in-sequence but NO episode recovered — an unbounded duration, v28's gate owns it) / `steady` (stated, never fell) / `unstated` (never stated). `CoherenceDurationFront` carries `dimension`, `floors` (per-round binding floor, `null` = unstated), the SORTED `latencies` (its recovered episode lengths), `closed_episodes`, `open_episodes`, `total_rounds` (the sum of `latencies`), `mean_duration` (`total_rounds / closed_episodes`, `null` when none recovered), `max_latency` (v28's per-front extreme, CARRIED for contrast), and its `class`. `computeCoherenceDuration(rounds)` DELEGATES the fall-to-recovery pairing to `computeCoherenceLatency` (called UNCHANGED — the JOIN pattern v38 used for v36+v37; the `localeCompare`-pinned front order and silence-skipping scan come for free), splits each front's episodes into the recovered (a finite length) and the open (EXCLUDED from the mean — an unbounded duration v28's gate owns, §3), sums the recovered lengths, classifies each front, and picks the deal's `longest_mean_dimension` by INTEGER CROSS-MULTIPLICATION (`total_rounds × closed_episodes` of the two fronts — never a float compare; earliest label on a tie; among fronts that ever recovered), returning deal-level `fronts[]`, `class_counts`, `total_closed_episodes` (= v28's `recovered_count` BY CONSTRUCTION), `total_open_episodes` (= v28's `open_count` BY CONSTRUCTION), `total_rounds`, `max_mean`/`longest_mean_dimension` (`null` when none recovered), `lingering` (any front averaging ≥2 rounds — the gate verdict), and a namespaced `duration_hash` (SHA-256 over the canonical per-front set — `dimension`, `floors`, sorted `latencies`, `open_episodes`, and class; the derived float `mean_duration`, `max_latency`, `longest_mean_dimension`, `max_mean`, and `lingering` OMITTED, so integer-exact over the inputs — apart from every coherence_hash/…/cadence_hash). `exposureLingers(duration)` = `duration.lingering` — the DURATION-MAGNITUDE gate predicate. **The distinctness:** DISTINCT from v28 (the EXTREME — a front that always recovers but slowly [mean 3, no open fall] trips v40 and CLEARS v28; a front that recovers promptly twice then falls and never returns trips v28 and CLEARS v40; AND the headline RANK-SWAPS — episodes `[1, 6]` own the deal's slowest single recovery [max 6] yet a mean of 3.5, while `[4, 4]` has a SMALLER worst spell [max 4] yet a LARGER mean [4], so v28 names the first and v40 the second — the test suite includes that fixture, cross-checked against `computeCoherenceLatency`'s `slowest_dimension`/`max_latency`) AND from v39 (the RATE — a front that flips every round but recovers each within a round is `oscillating` to v39 but `brief` here; rate and length are independent). The CENTRAL-TENDENCY-vs-EXTREME trap is handled explicitly: the gate reads `total_rounds ≥ 2 × closed_episodes` (mean ≥ 2), NOT `max > 1` — a front with one bad round among quick ones (`[1,1,1,1,5]`, mean 1.8) stays `brief` and CLEARS the gate, while a chronic lingerer (`[4,4,4]`, mean 4) trips it; the test suite includes the `[1,1,1,4]` mean-1.75 fixture proving a 4-round worst spell does NOT make a front lingering. **§3 honesty:** silence contributes no episode; an open fall is counted (`open_episodes`) but never folded into the mean; a never-fell front is `steady`. `buildCoherenceDurationJson` (`schema: vaulytica.posture-duration.v1`) + `renderCoherenceDurationSummary` (the chronic lingerer naming its mean rounds below floor, the class tally, then one line per front that recovered any episode, `lingering` first, with a "still open" note when open_episodes>0). **ZERO new pairing/crossing math AND zero changes to any existing source file**: v40 imports only `computeCoherenceLatency` (v28), the already-PUBLIC `PostureCoherence`/`NegotiationTier` types, and the shared hashing helpers — like v19–v39 it needed nothing newly exported. **The command, `tools/cli/coherence-duration.ts`** (sibling to coherence-cadence.ts): `computeCoherenceDurationArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceDuration` rendered markdown (default) or JSON, returning `{ok:true, output, lingering, ladderNote}`. `runCoherenceDuration(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-lingering-exposure` exits 2 ONLY when a front's recovered exposures average ≥2 rounds. A SEPARATE command, not a flag on v28. Wired into the run.ts dispatcher (`case "coherence-duration"`) + USAGE + header doc + unknown-command error list. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +23 tests (coherence-duration.test.ts ×14: the lingering vs brief front, the central-tendency case [one bad round stays brief — mean 1.75 despite a max of 4], the v28-rank-swap case [mean vs max name different fronts, total_closed_episodes = v28's], the open-episode exclusion [counted not averaged], the open-only front, steady/unstated fronts, longest-mean pick by exact integer ratio [ratio beats label order], tie-break by earliest label, the by-construction totals tie to v28, determinism, <2-rounds throws [delegated], render+JSON, none-recovered render; tools/cli/coherence-duration.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on a lingering front, gate clears when a front always recovers within a round, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,527 passing + 2 skips** (was 3,504), 239 test files (was 237). Version 9.36.0 → 9.37.0. New [`docs/spec-v40.md`](docs/spec-v40.md); BUILD_PROGRESS v40 §; CHANGELOG [9.37.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the duration workflow + the TWENTY-TWO-axis summary callout + v40 spec-table row + commands prose. The one Step (220) is shipped; a direction-resolved magnitude (mean fall depth — not viable, the floor is binary), a `--min-mean K` knob, a median/quantile instead of the mean, a relapse-interval magnitude (the ABOVE-floor mirror — v30's clean-rounds mean, the clean next axis), a standalone duration artifact, an order-inferring walker, and a browser N-round duration synthesis are principled deferrals. Feature frontier: every numbered spec v1–v40 now complete end-to-end; the posture matrix now has TWENTY-TWO axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32), RECOVERY-AFFINITY (v33), OPPOSITION (v34), PRECEDENCE (v35), CONCESSION (v36), RECOVERY-ORDER (v37), WEAK-FRONT (v38), CADENCE (v39), and DURATION (v40, the mean recovered-exposure length — the first FLOAT MAGNITUDE read). The per-front below-floor reads the BINARY floor admits are now complete down to magnitude — a count (v24), an index (v26/v27), a rate (v39), an extreme (v28), and now a central tendency (v40). The remaining genuinely-new directions are the ABOVE-floor mirror (v30's relapse interval has a mean too — the clean-rounds magnitude) or a 2-D read (per-front × per-step matrix export), not another reduction of the same below-floor episodes. The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a MEAN clean-rounds magnitude [the v30 above-floor mirror of v40]; a per-front×per-step 2-D matrix export; a TRANSITIVE lead-lag/recovery chain over the pairs; depth-weighting still NOT viable — the floor is binary).
- **Step 221 — The `coherence-durability` subcommand (shipped 2026-06-24, 9.38.0):** The ABOVE-FLOOR MIRROR of v40's below-floor mean — the MAGNITUDE axis v40 deferred BY NAME ("a relapse-interval magnitude — mean clean rounds between exposures — the above-floor mirror of v40's below-floor mean, a clean next magnitude axis"). v30 (`coherence-relapse`) pairs each RECOVERY above the acceptable floor with the FALL that undoes it, then reduces the clean-interval lengths two ways and BOTH ARE EXTREMES: `min_interval` (the single QUICKEST relapse across the deal) and `immediate` (whether any recovery was undone the very next round — the gate). Neither reads the MEAN. A front that holds five rounds twice and once bounces back the next round owns the deal's quickest single relapse, yet its fixes almost always last; a front that holds one round EVERY time has the same quickest relapse, yet it is the one whose every fix is fragile — the front whose concessions never hold. The `min_interval` extreme cannot tell them apart; the MEAN can, and the mean is the read no existing command supplies. v40 Open Question #1 steered here ("the remaining genuinely-new read is the ABOVE-floor mirror — v30's relapse interval has a mean too — or a 2-D read"). **The report (pure), `src/report/coherence-durability.ts`** (sibling to coherence-duration.ts): `DurabilityClass` = `fragile` (≥1 RELAPSED recovery AND `total_rounds < 2 × closed_intervals` — a mean BELOW two clean rounds, including a mean of exactly one, the gate-worthy class) / `durable` (≥1 relapsed recovery but a mean of at least two rounds) / `held` (recovered in-sequence but EVERY recovery held — an unbounded clean interval, the durable best case, the MIRROR of v40's `open` but semantically INVERTED) / `steady` (stated, never recovered) / `unstated` (never stated). `CoherenceDurabilityFront` carries `dimension`, `floors` (per-round binding floor, `null` = unstated), the SORTED `clean_intervals` (its relapsed interval lengths), `closed_intervals`, `open_intervals`, `total_rounds` (the sum of `clean_intervals`), `mean_durability` (`total_rounds / closed_intervals`, `null` when none relapsed), `min_interval` (v30's per-front extreme, CARRIED for contrast), and its `class`. `computeCoherenceDurability(rounds)` DELEGATES the recovery-to-relapse pairing to `computeCoherenceRelapse` (called UNCHANGED — the JOIN pattern v38 used for v36+v37 and v40 for v28; the `localeCompare`-pinned front order and silence-skipping scan come for free), splits each front's intervals into the relapsed (a finite clean length) and the held (EXCLUDED from the mean — an unbounded interval, the durable best case, §3), sums the relapsed lengths, classifies each front, and picks the deal's `most_fragile_dimension` by INTEGER CROSS-MULTIPLICATION (`total_rounds × closed_intervals` of the two fronts — never a float compare; earliest label on a tie; among fronts that ever relapsed), returning deal-level `fronts[]`, `class_counts`, `total_relapsed_intervals` (= v30's `relapse_count` BY CONSTRUCTION), `total_held_intervals` (= v30's `held_count` BY CONSTRUCTION), `total_rounds`, `min_mean`/`most_fragile_dimension` (`null` when none relapsed), `fragile` (any front averaging <2 rounds — the gate verdict), and a namespaced `durability_hash` (SHA-256 over the canonical per-front set — `dimension`, `floors`, sorted `clean_intervals`, `open_intervals`, and class; the derived float `mean_durability`, `min_interval`, `min_mean`, and `fragile` OMITTED, so integer-exact over the inputs — apart from every coherence_hash/…/duration_hash). `recoveryFragile(durability)` = `durability.fragile` — the DURABILITY-MAGNITUDE gate predicate. **The distinctness:** STRICTLY STRONGER EVIDENCE than v30's `immediate` gate — a fragile mean (`<2`) forces at least one clean interval of one round (an all-integers mean below two cannot avoid a 1), so EVERY `fragile` front also trips v30's `immediate` gate, but the CONVERSE FAILS (a front with intervals `[1, 5]`, mean 3, trips v30 on its single fast relapse yet is `durable` here); v41 fires ONLY on the CHRONICALLY fragile front, suppressing the single-fast-relapse alarm v30 raises — the test suite includes both the strictly-stronger relationship fixture and the `[1,5]`-durable-but-v30-immediate rank-swap, cross-checked against `computeCoherenceRelapse`'s `quickest_dimension`/`min_interval`/`immediate`. The ABOVE-FLOOR MIRROR of v40 (v40 reads how long a front lingers BELOW floor across its recovered exposures; v41 how long a fix holds ABOVE floor across its relapsed recoveries — the two means independent). **§3 honesty:** silence contributes no interval; a held recovery is counted (`open_intervals`) but never folded into the mean; a never-recovered front is `steady`. `buildCoherenceDurabilityJson` (`schema: vaulytica.posture-durability.v1`) + `renderCoherenceDurabilitySummary` (the most fragile recovery naming its mean clean rounds above floor, the class tally, then one line per front that relapsed any recovery, `fragile` first, with a "still holding" note when open_intervals>0). **ZERO new pairing/crossing math AND zero changes to any existing source file**: v41 imports only `computeCoherenceRelapse` (v30), the already-PUBLIC `PostureCoherence`/`NegotiationTier` types, and the shared hashing helpers — like v19–v40 it needed nothing newly exported. **The command, `tools/cli/coherence-durability.ts`** (sibling to coherence-duration.ts): `computeCoherenceDurabilityArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceDurability` rendered markdown (default) or JSON, returning `{ok:true, output, fragile, ladderNote}`. `runCoherenceDurability(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-fragile-recovery` exits 2 ONLY when a front's relapsed recoveries average <2 clean rounds. A SEPARATE command, not a flag on v30. Wired into the run.ts dispatcher (`case "coherence-durability"`) + USAGE + header doc + unknown-command error list. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +24 tests (coherence-durability.test.ts ×15: the fragile vs durable front, the central-tendency case [one fast relapse stays durable — mean 2.33 despite a quickest of 1, v30's `immediate` still firing], the strictly-stronger-than-v30 relationship, the v30-rank-swap case [mean vs min name different fronts], the held-recovery exclusion [counted not averaged], the held-only front, steady/unstated fronts, lowest-mean pick by exact integer ratio [ratio beats label order], tie-break by earliest label, the by-construction totals tie to v30, determinism, <2-rounds throws [delegated], render+JSON, none-relapsed render; tools/cli/coherence-durability.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on a fragile front, gate clears when a fix holds ≥2 rounds, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,551 passing + 2 skips** (was 3,527), 241 test files (was 239). Version 9.37.0 → 9.38.0. New [`docs/spec-v41.md`](docs/spec-v41.md); BUILD_PROGRESS v41 §; CHANGELOG [9.38.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the durability workflow + the TWENTY-THREE-axis summary callout + v41 spec-table row + commands prose + cheat-sheet + command-table row. The one Step (221) is shipped; a direction-resolved magnitude (mean fall depth — not viable, the floor is binary), a `--min-mean K` knob, a median/quantile instead of the mean, a standalone durability artifact, and a browser N-round durability synthesis are principled deferrals. Feature frontier: every numbered spec v1–v41 now complete end-to-end; the posture matrix now has TWENTY-THREE axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32), RECOVERY-AFFINITY (v33), OPPOSITION (v34), PRECEDENCE (v35), CONCESSION (v36), RECOVERY-ORDER (v37), WEAK-FRONT (v38), CADENCE (v39), DURATION (v40), and DURABILITY (v41, the mean relapsed-interval length — the ABOVE-floor mirror of v40, completing both magnitude directions the binary floor admits). The per-front magnitude family is now complete in BOTH directions — below-floor (v40) and above-floor (v41). The remaining genuinely-new directions are a 2-D read (per-front × per-step matrix export) or a TRANSITIVE read (lead-lag/recovery chains over the pairwise family), not another reduction of the same below/above-floor episodes. The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a per-front×per-step 2-D matrix export; a TRANSITIVE lead-lag/recovery chain over the pairs; depth-weighting still NOT viable — the floor is binary).
- **Step 222 — The `coherence-chain` subcommand (shipped 2026-06-24, 9.39.0):** The TRANSITIVE READ v41 deferred BY NAME ("a TRANSITIVE read — lead-lag/recovery chains over the pairwise family") and v41 Open Question #1 steered to ("the remaining genuinely-new read is the 2-D one or a TRANSITIVE one — lead-lag chains over the pairwise family"). v35 (`coherence-precedence`) reads each PAIR of fronts in isolation — does one cross the acceptable floor FIRST for a strict majority of comparisons (`leads`)? It NEVER COMPOSES the pairs, so two facts a deal lead wants stay hidden: the CHAIN (Cap leads Term, Term leads Indemnity ⟹ Cap is a transitive early-warning indicator for Indemnity THROUGH Term — the deal's HEADWATER — even with no direct Cap→Indemnity edge) and the CYCLE (Cap leads Term, Term leads Indemnity, Indemnity leads Cap — three clean pairwise leads that CANNOT BE GLOBALLY RANKED, a Condorcet cycle no pairwise read can detect; every pair on the loop is individually `leading` to v35). **The report (pure), `src/report/coherence-chain.ts`** (sibling to coherence-precedence.ts): `ChainRole` = `cyclic` (transitively leads ITSELF — on a directed cycle, the gate-worthy class) / `source` (not on a cycle, leads ≥1 front AND `led_by` 0 — a HEADWATER, nothing upstream) / `sink` (not on a cycle, led by ≥1 and leads none — a terminal follower) / `relay` (not on a cycle, both leads and is led — an intermediate link) / `isolated` (no edge touches it — reach 0 and led-by 0, incl. an unstated/never-crossing front). `CoherenceChainFront` carries `dimension`, the SORTED `leads_directly` (direct out-neighbours), `reach` (transitive out-reach, excl. self), `led_by` (transitive in-reach, excl. self), `in_cycle` (does it transitively lead itself), and its `class`. `computeCoherenceChain(rounds)` DELEGATES the per-pair lead-lag derivation to `computeCoherencePrecedence` (called UNCHANGED — the JOIN pattern v38 used for v36+v37, v40 for v28, v41 for v30; the `localeCompare`-pinned front order and silence-skipping crossing scan come for free), keeps ONLY its strict-majority `leading` pairs as directed `leader → follower` edges (the same edges v35's own `--fail-on-leading-front` gate fires on — NOT every non-null plurality leader), computes a plain integer Floyd–Warshall REACHABILITY FIXPOINT (paths of length ≥1, so `reach[v][v]` true ⟺ v on a cycle), classifies each front, picks the deal's `headwater` by GREATEST `reach` among SOURCE fronts (earliest label on a tie), returning deal-level `fronts[]`, `class_counts`, `edges` (= v35's `leading` count BY CONSTRUCTION), `max_reach`, `headwater` (`null` when no source), `acyclic`, `cyclic` (any front on a cycle — the gate verdict), and a namespaced `chain_hash` (SHA-256 over the canonical per-front set — `dimension`, sorted `leads_directly`, and class; the derived transitive `reach`/`led_by` integers and deal-level scalars OMITTED, since the edge set fully determines them — integer-/string-exact, apart from every coherence_hash/…/durability_hash). `exposureCyclic(chain)` = `chain.cyclic` — the TRANSITIVE gate predicate (no other command composes the edges to detect a global cycle). **The distinctness:** DISTINCT from v35's `--fail-on-leading-front` (the PRESENCE of a stable pair; v42 flags the INCOHERENCE of the composed global ordering instead — a deal whose every pair is `leading` [v35 fires] can be ACYCLIC [v42 clears — a clean pipeline] or CYCLIC [v42 fires — an intransitive loop]; the two verdicts independent, one over a pair, one over the whole graph). The cycle is STRUCTURALLY UNDETECTABLE per-pair — the test suite proves it with the canonical non-transitive Condorcet triple (crossing positions {1,3,8}/{0,5,7}/{2,4,6}, each pair a 5-of-9 strict majority in rotation: Bbb→Aaa, Aaa→Ccc, Ccc→Bbb), cross-checked against `computeCoherencePrecedence`'s `class_counts.leading`. **§3 honesty:** a front that never crosses the floor, or an `interleaved` pair with no consistent first-mover, contributes NO edge — it is `isolated`; silence is never an edge. **NOT a longest-simple-path read** (NP-hard on a cyclic graph; `reach` is the polynomial integer-exact cascade-breadth read); NOT a depth-weighting (the floor is binary); NOT a plurality-edge read (only strict-majority `leading` edges, the family's gating convention). `buildCoherenceChainJson` (`schema: vaulytica.posture-chain.v1`) + `renderCoherenceChainSummary` (the headwater naming its reach, the ordering verdict [acyclic / intransitive], the role tally, then one line per front that touches the graph — sources first, then relays, cyclic, sinks; an `isolated` front counted but never listed). **ZERO new crossing/ordering math AND zero changes to any existing source file**: v42 imports only `computeCoherencePrecedence` (v35), the already-PUBLIC `PostureCoherence` type, and the shared hashing helpers — like v19–v41 it needed nothing newly exported. **The command, `tools/cli/coherence-chain.ts`** (sibling to coherence-precedence.ts): `computeCoherenceChainArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceChain` rendered markdown (default) or JSON, returning `{ok:true, output, cyclic, ladderNote}`. `runCoherenceChain(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-lead-cycle` exits 2 ONLY when the lead-lag relation contains a directed cycle. A SEPARATE command, not a flag on v35. Wired into the run.ts dispatcher (`case "coherence-chain"`) + USAGE + header doc + unknown-command error list. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +19 tests (coherence-chain.test.ts ×10: the acyclic chain [source/relay/sink, reach and led-by, headwater], the intransitive Condorcet cycle [every front cyclic, the gate firing, no headwater], the edges = v35 `leading` tally invariant, the interleaved-pair no-edge case [both isolated], the never-crosses isolated front, the source-feeding-a-cycle case [a headwater plus a downstream loop], determinism, <2-rounds throws [delegated], render+JSON, intransitive-render; tools/cli/coherence-chain.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on a cycle, gate clears on an acyclic pipeline, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,570 passing + 2 skips** (was 3,551), 243 test files (was 241). Version 9.38.0 → 9.39.0. New [`docs/spec-v42.md`](docs/spec-v42.md); BUILD_PROGRESS v42 §; CHANGELOG [9.39.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the chain workflow + the TWENTY-FOUR-axis summary callout + v42 spec-table row + commands prose + cheat-sheet + command-table row. The one Step (222) is shipped; a longest-chain (cascade DEPTH) read (NP-hard on cycles — `reach`/breadth is the exact read), a plurality-edge graph (noise — strict-majority only), a `--min-reach K` knob, a standalone chain artifact, and a browser N-round chain synthesis are principled deferrals. Feature frontier: every numbered spec v1–v42 now complete end-to-end; the posture matrix now has TWENTY-FOUR axes — MOVEMENT (v16/v17/v18/v19), LEVEL (v20), TIME/duration (v21), BREADTH (v22), RECURRENCE (v23), VOLATILITY (v24), SYNCHRONY (v25), SETTLING (v26), ONSET (v27), LATENCY (v28), CONCURRENCY (v29), RELAPSE (v30), TENURE (v31), AFFINITY (v32), RECOVERY-AFFINITY (v33), OPPOSITION (v34), PRECEDENCE (v35), CONCESSION (v36), RECOVERY-ORDER (v37), WEAK-FRONT (v38), CADENCE (v39), DURATION (v40), DURABILITY (v41), and CHAIN (v42, the transitive closure of v35's pairwise lead-lag relation — the first TRANSITIVE read, composing the pairwise family into a global ordering and detecting its incoherence). The pairwise directional family is now CLOSED — each pair (v35), the direction-resolved orderings (v36/v37), the per-front join (v38), and now the transitive closure (v42). The remaining genuinely-new direction is the 2-D one (per-front × per-step MATRIX export — a different SHAPE of output, not another reduction of the lead-lag edges). The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidates now: a per-front×per-step 2-D matrix export; a TRANSITIVE recovery-chain over v37's recovery-order pairs [the recovery mirror of v42]; depth-weighting still NOT viable — the floor is binary).
- **Step 223 — The `coherence-recovery-chain` subcommand (shipped 2026-06-24, 9.40.0):** The RECOVERY MIRROR of v42 — the TRANSITIVE RECOVERY-CHAIN v42 deferred BY NAME ("a TRANSITIVE recovery-chain over v37's recovery-order pairs [the recovery mirror of v42]") and v42 Open Question #1 left as the one transitive read still unbuilt. v37 (`coherence-recovery-order`) reads each PAIR of fronts in isolation — when both RECOVER (climb back at-or-above the acceptable floor), does one consistently recover FIRST (and so the other LAST, the laggard)? It NEVER COMPOSES the pairs, so two facts a deal lead wants stay hidden: the CHAIN (Cap recovers before Term, Term recovers before Indemnity ⟹ Indemnity is restored AFTER Cap THROUGH Term — the deal's TAILWATER, the front exposed longest of all — even with no direct Cap→Indemnity edge) and the CYCLE (Cap recovers before Term, Term before Indemnity, Indemnity before Cap — three clean pairwise orders that CANNOT BE GLOBALLY RANKED, a Condorcet cycle no pairwise read can detect; every pair on the loop is individually `leading` to v37). **The report (pure), `src/report/coherence-recovery-chain.ts`** (sibling to coherence-recovery-order.ts and coherence-chain.ts): `RecoveryChainRole` = `cyclic` (transitively recovers before ITSELF — on a directed cycle, the gate-worthy class) / `source` (not on a cycle, recovers before ≥1 front AND `recovered_before_by` 0 — restored FIRST of all, nothing upstream) / `sink` (not on a cycle, recovered-before by ≥1 and recovers before none — the TAILWATER candidate, restored LAST) / `relay` (not on a cycle, both recovers before some and is recovered-before by others) / `isolated` (no edge touches it — reach 0 and led-by 0, incl. an unstated/never-recovering front). `CoherenceRecoveryChainFront` carries `dimension`, the SORTED `recovers_before_directly` (direct out-neighbours), `reach` (transitive out-reach, excl. self), `recovered_before_by` (transitive in-reach, excl. self), `in_cycle` (does it transitively recover before itself), and its `class`. `computeCoherenceRecoveryChain(rounds)` DELEGATES the per-pair recovery-order derivation to `computeCoherenceRecoveryOrder` (called UNCHANGED — the JOIN pattern v38/v40/v41/v42 use; the `localeCompare`-pinned front order and silence-skipping recovery scan come for free), keeps ONLY its strict-majority `leading` pairs as directed `first_recoverer → last_recoverer` edges (the same edges v37's own `--fail-on-lagging-recovery` gate fires on — NOT every non-null plurality first-recoverer), computes a plain integer Floyd–Warshall REACHABILITY FIXPOINT (paths of length ≥1, so `reach[v][v]` true ⟺ v on a cycle), classifies each front, picks the deal's `tailwater` by GREATEST `recovered_before_by` among SINK fronts (earliest label on a tie), returning deal-level `fronts[]`, `class_counts`, `edges` (= v37's `leading` count BY CONSTRUCTION), `max_lag`, `tailwater` (`null` when no sink), `acyclic`, `cyclic` (any front on a cycle — the gate verdict), and a namespaced `recovery_chain_hash` (SHA-256 over the canonical per-front set — `dimension`, sorted `recovers_before_directly`, and class; the derived transitive `reach`/`recovered_before_by` integers and deal-level scalars OMITTED, since the edge set fully determines them — integer-/string-exact, apart from every coherence_hash/…/chain_hash). `exposureRecoveryCyclic(chain)` = `chain.cyclic` — the RECOVERY-direction transitive gate predicate (the mirror of v42's `exposureCyclic`). **The distinctness:** DISTINCT from v37's `--fail-on-lagging-recovery` (the PRESENCE of a stable laggard; v43 flags the INCOHERENCE of the composed restoration order instead). DISTINCT from v42 (the lead chain composes v35's direction-blind CROSSING edges and names the HEADWATER — the front to watch FIRST; v43 composes v37's RECOVERY-only edges and names the TAILWATER — the front exposed LONGEST; the two transitive reads the two directions of a floor crossing admit). The cycle is STRUCTURALLY UNDETECTABLE per-pair — the test suite proves it with a recovery Condorcet triple (recovery positions {2,4,9}/{1,6,8}/{3,5,7}, each pair a 5-of-9 strict majority in rotation: Bbb→Aaa, Ccc→Bbb, Aaa→Ccc), cross-checked against `computeCoherenceRecoveryOrder`'s `class_counts.leading`. **§3 honesty:** a front that never recovers, or an `interleaved` pair with no consistent first-recoverer, contributes NO edge — it is `isolated`; silence is never an edge. **NOT a longest-simple-path read** (NP-hard on a cyclic graph; `recovered_before_by` is the polynomial integer-exact cascade-breadth read); NOT a depth-weighting (the floor is binary); NOT a plurality-edge read (only strict-majority `leading` edges). `buildCoherenceRecoveryChainJson` (`schema: vaulytica.posture-recovery-chain.v1`) + `renderCoherenceRecoveryChainSummary` (the tailwater naming its in-reach, the ordering verdict [acyclic / intransitive], the role tally, then one line per front that touches the graph — sinks first, then relays, cyclic, sources; an `isolated` front counted but never listed). **ZERO new crossing/ordering math AND zero changes to any existing source file**: v43 imports only `computeCoherenceRecoveryOrder` (v37), the already-PUBLIC `PostureCoherence` type, and the shared hashing helpers — like v19–v42 it needed nothing newly exported. **The command, `tools/cli/coherence-recovery-chain.ts`** (sibling to coherence-chain.ts): `computeCoherenceRecoveryChainArtifacts(texts, format?)` is the PURE core — `verifyCoherenceSequence` (shared parse + hash-verify + cross-ladder guard, UNCHANGED) then `computeCoherenceRecoveryChain` rendered markdown (default) or JSON, returning `{ok:true, output, cyclic, ladderNote}`. `runCoherenceRecoveryChain(argv)` is the handler (file IO + exit codes); requires ≥2 positionals; under `--fail-on-recovery-cycle` exits 2 ONLY when the recovery-order relation contains a directed cycle. A SEPARATE command, not a flag on v37. Wired into the run.ts dispatcher (`case "coherence-recovery-chain"`) + USAGE + header doc + unknown-command error list. **ADDITIVE:** a brand-new subcommand + one pure module — every existing command's output and every golden byte-for-byte unchanged; no existing source file's behavior changes; no new on-disk format (the report stays DERIVED). +19 tests (coherence-recovery-chain.test.ts ×10: the acyclic chain [source/relay/sink, reach and in-reach, tailwater], the intransitive recovery Condorcet cycle [every front cyclic, the gate firing, no tailwater], the edges = v37 `leading` tally invariant, the interleaved-pair no-edge case [both isolated], the never-recovers isolated front, the sink-fed-by-a-cycle case [a tailwater plus an upstream loop], determinism, <2-rounds throws [delegated], render+JSON, intransitive-render; tools/cli/coherence-recovery-chain.test.ts ×9: disk-vs-in-memory report identity, markdown summary, gate trips on a cycle, gate clears on an acyclic restoration order, ≥2-artifact requirement, cross-ladder refusal naming both rounds, unpinned-v1 note, round-prefixed tamper, gate-predicate parity). `npm run typecheck && lint && test && build` green; suite **3,589 passing + 2 skips** (was 3,570), 245 test files (was 243). Version 9.39.0 → 9.40.0. New [`docs/spec-v43.md`](docs/spec-v43.md); BUILD_PROGRESS v43 §; CHANGELOG [9.40.0]; README badge (tests + version) + "Saved coherence baselines" § extended with the recovery-chain workflow + the TWENTY-FIVE-axis summary callout + v43 spec-table row + commands prose + cheat-sheet + command-table row. The one Step (223) is shipped; a longest-chain (cascade DEPTH) read (NP-hard on cycles — in-reach/breadth is the exact read), a plurality-edge graph, a `--min-lag K` knob, a standalone recovery-chain artifact, and a browser N-round synthesis are principled deferrals. Feature frontier: every numbered spec v1–v43 now complete end-to-end; the posture matrix now has TWENTY-FIVE axes — …PRECEDENCE (v35), CONCESSION (v36), RECOVERY-ORDER (v37), WEAK-FRONT (v38), CADENCE (v39), DURATION (v40), DURABILITY (v41), CHAIN (v42, the transitive lead-lag closure), and RECOVERY-CHAIN (v43, the transitive recovery-order closure — the recovery mirror of v42, naming the deal's TAILWATER). The TRANSITIVE family is now CLOSED in BOTH directions — the lead chain (v42, who moves first) and the recovery chain (v43, who is restored last). The remaining genuinely-new direction is the 2-D one (per-front × per-step MATRIX export — a different SHAPE of output, not another reduction of the pairwise edges). The open frontier is the human-gated v5 corpus/attorney work or a net-new orthogonal coherence-* reduction (candidate now: a per-front×per-step 2-D matrix export; depth-weighting still NOT viable — the floor is binary).
