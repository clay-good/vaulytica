/**
 * Vaulytica — top-level barrel.
 *
 * The full pipeline (ingest → extract → engine → report) is wired together
 * in `src/ui/main.ts` (build step 12). This file is intentionally empty for
 * now; it exists to give the bundler a deterministic entry point if needed.
 */
export {};
