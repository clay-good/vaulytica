# Tasks

- [ ] 1. Rewrite the FTC Part 910 references: `PERS-001.ts` explanation, `v4/employment/_helpers.ts:69` citation factory, and the `stat-16-cfr-910` DKB node (status: vacated by *Ryan LLC v. FTC*, No. 3:24-cv-00986 (N.D. Tex. Aug. 20, 2024); never took effect; appeals dismissed September 2025 with the FTC acceding to vacatur; current federal posture is case-by-case FTC Act § 5 enforcement and warning letters; cite state-law anchors instead). Verify no other rule references Part 910 as operative.
- [ ] 2. `FIN-009`: (a) unstated period → do not annualize; emit an info-level "rate period unspecified — clarify" finding instead; (b) detect one-time late-fee phrasing ("late fee of N% of the overdue amount/invoice") and exclude it from the usury comparison — the info finding may note that recurring or compounding late fees risk recharacterization as interest, but never asserts usury; (c) add flat-fee, unstated-period, and per-annum regression tests.
- [ ] 3. Grep all numeric-threshold rules for implicit unit assumptions; list and fix any siblings (same pattern, same tests).
- [ ] 4. Refresh the non-compete state overlay for statutes enacted and effective in 2025: Florida CHOICE Act (eff. July 2025), Wyoming SF 107 (eff. 2025-07-01), Virginia SB 1218 (eff. 2025-07-01, ban extended to all overtime-eligible employees), Arkansas healthcare-worker ban (eff. 2025-08-03); each overlay node carries its `retrieved_at`.
- [ ] 5. Add `retrieved_at` to every DKB statute node; render "verify currency (retrieved <date>)" on findings whose cited node exceeds the horizon (default 12 months); config knob in the DKB manifest.
- [ ] 6. Extend the staleness gate: CI fails when any statute node exceeds the horizon without an entry in `dkb-staleness-ack.yml`. Note: `fix-dkb-build-integrity` adds shrinkage acks to the same file — entries carry a `kind` discriminator (`staleness` vs `shrinkage`) so the two schemas coexist; whichever change lands second adopts the discriminator.
- [ ] 7. Update README and `site/index.html`: state-overlay claim names the three topics; citation claim scoped ("findings that assert law cite pinned sources; drafting-practice findings say so").
- [ ] 8. Re-baseline affected goldens; full gate green.
