# Tasks

- [x] 1. Rewrite the FTC Part 910 references: `PERS-001.ts` explanation, `v4/employment/_helpers.ts:69` citation factory, and the `stat-16-cfr-910` DKB node (status: vacated by *Ryan LLC v. FTC*, No. 3:24-cv-00986 (N.D. Tex. Aug. 20, 2024); never took effect; appeals dismissed September 2025 with the FTC acceding to vacatur; current federal posture is case-by-case FTC Act § 5 enforcement and warning letters; cite state-law anchors instead). Verify no other rule references Part 910 as operative. *(Also found and rewrote the M&A-side `ftcNcr()` factory plus MNA-016/MNA-072 texts — the "sale-of-business exception § 910.2(a)(2)" framing rested on the never-effective rule; the operative anchor is now state-law goodwill doctrine (e.g., Cal. B&P § 16601). EMP-024 renamed off the FTC hook. Repo-wide grep confirms no rule describes Part 910 as operative; the DKB node's canonical URL moved from the "current" eCFR to the stable Federal Register final-rule document.)*
- [x] 2. `FIN-009`: (a) unstated period → do not annualize; emit an info-level "rate period unspecified — clarify" finding instead; (b) detect one-time late-fee phrasing ("late fee of N% of the overdue amount/invoice") and exclude it from the usury comparison — the info finding may note that recurring or compounding late fees risk recharacterization as interest, but never asserts usury; (c) add flat-fee, unstated-period, and per-annum regression tests. *(Rule version bumped 1.0.0 → 1.1.0.)*
- [x] 3. Grep all numeric-threshold rules for implicit unit assumptions; list and fix any siblings (same pattern, same tests). *(Sweep of `* 12`/`* 365`/period keywords found no true siblings — TEMP-003's `toDays`, FIN-008, and PERS-007 all require explicitly stated units; TEMP-003's separate detection issues belong to `fix-rule-detection-fidelity`.)*
- [x] 4. Refresh the non-compete state overlay for statutes enacted and effective in 2025: Florida CHOICE Act (eff. July 2025), Wyoming SF 107 (eff. 2025-07-01), Virginia SB 1218 (eff. 2025-07-01, ban extended to all overtime-eligible employees), Arkansas healthcare-worker ban (eff. 2025-08-03); each overlay node carries its `retrieved_at`. *(All four verified against primary sources 2026-07-04 and carry that per-entry `retrieved_at`: FL §§ 542.41–.45 (covered high earners, ≤4-year covenants/garden leave); WY Wyo. Stat. § 1-23-108 (void with sale-of-business/trade-secret/expense/executive carve-outs; physician covenants void); VA § 40.1-28.7:8 (low-wage now includes every FLSA overtime-eligible employee); AR Act 232 of 2025 (SB 139) — physicians specifically, effective August 2025 (primary sources say Aug. 5, not Aug. 3, and the ban covers physicians rather than all healthcare workers — text follows the sources). Overlay catalog is now 37 entries; docx/overlay tests that used Wyoming as the canonical uncovered state moved to Montana.)*
- [x] 5. Add `retrieved_at` to every DKB statute node; render "verify currency (retrieved <date>)" on findings whose cited node exceeds the horizon (default 12 months); config knob in the DKB manifest. *(`retrieved_at` was already schema-required on all 30 nodes. New `currency_horizon_months` manifest knob; `currencyLabel`/`dkbCurrency` in `src/report/citations.ts`. Design note: the rendered label anchors to the DKB's own `built_at`, never the wall clock — a wall-clock label would break the byte-identical-exports/no-wall-clock gates; real-time aging is enforced by the CI gate instead. Wired into HTML (inline + bibliography), DOCX + bundle bibliographies, SARIF (`verify_currency` citation property), fix-list md/csv, and the JSON report (`citation_currency_notes`, outside `run`).)*
- [x] 6. Extend the staleness gate: CI fails when any statute node exceeds the horizon without an entry in `dkb-staleness-ack.yml`. *(`tests/integration/dkb-currency-gate.test.ts` — deliberately wall-clock, sweeping the LATEST artifact's statute nodes. Deviation from the note below: `fix-dkb-build-integrity` landed shrinkage acks in a separate `dkb-shrinkage-ack.yml`, so no `kind` discriminator was needed; currency acks live under a new `currency_acknowledgments` key in `dkb-staleness-ack.yml`, coexisting with the v3 hash-drift list.)*
- [x] 7. Update README and `site/index.html`: state-overlay claim names the three topics; citation claim scoped ("findings that assert law cite pinned sources; drafting-practice findings say so"). *(README badge now "37 state-law overlays (non-compete · security deposit · usury)"; the "pinned public source" sentence and the site's three "Every finding cites…" spots (feature list, FAQ, checks section) distinguish legal assertions from drafting-practice findings.)*
- [x] 8. Re-baseline affected goldens; full gate green. *(DKB rebuilt as `v2026-07-04-r2-local` — same-day content change ships under a new version because the browser caches by version and the earlier artifact had already deployed; `--version` flag added to the build CLI. All golden suites + scoreboard regenerated; typecheck, lint, format:check, coverage — 3,669 tests / 259 files — build green.)*
