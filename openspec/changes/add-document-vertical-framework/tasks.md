# Tasks

- [ ] 1. Fallback banner: when the matched playbook is `generic-fallback`, stamp a `classification_notice` block into the `EngineRun` (inside the hashed run) stating no document family matched and contract-lint rules were applied; render it prominently in every export (DOCX, HTML, JSON, SARIF note, markdown summary) and in the tab.
- [ ] 2. Guard test: every rule outside the v1/v2 launch set (`LAUNCH_RULES`) declares a non-empty `applies_to_playbooks` OR a registered opt-in assertion gate (assertion names live in the vertical registry); fails naming offenders and unregistered assertions. Verify the current v3/v4 packs already pass (they do per audit — confirm in-test, don't assume).
- [ ] 3. Property test (hash isolation): for each launch golden fixture, register a synthetic vertical pack (fake playbook id + one rule gated to it) and assert the fixture's `result_hash` is byte-identical with and without the pack registered; repeat with a synthetic assertion-gated rule and no assertion made.
- [ ] 4. `docs/verticals.md`: pack contract (classifier family + profiles + gated rule pack + namespace + scope-of-review statement), assertion-gate registry (deadline profiles, `--estate-checks`), namespace reservation table (FILE, CITE, DDL, PROD, PNOT new; EST already owned by v4 trust-estate), honesty posture inheritance (presence-only doctrine from `src/delivery/types.ts:15-16`; tier badges only via the attorney ledger).
- [ ] 5. Scope-of-review rendering: a reusable report block fed by the active pack's declared scope statement (reuses the pattern of the closing-checklist and critical-dates disclaimers in `src/report/exports.ts`), wired for all packs including existing DPA/BAA.
- [ ] 6. Re-baseline generic-fallback goldens; full gate: `npm run typecheck && npm run lint && npm test && npm run build`.
