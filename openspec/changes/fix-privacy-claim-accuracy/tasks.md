# Tasks

- [x] 1. Measure current behavior: run a DOCX and a PDF analysis under Playwright request interception; record every request (URL, method, origin, body presence). Confirm or refute the pdf.js CDN question (worker script, standard fonts, cmaps). *(Measured: DOCX holds the invariant (same-origin GETs, no bodies). The pdf.js question resolved WORSE than the CDN hypothesis — with no `workerSrc`, real-browser PDF analysis didn't fetch from a CDN, it **failed outright** ('No "GlobalWorkerOptions.workerSrc" specified') before parsing a byte. Every real-browser PDF drop errored; the happy-dom suite masked it via Node's fake-worker path. No font/cmap fetches observed.)*
- [x] 2. Pin pdf.js assets: set `GlobalWorkerOptions.workerSrc` (and font/cmap paths if step 1 shows them fetched) to same-origin bundled assets; verify a PDF still ingests byte-identically (existing goldens). *(Worker pinned to `/pdf-worker/pdf.worker.min.mjs` — dev middleware mount + dist copy of the exact matching pdfjs-dist build; Node/vitest keep the fake worker so CLI output and goldens are untouched. This both restores browser PDF analysis and guarantees it never resolves an asset off-origin.)*
- [x] 2b. Pin OCR assets: `src/ingest/ocr.ts` calls `tesseract.createWorker` with no `workerPath`/`corePath`/`langPath` — library defaults fetch worker, wasm core, and `eng.traineddata` from CDNs, which the shipped `connect-src 'self'` CSP blocks. Bundle the assets same-origin, or explicitly disable OCR with an honest "scanned PDF not supported" message; add a no-text-layer PDF fixture to the interception e2e either way. *(Chose the honest disable: shipped OCR could never run under the CSP anyway, so the browser pipeline now passes `allowOcr: false` and the ingest emits a plain-language scanned-PDF warning instead of dying mid-analysis on a blocked CDN fetch; `ocr.ts` remains for explicit opt-in/future vendoring. The interception e2e includes the no-text-layer fixture.)*
- [x] 3. Rewrite the copy: `site/index.html:1792` ("zero network calls during analysis") and `:2252`, README.md:23 and :830 — claim document-content non-transmission plus same-origin-static-assets-only, and describe what DevTools will actually show. *(Also the FAQ JSON-LD "confirm zero requests during analysis", the privacy section, and the 30-second verify walkthrough — all now describe the observable truth: same-origin GETs of the app's own assets, no request bodies, no document content.)*
- [x] 4. Add the e2e gate: full analyze flow for DOCX and PDF with route interception asserting zero cross-origin requests, GET-only same-origin static assets, and no request bodies; wire into the existing e2e suite. *(`tests/e2e/privacy-interception.spec.ts` — DOCX, text-layer PDF, and scanned-style PDF; runs with the suite deploy.yml executes post-deploy.)*
- [x] 5. Sweep for other absolute privacy claims (docs/, DISCLAIMER.md, site meta tags) and align them with the same wording. *(DISCLAIMER.md and meta tags carry no absolute network claims; `docs/threat-model.md`'s "zero outbound bodies" instruction was already exactly right, and its §container-scan "zero network calls" describes that module accurately. Historical `docs/spec-v*.md` files are versioned specs, left as records.)*
- [x] 6. Full gate: `npm run typecheck && npm run lint && npm test && npm run build` plus the e2e suite. *(All green — 3,669 tests; privacy interception 3/3 + smoke e2e pass against the built site.)*
