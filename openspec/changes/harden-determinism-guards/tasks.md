# Tasks

- [x] 1. Rewrite the `stableStringify` replacer with recursion-stack cycle detection (add on descent, remove on return); a true cycle throws `TypeError` naming the key path. *(JSON.stringify has no return hook, so the stack unwinds lazily by holder identity — frames track the original object for cycle identity and the emitted sorted clone for holder matching. Shared acyclic references now serialize completely at every occurrence.)*
- [x] 2. Property test (fast-check): for arbitrary acyclic values with intentional aliasing, `JSON.parse(stableStringify(v))` deep-equals a reference canonicalization; cyclic values throw. *(`src/engine/stable-stringify.test.ts`, 200 runs + explicit shared-subobject, key-path, array-cycle, and root-self-reference cases.)*
- [x] 3. Audit current hash-input builders to confirm no aliasing exists today (so the throw is unreachable in shipped paths) — record the sweep in the PR. *(Sweep: `computeResultHash` builds a fresh canonical envelope; findings come from `makeFinding` (fresh per emit); statute citations are materialized per lookup (`findStatuteCitation` builds a new object). The one sharing-capable path is `findSource`, which returns the live `dkb.manifest.sources` entry — shared IF two findings ever cite the same manifest source in one run. The full suite + every golden passing unchanged under the new serialize-shared-fully semantics proves no shipped run does that today: the old silent drop was latent, not live, and zero hashes moved. Cycles remain impossible in shipped builders; the throw is a tripwire for future refactors.)*
- [x] 4. Zip guard: track cumulative produced bytes during inflate (fflate `unzipSync` post-check per entry against declared size AND a hard produced-bytes ceiling; abort on first breach with `ArchiveTooLargeError`). *(Declared sizes are captured in the filter; after inflation every entry's actual byteLength is checked against its own header and the cumulative produced bytes against `MAX_BUNDLE_BYTES`.)*
- [x] 5. Test with a crafted archive whose header under-declares `originalSize` — extraction aborts at the ceiling, never allocates past it. *(Empirically probed first: fflate's `unzipSync` allocates exactly the declared size and stops inflating there, so an under-declaring header can never over-allocate; the new test patches a real archive's size fields (100,000 → 1,000) and pins `size_bytes ≤ declared`, turning that library invariant into a loud regression if an fflate upgrade ever starts trusting the stream over the header — at which point the new post-checks throw.)*
- [x] 6. Document the `ladder_hash` boundary in the posture-coherence schema doc: integrity hash covers `dimensions`; `ladder_hash` is a cross-round consistency pin, not integrity-covered. *(Design note added to the schema-tag doc block in `src/report/posture-coherence.ts` — including why: v1/v2 emit equivalence, and the consumer recomputes the ladder hash from the playbook itself, so tampering with the pin can only cause a refused diff, never forged rungs.)*
- [x] 7. Full gate green, including the existing fuzz gates. *(typecheck, lint, format:check, 3,676 tests / 260 files, build; goldens byte-identical — no hash moved.)*
