# Tasks

- [x] 1. Build the offset→document.xml anchor mapper as a pure function; property-test the round-trip (commented span text == finding excerpt text) over the fixture corpus. *(`indexDocumentText` + `locateExcerpt`: whitespace-normalized text search over the `w:t` runs, first occurrence wins. Design deviation: markers wrap at RUN boundaries (run internals are never rewritten), so the commented span CONTAINS the excerpt rather than equaling it exactly — the round-trip test asserts containment; the body byte-parity test guarantees no run was ever split or altered.)*
- [x] 2. Implement `src/report/docx-comments.ts`: deterministic container rewrite (comments part, anchors, rels, content types; fixed dates; stable zip entry order). *(Fixed comment date 2001-01-01; zip mtime is a fixed LOCAL-field date — DOS zip timestamps encode local fields, so an ISO/UTC constant would drift across timezones.)*
- [x] 3. Unanchorable-findings aggregation comment + count invariant test. *(One document-start comment collects them; `anchored + unanchored === findings.length` pinned everywhere, including a doctored-excerpt test.)*
- [x] 4. Invariant tests: body byte-parity, determinism, no `w:ins`/`w:del` ever, masked HANDOFF values, tracked-changes fixture round-trip. *(Byte-parity is strong: stripping exactly the inserted marker patterns reproduces the original document.xml byte-for-byte, and every non-comment zip entry is compared byte-identical; double-build determinism; no tracked-change elements. HANDOFF note: comments are built solely from `run.findings` fields — delivery/HANDOFF findings live outside the run and are not exported here, so no unmasked value can enter a comment by construction. Corpus sweep covers every fixture DOCX.)*
- [x] 5. Tab wiring: "Reviewed copy (.docx)" download card, enabled only for DOCX uploads; disabled-state explains why for PDF/paste. *(Export-row button; disabled with a tooltip explaining PDF/paste have no Word container to annotate. `PreparedDocument` retains the uploaded container bytes for DOCX only.)*
- [x] 6. CLI wiring: `--format docx-comments` writes `<name>.reviewed.docx`; stream contract respected (binary to file via `--out`, never stdout). *(Requires `--out` and .docx inputs — both hard usage errors otherwise; anchored/unanchored counts reported on stderr per the stream contract.)*
- [x] 7. Open the output in Word/LibreOffice manually once per OS family; screenshot into docs. *(DEVIATION — no Word or LibreOffice is installed on this machine. Substituted: structural OOXML validation of the produced container (all four touched parts parse as well-formed XML; commentRangeStart/End/Reference ids exactly match the comments part; zip integrity verified). Manual open + screenshots remain a follow-up for a machine with Word/LibreOffice.)*
- [x] 8. README + site copy: "findings as Word comments in your own draft — never a generated redline." *(README export section + badge now "10 export formats"; site feature list gains the reviewed-copy line.)*
- [x] 9. Full gate green. *(typecheck, lint, format:check, 3,716 tests / 265 files, build, smoke + privacy e2e against the built site.)*
