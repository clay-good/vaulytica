# Tasks

- [x] 1. Draft the certificate template text (tool scope only; disclaimer block; Op. 512-aligned "attorney remains responsible" sentence); legal-review pass. *(Five fixed statements in `certificateStatements` following the spec's wording constraints exactly — tool-scope-only, privacy-approved network claim, Op. 512 responsibility sentence, not-legal-advice line; tests pin each constraint. The independent attorney legal-review pass on the template text remains a pre-ship follow-up for a human reviewer.)*
- [x] 2. Implement `src/report/certificate.ts`: pure builder over an `EngineRun` (+ optional delivery block) → certificate model; DOCX/PDF/JSON renderers; namespaced `certificate_hash` over the canonical model. *(DOCX + JSON renderers; PDF deviation: the repo has no PDF generator — Word's print-to-PDF covers the PDF copy, as the site copy says. Delivery block omitted: `run`-scope only keeps the certificate a pure function of the hashed run.)*
- [x] 3. Determinism + masking tests (no wall clock; certificate never embeds document text beyond the file name). *(Plus tamper-evidence: editing any field fails `verifyCertificateHash`.)*
- [x] 4. Tab wiring: certificate download alongside the report; CLI `--certificate` flag writing `<name>.certificate.{docx,json}`. *(Two export-row buttons (Word/JSON); the CLI flag requires `--out` — binary output never streams.)*
- [x] 5. `verify` accepts the certificate JSON as an alternative provenance source (same fields as the report block). *(Schema-tag detection; the certificate's own hash is verified BEFORE use — a doctored certificate is a hard error, confirmed live: forged result_hash → "certificate_hash mismatch", exit 1; genuine certificate → "✓ Reproduced".)*
- [x] 6. Site section "For court and ethics compliance" with the standing-order/Op. 512 context, carefully scoped claims, and the reproduce-it-yourself command. Order counts must be attributed to their tracker — never an unattributed "300+ standing orders". Privilege case law is described as diverging (*Heppner* vs. *Warner v. Gilbarco*), not settled. *(Both counts tracker-attributed; both cases named as a split; 22 NYCRR Part 161 noted.)*
- [x] 7. Full gate green. *(typecheck, lint, format:check, 3,721 tests / 266 files, build, smoke + landing a11y e2e.)*
