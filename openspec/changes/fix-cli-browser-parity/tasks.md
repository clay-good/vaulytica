# Tasks

- [x] 1. Extract `pickLatestDkb` from `vite.config.ts` into a shared module; keep the vite build importing it (verify: build output unchanged). *(`tools/dkb/resolve.ts`; build output verified unchanged.)*
- [x] 2. Add `resolveDkbDir` (explicit → report-pinned → latest) with unit tests for all three arms and the missing-manifest hard error. *(`tools/dkb/resolve.test.ts`, plus `loadDkbDirSync` validating through the browser's Zod schemas.)*
- [x] 3. Point `loadAccuracyDeps` at the resolved latest DKB, parsed through the browser's schema validators; remove the `_test-fixtures` import from `tools/accuracy/pipeline.ts`. *(Deps now cache per resolved DKB directory so pinned re-verification and latest runs coexist.)*
- [x] 4. Add a guard test asserting no file under `tools/cli/` or `tools/accuracy/` imports `src/engine/_test-fixtures` (mirror the existing accuracy-corpus-guard pattern). *(`tests/integration/cli-fixture-guard.test.ts`.)*
- [x] 5. Fix `size_bytes` in `runIngested` to the ingested input's byte length; align the browser paste path to UTF-8 byte length; shared unit test pinning both surfaces per input kind. *(`runIngested` now requires the byte length; the browser has no paste entry point — text inputs exist only headless, and `runDocument`/`analyzeText` stamp `Buffer.byteLength(text)`, the byte length the browser stamps for the same bytes. The golden helpers' `sections.length` stamping was aligned too; 25 goldens re-baselined.)*
- [x] 6. Add `--dkb <path>` to `analyze`, `compare`, `diff`, and `verify`; document in USAGE. *(Deviation: `diff` compares two playbook JSON files and never loads a DKB, so a `--dkb` flag there would be a dishonest no-op — added to `analyze`, `compare`, and `verify` only.)*
- [x] 7. Teach `verify` to resolve the saved report's `dkb_version` from `dkb/dist/` before falling back to latest; divergence kind `dkb` fires only when the pinned version is absent. *(Both scenarios pinned in `tools/cli/verify.test.ts`.)*
- [x] 8. Write `tests/integration/cross-surface-parity.test.ts` (browser pipeline vs CLI on the same DOCX + same DKB → identical `EngineRun` incl. `result_hash`). Confirm it fails before the fix and passes after. *(Runs the real `prepareDocument`+`runReport` with fetches served from the shipped artifact bytes vs the real `analyzeFile`; full-run identity with `executed_at`/`elapsed_ms` — the two hash-excluded timing fields — normalized. A companion test proves the sensitivity: the same document under the starter DKB produces a different `result_hash`, i.e. this test catches the shipped defect.)*
- [x] 9. End-to-end receipt check: analyze a fixture in the dev-server browser build, save the JSON, run `vaulytica verify` on it — reproduced. *(Exercised through the real CLI: `analyze` (DKB v2026-07-04-local, `size_bytes` = exact file size 9,667) → `verify` → "✓ Reproduced" exit 0; a starter-pinned report also reproduces via `dkb_version` pinning. Browser-run identity is pinned by the cross-surface test.)*
- [x] 10. Re-baseline CLI goldens; update README's parity wording to describe the enforced mechanism. *(25 goldens re-baselined for the byte-length basis — goldens deliberately stay pinned to the starter DKB via test scaffolding so DKB releases don't churn them; the cross-surface + latest-integrity tests cover artifact drift. Scoreboard regenerated, now stamped `v2026-07-04-local`. README §diagram wording updated. Note: the npm-tarball `files` allow-list ships only the starter DKB, so a published `npx vaulytica` would resolve starter-as-latest — publishing is disabled (`private: true`); revisit if the package ever publishes.)*
- [x] 11. Full gate green. *(typecheck, lint, format:check, coverage — 3,639 tests / 254 files — build, plus live CLI receipt checks.)*
